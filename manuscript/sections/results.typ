// Results — placeholder subsections mirroring the claim table (D-05/D-06). Each subsection
// corresponds to a claim row and names the figure/experiment that substantiates it.
// Intent: reserve one slot per headline claim so experiments are shaped by the claims they
// must support; keep honesty caveats (speedup caveat, "calibrated under the simulator",
// summary-orthogonal OOD blind spot) as explicit reserved slots.
// TODO(phase 5): SBC coverage proof, OOD flag, 2-way Bayes factor results.
// TODO(phase 8): non-circular external validation on the physical corpus.
// TODO(phase 9): cross-method "knows when the classics are wrong" comparison.
// TODO(phase 11): registration-uncertainty widening results.
// TODO(phase 12): spatial per-region colocalization map.
// TODO(phase 13): three-hypothesis Bayes factor.
// TODO(phase 14): decision layer.
// TODO(phase 15): operating envelope.

// Compile-safe stub — the top-level `= Results` heading is emitted by main.typ.
// Subsection slots (mirror claim rows; author in later phases; no citations yet):
//   == Amortized ms-scale inference and >100x speedup (caveated)
//   == SBC calibration under the simulator
//   == Honest OOD / misspecification flag (summary-orthogonal blind spot named)
//   == Amortized Bayes factor (2-way -> 3-way)
//   == Registration uncertainty as latent
//   == Spatial per-region colocalization map
//   == Cross-method comparison and external validation
_Draft slot._ Results subsections are reserved above, one per claim row, and filled by
Phases 5, 8, 9, and 11–15.

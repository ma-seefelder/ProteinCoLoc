// Data and Code Availability — the ProteinCoLoc package, trained NPE/NRE models, the
// forward simulator, and the reproducibility artifacts (fixed-seed demo, cached training
// pairs), with a Zenodo archive placeholder assigned on publication.
// Intent: reserve the availability statement slot.
// TODO(phase 16): author the final Data/Code Availability statement + Zenodo DOI. No citations yet.

// Compile-safe stub — the top-level `= Data and Code Availability` heading is emitted by main.typ.
_Draft slot._ ProteinCoLoc is open-source Julia; the trained models, simulator, and a
fixed-seed reproducibility package will be archived on Zenodo (DOI assigned on publication).

// Discussion — interpret the results, state limitations honestly (calibration is under the
// simulator; the fixed patch-correlation summary bounds OOD detection so summary-orthogonal
// misspecifications are undetectable), and position against related work.
// Intent: reserve the honesty-posture slots so later phases cannot quietly drop them.
// TODO(phase 16): author the final Discussion prose. Compile-safe stub — no citations yet.

// Compile-safe stub — the top-level `= Discussion` heading is emitted by main.typ.
_Draft slot._ Limitations to foreground: calibration proven under the simulator, and a
summary-orthogonal OOD blind spot named rather than hidden.

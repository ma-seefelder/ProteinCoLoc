// Methods — forward simulator, patch-correlation summary statistic, NPE/NRE training,
// SBC calibration protocol, and OOD / misspecification flag.
// Intent: reserve modular subsection slots (one per methodological component) so later
// phases append without merge collisions.
// TODO(phase 5): SBC calibration protocol + OOD flag methods.
// TODO(phase 7): productionized forward simulator, summary statistic, NPE/NRE training methods.

// Compile-safe stub — the top-level `= Methods` heading + #set-tier("M") are emitted by main.typ.
// Subsection slots (author in later phases; no citations yet):
//   == Forward simulator
//   == Patch-correlation summary statistic
//   == Neural posterior / ratio estimation (NPE / NRE)
//   == Simulation-based calibration (SBC)
//   == Out-of-distribution / misspecification flag
_Draft slot._ Methods subsections are reserved above and filled by Phases 5 and 7.

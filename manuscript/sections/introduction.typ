// Introduction — motivate amortized, calibrated colocalization inference and the
// v2.0 thesis (train once, infer new datasets in a single ms forward pass).
// Intent: frame the problem (per-dataset Bayesian coloc is slow; no calibration proof;
// no misspecification flag) and preview the four differentiators.
// TODO(phase 16): author the final Introduction prose. Compile-safe stub — no citations yet.

= Introduction

// Placeholder body (no citations until Phase 16 wires refs). Kept as plain prose so
// the section renders in the skeleton without emitting a dangling citekey/label.
_Draft slot._ Amortized simulation-based inference promises calibrated colocalization
posteriors and Bayes factors in milliseconds per dataset; this manuscript establishes the
method, its calibration proof, and its honest operating envelope.

// Related Work — comparison matrix + prose positioning against Tapqir / Costes /
// Manders / Pearson-Spearman / ProteinCoLoc v1 (ADVI), across the seven capability axes
// (amortization, calibrated UQ/SBC, BF/model comparison, registration-UQ, spatial map,
// OOD flag, speed). Includes the mandatory four-axis Tapqir differentiation paragraph.
// Intent: draft the positioning early so downstream experiment phases are shaped by the
// claims they must support; frame honestly (calibration "under the simulator", OOD blind spot).
// TODO(phase 10 plan 03): author the comparison matrix + Tapqir four-axis prose; verify the
// three flagged (⚠) Tapqir cells against Ordabayev et al. 2022 before locking. No citations yet.

// Compile-safe stub — the top-level `= Related Work` heading is emitted by main.typ.
// Placeholder body kept citation-free so the skeleton stays green.
_Draft slot._ ProteinCoLoc v2.0 differs from prior colocalization tools along amortization,
formal SBC calibration, registration uncertainty as an inferred latent, and a spatial
per-region map.

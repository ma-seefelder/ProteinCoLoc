---
phase: quick-260725-wb7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/simulator/calibration.jl
  - spike/simulator/ghat.jl
autonomous: true
requirements: [D-16]

must_haves:
  truths:
    - "The measured channel pair is a NAMED PARAMETER defaulting to (2, 3), not a hardcoded c1/c2"
    - "`_real_anchor()` reports masked AND unmasked induced mu + surviving-patch count per fixture FOR THE CONFIGURED PAIR"
    - "The (2,3) run reproduces positive masked +0.8238 (n=22) / unmasked 0.4603 (n=64) and negative masked -0.0338 (n=20) / unmasked 0.3815 (n=64)"
    - "`neg_reachable` still derives from the MASKED negative-fixture measurement, now on the correct pair"
    - "The emitted provenance comment and the printed report state the channel pair AND the c1/c2/c3 -> blue/green/red mapping from test/runtests.jl:105"
    - "ghat.jl records the corrected numbers and marks the old 0.3292 / 0.2481 figures as measured against the DAPI counterstain and SUPERSEDED"
    - "The vl8 WITHDRAWAL of the PRIOR-ONLY claim STANDS; neg_reachable flipping to true is not asserted as evidence of the opposite"
    - "The masked-estimator bias language is softened to a caveat, not a claimed negative bias"
    - "GHAT_MU_KNOTS / GHAT_RHO_KNOTS / GHAT_MU_MIN / GHAT_MU_MAX and ghat() are byte-identical to HEAD"
    - "patch_summary, the training path and the shipped inference path are untouched (no masking anywhere near them)"
  artifacts:
    - path: "spike/simulator/calibration.jl"
      provides: "channel-pair-parameterized real-anchor diagnostic + corrected emit template and printed report"
      contains: "ANCHOR_CHANNEL_PAIR"
    - path: "spike/simulator/ghat.jl"
      provides: "corrected real-anchor provenance comment naming the pair and superseding the DAPI-pair figures"
      contains: "SUPERSEDED"
  key_links:
    - from: "spike/simulator/calibration.jl::_real_anchor"
      to: "test/test_images/<cond>/<cond>_c<N>.tif"
      via: "pair-indexed load_tiff of the two configured channels"
      pattern: "_c\\$\\(.*\\)\\.tif|_c\\$"
    - from: "spike/simulator/calibration.jl::calibrate"
      to: "neg_reachable"
      via: "masked negative-fixture mu < 0 on the configured pair"
      pattern: "anchor\\.negative\\.masked_mu"
    - from: "spike/simulator/calibration.jl::emit_ghat"
      to: "spike/simulator/ghat.jl provenance header"
      via: "template interpolation of the pair + channel-name map"
      pattern: "ANCHOR_CHANNEL_(PAIR|NAMES)"
---

<objective>
Quick task 260725-vl8 fixed the MASKING of the real-anchor diagnostic but left the CHANNEL PAIR
wrong. `_real_anchor()` loads `<cond>_c1.tif` and `<cond>_c2.tif`. `test/runtests.jl:105` records
these fixtures' channels as `["blue", "green", "red"]`, so **channel 1 is the DAPI/Hoechst nuclear
counterstain, not a target protein**. The colocalization pair is green/red = channels 2 and 3. The
user (a domain expert) confirmed this directly.

All three pairs were measured (read-only probe, already deleted):

| pair            | condition | unmasked        | masked           |
|-----------------|-----------|-----------------|------------------|
| c1-c2 blue/green| positive  | mu=0.3292 n=64  | mu=-0.0729 n=21  |
| c1-c2 blue/green| negative  | mu=0.2481 n=64  | mu= 0.1149 n=20  |
| c1-c3 blue/red  | positive  | mu=0.2350 n=64  | mu=-0.0925 n=20  |
| c1-c3 blue/red  | negative  | mu=0.3756 n=64  | mu=-0.1568 n=44  |
| c2-c3 green/red | positive  | mu=0.4603 n=64  | mu= 0.8238 n=22  |
| c2-c3 green/red | negative  | mu=0.3815 n=64  | mu=-0.0338 n=20  |

The correct pair (c2/c3) MASKED separates the controls exactly as their labels promise:
positive +0.8238, negative -0.0338. Every pair involving the DAPI channel is noise between
-0.16 and +0.11 — the expected behaviour for a counterstain versus a target protein. Unmasked,
even the correct pair barely separates (0.4603 vs 0.3815), so masking AND the correct pair are
both required.

Purpose: measure the pair that actually answers the question, make the pair an explicit named
parameter recorded in the provenance so a future reader can never again be unsure which channels
produced a number, and correct the two pieces of vl8 text that were written against the wrong-pair
numbers — WITHOUT reinstating any withdrawn claim.

Output: a pair-parameterized `_real_anchor()`, corrected numbers and channel-name provenance in the
emit template + printed report, and a corrected (hand-patched, comment-only) `ghat.jl` header that
marks the old figures SUPERSEDED. The frozen fit is untouched.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@spike/simulator/calibration.jl
@spike/simulator/ghat.jl
@.planning/quick/260725-vl8-fix-the-real-anchor-diagnostic-in-spike-/260725-vl8-SUMMARY.md

READ-ONLY, DO NOT MODIFY (reference only):
@spike/contract.jl

<interfaces>
<!-- Everything the executor needs. Do not go exploring. -->

From spike/contract.jl (already in scope — calibration.jl includes it at line 56):
  build_mci(data::Vector{Matrix{Float64}}; name::String="sim") -> MultiChannelImage
      # asserts EXACTLY 2 channel matrices; labels them ["ch1","ch2"] internally (positional, not
      # semantic); stores otsu_threshold = Images.otsu_threshold.(data) per given matrix.
      # => feeding it channels 2 and 3 is correct and needs NO change to contract.jl.
  patch_summary(mci) -> Matrix{Union{Float64,Missing}}   # 8x8 on mci.data[1] vs mci.data[2]
  induced_mu(mci) -> Float64                             # mean(skipmissing(patch_summary)); NaN if all missing
  load_tiff(path) -> channel matrix

From src/LoadImages.jl (in scope via contract.jl's include; CALL, never edit):
  _calculate_mask(img) -> Vector{Matrix{Bool}}   # ch .> stored otsu threshold, per channel
  _apply_mask!(img, mask) -> img                 # MUTATES img.data in place

Current shape being changed (calibration.jl:173-198, 267-283):
  _anchor_measure(mci) -> (mu, n)                                  # KEEP AS IS
  _anchor_na() -> (unmasked_mu, unmasked_n, masked_mu, masked_n)   # KEEP SHAPE
  _real_anchor() -> (positive = <nt>, negative = <nt>)             # hardcodes _c1.tif / _c2.tif
  calibrate(...) -> (...; anchor, neg_reachable, ...)              # no pair recorded anywhere

Fixture files on disk (both conditions): <cond>_c1.tif, <cond>_c2.tif, <cond>_c3.tif — c3 exists.

Channel-name ground truth, test/runtests.jl:103-105:
  name = "test_image"
  channels = ["blue", "green", "red"]
</interfaces>
</context>

<constraints>
HARD, non-negotiable:
1. Modify EXACTLY two files: `spike/simulator/calibration.jl` and `spike/simulator/ghat.jl`.
   Nothing else. The plan-artifact corrections in Phase 11 and Phase 13 are handled separately by
   the orchestrator — do NOT touch `.planning/phases/`.
2. Do NOT modify `src/` (hard CLAUDE.md decoupling constraint). `_calculate_mask` / `_apply_mask!`
   are CALLED, never changed.
3. Do NOT modify `patch_summary` in `spike/contract.jl` or `src/amortized/summary.jl`, and add NO
   masking to any training or inference path. The v2.0 network was trained on the unmasked summary
   and inference is consistently unmasked; changing it would invalidate the shipped artifact, the
   ghat knots, SIM-02 and all calibration evidence. The mask lives ONLY inside `_real_anchor()`.
4. Do NOT change `GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX` or `ghat()`.
   `ghat.jl` stays a COMMENT-ONLY hand-patched edit — never regenerated by running the sweep
   (that would re-fit the frozen knots).
5. A Phase 11 executor is running CONCURRENTLY on `spike/simulator/prior.jl`, `forward.jl` and
   `simulator.jl`. Do not touch those. `calibration.jl` includes `forward.jl` — if an `include()`
   fails mid-write, RETRY rather than "fixing" forward.jl.
6. Do NOT revert anything vl8 did. The masked/unmasked dual reporting, the patch counts and the
   two-sided-bias discussion all STAY; this task changes WHICH CHANNELS are measured, updates the
   numbers, and softens one overstated sentence.
7. Do NOT reinstate the withdrawn PRIOR-ONLY claim, and do NOT assert its opposite. See Task 2's
   action for the exact required framing — this is the single highest-risk item in the plan.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Make the measured channel pair a named parameter defaulting to (2, 3)</name>
  <files>spike/simulator/calibration.jl</files>
  <action>
Replace the hardcoded `c1`/`c2` fixture load in `_real_anchor()` (lines 183-198) with an explicit,
named, defaulted channel pair, and thread it through `calibrate()` so it reaches the provenance.

(a) Above the anchor block (near line 156, inside the existing "FAITHFUL real anchor (D-16)"
comment section) add two module-level constants with a short comment recording WHY:

  ANCHOR_CHANNEL_PAIR = (2, 3)  — the colocalization pair, green/red.
  ANCHOR_CHANNEL_NAMES = ("blue (DAPI/Hoechst nuclear counterstain)", "green", "red")
      — sourced from `test/runtests.jl:105` (`channels = ["blue", "green", "red"]`).

The comment must state plainly: channel 1 is a nuclear COUNTERSTAIN, not a target protein, so any
pair involving c1 measures counterstain-vs-protein overlap, not colocalization — which is why the
pair is now explicit and defaulted to (2, 3). Add a tiny helper that renders a channel label, e.g.
`_channel_label(i) = "c$(i) = $(ANCHOR_CHANNEL_NAMES[i])"`, for use by the Task-2 report.

(b) Give `_real_anchor` a keyword parameter `pair::Tuple{Int,Int} = ANCHOR_CHANNEL_PAIR`. Inside
`measure_one(cond)`, load `"$(cond)_c$(pair[1]).tif"` and `"$(cond)_c$(pair[2]).tif"` (interpolated,
not hardcoded), keep the faithful `load_tiff` extraction unchanged (D-16 — NOT an RGB->luminance
reduction), and pass `name = "$(cond)_c$(pair[1])c$(pair[2])"` to `build_mci` so even the MCI name
carries the pair. Everything else in `measure_one` is unchanged and MUST stay: the unmasked
measurement is taken BEFORE `_apply_mask!(mci, _calculate_mask(mci))` because that mutates
`mci.data` in place (the load-bearing-ordering comment stays verbatim), and both measurements go
through `_anchor_measure`. Do not touch `_anchor_measure` or `_anchor_na`.

(c) In `calibrate()`: add a keyword `anchor_pair::Tuple{Int,Int} = ANCHOR_CHANNEL_PAIR`, call
`_real_anchor(; pair = anchor_pair)` inside the existing `try`, and add `anchor_pair` to the
returned NamedTuple (alongside `anchor` and `neg_reachable`) so `emit_ghat` and the printed report
can state the pair. The `catch` fallback keeps `_anchor_na()` for both fixtures, unchanged.

(d) `neg_reachable` (line 279) keeps its definition — `isfinite(anchor.negative.masked_mu) &&
anchor.negative.masked_mu < 0.0` — it just now reads the correct pair. Update ONLY the comment
above it (lines 275-278): the masked estimator remains the one comparable to the v1.0 analysis
pipeline; the predicate is still NOT by itself evidence of physical reachability; on the correct
pair the masked negative fixture reads approximately zero (about -0.03), so the predicate can be
`true` while establishing nothing — n = 2 fixtures cannot settle reachability either way. Do NOT
write that reachability is now established.

Touch nothing else: not the sweep, the PAVA fit, the W1/KS metric, size-invariance, or the sigma/tau/nu
diagnostics.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && julia --project=spike -e 'include("spike/simulator/calibration.jl"); @assert ANCHOR_CHANNEL_PAIR == (2,3); a = _real_anchor(); println("PAIR (2,3): ", a); @assert a.positive.unmasked_n == 64 && a.negative.unmasked_n == 64; @assert isapprox(a.positive.unmasked_mu, 0.4603; atol=5e-4) && isapprox(a.negative.unmasked_mu, 0.3815; atol=5e-4); @assert isapprox(a.positive.masked_mu, 0.8238; atol=5e-4) && isapprox(a.negative.masked_mu, -0.0338; atol=5e-4); @assert a.positive.masked_n == 22 && a.negative.masked_n == 20; b = _real_anchor(; pair=(1,2)); println("PAIR (1,2): ", b); @assert isapprox(b.positive.unmasked_mu, 0.3292; atol=5e-4) && isapprox(b.positive.masked_mu, -0.0729; atol=5e-4) && b.positive.masked_n == 21; println("REAL-ANCHOR-PAIR OK")'</automated>
  </verify>
  <done>`ANCHOR_CHANNEL_PAIR == (2,3)`; the default `_real_anchor()` reproduces positive
  0.4603 (64/64) / +0.8238 (22/64) and negative 0.3815 (64/64) / -0.0338 (20/64); passing
  `pair=(1,2)` reproduces the old vl8 numbers exactly (proving the parameter really drives the
  load); `calibrate()` returns `anchor_pair`; the script prints REAL-ANCHOR-PAIR OK. The
  `PROGRAM_FILE` guard means `include` runs no sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Put the pair, the channel-name map and the corrected numbers into the emit template and printed report</name>
  <files>spike/simulator/calibration.jl</files>
  <action>
Update `emit_ghat`'s template (the `real anchor (D-16)` / `anchor caveat` / `negative tail` block,
lines 329-348) and the matching `println` report at the script entry (lines 399-403). Keep the vl8
structure — UNMASKED block, MASKED block, caveat, negative-tail verdict — and change these things:

(1) NAME THE PAIR AND THE CHANNELS, in both places. Add, as the first line of the anchor block:
    the configured pair interpolated from `r.anchor_pair` (renders `(2, 3)`), written as
    `channel pair c2/c3 = green/red`, plus the full mapping on its own line:
    `channel map (test/runtests.jl:105): c1 = blue (DAPI/Hoechst nuclear counterstain), c2 = green,
    c3 = red`. Then one sentence recording WHY this matters: c1 is a counterstain, not a target
    protein, so pairs involving it measure counterstain-vs-protein overlap and NOT colocalization;
    the earlier figures (positive 0.3292 / negative 0.2481, unmasked c1/c2) were measured against
    the counterstain and are SUPERSEDED. Render the pair from `r.anchor_pair` and the names from
    `ANCHOR_CHANNEL_NAMES` / `_channel_label` — do not hardcode "2"/"3" in the template text.

(2) The four mu values and four counts keep interpolating `r.anchor.*` exactly as they do now, so
    they automatically become 0.4603/0.3815 (64/64 each) and +0.8238 (22/64) / -0.0338 (20/64).

(3) REWRITE the `anchor caveat` block. The vl8 text is now wrong in two ways and must change:
    - DELETE the "the two estimators disagree in SIGN and in ORDER" claim. On the correct pair they
      AGREE in order (positive > negative under both estimators) and the masked pair separates the
      controls far more sharply (+0.8238 vs -0.0338) than the unmasked pair does (0.4603 vs 0.3815).
      Say that.
    - SOFTEN the masked-bias sentence. Keep the range-restriction caveat: independent per-channel
      Otsu plus `_exclude_zero` keeps only pixels bright in BOTH channels, a selection on both
      variables, which can attenuate or distort the correlation (the known weakness of thresholded
      Pearson, the reason Manders/Costes coefficients exist), and masking leaves only about 20-22 of
      64 patches above the >=15-survivor floor. But state explicitly that the earlier "MASKED is
      biased NEGATIVE" framing OVERSTATED it: on the correct pair the masked positive control reads
      +0.8238, so the selection does not prevent detecting strong colocalization — the large
      negative readings that motivated that wording were predominantly the wrong-channel
      (counterstain) artifact, not the mask.
    - KEEP the unmasked-positive-bias sentence (dark background co-varies across channels), and add
      that on the correct pair this also COMPRESSES the contrast between the controls, which is why
      the unmasked pair barely separates them.

(4) NEGATIVE TAIL — the highest-risk text in this plan. `neg_reachable` now evaluates to `true`
    (-0.0338 < 0) where it was `false`. The verdict MUST NOT flip. Required substance:
    `neg_reachable` (masked negative-fixture mu < 0, on the configured pair) = true, but this is
    STILL NOT ESTABLISHED either way — the masked negative fixture reads -0.0338, which is
    approximately zero, not convincingly negative, and n = 2 fixtures cannot settle whether a
    negative induced-mu tail is physically reachable. The former unqualified "physically reachable
    = false => PRIOR-ONLY" reading stays WITHDRAWN (vl8) and is NOT reinstated; equally, the clean
    +0.82 / -0.03 separation is NOT evidence for the opposite claim. The SIM-02 consistency claim
    stays scoped to [GHAT_MU_MIN, GHAT_MU_MAX]; that scoping is unchanged and does not depend on
    this anchor.

Mirror the same content compactly in the `println` report (pair + channel map + the four numbers
with counts + the softened caveat + the NOT-ESTABLISHED verdict).

Julia string gotchas: `emit_ghat`'s body is a `"""` literal — every literal `$` must be escaped and
interior `"""` stays as `\"\"\"`. Keep every other evidence line (generator, sweep, induced-mu range,
monotonicity, SIM-02 match, size-invariance, sigma/tau, nu) byte-identical, and do NOT alter the
`GHAT_*` const emission or the emitted `ghat()` body.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && julia --project=spike -e 'include("spike/simulator/calibration.jl"); a = _real_anchor(); r = (; n_grid=25, n_per=100, imsize=(512,512), mu_min=-0.67976, mu_max=0.847149, spearman=1.0, w1=0.05228, ks=0.10123, induced_n=300, si_maxdev=0.0318, diag=(pooled_sd=0.3169, kurtosis=-0.0518), sigma_ok=true, anchor=a, anchor_pair=ANCHOR_CHANNEL_PAIR, neg_reachable=(isfinite(a.negative.masked_mu) && a.negative.masked_mu < 0.0), mu_knots=[-0.5,0.0,0.5], rho_knots=[-0.99,0.0,0.99]); p = emit_ghat(r; path=joinpath(tempdir(), "ghat_probe_wb7.jl")); s = read(p, String); for t in ["UNMASKED","MASKED","c2/c3","green","red","counterstain","test/runtests.jl:105","SUPERSEDED","NOT ESTABLISHED","WITHDRAWN","Manders"]; @assert occursin(t, s) "missing: $t"; end; for v in [a.positive.unmasked_mu, a.negative.unmasked_mu, a.positive.masked_mu, a.negative.masked_mu]; @assert occursin(string(round(v; digits=4)), s) "missing number: $v"; end; @assert occursin("22/64", s) && occursin("20/64", s) && occursin("64/64", s); @assert !occursin("disagree in SIGN", s); println("EMIT-TEMPLATE OK")' 2>&1 | tail -5</automated>
  </verify>
  <done>The template renders without a string/interpolation error into a TEMP path (never
  `spike/simulator/ghat.jl`), and the rendered text carries: the pair `c2/c3` with the green/red
  names, the `test/runtests.jl:105` channel map, the counterstain explanation, the SUPERSEDED note,
  all four current mu values with their 64/64, 22/64 and 20/64 counts, the softened mask caveat
  (Manders/Costes retained, "disagree in SIGN" gone), and the NOT-ESTABLISHED + WITHDRAWN verdict.
  Running `calibration.jl` as a script is NOT permitted here — it would re-fit the knots.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-patch the frozen ghat.jl provenance header with the corrected pair and numbers</name>
  <files>spike/simulator/ghat.jl</files>
  <action>
Replace the `real anchor (D-16)` / `anchor caveat` / `negative tail` block in the ghat.jl header
(lines 48-67) with the corrected block, copying the wording Task 2's template now produces so a
future regeneration is a no-op on this block. Use the numbers from Task 1's verify run verbatim
(if any differ from those below, the RUN wins):

  channel pair: c2/c3 = green/red (ANCHOR_CHANNEL_PAIR = (2, 3))
  channel map (test/runtests.jl:105): c1 = blue (DAPI/Hoechst nuclear counterstain), c2 = green, c3 = red
  UNMASKED: positive mu = 0.4603 (64/64), negative mu = 0.3815 (64/64)
  MASKED:   positive mu = 0.8238 (22/64), negative mu = -0.0338 (20/64)

The block MUST state plainly that the previous figures — positive 0.3292 / negative 0.2481 — were
measured against `c1`, the DAPI/Hoechst nuclear COUNTERSTAIN rather than a target protein, and are
therefore SUPERSEDED (not merely updated). Carry over the softened caveat and the unchanged
NOT-ESTABLISHED / WITHDRAWN verdict from Task 2.

Extend the existing `HAND-PATCH PROVENANCE (2026-07-25)` note (lines 25-32) with a second dated
entry for this correction: the anchor block was hand-patched again to change the measured channel
pair from c1/c2 to c2/c3, for the same reason as before (re-running `calibration.jl` would re-run
the stochastic sweep and re-fit the map, changing the frozen knots — forbidden), and the wording is
again copied verbatim from the corrected `calibration.jl` emit template.

Change NOTHING else: `GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX` and the
`ghat()` body stay byte-identical.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && ! git diff -U0 -- spike/simulator/ghat.jl | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*#' && julia --project=spike -e 'include("spike/simulator/ghat.jl"); @assert GHAT_MU_MIN == -0.67976 && GHAT_MU_MAX == 0.847149 && length(GHAT_MU_KNOTS) == 25 && length(GHAT_RHO_KNOTS) == 25; @assert isapprox(ghat(0.0), -0.12004360606355068; atol=1e-12); s = read("spike/simulator/ghat.jl", String); for t in ["c2/c3","counterstain","test/runtests.jl:105","0.4603","0.3815","0.8238","-0.0338","22/64","20/64","SUPERSEDED","0.3292","0.2481","NOT ESTABLISHED","WITHDRAWN"]; @assert occursin(t, s) "missing: $t"; end; @assert !occursin("disagree in SIGN", s); println("GHAT-HEADER OK")'</automated>
  </verify>
  <done>`git diff` on ghat.jl shows ONLY comment-line changes (no non-`#` line added or removed);
  the file still loads with all four `GHAT_*` constants at their original values, 25 knots each, and
  `ghat(0.0)` unchanged to 1e-12; the header names the c2/c3 pair and the channel map, carries the
  four corrected numbers with counts, marks 0.3292 / 0.2481 as counterstain-measured and SUPERSEDED,
  and keeps the NOT-ESTABLISHED / WITHDRAWN verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike/ -> src/ | read-only include() coupling; the spike may CALL src functions, never modify them |
| calibration.jl -> ghat.jl | generated-artifact boundary; the frozen fit must not drift from a diagnostic edit |
| diagnostic mask -> training/inference summary | the Otsu mask must stay confined to `_real_anchor()`; the shipped path is unmasked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wb7-01 | Tampering | spike/simulator/ghat.jl frozen knots | mitigate | Task 3 verify asserts the git diff is comment-only and re-checks all four `GHAT_*` constants plus `ghat(0.0)` to 1e-12 |
| T-wb7-02 | Tampering | shipped v2.0 summary / training / inference path | mitigate | constraints #2/#3 forbid touching `src/`, `contract.jl`, `src/amortized/summary.jl`; `files_modified` is exactly two spike files; the mask stays inside `_real_anchor()` |
| T-wb7-03 | Repudiation | which channels produced a published number | mitigate | pair becomes a named constant, is threaded into `calibrate()`'s return, and is rendered into BOTH the emitted provenance and the printed report together with the `test/runtests.jl:105` channel map |
| T-wb7-04 | Information disclosure (scientific overclaim) | negative-tail reachability verdict | mitigate | Task 2 (4) fixes the required framing verbatim; Task 2/3 verifies assert `NOT ESTABLISHED` and `WITHDRAWN` are still present after `neg_reachable` flips to `true` |
| T-wb7-05 | Tampering | concurrent Phase-11 edits to forward.jl/prior.jl/simulator.jl | accept | different files; on a mid-write include failure, wait and retry rather than editing them |

No package-manager installs in this plan; no legitimacy gate required.
</threat_model>

<verification>
1. `ANCHOR_CHANNEL_PAIR == (2,3)`, the default anchor reproduces the four target numbers, and
   `pair=(1,2)` reproduces the old vl8 numbers (Task 1 verify).
2. The emit template renders the pair, the channel map, the SUPERSEDED note, the four corrected
   numbers and the unchanged NOT-ESTABLISHED verdict (Task 2 verify).
3. ghat.jl's diff is comment-only, the frozen fit still loads identically, and the header carries
   the corrections (Task 3 verify).
4. `git status --porcelain src/ spike/contract.jl` is EMPTY — decoupling preserved.
5. `git status --porcelain spike/simulator/` shows changes only to `calibration.jl` and `ghat.jl`
   (any `prior.jl`/`forward.jl`/`simulator.jl` entries belong to the concurrent Phase-11 executor
   and MUST NOT be staged or committed by this plan).
6. `git status --porcelain .planning/phases/` is untouched by this plan (Phase 11/13 artifact
   corrections are the orchestrator's separate job).
</verification>

<success_criteria>
- The measured channel pair is a named, defaulted parameter `(2, 3)` — not a hardcoded c1/c2 — and
  is visible in the emitted provenance and the printed report.
- `_real_anchor()` reports, per fixture, masked and unmasked mu plus surviving-patch count for the
  configured pair: positive +0.8238 (22) / 0.4603 (64), negative -0.0338 (20) / 0.3815 (64).
- `neg_reachable` still derives from the MASKED negative measurement, now on the correct pair.
- Provenance and report record the `c1 = blue/DAPI counterstain, c2 = green, c3 = red` mapping from
  `test/runtests.jl:105`, so the reason c1 was wrong is on the record.
- `ghat.jl` carries the corrected numbers and states that 0.3292 / 0.2481 were measured against the
  DAPI counterstain and are SUPERSEDED.
- The vl8 withdrawal STANDS: reachability remains NOT ESTABLISHED, and `neg_reachable` becoming
  `true` is explicitly not presented as evidence for the opposite claim.
- The masked-estimator bias language is a caveat (range restriction, ~20-22/64 patches), no longer a
  claimed negative bias; the "disagree in SIGN and in ORDER" sentence is gone.
- The frozen fit (`GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX`, `ghat()`) is
  byte-identical to HEAD; `src/`, `spike/contract.jl` and every training/inference path untouched.
</success_criteria>

<output>
Create `.planning/quick/260725-wb7-correct-the-real-anchor-diagnostic-chann/260725-wb7-SUMMARY.md` when done.
Record in it: the six-row three-pair measurement table, the final four numbers with counts for the
(2,3) pair, the `neg_reachable` value AND why it flipping to `true` changes no verdict, and the
out-of-scope artifacts that still quote the old c1/c2 figures (`spike/NOTES.md` §3,
`spike/test/test_simulator.jl:225`, `.planning/phases/02-.../02-03-SUMMARY.md`,
`.planning/phases/13-.../13-RESEARCH.md`, `13-PATTERNS.md`, `13-15-PLAN.md`, `13-01-PLAN.md`) —
noting the Phase 11/13 artifact corrections are the orchestrator's separate job.
</output>

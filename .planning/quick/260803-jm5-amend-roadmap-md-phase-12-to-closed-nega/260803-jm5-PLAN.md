---
phase: quick-260803-jm5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements: [SPAT-01, SPAT-02, SPAT-03, SPAT-04, SPAT-05, SPAT-06, SPAT-07, SPAT-08, SPAT-09]
# NOTE on `requirements`: this plan IMPLEMENTS none of these. It RECORDS their true
# delivered status in ROADMAP.md, as measured by 12-VERIFICATION.md. Listed for
# traceability only.

must_haves:
  truths:
    - "A reader of ROADMAP.md alone learns that Phase 12 did NOT achieve its stated goal, without opening any other file"
    - "The summary bullet for Phase 12 is checked `[x]` and carries the CLOSED NEGATIVE annotation"
    - "All three Phase-12 success criteria carry an inline verdict, and the existing AMENDED pointer to 12-SC3-AMENDMENT.md still stands"
    - "The Phase-12 plan list shows all 20 real plans with their true status; no TBD remains in the Phase-12 section"
    - "The Closure block carries the §10.5 manuscript sentence verbatim and the §10.1/§10.5 no-retune ruling"
    - "No file other than .planning/ROADMAP.md is modified"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase-12 closure record matching 12-VERIFICATION.md"
      contains: "NONE-BEATS-ABLATION"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "12-VERIFICATION.md"
      via: "explicit filename citation in both the summary bullet and the Closure block"
      pattern: "12-VERIFICATION\\.md"
    - from: ".planning/ROADMAP.md"
      to: "12-D13-AUTHORISATION.md"
      via: "explicit filename citation with section numbers in the Closure block"
      pattern: "12-D13-AUTHORISATION\\.md"
---

<objective>
Amend `.planning/ROADMAP.md` so Phase 12's recorded status matches `12-VERIFICATION.md`.
Documentation only. Zero code, zero compute, zero thresholds touched.

Purpose: `12-VERIFICATION.md` (2026-08-03, `status: gaps_found`) recorded a **negative** headline
— the phase did not achieve its stated goal — and four of its twenty plans are FORECLOSED by rulings
already on the record. ROADMAP.md still shows Phase 12 as an open `[ ]` item with `**Plans**: TBD`.
That gap is the last thing standing between the project record and an honest reader. Left as-is, a
future reader (or a future agent running `execute-phase 12`) reads "unplanned, pending" where the
truth is "executed, closed, answered NO."

Output: one amended `.planning/ROADMAP.md` — summary bullet, three success-criteria annotations, the
real 20-plan list, and a Closure block mirroring Phase 11's.

**THIS PLAN DELIBERATELY OVERRIDES THE STANDARD QUICK-MODE RULE THAT QUICK TASKS MUST NOT TOUCH
ROADMAP.md.** A roadmap amendment is the entire deliverable here. This is stated so an executor does
not halt on the conflict, and so a reviewer does not read it as a violation.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-spatial-colocalization-map/12-VERIFICATION.md
@.planning/phases/12-spatial-colocalization-map/12-D13-AUTHORISATION.md

**Style template — READ THIS FIRST, then mirror its voice exactly:** ROADMAP.md lines 232-260, the
Phase 11 section. Phase 11 is the only prior phase in this project closed negative-but-useful, and
its annotation style (bold verdict, then the explanation, then a `**Closure**:` block with a legend)
is the pattern to reproduce. Do not invent a new format.
</context>

<constraints_for_the_executor>

**THE ONLY FILE YOU MAY MODIFY IS `.planning/ROADMAP.md`.**

FORBIDDEN, and each of these would be a serious breach of this phase's discipline:
- Editing any `12-*-PLAN.md` or `12-*-SUMMARY.md`, **including frontmatter**. Specifically:
  `12-11-SUMMARY.md` says `status: blocked` and that is STALE — the plan is complete in fact.
  **LEAVE IT BYTE-UNCHANGED.** This phase runs an append-never-overwrite discipline. The staleness is
  DOCUMENTED in the roadmap (Task 2) rather than repaired.
- Touching `spike/`, `src/`, or any `.jl` file.
- Adjusting, re-deriving or re-examining any threshold.
- Re-running, re-training, or re-seeding anything. No compute of any kind.
- Marking Phase 12 "complete" in any global counter, or editing `.planning/STATE.md`.

**SOURCE-OF-TRUTH RULE, and it is absolute.** Every number, every quoted phrase, and every filename
you write must be verified against `12-VERIFICATION.md` or `12-D13-AUTHORISATION.md` **as you write
it**. This plan paraphrases those documents for your convenience; the paraphrase is NOT the source.
If a figure in this plan disagrees with the source document, the source document wins and you report
the discrepancy in your SUMMARY.

**DO NOT SOFTEN THE NEGATIVE.** The verification's headline is *"NO — the phase did not achieve its
stated goal."* The roadmap must read as a NO with the same force. Words like "partially achieved",
"largely delivered", "promising", or "close" are prohibited. Equally: do not EMBELLISH — do not add
blame, do not editorialize about execution quality. The verification is explicit that this is *"not
read here as an execution failure"* and that every outcome came from a pre-registered, executed
measurement.

</constraints_for_the_executor>

<tasks>

<task type="auto">
  <name>Task 1: Summary bullet + the three success-criteria verdicts</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Two edits in this task. Both are in `.planning/ROADMAP.md`. Read Phase 11's entries (summary bullet
at line 30; criteria at lines 237-240) before writing either, and match that voice.

**EDIT 1 — the summary bullet, currently line 31.** It currently reads:

`- [ ] **Phase 12: Spatial Colocalization Map (GP/CAR)** - Lattice prior over the correlation grid → amortized per-region Δρ map + uncertainty (descope-to-v2.1 candidate)`

Change `[ ]` to `[x]` and APPEND a CLOSED NEGATIVE annotation, in the same em-dash-then-bold shape as
line 30's Phase 11 entry. Preserve the existing bullet text ahead of your annotation verbatim,
including the `(descope-to-v2.1 candidate)` parenthetical — it turned out to be prescient and is part
of the record. Your appended annotation must state, all four:

  a. **The goal was NOT achieved.** Date it 2026-08-03.
  b. **NONE-BEATS-ABLATION in three independent mini-spike runs**, therefore the shipped deliverable
     IS the neutralized ablation — the *same* network with spatial borrowing switched off — wearing
     the `SpatialColocResult` type, and **no trained spatial arm exists at any scale.** (Source:
     12-VERIFICATION.md "Headline answer" §1. Use the type name exactly as that section writes it.)
  c. **SPAT-06 was measured and FAILED its pre-registered band:** pooled leave-region-out coverage
     **0.9707** against **[0.87, 0.93]**. (Source: 12-VERIFICATION.md frontmatter `gaps`. Verify both
     figures there before writing them.)
  d. **SPAT-07 and SPAT-08 are DEFERRED TO v2.1 by recorded user rulings dated 2026-07-31** — not
     skipped, not forgotten. (Source: 12-VERIFICATION.md `overrides`.)

End the annotation with a pointer to `12-VERIFICATION.md`.

**EDIT 2 — the three success criteria in the Phase 12 detail section (currently lines 268-270).**

**FIRST, AND NON-NEGOTIABLE: the `**AMENDED**` paragraph immediately above them (currently line 267,
pointing at `12-SC3-AMENDMENT.md`) STAYS. Do not delete it, do not move it, do not reword it.** It
records that the criteria were amended *before* any Phase-12 result existed, which is what makes the
amendment legitimate. Your annotations go on the three numbered criteria BELOW it.

Annotate each criterion inline, in Phase 11's shape — bold verdict first, then the explanation:

  1. *A lattice prior (CAR vs. AbstractGPs, chosen by mini-spike) is placed over the `correlation()`
     grid with a CNN/DeepSet summary* → **MECHANISM BUILT; NO ARM WAS EVER SELECTED.** Both prior
     arms are implemented (as dense linear algebra rather than `AbstractGPs.jl`, per the pre-registered
     `12-SC3-AMENDMENT.md` §2) and the CNN summary net exists — but the "chosen by mini-spike" clause
     resolved to **neither arm**: `select_prior` returned `NONE-BEATS-ABLATION` in all three runs
     (unseeded 18-epoch, seeded 18-epoch control, seeded 100-epoch treatment). Name the function
     names only if you verify them in 12-VERIFICATION.md §SPAT-01/SPAT-03 first.

  2. *`coloc_map(...)` returns a Δρ map + uncertainty map, amortized in a forward pass* →
     **DELIVERED.** `p12_coloc_map` verified working — returns the per-region Δρ map together with
     per-region uncertainty, each stack one amortized forward pass. **Carry the named caveat from
     12-VERIFICATION.md §SPAT-05 with it:** the model behind it is the non-spatial D-13 ablation, not
     a trained spatial arm, and it exists at spike scale (10,000 pairs), not the 50,000-pair version.

  3. *The spatial (CAR) model beats independent pooling in coverage on ≥1 real image* → **NOT MET,
     AND UNREACHABLE AS WRITTEN.** Two independent reasons, state both: (i) there is **no spatial
     model to compare against** — see criterion 1; (ii) the real-image arm (**12-19**) **never ran**,
     hard-blocked on `p12_train_full_report.jld2`, confirmed absent on disk, which only the
     never-executed **12-17** produces. The reduced substitute that DID run (12-16, on simulated
     data, ablation against a prior-only floor) failed its own pre-registered band — quote
     `12-16-SUMMARY.md`'s own words: **"SPAT-06 IS NOT MET."**

Do not renumber, reorder, or reword the original criterion text. Annotate in place, exactly as
Phase 11 does.
  </action>
  <verify>
    <automated>
    cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && \
    test "$(grep -cE '^- \[x\] \*\*Phase 12: Spatial Colocalization Map' .planning/ROADMAP.md)" = "1" && \
    test "$(grep -c 'NONE-BEATS-ABLATION' .planning/ROADMAP.md)" -ge 2 && \
    test "$(grep -c '0\.9707' .planning/ROADMAP.md)" -ge 2 && \
    test "$(grep -c 'SPAT-06 IS NOT MET' .planning/ROADMAP.md)" -ge 1 && \
    test "$(grep -c '12-SC3-AMENDMENT\.md' .planning/ROADMAP.md)" -ge 1 && \
    echo TASK1_OK
    </automated>
  </verify>
  <done>
Summary bullet is `[x]` and carries all four required claims with a `12-VERIFICATION.md` pointer.
All three success criteria carry inline bold verdicts. The `**AMENDED**` pointer to
`12-SC3-AMENDMENT.md` is still present and byte-unchanged. Verify command prints `TASK1_OK`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the stale plan list; add the Closure block</name>
  <files>.planning/ROADMAP.md</files>
  <action>
**EDIT 3 — replace `**Plans**: TBD` and `- [ ] TBD (run /gsd:plan-phase 12 to break down)` (currently
lines 272-273) with the real 20-plan list.**

Use Phase 11's legend: `[x]` = complete, `[S]` = superseded/foreclosed, `[~]` = ran but blocked.
Twenty plans exist on disk (`12-01-PLAN.md` .. `12-20-PLAN.md`); `12-01` .. `12-16` have SUMMARY
files. **Derive each one-line description from that plan's own `<objective>` block — open the plan
file and read it. Do not guess and do not copy this plan's paraphrase.**

Header line: state that 20 plans exist, 16 executed (12-01..12-16), and 12-17..12-20 are FORECLOSED
and never ran.

**12-01 .. 12-16 → `[x]`.** Description from each plan's objective, plus the headline measured
outcome where one exists and is verifiable in 12-VERIFICATION.md (e.g. 12-10's per-region W1;
12-06's `eps_ratio`; 12-15's three-run `NONE-BEATS-ABLATION`; 12-16's 0.9707 and `spat07_scope =
:deferred`). Two of these entries need extra text:

  - **12-11 — RECORD THE STALE FRONTMATTER IN THE ROADMAP.** Mark it `[x]` (it is **complete in
    fact**: all artifacts on disk, and `12-STAGE1-VERDICT.md` reads `VERDICT: PROCEED` at commit
    `ee78cfe`). Then state plainly, in the roadmap entry, that its SUMMARY frontmatter still reads
    `status: blocked`, that this is **STALE**, and that it is **deliberately LEFT BYTE-UNCHANGED**
    per this phase's append-never-overwrite discipline — **and that this is precisely why the SDK
    counts 5 incomplete plans rather than 4.** Add an explicit instruction not to "fix" the plan file.
    A future reader must be able to resolve the 5-vs-4 discrepancy from the roadmap alone.
  - **12-14** — note that its Task 3, the only coded producer of the D-13 descope bundle, is keyed on
    `:descope` and therefore **ran as a no-op** under the `:proceed` verdict. This is the wiring
    defect that forced the standalone `12-D13-AUTHORISATION.md`.

**12-17 .. 12-20 → `[S]`, all four NEVER RUN.** Give each its specific foreclosure reason:

  - **12-17** (SPAT-03, SPAT-04 at production scale) — **FORECLOSED.** Keys its 50,000-pair pool on
    `P12_CHOSEN_PRIOR`, which is *asserted absent* at `spike/validation/p12_consts.jl:798-799`.
    Dispatching it throws. Superseded by the 2026-07-31 descope.
  - **12-18** (SPAT-07) — **DEFERRED TO v2.1** by user ruling dated 2026-07-31,
    `12-D13-AUTHORISATION.md` §9. `spike/test/test_p12_sbc.jl` remains a self-declaring
    `P12_PENDING_SCAFFOLD`.
  - **12-19** (SPAT-06 real-image arm) — **FORECLOSED.** Resolves and hash-verifies both bundles from
    `p12_train_full_report.jld2`, which only 12-17 produces and which is confirmed absent on disk.
    Dead behind 12-17.
  - **12-20** (SPAT-08, SPAT-09 guards + Stage-2 verdict) — **DEFERRED TO v2.1** by user ruling dated
    2026-07-31, `12-D13-AUTHORISATION.md` §7. Consequence worth stating: **the phase therefore makes
    no radial-confound claim at all**, and the S-4 warning is left standing and unaddressed.

**THE CLOSURE BLOCK — append after the plan list, mirroring Phase 11's `**Closure**:` block
(currently lines 254-260).**

Point at both `12-VERIFICATION.md` (2026-08-03, `status: gaps_found`, and its 8/9 must-have score
broken out as: 6 verified, 2 accepted as deferred-to-v2.1 by recorded user ruling, 1 FAILED) and
`12-D13-AUTHORISATION.md`. Give the legend, as Phase 11 does.

Then carry forward the two things the record says must survive:

  1. **§10.5's fixed manuscript sentence, QUOTED VERBATIM.** Open `12-D13-AUTHORISATION.md` §10.5 and
     copy it character-for-character from *"THE RESULTING CLAIM, IN THE EXACT FORM IT IS TO APPEAR IN
     THE MANUSCRIPT"*: **"We cannot exclude that longer training would have brought the spatial arms
     into the coverage band."** Record it as §10.5 does — a **NAMED LIMIT of v2.0, not a caveat**.
  2. **§10.1 / §10.5's ruling that SPAT-06 is NOT to be re-tuned** by adjusting epochs, N, thresholds
     or the noise model. Name the three specific moves that were each offered and each REFUSED:
     widening the admissibility loop at `run_p12_minispike.jl:249` after seeing `:none` fail it;
     re-deriving the [0.87, 0.93] band after seeing the arms miss it; and relaxing the early-stopping
     criterion to train longer. State that the loop **stays wrong and documented as wrong**.

Also carry §10.1's honest statement, which is stronger than "the spatial priors were miscalibrated"
and is the sentence that should survive: **no arm is calibrated at 100 epochs, and the fallback was
retained without being tested.** Verify its exact wording in §10.1 before writing it.

Close with two operational statements, both plainly worded:

  - **Phase 12 will remain `partial` to the SDK permanently, BY DESIGN.** Five requirements
    (SPAT-03, SPAT-04, SPAT-06 undelivered; SPAT-07, SPAT-08 deferred) will never be marked
    delivered, and the 12-11 frontmatter staleness will never be repaired. The phase must not be
    marked complete against 9 requirements.
  - **Re-running `/gsd:execute-phase 12` is NOT indicated.** All four remaining plans are foreclosed
    by rulings already on the record, not pending. `12-VERIFICATION.md` explicitly does not recommend
    executing 12-17/12-18/12-19/12-20, nor re-running, retuning or extending any existing measurement.
  </action>
  <verify>
    <automated>
    cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && \
    SEC=$(awk '/^### Phase 12:/{f=1} /^### Phase 13:/{f=0} f' .planning/ROADMAP.md) && \
    test "$(printf '%s\n' "$SEC" | grep -c 'TBD')" = "0" && \
    test "$(printf '%s\n' "$SEC" | grep -cE '^- \[(x|S)\] 12-[0-9]{2}-PLAN\.md')" = "20" && \
    test "$(printf '%s\n' "$SEC" | grep -cE '^- \[S\] 12-(17|18|19|20)-PLAN\.md')" = "4" && \
    test "$(printf '%s\n' "$SEC" | grep -cE '^- \[x\] 12-[0-9]{2}-PLAN\.md')" = "16" && \
    test "$(printf '%s\n' "$SEC" | grep -ci 'cannot exclude that longer training')" -ge 1 && \
    test "$(printf '%s\n' "$SEC" | grep -c '12-D13-AUTHORISATION\.md')" -ge 1 && \
    test "$(printf '%s\n' "$SEC" | grep -c '12-VERIFICATION\.md')" -ge 1 && \
    test "$(printf '%s\n' "$SEC" | grep -c '\*\*Closure\*\*')" = "1" && \
    test "$(git diff --name-only -- spike src test .planning/phases .planning/STATE.md | wc -l)" = "0" && \
    test "$(git diff --name-only -- .planning/ROADMAP.md | wc -l)" = "1" && \
    echo TASK2_OK
    </automated>
  </verify>
  <done>
Phase-12 section contains zero `TBD`; exactly 20 plan bullets (16 `[x]`, 4 `[S]`); a `**Closure**:`
block citing both source documents, carrying the §10.5 sentence verbatim and the no-retune ruling;
and the permanent-`partial` / do-not-re-execute statements. `git diff` shows `.planning/ROADMAP.md`
and nothing under `spike/`, `src/`, `test/`, `.planning/phases/`, or `.planning/STATE.md`. Verify
command prints `TASK2_OK`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
`.planning/ROADMAP.md` amended in four places: the Phase-12 summary bullet flipped to `[x]` with a
CLOSED NEGATIVE annotation; the three success criteria annotated with inline verdicts (AMENDED
pointer preserved); `**Plans**: TBD` replaced with the real 20-plan list; and a `**Closure**:` block
added. No other file touched.
  </what-built>
  <how-to-verify>
This one needs your eyes rather than a grep, because the thing at stake is *tone*, and the rulings
being transcribed are yours.

1. Open `.planning/ROADMAP.md`. Read the Phase 12 summary bullet and then the whole Phase 12 detail
   section, top to bottom, as a stranger would.
2. **The one question that matters: does it read as a NO?** Compare against `12-VERIFICATION.md`'s
   headline — *"NO — the phase did not achieve its stated goal."* If the roadmap reads softer than
   the verification, say so and it gets sharpened.
3. Check the two things you ruled on personally, in the Closure block:
   - The §10.5 sentence — **"We cannot exclude that longer training would have brought the spatial
     arms into the coverage band."** — appears verbatim and is labelled a named limit, not a caveat.
   - The no-retune ruling names the three refused moves (the `:249` loop, the [0.87, 0.93] band, the
     early-stopping criterion) and says the loop stays wrong and documented as wrong.
4. Check the 12-11 entry states its `status: blocked` frontmatter is stale, left byte-unchanged
   deliberately, and is why the SDK counts 5 rather than 4.
5. Confirm nothing was over-claimed in the other direction — no added blame, no editorializing about
   execution quality.
  </how-to-verify>
  <resume-signal>Type "approved", or name the sentences that are too soft / too strong / wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies one local markdown file. No untrusted input is parsed, no code executes, no network or package manager is invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | `.planning/ROADMAP.md` and the append-only Phase-12 record | mitigate | Task 2's verify asserts `git diff --name-only` lists `.planning/ROADMAP.md` and nothing under `spike/`, `src/`, `test/`, `.planning/phases/`, or `.planning/STATE.md`. The real tampering risk here is silently "fixing" `12-11-SUMMARY.md`'s stale frontmatter — explicitly forbidden and gated by that same diff check. |
| T-quick-02 | Repudiation | the recorded negative result | mitigate | Every claim written must be traceable to `12-VERIFICATION.md` or `12-D13-AUTHORISATION.md`; the human-verify checkpoint exists specifically so the user confirms their own rulings were transcribed without softening. |
| T-quick-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager install task exists in this plan; no dependency is added. |
</threat_model>

<verification>
- `.planning/ROADMAP.md` is the ONLY modified file (`git diff --name-only`).
- Phase-12 section: zero `TBD`; 20 plan bullets (16 `[x]`, 4 `[S]`); one `**Closure**:` block.
- Every figure written (0.9707, [0.87, 0.93], W1 = 0.00675, `eps_ratio` = 0.96307, "8/9 must-haves")
  re-read from `12-VERIFICATION.md` at write time, not from this plan's paraphrase.
- `12-SC3-AMENDMENT.md` pointer preserved.
- No `.jl` file, no `spike/` file, no `src/` file, no `12-*-PLAN.md`, no `12-*-SUMMARY.md` touched.
- Human confirms the section reads as a NO.
</verification>

<success_criteria>
- ROADMAP.md's Phase-12 record and `12-VERIFICATION.md` no longer disagree on any material point.
- A reader who opens only ROADMAP.md learns: the goal was not achieved; there is no trained spatial
  arm; SPAT-06 failed its band; SPAT-07/08 were deferred by dated user ruling; four plans are
  foreclosed; the phase stays `partial` by design; do not re-run it.
- The two carry-forward items (§10.5 manuscript sentence, §10.1/§10.5 no-retune ruling) are recorded
  where the next person will actually find them.
- Zero compute spent, zero thresholds moved, `P12_ITERATION_ALLOWANCE` untouched.
</success_criteria>

<output>
Create `.planning/quick/260803-jm5-amend-roadmap-md-phase-12-to-closed-nega/260803-jm5-SUMMARY.md`
when done. Record any discrepancy found between this plan's paraphrased figures and the source
documents — the source wins, and the divergence is worth recording.
</output>

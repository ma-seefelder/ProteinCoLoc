---
phase: quick-260725-vl8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/simulator/calibration.jl
  - spike/simulator/ghat.jl
autonomous: true
requirements: [D-16]

must_haves:
  truths:
    - "`_real_anchor()` reports, per fixture, BOTH an unmasked and a masked induced μ plus the surviving-patch count for each"
    - "`neg_reachable` is derived from the MASKED negative-fixture measurement, not the unmasked one"
    - "The emitted ghat.jl provenance comment and the printed calibration report both carry all four μ values and their patch counts"
    - "ghat.jl no longer asserts an unqualified 'physically reachable = false ⇒ PRIOR-ONLY'; it states the two-sided bias and that reachability is NOT ESTABLISHED"
    - "GHAT_MU_KNOTS / GHAT_RHO_KNOTS / GHAT_MU_MIN / GHAT_MU_MAX are byte-identical to HEAD"
    - "patch_summary, the training path and the shipped inference path are untouched"
  artifacts:
    - path: "spike/simulator/calibration.jl"
      provides: "masked+unmasked real-anchor diagnostic, masked-derived neg_reachable, both-numbers emit template + printed report"
      contains: "_apply_mask!"
    - path: "spike/simulator/ghat.jl"
      provides: "corrected real-anchor provenance comment with the two-sided bias recorded"
      contains: "NOT ESTABLISHED"
  key_links:
    - from: "spike/simulator/calibration.jl::_real_anchor"
      to: "src/LoadImages.jl::_calculate_mask / _apply_mask!"
      via: "called read-only through the contract.jl include() boundary"
      pattern: "_apply_mask!\\(.*_calculate_mask\\("
    - from: "spike/simulator/calibration.jl::calibrate"
      to: "neg_reachable"
      via: "masked negative-fixture μ < 0"
      pattern: "anchor\\.negative\\.masked_mu"
---

<objective>
`_real_anchor()` measures the two real microscopy fixtures WITHOUT the Otsu mask that the
v1.0 analysis pipeline applies (`src/utils.jl:86` → `_apply_mask!(img, _calculate_mask(img))`,
then `_exclude_zero` drops the zeroed background). Measured both ways today:

| fixture  | unmasked μ | patches | masked μ | patches |
|----------|-----------|---------|----------|---------|
| positive | 0.3292    | 64/64   | −0.0729  | 21/64   |
| negative | 0.2481    | 64/64   |  0.1149  | 20/64   |

The positive control measures NEGATIVE when masked, the ordering INVERTS between the two
estimators, and only ~20 of 64 patches clear the ≥15-survivor floor under masking. The
unmasked pair is the sole evidence behind the `ghat.jl:39-40` claim
"negative tail: physically reachable in real fluorescence = false ⇒ PRIOR-ONLY". That claim
does not survive its own data measured through the masked pipeline.

Purpose: make the diagnostic report both estimators honestly, and replace the overclaim with
an accurate statement of what n=2 fixtures do and do not establish — without replacing it with
the opposite overclaim.

Output: a two-sided `_real_anchor()`, a masked-derived `neg_reachable`, both numbers in the
emit template + printed report, and a corrected (hand-patched, header-only) `ghat.jl`
provenance block. The frozen fit is untouched.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@spike/simulator/calibration.jl
@spike/simulator/ghat.jl

READ-ONLY, DO NOT MODIFY (reference only):
@spike/contract.jl
@src/LoadImages.jl
@src/colocalization.jl

<interfaces>
<!-- Everything the executor needs. Do not go exploring. -->

From spike/contract.jl (in scope after `include(contract.jl)`, which calibration.jl already does):
  build_mci(data::Vector{Matrix{Float64}}; name::String="sim") -> MultiChannelImage
      # stores otsu_threshold = Images.otsu_threshold.(data), mirroring src/LoadImages.jl:445
  patch_summary(mci) -> Matrix{Union{Float64,Missing}}   # 8x8; <=15 survivors per patch => missing
  induced_mu(mci) -> Float64                             # mean(skipmissing(patch_summary)); NaN if all missing
  load_tiff(path) -> channel matrix

From src/LoadImages.jl (in scope via contract.jl's include; CALL, never edit):
  _calculate_mask(img) -> Vector{Matrix{Bool}}   # ch .> thr, per channel, using the STORED otsu thresholds
  _apply_mask!(img, mask) -> img                 # img.data[c] .*= mask[c]  -- MUTATES IN PLACE, returns img

v1.0 call site being mirrored (src/utils.jl:84-87):
  if mask
      images[i] = ProteinCoLoc._apply_mask!(images[i], ProteinCoLoc._calculate_mask(images[i]))
  end

From src/colocalization.jl:154-169 -- `_exclude_zero` drops any pixel pair where EITHER value is
0.0 or NaN. Combined with `_apply_mask!`, this keeps only pixels above Otsu in BOTH channels.

Current shape being replaced (calibration.jl:157-164, 234-244, 290-291, 342-343):
  _real_anchor() -> (positive = Float64, negative = Float64)
  neg_reachable = (isfinite(anchor.negative) && anchor.negative < 0.0)
</interfaces>
</context>

<constraints>
HARD, non-negotiable:
1. DO NOT modify `spike/contract.jl`, `src/amortized/summary.jl`, or any training/inference path.
   `patch_summary` produces the 128-dim summary the entire v2.0 network was trained on, and the
   shipped inference path is deliberately unmasked too — training and serving are CONSISTENT today.
   Masking there would invalidate the shipped artifact, the ghat knots, SIM-02 and all calibration
   evidence. OUT OF SCOPE.
2. DO NOT modify `src/` at all (CLAUDE.md hard decoupling constraint). `_calculate_mask` /
   `_apply_mask!` are CALLED from the spike, never changed.
3. Scope is exactly two files: `spike/simulator/calibration.jl` and `spike/simulator/ghat.jl`.
4. DO NOT change `GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX`, or the `ghat()`
   function. The fit stays exactly as it is.
5. A Phase 11 executor is concurrently editing `spike/simulator/{prior,forward,simulator}.jl`.
   Stay out of those three files. `calibration.jl` `include`s `forward.jl`; if a verify run fails
   because `forward.jl` is mid-write, WAIT AND RETRY — do not "fix" forward.jl.
6. Do NOT re-run the full `calibrate()` sweep to regenerate ghat.jl (it would re-fit and change the
   frozen knots, violating #4). ghat.jl's header is hand-patched with the already-measured numbers.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Measure the real anchor BOTH ways and derive neg_reachable from the masked one</name>
  <files>spike/simulator/calibration.jl</files>
  <action>
Rewrite `_real_anchor()` (lines 156-164) so each fixture is measured twice — once unmasked (what
`patch_summary` and the v2.0 training/inference path see) and once masked (what the v1.0 analysis
pipeline sees) — and so each measurement carries its surviving-patch count.

Keep the faithful `load_tiff` extraction exactly as it is (D-16: NOT an RGB→luminance reduction).
For each condition:
  - load `<cond>_c1.tif` / `<cond>_c2.tif`, build the MCI via `build_mci` with `name = cond`;
  - measure UNMASKED first: `induced_mu(mci)` and the surviving-patch count;
  - then mask that same MCI in place with `_apply_mask!(mci, _calculate_mask(mci))`, mirroring
    `src/utils.jl:86` verbatim, and measure again.
  The unmasked measurement MUST be taken BEFORE the in-place mask (`_apply_mask!` mutates
  `mci.data`). Order is load-bearing — state that in a comment. The mask uses the thresholds
  `build_mci` already stored, so this is the v1.0 semantics, not a re-thresholding.

Add a small local helper that returns `(mu, n)` for a given MCI, where `n` counts the 8x8 summary
entries that are non-`missing` AND `isfinite` — i.e. exactly the patches entering the mean (total
is 64). Do not reimplement `patch_summary`; call it.

Return shape: a NamedTuple per fixture, e.g.
  `(positive = (unmasked_mu, unmasked_n, masked_mu, masked_n), negative = (...))`.

Then update the three consumers in `calibrate()`:
  - the `catch` fallback (lines 234-239) must produce the SAME nested shape with `NaN` μ values and
    `0` counts, so downstream interpolation cannot throw on a load failure;
  - `neg_reachable` (line 240) becomes
    `isfinite(anchor.negative.masked_mu) && anchor.negative.masked_mu < 0.0` — derived from the
    MASKED measurement, because that is the one comparable to the v1.0 analysis pipeline. Add a
    comment stating that this predicate is NOT by itself evidence of physical reachability (see
    the two-sided bias recorded in Task 2);
  - the returned NamedTuple (lines 242-244) keeps `anchor` and `neg_reachable` — no field renames
    beyond the anchor's new nested shape.

Do not touch the sweep, the PAVA fit, the W1/KS metric, size-invariance, or the σ/τ/ν diagnostics.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && julia --project=spike -e 'include("spike/simulator/calibration.jl"); a = _real_anchor(); println(a); @assert isfinite(a.positive.unmasked_mu) && isfinite(a.positive.masked_mu) && isfinite(a.negative.unmasked_mu) && isfinite(a.negative.masked_mu); @assert a.positive.unmasked_n == 64 && a.negative.unmasked_n == 64; @assert 0 < a.positive.masked_n < 64 && 0 < a.negative.masked_n < 64; @assert a.positive.masked_mu < 0.0 < a.positive.unmasked_mu; println("REAL-ANCHOR OK")'</automated>
  </verify>
  <done>`_real_anchor()` returns four μ values and four patch counts; the printed values reproduce
  positive ≈ 0.3292 (64) / −0.0729 (≈21) and negative ≈ 0.2481 (64) / 0.1149 (≈20); `neg_reachable`
  reads `anchor.negative.masked_mu`; the assertion script prints REAL-ANCHOR OK. (The script
  `include`s the file, so the `PROGRAM_FILE` guard means no sweep runs.)</done>
</task>

<task type="auto">
  <name>Task 2: Carry both measurements and the two-sided bias into the emit template and printed report</name>
  <files>spike/simulator/calibration.jl</files>
  <action>
Update `emit_ghat`'s template (lines 290-291) and the script-entry printed report (lines 342-343) so
both estimators, both patch counts, and the two-sided bias are recorded — not one silently-chosen
number.

In the `emit_ghat` template replace the single `real anchor (D-16)` line and the `negative tail`
ternary with a block interpolating `r.anchor.*` and `r.neg_reachable` that says, in substance:

  real anchor (D-16): faithful LoadImages.jl load_tiff (NOT luminance), measured BOTH ways
    UNMASKED (what patch_summary / the v2.0 training + inference path sees — no Otsu mask):
      positive mu = <..> (<n>/64 patches), negative mu = <..> (<n>/64 patches)
    MASKED (the v1.0 analysis pipeline: per-channel Otsu _apply_mask! then _exclude_zero):
      positive mu = <..> (<n>/64 patches), negative mu = <..> (<n>/64 patches)
  anchor caveat: the two estimators disagree in SIGN and in ORDER, and BOTH are biased —
    - UNMASKED is biased POSITIVE: background pixels are dark in both channels and co-vary.
    - MASKED is biased NEGATIVE: each channel is thresholded independently and _exclude_zero
      then keeps only pixels bright in BOTH — a selection on both variables, which restricts
      the joint range and induces spurious negative correlation. This is the known weakness of
      thresholded Pearson (the reason Manders/Costes exist). Masking also leaves only ~20 of
      64 patches above the >=15-survivor floor.
  negative tail: neg_reachable (masked negative-fixture mu < 0) = <r.neg_reachable>.
    NOT ESTABLISHED either way — n = 2 fixtures and two estimators biased in OPPOSITE directions
    cannot settle whether a negative induced-mu tail is physically reachable. The former
    unqualified "physically reachable = false => PRIOR-ONLY" rested on the unmasked numbers alone
    and is WITHDRAWN. The SIM-02 consistency claim stays scoped to the realized range
    [GHAT_MU_MIN, GHAT_MU_MAX]; that scoping is unchanged and does not depend on this anchor.

Mirror the same content (compactly — the four numbers, the four counts, both bias directions, and
the NOT-ESTABLISHED verdict) in the `println` report at lines 342-343.

Julia string gotchas in this template: it is a `"""` literal, so every literal `$` must be escaped
and interior `"""` stays written as `\"\"\"`. Keep every existing evidence line (generator, sweep,
induced μ range, monotonicity, SIM-02 match, size-invariance, σ/τ, ν) byte-identical — only the
anchor and negative-tail lines change. Do NOT alter the `GHAT_*` const emission or the emitted
`ghat()` body.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && julia --project=spike -e 'include("spike/simulator/calibration.jl"); a = _real_anchor(); r = (; n_grid=25, n_per=100, imsize=(512,512), mu_min=-0.67976, mu_max=0.847149, spearman=1.0, w1=0.05228, ks=0.10123, induced_n=300, si_maxdev=0.0318, diag=(pooled_sd=0.3169, kurtosis=-0.0518), sigma_ok=true, anchor=a, neg_reachable=(isfinite(a.negative.masked_mu) && a.negative.masked_mu < 0.0), mu_knots=GHAT_MU_KNOTS_STUB(), rho_knots=[0.0]); p = emit_ghat(r; path=joinpath(tempdir(), "ghat_probe.jl")); s = read(p, String); for t in ["UNMASKED", "MASKED", "0.3292", "0.2481", "-0.0729", "0.1149", "NOT ESTABLISHED", "Manders"]; @assert occursin(t, s) "missing: $t"; end; println("EMIT-TEMPLATE OK")' 2>&1 | tail -5</automated>
  </verify>
  <done>The emit template renders without a string/interpolation error and the rendered text contains
  all four μ values, both patch-count pairs, both bias directions, and the NOT-ESTABLISHED verdict;
  the probe writes to a temp path, never to `spike/simulator/ghat.jl`. Note: the verify command above
  is a sketch — define whatever minimal stub `r` the current `emit_ghat` signature needs (a
  `mu_knots`/`rho_knots` vector literal is fine; there is no `GHAT_MU_KNOTS_STUB` function) and keep
  the `path=` argument pointed at `tempdir()`. Running `calibration.jl` as a script is NOT permitted
  here — that would re-fit the knots.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-patch the frozen ghat.jl provenance header (comment only, knots untouched)</name>
  <files>spike/simulator/ghat.jl</files>
  <action>
Replace lines 39-40 of `spike/simulator/ghat.jl` with the corrected anchor block, using the numbers
already measured (and re-confirmed by Task 1's verify run):

  UNMASKED: positive μ = 0.3292 (64/64), negative μ = 0.2481 (64/64)
  MASKED:   positive μ = −0.0729 (21/64), negative μ = 0.1149 (20/64)

Use the exact same wording produced by the Task-2 template (copy it, so a future regeneration is a
no-op on this block) — including both bias directions and the NOT-ESTABLISHED / WITHDRAWN verdict.
If Task 1's run reports masked patch counts that differ from 21/20, use the RUN's numbers, not these.

Because the file header says "AUTO-GENERATED — DO NOT EDIT BY HAND", add one short provenance line
directly under that banner recording that the anchor block was hand-patched on 2026-07-25, why
(re-running `calibration.jl` would re-fit and change the frozen knots, which is forbidden), and that
the knots and `ghat()` are byte-identical to the generated artifact.

Change NOTHING else: `GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX` and the
`ghat()` body stay byte-identical.

OUT OF SCOPE, but record in the SUMMARY as follow-ups (do NOT edit these files):
`spike/NOTES.md` §3 "Real anchor (D-16)", `spike/test/test_simulator.jl:225`, and
`.planning/phases/02-.../02-03-SUMMARY.md` still repeat the withdrawn PRIOR-ONLY reading.
  </action>
  <verify>
    <automated>cd "C:/Users/Manuel/Documents/GitHub/ProteinCoLoc" && ! git diff -U0 -- spike/simulator/ghat.jl | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*#' && julia --project=spike -e 'include("spike/simulator/ghat.jl"); @assert GHAT_MU_MIN == -0.67976 && GHAT_MU_MAX == 0.847149 && length(GHAT_MU_KNOTS) == 25 && length(GHAT_RHO_KNOTS) == 25 && ghat(0.0) isa Float64; s = read("spike/simulator/ghat.jl", String); for t in ["UNMASKED", "MASKED", "0.3292", "0.2481", "-0.0729", "0.1149", "NOT ESTABLISHED", "WITHDRAWN"]; @assert occursin(t, s) "missing: $t"; end; @assert !occursin("physically reachable in real fluorescence = false", s); println("GHAT-HEADER OK")'</automated>
  </verify>
  <done>`git diff` on ghat.jl shows ONLY comment-line changes (no non-`#` line added or removed);
  the file still loads with all four `GHAT_*` constants at their original values and 25 knots; the
  header carries both measurements, both patch-count pairs, both bias directions, and the
  NOT-ESTABLISHED/WITHDRAWN verdict; the old unqualified reachability sentence is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike/ → src/ | read-only include() coupling; the spike may CALL src functions, never modify them |
| calibration.jl → ghat.jl | generated-artifact boundary; the frozen fit must not drift from a diagnostic edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vl8-01 | Tampering | spike/simulator/ghat.jl frozen knots | mitigate | Task 3 verify asserts the git diff contains only comment lines and the four `GHAT_*` constants still hold their original values |
| T-vl8-02 | Tampering | shipped v2.0 summary/inference path | mitigate | constraints #1/#2 forbid touching `contract.jl`, `src/`, `src/amortized/summary.jl`; `files_modified` is exactly two spike files |
| T-vl8-03 | Repudiation | provenance of a hand-edited auto-generated file | mitigate | Task 3 adds an explicit hand-patch provenance line under the AUTO-GENERATED banner |
| T-vl8-04 | Tampering | concurrent Phase-11 edits to forward.jl/prior.jl/simulator.jl | accept | different files; on a mid-write include failure, wait and retry rather than editing them |

No package-manager installs in this plan; no legitimacy gate required.
</threat_model>

<verification>
1. `_real_anchor()` emits four μ values and four counts (Task 1 verify).
2. The emit template renders both sets plus the two-sided bias (Task 2 verify).
3. ghat.jl diff is comment-only and the frozen fit still loads (Task 3 verify).
4. `git status --porcelain src/ spike/contract.jl` is EMPTY — decoupling preserved.
5. `git status --porcelain spike/simulator/` shows changes only to `calibration.jl` and `ghat.jl`
   (any `prior.jl`/`forward.jl`/`simulator.jl` entries belong to the concurrent Phase-11 executor
   and MUST NOT be staged or committed by this plan).
</verification>

<success_criteria>
- The real-anchor diagnostic reports masked AND unmasked μ with surviving-patch counts per fixture.
- `neg_reachable` is computed from the masked negative-fixture μ.
- Both the generated provenance comment and the printed report carry all four numbers and counts.
- ghat.jl records the two-sided bias (unmasked → positive bias from co-varying dark background;
  masked → negative bias from double-thresholded range restriction) and states that negative-tail
  reachability is NOT ESTABLISHED by either estimator on n=2 fixtures.
- The frozen fit (`GHAT_MU_KNOTS`, `GHAT_RHO_KNOTS`, `GHAT_MU_MIN`, `GHAT_MU_MAX`, `ghat()`) is
  byte-identical to HEAD.
- `src/` and `spike/contract.jl` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260725-vl8-fix-the-real-anchor-diagnostic-in-spike-/260725-vl8-SUMMARY.md` when done.
Record in it: the four measured μ values with counts, the `neg_reachable` value, and the three
out-of-scope files that still repeat the withdrawn PRIOR-ONLY reading (`spike/NOTES.md` §3,
`spike/test/test_simulator.jl:225`, `.planning/phases/02-forward-simulator-summary-contract/02-03-SUMMARY.md`).
</output>

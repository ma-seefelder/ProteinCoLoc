---
phase: 02-forward-simulator-summary-contract
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - spike/02_simulator_demo.jl
  - spike/test/test_simulator.jl
  - spike/figures/plausibility.png
  - spike/figures/plausibility.pdf
autonomous: true
requirements: [SIM-04]
must_haves:
  truths:
    - "A ρ_true sweep shows monotone ρ_true↑ → mean patch-correlation↑ with Spearman ≥ threshold (SIM-04a, D-12)"
    - "Increasing spillover measurably raises, and increasing |sub-pixel shift| measurably lowers, mean patch-correlation beyond Monte-Carlo noise (SIM-04b, D-12)"
    - "Output verifies as a valid MultiChannelImage across the perturbation runs (SIM-04c / SIM-03)"
    - "CairoMakie plausibility figures (image panels, sweep curve, induced-μ overlay, perturbation panels) are saved headless under spike/figures/ (D-13)"
  artifacts:
    - path: "spike/02_simulator_demo.jl"
      provides: "seeded runnable demo: sample_prior → simulate_pair → summary → CairoMakie figures"
      contains: "CairoMakie"
    - path: "spike/figures/plausibility.png"
      provides: "rendered plausibility evidence figure"
  key_links:
    - from: "spike/test/test_simulator.jl"
      to: "spike/simulator/forward.jl + prior.jl + contract.jl"
      via: "SIM-04 quantitative gate over sweep + perturbation"
      pattern: "corspearman"
    - from: "spike/02_simulator_demo.jl"
      to: "spike/figures/"
      via: "CairoMakie save()"
      pattern: "save\\("
---

<objective>
Close the phase with the quantitative plausibility gate (SIM-04) and the CairoMakie evidence figures (D-13). Prove three measurable sub-gates in the re-runnable harness: (a) monotone ρ_true↑ → mean patch-correlation↑ (Spearman ≥ threshold), (b) spillover raises and sub-pixel shift lowers mean patch-correlation beyond Monte-Carlo noise (paired tests), (c) output stays a valid MultiChannelImage. Render the static, headless plausibility figures under `spike/figures/`.

Purpose: A passing quantitative gate plus regenerated figures is the phase's falsifiable proof that the simulator behaves like real microscopy — the Phase-1 "green + correctness" philosophy applied to physics plausibility.
Output: `spike/02_simulator_demo.jl` (the seeded demo that generates figures), the SIM-04 testset, and `spike/figures/plausibility.{png,pdf}`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md
@.planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md
@.planning/phases/02-forward-simulator-summary-contract/02-CONTEXT.md
@.planning/phases/02-forward-simulator-summary-contract/02-VALIDATION.md
@spike/contract.jl
@spike/simulator/forward.jl
@spike/simulator/prior.jl
@spike/00_smoke.jl

<interfaces>
<!-- Everything the gate composes (from Waves 1-3). -->
```julia
# spike/contract.jl
summary(mci) -> 8×8 ρ ;  induced_mu(mci) -> Float64 ;  build_mci(data) -> MultiChannelImage
# spike/simulator/forward.jl
simulate_pair(rng, θ; imsize) -> Vector{Matrix{Float64}}      # θ = 7-field NamedTuple
# spike/simulator/prior.jl
sample_prior(rng) -> θ
```
Gate primitives (RESEARCH §Plausibility Gate): `StatsBase.corspearman(ρ_grid, mean_patch_corr) ≥ threshold` (default 0.95, lower to ~0.9 if flaky — A4/discretion); paired spillover/shift effect beyond MC noise (paired t / sign test over seeds).
CairoMakie recipe (RESEARCH §Plausibility Gate, [ASSUMED — verify save() on Windows): `CairoMakie.activate!()` (headless, no OpenGL); `Figure`; `heatmap`/`scatterlines`/`hist`; `save(joinpath(@__DIR__,"figures","plausibility.png"), fig)` and `.pdf`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded plausibility demo + CairoMakie figures</name>
  <files>spike/02_simulator_demo.jl, spike/figures/plausibility.png, spike/figures/plausibility.pdf</files>
  <read_first>
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Plausibility Gate (CairoMakie figure recipe; the three sub-gates), Open Question 3 (optional real-image μ anchor)
    - .planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md §`spike/02_simulator_demo.jl` (offline script shape; AGPL header; explicit rng), §`spike/figures/*` (no in-repo analog — CairoMakie verbatim recipe)
    - 02-CONTEXT.md D-13 (CairoMakie, spike-local only), D-12 (quantitative gate)
    - spike/00_smoke.jl (script + correctness-assert idiom)
  </read_first>
  <action>
    Create `spike/02_simulator_demo.jl` (AGPL header). After `using CairoMakie, StatsBase, Statistics, Random` and `include`s of contract.jl + simulator/forward.jl + simulator/prior.jl, call `CairoMakie.activate!()` for headless rendering. With a fixed `rng = Random.Xoshiro(2026)` (D-14), compute the evidence series: a ρ_true sweep (≥15 points, nuisances at prior medians to isolate ρ) → `mean(skipmissing(summary(build_mci(simulate_pair(...)))))` per point; a spillover sweep and a |shift| sweep at fixed ρ_true; and the induced-μ sample under `sample_prior`. Build a `Figure` with: image `heatmap` panels for a low-ρ and a high-ρ pair (both channels), the ρ_true→mean-patch-corr `scatterlines` (monotonicity), the induced-μ `hist` overlaid on the `Truncated(Cauchy(0,0.3),-1,1)` density, and `scatterlines` for the spillover and shift effects. `save` to `spike/figures/plausibility.png` (raster) and `.pdf` (vector); create `spike/figures/` if absent. After saving, have the SCRIPT self-assert its outputs exist (the Phase-1 `00_smoke.jl` correctness-gate idiom): `@assert isfile(joinpath(@__DIR__, "figures", "plausibility.png"))` and `@assert isfile(joinpath(@__DIR__, "figures", "plausibility.pdf"))`. Keep the demo's sweep sizes modest (e.g. 512²) so it finishes in minutes on CPU. Optionally overlay the real `test/test_images/{positive,negative}` μ anchors if a pure-Julia TIFF read is trivial (TiffImages) — OPTIONAL sanity, not a gate.
  </action>
  <verify>
    <automated>julia --project=spike spike/02_simulator_demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/02_simulator_demo.jl` exits 0 with `CairoMakie.activate!()` (no OpenGL/display required)
    - The demo script self-asserts (`@assert isfile(...)`) that `spike/figures/plausibility.png` and `spike/figures/plausibility.pdf` exist; a missing figure fails the run (PowerShell-safe, no POSIX `test -f`)
    - The demo threads an explicit `Random.Xoshiro` rng (deterministic, CPU-only)
    - `git diff --quiet f581d95 -- src/ Project.toml Manifest.toml` exits 0
  </acceptance_criteria>
  <done>A seeded, headless demo regenerates the plausibility figures from the full sample→simulate→summary chain under spike/figures/.</done>
</task>

<task type="auto">
  <name>Task 2: SIM-04 quantitative gate (monotonicity + perturbation + valid image)</name>
  <files>spike/test/test_simulator.jl</files>
  <read_first>
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Plausibility Gate sub-gates (a)/(b)/(c) — exact assertions, thresholds, paired-effect method
    - .planning/phases/02-forward-simulator-summary-contract/02-VALIDATION.md §Per-Task Verification Map rows 2-04-01 / 2-04-02 and §Sampling Rate (quick subset vs full sweep)
    - 02-CONTEXT.md D-12 (the three measurable sub-gates), Claude's-discretion (Spearman threshold)
    - spike/test/test_simulator.jl (extend; keep SIM-01/02/03 testsets and Phase-1 guards intact)
  </read_first>
  <action>
    Extend `spike/test/test_simulator.jl` with `@testset "SIM-04 plausibility"`. Sub-gate (a): sweep ρ_true over ≥15 points (nuisances at prior medians, N sims each), compute mean patch-correlation per point through the real summary, and assert `StatsBase.corspearman(ρ_grid, mean_patch_corr) ≥ threshold` (default 0.95; if the clean sim under nuisance spread cannot clear it, lower to ~0.9 and record the chosen value — A4/D-12 discretion). Sub-gate (b): at fixed ρ_true, assert `mean_corr(spillover>0) − mean_corr(spillover=0) > δ_spill` and `mean_corr(|shift|=0) − mean_corr(|shift|=k) > δ_shift`, each as a PAIRED difference over shared seeds exceeding Monte-Carlo noise (paired t or sign test). Sub-gate (c): assert the perturbation runs still build a valid MultiChannelImage with ≤64 finite per-patch ρ (reuse contract helpers). Use a quick subset (small size, few points) for the per-commit run and gate the full sweep on the wave merge per the validation sampling rate. Run the full suite green before sign-off and re-assert decoupling.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - "SIM-04 plausibility" testset passes inside `julia --project=spike spike/test/runtests.jl` (exit 0): monotonicity Spearman ≥ chosen threshold, paired spillover/shift effects significant, valid MultiChannelImage
    - All prior testsets (SIM-01/02/03, Phase-1 CUDA/NE guards) still pass (full-suite green)
    - `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` exits 0
  </acceptance_criteria>
  <done>The SIM-04 quantitative plausibility gate is green across monotonicity, paired perturbation, and contract-validity, completing the phase's falsifiable proof with decoupling intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep/perturbation measurements → pass/fail gate | A flaky or threshold-gamed gate gives a false plausibility guarantee |
| CairoMakie render → spike/figures/ | Plotting stack must stay spike-local and headless; zero src/ risk |
| spike/ → src/ (include via contract.jl) | Read-only; baseline f581d95 must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-GATE | Tampering (gate integrity) | SIM-04 thresholds | mitigate | Monotonicity gate uses a fixed, documented Spearman threshold; perturbation effects asserted as PAIRED differences beyond MC noise (not single-seed flukes); chosen threshold recorded in the summary. |
| T-02-PLOT | Tampering | CairoMakie resolve / src/plot.jl | mitigate | CairoMakie is spike-local (D-13), headless `activate!()`, no GLMakie; the Wave-1 NeuralEstimators-pin guard already proved no downgrade; figures write only under spike/figures/. |
| T-02-DC | Tampering | src/ + root manifests | mitigate | Summary measured via contract.jl include() (read-only); `git diff --quiet f581d95` gate at sign-off. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | None — no package installs in this plan. |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` exits 0 (full suite: SIM-01/02/03/04 + Phase-1 guards)
- `julia --project=spike spike/02_simulator_demo.jl` exits 0 and writes spike/figures/plausibility.{png,pdf}
- `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` exits 0
</verification>

<success_criteria>
- SIM-04 quantitative gate green: monotone ρ_true→mean-patch-corr (Spearman ≥ threshold), measurable paired spillover/shift effects, valid MultiChannelImage
- Headless CairoMakie plausibility figures regenerated under spike/figures/ (D-13, spike-local)
- Decoupling invariant preserved across the full phase
</success_criteria>

<output>
Create `.planning/phases/02-forward-simulator-summary-contract/02-04-SUMMARY.md` when done
</output>

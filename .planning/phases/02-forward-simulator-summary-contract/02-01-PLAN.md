---
phase: 02-forward-simulator-summary-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/contract.jl
  - spike/test/runtests.jl
  - spike/test/test_simulator.jl
autonomous: true
requirements: [SIM-03]
must_haves:
  truths:
    - "Spike env gains StatsBase, Images, ImageFiltering, CairoMakie, HypothesisTests WITHOUT downgrading NeuralEstimators 0.2.1 or installing CUDA (resolve-risk gate)"
    - "src/colocalization.jl and src/LoadImages.jl include() cleanly into the spike env, read-only (Phase-1 D-01 coupling)"
    - "A synthetic 2-channel positive-intensity image builds a valid MultiChannelImage and yields ≤64 finite per-patch ρ through the UNCHANGED patch()/correlation() (SIM-03 contract proven)"
    - "Root is byte-identical to baseline f581d95 — Project.toml / Manifest.toml / src/ untouched (decoupling preserved)"
  artifacts:
    - path: "spike/contract.jl"
      provides: "include() coupling boundary + build_mci() + summary()/induced_mu() helpers calling src/ unchanged"
      contains: "include(joinpath(@__DIR__"
    - path: "spike/test/test_simulator.jl"
      provides: "SIM-03 contract testset (synthetic image), included from runtests.jl"
      contains: "@testset"
    - path: "spike/Manifest.toml"
      provides: "re-frozen reproducibility artifact with new deps; NeuralEstimators pinned 0.2.1, no top-level CUDA"
  key_links:
    - from: "spike/contract.jl"
      to: "src/colocalization.jl"
      via: "include() read-only"
      pattern: "include.*colocalization"
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_simulator.jl"
      via: "include()"
      pattern: "include.*test_simulator"
---

<objective>
Extend the isolated spike environment with the simulator/summary stack (StatsBase — mandatory before the `src/colocalization.jl` include — plus Images, ImageFiltering, CairoMakie, HypothesisTests), re-freeze the pinned Manifest, and establish the read-only `include()` contract boundary. Prove the SIM-03 summary contract end-to-end with a hand-built synthetic image BEFORE any physics exists, so the simulator in Wave 2 builds against a known-good contract rather than a moving target.

Purpose: De-risk the two Phase-1-style failure modes up front — (a) a plotting/imaging dep silently downgrading the pinned NeuralEstimators 0.2.1 (the exact Phase-1 GLMakie regression), and (b) the StatsBase-before-include trap — and lock the decoupling guarantee against baseline f581d95.
Output: Extended `spike/Project.toml` + re-frozen `spike/Manifest.toml`, `spike/contract.jl` (the include() coupling + MultiChannelImage builder + summary helpers), the `spike/test/test_simulator.jl` Wave-0 scaffold with the SIM-03 contract testset, and an extended `spike/test/runtests.jl` guard.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md
@.planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md
@.planning/phases/02-forward-simulator-summary-contract/02-CONTEXT.md
@spike/NOTES.md
@spike/Project.toml
@spike/test/runtests.jl

<interfaces>
<!-- Key src/ contracts the spike calls UNCHANGED. Extracted from source this session. Use directly — do not re-read or reimplement. -->

From src/LoadImages.jl (bare file, no module wrapper — names like Images must be in scope before include):
```julia
# struct field + basic constructor (lines 118-130)
mutable struct MultiChannelImage{T<:Union{Missing,Float64}, S<:AbstractString, F<:AbstractFloat, I<:Integer}
    data::Vector{Matrix{T}}; channels::Vector{S}; name::S; path::Vector{S}
    pixel_size::Tuple{I,I}; otsu_threshold::Vector{F}
end
# constructor asserts length(data)==length(channels)==length(otsu_threshold); I<:Int (pixel_size must be Tuple{Int,Int})
MultiChannelImage(data, channels, name, path, pixel_size, otsu_threshold)
# convenience constructor (line 444): pixel_size = size(data[1]); otsu_threshold = Images.otsu_threshold.(data)
mutable struct MultiChannelImageStack{T,S}  # MultiChannelImageStack(img::Vector, name::String)
```

From src/colocalization.jl (bare file; references corspearman/corkendall (StatsBase), mean/median/quantile (Statistics)):
```julia
patch(img::AbstractMatrix{T}, num_patches::Integer) where T<:Union{Float64,Missing}  # → Array{Union{Float64,Missing},4} shape (n,n,W÷n,H÷n); trims non-multiples
correlation(x::Array{T,4}, y::Array{T,4}; method::Symbol=:pearson) where T<:Union{Float64,Missing}  # → Matrix{Union{Float64,Missing}} (8×8)
# per patch: _exclude_zero(a,b) drops 0.0 / NaN / missing; length(a) <= 15 ⇒ missing else cor(a,b)
```

NOTE on _prepare_data: it lives in src/bayes.jl (line 164), which carries Turing/GLMakie module-level deps NOT in the lean spike env. Its body is `x,y = patch.([x,y],n); correlation(x,y)` then reshape+filter !ismissing. The lean path replicates the induced-μ as `mean(skipmissing(ρ))` from patch()/correlation() — the load-bearing summary functions — so the SIM-03 contract is honored without dragging Turing into the isolated env. This is decoupling-faithful, not scope reduction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend spike env, re-freeze Manifest, harden the resolve-risk + CUDA + decoupling guard</name>
  <files>spike/Project.toml, spike/Manifest.toml, spike/test/runtests.jl</files>
  <read_first>
    - spike/Project.toml (current minimal deps — Distributions, Flux, NeuralEstimators)
    - spike/test/runtests.jl (Phase-1 CUDA-absence guard at lines 52-59 — PRESERVE verbatim, extend)
    - spike/NOTES.md §2 (pinned versions: NeuralEstimators 0.2.1, Flux 0.16.10, Distributions 0.25.128; CPU-only guarantee) and §5 (decoupling proof command vs f581d95)
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Standard Stack, §Package Legitimacy Audit, §Resolve-risk gate, Pitfall 3
  </read_first>
  <action>
    Run `julia --project=spike -e 'using Pkg; Pkg.add(["StatsBase","Images","ImageFiltering","CairoMakie","HypothesisTests"])'` (Statistics and Random are stdlib — do NOT add; Distributions already present). Let the resolver pick a current CairoMakie (Makie 0.24 series, which NeuralEstimators 0.2.1 declares as its weakdep compat). Then `Pkg.instantiate()` and commit the re-resolved `spike/Manifest.toml` as the refreshed reproducibility artifact.
    Extend `spike/test/runtests.jl`: keep the `"D-04 CPU-only (no CUDA dependency, none loaded)"` testset checks (a)/(b)/(c) UNCHANGED; add a sibling `@test` in that block asserting NeuralEstimators is still exactly `v"0.2.1"` via `Pkg.dependencies()` keyed by its UUID `38f6df31-6b4a-4144-b2af-7ace2da57606` (the resolve-risk gate from RESEARCH — if CairoMakie/Images dragged a downgrade this fails loudly). Append `include(joinpath(@__DIR__, "test_simulator.jl"))` at the end of the top-level testset so the Wave-0 scaffold (Task 2) runs in the same harness.
    Re-run the Phase-1 smoke as a regression: `julia --project=spike spike/test/runtests.jl` must still recover the toy posterior within tol. Finally assert decoupling: from repo root `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` must exit 0 — all adds touch ONLY spike/. If CairoMakie forces a NeuralEstimators downgrade despite the Makie 0.24 alignment, do NOT proceed: isolate plotting into a separate Julia env/process reading sim outputs from disk (RESEARCH documented fallback) and record the deviation in the summary.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/runtests.jl` exits 0 (Phase-1 smoke green + CUDA-absence + NeuralEstimators==v0.2.1 assertions pass)
    - `julia --project=spike -e 'using Pkg; v=Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version; @assert v==v"0.2.1" v'` exits 0
    - `julia --project=spike -e 'using Pkg; @assert !any(p->occursin("CUDA",p.name), values(Pkg.dependencies()))'` exits 0
    - `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` exits 0 (root untouched)
    - `spike/Project.toml` [deps] now lists StatsBase, Images, ImageFiltering, CairoMakie, HypothesisTests
  </acceptance_criteria>
  <done>Env extended and re-frozen; NeuralEstimators stays 0.2.1, no CUDA installed; Phase-1 smoke still green; root byte-identical to f581d95.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the include() contract boundary and prove SIM-03 on a synthetic image</name>
  <files>spike/contract.jl, spike/test/test_simulator.jl, spike/test/runtests.jl</files>
  <read_first>
    - spike/00_smoke.jl lines 1-19 (verbatim AGPL header block to copy) and the `@assert ... "msg"` correctness-gate idiom
    - .planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md §`spike/contract.jl` (include() recipe, MultiChannelImage construction, summary call sequence)
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Summary Contract (VERIFIED data flow + Hard requirements table + end-to-end contract check)
    - The <interfaces> block above (patch/correlation/MultiChannelImage signatures)
    - spike/NOTES.md §4 (StatsBase+Statistics must load before the include)
  </read_first>
  <behavior>
    - build_mci(data::Vector{Matrix{Float64}}) with 2 channels returns a valid MultiChannelImage: pixel_size == size(data[1]), 2 otsu thresholds, constructor asserts pass
    - summary(mci) returns an 8×8 Matrix{Union{Float64,Missing}} via patch.()+correlation() UNCHANGED
    - induced_mu(mci) == mean(skipmissing(summary(mci))), a finite Float64
    - For a synthetic 2-channel positive-intensity correlated image (no hard zeros), the 8×8 ρ has ≤64 finite entries and few/zero missing (background-not-zero trap respected)
    - A construction with a hard-zero background region produces many missing entries (negative control proving the trap matters)
  </behavior>
  <action>
    Create `spike/contract.jl` opening with the verbatim AGPL header. After `using StatsBase, Statistics, Images` (names required in scope before the bare-file includes), `include(joinpath(@__DIR__, "..", "src", "LoadImages.jl"))` then `include(joinpath(@__DIR__, "..", "src", "colocalization.jl"))` — read-only, never edit src/. Define `build_mci(data; name="sim")` mirroring src/LoadImages.jl:444-454 — set `pixel_size = size(data[1])` (the (W,H) pixel tuple per D-11, NOT micrometers), `otsu_threshold = Images.otsu_threshold.(data)`, channels `["ch1","ch2"]`, path `["",""]`. Define `summary(mci)` that pulls `x=mci.data[1]; y=mci.data[2]`, calls `x,y = patch.([x,y], 8)` and `correlation(x, y; method=:pearson)` — the UNCHANGED src functions — returning the 8×8 ρ. Define `induced_mu(mci) = mean(skipmissing(summary(mci)))`. Document inline that this replicates `src/bayes.jl::_prepare_data`'s reshape/filter without including bayes.jl (Turing/GLMakie not in the lean env).
    Create `spike/test/test_simulator.jl` opening with the AGPL header, `using Test`, then `include(joinpath(@__DIR__, "..", "contract.jl"))`. Add `@testset "SIM-03 summary contract" begin ... end` that builds a synthetic 2-channel positive correlated image (e.g. a shared smoothed positive field per channel, all intensities strictly > 0, size 256² for speed), asserts `build_mci` returns a valid MultiChannelImage with `pixel_size == size(data[1])`, asserts the 8×8 ρ has ≤64 entries all finite where present and `count(ismissing, ρ)` is small (background-not-zero trap), and asserts `isfinite(induced_mu(mci))`. Add a negative-control sub-assertion that a hard-zero background yields materially more `missing` entries.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/contract.jl` contains `include(joinpath(@__DIR__, "..", "src", "colocalization.jl"))` and `include(joinpath(@__DIR__, "..", "src", "LoadImages.jl"))`
    - The "SIM-03 summary contract" testset passes inside `julia --project=spike spike/test/runtests.jl` (exit 0)
    - `git diff --quiet f581d95 -- src/` exits 0 (src untouched by the include)
    - No reimplementation of patch/correlation exists in spike/ (grep `function patch` and `function correlation` over spike/ returns nothing): `! grep -rn -E 'function (patch|correlation)\b' spike/`
  </acceptance_criteria>
  <done>contract.jl couples to src/ read-only and the SIM-03 contract is proven on a synthetic image with the background-not-zero and ≥15-px-floor traps respected; the simulator can now target a known-good contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Julia General registry → spike env | New package versions resolved into the isolated environment |
| spike/ → src/ (include) | Read-only coupling; src/ and root manifests are the frozen publication baseline f581d95 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | new spike deps (StatsBase/Images/ImageFiltering/CairoMakie/HypothesisTests) | mitigate | All are mature JuliaStats/JuliaImages/Makie General-registry packages (RESEARCH Package Legitimacy Audit: all Approved, no [ASSUMED]/[SUS]/[SLOP]; slopcheck N/A for Julia). Pin exact versions in the re-frozen committed Manifest; assert post-resolve NeuralEstimators stays v0.2.1 and no CUDA node — automated in runtests.jl. |
| T-02-DC | Tampering | src/ + root Project.toml/Manifest.toml | mitigate | include() is read-only; `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` gate run at end of both tasks. |
| T-02-RD | Tampering | NeuralEstimators pin (resolve downgrade) | mitigate | Use current CairoMakie (Makie 0.24, NE-aligned weakdep); automated `Pkg.dependencies()` version==v"0.2.1" assertion; documented fallback to isolate plotting in a separate process if downgrade occurs. |
| T-02-SC2 | Tampering | npm/pip/cargo installs | accept | None — pure Julia/General-registry; no npm/pip/cargo in this phase. |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` exits 0 (Phase-1 smoke + CUDA-absence + NeuralEstimators-pin + SIM-03 contract)
- `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` exits 0
- spike/Manifest.toml re-frozen and committed; NeuralEstimators 0.2.1, no top-level CUDA
</verification>

<success_criteria>
- Spike env extended with the full simulator/summary stack without disturbing the pinned NeuralEstimators 0.2.1 / CPU-only contract
- The SIM-03 summary contract is proven end-to-end on a synthetic image via the UNCHANGED src/ patch()/correlation()
- Decoupling invariant (root byte-identical to f581d95) holds
</success_criteria>

<output>
Create `.planning/phases/02-forward-simulator-summary-contract/02-01-SUMMARY.md` when done
</output>

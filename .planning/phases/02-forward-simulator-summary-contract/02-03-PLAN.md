---
phase: 02-forward-simulator-summary-contract
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - spike/simulator/calibration.jl
  - spike/simulator/ghat.jl
  - spike/simulator/prior.jl
  - spike/NOTES.md
  - spike/test/test_simulator.jl
autonomous: true
requirements: [SIM-02]
must_haves:
  truths:
    - "An offline ρ_true sweep (≥25 points) over a span extending slightly past ±0.9 (e.g. [−0.95, 0.95], widening toward [−0.99, 0.99] if needed) measures induced μ through the REAL patch()/correlation() summary and fits a monotone inverse ĝ : μ ↦ ρ_true, with induced-μ size-invariance verified across the sweep set {256²,512²,1024²,1376×1028 anchor,2048²} (SIM-02, D-02, D-08, D-09)"
    - "The calibration runs on the spike-validated shared-latent correlated smooth Gaussian-field generator from forward.jl, and the knob span extends past ±0.9 because that generator induces only μ≈±0.76 at knob ±0.9 post-degrade — the extension lets ĝ cover the Turing μ-prior tails (D-15)"
    - "sample_prior(rng) draws μ*~Truncated(Cauchy(0,0.3),-1,1), sets ρ_true=ĝ(μ*), and draws the 6 nuisances from their priors (SIM-02, D-01)"
    - "The induced-μ distribution under sample_prior matches the Turing μ-prior within a pre-declared KS/Wasserstein tolerance"
    - "The real-image μ anchor extracts true per-channel signal via the LoadImages.jl convention (the package's own loader, NOT an RGB→luminance reduction), and NOTES §3 records whether the negative μ-prior tail is physically reachable in real fluorescence — documenting it as prior-only if not (D-16)"
    - "Induced σ/τ/ν are checked for consistency with the Turing scale/tail hyperpriors and ĝ + evidence documented in spike/NOTES.md §3 (D-02, D-03)"
  artifacts:
    - path: "spike/simulator/calibration.jl"
      provides: "offline ρ_true→induced-μ sweep (shared-latent generator, span past ±0.9) + monotone ĝ fit + KS/Wasserstein evidence + size-invariance check + faithful real-anchor extraction"
      contains: "corspearman"
    - path: "spike/simulator/prior.jl"
      provides: "MU_PRIOR const + sample_prior(rng) using frozen ĝ"
      contains: "Truncated(Cauchy(0.0, 0.3), -1.0, 1.0)"
    - path: "spike/simulator/ghat.jl"
      provides: "frozen ĝ knots/coefficients emitted by calibration, included by prior.jl"
  key_links:
    - from: "spike/simulator/calibration.jl"
      to: "spike/contract.jl"
      via: "induced_mu measured through the real summary"
      pattern: "induced_mu|summary"
    - from: "spike/simulator/prior.jl"
      to: "spike/simulator/ghat.jl"
      via: "include() frozen calibration map"
      pattern: "include.*ghat"
---

<objective>
Make the simulator prior provably consistent with the Turing model (SIM-02). Run the offline induced-μ calibration (D-01/D-02) on the spike-validated shared-latent generator (D-15): sweep ρ_true over a span extending slightly past ±0.9 (so ĝ can reach the Turing μ-prior tails — the generator induces only μ≈±0.76 at knob ±0.9 post-degrade), measure the induced per-patch-correlation mean μ through the UNCHANGED summary, fit a monotone inverse ĝ : μ ↦ ρ_true, freeze it, and build `sample_prior(rng)` so the induced μ distribution matches the Turing μ-prior `Truncated(Cauchy(0,0.3),-1,1)`. Anchor against the real images by extracting per-channel signal faithfully (LoadImages.jl convention, not luminance — D-16) and record whether the negative μ tail is physically reachable. Document the fit, the evidence, the anchor finding, and the σ/τ/ν consistency in `spike/NOTES.md §3`.

Purpose: One generative prior must serve both the ADVI benchmark (which infers μ) and the NPE/BF validation (which infers ρ_true/Δρ). Asserting equal ranges is insufficient — the nonneg softplus mixing + PSF + noise attenuate ρ_true into μ nonlinearly, so the mapping must be measured and inverted.
Output: `spike/simulator/calibration.jl`, the frozen `spike/simulator/ghat.jl`, `spike/simulator/prior.jl`, NOTES §3 evidence, and the SIM-02 testset.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md
@.planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md
@.planning/phases/02-forward-simulator-summary-contract/02-CONTEXT.md
@spike/contract.jl
@spike/simulator/forward.jl
@spike/NOTES.md

<interfaces>
<!-- Turing target (src/bayes.jl ~274-291, verified) the prior must match. -->
```julia
mu    ~ Truncated(Cauchy(0.0, 0.3), -1.0, 1.0)   # SIM-02 calibration target (induced μ)
nu    ~ Exponential()                            # tail heaviness — induced & checked (D-03)
sigma ~ Truncated(Cauchy(0.1, 0.3), 1e-4, 1.0)   # per-patch-corr spread — induced & checked
tau   ~ Truncated(Cauchy(0.1, 0.3), 1e-4, 1.0)
```
From spike/contract.jl: `induced_mu(mci) -> Float64`, `summary(mci) -> 8×8 ρ`.
From spike/simulator/forward.jl: `simulate_pair(rng, θ; imsize)`, 7-field θ NamedTuple; STAGE 1 is the shared-latent correlated smooth Gaussian-field generator (D-15) — calibration sweeps θ.ρ_true into induced μ through exactly this generator.
Real anchor extraction (D-16): reuse the package's own loader path via the contract include() coupling on `test/test_images/{positive,negative}/*_c{1,2}.tif` (1376×1028, 16-bit) — NOT a hand-rolled RGB→luminance reduction; each `_cN.tif` channel is loaded the way `src/LoadImages.jl` (`load_tiff`) does.
Monotone-fit options (RESEARCH §Don't Hand-Roll): isotonic regression OR PCHIP/monotone cubic via Interpolations on (ρ_true, E[μ|ρ_true]); then invert for ĝ : μ ↦ ρ_true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline induced-μ sweep, monotone ĝ fit, real-anchor extraction, and consistency evidence</name>
  <files>spike/simulator/calibration.jl, spike/simulator/ghat.jl</files>
  <read_first>
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Induced-μ Calibration (the load-bearing method: sweep → fit ĝ → evidence), §Image-Size Sweep, Open Questions 1-2 (ρ_true span; calibrate at 512² then verify size-invariance), §Common Pitfalls 2 (ρ_true ≠ induced μ)
    - .planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md §`spike/simulator/calibration.jl` (offline-script shape; no in-repo analog for the fit)
    - 02-CONTEXT.md D-01, D-02 (calibration is a concrete measured step), D-03 (σ/τ/ν consistency), D-15 (shared-latent generator; ±0.76 induced-μ at knob ±0.9 — calibrate past ±0.9), D-16 (faithful LoadImages.jl anchor; negative-tail reachability)
    - src/LoadImages.jl §`load_tiff` (lines ~214-216) — the faithful per-channel extraction convention the anchor must follow (D-16), reached via the contract include() coupling
    - spike/contract.jl (induced_mu), spike/simulator/forward.jl (simulate_pair, shared-latent stage 1)
  </read_first>
  <action>
    Create `spike/simulator/calibration.jl` (AGPL header). Implement an offline `calibrate(rng; size=(512,512), n_grid=25, n_per=200)` that: sweeps `ρ_true` over a grid spanning slightly PAST ±0.9 — start at roughly [−0.95, 0.95] and widen toward [−0.99, 0.99] if the induced μ has not yet covered the Turing μ-prior tails (the D-15 spike showed the shared-latent generator induces only μ≈±0.76 at knob ±0.9 post-degrade, so the knob MUST extend past ±0.9 for ĝ to reach the tails — Open Question 1); at each grid point draws `n_per` pairs with the 6 NUISANCES sampled from their own priors (so induced μ reflects the real generative spread, not a single nuisance setting), runs `simulate_pair` → `build_mci` → `induced_mu`, and records `E[μ | ρ_true]` and the conditional spread. The sweep runs on forward.jl's shared-latent stage-1 generator (D-15) — do not re-implement the field generator here. Fit a MONOTONE map `(ρ_true, E[μ])` via isotonic regression or a PCHIP/monotone cubic (Interpolations) and invert to obtain `ĝ : μ ↦ ρ_true`. Emit the frozen ĝ as `spike/simulator/ghat.jl` — a plain Julia literal of knots/coefficients plus a `ghat(μ)` evaluator (no JLD2; that dep is Phase 3). After writing calibration.jl, run it with `julia --project=spike spike/simulator/calibration.jl` to generate spike/simulator/ghat.jl on disk, then commit ghat.jl as the frozen reproducibility artifact for this task.
    REAL-ANCHOR step (D-16): extract the real per-channel anchor signal FAITHFULLY by reusing the package's own loader path (the `src/LoadImages.jl` `load_tiff` convention, reached via the contract include() coupling) on `test/test_images/{positive,negative}/*_c{1,2}.tif` — NOT a hand-rolled RGB→luminance reduction. Run the positive and negative real pairs through the UNCHANGED summary to measure their real induced μ, and determine whether the NEGATIVE tail of the μ-prior is physically reachable in real fluorescence (the crude luminance proxy showed only a narrow moderate band ~0.25–0.35). Export this anchor finding (real positive/negative induced μ + reachability verdict) for Task 2 to record; if the negative tail is not physically realized, mark it as prior-only.
    Compute the SIM-02 pass metric: draw μ*~`Truncated(Cauchy(0,0.3),-1,1)`, set ρ_true=ĝ(μ*), simulate, measure the resulting induced-μ sample, and compute a one-sample KS (HypothesisTests `ExactOneSampleKSTest`) or Wasserstein-1 distance to the target with a PRE-DECLARED tolerance (declare it over the physically-realized μ range per D-16 if the negative tail is prior-only). Verify size-invariance: re-measure induced μ at a couple of other sweep sizes ({256², 1024²}) for a few ρ_true and assert the curve is stable (Pearson is sample-size-robust above the 15-px floor). Keep the sweep at 512² for cost.
    Also measure the induced per-patch-correlation SD (→ σ/τ) and tail heaviness (→ ν) under the prior and record whether they fall within / are consistent with the Turing scale and Exponential ranges (D-03 is a consistency CHECK, not a fit). Export the numeric results so Task 2 can write them into NOTES §3.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/simulator/ghat.jl` exists and defines a callable `ghat(μ)` with frozen knots/coefficients
    - The ρ_true sweep grid spans past ±0.9 (e.g. ≥|0.95| endpoints), reflecting the D-15 finding that knob ±0.9 only induces μ≈±0.76 post-degrade
    - The real anchor μ is measured via the faithful LoadImages.jl loader path (NOT RGB→luminance), and the negative-tail reachability verdict is exported for NOTES §3 (D-16)
    - ĝ is monotone over its μ domain (asserted in the SIM-02 testset: `corspearman` of (μ_grid, ĝ.(μ_grid)) == 1.0 or ≥ 0.999)
    - The induced-μ KS/Wasserstein distance to `Truncated(Cauchy(0,0.3),-1,1)` (over the physically-realized μ range per D-16) is below the pre-declared tolerance (asserted in the SIM-02 testset)
    - `git diff --quiet f581d95 -- src/ Project.toml Manifest.toml` exits 0
  </acceptance_criteria>
  <done>The induced-μ calibration is run end-to-end on the shared-latent generator through the real summary with the knob span extended past ±0.9, the monotone ĝ is fitted and frozen, the real anchor is extracted faithfully (LoadImages.jl, not luminance) with the negative-tail reachability recorded, the match metric clears the pre-declared bar, and size-invariance + σ/τ/ν consistency are measured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sample_prior with frozen ĝ, NOTES §3 evidence, and the SIM-02 gate</name>
  <files>spike/simulator/prior.jl, spike/NOTES.md, spike/test/test_simulator.jl</files>
  <read_first>
    - .planning/phases/02-forward-simulator-summary-contract/02-RESEARCH.md §Code Examples (prior skeleton: MU_PRIOR + sample_prior(rng, ĝ)), §Induced-μ Calibration step 4 (evidence artifacts)
    - .planning/phases/02-forward-simulator-summary-contract/02-PATTERNS.md §`spike/simulator/prior.jl` (consistency target; do NOT assert ρ_true == μ)
    - 02-CONTEXT.md D-15 (shared-latent generator, span past ±0.9), D-16 (faithful anchor + negative-tail note)
    - spike/NOTES.md §3 (pre-seeded Turing μ/ν/σ/τ ranges — append the calibration mapping + evidence here)
    - spike/simulator/ghat.jl + calibration.jl outputs from Task 1
  </read_first>
  <behavior>
    - sample_prior(Xoshiro(s)) returns the 7-field θ NamedTuple with ρ_true = ghat(μ*) for μ*~MU_PRIOR
    - The 6 nuisances are drawn from documented prior ranges (Claude's discretion, recorded in NOTES §3)
    - Over many draws, the induced-μ sample (via simulate→summary) is KS/Wasserstein-close to Truncated(Cauchy(0,0.3),-1,1) below the pre-declared tol (over the physically-realized μ range per D-16)
    - ghat is monotone; sample_prior is deterministic under a fixed rng
  </behavior>
  <action>
    Create `spike/simulator/prior.jl` (AGPL header). Define `const MU_PRIOR = Truncated(Cauchy(0.0, 0.3), -1.0, 1.0)` (the verbatim Turing μ-prior) and `include(joinpath(@__DIR__, "ghat.jl"))`. Implement `sample_prior(rng::AbstractRNG)` that draws `μ_star = rand(rng, MU_PRIOR)`, sets `ρ_true = ghat(μ_star)`, and draws the 6 nuisances (spillover, autofluorescence, label_efficiency, shift_dx, shift_dy, noise) from their prior ranges (discretion — choose ranges that, per Task 1's measurement, keep induced σ within the Turing scale prior; document the chosen ranges). Return the 7-field θ NamedTuple. Do NOT assert ρ_true == μ (Pitfall 2 / Anti-Pattern).
    Append to `spike/NOTES.md` a completed §3 documenting: the fitted ĝ (method + where the knots live), the ρ_true→E[μ] sweep summary and the chosen ρ_true span extending past ±0.9 (with the D-15 rationale that knob ±0.9 induces only μ≈±0.76 post-degrade), the calibration runs on the shared-latent generator (D-15), the induced-vs-target μ match metric (KS/Wasserstein value + pre-declared tolerance + pass) over the physically-realized μ range, the calibration size (512²) with the size-invariance result, the REAL-ANCHOR finding (D-16: real positive/negative induced μ extracted via the faithful LoadImages.jl loader not luminance, and whether the negative μ tail is physically reachable — documented as prior-only if not), the chosen nuisance ranges, and the σ/τ/ν consistency findings (D-03). This is the SIM-02 documentation deliverable.
    Extend `spike/test/test_simulator.jl`: `include` prior.jl, add `@testset "SIM-02 prior consistency"` asserting ĝ monotonicity and that the induced-μ sample from `sample_prior` (modest N at 256²/512² for speed) is within the pre-declared KS/Wasserstein tolerance of MU_PRIOR (over the physically-realized μ range per D-16).
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/simulator/prior.jl` contains `Truncated(Cauchy(0.0, 0.3), -1.0, 1.0)` and `function sample_prior(rng`
    - "SIM-02 prior consistency" testset passes inside `julia --project=spike spike/test/runtests.jl` (exit 0)
    - `spike/NOTES.md` §3 documents the ĝ map, the span-past-±0.9 rationale (D-15), the real-anchor finding + negative-tail reachability note (D-16), the match metric + tolerance, and the σ/τ/ν consistency check
    - `git diff --quiet f581d95 -- src/ Project.toml Manifest.toml` exits 0
  </acceptance_criteria>
  <done>sample_prior draws ρ_true through the frozen ĝ so the induced μ matches the Turing μ-prior within tolerance, with the mapping, the span-past-±0.9 rationale (D-15), and the faithful real-anchor + negative-tail finding (D-16) recorded in NOTES §3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibration sweep → frozen ĝ | A miscalibrated/non-monotone ĝ silently breaks prior consistency for all later phases |
| real anchor TIFs → induced-μ | A wrong channel-extraction (luminance instead of LoadImages convention) gives a false anchor and a misjudged negative-tail reachability |
| spike/ → src/ (include via contract.jl) | Read-only; baseline f581d95 must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CAL | Tampering (statistical integrity) | ĝ fit / induced-μ match | mitigate | Pre-declared KS/Wasserstein tolerance asserted in the SIM-02 testset; monotone fit enforced (isotonic/PCHIP) and asserted; tolerance fixed BEFORE the run, not tuned to pass (data-snooping guard, mirrors the Phase-5 pre-registration concern). |
| T-02-AN | Spoofing (anchor fidelity) | real-image channel extraction | mitigate | Extract anchor via the faithful LoadImages.jl loader path (D-16), NOT RGB→luminance; record the negative-tail reachability verdict so the D-01 consistency claim is scoped to the physically-realized μ range. |
| T-02-SI | Tampering | size-invariance assumption | mitigate | Explicit size-invariance re-measurement at {256²,1024²} so the 512² ĝ is not silently size-specific. |
| T-02-DC | Tampering | src/ + root manifests | mitigate | Summary + anchor measured via contract.jl include() (read-only); `git diff --quiet f581d95` gate. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | None — no package installs in this plan. |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` exits 0 (SIM-02 + all prior testsets + Phase-1 guards)
- spike/NOTES.md §3 completed with ĝ + span-past-±0.9 rationale (D-15) + faithful real-anchor + negative-tail note (D-16) + match metric + tolerance + σ/τ/ν consistency
- `git diff --quiet f581d95 -- Project.toml Manifest.toml src/` exits 0
</verification>

<success_criteria>
- The induced-μ distribution under sample_prior matches the Turing μ-prior within a pre-declared tolerance, fitted via a frozen monotone ĝ measured through the unchanged summary on the shared-latent generator (D-15), with the knob span extended past ±0.9 to reach the tails
- The real anchor is extracted faithfully (LoadImages.jl, not luminance) and the negative-tail reachability is recorded, scoping the D-01 consistency claim to the physically-realized μ range where needed (D-16)
- σ/τ/ν consistency checked and documented (D-03)
- Decoupling invariant preserved
</success_criteria>

<output>
Create `.planning/phases/02-forward-simulator-summary-contract/02-03-SUMMARY.md` when done
</output>

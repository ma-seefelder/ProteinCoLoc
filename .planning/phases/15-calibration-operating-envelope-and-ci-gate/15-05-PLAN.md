---
phase: 15-calibration-operating-envelope-and-ci-gate
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - test/gate/run_gate.jl
  - test/gate/test_p15_envelope_arm.jl
autonomous: true
requirements: [D-01, D-02a, D-05, D-10, D-11, D-13]

must_haves:
  truths:
    - "The sweep is a NEW ARM on the existing CLI (`run_gate.jl --envelope`), not a forked runner — it inherits `--consts`, `--artifacts-root`, the atomic report writer and the honest `:not_trained` / `:provenance_mismatch` statuses for free (D-01)"
    - "The arm is orchestration only: every statistic comes from `sbc_gate`, `sbc_coverage`, `sbc_uniformity` and `gate_ood_roc` as they already exist; no new statistical code is written (D-01)"
    - "`P15_FAMILIES` is passed through `gate_ood_roc`'s existing `families =` keyword; `OOD_FAMILIES` is never mutated (D-02a)"
    - "`seed_gate_global!(G)` is wired into every Phase-15 arm, so the flow's base draws — which `sampleposterior` takes from the GLOBAL RNG and not from the passed rng — ride the P15 stream and the run is reproducible (D-11)"
    - "The arm refuses to start below `P15_SWEEP_MIN_THREADS` and aborts with a recorded `:budget_exceeded` status past `P15_SWEEP_WALLCLOCK_CEILING_HOURS`, so the ~8.5 h budget is enforced rather than hoped for"
    - "Rung 0 is measured ONCE per net through the unmisspecified simulator and shared across all seven axes (D-13)"
    - "The report is written atomically to a NEW path; `gate_report_8.jld2` is never overwritten (D-10)"
  artifacts:
    - path: "test/gate/run_gate.jl"
      provides: "envelope_gate(...) + the --envelope CLI flag + --sweep-m / --axes overrides for the smoke path"
      contains: "function envelope_gate"
    - path: "test/gate/test_p15_envelope_arm.jl"
      provides: "Fixture-scale end-to-end proof of the arm with an injected fake simulator, plus the CLI parse and the thread/budget guards"
      min_lines: 120
  key_links:
    - from: "test/gate/run_gate.jl envelope_gate"
      to: "test/gate/sbc.jl sbc_gate"
      via: "sim = misspec_simulator(P15_FAMILIES[axis]; level = rung, G = G)"
      pattern: "misspec_simulator"
    - from: "test/gate/run_gate.jl envelope_gate"
      to: "test/gate/misspec.jl gate_ood_roc"
      via: "families = P15_FAMILIES, levels = P15_RUNGS keyword pass-through"
      pattern: "families = P15_FAMILIES"
    - from: "test/gate/run_gate.jl envelope_gate"
      to: "test/gate/harness.jl seed_gate_global!"
      via: "explicit call at the top of the arm"
      pattern: "seed_gate_global!"
---

<objective>
Add the sweep as one new arm on the existing gate CLI, and prove it end to end at fixture scale
before it is allowed to spend seven hours.

Purpose: this phase's novelty is the sweep and its pre-registration, not its machinery.
`misspec_simulator` already produces exactly the `(sample_prior, simulate_pair, build_mci)` contract
`sbc_gate` consumes, and `gate_ood_roc` already returns `fire_rate[fam][lvl]` alongside the measured
`id_fire_rate` — D-07's two inputs. So the new code is an orchestration loop, a break/OOD-crossing
reduction and a report writer. Every line of new statistical code here would be a defect: a second
ECE implementation makes the envelope incomparable to the shipped gate report.

Output: `envelope_gate` plus the `--envelope` flag in `test/gate/run_gate.jl`, and a fixture-scale
test that runs the whole arm in seconds with an injected fake simulator.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CONTEXT.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md

<interfaces>
<!-- Verified against test/gate/run_gate.jl, sbc.jl, misspec.jl, harness.jl this research session. -->

run_gate.jl structure to extend:
  :63-77   the guarded pre-registration include (`--consts FILE` wins) then harness / sbc / misspec
  :80-87   `_gate_grid_dir/_gate_npe_path/_gate_ratio_path/_gate_ood_path/_gate_report_path(root, G)`
           and `const GATE_REPORT_SCHEMA = 1`
  :97-106  `write_gate_report(path, report)` — `.tmp` -> reopen-integrity-@assert -> `mv(...; force=true)`
  :114-127 `read_gate_report(path)` and `_prev_arm(prev, k)` (the arm-preserving merge)
  :290-380 `ood_gate(m, ood_nulls; G, sim, rng, n, L, imsize, imsize_set, imsize_weights,
                     pos_sim, neg_sim, families = OOD_FAMILIES, levels = OOD_GRID_LEVELS,
                     n_pos = 40, negctrl_reps = 30)` — calls `seed_gate_global!(G)` at its top
  :404-497 `run_gate(grid; sbc, bf, ood, artifacts_root, sim, write_report, M, L, bins, chi2_bins,
                     imsize, imsize_set, imsize_weights, ood_n, require_provenance, pos_sim, neg_sim)`
           returns `(status = :not_trained, ...)` when the npe artifact is absent, and
           `(status = :provenance_mismatch, ...)` running NO arm when
           `assert_imsize_provenance` fails under `SBC_REQUIRE_IMSIZE_PROVENANCE`
  :500-534 `_parse_gate_args(args)` — `--grid` required, `--sbc/--bf/--ood` flags,
           `--artifacts-root PATH`, `--consts FILE` (consumed at include time, accepted and ignored here)
  :536-541 the `abspath(PROGRAM_FILE) == @__FILE__` entry point

sbc_gate(m; G, M, L, bins, chi2_bins, imsize, imsize_set, imsize_weights, sim, rng)
  -> NamedTuple with `.ranks` (M x 8 Int), `.per_param` (8 NamedTuples carrying
     label/ks_p/chi2_p/ece/mce/light/shrinkage/post_sd/prior_sd/vacuous),
     `.realised_imsize_counts`, `.ece_pass`, `.passed`, `.v1_verdict`, `.v2_verdict`
  `sbc_ranks_and_spread` calls `seed_gate_global!(G)` internally, so sbc_gate is self-seeding.

gate_ood_roc(m, density; G, rng, families, levels, n_id, n_pos, n_fit, imsize, imsize_set,
             imsize_weights) -> NamedTuple including fire_rate::Dict{Symbol,Vector{Float64}},
             id_fire_rate::Float64, fused_auc, density_auc, noise_auc, combined_auc,
             id_threshold, fused_threshold, imsizes
  It is NOT self-seeding — `ood_gate` calls `seed_gate_global!(G)` before it. A direct caller must too.

misspec_simulator(gen; level, G) -> (sample_prior, simulate_pair, build_mci)  (misspec.jl:194-199)
default_simulator() -> the promoted ProteinCoLoc chain (harness.jl) — the RUNG-0 simulator

harness.jl:50-59 caveat: `posterior_for` -> `NeuralEstimators.sampleposterior` accepts NO rng; the
flow's base draws come from the GLOBAL RNG. The passed Philox drives only `sample_prior`,
`gate_imsize` and `simulate_pair`. `gate_global_seed(G)` derives from `prod_seed(G)`, which
`p15_consts.jl` aliases onto the P15 stream — so seeding rides P15 automatically once the arm calls
`seed_gate_global!(G)`.

Cost model to reconcile against (07-GATE-AMENDMENT.md:140-149 + this session's measurements):
  E[simulate cost] = 0.08346 s/pair at 32 threads over the SBC_IMSIZE_SET mixture
  E[patch_summary G=8] ~= 0.107 s ; posterior_for(N=999) = 0.028 s
  per SBC draw the harness does 3 simulate + 3 summary + 3 posterior => t_draw ~= 0.656 s
  predicted 29.4 min vs 30.9 min observed for the one reported gate run — reconciles within 5%
  misspecification generators cost ~1.3x simulate_pair (assumption A2, unmeasured)

Bars from p15_consts.jl: P15_SWEEP_M, P15_RUNGS, P15_RUNG0, P15_AXES, P15_SWEEP_MIN_THREADS,
P15_SWEEP_WALLCLOCK_CEILING_HOURS, P15_OOD_N_ID/N_FIT/N_POS, P15_ARTIFACTS_ROOT.
Reductions from p15_envelope.jl: p15_break_threshold_for, p15_axis_break, p15_ood_rung,
p15_axis_verdicts, p15_sc2_pass, p15_report_only.
</interfaces>

@test/gate/run_gate.jl
@test/gate/p15_envelope.jl
@test/gate/p15_misspec.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: envelope_gate — the orchestration loop, the budget guards and the report schema</name>
  <files>test/gate/run_gate.jl</files>
  <read_first>
    - test/gate/run_gate.jl (READ IN FULL — the include guard at :63-77, the atomic writer at :97-106, `ood_gate` at :290-380 for the arm shape to imitate, `run_gate` at :404-497 for the provenance assert and the arm-preserving merge)
    - test/gate/sbc.jl lines 420-496 (`sbc_gate`'s full return shape — every field the report will carry)
    - test/gate/misspec.jl lines 194-199 and 298-396 (`misspec_simulator` and `gate_ood_roc`)
    - test/gate/harness.jl lines 40-100 and 140-175 (the global-RNG caveat, `gate_imsize`)
    - test/gate/p15_envelope.jl and test/gate/p15_misspec.jl (the reductions and the family table this arm composes)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md §"Pattern 3", §"Compute Budget", §"Pitfall 5", §"Pitfall 7"
  </read_first>
  <action>
Extend `test/gate/run_gate.jl` — do not fork it. Add the two Phase-15 includes to the existing guarded
include block at :75-77, each guarded the same way: include `p15_misspec.jl` unless `P15_FAMILIES` is
defined, and `p15_envelope.jl` unless `p15_axis_verdict` is defined. Both must be tolerant of a
non-Phase-15 pre-registration being loaded (if `P15_SWEEP_M` is undefined, skip the includes and let
`--envelope` error with a clear message naming `--consts p15_consts.jl`). This keeps every existing
invocation of `run_gate.jl` byte-equivalent in behaviour.

Add `envelope_gate(m, ood_nulls; G, artifacts_root, rng_factory = () -> prod_rng(G), M = P15_SWEEP_M,
L = SBC_L, axes = P15_AXES, rungs = P15_RUNGS, families = P15_FAMILIES, imsize = SBC_IMSIZE,
imsize_set = GATE_IMSIZE_SET, imsize_weights = GATE_IMSIZE_WEIGHTS, ood_n_id = P15_OOD_N_ID,
ood_n_fit = P15_OOD_N_FIT, ood_n_pos = P15_OOD_N_POS, wallclock_ceiling_hours =
P15_SWEEP_WALLCLOCK_CEILING_HOURS, min_threads = P15_SWEEP_MIN_THREADS)`, modelled structurally on
`ood_gate`. Its body, in order:

 1. THREAD GUARD. `Threads.nthreads() >= min_threads` or `error(...)` naming the measured 13x
    single-thread penalty and the reconciled cost model — this is a budget error, not a style
    preference. Record `Threads.nthreads()` in the report.
 2. GLOBAL SEED — PINNED PER RUNG, NOT ONCE. Call `seed_gate_global!(G)` at the top of the arm and
    capture the returned seed into the report, with a comment citing `harness.jl:50-59`:
    `sampleposterior` takes its flow base draws from the GLOBAL RNG, so an arm that does not pin it
    produces numbers that are not reproducible — silently, and only discoverable by trying to re-run.
    Also record `prod_seed(G)` so the report names the stream it rode.

    **Then add `p15_rung_seed(G, axis, rung) -> UInt64` and re-pin the global RNG before EVERY rung.**
    Derivation, by the same counter-based rule the rest of the project uses: one `UInt64` draw from
    `Philox4x(UInt64, (PROD_SEED_P15, UInt64(2)))` mixed with a stable integer index for
    `(axis, rung)` — the axis index taken from `findfirst(==(axis), P15_AXES)` so it is positional
    and frozen, NOT from `hash(::Symbol)`, which is not stable across Julia versions.

    **Two reasons, and the second is the one that is easy to miss:**
    (a) *Resume correctness.* A single top-of-arm pin makes the global stream advance sequentially
    across rungs, so a resumed run restarts it at the beginning and produces DIFFERENT posterior
    draws for the resumed rungs than a continuous run would. Per-rung pinning makes every rung
    independently reconstructible, which is what makes checkpoint/resume byte-identical to an
    uninterrupted run rather than merely approximately equal.
    (b) *It makes the matched-pairs claim in item 4 TRUE.* Under a single top-level pin, only the
    prior draw and the forward simulation are matched across rungs; the flow base draws drift. Then
    "the same θ*, the same image sizes, the same latent fields; only the misspecification magnitude
    varies" is false for the posterior half. Per-rung pinning makes it hold end to end.
    Record `p15_rung_seed` per row so each rung names the stream it rode.

    This is a pre-registration-time design fix: nothing has run, so no number is being changed after
    the fact.
 3. RUNG 0, ONCE. One `sbc_gate` call with `sim = default_simulator()` and `rng = rng_factory()` at
    the sweep's M. Its per-column ECEs are the anchor; compute the gating thresholds immediately with
    `p15_break_threshold_for` for `P15_BREAK_COLUMNS` and freeze them into the report. Every axis
    shares this one rung-0 measurement (D-13).
 4. THE LADDER. For each `axis` in `axes` and each `rung` in `1:rungs`, one `sbc_gate` call with
    `sim = misspec_simulator(families[axis]; level = rung, G = G)` and a FRESH `rng_factory()` — so
    every rung starts from the same Philox counter and the ladder is matched-pairs on the in-prior
    axes (the same θ*, the same image sizes, the same latent fields; only the misspecification
    magnitude varies). Add the Pitfall-7 comment: cross-rung ECEs are therefore NOT independent
    replicates, which is the reason the ladder is readable at M = 1000 and the reason no multiplicity
    correction is applied across rungs. Call `Random.seed!(p15_rung_seed(G, axis, rung))` immediately
    before the `sbc_gate` call (item 2). After each rung, push a row carrying `axis`, `rung`,
    `mechanism` from `P15_AXIS_MECHANISM`, `prior_edge` = `rung == P15_PRIOR_BOUNDARY_RUNG[axis]`,
    the 8-vector of ECEs, `p15_report_only(...)` for everything non-gating, the elapsed seconds,
    `rung_seed`, and the realised imsize counts.
 4b. CHECKPOINT AFTER EVERY RUNG — persist-before-assert. Immediately after pushing each row, write
    the accumulated report to disk through the SAME atomic `write_gate_report` path
    (`.tmp` → reopen-integrity → `mv(...; force = true)`), with `status = :partial` and a `completed`
    vector of `(axis, rung)` tuples. Do NOT wait for the ladder to finish.
    **Rationale, stated so it is not optimised away as "redundant I/O":** the atomic-write path
    protects against a CORRUPT artifact, not a LOST one. Without this, a hard crash (power, OOM,
    Ctrl-C) at hour seven discards every measured rung, and the restart would need `--force`, which
    is reserved for deliberate re-measurement rather than for recovering from an outage. This follows
    the project's established `persist-before-assert` convention (Phase 14, commit `b288a36`).
    The write is a few hundred KB against a ~14 min rung — the overhead is not measurable.
 4c. RESUME. On entry, if a report already exists at the envelope path AND its `status` is `:partial`
    or `:budget_exceeded` AND its `consts_version`, `seed`, `M`, `L`, `axes` and `rungs` all match the
    current pre-registration, then **resume**: load its `rows`, skip every `(axis, rung)` already in
    `completed`, and continue at the first missing one. Reuse the stored `rung0`/`break_thresholds`
    rather than re-measuring the anchor.
    **Resuming does NOT require `--force`, and that is deliberate**: continuing an interrupted
    measurement is not a second measurement. Per-rung seeding (item 2) makes the resumed rows
    byte-identical to those of an uninterrupted run, so a resumed sweep and a continuous sweep are
    the same artifact. If any of the identity fields disagree, REFUSE and report the mismatch — a
    report from a different pre-registration must never be silently continued.
    Log `resuming from N/M rungs` so the operator sees it happened.
 5. BUDGET CEILING. Track cumulative elapsed time; if it exceeds `wallclock_ceiling_hours`, STOP,
    write what has been measured so far, and return `status = :budget_exceeded` with the completed
    (axis, rung) list. A partial sweep honestly recorded is a result; a silently over-running one is
    not. Log a projected total after the first three rungs so the operator sees the reconciliation
    against the ~8.5 h prediction early. **Note the cumulative clock counts only THIS invocation's
    elapsed time**; carry the resumed rows' recorded `elapsed_seconds` forward and add them, so the
    ceiling bounds the total measurement cost and cannot be evaded by restarting.
 6. THE OOD ARM. ONE `gate_ood_roc(m, density; G = G, rng = rng_factory(), families = families,
    levels = rungs, n_id = ood_n_id, n_pos = ood_n_pos, n_fit = ood_n_fit, imsize = imsize,
    imsize_set = imsize_set, imsize_weights = imsize_weights)` call for the whole grid — it runs no
    posterior draws, so all seven axes at five rungs cost minutes. Call `seed_gate_global!(G)` before
    it, because unlike `sbc_gate` this function is not self-seeding. Extract `fire_rate` and
    `id_fire_rate` for the crossing rule. Pass `families` through the KEYWORD; never touch
    `OOD_FAMILIES`.
 7. REDUCTION. `p15_axis_break` per axis, `p15_ood_rung` per axis, then `p15_axis_verdicts` and
    `p15_sc2_pass`. Attach the `degenerate` reading.
 8. RETURN a NamedTuple with at least: `status` (`:ran` / `:partial` / `:budget_exceeded`),
    `completed` (the `(axis, rung)` vector, always present — it is what item 4c resumes from),
    `resumed_from` (the rung count loaded from a prior partial report, or `0`),
    `grid`, `seed`,
    `global_seed`, `nthreads`, `consts_version`, `M`, `L`, `axes`, `rungs`, `rung0` (the full
    `sbc_gate` return for the anchor), `break_thresholds`, `rows` (the per-rung records),
    `axis_break`, `axis_ood`, `axis_verdicts`, `sc2_pass`, `degenerate`, `ood` (the full
    `gate_ood_roc` return minus its bulky `noise_null`), `elapsed_seconds`, `mechanism` map,
    `prior_boundary_rung` map, and `grid16_contrast = P15_GRID16_CONTRAST` with
    `grid16_drop_reason = P15_GRID16_DROP_REASON` so the named limit travels inside the artifact
    rather than only in prose (D-10a).

Add `run_envelope(grid; artifacts_root = P15_ARTIFACTS_ROOT, write_report = true, kwargs...)`
mirroring `run_gate`'s resolution discipline exactly: return `(status = :not_trained, grid, npe_path)`
if the NPE artifact is absent; load the net with `load_gate_model`; run
`assert_imsize_provenance(m; imsize_set, imsize_weights, require = GATE_REQUIRE_IMSIZE_PROVENANCE)`
and return `(status = :provenance_mismatch, ...)` running NO arm on failure — this is the invariant
that makes the D-10a grid_16 drop mechanical rather than a judgement call; load the OOD nulls with
`ProteinCoLoc.load_ood_nulls`; then call `envelope_gate` and persist through the EXISTING
`write_gate_report` to a NEW path `_gate_envelope_path(root, G) = joinpath(_gate_grid_dir(root, G),
"envelope_report_$(G).jld2")`. Never write to `_gate_report_path` — the shipped `gate_report_8.jld2`
is an input to this phase and must stay byte-unchanged. Bump nothing in `GATE_REPORT_SCHEMA`; add
`const P15_ENVELOPE_SCHEMA = 1` instead and carry it inside the report NamedTuple.

Extend `_parse_gate_args` with `--envelope` (a Bool flag), `--sweep-m N` and `--axes a,b,c` (both
overrides existing ONLY to make the fixture-scale smoke runnable — document that inline, and log a
loud `@warn` naming the pre-registered values whenever either is passed). Update the docstring at
:500-508 and the file header at :7-21 to list the new flag. Extend the `PROGRAM_FILE` entry point so
`--envelope` dispatches `run_envelope` and everything else keeps its current behaviour verbatim.
Leave `--sbc/--bf/--ood` semantics and the arm-preserving merge untouched.
  </action>
  <verify>
    <automated>julia --project=. --threads=auto -e 'include("test/gate/run_gate.jl"); o = _parse_gate_args(["--grid","8","--envelope","--consts","p15_consts.jl","--artifacts-root","artifacts/amended_v2"]); @assert o.envelope && o.grid == 8 && o.artifacts_root == "artifacts/amended_v2"; @assert isdefined(@__MODULE__, :envelope_gate) && isdefined(@__MODULE__, :run_envelope); r = run_envelope(99; artifacts_root = "artifacts/amended_v2", write_report = false); @assert r.status === :not_trained r; println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `envelope_gate` and `run_envelope` are defined after including `run_gate.jl`
    - `_parse_gate_args` accepts `--envelope`, `--sweep-m` and `--axes` and still parses every pre-existing flag combination
    - `run_envelope` on an untrained grid returns `status = :not_trained` without throwing
    - Source contains `seed_gate_global!` inside `envelope_gate` and again before the `gate_ood_roc` call
    - Source contains `families = families` passed to `gate_ood_roc` and contains no assignment to `OOD_FAMILIES`
    - Source contains `_gate_envelope_path` and does NOT write to `_gate_report_path` anywhere in the new code
    - `grep -v '^\s*#' test/gate/run_gate.jl | grep -c '\.mce'` returns 0
    - `git diff -U0 HEAD -- test/gate/run_gate.jl | grep -E '^-[^-]' | grep -vE '^\-\s*(#|$)'` shows only the docstring/header lines the plan names as edited — no existing behavioural line is removed
  </acceptance_criteria>
  <done>The sweep is one new arm on the existing CLI, reusing every statistic and the atomic writer, with the thread and wall-clock budgets enforced in code.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture-scale end-to-end proof of the arm with an injected fake simulator</name>
  <files>test/gate/test_p15_envelope_arm.jl</files>
  <read_first>
    - test/gate/run_gate.jl (the arm from Task 1)
    - test/runtests.jl lines 878-930 (the existing "ship-gate harness + SBC" testset — the tiny-trained-net + injected-fake-simulator fixture recipe to reuse verbatim)
    - test/gate/harness.jl lines 240-299 (`load_gate_model` and `default_simulator`'s error contract)
    - test/gate/p15_envelope.jl (the reductions being exercised)
  </read_first>
  <action>
Create `test/gate/test_p15_envelope_arm.jl`, standalone-runnable and includable (15-01 wired the
list). It must NOT load the shipped net and must NOT touch the `artifacts/` tree.

Build the fixture the way `test/runtests.jl:895-925` already does: a tiny trained net at `G = 4` from
random summaries via `ProteinCoLoc.fit_summary_transform` / `train_npe` with 2 epochs, plus an
INJECTED fake simulator obeying the `(sample_prior, simulate_pair, build_mci)` contract at a small
concrete `imsize` such as `(64, 64)`. A concrete imsize tuple takes `gate_imsize`'s scalar branch and
consumes zero rng, so the fixture is fast and its rng accounting is trivial.

Testsets:

1. "envelope arm runs end to end at fixture scale" — call `envelope_gate` directly with
   `M = 6`, `L = 15`, `axes = (:spillover, :registration)`, `rungs = 2`,
   `ood_n_id = ood_n_fit = 12`, `ood_n_pos = 8`, `min_threads = 1` (explicitly overriding the guard
   for the fixture, with a comment that the REPORTED run must not), and a fake `ood_nulls`. Assert
   the returned NamedTuple has every field the Task-1 action lists; that `rows` has exactly
   `length(axes) * rungs` entries; that `rung0` is present exactly once; that every row carries
   `mechanism` and `prior_edge`; and that `break_thresholds` has one entry per `P15_BREAK_COLUMNS`.

2. "thread guard fires" — call `envelope_gate` with `min_threads = 10_000` and assert it throws, with
   the error message mentioning threads.

3. "budget ceiling fires and records honestly" — call with `wallclock_ceiling_hours = 0.0` and assert
   the return has `status === :budget_exceeded`, a non-empty completed-rung list, and that it still
   returns rather than throwing.

4. "OOD table is passed, not mutated (D-02a)" — after the fixture run, assert
   `keys(OOD_FAMILIES) == (:texture, :noise, :optics, :background)` and that
   `bytes2hex(SHA.sha256(read("test/gate/misspec.jl")))` still equals `P15_FROZEN_FILE_SHA256.misspec`.

5. "global RNG is pinned (D-11, Pitfall 5)" — run the fixture arm TWICE in the same process with
   identical arguments and assert the two `rung0.ranks` matrices are bit-identical. Then assert
   `envelope_gate`'s reported `global_seed` equals `gate_global_seed(G)` under the loaded
   pre-registration, and that this differs from the value the same rule gives under
   `gate_consts_8_v2.jl` (load that into an isolated module to compute the comparison).

6. "report path and atomicity (D-10)" — call `run_envelope` against a `mktempdir()` artifacts root
   containing a copied fixture npe/ood bundle if one can be produced cheaply; otherwise assert
   `_gate_envelope_path(root, 8)` ends in `envelope_report_8.jld2` and that
   `_gate_report_path(root, 8)` is a DIFFERENT path. Additionally write a small NamedTuple through
   `write_gate_report` to a temp path, reopen it, and assert the round-trip — proving the Phase-15
   arm rides the existing `.tmp` -> reopen -> `mv` writer rather than a new one.

7. "checkpoint + resume is byte-identical to an uninterrupted run" — **the test that makes the resume
   path evidence rather than a promise.** Against a `mktempdir()` artifacts root:
   (a) run the fixture arm to completion and keep its `rows` as the reference;
   (b) run it again from a clean root but with `wallclock_ceiling_hours` set so it stops partway,
       and assert the on-disk report exists with `status === :partial`, a non-empty `completed`, and
       FEWER rows than the reference — i.e. the checkpoint really was written mid-ladder, not at the
       end;
   (c) re-invoke with the full ceiling against that same root and assert the run **resumes**
       (`resumed_from > 0`), does NOT re-measure the anchor, and that its final `rows` are
       **bit-identical to the reference from (a)**, row for row, including the per-row `rung_seed`.
   This is the assertion that per-rung seeding exists to support: if the global RNG were pinned only
   once at the top of the arm, (c) would differ from (a) and this test would fail. It therefore
   guards the seeding design, not just the file I/O.
   Add two negative cases: a stored report whose `consts_version` differs must be REFUSED rather than
   resumed, and one whose `seed` differs must likewise be refused — each with the mismatch named in
   the error.

8. "budget reconciliation smoke (assumption A1)" — a `@info`-only measurement, not an assertion: time
   ONE fixture rung and print the per-draw seconds alongside the model's predicted `t_draw ~= 0.656 s`
   at 32 threads, so the executor of plan 15-07 has a local reconciliation datum before committing
   the ~8.5 h grid. Guard the whole testset behind an env check so it never fails CI on a slow runner.
  </action>
  <verify>
    <automated>julia --project=. --threads=auto test/gate/test_p15_envelope_arm.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=. --threads=auto test/gate/test_p15_envelope_arm.jl` exits 0 with zero failures and zero errors
    - The end-to-end testset produces `length(axes) * rungs` rows plus exactly one rung-0 record
    - The thread guard and the budget-ceiling behaviours are both asserted
    - The twice-run bit-identity of `rung0.ranks` is asserted, and the P15 vs v2 global-seed difference is asserted
    - The `misspec.jl` SHA-256 is re-asserted after the arm has run
    - The test does not read from or write to the repository `artifacts/` tree
  </acceptance_criteria>
  <done>The arm is proven to run, to be reproducible, to respect its budget guards and to leave the shipped tables and reports untouched — all in seconds, before any real compute is spent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-15 arm -> shipped gate report | `gate_report_8.jld2` is a published input; a runner that writes to it destroys the record it is measured against |
| Phase-15 arm -> frozen family table | `gate_ood_roc`'s `families` keyword is the sanctioned extension point; anything else rewrites a PASSED arm |
| operator intent -> compute budget | An unbounded sweep silently consumes a machine for a day and yields a partial artifact nobody can interpret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-21 | Tampering | `artifacts/amended_v2/grid_8/gate_report_8.jld2` | mitigate | The arm writes only `_gate_envelope_path`; the acceptance criteria assert `_gate_report_path` appears in no new code |
| T-15-09 | Tampering | `OOD_FAMILIES` | mitigate | `families` passed as a keyword; the SHA-256 of `misspec.jl` is re-asserted after the arm runs |
| T-15-22 | Repudiation | irreproducible reported numbers | mitigate | `seed_gate_global!(G)` called at the arm top and before `gate_ood_roc`; both the Philox seed and the global seed are recorded in the report; a twice-run bit-identity test asserts it |
| T-15-07 | Denial of Service | unbounded sweep wall-clock | mitigate | Hard thread guard plus `P15_SWEEP_WALLCLOCK_CEILING_HOURS` with an honest `:budget_exceeded` partial write |
| T-15-23 | Tampering | partial write corrupting an artifact | mitigate | Reuses the existing `write_gate_report` `.tmp` -> reopen-integrity -> `mv(...; force=true)` path; a crash leaves only a discardable `.tmp` |
| T-15-SC | Tampering | package-manager installs | accept | No install step; all imports already in the committed manifest |
</threat_model>

<verification>
- `julia --project=. --threads=auto test/gate/test_p15_envelope_arm.jl` exits 0
- `julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'` exits 0 (the arm's includes must not disturb the existing gate testsets)
- `git status --porcelain test/gate/misspec.jl test/gate/sbc.jl test/gate/harness.jl` is empty
- `julia --project=. --threads=auto test/gate/run_gate.jl --grid 8 --sbc --artifacts-root artifacts/amended_v2` still behaves exactly as before the edit (dry check: it must not silently pick up `p15_consts.jl`, since `--consts` was not passed)
</verification>

<success_criteria>
- `run_gate.jl --envelope` exists as one new arm inheriting `--consts`, `--artifacts-root`, the atomic writer and the honest status returns
- Every statistic is computed by existing code; no new statistical implementation was added
- Rung 0 is measured once and shared; the ladder is matched-pairs by construction
- The global RNG half is pinned and the arm is bit-reproducible across two runs in one process
- Thread and wall-clock budgets are enforced with an honest partial-result path
</success_criteria>

<output>
Create `.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-05-SUMMARY.md` when done.
Record the fixture-rung timing and its comparison against the predicted `t_draw ~= 0.656 s/draw` at
32 threads — plan 15-07 uses it to reconcile the budget before committing the full grid.
</output>

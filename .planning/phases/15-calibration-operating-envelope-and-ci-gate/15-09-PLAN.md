---
phase: 15-calibration-operating-envelope-and-ci-gate
plan: 09
type: execute
wave: 5
depends_on: ["15-07", "15-08"]
files_modified:
  - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md
  - docs/amortized.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [D-02, D-04a, D-05, D-06, D-10a, D-13, D-14]

must_haves:
  truths:
    - "The domain-of-applicability map is readable as TWO OVERLAID STORIES — where TRAINING is insufficient (an in-prior axis that breaks) and where the MODEL runs out (an out-of-model axis that breaks) — because the mechanism typing is a deliverable, not an annotation (D-02)"
    - "The map is stated to be read PER AXIS, not per rung, wherever it is reported: q95 was chosen knowing it costs ~5% per-rung false breaks, i.e. ~1.75 expected false breaks over 35 axis-rung tests (D-04a)"
    - "The envelope is reported for Δρ and ρ_true; the six nuisance marginals are shown per rung and explicitly gate nothing, with the ~0.08 SD drift named as an already-disclosed v2.0 limit rather than re-litigated (D-05)"
    - "SC2 is reported per axis as PROTECTIVE / LATE / SILENT-BUT-SAFE, with the pass rule stated and the in-prior asymmetry explained (D-06)"
    - "Any LATE axis is written up as a NAMED LIMIT and the phase closes on it — no detector retuning, no new detector channel, both explicitly recorded as refused (D-14)"
    - "The dropped grid_16 contrast appears as a named limit with its evidence, never as a silent scope cut (D-10a)"
    - "If the envelope came out empty or unbounded, the pre-declared reading is quoted verbatim from `p15_consts.jl` and the result is written as a finding, not as a phase failure (D-13)"
    - "Cross-rung ECEs are stated to be correlated by design (matched-pairs ladders, one global seed), so no multiplicity correction is applied across rungs and no independent per-rung interval is quoted"
  artifacts:
    - path: ".planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md"
      provides: "The reported domain map, the SC1/SC2/SC3 verdicts against the ROADMAP wording as extended, and every named limit"
      min_lines: 150
    - path: "docs/amortized.md"
      provides: "The Phase-15 named limits appended to the shipped v2.0 limits section"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 entry updated with the plan list, the outcome and the criteria read against it"
  key_links:
    - from: ".planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md"
      to: "artifacts/amended_v2/grid_8/envelope_report_8.jld2"
      via: "every number in the map is read from the artifact, none retyped from a summary"
      pattern: "envelope_report_8"
    - from: "docs/amortized.md"
      to: "15-ENVELOPE.md"
      via: "named-limit entries pointing at the report"
      pattern: "15-ENVELOPE"
---

<objective>
Write down what the phase found, in the form the manuscript and the next phase can cite, with every
limit named rather than implied.

Purpose: Phases 11 and 12 both closed negative-but-useful, and this project's standard is that a
negative is publishable and must be written that way. The map has to be readable by someone deciding
whether to trust the tool on their images: which nuisance axes it survives, how far, whether the OOD
flag warned them first, and what was NOT tested. Every one of those is already in
`envelope_report_8.jld2`; this plan turns the artifact into the claim, at exactly the strength the
data support.

Output: `15-ENVELOPE.md`, the appended named limits in `docs/amortized.md`, and the ROADMAP Phase-15
entry.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CONTEXT.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-07-SUMMARY.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-08-SUMMARY.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CI-EVIDENCE.md

<interfaces>
<!-- What the report artifact carries, and what the ROADMAP says the phase must answer. -->

`artifacts/amended_v2/grid_8/envelope_report_8.jld2`, key `report`, carries at least:
  status, grid, seed, global_seed, nthreads, consts_version, M, L, axes, rungs,
  rung0 (the full anchor sbc_gate return, including its M x 8 rank table),
  break_thresholds, rows (per-rung: axis, rung, mechanism, prior_edge, the 8-vector of ECEs,
  the report-only record, elapsed seconds, realised imsize counts),
  axis_break, axis_ood, axis_verdicts, sc2_pass, degenerate,
  ood (fire_rate per family x level, id_fire_rate, fused/density/noise AUC, thresholds),
  elapsed_seconds, mechanism map, prior_boundary_rung map,
  grid16_contrast = :dropped with grid16_drop_reason.
`artifacts/amended_v2/grid_8/envelope_anchor_8.jld2` carries the rung-0 rank table, so any
rank-derived statistic can still be recomputed post hoc without re-running anything.

ROADMAP Phase 15 success criteria, as written:
  1. An adversarial sweep over spillover/PSF/autofluorescence/registration yields a
     domain-of-applicability map
  2. The OOD flag demonstrably fires before empirical coverage breaks ("OOD-before-break")
  3. An SBC/coverage regression test fails CI on calibration drift after a code change
SC1 is EXTENDED by D-02 (all four named axes swept; `texture` and `noise` added; a seventh pure-offset
`autofluorescence` axis added by D-02a) and SC2's evidential form is FIXED by D-06. BOTH the original
wording and the amendment must be cited alongside any Phase-15 result — the same discipline Phase 12
applies to `12-SC3-AMENDMENT.md`.

Already-disclosed v2.0 limits this report must NOT re-litigate (docs/amortized.md): the ~0.08 SD
nuisance-marginal location drift on non-identified nuisances at the M = 2000 over-power edge. D-05
declines to re-gate on it; the report shows the nuisance columns and says so.

Honesty items already known and owed to the report:
  - `misspec_optics` perturbs both σy and σx, so that axis moves anisotropy AND overall blur — it is
    "aberrated/out-of-focus", not pure anisotropy
  - `misspec_texture`'s smoothing saturates at σ = 0.8 for level >= 4.4, so its rung 5 differs from
    rung 4 in spot count only
  - `background` is a COMPOUND axis (illumination gradient x radial vignette x bleed) reclassified
    out-of-model by D-02a; its prior boundary lies BELOW rung 1, so its marked boundary is rung 0
  - the ladders are matched-pairs on the three in-prior axes and NOT on `texture`/`noise`, whose
    generators draw extra rng values
  - the fast CI tier detects change, not miscalibration; the slow tier detects miscalibration
</interfaces>

@.planning/ROADMAP.md
@docs/amortized.md
@test/gate/p15_consts.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: 15-ENVELOPE.md — the domain map, the three verdicts, and the named limits</name>
  <files>.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md</files>
  <read_first>
    - artifacts/amended_v2/grid_8/envelope_report_8.jld2 (open it with JLD2 and read every number FROM THE ARTIFACT; do not retype numbers from a SUMMARY)
    - test/gate/p15_consts.jl (the frozen bars, the D-13 degenerate readings and `P15_LATE_AXIS_RESPONSE` — quote them verbatim rather than paraphrasing)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-07-SUMMARY.md and 15-08-SUMMARY.md
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CI-EVIDENCE.md (the observed-red evidence SC3 rests on)
    - .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CLOSURE.md and .planning/phases/12-spatial-colocalization-map/12-VERIFICATION.md (the negative-but-useful writing style this project uses — match its directness)
    - docs/amortized.md (the existing named-limits section, so the new entries match its form and nothing already disclosed is re-litigated)
  </read_first>
  <action>
Write `.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md`. Read every
number out of the JLD2 artifact in a short throwaway Julia snippet and paste the values; do not carry
numbers over from prose.

Structure:

§1 HEADLINE, in one paragraph. What the envelope IS for the shipped `amended_v2/grid_8` net, and
whether SC2 passed. If the outcome is degenerate, quote `P15_DEGENERATE_EMPTY_ENVELOPE_READING` or
`P15_DEGENERATE_UNBOUNDED_ENVELOPE_READING` verbatim from `p15_consts.jl` and state that the reading
was pre-declared before any rung ran. Do not soften a negative and do not inflate a positive.

§2 WHAT WAS MEASURED. The net (`artifacts/amended_v2/grid_8`), `M = P15_SWEEP_M`, `L = SBC_L`, the
imsize mixture and its realised counts, the seven axes, five rungs, the fresh `PROD_SEED_P15` stream
and the global seed, the thread count and the wall-clock, and the pointer to the two artifacts and
`15-sweep.log`. State that all draws went through `harness.jl` on a stream with a DIFFERENT Philox
key from every burned stream — not a different index range on a burned key — and why that distinction
is the one that matters (CONVENTIONS C-04).

§3 THE BREAK CRITERION AS APPLIED. The measured rung-0 anchor ECEs for columns 1 and 8, the margin,
and the resulting thresholds. Show the shipped `gate_report_8.jld2` ECEs alongside, labelled CONTEXT,
with the one-sentence reason they are not the anchor. State the operating characteristic in plain
terms: q95 gives about a 5% per-rung false-break rate under exact calibration, so across 35
axis-rung tests roughly 1.75 false breaks are EXPECTED, and **the map must therefore be read per
axis, not per rung.** Put that sentence in bold and repeat it in §4's table caption.

§4 THE DOMAIN MAP. One table, one row per axis: axis, mechanism (`in-prior extrapolation` /
`out-of-model misspecification`), what the ladder moves and in what units, the prior boundary and its
rung (or "none — no prior support" for the out-of-model axes), the five per-rung ECEs for ρ_true and
Δρ, `L_break`, `L_ood`, the measured `id_fire_rate` and the per-rung fire rates, and the SC2 verdict.
Below the table, write the TWO OVERLAID STORIES explicitly (D-02): first the in-prior axes — where
the net saw those θ in training and still miscalibrates, which is a TRAINING failure — and then the
out-of-model axes — where the model runs out, which is a SCOPE limit, expected, honest, and precisely
what the OOD flag exists for. A single pooled number would produce a map nobody can act on; say so.

§5 SC2, PER AXIS. Restate the three states and the pass rule from `P15_SC2_STATES` /
`P15_SC2_PASS_RULE`. Give each axis its verdict with the reason. Explain the asymmetry that motivated
the third state: the three in-prior axes are inside the training distribution, so the OOD detector was
fit on ID data CONTAINING them and may correctly never fire on them — `SILENT-BUT-SAFE` is a correct
outcome there, not a miss. For any LATE axis, write the named limit in the form
"the OOD flag does not protect against X", quote `P15_LATE_AXIS_RESPONSE`, and record explicitly that
two responses were available and both REFUSED: lowering `OOD_ID_QUANTILE` until the flag fires early
enough (tuning a detector until it passes its own gate) and building a new detector channel for the
failing mechanism (new capability, its own phase). Cite D-14.

§6 REPORTED BUT NOT GATED (D-05). The six nuisance marginals per rung, the KS and χ² p-values per
rung, and the coverage curves for columns 1 and 8. State that none of these gated, why (an M-driven
significance criterion measures `SBC_M`, not the tool), and that the ~0.08 SD nuisance-marginal drift
is an ALREADY-DISCLOSED v2.0 limit in `docs/amortized.md` which this phase deliberately does not
re-gate on. State that cross-rung ECEs are correlated by design — the in-prior ladders are
matched-pairs and every rung shares one global seed — so no multiplicity correction is applied across
rungs and no independent per-rung interval is quoted.

§7 SC1 / SC2 / SC3 AGAINST THE ROADMAP. Quote each ROADMAP criterion verbatim, then state the result
and the amendment it must be read with: SC1 EXTENDED by D-02 and D-02a (all four named axes swept,
three added), SC2's evidential form FIXED by D-06, SC3 answered in two tiers with the fast tier's
scope stated honestly. For SC3, cite `15-CI-EVIDENCE.md`'s red run URL: the claim rests on an observed
failure and an observed recovery, not on a green run.

§8 NAMED LIMITS. A numbered list, each with its evidence and each written to survive being quoted out
of context:
  1. Grid-dependence of the operating envelope is UNTESTED. The grid_16 contrast was attempted and
     blocked by missing training-imsize provenance on the only available higher-dimension net
     (`recorded = false`, so `run_gate` returns `:provenance_mismatch` and runs no arm); bypassing the
     guard would have confounded grid x imsize-regime x θ-space in a single comparison. Quote
     `P15_GRID16_DROP_REASON` (D-10a).
  2. The envelope is per-pair, not per-region — Phase 12 delivered no calibrated per-region
     uncertainty.
  3. The envelope is measured UNDER THE SIMULATOR; it is a statement about the forward model's
     neighbourhood, and it is paired with the OOD result rather than standing alone.
  4. `misspec_optics` moves both σy and σx, so that axis is "aberrated/out-of-focus", not pure
     anisotropy.
  5. `misspec_texture`'s smoothing saturates at σ = 0.8 for level >= 4.4, so its rung 5 differs from
     rung 4 in spot count only.
  6. `background` is a COMPOUND axis reclassified out-of-model by D-02a, with its prior boundary below
     rung 1 — its marked boundary is rung 0.
  7. The ladders are matched-pairs on the three in-prior axes and NOT on `texture`/`noise`.
  8. The fast CI tier detects change, not miscalibration; only the slow tier detects miscalibration.
  9. Any LATE axis from §5, restated here in one line.
  Plus any limit the run itself revealed (a `:budget_exceeded` partial sweep, a non-finite rung, a
  saturated ladder). Do not omit an inconvenient one.

§9 WHAT WAS NOT DONE, AND WHY. `P15_ITERATION_ALLOWANCE` status (spent or unspent, and on what); the
deferred items carried forward (a Windows fast-tier runner, a scheduled slow tier, a new OOD detector
channel for a LATE axis, `grid_4` and the three-grid trend, per-region envelope mapping); and the
standing rule that this phase MEASURES a frozen shipped net — `07-GATE-AMENDMENT.md` §6.4 blocks
iterating the current gate, and nothing here retrained, retuned or reshipped anything.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md && for s in "Named Limits" "SILENT-BUT-SAFE" "per axis, not per rung" "in-prior" "out-of-model" "grid_16" "envelope_report_8"; do grep -qi "$s" .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md || { echo "MISSING: $s"; exit 1; }; done; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `15-ENVELOPE.md` exists with all nine sections
    - §4's table has exactly seven axis rows, each carrying mechanism, boundary rung, `L_break`, `L_ood` and an SC2 verdict drawn from `P15_SC2_STATES`
    - The sentence "the map must be read per axis, not per rung" appears in bold in §3 and again in §4's caption
    - §5 gives a verdict and a reason for every axis, and for any LATE axis records both refused responses with D-14 cited
    - §7 quotes all three ROADMAP criteria verbatim and names the amendment each must be read with
    - §8 contains at least the nine enumerated limits, with `P15_GRID16_DROP_REASON` quoted
    - Every quantitative claim in the file is traceable to `envelope_report_8.jld2`, `envelope_anchor_8.jld2` or `gate_report_8.jld2`; no number appears that is not in one of them
    - The words "v1", "simplified", "for now" and "future enhancement" do not appear
  </acceptance_criteria>
  <done>The measurement is a citable claim with its mechanism typing, its per-axis reading rule, its three verdicts and its limits all written down.</done>
</task>

<task type="auto">
  <name>Task 2: Append the Phase-15 named limits to docs/amortized.md and update the ROADMAP entry</name>
  <files>docs/amortized.md, .planning/ROADMAP.md</files>
  <read_first>
    - docs/amortized.md (READ IN FULL — the existing named-limits section, its numbering and its voice; new entries must extend it, never edit an existing one)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md (the source of the entries)
    - .planning/ROADMAP.md Phase 15 detail block and the progress table row (lines around the Phase-15 entry)
    - .planning/ROADMAP.md Phase 11 and Phase 12 entries (the precedent for how a phase outcome — including a negative — is written into the roadmap)
  </read_first>
  <action>
Append to `docs/amortized.md`'s named-limits section — APPEND ONLY; do not renumber, reword or delete
any existing limit. Add the Phase-15 entries in the same voice as the existing ones, each one line
plus a pointer to `15-ENVELOPE.md`:
  - the operating envelope for the shipped 8x8 net, stated per axis with its mechanism typing, and the
    instruction to read it per axis rather than per rung
  - grid-dependence of the envelope is untested, with the provenance reason (D-10a)
  - the envelope is per-pair and measured under the simulator
  - any LATE axis, in the form "the OOD flag does not protect against X" (D-14)
  - the fast CI tier detects change, not miscalibration
Do not restate the ~0.08 SD nuisance drift; it is already there, and D-05 declined to re-gate on it.

Update `.planning/ROADMAP.md`:
  - in the Phase 15 detail block, replace `**Plans**: TBD` and the `- [ ] TBD` placeholder with the
    nine-plan list, one line each, naming what the plan delivered and its wave
  - set `**Requirements**` to the decision set actually used — `D-01..D-14 plus D-02a, D-04a, D-08a,
    D-10a (CONTEXT.md decisions; no REQ IDs were minted for this phase)` — so no plan's requirements
    field points at a `TBD`
  - directly under the Success Criteria, add an OUTCOME block dated today in the style of Phase 11's
    and Phase 12's: one sentence per criterion stating the result and the amendment it must be read
    with, and a pointer to `15-ENVELOPE.md`, `15-CI-EVIDENCE.md` and the two artifacts. If the phase
    closed on a negative or a degenerate envelope, say so in the first sentence — this project's
    precedent is that a negative is a result, and burying it in the third clause is what the roadmap's
    Phase 11 and 12 entries deliberately avoid
  - update the progress table row for Phase 15 with the plan count and status
  - change nothing else in the file
  </action>
  <verify>
    <automated>grep -q '15-ENVELOPE' docs/amortized.md && grep -q '15-09-PLAN\|15-01-PLAN' .planning/ROADMAP.md && ! grep -n 'Phase 15' -A 12 .planning/ROADMAP.md | grep -q 'Plans\*\*: TBD' && git diff -U0 HEAD -- docs/amortized.md | grep -E '^-[^-]' | grep -v '^---' | wc -l | grep -qx 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `docs/amortized.md` gained the Phase-15 limit entries and lost nothing: `git diff -U0 HEAD -- docs/amortized.md` shows zero removed content lines
    - `.planning/ROADMAP.md` Phase 15 lists all nine plans with their waves, has `**Requirements**` naming the decision set instead of `TBD`, and carries a dated OUTCOME block covering SC1, SC2 and SC3
    - The progress table row for Phase 15 shows the plan count and status
    - No other ROADMAP phase entry was modified
  </acceptance_criteria>
  <done>The phase's limits are on the shipped documentation and its outcome is legible from the roadmap without opening a summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ratify the reported map and its named limits</name>
  <files>.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md</files>
  <action>
Walk the human through the reported map section by section, apply any correction they ask for
directly to `15-ENVELOPE.md` (and, if it touches a limit, to `docs/amortized.md` as a further append),
and re-present until ratified. Record the ratification and any change made in `15-09-SUMMARY.md`. Then
run the closing decoupling check: `git status --porcelain` must show no modification under `src/`, to
either manifest, to the spike environment, or to the four frozen gate files, and the full suite must
be green.
  </action>
  <read_first>
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-ENVELOPE.md (all nine sections)
    - docs/amortized.md (the appended entries in the context of the existing ones)
    - .planning/ROADMAP.md Phase 15 (the OUTCOME block)
  </read_first>
  <what-built>
    The domain-of-applicability map for the shipped `amended_v2/grid_8` net: seven mechanism-typed
    axes at five rungs, break rungs against a rung-0-anchored ECE threshold, per-axis three-valued SC2
    verdicts against a measured ID fire-rate baseline, the two-tier CI with an observed red run, and
    the named limits — including the untested grid-dependence and any LATE axis.
  </what-built>
  <how-to-verify>
    1. Read §1. Does the headline say what the phase found, in the first sentence, including if that
       is a negative or a degenerate envelope? Phases 11 and 12 both closed negative-but-useful and
       said so up front; this should read the same way.
    2. Read §4. For each axis, is the mechanism typing doing real work — can you tell a TRAINING
       failure from a SCOPE limit by reading the row? Is the "read per axis, not per rung" instruction
       impossible to miss?
    3. Read §5. Is every axis's SC2 verdict justified? For any LATE axis, are BOTH refused responses
       recorded (lowering `OOD_ID_QUANTILE`; a new detector channel)?
    4. Read §8. Is any limit missing that you know about — anything inconvenient that got smoothed
       over? Is the grid_16 drop stated as an attempted-and-blocked contrast with its evidence, rather
       than as a scope decision?
    5. Confirm nothing in the phase retrained, retuned or reshipped: `git status --porcelain` shows no
       change under `src/`, `artifacts/` carries only the two new envelope files, and both manifests
       and the spike environment are byte-unchanged.
  </how-to-verify>
  <resume-signal>Type "approved", or name the section and the change you want.</resume-signal>
  <verify>
    <automated>git status --porcelain src/ Project.toml Manifest.toml spike/Project.toml spike/Manifest.toml test/gate/misspec.jl test/gate/sbc.jl test/gate/harness.jl test/gate/gate_consts_8_v2.jl && julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` reports no modification to `src/`, `Project.toml`, `Manifest.toml`, `spike/Project.toml`, `spike/Manifest.toml`, `test/gate/misspec.jl`, `test/gate/sbc.jl`, `test/gate/harness.jl` or `test/gate/gate_consts_8_v2.jl`
    - `julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'` exits 0
    - The human has ratified `15-ENVELOPE.md`, or the requested changes have been made and re-ratified
  </acceptance_criteria>
  <done>The reported map, its verdicts and its limits are ratified, and the phase provably measured a frozen shipped net without touching it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact -> reported claim | A number retyped from prose rather than read from the artifact is unverifiable and drifts silently |
| inconvenient result -> published summary | The failure mode this project has already paid for four times is a bar or a caveat quietly softened between measurement and write-up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-30 | Tampering | numbers in `15-ENVELOPE.md` | mitigate | Every quantitative claim is read from `envelope_report_8.jld2` / `envelope_anchor_8.jld2` / `gate_report_8.jld2`; the acceptance criteria require traceability to one of the three, and the rung-0 rank table is persisted so any statistic is recomputable without a re-run |
| T-15-31 | Repudiation | a softened or omitted limit | mitigate | §8 has a fixed minimum list; the human ratification checkpoint asks explicitly whether any inconvenient limit was smoothed over; D-13's degenerate readings and `P15_LATE_AXIS_RESPONSE` are quoted verbatim from the frozen file rather than paraphrased |
| T-15-32 | Tampering | `docs/amortized.md` existing limits | mitigate | Append-only edit; the Task-2 verify asserts zero removed content lines in the diff |
| T-15-33 | Tampering | the shipped package during a reporting phase | mitigate | The ratification verify asserts `git status --porcelain` is clean for `src/`, both manifests, the spike environment and the four frozen gate files |
| T-15-SC | Tampering | package-manager installs | accept | Documentation plan; no install step anywhere |
</threat_model>

<verification>
- `15-ENVELOPE.md` contains all nine sections and the seven-row map table
- `git diff -U0 HEAD -- docs/amortized.md` shows zero removed content lines
- `.planning/ROADMAP.md` Phase 15 has no remaining `TBD`
- `git status --porcelain src/ Project.toml Manifest.toml spike/Project.toml spike/Manifest.toml` is empty
- `julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'` exits 0
</verification>

<success_criteria>
- The domain map is reported per axis with mechanism typing that distinguishes a training failure from a scope limit
- SC2 is reported three-valued per axis with the pass rule and the in-prior asymmetry explained
- SC3 rests on an observed red CI run, cited by URL
- Every limit is named, including the untested grid-dependence and any LATE axis, with the refused responses recorded
- The shipped package, both manifests and the spike environment are provably untouched
</success_criteria>

<output>
Create `.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-09-SUMMARY.md` when done.
Record the final SC1/SC2/SC3 verdicts, the per-axis SC2 states, the `P15_ITERATION_ALLOWANCE` status,
and the full named-limits list as it will be cited by Phase 16.
</output>

---
phase: 15-calibration-operating-envelope-and-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gate/p15_consts.jl
  - test/gate/test_p15_consts.jl
  - test/runtests.jl
autonomous: true
requirements: [D-01, D-03, D-04a, D-05, D-07, D-10a, D-11, D-12, D-13]

must_haves:
  truths:
    - "The Phase-15 pre-registration lives src-side in `test/gate/` and is selected by the existing `run_gate.jl --consts` override, never by a default path (D-01)"
    - "A FRESH `PROD_SEED_P15` exists whose Philox KEY differs from every burned key, and whose disjointness from every burned seed is asserted both in-file and in an independent testset that has been SHOWN to fire on a deliberate collision (D-11)"
    - "`prod_seed`/`prod_rng` alias onto the P15 stream so `gate_global_seed(8)` — the global half of reproducibility — also rides P15 and provably differs from its `gate_consts_8_v2.jl` value (D-11)"
    - "Every Tier-1 bar carries its derivation inline; no bar is a bare number (D-12)"
    - "Tier 2 is a SEQUENCE of separately-sentinel-keyed append blocks; no constant is ever edited (D-12)"
    - "The ladder endpoints for all seven axes are frozen before any rung runs, and the prior boundary is an exact rung on every in-prior axis (D-03)"
    - "The ECE break threshold is declared as a FORM anchored on a rung-0 measurement at the sweep's own M, with margin `q95(ECE0) - E[ECE0]`; the shipped report's ECE is never the anchor (D-04a)"
    - "Gating and reporting-only constant names are two disjoint tuples with an asserted-empty intersection, so 'reported is not gated' is machine-checkable (D-04a, D-05)"
    - "Both degenerate outcomes and the three-valued SC2 states are written down before any rung exists, and `P15_ITERATION_ALLOWANCE = 1` (D-13)"
    - "The dropped grid_16 contrast is recorded as a named limit with its evidence, not silently omitted (D-10a)"
    - "The frozen files this phase must not touch are pinned by content hash and asserted (D-01)"
  artifacts:
    - path: "test/gate/p15_consts.jl"
      provides: "Two-tier Phase-15 pre-registration: fresh seed stream, frozen ladders, frozen bars, D-13 declarations"
      contains: "const PROD_SEED_P15"
      min_lines: 380
    - path: "test/gate/test_p15_consts.jl"
      provides: "Independent seed-disjointness, ladder-derivation, tier-split and frozen-file-hash assertions plus a firing negative fixture"
      min_lines: 120
    - path: "test/runtests.jl"
      provides: "Phase-15 testset block that includes every test/gate/test_p15_*.jl present, skipping absent ones honestly"
      contains: "test_p15_"
  key_links:
    - from: "test/gate/p15_consts.jl"
      to: "test/gate/harness.jl gate_global_seed"
      via: "prod_seed/prod_rng aliases onto prod_seed_p15/prod_rng_p15"
      pattern: "prod_seed\\(G::Integer\\) = prod_seed_p15\\(G\\)"
    - from: "test/gate/test_p15_consts.jl"
      to: "test/gate/gate_consts_8_v2.jl"
      via: "isolated-module load + per-const equality of the shared amended rule set"
      pattern: "module P15RefV2"
---

<objective>
Freeze the Phase-15 pre-registration before a single Phase-15 number can exist, and make its two
load-bearing properties — a fresh, provably disjoint RNG stream, and bars that carry their own
derivation — executable rather than asserted in prose.

Purpose: `PROD_SEED_V2[8]` is SPENT (`test/gate/run_gate.jl:47-54` binds it to exactly one run,
already consumed and recorded in `artifacts/amended_v2/grid_8/gate_report_8.jld2` as
`seed = 1135605683775656488`). Under CONVENTIONS C-04 a different index range on the same key is
NOT a fresh stream; only a different key is. An undetected seed collision would invalidate every
number this phase produces, so this plan gates all downstream work.

Output: `test/gate/p15_consts.jl` (two-tier), `test/gate/test_p15_consts.jl`, and the Phase-15
testset wiring in `test/runtests.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CONTEXT.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md
@.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly, no exploration. -->

test/gate/gate_consts_template.jl and test/gate/gate_consts_8_v2.jl are BOTH guarded on
`if !isdefined(@__MODULE__, :SBC_M)`. `test/gate/run_gate.jl:63-74` loads exactly one
pre-registration into the module: `--consts FILE` wins, else `gate_consts_<G>.jl`, else the
template. So `p15_consts.jl` must define the FULL const set, not a P15-only delta.

Consumers that read pre-registration names with graceful fallbacks:
  test/gate/harness.jl:138-143  GATE_IMSIZE_SET / GATE_IMSIZE_WEIGHTS / GATE_REQUIRE_IMSIZE_PROVENANCE
                                 <- SBC_IMSIZE_SET / SBC_IMSIZE_WEIGHTS / SBC_REQUIRE_IMSIZE_PROVENANCE
  test/gate/harness.jl:75-95    gate_global_seed(G) = rand(Philox4x(UInt64, (prod_seed(G), UInt64(1))), UInt64)
                                 seed_gate_global!(G) = Random.seed!(gate_global_seed(G))
  test/gate/sbc.jl:365,381-397  sbc_holm <- holm_adjusted ; SBC_RULES_VERSION <- SBC_MULTIPLICITY
                                 GATE_CHI2_BINS <- SBC_CHI2_BINS ; _sbc_ks_fwer <- SBC_KS_FWER
  test/gate/run_gate.jl:172-181 _bf_ci <- fisher_z_ci ; _bf_verdict <- bf_corr_verdict
                                 GATE_BF_N <- BF_GATE_N ; BF_RULES_VERSION <- BF_DECISION_RULE

The 48 const names gate_consts_8_v2.jl defines (enumerate with
`grep -o '^\s*const [A-Za-z_0-9]*' test/gate/gate_consts_8_v2.jl`):
  GATE_CONSTS_VERSION GATE_CONSTS_GRID GATE_AMENDMENT_DOC
  SBC_M SBC_L SBC_BINS SBC_ECE_GREEN SBC_ECE_YELLOW SBC_MULTIPLICITY SBC_N_TESTS SBC_KS_FWER
  SBC_CHI2_FWER SBC_CHI2_BINS SBC_IMSIZE_SET SBC_IMSIZE_WEIGHTS SBC_IMSIZE
  SBC_REQUIRE_IMSIZE_PROVENANCE SBC_FIX_M SBC_FIX_L SBC_FIX_BINS
  BF_CORR_MIN BF_GATE_N BF_GATE_N_MIN BF_CI_HALFWIDTH_TARGET BF_CI_LEVEL BF_DECISION_RULE
  BF_LOGBF_TOL BF_LOGBF_Q BF_SWEEP_LO BF_SWEEP_HI BF_SWEEP_N
  OOD_ID_QUANTILE OOD_KS_EPS OOD_AUC_MIN OOD_GRID_LEVELS OOD_PP_REPS
  NPE_MASTER_SEED VAL_MASTER_SEED DEFAULT_MASTER_SEED DEV_SEEDS
  PROD_SALT PROD_MASTER PROD_SEED AMEND_SALT AMEND_MASTER PROD_SEED_V2
  Z_TWO_SIDED_95 Z_ONE_SIDED_95
plus the functions `_derive_prod_seed`, `_forbidden_seeds`, `_derive_prod_seed_v2`, `prod_seed_v2`,
`prod_rng_v2`, `prod_seed`, `prod_rng`, `holm_adjusted`, `bf_required_n`, `fisher_z_ci`,
`bf_corr_verdict`.

Simulator prior extrema (src/amortized/simulator.jl:92-95), the numeric source of the D-03 boundaries:
  SPILLOVER_PRIOR        = Uniform(0.0, 0.2)    -> boundary 0.2
  AUTOFLUORESCENCE_PRIOR = Uniform(0.0, 0.1)    -> boundary 0.1
  SHIFT_PRIOR            = Uniform(-1.0, 1.0)   -> boundary |(dx,dy)| = 1.0 px
  const σ_psf = 1.3 (simulator.jl:179) is FIXED, not a prior -> optics has no boundary rung
Simulator range guards (simulator.jl:232-238): `0 <= spillover <= 1`, `autofluorescence >= 0`,
no range guard on shift_dx/shift_dy beyond finiteness.

The measured ECE null (research §"The ECE Null Distribution", 2000 replicates of uniform ranks on
0:999, n_bins = 50, reproducing `_bin_calibration` exactly):
  M=250  mean 0.02059  q95 0.03789
  M=500  mean 0.01431  q95 0.02737
  M=1000 mean 0.01038  q95 0.01884   <- the sweep's M
  M=2000 mean 0.00751  q95 0.01424
  closed form E[ECE0] ~= 0.336/sqrt(M)

Repo salts that P15_SALT must not be (verified at spike/validation/p12_consts.jl:234-245):
  0x9E3779B97F4A7C15 0xD1B54A32D192ED03 0xBF58476D1CE4E5B9 0x94D049BB133111EB
  0xC4CEB9FE1A85EC53 0xA24BAED4663EE121 0xA5A5A5A5A5A5A5A5 0xA5A5A5A5DEADBEEF
  0x2545F4914F6CDD1D 0xFF51AFD7ED558CCD 0xC2B2AE3D27D4EB4F
</interfaces>

@test/gate/gate_consts_8_v2.jl
@test/gate/gate_consts_template.jl
@test/gate/harness.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork the amended v2 pre-registration into p15_consts.jl and swap in a fresh, key-distinct seed stream</name>
  <files>test/gate/p15_consts.jl</files>
  <read_first>
    - test/gate/gate_consts_8_v2.jl (READ IN FULL — this file is the byte source for the fork; lines 1-56 are the provenance-disclosure header to mirror, 250-343 are the seed block to replace, 430-454 are the self-check block)
    - test/gate/gate_consts_template.jl (the v1 `_derive_prod_seed` rule that must be replicated verbatim so PROD_SEED can be RECOMPUTED and forbidden)
    - test/gate/harness.jl lines 40-100 (gate_global_seed / seed_gate_global! — why the prod_seed alias is load-bearing)
    - test/gate/run_gate.jl lines 28-77 (the include-guard and `--consts` selection this file must satisfy)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md §"Seed Derivation (D-11)"
    - .planning/CONVENTIONS.md C-04 (line 246 ff.)
  </read_first>
  <action>
Create `test/gate/p15_consts.jl` by starting from a BYTE COPY of `test/gate/gate_consts_8_v2.jl`,
then making exactly the edits below. Copying rather than writing fresh is deliberate: it makes the
"same amended rule set" property a diff rather than a claim, and Task 3 asserts it per-constant.

Header (replace lines 1-56 of the copy, keep the same shape):
  - State that this file is the Phase-15 pre-registration, the executable form of `15-CONTEXT.md`
    decisions D-01..D-14 plus amendments D-02a, D-04a, D-08a, D-10a.
  - State that it MIRRORS `gate_consts_8_v2.jl` and does NOT extend it, because that file is bound
    to exactly one spent run on `PROD_SEED_V2[8]` (`run_gate.jl:47-54`), and that it defines the
    same const names, so the two must NEVER be loaded into the same module.
  - Record the licensing standard from `.planning/STATE.md`: this milestone has concluded "the bar
    was wrong, not the model" four times, so every bar below carries a derivation from evidence
    INDEPENDENT of the machinery under audit, stated next to it.
  - Record the two-tier discipline verbatim in the shape of `spike/validation/p12_consts.jl:30-52`:
    Tier 1 is the single guard block keyed on `:SBC_M`; Tier 2 is a SEQUENCE of blocks each keyed on
    its OWN sentinel const, because a second append reusing an earlier sentinel is silently skipped;
    NO constant in either tier is ever EDITED, so the git history of this file is the audit trail.
  - Reserve the Tier-2 sentinels and name the plan that opens each:
    `:P15_GOLDEN_RANK_DIGEST` (opened by 15-06), `:P15_ECE_ANCHOR_MEASURED` (opened by 15-07),
    `:P15_ITERATION_SPENT` (opened only if D-13's one allowance is spent).

Constant edits inside the Tier-1 guard block:
  - `GATE_CONSTS_VERSION = 15` and `GATE_AMENDMENT_DOC` -> the Phase-15 CONTEXT path. These two are
    the ONLY shared-name values allowed to differ from `gate_consts_8_v2.jl`; Task 3 asserts that.
  - Keep every other copied constant AND every copied helper function (`holm_adjusted`,
    `bf_required_n`, `fisher_z_ci`, `bf_corr_verdict`, `Z_TWO_SIDED_95`, `Z_ONE_SIDED_95`)
    byte-identical in value, so the sweep's SBC arm runs under the amended v2 rule set and its ECE
    numbers stay comparable to `gate_report_8.jld2`.

Seed section (replace the `AMEND_SALT`/`AMEND_MASTER`/`_derive_prod_seed_v2`/`PROD_SEED_V2` block
with the P15 construction, KEEPING the v1 and v2 derivations present so both families are
RECOMPUTED here and forbidden — never trusted from a comment):
  - Add the burned seeds `gate_consts_8_v2.jl` does not carry, with their source files named in a
    comment: `VAL_FIX_SEED = 0x0000_0000_00F1_F7ED`, `RATIO_PAIR_SEED = 0x0000_0000_004A_7107`,
    `CORPUS_MASTER_SEED = 0x0000_0000_00C0_5EED`, `P11_DEV_SEED = 0x0000_0000_0B11_DE71`,
    `P12_DEV_SEED = 0x0000_0000_0B12_DE71`, `P12_FIX_SEED = 0x0000_0000_0B12_F1F7`,
    `P13_DEV_SEED = 0x0000_0000_0B13_DE71`, `P13_FIX_SEED = 0x0000_0000_0B13_F1F7`.
    Verify each literal against its defining file before writing it; if any differs on disk, use the
    on-disk value and note the correction in the comment.
  - `_p15_forbidden()` returns the tuple `(UInt64(0), NPE_MASTER_SEED, VAL_MASTER_SEED,
    VAL_FIX_SEED, DEFAULT_MASTER_SEED, RATIO_PAIR_SEED, CORPUS_MASTER_SEED, DEV_SEEDS...,
    P11_DEV_SEED, P12_DEV_SEED, P12_FIX_SEED, P13_DEV_SEED, P13_FIX_SEED, values(PROD_SEED)...,
    values(PROD_SEED_V2)...)`.
  - `const P15_REPO_SALTS` = the eleven repo salts listed in the `<interfaces>` block above, as a
    tuple, each with its owner named in a trailing comment.
  - `const P15_SALT = 0x8EBC_6AF0_9C88_C6E3` (a degski64 finalizer constant; comment that it is NOT
    a SplitMix64/Murmur3 constant already in `P15_REPO_SALTS`) and
    `const P15_MASTER = 0x0000_0000_0B15_2026`.
  - `_derive_prod_seed_p15(G)` = `Philox4x(UInt64, (UInt64(P15_MASTER) ⊻ P15_SALT, UInt64(G)))`,
    one `rand(rng, UInt64)`, then `while s in _p15_forbidden(); s = rand(rng, UInt64); end`.
    Copy the loop shape from `gate_consts_8_v2.jl:301-305` exactly.
  - `const PROD_SEED_P15 = Dict{Int,UInt64}(G => _derive_prod_seed_p15(G) for G in (4, 8, 16, 32))`,
    `prod_seed_p15(G)`, `prod_rng_p15(G) = Philox4x(UInt64, (prod_seed_p15(G), UInt64(0)))`, and the
    two aliases `prod_seed(G::Integer) = prod_seed_p15(G)` / `prod_rng(G::Integer) = prod_rng_p15(G)`
    mirroring `gate_consts_8_v2.jl:342-343`. The aliases are what make `gate_global_seed(G)` — the
    GLOBAL half of reproducibility, `harness.jl:50-59` — ride the P15 stream automatically.
  - Comment, next to the aliases, WHY a different key rather than a different index range: under
    C-04 an index-keyed pool at the same key is a superset, not a held-out set; a distinct
    `(master, salt)` pair is the only structural immunity.
  </action>
  <verify>
    <automated>julia --project=. --threads=auto -e 'module P15C; include("test/gate/p15_consts.jl"); end; println(P15C.PROD_SEED_P15[8]); @assert !(P15C.PROD_SEED_P15[8] in P15C._p15_forbidden()); @assert !(P15C.P15_SALT in P15C.P15_REPO_SALTS); println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `test/gate/p15_consts.jl` exists and loads into an isolated module without error
    - Source contains `const PROD_SEED_P15`, `_derive_prod_seed_p15`, `_p15_forbidden`,
      `const P15_SALT`, `const P15_REPO_SALTS`, `prod_seed(G::Integer) = prod_seed_p15(G)` and
      `prod_rng(G::Integer)  = prod_rng_p15(G)`
    - Source still contains `const PROD_SEED` and `const PROD_SEED_V2` (both RECOMPUTED, both inside
      `_p15_forbidden()`)
    - `P15C.GATE_CONSTS_VERSION == 15`
    - `P15C.SBC_REQUIRE_IMSIZE_PROVENANCE === true` and `P15C.SBC_IMSIZE === :mixture`
    - The isolated-module load asserts above exit 0
    - `git diff --quiet HEAD -- test/gate/gate_consts_8_v2.jl test/gate/misspec.jl test/gate/sbc.jl test/gate/harness.jl` exits 0 (nothing frozen was touched)
  </acceptance_criteria>
  <done>A loadable Phase-15 pre-registration exists whose amended rule set is a byte-fork of the v2 file and whose RNG stream is keyed on a fresh, repo-unique salt with an asserted redraw loop.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the Tier-1 bars, ladders, mechanism typing and the D-13 declarations, each with its derivation inline</name>
  <files>test/gate/p15_consts.jl</files>
  <read_first>
    - test/gate/p15_consts.jl (the file created in Task 1)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md §"The ECE Statistic, Exactly", §"The ECE Null Distribution", §"Prior Support vs level Units", §"Compute Budget", §"The OOD Arm"
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-CONTEXT.md decisions D-03, D-04a, D-05, D-07, D-10a, D-13
    - src/amortized/simulator.jl lines 85-100 and 225-245 (prior extrema and the range guards a ladder must not violate)
    - test/gate/misspec.jl lines 104-184 (what `level` scales in each shipped family)
    - spike/validation/p12_consts.jl lines 18-100 (the two-tier header + gating/reporting tuple pattern to mirror)
  </read_first>
  <action>
Append a clearly delimited "PHASE-15 TIER-1 BARS" section INSIDE the existing `:SBC_M` guard block
of `test/gate/p15_consts.jl`. Every constant below carries its derivation as an adjacent comment; a
bar with no derivation is the exact liability the Stage-1 ceiling adjudication cost this project.

Sweep design:
  - `P15_SWEEP_M = 1000` — derivation: the ECE null table in the `<interfaces>` block. Break
    resolution equals the margin: 0.0085 at M=1000 vs 0.0067 at M=2000 for 2x the compute, against a
    shipped rho_true ECE of 0.03713, i.e. a ~23% relative degradation is resolvable. Record that M
    is a PRECISION property here, not a power lever, because D-04 moved the break statistic off
    significance onto an effect size.
  - `P15_RUNGS = 5`, `P15_RUNG0 = 0` (the shared in-prior anchor rung, measured once per net).
  - `P15_SWEEP_MIN_THREADS = 16` — derivation: `07-GATE-AMENDMENT.md:140-149` measures
    `E[cost] = 0.08346 s/pair` at 32 threads and the single-threaded arm is ~13x slower; a
    single-threaded sweep is a budget error, not a style choice.
  - `P15_SWEEP_WALLCLOCK_CEILING_HOURS = 12.0` — derivation: the reconciled cost model predicts
    **~8.5 h** for 1 rung-0 + **7** axes x 5 rungs at M=1000 at 32 threads; the ceiling carries
    **~1.41x** headroom for research assumption A2 (misspecification generators average ~1.3x
    `simulate_pair`, unmeasured).
    **Do NOT write "7.3 h" or "1.6x" here.** 7.3 h is `15-RESEARCH.md`'s SIX-axis estimate, computed
    before D-02a added the seventh (pure-offset autofluorescence) axis; 15-07's interfaces block
    carries the reconciled 7-axis figure. This is a frozen Tier-1 derivation comment — a bar whose
    stated justification does not match its actual basis is the precise failure this project has
    already had to correct four times (`.planning/STATE.md`, the four-amendments observation).

Break criterion (D-04a):
  - `P15_ECE_NULL_B = 20_000`, `P15_ECE_NULL_SEED = 20260804` — the MC design that produced the
    numbers below, recorded so they are reproducible without a net.
  - `P15_ECE_NULL_MEAN = 0.01038`, `P15_ECE_NULL_Q95 = 0.01884` (at M = `P15_SWEEP_M`, L = `SBC_L`).
  - `P15_ECE_MARGIN = 0.00846` with the derivation `P15_ECE_NULL_Q95 - P15_ECE_NULL_MEAN`, asserted
    in-file to equal that difference to 1e-12. Record the operating characteristic explicitly: q95
    was chosen over q99 knowing it costs a ~5% per-rung false-break rate, i.e. **~1.75** expected
    false breaks over **35** axis x rung tests (0.05 x 7 axes x 5 rungs), WHICH IS WHY THE MAP MUST
    BE READ PER AXIS, NOT PER RUNG.
    **Do NOT write "~1.5" or "~30".** Those are the stale six-axis figures from before D-02a.
    `15-09-PLAN.md` Task 1 independently computes ~1.75 over 35 tests; these two numbers appear in
    both the frozen consts comment and the reported domain map and MUST agree.
  - `p15_break_threshold(ece_rung0) = ece_rung0 + P15_ECE_MARGIN` as a FUNCTION, with a comment
    stating that the anchor is a Tier-2 measurement at the sweep's own M on the P15 stream, and that
    the shipped `gate_report_8.jld2` ECEs (rho_true 0.03713, Delta-rho 0.00429) are NOT the anchor
    because Delta-rho's value sits BELOW its own null mean of 0.00751 at M=2000, so anchoring there
    would declare a break on ~90% of perfectly calibrated rungs.
  - `P15_BREAK_COLUMNS = (1, 8)` — `SBC_PARAM_LABELS` column 1 is rho_true and column 8 is Delta-rho
    (`test/gate/sbc.jl:123-124`). D-05: the six nuisance marginals are measured and reported, never
    gated.
  - `P15_NUISANCE_COLUMNS = (2, 3, 4, 5, 6, 7)`.
  - `P15_COVERAGE_LEVELS = 0.05:0.05:0.95` — the 19 nominal levels the ECE identity averages over.
  - `P15_MCE_FORBIDDEN = true` with the derivation: `_bin_calibration` walks all 50 reliability bins
    including the 31 empty ones where `predicted_rate = midpoint` and `observed_rate = 0`, pinning
    `mce = 0.99` on all eight columns of the shipped report. It is an artifact, not a measurement,
    and no Phase-15 code may read it (D-04a). Plan 15-04 makes this an executable source guard.

OOD crossing (D-07):
  - `P15_OOD_N_ID = 200`, `P15_OOD_N_FIT = 200`, `P15_OOD_N_POS = 200` — derivation: `fire_rate` at a
    rung is a binomial proportion with n = n_pos; at the shipped `n_pos = 40` its SE at p ~= 0.05 is
    0.034, so any margin below ~0.10 is inside one SE and is not a detection. The OOD arm runs no
    posterior draws, so raising n_pos to 200 costs ~20 min for the whole experiment.
  - `P15_OOD_FIRE_MARGIN = 0.025` — derivation: `quantile(Binomial(P15_OOD_N_POS, 1 - OOD_ID_QUANTILE), 0.95) / P15_OOD_N_POS - (1 - OOD_ID_QUANTILE)` = 15/200 - 0.05. Add a dependency-free
    integer helper `_p15_binom_quantile(n, p, q)` that accumulates the exact binomial PMF and assert
    in-file that it returns 15 for (200, 0.05, 0.95), so the bar is derived rather than typed.
  - Comment that the RULE compares against the MEASURED `id_fire_rate` returned by `gate_ood_roc`,
    never against the design value `1 - OOD_ID_QUANTILE` (D-07).

Ladders and mechanism typing (D-02, D-02a, D-03):
  - `P15_PRIOR_BOUNDARY = (spillover = 0.2, registration = 1.0, autofluorescence = 0.1)` — literals
    with `src/amortized/simulator.jl:92-95` cited per entry. Task 3 asserts each equals
    `maximum(ProteinCoLoc.<PRIOR>)`, so the literal cannot drift from the simulator while keeping
    this pre-registration dependency-free (Random123 only, as every gate consts file is).
  - `P15_SPILLOVER_LADDER = (0.1, 0.2, 0.35, 0.6, 1.0)` — rung 2 IS the prior boundary; rung 5 is the
    simulator's own `spillover <= 1.0` guard (`simulator.jl:233`), i.e. the endpoint is the model's
    limit, not a choice.
  - `P15_SHIFT_LADDER = (0.5, 1.0, 2.0, 4.0, 8.0)` px of `|(dx, dy)|` — rung 2 IS the prior boundary;
    rung 5 is 8x the boundary and 2.7x Phase-11's widest researched shift (3 px), beyond which the
    vacated border fill (`BG_FLOOR`, simulator.jl:290) dominates the image rather than the
    registration error.
  - `P15_AF_LADDER = (0.05, 0.1, 0.2, 0.4, 0.8)` — rung 2 IS the prior boundary; rung 5 is 8x it and
    matches the magnitude the shipped compound `background` family reaches at its level 2.
  - `P15_NATIVE_LEVELS = 1:P15_RUNGS` for the four shipped families, which take their own `level`
    formulas. Note that `misspec_texture`'s smoothing saturates at sigma = 0.8 for level >= 4.4, so
    its rung 5 differs from rung 4 in spot count only — an honesty note for the report.
  - `P15_AXIS_MECHANISM = (texture = :out_of_model, noise = :out_of_model, optics = :out_of_model,
    background = :out_of_model, spillover = :in_prior, registration = :in_prior,
    autofluorescence = :in_prior)`. Record D-02a's reclassification with its evidence: `background`
    reaches bleed 0.4 at level 1 against a prior max of 0.1 AND composes a radial vignette and
    illumination gradient the simulator has no parameter for, so it cannot express "at the prior
    edge" in any units; the seventh pure-offset `autofluorescence` axis is what makes D-03 hold on
    every in-prior axis without exception.
  - `P15_PRIOR_BOUNDARY_RUNG = (spillover = 2, registration = 2, autofluorescence = 2,
    texture = nothing, noise = nothing, optics = nothing, background = nothing)` — `nothing` means
    "no prior support to mark", not "unmeasured".
  - `P15_AXES` = the seven axis symbols in report order.

D-13 declarations, written before any rung can exist:
  - `P15_ITERATION_ALLOWANCE = 1` with the constraint stated: spendable on ladder EXTENSION ONLY in
    the "never breaks within the ladder" case, recorded as spent via the `:P15_ITERATION_SPENT`
    Tier-2 sentinel. Any other extension is an amendment and must be argued as one.
  - `P15_DEGENERATE_EMPTY_ENVELOPE_READING` (String): breaks at rung 1 on every axis means the tool
    has no usable operating range at the swept resolution — a reportable negative, not a phase
    failure.
  - `P15_DEGENERATE_UNBOUNDED_ENVELOPE_READING` (String): never breaks within the ladder means the
    envelope exceeds the swept range and the honest report is the BOUND, not a wider search.
  - `P15_SC2_STATES = (:protective, :late, :silent_but_safe)` and `P15_SC2_PASS_RULE` (String):
    SC2 passes iff no axis is `:late`; `:silent_but_safe` exists because "the flag never fired" has
    two opposite meanings and a two-valued verdict collapses them.
  - `P15_LATE_AXIS_RESPONSE` (String): a LATE axis is reported as a NAMED LIMIT and the phase closes
    on it; lowering `OOD_ID_QUANTILE` and adding a detector channel are both forbidden (D-14).

D-10a named limit:
  - `P15_GRID16_CONTRAST = :dropped` and `P15_GRID16_DROP_REASON` (String) recording the evidence:
    `artifacts/grid_16/npe_16.jld2`'s `meta` is `(:grid, :n_pairs, :use_gpu)` with
    `training_imsize_provenance` ABSENT, so `SBC_REQUIRE_IMSIZE_PROVENANCE = true` makes `run_gate`
    return `:provenance_mismatch` and run no arm; it is a v1-era 256-squared unbounded-theta net, so
    bypassing the guard would confound grid x imsize-regime x theta-space in one comparison. The
    carried limit is "grid-dependence of the operating envelope is UNTESTED; the contrast was
    attempted and blocked by missing provenance on the only available higher-dimension net."
  - `P15_REPORTED_NET = "artifacts/amended_v2/grid_8"` and `P15_ARTIFACTS_ROOT = "artifacts/amended_v2"`,
    with the note that `run_gate`'s default root is the v1 `artifacts/` tree and every Phase-15
    invocation must pass `--artifacts-root artifacts/amended_v2`.

Frozen-file pins (executable form of the phase's do-not-touch constraints):
  - `P15_FROZEN_FILE_SHA256` — a NamedTuple mapping repo-relative paths to their current SHA-256,
    computed at write time with `bytes2hex(SHA.sha256(read(path)))`, covering
    `test/gate/misspec.jl`, `test/gate/sbc.jl`, `test/gate/harness.jl`,
    `test/gate/gate_consts_8_v2.jl`, `Project.toml`, `Manifest.toml`, `spike/Project.toml`,
    `spike/Manifest.toml`. Comment why each is pinned: `misspec.jl` because appending to
    `OOD_FAMILIES` would silently rewrite the shipped, PASSED OOD arm's `combined_auc` and its pass
    conjunction (D-02a); `Project.toml`/`Manifest.toml` because `test/runtests.jl:32-49` asserts the
    EXACT strings "0.2.1"/"0.16.10" while `[compat]` permits a wider range, so any re-resolve is a
    coin flip (D-08a); the spike env because `spike/test/test_p12_decoupling.jl:176-178` asserts it
    byte-unchanged. Note `SHA` is a Julia stdlib — this adds no dependency.

Tier split (D-04a/D-05, machine-checkable):
  - `P15_GATING_CONSTANTS = (:P15_ECE_MARGIN, :P15_BREAK_COLUMNS, :P15_OOD_FIRE_MARGIN,
    :P15_GOLDEN_ECE_TOL)` and `P15_REPORTING_ONLY_CONSTANTS = (:P15_NUISANCE_COLUMNS,
    :P15_COVERAGE_LEVELS, :P15_KS_REPORTED, :P15_CHI2_REPORTED, :P15_AXIS_MECHANISM,
    :P15_PRIOR_BOUNDARY_RUNG)`, with `P15_KS_REPORTED = true` and `P15_CHI2_REPORTED = true` defined
    and an in-file `@assert isempty(intersect(P15_GATING_CONSTANTS, P15_REPORTING_ONLY_CONSTANTS))`.

Golden bars (consumed by 15-06, frozen here so the fast gate's tolerance is pre-registered too):
  - `P15_GOLDEN_M = SBC_FIX_M`, `P15_GOLDEN_L = SBC_FIX_L`, `P15_GOLDEN_BINS = SBC_FIX_BINS`,
    `P15_GOLDEN_IMSIZE = (256, 256)` — a CONCRETE tuple so `gate_imsize` takes the scalar branch and
    consumes zero rng (`harness.jl:158-171`); 256/8 = 32 >= 4 clears `_guard_misspec_imsize`.
  - `P15_GOLDEN_ECE_TOL = 1e-8` — derivation: under a pinned Julia 1.12.6 and the committed
    manifest the only admissible drift is float-summation reassociation, order 1e-12 at these
    magnitudes; 1e-8 leaves four orders of headroom while still failing any real change. State that a
    Julia version bump is a legitimate re-bless reason and must be named as such in the commit.
  - `P15_GOLDEN_HONESTY` (String): at M = 8 the ECE null mean is ~0.12, so the fast gate detects
    CHANGE, not miscalibration; the slow tier detects miscalibration. Anything else oversells SC3.

Extend the existing self-check block at the end of the Tier-1 guard with the cheap arithmetic
assertions: margin identity, `_p15_binom_quantile(200, 0.05, 0.95) == 15`, ladder monotonicity for
all three new ladders, `P15_SPILLOVER_LADDER[2] == P15_PRIOR_BOUNDARY.spillover` (and the other two),
`P15_SPILLOVER_LADDER[end] <= 1.0`, `length(P15_AXES) == 7`, `keys(P15_AXIS_MECHANISM) == keys(P15_PRIOR_BOUNDARY_RUNG)`,
`P15_ITERATION_ALLOWANCE == 1`, and that the gating target set and the reporting target set intersect
exactly in the two D-05 targets (`ρ_true`, `Δρ`) — i.e. every gating target is also reported, and no
nuisance marginal is gating. No inference, no simulation, no file reads inside the guard block except
the SHA-256 pins.
  </action>
  <verify>
    <automated>julia --project=. --threads=auto -e 'module P15C; include("test/gate/p15_consts.jl"); end; P=P15C; @assert P.P15_ECE_MARGIN ≈ P.P15_ECE_NULL_Q95 - P.P15_ECE_NULL_MEAN atol=1e-12; @assert P.P15_SPILLOVER_LADDER[2] == P.P15_PRIOR_BOUNDARY.spillover; @assert P.P15_SHIFT_LADDER[2] == P.P15_PRIOR_BOUNDARY.registration; @assert P.P15_AF_LADDER[2] == P.P15_PRIOR_BOUNDARY.autofluorescence; @assert isempty(intersect(P.P15_GATING_CONSTANTS, P.P15_REPORTING_ONLY_CONSTANTS)); @assert P.P15_ITERATION_ALLOWANCE == 1; @assert length(P.P15_AXES) == 7; println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The isolated-module assertion command above exits 0
    - `grep -v '^\s*#' test/gate/p15_consts.jl | grep -c 'P15_ECE_MARGIN'` is at least 2 (declaration plus the self-check)
    - Source contains `P15_DEGENERATE_EMPTY_ENVELOPE_READING`, `P15_DEGENERATE_UNBOUNDED_ENVELOPE_READING`, `P15_SC2_STATES`, `P15_SC2_PASS_RULE`, `P15_LATE_AXIS_RESPONSE`, `P15_GRID16_DROP_REASON`, `P15_FROZEN_FILE_SHA256`, `P15_GOLDEN_ECE_TOL`
    - `P15C.P15_AXIS_MECHANISM.background === :out_of_model` and `P15C.P15_AXIS_MECHANISM.autofluorescence === :in_prior`
    - `P15C.P15_PRIOR_BOUNDARY_RUNG.optics === nothing`
    - Every Tier-1 bar has a non-comment-free derivation: no line matching `^\s*const P15_[A-Z_]+ = [0-9.]+\s*$` exists (i.e. no bare-number bar without a trailing or preceding derivation comment)
    - `julia --project=. -e 'using SHA; for (k,v) in pairs(<the pinned NamedTuple>) ...'` reproduces every pinned hash (covered by Task 3's testset)
  </acceptance_criteria>
  <done>Every bar the phase will score against is frozen, derived from test design or from a shipped invariant, and self-checked at load time.</done>
</task>

<task type="auto">
  <name>Task 3: Independent disjointness/derivation testset with a firing negative fixture, wired into runtests.jl</name>
  <files>test/gate/test_p15_consts.jl, test/runtests.jl</files>
  <read_first>
    - test/gate/p15_consts.jl (the file under test)
    - test/runtests.jl lines 1-60 (the co-resolution hard gate that must keep running first) and lines 1138-1180 (the `module GateConstsV2` isolated-load pattern and its testset — the shape to mirror)
    - .planning/phases/15-calibration-operating-envelope-and-ci-gate/15-RESEARCH.md §"The exact assertion set"
    - .planning/CONVENTIONS.md C-03 (declaration-line diffs) and C-04 rule 3 ("an isempty(intersect(...)) never shown non-empty is an assurance, not a test")
    - src/amortized/simulator.jl lines 85-100
  </read_first>
  <action>
Create `test/gate/test_p15_consts.jl` as a file that runs BOTH standalone
(`julia --project=. --threads=auto test/gate/test_p15_consts.jl`) and when `include`d from
`test/runtests.jl`. Open with `using Test`, `using ProteinCoLoc`, `import Random123`, `import SHA`,
then load two isolated modules at top level — `module P15Consts include(".../p15_consts.jl") end`
and `module P15RefV2 include(".../gate_consts_8_v2.jl") end` — using names that do not collide with
runtests.jl's existing `GateConstsV2`.

Testsets to write:

1. "Phase-15 seed disjointness (D-11)" — the exact assertion set from research: `fresh != 0`; not
   equal to `NPE_MASTER_SEED`, `VAL_MASTER_SEED`, `VAL_FIX_SEED`, `DEFAULT_MASTER_SEED`,
   `RATIO_PAIR_SEED`, `CORPUS_MASTER_SEED`; not in `DEV_SEEDS`; not equal to any `P1x` dev/fix seed;
   not equal to `PROD_SEED[G]` or `PROD_SEED_V2[G]` for G in (4,8,16,32) — INCLUDING the explicit
   `fresh != PROD_SEED_V2[8]`, the spent seed D-11 names; `isempty(intersect(...))` for both seed
   families; `length(unique(values(PROD_SEED_P15))) == 4`; `!(fresh in _p15_forbidden())`;
   `!(P15_SALT in P15_REPO_SALTS)`; and the KEY-difference assertion
   `(UInt64(P15_MASTER) ⊻ P15_SALT) != (UInt64(P15RefV2.AMEND_MASTER) ⊻ P15RefV2.AMEND_SALT)`.

2. "Phase-15 seed guard FIRES (C-04 rule 3)" — the negative fixture. Construct a local derivation
   identical to `_derive_prod_seed_p15` but keyed on `(P15RefV2.AMEND_MASTER, P15RefV2.AMEND_SALT)`
   WITHOUT the redraw loop, take its first draw for G = 8, and assert that value IS in
   `P15Consts._p15_forbidden()` — demonstrating the guard catches a real collision rather than
   never having been shown non-empty.

3. "Phase-15 stream and its global half (D-11, Pitfall 5)" — `prod_seed(8) == PROD_SEED_P15[8]`;
   `prod_rng(8) isa Random123.Philox4x`; two constructions of `prod_rng(8)` give bit-identical draws;
   `prod_rng(8)` and `prod_rng(4)` differ. Then recompute the global half in both modules with the
   `harness.jl:75` rule — `rand(Philox4x(UInt64, (M.prod_seed(8), UInt64(1))), UInt64)` — and assert
   the P15 value differs from the v2 value, so `seed_gate_global!(8)` provably rides the P15 stream.

4. "Phase-15 inherits the amended v2 rule set exactly (D-01)" — iterate the const names of
   `P15RefV2` (via `names(P15RefV2; all = true)` filtered to `isconst` uppercase symbols), skip
   `:GATE_CONSTS_VERSION` and `:GATE_AMENDMENT_DOC`, and assert per name that
   `getfield(P15Consts, n) == getfield(P15RefV2, n)`. This is what makes "same rules, fresh stream"
   a check rather than a claim.

5. "Phase-15 ladders derive from the simulator prior (D-03)" — assert
   `P15_PRIOR_BOUNDARY.spillover == maximum(ProteinCoLoc.SPILLOVER_PRIOR)`,
   `P15_PRIOR_BOUNDARY.autofluorescence == maximum(ProteinCoLoc.AUTOFLUORESCENCE_PRIOR)`,
   `P15_PRIOR_BOUNDARY.registration == maximum(ProteinCoLoc.SHIFT_PRIOR)`; that each ladder is
   strictly increasing and length `P15_RUNGS`; that rung 2 equals the boundary on each in-prior
   axis; that `P15_SPILLOVER_LADDER[end] <= 1.0` (the `simulate_pair` guard); and that
   `P15_AXIS_MECHANISM` has exactly the seven `P15_AXES` keys with `background => :out_of_model`.

6. "Phase-15 tier split and D-13 declarations (D-12, D-13)" — the gating/reporting intersection is
   empty; every symbol in both tuples is `isdefined` in `P15Consts`; `P15_ITERATION_ALLOWANCE == 1`;
   the three degenerate/SC2 declaration strings are non-empty; `P15_SC2_STATES` has exactly the three
   symbols; `P15_GRID16_CONTRAST === :dropped` with a non-empty reason string.

7. "Phase-15 frozen files are byte-unchanged (D-02a, D-08a)" — for every entry of
   `P15_FROZEN_FILE_SHA256`, assert `bytes2hex(SHA.sha256(read(path)))` equals the pinned value.
   Give the failure message the reason: mutating `OOD_FAMILIES` silently rewrites the shipped, PASSED
   OOD arm; a re-resolved manifest fails the exact-string co-resolution gate.

8. "Phase-15 ECE margin is design arithmetic (D-04a)" — `P15_ECE_MARGIN == P15_ECE_NULL_Q95 - P15_ECE_NULL_MEAN`; `P15_ECE_NULL_MEAN` is within 5% of `0.336 / sqrt(P15_SWEEP_M)`;
   `_p15_binom_quantile(P15_OOD_N_POS, 1 - OOD_ID_QUANTILE, 0.95) / P15_OOD_N_POS - (1 - OOD_ID_QUANTILE) == P15_OOD_FIRE_MARGIN`.

Then wire `test/runtests.jl`: append ONE Phase-15 block at the very end of the file, after the
existing amended-grid-8 pre-registration testset. The block iterates the fixed list
`("test_p15_consts", "test_p15_families", "test_p15_break", "test_p15_envelope_arm", "test_p15_golden")`,
`include`s each `test/gate/<name>.jl` that exists, and emits an `@info` naming any that are absent —
the honest `:not_trained` pattern from `run_gate.jl:16-19`, so later plans add files without ever
editing `runtests.jl` again. Do not move, reorder or modify anything else in `runtests.jl`; the
co-resolution gate must still run first.
  </action>
  <verify>
    <automated>julia --project=. --threads=auto test/gate/test_p15_consts.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=. --threads=auto test/gate/test_p15_consts.jl` exits 0 with zero failures and zero errors
    - The file contains a testset asserting a deliberately-colliding derivation IS in `_p15_forbidden()` (the C-04 rule-3 firing fixture)
    - The file asserts `fresh != PROD_SEED_V2[8]` literally
    - The file asserts the global-half seeds differ between `P15Consts` and `P15RefV2`
    - `test/runtests.jl` contains a Phase-15 block that includes `test/gate/test_p15_consts.jl` and tolerates absent siblings
    - `julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'` exits 0, and its wall-clock is recorded in the plan SUMMARY (this is the CI fast-tier budget datum VALIDATION.md's Wave 0 requires)
    - `git diff -U0 HEAD -- test/runtests.jl | grep -E '^[-]' | grep -v '^---' | wc -l` is 0 (append-only edit; no existing line removed)
  </acceptance_criteria>
  <done>Seed disjointness, ladder derivation, rule-set inheritance and frozen-file integrity are all asserted by a test that has been shown to fire, and the Phase-15 suite is wired into the root test entry point once and for all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration -> reported result | A bar edited after results exist converts a measurement into a rationalisation; this is the phase's only real security-relevant boundary at this stage |
| repo file -> frozen shipped gate | `misspec.jl` / `Manifest.toml` are inputs to results already published; an edit here silently rewrites a recorded verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `test/gate/p15_consts.jl` Tier-1 bars | mitigate | Two-tier append-only structure with per-block sentinels; every bar carries its derivation; C-03 declaration-line diff recipe (`git diff -U0 \| grep -E '^[+-]\s*const '`) is the review gate; 15-06 adds the running guard |
| T-15-11 | Tampering | `test/gate/misspec.jl`, `Project.toml`, `Manifest.toml`, `spike/*.toml` | mitigate | `P15_FROZEN_FILE_SHA256` pins each and Task 3 asserts them on every test run |
| T-15-12 | Repudiation | seed provenance | mitigate | `PROD_SEED_P15` is DERIVED from a recorded `(P15_MASTER, P15_SALT)` pair, never a literal; `PROD_SEED`/`PROD_SEED_V2` are recomputed rather than trusted from comments |
| T-15-SC | Tampering | package-manager installs | accept | Zero new dependencies; the only registry interaction anywhere in this phase is `Pkg.instantiate()` against the committed manifest, which installs pinned tree-hashes and resolves nothing. No `Pkg.add` appears in any task. `SHA` is a Julia stdlib. |
| T-15-13 | Information Disclosure | secrets | accept | No credentials, no PII, no network input in this plan |
</threat_model>

<verification>
- `julia --project=. --threads=auto test/gate/test_p15_consts.jl` exits 0
- `julia --project=. --threads=auto -e 'using Pkg; Pkg.test()'` exits 0 and its runtime is recorded
- `git status --porcelain test/gate/misspec.jl test/gate/sbc.jl test/gate/harness.jl test/gate/gate_consts_8_v2.jl Project.toml Manifest.toml spike/Project.toml spike/Manifest.toml` is empty
</verification>

<success_criteria>
- A fresh, key-distinct `PROD_SEED_P15` exists, asserted disjoint from every burned stream, with the guard demonstrated to fire
- `prod_seed`/`prod_rng` alias onto P15 so the global RNG half rides it too
- Every Tier-1 bar is frozen with an inline derivation and a load-time self-check
- Both degenerate outcomes, the three SC2 states, the LATE-axis response and the dropped grid_16 contrast are recorded before any rung exists
- The Phase-15 test suite is wired into `test/runtests.jl` once, tolerant of not-yet-created siblings
</success_criteria>

<output>
Create `.planning/phases/15-calibration-operating-envelope-and-ci-gate/15-01-SUMMARY.md` when done.
Record in it: the derived `PROD_SEED_P15[8]` value, the measured full-suite wall-clock (the CI
fast-tier budget datum), and the eight pinned frozen-file SHA-256 values.
</output>

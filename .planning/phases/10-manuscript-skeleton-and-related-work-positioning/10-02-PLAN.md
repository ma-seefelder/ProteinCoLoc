---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - manuscript/claims.typ
autonomous: true
requirements: [D-06, D-07, SC1]
must_haves:
  truths:
    - "The claim table is the phase spine: each row maps scientific claim → differentiator axis → supporting phase(s) → figure/experiment ID → status (D-06)"
    - "Every claim row names the artifact (figure spec ID and/or phase deliverable) that will substantiate it (D-07)"
    - "The status column distinguishes 'supported (measured)' from 'reserved slot (pending phase)' so the skeleton never overclaims"
    - "The >100x speedup row carries the Phase-4 honest caveat verbatim (median ~325x forward-pass, ~16x on the full posterior-sample workload, realized ADVI ~0.5 s/pair, RMSE parity 0.1271 vs 0.1270, NPE intervals wider 0.884 vs 0.605)"
    - "Explicit rows/slots exist for 'calibration under the simulator' and the 'summary-orthogonal OOD blind spot' so later phases cannot quietly drop them"
    - "claims.typ remains #import-only (#let definitions, no bare content) and main.typ still compiles green"
  artifacts:
    - path: "manuscript/claims.typ"
      provides: "Full seeded claim spine: array-of-dicts of headline v2.0 claims + claim_table() render function"
      contains: "claim_table"
      min_lines: 20
  key_links:
    - from: "manuscript/claims.typ"
      to: "manuscript/main.typ"
      via: "#import consumed by <tab:claims> figure"
      pattern: "#let claims"
---

<objective>
Fill `claims.typ` with the full, honestly-caveated claim spine: the array-of-dicts that maps each
v2.0 headline claim to its differentiator axis, owning/supporting phase, figure spec ID, and status
(supported-measured vs reserved-slot). This is the contract that "shapes experiments by the claims
they must support."

Purpose: Make the claim table the falsifiable spine of the manuscript (D-06/D-07) with the honesty
posture baked in — the speedup caveat, the "calibrated under the simulator" slot, and the
"summary-orthogonal OOD blind spot" slot are present by construction.
Output: A populated `manuscript/claims.typ` that renders a complete claim table via `<tab:claims>`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the claim spine with honestly-caveated rows</name>
  <files>manuscript/claims.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-06 seed claims list; D-07 artifact linkage; §"Specific Ideas" honesty posture + speedup caveat)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (§"Phase Requirements → claim→phase→status mapping table"; §"Honest Headline Numbers" — carry verbatim; Pattern 1 array-of-dicts + claim_table)
    - manuscript/claims.typ (the placeholder machinery from Plan 01 — keep the claim_table() function, replace the rows)
  </read_first>
  <action>
    Replace the single placeholder row in `#let claims` with the full seeded set from CONTEXT D-06
    and the RESEARCH §"Phase Requirements" mapping table, keeping the existing `claim_table()`
    function and the (id, claim, axis, phase, fig, status) schema. Seed one row per v2.0 headline
    claim: amortized ms-scale inference (axis Amortization, phase 4,7, fig F1, status "supported"),
    >100x speedup at comparable RMSE (axis Speed, phase 4, fig F1, status "supported (caveat)"),
    SBC calibration-under-the-simulator (axis Calibration/SBC, phase 5, fig F2, status "reserved
    slot"), honest OOD/misspecification flag (axis OOD, phase 5, fig F3, status "reserved slot;
    blind-spot named"), amortized Bayes factor 2-way→3-way (axis Model comparison, phase 5,13, fig
    F7, status "reserved slot"), registration uncertainty as latent (axis Registration-UQ, phase 11,
    fig F5, status "reserved slot"), spatial per-region map (axis Spatial map, phase 12, fig F6,
    status "reserved slot"), cross-method "knows when the classics are wrong" (axis Comparator,
    phase 9, fig F4, status "reserved slot"), non-circular external validation (axis External
    validation, phase 8, fig F4, status "reserved slot"). D-07: every row's `fig` field names a
    figure spec ID and its `phase` field names the owning/supporting phase — the artifact that
    substantiates it. Honesty (carry VERBATIM from RESEARCH §"Honest Headline Numbers"): the speedup
    row's claim/notes text must state median ~325x forward-pass, ~16x on the full posterior-sample
    (N=2000) workload, realized ADVI baseline ~0.5 s/pair, paired with RMSE parity (0.1271 vs 0.1270)
    and wider NPE intervals (0.884 vs 0.605) — do NOT round up and do NOT state a bare ">100x vs
    minutes". Add explicit dedicated rows (or a notes field) for the "calibration under the
    simulator" caveat and the "summary-orthogonal OOD blind spot" so the honesty slots exist by
    construction. Keep `claims.typ` as `#let` definitions only (no bare content, Pitfall 4) so
    `#import` in main.typ still emits nothing.
  </action>
  <acceptance_criteria>
    - `manuscript/claims.typ` `#let claims` array contains ≥9 rows (one per headline claim)
    - The speedup row text contains "325" and "16" and "0.5" (the verbatim honest caveat, not a bare >100x)
    - At least one row status contains "supported" and at least one contains "reserved slot"
    - A row or notes field references "under the simulator" and a row references the OOD blind spot
    - Every row has a non-empty `fig` and `phase` field (D-07 artifact linkage)
    - `bash manuscript/build.sh` exits 0 and the rendered `<tab:claims>` table shows the seeded rows
  </acceptance_criteria>
  <verify>grep -c "reserved slot" manuscript/claims.typ (expect ≥5); grep -o "325" manuscript/claims.typ; bash manuscript/build.sh; echo "exit=$?"</verify>
  <done>The claim table renders the full honest claim set with per-row axis/phase/figure/status; the speedup caveat and the calibration/OOD honesty slots are present; the skeleton compiles green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. Static Typst source only; no runtime, network, input, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | overclaiming in the seeded claim table | mitigate | Status column + verbatim Phase-4 caveats prevent asserting pending results as done (honesty posture, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 with the full claim table rendering.
- The speedup caveat and calibration/OOD honesty slots are present verbatim.
</verification>

<success_criteria>
The claim spine (D-06) is fully seeded with each row naming its substantiating artifact (D-07), the
status column separates supported-measured from reserved-slot, and the honesty posture (speedup
caveat, calibration-under-the-simulator, OOD blind spot) is present by construction. Skeleton
compiles green (SC1).
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-02-SUMMARY.md` when done
</output>

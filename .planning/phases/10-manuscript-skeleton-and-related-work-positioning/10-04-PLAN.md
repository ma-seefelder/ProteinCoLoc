---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - manuscript/figures/specs.typ
autonomous: true
requirements: [D-10, SC3]
must_haves:
  truths:
    - "Each planned figure has a spec entry (not a rendered figure): figure ID, working title, panel list, producing phase, supported claim row(s), status placeholder (D-10)"
    - "The specs enumerate at minimum the claim-table-implied figures: speedup-vs-RMSE, SBC rank histograms + coverage, OOD ROC, cross-method comparison, registration-UQ widening, spatial map, three-hypothesis BF (D-10, SC3)"
    - "Each spec names the phase that produces its data, so figure specs enumerate the panels each later phase must deliver (SC3)"
    - "Each spec references the claim row(s) it supports, closing the loop with claims.typ (D-07 traceability)"
    - "The speedup figure spec (F1) carries the Phase-4 honest caveat, not the bare >100x headline"
  artifacts:
    - path: "manuscript/figures/specs.typ"
      provides: "Figure spec entries as captioned placeholder #figure blocks with ID/title/panels/phase/claims/status"
      contains: "SPEC F"
      min_lines: 30
  key_links:
    - from: "manuscript/figures/specs.typ"
      to: "manuscript/claims.typ"
      via: "each spec references the claim row IDs it supports"
      pattern: "Supports"
---

<objective>
Enumerate the figure specifications: one spec entry per planned figure (ID, working title, panel
list, producing phase, supported claim rows, status placeholder) covering at minimum every figure
implied by the claim table. No figures are rendered — later phases (esp. 16) drop in the panels.

Purpose: The figure specs are the per-phase deliverable contract (D-10, SC3) — they tell each later
phase exactly which panels it must produce, closing the claim → experiment loop.
Output: A populated `manuscript/figures/specs.typ` that compiles green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the claim-implied figure specs</name>
  <files>manuscript/figures/specs.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-10 spec fields + enumerated figures; §"Specific Ideas" speedup caveat)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (Pattern 3 figure-spec stub #figure; §"Honest Headline Numbers" for the F1 caveat)
    - manuscript/claims.typ (the claim row IDs each figure spec must reference — cross-reference F1..F7 back to C-rows)
    - manuscript/figures/specs.typ (the Wave-1 stub to replace)
  </read_first>
  <action>
    Replace the Wave-1 stub with the figure spec entries per RESEARCH Pattern 3: each spec is a
    captioned placeholder `#figure(rect(...)[ *SPEC Fn — title* ... Panels: ... Data: Phase N.
    Supports: Cx,Cy. Status: PENDING ], caption: [Placeholder — Phase 16 inserts the rendered panel.])
    <fig:...>`. Enumerate at minimum the claim-table-implied figures (D-10): F1 speedup-vs-RMSE
    (Phase 4; supports the amortization + speedup claim rows) — its panel/caption text MUST carry the
    Phase-4 honest caveat (median ~325x forward-pass, ~16x full posterior-sample workload, ADVI
    ~0.5 s/pair, RMSE parity, wider NPE intervals), NOT the bare >100x; F2 SBC rank histograms +
    coverage curve (Phase 5); F3 OOD ROC over the misspecification grid with the summary-orthogonal
    blind-spot named (Phase 5); F4 cross-method comparison (Phase 9, with the Phase-8 external corpus);
    F5 registration-UQ posterior widening (Phase 11); F6 spatial per-region Δρ map + uncertainty
    (Phase 12); F7 three-hypothesis Bayes factor simplex (Phase 13). Each spec: figure ID, working
    title, panel list, producing phase, the claim row ID(s) it supports (matching claims.typ), and a
    PENDING status placeholder. Use unique `<fig:...>` labels but do NOT `@`-reference any label that
    has no target elsewhere unless you also add the reference target (Pitfall 2 — safest is to define
    labels and not @-cite them from stubs). Do NOT paste code beyond the verified Pattern-3 idiom.
  </action>
  <acceptance_criteria>
    - `manuscript/figures/specs.typ` contains ≥7 `SPEC F` entries (F1..F7 minimum)
    - Each spec names a producing Phase (contains "Phase " / "Data:") and the supported claim row(s) (contains "Supports")
    - The F1 speedup spec text contains the honest caveat markers ("325" and "16" and "0.5"), not a bare >100x
    - Each spec carries a status placeholder (contains "PENDING")
    - `bash manuscript/build.sh` exits 0 with the specs rendering in the PDF
  </acceptance_criteria>
  <verify><automated>grep -c "SPEC F" manuscript/figures/specs.typ (expect ≥7); grep -c "Supports" manuscript/figures/specs.typ (expect ≥7); bash manuscript/build.sh; echo "exit=$?"</automated></verify>
  <done>All claim-implied figure specs are enumerated with ID/title/panels/producing-phase/supported-claims/status; F1 carries the honest caveat; the skeleton compiles green (SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. Static Typst source only; no runtime, network, input, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | overclaiming in figure-spec captions | mitigate | F1 caption carries the Phase-4 caveat; all specs carry PENDING status (honesty posture, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 with figure specs rendering.
- ≥7 specs each naming producing phase + supported claim rows; F1 caveated.
</verification>

<success_criteria>
Figure specs enumerate the panels each later phase must deliver (D-10, SC3), each linked to its
producing phase and supported claim rows, with the F1 speedup caveat present. Skeleton compiles green.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-04-SUMMARY.md` when done
</output>

---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - manuscript/figures/specs.typ
  - manuscript/figures/f1_speedup.typ
  - manuscript/figures/f1_speedup.yaml
  - manuscript/figures/f1_speedup_data/speedup.csv
autonomous: true
requirements: [D-10, D-14, D-16, SC3]
must_haves:
  truths:
    - "Each planned figure has a spec entry (not a rendered figure): figure ID, working title, panel list, producing phase, supported claim row(s), status placeholder (D-10)"
    - "Each spec also FORWARD-DECLARES its per-figure YAML+typ target path (figures/<name>.yaml + figures/<name>.typ) so later phases clone the F1 convention (D-14)"
    - "The specs enumerate at minimum the claim-table-implied figures: speedup-vs-RMSE, SBC rank histograms + coverage, OOD ROC, cross-method comparison, registration-UQ widening, spatial map, three-hypothesis BF (D-10, SC3)"
    - "Each spec references the claim row(s) it supports, closing the loop with claims.typ (D-07 traceability)"
    - "The speedup figure spec (F1) carries the Phase-4 honest caveat, not the bare >100x headline"
    - "ONE worked figure (F1 speedup-vs-RMSE) is rendered end-to-end and is FULLY YAML-driven: figures/f1_speedup.typ reads every graphical parameter from figures/f1_speedup.yaml via lib/figstyle.typ; NO panel size / axis limit / color / label is hard-coded in the .typ (D-14, D-16)"
    - "F1 uses honest Phase-4 numbers from its CSV data (median ~325x forward-pass, ~16x full workload, ADVI ~0.5 s/pair, RMSE 0.1271 vs 0.1270, intervals 0.884 vs 0.605)"
  artifacts:
    - path: "manuscript/figures/specs.typ"
      provides: "Figure spec entries (ID/title/panels/phase/claims/status + YAML target path) and the placed F1 exemplar"
      contains: "SPEC F"
      min_lines: 30
    - path: "manuscript/figures/f1_speedup.yaml"
      provides: "F1 graphical spec: panel geometry, axes/labels/ticks, series styling, legend, color roles (D-14)"
      contains: "panel"
    - path: "manuscript/figures/f1_speedup.typ"
      provides: "F1 figure drawn from f1_speedup.yaml via figstyle + colours.yaml roles; exports f1-speedup-figure"
      contains: "figstyle"
    - path: "manuscript/figures/f1_speedup_data/speedup.csv"
      provides: "Honest Phase-4 speedup/RMSE/interval numbers driving F1"
      contains: "0.1271"
  key_links:
    - from: "manuscript/figures/specs.typ"
      to: "manuscript/claims.typ"
      via: "each spec references the claim row IDs it supports"
      pattern: "Supports"
    - from: "manuscript/figures/f1_speedup.typ"
      to: "manuscript/figures/f1_speedup.yaml"
      via: "figstyle loads the per-figure yaml"
      pattern: "figstyle(\"f1_speedup\")"
    - from: "manuscript/figures/f1_speedup.typ"
      to: "manuscript/lib/figstyle.typ"
      via: "#import figstyle"
      pattern: "figstyle"
---

<objective>
Enumerate the figure specifications (one spec per planned figure: ID, title, panels, producing
phase, supported claim rows, status, AND its per-figure YAML+typ target path), and build the ONE
worked reference figure — F1 speedup-vs-RMSE — fully driven by `figures/f1_speedup.yaml` through
`lib/figstyle.typ` (D-14/D-16). F1 is the copy-able exemplar every later figure-producing phase
clones; F2–F7 remain specs.

Purpose: The specs are the per-phase deliverable contract (D-10, SC3) — they tell each later phase
which panels to produce AND where its YAML/typ pair lives. F1 proves the YAML-driven convention
compiles and renders with honest data.
Output: A populated `manuscript/figures/specs.typ` plus a rendered, YAML-driven `f1_speedup` figure;
the skeleton compiles green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the claim-implied figure specs with per-figure YAML target paths</name>
  <files>manuscript/figures/specs.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-10 spec fields + enumerated figures; D-14 per-figure YAML convention; §"Specific Ideas" speedup caveat)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (Pattern 3 figure-spec stub #figure; §"Honest Headline Numbers" for the F1 caveat)
    - manuscript/claims.typ (the claim row IDs each spec must reference — cross-reference F1..F7 back to C-rows)
    - manuscript/figures/specs.typ (the Wave-1 stub to replace)
  </read_first>
  <action>
    Replace the Wave-1 stub with the figure spec entries per RESEARCH Pattern 3: each spec is a
    captioned placeholder `#figure(rect(...)[ *SPEC Fn — title* ... Panels: ... Data: Phase N.
    Supports: Cx,Cy. Status: PENDING. YAML: figures/<name>.yaml + figures/<name>.typ ], caption:
    [Placeholder — the producing phase renders the panel via its per-figure YAML.]) <fig:...>`.
    Enumerate at minimum the claim-table-implied figures (D-10): F1 speedup-vs-RMSE (Phase 4;
    name f1_speedup) — panel/caption text MUST carry the Phase-4 honest caveat (median ~325x
    forward-pass, ~16x full posterior-sample workload, ADVI ~0.5 s/pair, RMSE parity, wider NPE
    intervals), NOT the bare >100x; F2 SBC rank histograms + coverage (Phase 5; f2_sbc); F3 OOD ROC
    over the misspecification grid with the summary-orthogonal blind-spot named (Phase 5; f3_ood);
    F4 cross-method comparison (Phase 9 + Phase-8 corpus; f4_comparator); F5 registration-UQ
    posterior widening (Phase 11; f5_registration); F6 spatial per-region Δρ map + uncertainty
    (Phase 12; f6_spatial); F7 three-hypothesis Bayes factor simplex (Phase 13; f7_threebf). Each
    spec: figure ID, working title, panel list, producing phase, supported claim row ID(s) (matching
    claims.typ), a PENDING status, and the per-figure YAML+typ target path (D-14). Use unique
    `<fig:...>` labels; do NOT `@`-reference a label with no target (Pitfall 2). Do NOT paste code
    beyond the verified Pattern-3 idiom.
  </action>
  <acceptance_criteria>
    - `manuscript/figures/specs.typ` contains ≥7 `SPEC F` entries (F1..F7 minimum)
    - Each spec names a producing Phase (contains "Phase "/"Data:"), the supported claim row(s) (contains "Supports"), and a per-figure YAML target (contains "figures/" and ".yaml")
    - The F1 spec text contains the honest caveat markers ("325" and "16" and "0.5"), not a bare >100x
    - Each spec carries a status placeholder (contains "PENDING")
    - `bash manuscript/build.sh` exits 0 with the specs rendering in the PDF
  </acceptance_criteria>
  <verify><automated>grep -c "SPEC F" manuscript/figures/specs.typ (expect ≥7); grep -c "Supports" manuscript/figures/specs.typ (expect ≥7); grep -c ".yaml" manuscript/figures/specs.typ (expect ≥7); bash manuscript/build.sh; echo "exit=$?"</automated></verify>
  <done>All claim-implied figure specs are enumerated with ID/title/panels/producing-phase/supported-claims/status/YAML-target; F1 carries the honest caveat; the skeleton compiles green (SC3).</done>
</task>

<task type="auto">
  <name>Task 2: Build the worked F1 speedup figure — fully YAML-driven via figstyle</name>
  <files>manuscript/figures/f1_speedup_data/speedup.csv, manuscript/figures/f1_speedup.yaml, manuscript/figures/f1_speedup.typ, manuscript/figures/specs.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-14 YAML must cover geometry/axes/series/legend/color; D-15 figstyle contract; D-16 F1 honest numbers)
    - manuscript/lib/figstyle.typ (the loader API: figstyle(name) -> {panel, axes, series, legend, color(...)} — the exact keys to consume; from Plan 01)
    - manuscript/colours.yaml (palette roles the YAML references, e.g. tool_series.* / model_series.* / reference.*)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/figures/benchmark_hein.typ (the lilaq diagram/plot idiom to mirror — but pull all geometry/limits/colors from figstyle, not hard-coded)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-CONTEXT.md + STATE.md (the honest Phase-4 numbers to seed the CSV)
  </read_first>
  <action>
    Create `manuscript/figures/f1_speedup_data/speedup.csv` with the honest Phase-4 numbers (D-16):
    per-method (NPE, ADVI) columns for wall-clock (NPE forward-pass vs ADVI ~0.5 s/pair), the
    speedup factors (median ~325x forward-pass; ~16x full posterior-sample/N=2000 workload), RMSE
    (NPE 0.1271 vs ADVI 0.1270), and interval width (NPE 0.884 vs ADVI 0.605). Structure the CSV so
    the figure can draw a speedup-vs-RMSE panel (and/or a paired bar of the two speedup regimes) —
    header row + data rows; keep the exact numeric strings so the honesty gate can grep them.

    Create `manuscript/figures/f1_speedup.yaml` declaring ALL graphical aspects (D-14) — this is the
    file the user edits to restyle F1 without touching Typst: `panel` (width, height, scale),
    `axes` (xlim, ylim, xticks, yticks, xlabel e.g. "RMSE (ρ)", ylabel e.g. "Speedup (×, log)",
    title), `series` entries for NPE-forward-pass and NPE-full-workload (each with a color given as a
    colours.yaml ROLE, e.g. "model_series.meta_learner" / "tool_series.hero", plus thickness, mark,
    and an optional "#rrggbb" override field left commented), `legend` (position, cols), `text`
    (size). Reference roles that actually exist in colours.yaml (read it first); if a needed semantic
    role is missing, add ONE new role to colours.yaml rather than inlining hex (palette stays the
    single source — D-14).

    Create `manuscript/figures/f1_speedup.typ`: `#import "@preview/lilaq:0.6.0" as lq` (or the
    version the template uses), `#import "../lib/figstyle.typ": figstyle`. Load `#let fs =
    figstyle("f1_speedup")` and `#let data = csv("f1_speedup_data/speedup.csv")`. Build the panel
    with EVERY graphical value pulled from `fs` — `lq.diagram(width: fs.panel.width, height:
    fs.panel.height, xlim: fs.axes.xlim, ylim: fs.axes.ylim, xlabel: fs.axes.xlabel, ylabel:
    fs.axes.ylabel, ...)` and series drawn with `stroke: (paint: fs.color(<series role>), thickness:
    ...from fs...)`. NO literal `cm` dimension, axis limit, color, or label string may appear in the
    `.typ` — all come from `fs` (D-14/D-16). Export `#let f1-speedup-figure = scale(x: fs.panel.scale
    * 100%, ...) [ ... ]` and end with a trailing bare `#f1-speedup-figure` for standalone preview
    (discarded on #import), mirroring the template's module contract. Set a standalone `#set
    page(width: auto, height: auto, margin: 4pt)` guarded so it doesn't affect the manuscript when
    imported (template idiom).

    Place F1 in the manuscript: in `manuscript/figures/specs.typ`, `#import "f1_speedup.typ":
    f1-speedup-figure` and render it as a real `#figure(f1-speedup-figure, caption: [Speedup vs RMSE
    — amortized NPE vs per-dataset ADVI. Honest Phase-4 numbers: ...caveat...]) <fig:f1_speedup>`
    replacing/beside the F1 PENDING spec (F1 is now RENDERED; F2–F7 stay PENDING). Prove it compiles
    both standalone (`typst compile --root manuscript manuscript/figures/f1_speedup.typ` → a preview
    PDF) and via `bash manuscript/build.sh` (integrated). Assert the .typ has no hard-coded geometry.
  </action>
  <acceptance_criteria>
    - `manuscript/figures/f1_speedup.yaml` declares panel geometry, axes (xlim/ylim/labels), ≥1 series with a color role, and legend keys (D-14)
    - `manuscript/figures/f1_speedup.typ` contains `figstyle("f1_speedup")` and draws using `fs.` accessors; grep finds NO hard-coded `cm` dimension and NO `rgb("#` literal in the .typ (all styling via fs/colours.yaml)
    - `manuscript/figures/f1_speedup_data/speedup.csv` contains "0.1271", "0.884", and a "325" and "16" and "0.5" bearing the honest numbers
    - `typst compile --root manuscript manuscript/figures/f1_speedup.typ /tmp/f1.pdf` (or scratch path) exits 0 (standalone renders)
    - specs.typ imports `f1-speedup-figure` and renders it under `<fig:f1_speedup>`; F1 is no longer PENDING
    - `bash manuscript/build.sh` exits 0 and the integrated main.pdf renders F1
    - Editing a value in f1_speedup.yaml (e.g. panel width) and recompiling changes the output (spot-confirm the YAML actually drives rendering); revert the probe edit
  </acceptance_criteria>
  <verify><automated>grep -q 'figstyle("f1_speedup")' manuscript/figures/f1_speedup.typ && echo FIGSTYLE_USED; ! grep -Eq '[0-9]+(\.[0-9]+)?cm' manuscript/figures/f1_speedup.typ && echo NO_HARDCODED_CM; ! grep -q 'rgb("#' manuscript/figures/f1_speedup.typ && echo NO_INLINE_HEX; grep -Eq "0\.1271" manuscript/figures/f1_speedup_data/speedup.csv && echo HONEST_NUMS; bash manuscript/build.sh; echo "exit=$?"</automated></verify>
  <done>F1 is a rendered, fully YAML-driven exemplar (all graphical aspects in f1_speedup.yaml, resolved via figstyle + colours.yaml, honest Phase-4 data in CSV); it compiles standalone and integrated; F2–F7 remain specs naming their YAML targets — the convention is proven and copy-able (D-14/D-16, SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. Static Typst/YAML/CSV source only; no runtime, network (beyond Typst package
fetch), input, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | overclaiming in the F1 figure/caption | mitigate | F1 caption + CSV carry the Phase-4 caveat (325x/16x/0.5s, RMSE parity); F2–F7 carry PENDING status (honesty posture, not security) |
| T-10-14 | Tampering | hard-coded styling defeating the YAML convention | mitigate | Verify asserts no `cm`/inline-hex in f1_speedup.typ — all graphical values must flow from the YAML (reliability, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 with ≥7 specs + the rendered F1 figure.
- f1_speedup.typ pulls all geometry/color/labels from f1_speedup.yaml via figstyle (no hard-coded styling).
- F1 CSV carries the honest Phase-4 numbers; F1 caption caveated.
</verification>

<success_criteria>
Figure specs enumerate the panels each later phase must deliver with per-figure YAML targets (D-10,
D-14, SC3); the worked F1 figure is rendered and fully YAML-driven with honest data (D-16), proving
the convention. Skeleton compiles green.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-04-SUMMARY.md` when done
</output>

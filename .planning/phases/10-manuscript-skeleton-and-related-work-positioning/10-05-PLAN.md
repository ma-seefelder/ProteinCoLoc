---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - manuscript/build.sh
autonomous: true
requirements: [D-12, SC1, SC2, SC3]
must_haves:
  truths:
    - "After all Wave-2 content is merged, `bash manuscript/build.sh` compiles the full manuscript to PDF at exit 0 (D-12, SC1)"
    - "The compile gate is upgraded from compile-only to compile + content assertions so later phases regression-check the skeleton (D-12)"
    - "The gate asserts the claim table renders (SC1), the related-work matrix + four Tapqir axes are present (SC2), and ≥7 figure specs enumerate producing phases (SC3)"
    - "The gate reads typst's own exit status (no pipe) and fails non-zero if any assertion fails"
    - "The integrated PDF renders the claim table AND the related-work matrix AND the figure specs together (final DoD)"
  artifacts:
    - path: "manuscript/build.sh"
      provides: "Full regression gate: typst compile (exit-code-correct) + content assertions for SC1/SC2/SC3"
      contains: "typst compile"
      min_lines: 10
  key_links:
    - from: "manuscript/build.sh"
      to: "manuscript/sections/related_work.typ"
      via: "grep content assertion for the four Tapqir axes"
      pattern: "related_work"
    - from: "manuscript/build.sh"
      to: "manuscript/figures/specs.typ"
      via: "grep content assertion for SPEC count"
      pattern: "specs.typ"
---

<objective>
Close the phase: upgrade `build.sh` from a compile-only gate to a full regression gate that runs
`typst compile` (exit-code-correct) AND asserts the Wave-2 content is present (claim table,
related-work matrix + four axes, ≥7 figure specs), then confirm the integrated manuscript compiles
green with everything rendering together — the Definition of Done (D-12).

Purpose: Give later phases a single scriptable regression gate that fails if the skeleton stops
building or if any load-bearing content is dropped. This is the phase's falsifiable DoD.
Output: An upgraded `manuscript/build.sh`; a green integrated `manuscript/main.pdf`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade build.sh to a content-asserting regression gate and prove the integrated DoD</name>
  <files>manuscript/build.sh</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (§"Compile gate" exit-code-correct; Pitfall 1 pipe masks exit; §"Validation Architecture" Phase Requirements → Test Map)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md (Per-Task Verification Map; the grep content-check commands per SC)
    - manuscript/build.sh (the compile-only gate from Plan 01 to extend)
    - manuscript/claims.typ, manuscript/sections/related_work.typ, manuscript/figures/specs.typ (the content the assertions target)
  </read_first>
  <action>
    Extend `manuscript/build.sh` (keep `set -euo pipefail`, forward-slash repo-relative paths). Step
    1: run `typst compile "manuscript/main.typ" "manuscript/main.pdf"` on its own line with NO pipe
    (Pitfall 1) — with `set -e` a non-zero typst exit aborts the script. Step 2: add content
    assertions that exit non-zero on failure, one per success criterion: SC1 — `test -f
    manuscript/main.pdf` and `grep -q "claim_table" manuscript/claims.typ`; SC2 — assert
    related_work.typ contains all four axis terms (`grep -qi amortiz`, `grep -qi "SBC\|calibrat"`,
    `grep -qi registration`, `grep -qi spatial`) and a `table(` matrix; SC3 — assert
    `[ "$(grep -c 'SPEC F' manuscript/figures/specs.typ)" -ge 7 ]`. Guard each grep count against the
    header-comment self-invalidation trap by filtering comment lines where relevant
    (`grep -v '^//' | grep -c`). On all-pass, echo a clear "OK: DoD met — compiles + claim table +
    matrix + ≥7 specs". Then run the upgraded gate and confirm exit 0. Finally open/spot-confirm the
    integrated `main.pdf` renders the claim table, the related-work matrix, and the figure specs
    together (final DoD). Do NOT pipe the compile line; do NOT use bare `== 0` grep gates on unfiltered files.
  </action>
  <acceptance_criteria>
    - `manuscript/build.sh` runs `typst compile` on a line with no `|` pipe
    - `manuscript/build.sh` contains content assertions for all four Tapqir axes, a matrix `table(`, `claim_table`, and a `SPEC F` count ≥7
    - `bash manuscript/build.sh` exits 0 on the fully-merged manuscript and produces `manuscript/main.pdf`
    - Deliberately blanking related_work.typ's axis content makes `bash manuscript/build.sh` exit non-zero (revert after checking) — proves the assertions are falsifiable
    - The integrated `main.pdf` renders the claim table, the related-work matrix, and the figure specs
  </acceptance_criteria>
  <verify>bash manuscript/build.sh; echo "exit=$?"  (expect exit=0 with "DoD met" message and manuscript/main.pdf present)</verify>
  <done>The regression gate compiles the full manuscript and asserts SC1/SC2/SC3 content; the integrated PDF renders claim table + matrix + specs; the phase DoD (D-12) is met and scriptable for later phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. The gate is a local shell script over static Typst source; no runtime service,
network I/O, untrusted input, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | build.sh gate correctness | mitigate | Compile line has no pipe (reads typst's own exit); grep counts filter comment lines to avoid header self-invalidation (reliability, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 with the "DoD met" message and produces `manuscript/main.pdf`.
- Content assertions fail non-zero when load-bearing content is removed (falsifiable regression gate).
- Integrated PDF renders claim table + related-work matrix + figure specs together.
</verification>

<success_criteria>
The scriptable compile gate (D-12) is upgraded to a full regression gate asserting SC1 (compiles +
claim table), SC2 (matrix + four Tapqir axes), and SC3 (≥7 figure specs); the integrated manuscript
compiles green with everything rendering — the phase Definition of Done.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-05-SUMMARY.md` when done
</output>

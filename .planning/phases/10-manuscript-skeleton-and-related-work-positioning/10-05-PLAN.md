---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 05
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - manuscript/build.ps1
  - manuscript/build.sh
autonomous: true
requirements: [D-12, D-14, D-16, D-17, SC1, SC2, SC3]
must_haves:
  truths:
    - "After all Wave-2/3 content is merged, BOTH build.ps1 (primary) and build.sh (fallback) compile the full manuscript to PDF at exit 0 (D-12, D-17, SC1)"
    - "Both gates are upgraded from compile-only to compile + content assertions so later phases regression-check the skeleton (D-12)"
    - "The gates assert: the claim table renders (SC1); the related-work matrix + four Tapqir axes are present (SC2); ≥7 figure specs enumerate producing phases (SC3); the per-figure YAML machinery holds — lib/figstyle.typ exists, f1_speedup.yaml exists, and f1_speedup.typ has NO hard-coded geometry/hex (D-14/D-16)"
    - "Both gates read the compiler's own exit status (no pipe) and fail non-zero if any assertion fails"
    - "The D-07 cross-check holds: every 'Supports: Cx' token in figures/specs.typ resolves to a claim id in claims.typ"
    - "The integrated PDF renders the claim table AND the related-work matrix AND the figure specs AND the rendered F1 figure together (final DoD)"
  artifacts:
    - path: "manuscript/build.ps1"
      provides: "Primary Windows regression gate: typst compile (exit-code-correct) + content assertions for SC1/SC2/SC3 + YAML-machinery checks"
      contains: "typst"
      min_lines: 15
    - path: "manuscript/build.sh"
      provides: "Fallback/CI regression gate mirror with the same assertions"
      contains: "typst compile"
      min_lines: 12
  key_links:
    - from: "manuscript/build.sh"
      to: "manuscript/sections/related_work.typ"
      via: "grep content assertion for the four Tapqir axes"
      pattern: "related_work"
    - from: "manuscript/build.sh"
      to: "manuscript/figures/f1_speedup.typ"
      via: "grep assertion that F1 is YAML-driven (figstyle used, no hard-coded geometry)"
      pattern: "figstyle"
    - from: "manuscript/build.ps1"
      to: "manuscript/figures/specs.typ"
      via: "SPEC count + Supports->claims cross-check assertion"
      pattern: "specs"
---

<objective>
Close the phase: upgrade BOTH `build.ps1` (primary) and `build.sh` (fallback) from compile-only
gates to full regression gates that run the compiler exit-code-correct AND assert the Wave-2/3
content is present (claim table, related-work matrix + four axes, ≥7 figure specs, the D-07
Supports→claims cross-check, AND the per-figure YAML machinery: figstyle loader present, F1 YAML
present, F1 typ has no hard-coded styling). Then confirm the integrated manuscript compiles green
with everything — claim table, matrix, specs, and the rendered F1 figure — rendering together (D-12,
D-14, D-16, D-17).

Purpose: Give later phases a single scriptable regression gate (on Windows via ps1, on CI via sh)
that fails if the skeleton stops building, if load-bearing content is dropped, or if the YAML-driven
figure convention is violated. This is the phase's falsifiable DoD.
Output: Upgraded `manuscript/build.ps1` + `manuscript/build.sh`; a green integrated `manuscript/main.pdf`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade both build scripts to content-asserting regression gates and prove the integrated DoD</name>
  <files>manuscript/build.ps1, manuscript/build.sh</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (§"Compile gate" exit-code-correct; Pitfall 1 pipe masks exit; §"Validation Architecture" Phase Requirements → Test Map)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-14/D-16 the YAML machinery to assert; D-17 dual build)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md (Per-Task Verification Map; the grep content-check commands per SC)
    - manuscript/build.ps1, manuscript/build.sh (the compile-only gates from Plan 01 to extend)
    - manuscript/claims.typ, manuscript/sections/related_work.typ, manuscript/figures/specs.typ, manuscript/figures/f1_speedup.typ, manuscript/figures/f1_speedup.yaml (the content the assertions target)
  </read_first>
  <action>
    Extend BOTH gates with the SAME assertion set (D-17), keeping each exit-code-correct (no pipe on
    the compile line). build.sh keeps `set -euo pipefail` + forward-slash paths; build.ps1 keeps
    `$ErrorActionPreference = "Stop"` + `exit $LASTEXITCODE` semantics and uses PowerShell-native
    checks (`Select-String`, `Test-Path`, and `if (...) { exit 1 }`).

    Step 1 — compile: run `typst compile --root manuscript --font-path <fonts> "manuscript/main.typ"
    "manuscript/main.pdf"` on its own line, no pipe; a non-zero compiler exit aborts the gate.

    Step 2 — content assertions, each exits non-zero on failure, one per success criterion:
      - SC1: `manuscript/main.pdf` exists AND `claims.typ` contains `claim_table`.
      - SC2: `related_work.typ` contains all four axis terms (amortiz; SBC|calibrat; registration;
        spatial) AND a `table(` matrix.
      - SC3: `[ SPEC-F count in figures/specs.typ ] >= 7`.
      - D-07 cross-check: for every `Supports: Cx` token in figures/specs.typ, assert a matching id
        exists in claims.typ; fail non-zero otherwise.
      - D-14/D-16 YAML-machinery checks (NEW): `lib/figstyle.typ` exists and contains `figstyle`;
        `figures/f1_speedup.yaml` exists; `figures/f1_speedup.typ` contains `figstyle("f1_speedup")`
        AND contains NO hard-coded geometry (`grep -Eq '[0-9]+(\.[0-9]+)?cm'` must find nothing) and
        NO inline hex (`rgb("#` must find nothing) — proving F1 is fully YAML-driven.
      Guard grep counts against header-comment self-invalidation (filter `^//` / `^#` lines where a
      literal token could appear in a comment).

    Step 3: on all-pass, echo a clear "OK: DoD met — compiles + claim table + matrix + >=7 specs +
    YAML-driven F1". Run BOTH gates and confirm exit 0. Finally spot-confirm the integrated main.pdf
    renders the claim table, the related-work matrix, the figure specs, and the F1 figure together.

    Prove falsifiability: temporarily blank related_work.typ's axis content (or inject a `2cm` into
    f1_speedup.typ) and confirm each gate exits non-zero; revert after checking.
  </action>
  <acceptance_criteria>
    - Both build.ps1 and build.sh run `typst compile` on a line with no `|` pipe
    - Both gates assert: four Tapqir axes, a matrix `table(`, `claim_table`, SPEC-F count ≥7, the D-07 Supports→claims cross-check, figstyle present, f1_speedup.yaml present, and F1 typ has no `cm`/inline-hex
    - `bash manuscript/build.sh` exits 0 on the fully-merged manuscript and produces manuscript/main.pdf with the "DoD met" message
    - `pwsh -File manuscript/build.ps1` (fallback `powershell -File`) exits 0 with the same message and PDF
    - Blanking related_work.typ axis content OR injecting a `cm` literal into f1_speedup.typ makes BOTH gates exit non-zero (revert after) — assertions are falsifiable
    - The integrated main.pdf renders claim table + related-work matrix + figure specs + F1 figure
  </acceptance_criteria>
  <verify><automated>bash manuscript/build.sh; echo "sh_exit=$?"; pwsh -File manuscript/build.ps1 2>/dev/null || powershell -File manuscript/build.ps1; echo "ps_exit=$?"  (expect both exit 0 with "DoD met" and manuscript/main.pdf present)</automated></verify>
  <done>Both regression gates compile the full manuscript and assert SC1/SC2/SC3 + D-07 cross-check + the D-14/D-16 YAML machinery; the integrated PDF renders claim table + matrix + specs + F1; the phase DoD (D-12/D-17) is met and scriptable on both Windows and CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. The gates are local shell/PowerShell scripts over static Typst source; no runtime
service, network I/O, untrusted input, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | build gate correctness | mitigate | Both gates' compile line has no pipe (reads the compiler's own exit); grep counts filter comment lines to avoid header self-invalidation (reliability, not security) |
| T-10-17 | Tampering | ps1/sh gate drift | mitigate | Both scripts assert the identical content set so the Windows and CI gates cannot silently diverge (reliability, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- Both `bash manuscript/build.sh` and `pwsh -File manuscript/build.ps1` exit 0 with the "DoD met" message and produce manuscript/main.pdf.
- Content + YAML-machinery assertions fail non-zero when load-bearing content is removed or F1 styling is hard-coded (falsifiable regression gate).
- Integrated PDF renders claim table + related-work matrix + figure specs + F1 figure together.
</verification>

<success_criteria>
The scriptable dual compile gate (D-12, D-17) is upgraded to a full regression gate asserting SC1
(compiles + claim table), SC2 (matrix + four Tapqir axes), SC3 (≥7 figure specs), the D-07
cross-check, and the D-14/D-16 YAML-driven-figure machinery; the integrated manuscript compiles
green with everything rendering — the phase Definition of Done.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-05-SUMMARY.md` when done
</output>

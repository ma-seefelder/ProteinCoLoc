---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - manuscript/sections/related_work.typ
autonomous: true
requirements: [D-08, D-09, SC2]
must_haves:
  truths:
    - "Related work is drafted as a comparison matrix + prose (D-08)"
    - "The matrix rows are ProteinCoLoc v2.0, Tapqir, Costes randomization, Manders M1/M2, Pearson/Spearman, ProteinCoLoc v1 ADVI; columns are amortized inference, calibrated UQ/SBC, Bayes-factor, registration-UQ-as-latent, spatial per-region map, OOD flag, speed (D-08)"
    - "A mandatory Tapqir differentiation paragraph explicitly covers all four axes: amortization, SBC/calibration, registration-UQ, spatial map (D-09, SC2)"
    - "The three ASSUMED Tapqir matrix cells (formal SBC, explicit BF, registration-UQ) are verified against Ordabayev et al. 2022 before prose is locked, or framed conservatively if unverifiable (D-09)"
    - "Prose frames honestly: v2.0 calibration is 'under the simulator', the fixed-summary OOD blind spot is named, and Tapqir vs v2.0 are positioned as overlapping-but-distinct data regimes rather than blanket superiority"
    - "Every @citekey used resolves to an entry in refs.bib (no dangling citation → compile stays green)"
  artifacts:
    - path: "manuscript/sections/related_work.typ"
      provides: "Comparison matrix (Typst table) + four-axis Tapqir differentiation prose"
      contains: "table"
      min_lines: 30
  key_links:
    - from: "manuscript/sections/related_work.typ"
      to: "manuscript/refs.bib"
      via: "@ordabayev2022 / @costes2004 / @manders1993 citations"
      pattern: "@ordabayev2022"
---

<objective>
Draft the related-work section: a capability comparison matrix across the six methods and seven
axes, plus the mandatory four-axis Tapqir differentiation prose — honestly framed and factually
defensible under reviewer scrutiny. Verify the three flagged-ASSUMED Tapqir cells against the eLife
paper before locking the prose.

Purpose: Positioning is the reviewer-facing spine of the paper (D-08/D-09, SC2); it shapes which
experiments later phases must run. Accuracy here is load-bearing.
Output: A populated `manuscript/sections/related_work.typ` with matrix + prose that compiles green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the three ASSUMED Tapqir matrix cells against Ordabayev et al. 2022</name>
  <files>manuscript/sections/related_work.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (§"Related Work — Factual Accuracy"; the ⚠ ASSUMED cells; §"Assumptions Log" A1/A2/A3; §"Open Questions" Q1)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-09 accuracy requirement)
  </read_first>
  <action>
    Verify the three ASSUMED Tapqir comparison cells flagged ⚠ in RESEARCH.md against the Tapqir
    paper (Ordabayev, Friedman, Gelles, Theobald, eLife 2022;11:e73860, DOI 10.7554/eLife.73860):
    (A1) whether Tapqir runs formal SBC / rank-uniformity calibration; (A2) whether Tapqir emits an
    explicit Bayes factor / model-comparison output vs only per-spot classification probabilities;
    (A3) whether Tapqir propagates registration/drift uncertainty into the colocalization call as an
    inferred latent or relies on external/preprocessing drift correction. Fetch the eLife article
    (https://elifesciences.org/articles/73860) and read the methods/model description to confirm or
    correct each cell. Record the finding for each of A1/A2/A3 as a short comment block at the top of
    `related_work.typ` (a `//`-comment provenance note: cell → verdict → source). Where the paper
    confirms the ASSUMED value, keep the matrix cell. Where it contradicts or is inconclusive, adjust
    the cell and frame the prose conservatively (per D-09 the delta must survive scrutiny — a softer,
    accurate claim beats an overclaim). Do NOT lock the Task-2 prose until these three verdicts are
    recorded.
  </action>
  <acceptance_criteria>
    - `manuscript/sections/related_work.typ` contains a `//` provenance comment recording a verdict for each of A1 (formal SBC), A2 (explicit BF), A3 (registration-UQ)
    - Each verdict names its source (eLife article / DOI 10.7554/eLife.73860)
    - Any cell contradicted by the paper is corrected before Task 2 writes the matrix
  </acceptance_criteria>
  <verify>grep -c "10.7554/eLife.73860\|elifesciences.org/articles/73860" manuscript/sections/related_work.typ (expect ≥1); grep -ci "formal SBC\|Bayes factor\|registration" manuscript/sections/related_work.typ (expect ≥3)</verify>
  <done>The three ASSUMED Tapqir cells carry a recorded verdict + source; contradicted cells are corrected; prose is safe to lock.</done>
</task>

<task type="auto">
  <name>Task 2: Write the comparison matrix and four-axis Tapqir differentiation prose</name>
  <files>manuscript/sections/related_work.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (§"Comparison matrix (seed for related_work.typ)"; §"The four-axis Tapqir differentiation — honest framing"; §"Citation Entries" for the exact refs.bib keys)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-08 matrix rows/columns; D-09 four mandatory axes + honesty posture)
    - manuscript/refs.bib (the citation keys that must exist before @-citing them — Pitfall 2)
    - manuscript/sections/related_work.typ (the verdict provenance comment from Task 1)
  </read_first>
  <action>
    Replace the Wave-1 stub body with the related-work content. Render the comparison matrix as a
    Typst `table()` per D-08: rows = ProteinCoLoc v2.0, Tapqir, Costes randomization, Manders M1/M2,
    Pearson/Spearman, ProteinCoLoc v1 (ADVI); columns = amortized inference, calibrated UQ/SBC,
    Bayes-factor/model comparison, registration uncertainty as latent, spatial (per-region) map,
    misspecification/OOD flag, speed. Populate cells from RESEARCH §"Comparison matrix", using the
    Task-1-verified values for the three Tapqir cells. Then write the mandatory four-axis Tapqir
    differentiation paragraph(s) (D-09, SC2) explicitly covering: (1) amortization — v2.0 trains once
    and infers in a single ms forward pass vs Tapqir's per-dataset SVI (the cleanest delta); (2)
    SBC/calibration — frame as "v2.0 provides a formal SBC coverage proof, under the simulator", not
    "Tapqir has none"; (3) registration-UQ — v2.0 (Phase 11) promotes registration to an inferred
    latent so the posterior widens honestly, per the Task-1 verdict on Tapqir's drift handling; (4)
    spatial map — frame as complementary domains (v2.0 dense/diffuse per-region Δρ map vs Tapqir's
    sparse single-molecule AOIs), not blanket superiority. Include the overarching honesty point:
    Tapqir and v2.0 target partly different data regimes; state v2.0's calibration as "under the
    simulator" and name the summary-orthogonal OOD blind spot. Cite `@ordabayev2022`, `@costes2004`,
    `@manders1993`, and `@talts2018` where relevant — only keys that exist in refs.bib (Pitfall 2).
    Do NOT paste code; follow the verified Typst table idiom in RESEARCH.md.
  </action>
  <acceptance_criteria>
    - `related_work.typ` contains a Typst `table(` with all six method rows and seven capability columns
    - The prose explicitly names all four axes: amortization, SBC/calibration, registration, spatial map
    - The prose contains "under the simulator" and names the OOD/summary-orthogonal blind spot (honesty posture)
    - `@ordabayev2022` is cited and resolves in refs.bib (no dangling citation)
    - `bash manuscript/build.sh` exits 0 with the matrix rendering in the PDF
    - The four-axis framing matches the Task-1 verified cell values (no overclaim beyond what the paper supports)
  </acceptance_criteria>
  <verify>grep -qi "amortiz" manuscript/sections/related_work.typ && grep -qi "SBC\|calibrat" manuscript/sections/related_work.typ && grep -qi "registration" manuscript/sections/related_work.typ && grep -qi "spatial" manuscript/sections/related_work.typ && echo "4 axes present"; bash manuscript/build.sh; echo "exit=$?"</verify>
  <done>The comparison matrix and honest four-axis Tapqir differentiation are drafted, cells match the verified paper reading, and the skeleton compiles green (SC2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. Static Typst source + one read-only web fetch of a public paper; no runtime,
no untrusted input processing, no data or auth surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Repudiation | unverified related-work claims | mitigate | Task-1 records per-cell verdict + source against DOI 10.7554/eLife.73860 before prose locks (scientific accuracy, not security) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 with the comparison matrix rendering.
- All four Tapqir axes appear; honesty framing ("under the simulator", OOD blind spot) present.
- Per-cell verdicts for A1/A2/A3 recorded with source.
</verification>

<success_criteria>
Related work is drafted as matrix + prose (D-08) with the mandatory four-axis Tapqir differentiation
(D-09, SC2), the three ASSUMED cells verified against the eLife paper, and honest framing throughout.
Skeleton compiles green.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-03-SUMMARY.md` when done
</output>

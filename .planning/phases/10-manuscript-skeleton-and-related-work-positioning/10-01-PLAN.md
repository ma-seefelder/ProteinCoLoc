---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manuscript/main.typ
  - manuscript/refs.bib
  - manuscript/colours.yaml
  - manuscript/build.ps1
  - manuscript/build.sh
  - manuscript/lib/template.typ
  - manuscript/lib/helpers.typ
  - manuscript/lib/curves.typ
  - manuscript/lib/tiered-bib.typ
  - manuscript/lib/abbreviations.typ
  - manuscript/lib/figstyle.typ
  - manuscript/styles/nature.csl
  - manuscript/fonts/
  - manuscript/claims.typ
  - manuscript/figures/specs.typ
  - manuscript/sections/introduction.typ
  - manuscript/sections/related_work.typ
  - manuscript/sections/methods.typ
  - manuscript/sections/results.typ
  - manuscript/sections/discussion.typ
  - manuscript/sections/availability.typ
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-11, D-12, D-13, D-15, D-17, SC1]
must_haves:
  truths:
    - "manuscript/ is founded on the BI Typst template: lib/{template,helpers,curves,tiered-bib,abbreviations}.typ, styles/nature.csl, fonts/, colours.yaml, and build.ps1 are COPIED in from C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper (D-13)"
    - "The BI template repo is NOT modified — it is a read-only source; only files under manuscript/ are written (D-13, D-03)"
    - "src/, bayes.jl, colocalization.jl, spike/ model code, and the existing manuscript pipelines are untouched (D-03)"
    - "main.typ is adapted to ProteinCoLoc via `#show: project.with(...)` (title/authors/abstract) and `#show: tiered-bib(bib: \"refs.bib\", style: \"styles/nature.csl\", read: path => read(path))`, then #includes our section set + the claim table + figures/specs.typ (D-13, D-04)"
    - "manuscript/lib/figstyle.typ exports figstyle(name) that loads figures/<name>.yaml, merges over documented defaults, and resolves color roles against colours.yaml with per-figure hex override (D-15, D-14)"
    - "Both build.ps1 (primary) and build.sh (fallback) run typst compile exit-code-correct (no pipe) with --root manuscript --font-path <fonts> and produce manuscript/main.pdf (D-17, D-12)"
    - "All section stubs exist with a one-line intent comment and a // TODO(phase N) marker; no stub emits a dangling @citekey/@label (compile stays green) (D-05, D-11)"
  artifacts:
    - path: "manuscript/lib/template.typ"
      provides: "Copied project() page/text/heading/figure rule set from the BI template"
      contains: "project"
    - path: "manuscript/colours.yaml"
      provides: "Shared palette (single source of hex) copied from the BI template"
      contains: "tool_series"
    - path: "manuscript/lib/figstyle.typ"
      provides: "Per-figure YAML loader: figstyle(name) -> merged dict with color roles resolved against colours.yaml"
      contains: "figstyle"
    - path: "manuscript/main.typ"
      provides: "ProteinCoLoc-adapted entry: project.with + tiered-bib + section includes + claim table + figure specs"
      contains: "tiered-bib"
    - path: "manuscript/build.ps1"
      provides: "Primary Windows compile gate (exit-code-correct, --font-path, --root)"
      contains: "typst"
    - path: "manuscript/build.sh"
      provides: "Fallback/CI compile gate mirror"
      contains: "typst compile"
    - path: "manuscript/refs.bib"
      provides: "BibTeX store seeded with load-bearing references + folded self-cite"
      contains: "@article{ordabayev2022"
  key_links:
    - from: "manuscript/main.typ"
      to: "manuscript/lib/template.typ"
      via: "#import project"
      pattern: "#import \"lib/template.typ\""
    - from: "manuscript/main.typ"
      to: "manuscript/refs.bib"
      via: "tiered-bib bib argument"
      pattern: "refs.bib"
    - from: "manuscript/lib/figstyle.typ"
      to: "manuscript/colours.yaml"
      via: "yaml() palette load + role resolution"
      pattern: "yaml"
    - from: "manuscript/build.ps1"
      to: "manuscript/main.typ"
      via: "typst compile invocation"
      pattern: "main.typ"
---

<objective>
Found the manuscript on the user's existing Typst template and stand up a compiling, template-based
skeleton plus the per-figure-YAML machinery. Copy the BI method-paper template's foundation
(`lib/`, `styles/nature.csl`, `fonts/`, `colours.yaml`, `build.ps1`) into a new top-level
`manuscript/` tree, adapt `main.typ` to ProteinCoLoc, add the `lib/figstyle.typ` per-figure YAML
loader (D-14/D-15), create modular section stubs, seed `refs.bib`, and provide a dual
`build.ps1`(primary)/`build.sh`(fallback) exit-code-correct compile gate. This is the provable
green baseline every later plan hangs off.

Purpose: Reuse the mature template (page rules, tiered bibliography, helpers, bundled fonts, shared
palette) instead of a from-scratch skeleton, and establish the YAML-driven figure convention so the
user can change any graphical aspect of a figure by editing a YAML file (D-14).
Output: A `manuscript/` tree that compiles to `manuscript/main.pdf` at exit 0 via both build scripts.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md
@CITATION.bib
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the BI template foundation into manuscript/ and adapt main.typ + section stubs + refs.bib</name>
  <files>manuscript/lib/template.typ, manuscript/lib/helpers.typ, manuscript/lib/curves.typ, manuscript/lib/tiered-bib.typ, manuscript/lib/abbreviations.typ, manuscript/styles/nature.csl, manuscript/fonts/, manuscript/colours.yaml, manuscript/build.ps1, manuscript/main.typ, manuscript/refs.bib, manuscript/sections/introduction.typ, manuscript/sections/related_work.typ, manuscript/sections/methods.typ, manuscript/sections/results.typ, manuscript/sections/discussion.typ, manuscript/sections/availability.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-13 exact copy list; D-02 venue-neutral; D-03 decoupling; D-05 section stubs; D-11 seed cites)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/main.typ (the wiring to adapt: #import lib/template, #show: project.with(...), #show: tiered-bib(...), #include sections)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/lib/template.typ (project() signature — the args main.typ must pass)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/lib/tiered-bib.typ (tiered-bib signature: bib/style/read args)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/build.ps1 (the compile command + font-path + version hook to adapt)
    - CITATION.bib (existing self-citation to fold into refs.bib)
  </read_first>
  <action>
    Create the new top-level `manuscript/` directory (D-03). Treat
    `C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper` as a READ-ONLY source — copy
    files OUT of it, never write INTO it.

    (a) COPY the template foundation verbatim into manuscript/ (D-13):
      - `lib/template.typ`, `lib/helpers.typ`, `lib/curves.typ`, `lib/tiered-bib.typ`,
        `lib/abbreviations.typ` → `manuscript/lib/`
      - `styles/nature.csl` → `manuscript/styles/`
      - `fonts/` (the whole Libertinus OTF directory) → `manuscript/fonts/`
      - `colours.yaml` → `manuscript/colours.yaml` (shared palette, single source of hex — D-14)
      - `build.ps1` → `manuscript/build.ps1`
      Do NOT copy the BI repo's sections, figures, literature.bib, main.pdf, data, or .planning.

    (b) ADAPT `manuscript/build.ps1` for this repo: default the typst binary to the installed
      `typst` (keep the `$env:TYPST` override hook; the template pins `typst-0.14`, we run the
      installed 0.15.0 — D-17), set `--font-path` to `manuscript/fonts` (or `fonts` relative to
      $PSScriptRoot), add `--root manuscript` (or `$PSScriptRoot`) so root-absolute lookups of
      `/colours.yaml` and `/figures/*.yaml` resolve, compile `manuscript/main.typ` →
      `manuscript/main.pdf`, and `exit $LASTEXITCODE`. NO pipe on the compile line.

    (c) WRITE `manuscript/main.typ` adapted to ProteinCoLoc (do NOT copy the BI main.typ content —
      re-author using its wiring): `#import "lib/template.typ": *`, `#import
      "lib/tiered-bib.typ": *`, optionally abbreviations; `#import "claims.typ": claims,
      claim_table`. Then `#show: project.with(title: [ProteinCoLoc v2.0 — AmortizedColoc: amortized,
      calibrated colocalization inference], authors: ((name: "Manuel Seefelder", email:
      "seefelder.manuel@gmail.com", affiliation: "1"),), aff: ((id: "1", dept: "Department of Gene
      Therapy, Ulm University"),), abstract: [ // TODO(phase 16) ... ], keywords: [...], header:
      "Seefelder 2026")`. Add a one-line NOT-LOCKED candidate-venues comment (Bioinformatics, Nature
      Methods, eLife methods) per D-02. Wire the tiered bibliography: `#show: tiered-bib(enabled:
      true, bib: "refs.bib", style: "styles/nature.csl", read: path => read(path))`. Then `#include`
      the section set in order (introduction; `= Related Work` + include related_work; `= Methods`
      with `#set-tier("M")` + include methods; `= Results` + include results; `= Discussion` +
      include discussion; `= Data and Code Availability` + include availability), render the claim
      spine under a `= Claims` heading as `#figure(claim_table(claims), caption: [...]) <tab:claims>`,
      and `#include "figures/specs.typ"`. If the template's `project()` or `tiered-bib` require args
      this list omits, pass sensible values read from their signatures (read_first). CRITICAL: if any
      copied `lib/` file imports a Typst package the installed toolchain lacks, note it in the SUMMARY
      and either keep it (packages auto-download) or stub the unused helper — the compile MUST stay
      green.

    (d) CREATE the six section stubs under `manuscript/sections/` (D-05): introduction, related_work,
      methods, results, discussion, availability. Each stub: its heading comment + a one-line intent
      comment + a `// TODO(phase N)` marker (methods → 5,7; results → 5,8,9,11-15; related_work →
      this phase (plan 03); intro/discussion/availability → 16). CRITICAL (compile safety): stubs
      MUST NOT contain any `@citekey` or `@label` reference yet.

    (e) SEED `manuscript/refs.bib` with the verified entries from RESEARCH.md §"Citation Entries"
      (ordabayev2022, costes2004, manders1993, talts2018, sainsburydale2024, radev2020,
      radev2023joss) plus the self-cite folded from CITATION.bib; TODO-placeholder the Sci-Reports
      DOI. Uncited entries are fine; do not cite any key from a stub.

    Note: `claims.typ` and `figures/specs.typ` are created as minimal compile-safe stubs in Task 2
    of this plan (claims placeholder machinery) and Plan 04 (specs) — main.typ imports/includes them,
    so they must at least exist by the time build runs (Task 2 handles both stubs).
  </action>
  <acceptance_criteria>
    - manuscript/lib/ contains template.typ, helpers.typ, curves.typ, tiered-bib.typ, abbreviations.typ (copied from BI template)
    - manuscript/colours.yaml, manuscript/styles/nature.csl, and manuscript/fonts/ (with ≥1 .otf) exist
    - `git -C C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper status --porcelain` shows NO changes (source repo untouched)
    - manuscript/main.typ contains `#import "lib/template.typ"`, a `tiered-bib(` call referencing `refs.bib`, a `<tab:claims>` label, and a candidate-venues comment (D-02)
    - manuscript/sections/ contains 6 stub files, each containing `TODO(phase`
    - No sections/*.typ stub contains an `@` citation or `@`-label (grep finds none)
    - manuscript/refs.bib contains `@article{ordabayev2022`, `@article{costes2004`, `@article{manders1993`
    - `git status` shows no changes under src/, spike/, bayes.jl, colocalization.jl (D-03)
  </acceptance_criteria>
  <verify><automated>ls manuscript/lib/template.typ manuscript/colours.yaml manuscript/styles/nature.csl >/dev/null && echo TEMPLATE_OK; ls manuscript/fonts/*.otf >/dev/null 2>&1 && echo FONTS_OK; grep -rl "TODO(phase" manuscript/sections/ | wc -l  (expect 6); grep -c "@article{ordabayev2022" manuscript/refs.bib (expect 1); (cd "C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper" && git status --porcelain | wc -l)  (expect 0)</automated></verify>
  <done>The template foundation is copied into manuscript/, main.typ is adapted to ProteinCoLoc with tiered-bib + section includes, section stubs and refs.bib exist, the BI source repo and src/spike are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the figstyle per-figure YAML loader, claims/specs stubs, and build.sh; prove the green baseline via both build scripts</name>
  <files>manuscript/lib/figstyle.typ, manuscript/claims.typ, manuscript/figures/specs.typ, manuscript/build.sh</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-14 YAML keys to cover; D-15 figstyle contract; D-17 dual build)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (Pattern 1 data-driven claim_table; Pattern 3 figure stub; compile-gate exit-code-correctness; Pitfalls)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/colours.yaml (the palette structure figstyle resolves roles against — e.g. palette.tool_series.<role>)
    - C:/Users/Manuel/Documents/GitHub/BayesInteractomics_Method_paper/figures/benchmark_hein.typ (how a figure loads yaml("/colours.yaml") and references rgb(palette.group.role) — the idiom figstyle generalizes)
    - manuscript/main.typ (the entry importing claims and including figures/specs — from Task 1)
  </read_first>
  <action>
    Write `manuscript/lib/figstyle.typ` implementing the per-figure YAML convention (D-14/D-15).
    Export `#let figstyle(name) = { ... }` that:
      1. Loads the shared palette: `let palette = yaml("/colours.yaml")`.
      2. Loads the per-figure spec: `let spec = yaml("/figures/" + name + ".yaml")`.
      3. Merges `spec` over a documented DEFAULTS dict covering ALL graphical groups (D-14):
         `panel` (width, height, gutter, scale), `axes` (xlim, ylim, xticks, yticks, xlabel,
         ylabel, title), `series` (a list/dict of {role or hex, thickness, dash, mark, bar_width,
         offset}), `legend` (position, cols), and `text` (size). Later phases override only what they
         need; unset keys fall back to defaults.
      4. Resolves color: provide `let color = role => if role.starts-with("#") { rgb(role) } else {
         rgb(palette.at(..role path..)) }` so a series entry may name a colours.yaml role (e.g.
         "tool_series.hero") OR an explicit "#rrggbb" override (D-14). Return a dict exposing the
         merged geometry/axes/legend plus a `color(role_or_hex)` helper and the raw `spec`.
    Add a top-of-file comment block documenting the YAML schema (every key + type + default) so later
    phases author `<name>.yaml` files without reading the code. `figstyle.typ` must be `#let`-only
    (no bare content) so `#import` emits nothing.

    Write `manuscript/claims.typ` per RESEARCH Pattern 1: `#let claims = (...)` array-of-dicts
    (schema id, claim, axis, phase, fig, status) seeded with a SINGLE placeholder row (id "C0",
    status "reserved slot") + a `#let claim_table(claims) = table(columns: 6, table.header(...),
    ..claims.map(...).flatten())` render function. `#let`-only (Pitfall 4). Full claim set is Plan 02.

    Write `manuscript/figures/specs.typ` as a minimal valid stub: a `= Figure Specifications`
    heading + a `// TODO(phase 10 plan 04): figure spec entries + per-figure YAML targets` comment,
    so main.typ's `#include "figures/specs.typ"` resolves. Full specs + the worked F1 figure are Plan 04.

    Write `manuscript/build.sh` as the exit-code-correct FALLBACK gate (D-17) mirroring build.ps1:
    shebang, `set -euo pipefail`, run `typst compile --root manuscript --font-path manuscript/fonts
    "manuscript/main.typ" "manuscript/main.pdf"` on its own line WITH NO PIPE (Pitfall 1), then echo
    success. Forward-slash repo-relative paths from repo root (Pitfall 3). Content assertions are
    added in Plan 05 (they would fail against Wave-1 stubs).

    Run BOTH gates and confirm the green baseline: `pwsh -File manuscript/build.ps1` (or
    `powershell -File ...`) exits 0 AND `bash manuscript/build.sh` exits 0, each producing
    `manuscript/main.pdf`.
  </action>
  <acceptance_criteria>
    - manuscript/lib/figstyle.typ exports `figstyle`, loads `yaml("/colours.yaml")` and a per-figure yaml, and has a documented schema comment covering panel/axes/series/legend/color; `#let`-only
    - manuscript/claims.typ contains `claim_table` and a `#let claims` array (single placeholder row); no bare top-level content
    - manuscript/figures/specs.typ exists and resolves under `#include` (compile succeeds)
    - manuscript/build.sh contains the typst compile line with `--root` and no `|` pipe
    - `bash manuscript/build.sh` exits 0 and produces manuscript/main.pdf
    - `pwsh -File manuscript/build.ps1` (fallback: `powershell -File`) exits 0 and produces manuscript/main.pdf
    - Injecting a dangling `@nonexistent` into any stub makes the build exit non-zero (revert after) — the gate is falsifiable
  </acceptance_criteria>
  <verify><automated>grep -q "figstyle" manuscript/lib/figstyle.typ && grep -q "colours.yaml" manuscript/lib/figstyle.typ && echo FIGSTYLE_OK; bash manuscript/build.sh; echo "sh_exit=$?"; pwsh -File manuscript/build.ps1 2>/dev/null || powershell -File manuscript/build.ps1; echo "ps_exit=$?"  (expect both exit 0 and manuscript/main.pdf present)</automated></verify>
  <done>figstyle.typ implements the documented per-figure YAML loader; claims/specs stubs exist; both build.ps1 and build.sh compile main.typ to main.pdf at exit 0 — a provable green, template-based, YAML-ready baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. This plan copies static Typst/BibTeX/YAML/font assets and writes local shell +
PowerShell scripts; no code executes at runtime, no network I/O (beyond Typst's own package fetch),
no untrusted input, no auth or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | build gate exit-code handling | mitigate | Both build.ps1 and build.sh run `typst compile` with no pipe so a broken build cannot silently pass (reliability, not security) |
| T-10-13 | Tampering | writing into the read-only BI template repo | mitigate | Copy OUT only; verify `git status` in the BI repo shows zero changes (acceptance criterion) |
| T-10-SC | Tampering | package installs | accept | Typst is a preinstalled system binary; template packages auto-download from the Typst registry — nothing to audit |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- Both `bash manuscript/build.sh` and `pwsh -File manuscript/build.ps1` exit 0 and produce manuscript/main.pdf.
- The BI template repo git status is clean (source untouched); src/, spike/, bayes.jl, colocalization.jl untouched (D-03).
- lib/figstyle.typ implements the documented per-figure YAML loader (D-14/D-15).
</verification>

<success_criteria>
The manuscript is founded on the BI Typst template (D-13) in a new top-level tree (D-03), compiles
green via both build scripts (D-17, SC1), carries the tiered bibliography + modular section stubs
(D-04, D-05) + seeded refs.bib (D-11), and ships the per-figure YAML loader (D-14/D-15) ready for
the worked figure in Plan 04. main.typ is fully wired so later plans edit only their own files.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-01-SUMMARY.md` when done
</output>

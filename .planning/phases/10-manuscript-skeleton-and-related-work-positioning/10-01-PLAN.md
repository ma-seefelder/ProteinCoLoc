---
phase: 10-manuscript-skeleton-and-related-work-positioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manuscript/main.typ
  - manuscript/refs.bib
  - manuscript/build.sh
  - manuscript/claims.typ
  - manuscript/figures/specs.typ
  - manuscript/sections/abstract.typ
  - manuscript/sections/introduction.typ
  - manuscript/sections/related_work.typ
  - manuscript/sections/methods.typ
  - manuscript/sections/results.typ
  - manuscript/sections/discussion.typ
  - manuscript/sections/availability.typ
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-11, D-12, SC1]
must_haves:
  truths:
    - "Running `typst compile manuscript/main.typ manuscript/main.pdf` exits 0 and produces a PDF (SC1, D-01, D-12)"
    - "manuscript/ is a new top-level directory; src/, bayes.jl, colocalization.jl, spike/ model code, and existing manuscript pipelines are untouched (D-03)"
    - "The skeleton is a generic venue-neutral single-column article; candidate venues appear only as a comment (D-02)"
    - "All nine methods-paper sections exist as modular #include stubs, each carrying a one-line intent comment and a // TODO(phase N) marker (D-04, D-05)"
    - "refs.bib holds the load-bearing seed citations with verified DOIs; no stub emits a citation to a key that does not exist (D-11)"
    - "build.sh runs typst compile without a pipe and reports typst's own exit code (D-12)"
  artifacts:
    - path: "manuscript/main.typ"
      provides: "Venue-neutral entry: set-rules + #import claims + #include sections + #include figures + #bibliography"
      contains: "#bibliography"
    - path: "manuscript/refs.bib"
      provides: "BibTeX citation store seeded with load-bearing references"
      contains: "@article{ordabayev2022"
    - path: "manuscript/build.sh"
      provides: "Exit-code-correct compile gate (compile-only in Wave 1)"
      contains: "typst compile"
    - path: "manuscript/claims.typ"
      provides: "claim_table() render function + placeholder row so the spine renders (content filled in Plan 02)"
      contains: "claim_table"
    - path: "manuscript/figures/specs.typ"
      provides: "Figure-spec stub so main.typ include compiles (content filled in Plan 04)"
      min_lines: 1
    - path: "manuscript/sections/related_work.typ"
      provides: "Related-work section stub (matrix + Tapqir prose filled in Plan 03)"
      contains: "TODO"
  key_links:
    - from: "manuscript/main.typ"
      to: "manuscript/claims.typ"
      via: "#import"
      pattern: "#import \"claims.typ\""
    - from: "manuscript/main.typ"
      to: "manuscript/refs.bib"
      via: "#bibliography"
      pattern: "#bibliography\\(\"refs.bib\""
    - from: "manuscript/build.sh"
      to: "manuscript/main.typ"
      via: "typst compile invocation"
      pattern: "typst compile"
---

<objective>
Stand up the compiling manuscript skeleton: a new top-level `manuscript/` tree with a
venue-neutral Typst 0.15.0 entry file, all nine methods-paper section stubs, a seeded
`refs.bib`, a `claims.typ` carrying the `claim_table()` render function (with a placeholder row
so the spine renders), a figure-spec stub, and an exit-code-correct compile gate. This is the
provable green baseline every Wave-2 content plan hangs off.

Purpose: Establish the modular structure (one file per concern) so later phases append prose
without merge collisions, and lock `main.typ` so Wave-2 plans touch only their own content files.
Output: `manuscript/{main.typ, claims.typ, refs.bib, build.sh, figures/specs.typ, sections/*.typ}`
that compiles to `manuscript/main.pdf` at exit 0.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md
@.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-VALIDATION.md
@CITATION.bib
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the venue-neutral Typst skeleton (main.typ, section stubs, refs.bib)</name>
  <files>manuscript/main.typ, manuscript/refs.bib, manuscript/sections/abstract.typ, manuscript/sections/introduction.typ, manuscript/sections/related_work.typ, manuscript/sections/methods.typ, manuscript/sections/results.typ, manuscript/sections/discussion.typ, manuscript/sections/availability.typ</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-CONTEXT.md (D-01..D-05, D-11 locked decisions)
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (verified minimal main.typ scaffold; §"Citation Entries to Seed refs.bib"; Pitfall 2 on undefined @cite/label; Pattern 2 #include vs #import)
    - CITATION.bib (existing self-citation to fold into refs.bib)
  </read_first>
  <action>
    Create the new top-level `manuscript/` directory (D-03) — do NOT edit src/, bayes.jl,
    colocalization.jl, spike/ model code, or the existing manuscript pipelines.
    Write `manuscript/main.typ` as a generic single-column article per the verified scaffold in
    RESEARCH.md: `#set document` (title "ProteinCoLoc v2.0 — AmortizedColoc", author
    "M. Seefelder"), `#set page(paper: "a4", numbering: "1")`, `#set text(size: 10pt)`,
    `#set heading(numbering: "1.1")`. Add a one-line comment listing candidate venues
    (Bioinformatics, Nature Methods, eLife methods) marked NOT-locked per D-02 — no journal
    template. In main.typ, `#import "claims.typ": claims, claim_table`, then `#include` the eight
    prose sections in order (abstract, introduction, related_work, methods, results, discussion,
    availability), render the claim spine as a `#figure(claim_table(claims), caption: [...]) <tab:claims>`
    under a `= Claims` heading, `#include "figures/specs.typ"`, and finish with
    `#bibliography("refs.bib", style: "ieee")`. Create the nine section stub files under
    `manuscript/sections/` (D-05): Abstract, Introduction, Related Work, Methods (with subsection
    stubs for forward simulator / summary statistic / NPE-NRE / SBC / OOD), Results (placeholder
    subsections mirroring the claim table rows), Discussion, Data/Code Availability. Each stub
    emits exactly its `=`/`==` heading, a one-line intent comment, and a `// TODO(phase N)` marker
    pointing at the owning phase (methods → 5,7; results → 5,8,9,11-15; abstract/intro/discussion/
    availability → 16; related_work → this phase). CRITICAL (Pitfall 2): stubs MUST NOT contain any
    `@citekey` or `@label` reference yet — undefined refs fail compile exit 1. Seed
    `manuscript/refs.bib` with the BibTeX entries verified in RESEARCH.md §"Citation Entries"
    (ordabayev2022, costes2004, manders1993, talts2018, sainsburydale2024, radev2020, radev2023joss)
    plus the existing self-cite folded from CITATION.bib; keep TODO placeholders for the
    Scientific Reports DOI. Uncited bib entries are fine; do not cite any key from a stub.
  </action>
  <acceptance_criteria>
    - `manuscript/main.typ` exists and contains `#import "claims.typ"`, `#bibliography("refs.bib"`, and eight `#include "sections/` lines
    - `manuscript/sections/` contains 8 stub files; each contains the string `TODO(phase`
    - `manuscript/main.typ` contains a `<tab:claims>` label and a candidate-venues comment (D-02)
    - `manuscript/refs.bib` contains `@article{ordabayev2022`, `@article{costes2004`, and `@article{manders1993`
    - No `sections/*.typ` stub contains an `@` citation or `@`-label reference (grep finds none)
    - `git status` shows no changes under src/, spike/, bayes.jl, or colocalization.jl (D-03 decoupling)
  </acceptance_criteria>
  <verify><automated>grep -rl "TODO(phase" manuscript/sections/ | wc -l  (expect 8); grep -c "@article{ordabayev2022" manuscript/refs.bib (expect 1)</automated></verify>
  <done>The section skeleton, entry file, and seeded bibliography exist; stubs carry TODO markers and no dangling citations; decoupling holds.</done>
</task>

<task type="auto">
  <name>Task 2: Create claims.typ machinery, figures stub, and the compile-gate build.sh; prove green baseline</name>
  <files>manuscript/claims.typ, manuscript/figures/specs.typ, manuscript/build.sh</files>
  <read_first>
    - .planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-RESEARCH.md (Pattern 1 data-driven claim_table; Pattern 3 figure stub; §"Compile gate (build.sh) — exit-code-correct"; Pitfall 1 pipe masks exit code; Pitfall 3 Windows paths)
    - manuscript/main.typ (the entry that imports claims and includes figures/specs — created in Task 1)
  </read_first>
  <action>
    Write `manuscript/claims.typ` per RESEARCH.md Pattern 1: a `#let claims = (...)` array-of-dicts
    with the column schema (id, claim, axis, phase, fig, status) and a `#let claim_table(claims) =
    table(columns: 6, table.header(...), ..claims.map(...).flatten())` render function. Seed it with
    a SINGLE placeholder row only (e.g. id "C0", status "reserved slot") so the table renders and
    main.typ compiles — the full honest claim set is Plan 02's job; do not duplicate that content
    here. `claims.typ` must contain only `#let` definitions (no bare content) so `#import` emits
    nothing (Pitfall 4). Write `manuscript/figures/specs.typ` as a minimal valid stub that emits a
    `= Figure Specifications` heading and a one-line `// TODO(phase 10 plan 04): figure spec entries`
    comment (full specs are Plan 04) — this exists so main.typ's `#include "figures/specs.typ"`
    resolves. Write `manuscript/build.sh` as the exit-code-correct gate from RESEARCH.md: shebang,
    `set -euo pipefail`, run `typst compile "manuscript/main.typ" "manuscript/main.pdf"` on its own
    line WITH NO PIPE (Pitfall 1 — a pipe reports the tail's exit status and silently passes broken
    builds), then echo success. Use forward-slash repo-relative paths run from repo root (Pitfall 3).
    Content assertions (grep for axis terms / SPEC count) are deliberately NOT added here — they
    would fail against Wave-1 stubs; they are added in Plan 05 once content exists. Run the gate and
    confirm the green baseline.
  </action>
  <acceptance_criteria>
    - `manuscript/claims.typ` contains `claim_table` and a `#let claims` array; contains no bare top-level content (only `#let`)
    - `manuscript/figures/specs.typ` exists and resolves under `#include` (compile succeeds)
    - `manuscript/build.sh` contains `typst compile "manuscript/main.typ" "manuscript/main.pdf"` on a line with no `|` pipe
    - `bash manuscript/build.sh` exits 0 and produces/updates `manuscript/main.pdf`
    - Injecting a deliberate dangling `@nonexistent` into any stub makes `bash manuscript/build.sh` exit 1 (revert after checking) — proves the gate is falsifiable
  </acceptance_criteria>
  <verify><automated>bash manuscript/build.sh; echo "exit=$?"  (expect exit=0 and manuscript/main.pdf present)</automated></verify>
  <done>The claim-table machinery, figure stub, and compile-only gate exist; `bash manuscript/build.sh` produces main.pdf at exit 0 — a provable green baseline that later plans regress against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary exists. This plan produces static Typst/BibTeX source and a local shell script;
no code executes at runtime, no network I/O, no untrusted input, no auth or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | build.sh exit-code handling | mitigate | Run `typst compile` with no pipe so a broken build cannot silently pass (reliability, not security) |
| T-10-SC | Tampering | package installs | accept | No packages installed — Typst is a preinstalled system binary; nothing to audit (RESEARCH §Package Legitimacy Audit) |

No meaningful threat surface — documentation-only phase; no code executes, no data flows.
</threat_model>

<verification>
- `bash manuscript/build.sh` exits 0 and produces `manuscript/main.pdf`.
- All eight section stubs and the two data/gate files exist under `manuscript/`.
- `git status` confirms src/, spike/, bayes.jl, colocalization.jl untouched (D-03).
</verification>

<success_criteria>
The manuscript skeleton compiles (SC1); it is venue-neutral (D-02) in a new top-level tree (D-03)
with modular section stubs (D-04, D-05), a seeded bibliography (D-11), claim-table machinery (D-06
scaffold), and an exit-code-correct compile gate (D-12). main.typ is fully wired so Wave-2 plans
edit only their own content files.
</success_criteria>

<output>
Create `.planning/phases/10-manuscript-skeleton-and-related-work-positioning/10-01-SUMMARY.md` when done
</output>

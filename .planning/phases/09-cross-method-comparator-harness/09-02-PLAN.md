---
phase: 09-cross-method-comparator-harness
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - spike/comparator/classical.jl
autonomous: true
requirements: [CMP-01, CMP-02]
must_haves:
  truths:
    - "Manders M1/M2, whole-image Pearson, whole-image Spearman, and patch-grid correlation all return a NaN-safe scalar for any MultiChannelImage"
    - "manders(mci) reproduces the exact M1/M2 that encode_aug computes for the same mci, with encode.jl left byte-identical"
    - "Costes-p uses block scramble (not pixel scramble), is seeded off a disjoint Philox salt, and is bit-reproducible"
  artifacts:
    - path: "spike/comparator/classical.jl"
      provides: "NaN-safe classical estimator callables + seeded Costes randomization p-value"
      contains: "function costes_p"
  key_links:
    - from: "spike/comparator/classical.jl"
      to: "spike/contract.jl (patch, correlation, MultiChannelImage)"
      via: "read-only symbols already in scope via contract.jl include"
      pattern: "correlation\\("
    - from: "spike/comparator/classical.jl"
      to: "spike/comparator/config.jl"
      via: "COSTES_N_SCRAMBLE / COSTES_BLOCK_PX / COSTES_SALT"
      pattern: "COSTES_"
---

<objective>
Implement the classical colocalization estimator battery as NaN-safe callables in a
new spike-local module, reproducing (never editing) the hash-guarded `encode_aug`
Manders/Pearson formulas, reusing the frozen `src/` correlation math read-only via
`contract.jl`, and implementing the one genuinely new algorithm — the Costes
block-scramble randomization p-value (D-05).

Purpose: These callables are the columns of the comparison table. The Manders
formula MUST stay bit-consistent with the augmented encoder, and the Costes null
MUST be a block scramble (pixel scramble biases p→0).
Output: `spike/comparator/classical.jl` exporting `manders`, `pearson_whole`,
`spearman_whole`, `patch_correlation`, and `costes_p`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md
@.planning/phases/09-cross-method-comparator-harness/09-PATTERNS.md
@spike/data/encode.jl
@spike/contract.jl

<interfaces>
<!-- MultiChannelImage fields the estimators read (spike/contract.jl:60-68) -->
mci.data            # Vector{Matrix{Float64}} — data[1], data[2] are the two channels
mci.otsu_threshold  # Vector — otsu_threshold[1], [2]; SAME source encode_aug uses (load-bearing)

<!-- Frozen src/ math reached ONLY through contract.jl (already included by the harness) -->
patch(x, 8)                              # src/colocalization.jl:37 (frozen) — 8x8 grid
correlation(xp, yp; method=:spearman)    # src/colocalization.jl:221 (frozen) — Dict at call time; ≤15-px patches → missing

<!-- _safe NaN-not-throw idiom to reproduce (spike/data/encode.jl:80) -->
_safe(f, v::AbstractVector) = isempty(v) ? NaN : f(v)

<!-- Exact encode_aug Manders + Pearson formula to reproduce byte-for-byte (encode.jl:99-109) -->
t1 = mci.otsu_threshold[1]; t2 = mci.otsu_threshold[2]
s1 = sum(ch1); s2 = sum(ch2)
M1 = s1 > 0 ? sum(ch1 .* (ch2 .> t2)) / s1 : NaN
M2 = s2 > 0 ? sum(ch2 .* (ch1 .> t1)) / s2 : NaN
pearson_whole = cor(vec(ch1), vec(ch2))

<!-- Seeding salt idiom (spike/data/seeding.jl:75-87) -->
Philox4x(UInt64, (UInt64(master_seed) ⊻ SALT, UInt64(idx)))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the NaN-safe classical estimators (Manders, Pearson, Spearman, patch)</name>
  <files>spike/comparator/classical.jl</files>
  <read_first>
    - spike/data/encode.jl (lines 80, 96-137: the `_safe` idiom and the EXACT Manders M1/M2 + pearson_whole formula to reproduce — do NOT edit this hash-guarded file)
    - spike/contract.jl (lines 45-90: the sole read-only include of src/, and patch_summary which already calls patch(...,8) + correlation(...; method=...))
    - src/colocalization.jl (line ~221 correlation, ~37 patch: frozen; note ≤15-px patches become missing)
    - spike/comparator/config.jl (created in 09-01)
  </read_first>
  <behavior>
    - manders(mci) returns a tuple (M1, M2) computed with mci.otsu_threshold, matching encode_aug exactly
    - manders returns NaN for a channel whose total signal is 0 (degenerate guard), never throws
    - pearson_whole(mci) == cor(vec(data[1]), vec(data[2]))
    - spearman_whole(mci) == corspearman(vec(data[1]), vec(data[2]))
    - patch_correlation(mci; method=:spearman) returns mean over non-missing 8x8 patches, or NaN when all patches are missing
  </behavior>
  <action>Create `spike/comparator/classical.jl` with the verbatim AGPL header and a module-doc `# spike/comparator/classical.jl --- CMP-01/CMP-02: classical estimator battery (D-04/D-06)` plus the DECOUPLING note stating encode.jl is NOT edited (hash-guarded) and src/ is reached only via contract.jl. Reproduce `_safe(f, v)` locally (mirror encode.jl:80). Implement `manders(mci) -> (M1, M2)` using the byte-identical encode_aug formula with `mci.otsu_threshold[1]/[2]` as the SAME threshold source (Specific Idea: must never silently diverge). Implement `pearson_whole(mci) = cor(vec(mci.data[1]), vec(mci.data[2]))` and `spearman_whole(mci) = corspearman(vec(mci.data[1]), vec(mci.data[2]))`. Implement `patch_correlation(mci; method::Symbol=:spearman)` that calls the FROZEN `patch.(...,8)` + `correlation(...; method)` reached through contract.jl (do NOT add a second `include` of src/colocalization.jl — that double-defines frozen functions) and reduces via `_safe(mean, collect(skipmissing(ρ)))`. Every estimator returns a NaN-safe scalar and never throws on all-zero/all-missing input.</action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/contract.jl"); include("spike/comparator/config.jl"); include("spike/comparator/classical.jl"); m=build_mci([abs.(randn(64,64)), abs.(randn(64,64))]); M1,M2=manders(m); @assert isfinite(M1)&&isfinite(M2); @assert isfinite(pearson_whole(m)); @assert isfinite(spearman_whole(m)); z=build_mci([zeros(64,64), zeros(64,64)]); zM1,_=manders(z); @assert isnan(zM1); println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/comparator/classical.jl` defines `manders`, `pearson_whole`, `spearman_whole`, `patch_correlation`
    - `spike/data/encode.jl` is byte-identical (git diff empty) — no refactor of the hash-guarded file
    - `classical.jl` contains no `include(` of `src/` (reaches frozen math via contract.jl only)
    - The verify command prints `OK` (finite on non-degenerate input, NaN on all-zero input)
  </acceptance_criteria>
  <done>The four reuse-based estimators are NaN-safe callables consistent with encode_aug, with encode.jl and src/ untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the seeded Costes block-scramble p-value (D-05, CMP-02)</name>
  <files>spike/comparator/classical.jl</files>
  <read_first>
    - spike/data/seeding.jl (lines 45-46, 75-87: the disjoint XOR-salted Philox stream idiom to copy for a NEW COSTES_SALT stream)
    - spike/comparator/config.jl (COSTES_N_SCRAMBLE=200, COSTES_BLOCK_PX=7, COSTES_SALT)
    - .planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md (Pattern 2: block scramble, Davison–Hinkley +1/+1; Pitfall 2: NOT pixel scramble)
  </read_first>
  <behavior>
    - costes_p(master_seed, idx, mci) returns a Float64 in [1/(n+1), 1] where n=COSTES_N_SCRAMBLE
    - Two calls with the same (master_seed, idx, mci) return the identical p (bit-reproducible)
    - The null scrambles ch2 in square blocks of side COSTES_BLOCK_PX (grid-block position permutation), NOT individual pixels
    - Returns NaN when the observed correlation is non-finite (degenerate input)
    - p is computed as (#{r_scramble ≥ r_obs} + 1)/(n + 1)
  </behavior>
  <action>Append to `spike/comparator/classical.jl`: `const COSTES_SALT` is already in config.jl (do not redeclare); add `costes_rng(master_seed, idx) = Philox4x(UInt64, (UInt64(master_seed) ⊻ COSTES_SALT, UInt64(idx)))` (disjoint stream, mirror seeding.jl). Add a private `_block_permute(rng, ch2, block)` that partitions `ch2` into square blocks of side `block` and randomly permutes the block POSITIONS on the grid (preserving within-block spatial autocorrelation — the canonical Costes null; edge blocks handled by clamping to image bounds). Add `costes_p(master_seed, idx, mci; n=COSTES_N_SCRAMBLE, block=COSTES_BLOCK_PX) -> Float64`: compute `r_obs = cor(vec(ch1), vec(ch2))`; return NaN if not finite; draw `rng = costes_rng(master_seed, idx)`; loop `n` times computing `cor(vec(ch1), vec(_block_permute(rng, ch2, block)))`, count `r_scr ≥ r_obs`; return `(ge + 1)/(n + 1)` (Davison–Hinkley correction). Cite Costes et al. 2004 (Biophys J 86:3993) in a comment. Statistic is whole-image Pearson to match `pearson_whole`.</action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/contract.jl"); include("spike/comparator/config.jl"); include("spike/comparator/classical.jl"); m=build_mci([abs.(randn(64,64)), abs.(randn(64,64))]); p1=costes_p(0x00C0FFEE,1,m); p2=costes_p(0x00C0FFEE,1,m); @assert p1==p2; @assert 1/(COSTES_N_SCRAMBLE+1) <= p1 <= 1.0; @assert isfinite(p1); println("OK p=",p1)'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/comparator/classical.jl` contains `function costes_p(` and a `_block_permute` helper that scrambles blocks (not pixels)
    - `costes_p` seeds off `COSTES_SALT` via a `Philox4x` stream distinct from `sample_rng`/`holdout_rng`
    - The verify command prints `OK p=...` with `p1 == p2` (bit-reproducible) and `p ∈ [1/(n+1), 1]`
    - A comment cites Costes et al. 2004 and states block-scramble (not pixel-scramble)
  </acceptance_criteria>
  <done>The Costes randomization p-value is a seeded, bit-reproducible, block-scramble callable that never returns 0 (Davison–Hinkley) and guards degenerate input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator/external image → estimator | Estimators must accept arbitrary (possibly degenerate) MultiChannelImage input without throwing |
| new module → hash-guarded encode.jl | Any byte edit to encode.jl invalidates the Phase-3/4 cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Phase-3/4 content-hash cache | mitigate | classical.jl reproduces the formula in a NEW file; acceptance criterion asserts encode.jl byte-identical (git diff empty) |
| T-09-05 | Denial of Service | estimator on degenerate input | mitigate | `_safe` NaN-not-throw guarding on every estimator; verify covers all-zero input returning NaN |
| T-09-06 | Information Disclosure | biased Costes p (pixel scramble) | mitigate | Block-scramble null preserves autocorrelation; behavior/acceptance assert block (not pixel) scramble |
</threat_model>

<verification>
- Inline julia smokes above pass; `spike/data/encode.jl` and `src/` remain byte-identical.
- Formal finiteness + Manders-equality + Costes-reproducibility gates are wired in plan 09-06.
</verification>

<success_criteria>
Five estimator callables (`manders`, `pearson_whole`, `spearman_whole`, `patch_correlation`, `costes_p`) exist in `classical.jl`, are NaN-safe, consistent with `encode_aug`, and the Costes-p is a seeded block-scramble that is bit-reproducible — all with zero edits to `encode.jl`/`src/`.
</success_criteria>

<output>
Create `.planning/phases/09-cross-method-comparator-harness/09-02-SUMMARY.md` when done
</output>

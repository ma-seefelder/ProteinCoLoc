---
phase: 09-cross-method-comparator-harness
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - spike/comparator/tapqir_env/Project.toml
  - spike/comparator/tapqir_env/Manifest.toml
  - spike/comparator/tapqir_bridge.jl
  - spike/comparator/config.jl
autonomous: false
requirements: [CMP-06]
user_setup:
  - service: conda-tapqir
    why: "Optional Tapqir sanity anchor (CoSMoS single-molecule model); the classical battery runs green without it"
    env_vars:
      - name: JULIA_CONDAPKG_BACKEND
        source: "Set to a Conda backend only if reproducing the Tapqir anchor; unset → graceful skip"
    dashboard_config:
      - task: "Materialize the isolated Conda env and run the published Tapqir tutorial once to capture the anchor value"
        location: "spike/comparator/tapqir_env (isolated sub-env; tapqir 1.1.19, Apache-2.0)"
must_haves:
  truths:
    - "The Tapqir bridge is fully isolated in its own sub-env (PythonCall/CondaPkg never enter the main spike env)"
    - "The bridge probes availability first and returns status=:skipped with a clear reason on any failure — the classical battery still runs green with zero Python present"
    - "When the env is available, the bridge reproduces the published Tapqir tutorial anchor within a pre-declared tolerance"
  artifacts:
    - path: "spike/comparator/tapqir_bridge.jl"
      provides: "Optional out-of-process Tapqir anchor with graceful skip-with-flag"
      contains: "function tapqir_anchor"
    - path: "spike/comparator/tapqir_env/Project.toml"
      provides: "Isolated PythonCall/CondaPkg sub-env (mirror spike/baseline/)"
      contains: "PythonCall"
  key_links:
    - from: "spike/comparator/tapqir_bridge.jl"
      to: "spike/comparator/config.jl (TAPQIR_PUBLISHED_VALUE, TAPQIR_TOL)"
      via: "pre-declared anchor value + tolerance"
      pattern: "TAPQIR_"
---

<objective>
Stand up the optional Tapqir sanity anchor as a strictly isolated, gracefully-
degrading bridge (D-07/D-08). Tapqir models CoSMoS single-molecule spot data — a
different regime from our diffuse 2-channel fields — so it is NOT a comparator
column; it only proves "we can invoke Tapqir correctly" by reproducing a published
tutorial value. The Python stack lives entirely in its own sub-env; the classical
battery must run green with zero Python present.

Purpose: The anchor strengthens the positioning ("we validated against the modern
SBI-adjacent baseline") without ever gating the core table.
Output: An isolated `tapqir_env/`, a `tapqir_bridge.jl` with skip-with-flag, and a
pinned `TAPQIR_PUBLISHED_VALUE`/`TAPQIR_TOL` (or a documented clean-skip).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md
@.planning/phases/09-cross-method-comparator-harness/09-PATTERNS.md
@spike/baseline/Project.toml

<interfaces>
<!-- Isolated sub-env precedent: spike/baseline/ isolates Turing/AdvancedVI in its own Project.toml/Manifest -->
<!-- Graceful skip-with-flag contract the harness consumes: -->
tapqir_anchor(; tol=TAPQIR_TOL) -> (status=:passed|:failed|:skipped, value=Float64|missing, reason=String)

<!-- Tapqir package (audited legitimate): PyPI tapqir 1.1.19, Apache-2.0, Ordabayev et al. eLife 2022, gelles-brandeis/tapqir -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolated Tapqir sub-env + graceful skip-with-flag bridge</name>
  <files>spike/comparator/tapqir_env/Project.toml, spike/comparator/tapqir_env/Manifest.toml, spike/comparator/tapqir_bridge.jl</files>
  <read_first>
    - spike/baseline/Project.toml (the isolated-sub-env precedent to replicate exactly for the Python stack)
    - spike/test/runtests.jl (lines ~52-58: the CUDA graceful-degradation gate — the "optional heavy stack never hard-fails the core path" idiom)
    - spike/comparator/config.jl (TAPQIR_PUBLISHED_VALUE, TAPQIR_TOL placeholders from 09-01)
    - .planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md (Pattern 4 + Pitfall 5 + OQ2: prefer a subprocess run under the isolated env)
  </read_first>
  <action>Create `spike/comparator/tapqir_env/Project.toml` with `[deps]` PythonCall (`6099a3de-0909-46bc-b1f4-468b9a2dfc0d`) + CondaPkg (`992eb4ea-22a4-4c86-a8db-e01b23e3f0c6`), a `[compat]` block pinning them, and resolve it with `julia --project=spike/comparator/tapqir_env -e 'using Pkg; Pkg.instantiate()'` to produce the committed `Manifest.toml`. These deps MUST NEVER be added to the main `spike/Project.toml` (co-resolve landmine — Pitfall 4/5). Create `spike/comparator/tapqir_bridge.jl` with the AGPL header + `# spike/comparator/tapqir_bridge.jl --- CMP-06: optional Tapqir anchor (D-07/D-08)` + DECOUPLING note (isolated sub-env; classical table stays green with zero Python). Implement `_tapqir_env_available()` that probes whether the isolated env + Python + tapqir are reachable (check the sub-env Manifest exists AND a subprocess `julia --project=<tapqir_env>` can import PythonCall/tapqir), returning `false` on any error. Implement `tapqir_anchor(; tol=TAPQIR_TOL)` returning a NamedTuple `(status, value, reason)`: if unavailable, `(status=:skipped, value=missing, reason="Tapqir/Python env unavailable")`; else run the published tutorial via a SUBPROCESS under the isolated env (returning the recovered scalar via stdout/JSON — no in-process PythonCall in the main harness, OQ2), and return `:passed` if `abs(recovered - TAPQIR_PUBLISHED_VALUE) ≤ tol` else `:failed`; wrap the run in try/catch that maps ANY exception to `(status=:skipped, reason=sprint(showerror,e))`. Prefer subprocess isolation; never `eval` untrusted content.</action>
  <verify>
    <automated>julia --project=spike -e 'import Pkg; include("spike/comparator/config.jl"); include("spike/comparator/tapqir_bridge.jl"); @assert isdefined(Main,:tapqir_anchor) "bridge failed to load"; @assert isdefined(Main,:_tapqir_env_available) "probe failed to load"; r=tapqir_anchor(); @assert r.status in (:passed,:failed,:skipped); @assert (r.status==:skipped) ? (r.value===missing) : true; @assert !haskey(Pkg.project().dependencies,"PythonCall"); @assert !haskey(Pkg.project().dependencies,"CondaPkg"); println("OK status=",r.status)'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/comparator/tapqir_env/Project.toml` lists PythonCall + CondaPkg; `Manifest.toml` is committed
    - The main `spike/Project.toml` does NOT contain PythonCall or CondaPkg
    - `tapqir_bridge.jl` defines `tapqir_anchor` returning `status ∈ {:passed,:failed,:skipped}` and `_tapqir_env_available` that returns `false` on any error (never throws)
    - The verify command prints an `OK ...` line (the bridge returns a clean status and PythonCall/CondaPkg are absent from the main env)
    - No in-process PythonCall import in the main harness path (subprocess isolation)
  </acceptance_criteria>
  <done>The Tapqir bridge exists, is fully isolated, and degrades to a clean skip-with-flag when the Python env is absent — the classical battery is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Attempt to capture the published Tapqir anchor and pin it in config.jl</name>
  <files>spike/comparator/config.jl</files>
  <read_first>
    - spike/comparator/config.jl (TAPQIR_PUBLISHED_VALUE=NaN, TAPQIR_TOL placeholders)
    - spike/comparator/tapqir_bridge.jl (the subprocess runner from Task 1)
    - .planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md (OQ1/A3: the eLife-2022 Part II "sample data set" tutorial, cosmos model; exact scalar + value + tolerance UNCONFIRMED — capture by running once)
  </read_first>
  <action>Attempt to materialize the isolated Conda env (`julia --project=spike/comparator/tapqir_env -e 'using CondaPkg; ...'` installing tapqir 1.1.19) and run the published eLife-2022 Part II tutorial (`cosmos` model on the distributed sample dataset) once, capturing the recovered scalar (e.g. the average target-specific spot probability or a documented `cosmos` global parameter). If the run succeeds, pin `TAPQIR_PUBLISHED_VALUE` to the captured value and set a defensible `TAPQIR_TOL` in `config.jl`, documenting the exact dataset identifier + recovered quantity + source (eLife 11:e73860) in the comment — and note this value was pinned BEFORE the anchor test asserts it (the capture is NOT threshold-tuning; it records the published ground truth). If the env cannot be materialized on this machine (no Conda/network/Windows friction), LEAVE `TAPQIR_PUBLISHED_VALUE = NaN` with a comment documenting the clean-skip path, so the D-13 test accepts `status=:skipped`. Do NOT block the phase on Conda availability.</action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/comparator/config.jl"); @assert isdefined(Main,:TAPQIR_PUBLISHED_VALUE) && isdefined(Main,:TAPQIR_TOL); @assert isnan(TAPQIR_PUBLISHED_VALUE) || (isfinite(TAPQIR_PUBLISHED_VALUE) && TAPQIR_TOL > 0); println("OK anchor=",TAPQIR_PUBLISHED_VALUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `config.jl` `TAPQIR_PUBLISHED_VALUE` is EITHER a finite captured value with a documented dataset/source comment AND `TAPQIR_TOL > 0`, OR `NaN` with a documented clean-skip comment
    - The verify command prints `OK anchor=...`
    - The captured value (if any) is recorded before the anchor assertion is wired (09-06), documented as published ground truth not a tuned threshold
  </acceptance_criteria>
  <done>The Tapqir anchor value is either pinned from a real tutorial run or explicitly left as a documented clean-skip — no phase blockage on Conda availability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Tapqir anchor value or the documented clean-skip</name>
  <action>Human reviews whether the Tapqir anchor was captured from a real tutorial run (finite TAPQIR_PUBLISHED_VALUE with a documented eLife-2022 source) or intentionally left as a NaN clean-skip, and confirms the classical battery is unaffected. Claude has already automated env setup, the skip-with-flag bridge, and the capture attempt in Tasks 1-2.</action>
  <what-built>An isolated Tapqir sub-env, a graceful skip-with-flag bridge, and either a pinned published anchor value or a documented clean-skip.</what-built>
  <how-to-verify>
    1. Confirm the classical path is unaffected: `julia --project=spike spike/test/runtests.jl` is green with PythonCall/CondaPkg absent from the main env (resolve-risk gate g).
    2. Inspect `spike/comparator/config.jl`: is `TAPQIR_PUBLISHED_VALUE` a real captured value (with a documented eLife-2022 dataset/source) or an intentional `NaN` clean-skip? Confirm the choice matches whether you have a working Conda/Tapqir env on this machine.
    3. If you intend to report the Tapqir anchor in the manuscript, materialize the isolated env and re-run Task 2 to capture the value; otherwise accept the documented skip.
  </how-to-verify>
  <resume-signal>Type "approved" (accept the pinned value or the documented skip) or describe the anchor value/tolerance to pin.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main spike env → isolated Python sub-env | The only new external/native-code edge in the phase; untrusted/unpinned Python deps + a materialized Conda env |
| Conda materialization → local filesystem | CondaPkg writes a large machine-local env dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Elevation | PythonCall/CondaPkg co-resolving into main env | mitigate | Deps live ONLY in `tapqir_env/`; resolve-risk gate (g, plan 09-01) asserts them absent from the main env |
| T-09-11 | Tampering | arbitrary code exec via Conda env | mitigate | Tapqir is Apache-2.0, peer-reviewed (eLife 2022), pinned in the isolated Manifest; bridge runs it in a subprocess, never `eval`s untrusted content |
| T-09-12 | Denial of Service | missing/broken Python env hard-failing the core path | mitigate | `_tapqir_env_available` probes first; any failure → `status=:skipped`; classical battery runs green with zero Python |
| T-09-SC2 | Tampering | tapqir PyPI supply chain | mitigate | Pinned tapqir 1.1.19 in the isolated Manifest; legitimacy audited in RESEARCH (established, peer-reviewed); isolation limits blast radius |
</threat_model>

<verification>
- Classical `runtests.jl` green with PythonCall/CondaPkg absent from the main env.
- `tapqir_anchor()` returns a clean `:skipped` (or `:passed`) status without throwing.
- CondaPkg materialized dir is git-ignored (from plan 09-01); `tapqir_env/Project.toml`+`Manifest.toml` are tracked.
</verification>

<success_criteria>
The Tapqir anchor is a fully isolated, gracefully-degrading bridge that never gates the classical table, with the published anchor value either captured-and-pinned or documented as a clean skip.
</success_criteria>

<output>
Create `.planning/phases/09-cross-method-comparator-harness/09-04-SUMMARY.md` when done
</output>

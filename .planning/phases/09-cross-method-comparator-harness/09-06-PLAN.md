---
phase: 09-cross-method-comparator-harness
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05"]
files_modified:
  - spike/comparator/run_comparator.jl
  - spike/test/test_comparator.jl
autonomous: true
requirements: [CMP-08, CMP-09]
must_haves:
  truths:
    - "A single seeded entry point builds the shared inputs, runs all estimators + the optional Tapqir anchor, assembles the table, and writes the artifact"
    - "Two seeded run_comparator runs produce a byte-identical JLD2 table and the identical content-hash dir, independent of thread count"
    - "The comparator testset asserts finiteness, Manders-equality with encode_aug, Costes reproducibility, traffic-light-by-oracle, divergence()-semantic-by-oracle, determinism, and Tapqir anchor-or-skip"
  artifacts:
    - path: "spike/comparator/run_comparator.jl"
      provides: "Single seeded entry point (D-12)"
      contains: "function run_comparator"
    - path: "spike/test/test_comparator.jl"
      provides: "Filled D-13 gate replacing the Wave-0 @test_skip scaffolds"
      contains: "@test"
  key_links:
    - from: "spike/comparator/run_comparator.jl"
      to: "inputs.jl + classical.jl + table.jl + audit.jl + tapqir_bridge.jl"
      via: "guarded-include composition (contract.jl first)"
      pattern: "include.*classical"
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_comparator.jl"
      via: "already wired in 09-01"
      pattern: "test_comparator"
---

<objective>
Wire the whole harness together behind one seeded entry point (D-12) and replace the
Wave-0 `@test_skip` scaffolds with the real D-13 gate. `run_comparator` composes the
frozen chain + the new comparator modules, builds shared inputs, runs the estimator
battery + the optional Tapqir anchor, assembles the divergence table, and writes the
content-addressed artifact — bit-reproducibly across runs and thread counts.

Purpose: This is the callable deliverable and its proof of reproducibility. The
filled testset is the phase's acceptance gate (SC1/SC2/SC3).
Output: `spike/comparator/run_comparator.jl` + a filled `spike/test/test_comparator.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md
@.planning/phases/09-cross-method-comparator-harness/09-PATTERNS.md
@spike/data/generate.jl
@spike/test/test_npe.jl

<interfaces>
<!-- Modules to compose (all created in 09-02..09-05); reach frozen src/ via contract.jl only -->
build_shared_inputs(...)                          # inputs.jl (09-03)
manders / pearson_whole / spearman_whole / costes_p  # classical.jl (09-02)
build_table(inputs; master_seed, npe) / write_table(df, config; outdir)  # table.jl (09-05)
comparator_audit(df) -> String                   # audit.jl (09-05)
tapqir_anchor(; tol=TAPQIR_TOL) -> (status, value, reason)  # tapqir_bridge.jl (09-04)

<!-- Guarded-include composition order (spike/data/generate.jl:42-51): contract.jl FIRST -->
<!-- encode_aug for the Manders-equality oracle (M1/M2 are indices 129,130 of the 142-vec) -->
encode_aug(mci, M) -> Vector (AUG_DIM=142); M1@129, M2@130

<!-- Bit-exact determinism rule to copy (spike/test/test_npe.jl:408-427): use == not ≈ on the JLD2 payload -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single seeded entry point run_comparator.jl (D-12, CMP-08)</name>
  <files>spike/comparator/run_comparator.jl</files>
  <read_first>
    - spike/data/generate.jl (lines 42-51: guarded-include composition, contract.jl FIRST; the seeded driver idiom)
    - spike/comparator/inputs.jl, classical.jl, table.jl, audit.jl, tapqir_bridge.jl (the callables to compose)
    - spike/comparator/config.jl (MASTER_SEED, TAPQIR_TOL)
  </read_first>
  <action>Create `spike/comparator/run_comparator.jl` with the AGPL header + `# spike/comparator/run_comparator.jl --- CMP-08: seeded entry point (D-12)` + DECOUPLING note. Add the guarded-include block in dependency order: contract.jl FIRST, then config.jl, classical.jl, inputs.jl, table.jl, audit.jl, tapqir_bridge.jl (mirror generate.jl's `isdefined(@__MODULE__, :sym) || include(...)`). Implement `run_comparator(; master_seed=MASTER_SEED, n_per_regime::Int=…, imsize=(128,128), inputs=nothing, outdir=…, run_tapqir::Bool=true)`: build shared inputs via `build_shared_inputs(...)` (or the D-03 passthrough when `inputs` is supplied), assemble the table via `build_table(...)`, optionally call `tapqir_anchor(; tol=TAPQIR_TOL)` (storing its `(status,value)` in the run meta — never letting a skip fail the run), write the artifact via `write_table(df, config; outdir)` where `config` includes `master_seed` + input count + the Tapqir status, and generate the audit via `comparator_audit(df)`. Return a NamedTuple `(table=df, artifact_dir, audit, tapqir)`. Seed everything off Philox via the existing `sample_rng`/`costes_p` streams so results are thread- and order-independent.</action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/comparator/run_comparator.jl"); t=mktempdir(); r1=run_comparator(n_per_regime=2, imsize=(96,96), outdir=t, run_tapqir=true); r2=run_comparator(n_per_regime=2, imsize=(96,96), outdir=t, run_tapqir=true); @assert r1.artifact_dir==r2.artifact_dir; @assert r1.table.Costes_p==r2.table.Costes_p; @assert r1.tapqir.status in (:passed,:failed,:skipped); println("OK dir=",basename(r1.artifact_dir)," tapqir=",r1.tapqir.status)'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/comparator/run_comparator.jl` defines `run_comparator` composing inputs+estimators+table+audit+optional Tapqir
    - A Tapqir `:skipped` status does NOT fail the run (graceful degradation)
    - Two same-seed runs produce the identical `artifact_dir` and identical `Costes_p` column (bit-reproducible)
    - The verify command prints `OK dir=... tapqir=...`
    - `run_comparator.jl` contains no `include(` of `src/` (frozen chain reached via contract.jl)
  </acceptance_criteria>
  <done>One seeded entry point produces the full comparison artifact + audit, reproducibly, with the Tapqir anchor optional and non-blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the D-13 comparator test gate (CMP-09)</name>
  <files>spike/test/test_comparator.jl</files>
  <read_first>
    - spike/test/test_comparator.jl (the Wave-0 skipped scaffold from 09-01 to fill)
    - spike/test/test_npe.jl (lines ~274-313: the fixture-oracle idiom — test against HAND-BUILT known-answer inputs, not by re-deriving the function's own formula; lines ~408-427: the bit-exact `==` determinism rule)
    - spike/data/encode.jl (M1/M2 at indices 129,130 of the AUG_DIM=142 vector — the Manders-equality oracle)
    - all spike/comparator/*.jl (the callables under test)
  </read_first>
  <action>Replace the `@test_skip` placeholders in `spike/test/test_comparator.jl` with real assertions, each tagged with its CMP id: (CMP-01 finiteness) build a non-degenerate fixture mci; assert `costes_p`, both `manders` outputs, `pearson_whole`, `spearman_whole` all `isfinite`. (CMP-02 Manders-equality) assert `manders(mci)` equals the M1/M2 slice (indices 129,130) of `encode_aug(mci, patch_summary(mci))` for the same mci — the D-04 consistency proof with encode.jl untouched; use `≈` only for floating tolerance if the formula is identical bit-for-bit prefer `==`. (CMP-02 Costes) assert `costes_p(seed,idx,mci)==costes_p(seed,idx,mci)` (bit-reproducible) and `1/(COSTES_N_SCRAMBLE+1) ≤ p ≤ 1`. (CMP-03) assert `build_shared_inputs` is reproducible (bit-exact images across two same-seed builds) and regime labels ⊆ {:coloc,:random,:exclusion}. (CMP-04 divergence + traffic-light ORACLE) TWO parts, both independent oracles (never re-derive the function's own formula): (i) assert `traffic_light` against hand-built divergence values with KNOWN expected colors at/around the band boundaries (green<WARN, amber at WARN, red at FAIL); (ii) assert the SEMANTIC correctness of `divergence(regime, verdict)` — the phase's core positioning payload — against known ground-truth-vs-classical mismatches: `traffic_light(divergence(:random, classical_verdict(0.8, 0.001)))==:red` (a random-regime input with a spuriously high significant Pearson MUST flag red), `traffic_light(divergence(:coloc, classical_verdict(0.9, 0.001)))==:green` (a true coloc MUST stay green), `traffic_light(divergence(:coloc, classical_verdict(0.05, 0.9)))==:red` (a missed real coloc MUST flag red), and `divergence(:unknown, 0.5)===missing`. This catches a no-op or sign-inverted `divergence`. (CMP-05/08 determinism) run `run_comparator` twice into a tempdir with a small Costes N; assert byte-identical JLD2 payload (`==`) and identical content-hash dir. (CMP-06 Tapqir) assert `tapqir_anchor().status ∈ (:passed,:skipped)` (accept skip when env absent). (CMP-07 audit) assert `comparator_audit(df)` returns a non-empty Markdown string containing band-count rows. Keep Costes N small in tests for speed (pass a reduced `n` kwarg). Remove all `@test_skip`.</action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/test/test_comparator.jl` contains NO `@test_skip` (all gates filled)
    - The suite asserts: finiteness, `manders`≡encode_aug M1/M2, Costes bit-reproducibility + range, shared-input reproducibility, traffic-light-by-oracle, `divergence(regime, verdict)` SEMANTIC correctness by fixture-oracle (random+high-Pearson→red, true coloc→green, missed coloc→red, :unknown→missing), run_comparator determinism (`==` on JLD2 + content-hash dir), Tapqir `status ∈ (:passed,:skipped)`, and a non-empty audit report
    - `julia --project=spike spike/test/runtests.jl` exits 0 (full suite green, including the resolve-risk gate g)
    - `git diff` shows `spike/data/encode.jl` and `src/` byte-unchanged
  </acceptance_criteria>
  <done>The D-13 gate is fully wired green in the single spike test entry point, proving SC1/SC2/SC3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional Tapqir anchor → run_comparator | A Python-env failure must never fail the seeded run or the test gate |
| test oracle → implementation | Tests must use independent hand-built oracles, not re-derive the code's own formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Denial of Service | Tapqir skip failing the run/gate | mitigate | run_comparator stores the anchor status without raising; test accepts `status ∈ (:passed,:skipped)` |
| T-09-17 | Tampering | non-reproducible artifact across threads | mitigate | Philox `sample_rng`/`costes_p` streams are thread/order independent; determinism gate asserts `==` on the JLD2 payload + content-hash dir |
| T-09-18 | Repudiation | test that re-derives the formula it checks | mitigate | Traffic-light + Manders gates use independent oracles (hand-built values / encode_aug slice), catching a `<`/`>` flip copied into both |
| T-09-19 | Tampering | Phase-3/4 hashed source | accept | Entry point + tests reach frozen math via contract.jl only; acceptance asserts encode.jl/src byte-unchanged |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` green (all comparator gates filled, resolve-risk gate g intact).
- A real `run_comparator` produces a stable content-hash CSV+JLD2 artifact.
- `git diff` confirms `spike/data/encode.jl`, `src/`, and root project files untouched.
</verification>

<success_criteria>
A single seeded `run_comparator` entry point produces the full, bit-reproducible comparison artifact + audit with the optional Tapqir anchor non-blocking, and the filled D-13 testset proves SC1 (per-method finite table), SC2 (Tapqir anchor-or-skip), and SC3 (BayesInteractomics-pattern, seeded/reproducible) green in the single spike gate.
</success_criteria>

<output>
Create `.planning/phases/09-cross-method-comparator-harness/09-06-SUMMARY.md` when done
</output>

---
phase: 09-cross-method-comparator-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/test/runtests.jl
  - spike/comparator/config.jl
  - spike/test/test_comparator.jl
  - .gitignore
autonomous: true
requirements: [CMP-04, CMP-09]
must_haves:
  truths:
    - "DataFrames and CSV are direct deps of the spike env without downgrading NeuralEstimators v0.2.1"
    - "All comparator thresholds are pre-declared as consts before any table is produced"
    - "The test suite includes a comparator testset that runs green (skipped scaffolds)"
    - "The resolve-risk gate re-asserts the NeuralEstimators pin and asserts PythonCall/CondaPkg absent from the main env"
  artifacts:
    - path: "spike/comparator/config.jl"
      provides: "Pre-declared D-14 constants (Costes, divergence bands, master seed, Tapqir anchor placeholders)"
      contains: "const COSTES_N_SCRAMBLE"
    - path: "spike/test/test_comparator.jl"
      provides: "SC1/SC2/SC3 skipped-scaffold testset wired into runtests.jl"
      contains: "@testset"
    - path: "spike/Project.toml"
      provides: "DataFrames + CSV promoted to direct deps"
      contains: "DataFrames ="
  key_links:
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_comparator.jl"
      via: "include after test_npe.jl"
      pattern: "include.*test_comparator"
    - from: "spike/test/test_comparator.jl"
      to: "spike/comparator/config.jl"
      via: "include of config consts"
      pattern: "include.*config"
---

<objective>
Lay the Wave-0 foundation for the Phase-9 comparator: promote the tabular deps to
direct status without disturbing the pinned NeuralEstimators stack, pre-declare
every "knows when classics are wrong" threshold as a committed constant (D-14
anti-data-snooping), and stand up the comparator testset scaffold wired into the
single spike test gate.

Purpose: Everything downstream (estimators, table, entry point, tests) reads these
constants and lands in this testset. Pre-declaring the thresholds BEFORE any table
exists is the anti-data-snooping guarantee the roadmap flagged as a hard blocker.
Output: A green `runtests.jl` with a skipped comparator scaffold, a committed
`config.jl` of pre-registered consts, and DataFrames/CSV as direct deps.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cross-method-comparator-harness/09-CONTEXT.md
@.planning/phases/09-cross-method-comparator-harness/09-RESEARCH.md
@.planning/phases/09-cross-method-comparator-harness/09-PATTERNS.md

<interfaces>
<!-- Established spike seeding salts the new COSTES_SALT must NOT collide with (spike/data/seeding.jl:45-46) -->
const HOLDOUT_SALT = 0x9E3779B97F4A7C15
const FOLD_SALT    = 0xD1B54A32D192ED03

<!-- Existing resolve-risk gate shape to copy (spike/test/runtests.jl:52-85). NeuralEstimators UUID: -->
@test Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"

<!-- UUIDs for promotion (confirmed in spike/baseline/Project.toml + resolved Manifest) -->
DataFrames = "a93c6f00-e57d-5684-b7b6-d8193f3e46c0"
CSV        = "336ed68f-0bac-5ca0-87d4-7b16caf5d00b"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote DataFrames + CSV to direct deps and re-freeze the Manifest</name>
  <files>spike/Project.toml, spike/Manifest.toml, .gitignore</files>
  <read_first>
    - spike/Project.toml (current direct deps; JLD2/Random123 are the promotion precedent at lines 12,14)
    - spike/Manifest.toml (confirm DataFrames + CSV already resolved transitively; capture their resolved versions)
    - spike/baseline/Project.toml (DataFrames UUID a93c6f00-e57d-5684-b7b6-d8193f3e46c0 confirmed)
    - .gitignore (lines ~403-409 show the spike figures negation idiom to mirror for a CondaPkg ignore)
  </read_first>
  <action>FIRST capture the pre-promotion resolved versions so inertness can be asserted (RESEARCH A2): `cp spike/Manifest.toml spike/.manifest_baseline.toml`. THEN run `julia --project=spike -e 'using Pkg; Pkg.add(["DataFrames","CSV"])'` in the spike env ONLY — never the root Project.toml (hard decoupling constraint). This promotes the two transitively-resolved packages to `[deps]` and re-freezes `spike/Manifest.toml`. Verify DataFrames and CSV appear in `spike/Project.toml [deps]` with the UUIDs above, and that the post-promotion resolved DataFrames/CSV versions EQUAL the versions recorded in `spike/.manifest_baseline.toml` (promotion must be inert — no version drift). Append TWO `.gitignore` entries: (1) the future CondaPkg materialized env dir `spike/comparator/tapqir_env/.CondaPkg/` (the on-disk Conda materialization; large + machine-local) while keeping `tapqir_env/Project.toml` and `tapqir_env/Manifest.toml` tracked; (2) the scratch baseline `spike/.manifest_baseline.toml` (an inertness-check artifact, not a deliverable).</action>
  <verify>
    <automated>julia --project=spike -e 'using Pkg, TOML; uu(x)=Base.UUID(x); cur=Pkg.dependencies(); d=Pkg.project().dependencies; @assert haskey(d,"DataFrames") && haskey(d,"CSV"); pre=TOML.parsefile("spike/.manifest_baseline.toml")["deps"]; dfc=string(cur[uu("a93c6f00-e57d-5684-b7b6-d8193f3e46c0")].version); csvc=string(cur[uu("336ed68f-0bac-5ca0-87d4-7b16caf5d00b")].version); @assert dfc==pre["DataFrames"][1]["version"] "DataFrames version drifted"; @assert csvc==pre["CSV"][1]["version"] "CSV version drifted"; @assert cur[uu("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"; println("OK versions unchanged")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/Project.toml` `[deps]` contains `DataFrames = "a93c6f00-e57d-5684-b7b6-d8193f3e46c0"` and `CSV = "336ed68f-0bac-5ca0-87d4-7b16caf5d00b"`
    - The verify command prints `OK versions unchanged` (NeuralEstimators still resolves to exactly v0.2.1 AND the resolved DataFrames/CSV versions equal the pre-promotion baseline — promotion is inert)
    - Root `Project.toml`/`Manifest.toml` are byte-unchanged (git diff shows no root-package edits)
    - `.gitignore` contains lines ignoring `spike/comparator/tapqir_env/.CondaPkg/` and `spike/.manifest_baseline.toml`
  </acceptance_criteria>
  <done>DataFrames + CSV are direct spike deps, Manifest re-frozen with no NeuralEstimators downgrade, CondaPkg dir git-ignored, root project untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-declare the comparator constants in config.jl (D-14)</name>
  <files>spike/comparator/config.jl</files>
  <read_first>
    - spike/test/test_npe.jl (lines ~70-96: the pre-registered `const` block committed BEFORE any run — the exact D-14 idiom to mirror, including the "fixed BEFORE the run, NOT tuned to pass" comment style)
    - spike/simulator/prior.jl (lines ~40-57: module-level const rationale-comment style)
    - spike/data/encode.jl (lines 1-19: the AGPL header block every .jl file carries verbatim; lines 40-42: the module-doc + DECOUPLING note idiom)
    - spike/data/seeding.jl (lines 45-46: HOLDOUT_SALT/FOLD_SALT — COSTES_SALT must be a distinct nonzero UInt64)
  </read_first>
  <action>Create `spike/comparator/config.jl` opening with the verbatim 19-line AGPL header, then a module-doc comment `# spike/comparator/config.jl --- CMP-04/CMP-14: pre-declared thresholds (D-14)` plus the DECOUPLING note. Declare these consts with rationale comments (values from RESEARCH Pattern 2/3): `COSTES_N_SCRAMBLE = 200` (Costes et al. 2004 standard), `COSTES_BLOCK_PX = 7` (≈ resolution element for σ_psf=1.3), `COSTES_SALT = 0xA5A5A5A5DEADBEEF` (distinct from HOLDOUT_SALT/FOLD_SALT), `COSTES_ALPHA = 0.05` (significance verdict cutoff), `DIVERGENCE_WARN = 0.30`, `DIVERGENCE_FAIL = 0.60` (amber/red bands), `MASTER_SEED = 0x00000000_00C0FFEE` (harness Philox master seed), `TAPQIR_PUBLISHED_VALUE = NaN` (placeholder — pinned by plan 09-04 after the tutorial is run once; NaN documents "not yet captured"), `TAPQIR_TOL = 0.05` (provisional acceptance tolerance, confirmed in 09-04). Document each threshold + its rationale in a comment block so the artifact header (written by 09-05) can quote it. Use lower_snake filenames and UPPER_SNAKE consts per the observed convention. No fenced logic beyond const declarations — this file is declarative.</action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/comparator/config.jl"); @assert COSTES_N_SCRAMBLE == 200; @assert COSTES_BLOCK_PX == 7; @assert COSTES_SALT != 0x9E3779B97F4A7C15 && COSTES_SALT != 0xD1B54A32D192ED03; @assert DIVERGENCE_WARN < DIVERGENCE_FAIL; @assert isnan(TAPQIR_PUBLISHED_VALUE); println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/comparator/config.jl` exists, begins with the AGPL header block, and declares all nine consts named above
    - `COSTES_SALT` differs from both `HOLDOUT_SALT` and `FOLD_SALT`
    - `DIVERGENCE_WARN < DIVERGENCE_FAIL`
    - `TAPQIR_PUBLISHED_VALUE` is `NaN` (unpinned placeholder) and there is a comment noting plan 09-04 pins the real value
    - The verify command prints `OK`
  </acceptance_criteria>
  <done>All comparator thresholds are committed as pre-declared consts with documented rationale, before any table exists (D-14 satisfied).</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold test_comparator.jl and wire the resolve-risk gate (g)</name>
  <files>spike/test/test_comparator.jl, spike/test/runtests.jl</files>
  <read_first>
    - spike/test/test_npe.jl (whole file: the scaffold template — header, guarded include, `@testset ... verbose = true` with named child testsets, the `@test_skip` Wave-0 idiom)
    - spike/test/runtests.jl (lines ~52-103: the existing resolve-risk gate d/e/f keyed by the NeuralEstimators UUID, and the `include(...)` chain appended after each phase testset)
    - spike/comparator/config.jl (the consts this testset includes)
  </read_first>
  <action>Create `spike/test/test_comparator.jl` with the AGPL header, a guarded `include` of `spike/comparator/config.jl`, and a top-level `@testset "Phase 9: Comparator harness" verbose = true begin ... end` containing named child testsets — one per CMP requirement — each currently holding a single `@test_skip true` tagged with a comment naming the CMP-* req it will fill (CMP-01 finiteness, CMP-02 Costes/Manders-equality, CMP-03 shared inputs, CMP-04 traffic-light, CMP-05/08 determinism, CMP-06 Tapqir anchor-or-skip, CMP-07 audit). Then edit `spike/test/runtests.jl`: (a) append `include(joinpath(@__DIR__, "test_comparator.jl"))` after the existing `test_npe.jl` include; (b) add a new resolve-risk assertion block (g) copying the shape of the existing (d)/(e)/(f): assert `haskey(_deps_by_name, "DataFrames")`, `haskey(_deps_by_name, "CSV")`, `!haskey(Pkg.project().dependencies, "PythonCall")`, `!haskey(Pkg.project().dependencies, "CondaPkg")`, and re-assert `Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"`.</action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/runtests.jl` exits 0 (all green, comparator child testsets skipped)
    - `spike/test/runtests.jl` contains `include` of `test_comparator.jl` after the `test_npe.jl` include
    - `spike/test/runtests.jl` gate (g) asserts DataFrames + CSV present AND PythonCall + CondaPkg absent from the main env AND NeuralEstimators == v0.2.1
    - `spike/test/test_comparator.jl` contains one skipped child testset per CMP-* requirement, each tagged with its CMP id
  </acceptance_criteria>
  <done>The comparator testset is part of the single spike gate (green, skipped), and the resolve-risk gate now protects the DataFrames/CSV promotion and forbids Python deps in the main env.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency resolver → pinned env | `Pkg.add` may co-resolve and downgrade the NeuralEstimators v0.2.1 pin (the recurring GLMakie-class landmine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | spike/Manifest.toml resolve | mitigate | Resolve-risk gate (g) re-asserts NeuralEstimators == v0.2.1 after promotion; task fails loudly if downgraded |
| T-09-02 | Tampering | root Project.toml/Manifest.toml | mitigate | All `Pkg.add` scoped to `--project=spike`; acceptance criterion checks root files byte-unchanged |
| T-09-03 | Elevation | PythonCall/CondaPkg leaking into main env | mitigate | Gate (g) asserts PythonCall + CondaPkg absent from the main spike env |
| T-09-SC | Tampering | DataFrames/CSV supply chain | accept | Established Julia General packages already resolved in the pinned Manifest; slopcheck N/A (not npm/PyPI); resolve-risk gate is the applicable integrity check |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` green with the comparator scaffold skipped.
- `git diff` shows zero edits to root `Project.toml`/`Manifest.toml` and zero edits to `spike/data/encode.jl` or `src/`.
</verification>

<success_criteria>
DataFrames + CSV are direct spike deps (no NeuralEstimators downgrade), all D-14 thresholds are pre-declared in `config.jl`, the comparator testset is wired green into `runtests.jl`, and the resolve-risk gate forbids Python deps in the main env.
</success_criteria>

<output>
Create `.planning/phases/09-cross-method-comparator-harness/09-01-SUMMARY.md` when done
</output>

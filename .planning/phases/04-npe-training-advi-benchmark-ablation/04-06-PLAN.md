---
phase: 04-npe-training-advi-benchmark-ablation
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-03, 04-05]
files_modified:
  - spike/npe/ablation.jl
  - spike/npe/summary_choice.md
  - spike/test/test_npe.jl
autonomous: true
requirements: [ABL-01, ABL-02]
must_haves:
  truths:
    - "The minimal (:min, 128-dim) and augmented (:aug, 142-dim) cached summaries are trained on the SAME architecture under leak-free k=5 CV with zero re-simulation (D-06)"
    - "Per-parameter RMSE (all 7 θ, ρ_true headline) is scored per fold for both variants"
    - "A pre-registered decision rule selects :min or :aug (aug wins iff RMSE_aug ≤ (1−ABL_REL_MARGIN)·RMSE_min on ρ_true AND improves in ≥ABL_FOLD_CONSISTENCY/5 folds)"
    - "The chosen summary is justified in writing with its OOD-detectability interaction noted (couples to Phase 5)"
  artifacts:
    - path: "spike/npe/ablation.jl"
      provides: ":min vs :aug k=5 CV per-parameter RMSE + the D-07 decision rule"
      contains: "ABL_REL_MARGIN"
    - path: "spike/npe/summary_choice.md"
      provides: "ABL-02 written justification + OOD-detectability note"
      contains: "OOD"
  key_links:
    - from: "spike/npe/ablation.jl"
      to: "spike/data/loader.jl load_fold(...; variant=:min|:aug)"
      via: "variant toggle over the same cache (zero re-simulation)"
      pattern: "variant\\s*=\\s*:aug"
    - from: "spike/npe/ablation.jl"
      to: "spike/npe/architecture.jl + train_npe.jl"
      via: "same build_estimator/train_fold reused per variant"
      pattern: "train_fold|build_estimator"
---

<objective>
Run the summary-statistic ablation: train the SAME NPE architecture on the minimal (`:min`, 128-dim)
and augmented (`:aug`, 142-dim) cached summaries under the loader's leak-free k=5 CV (zero
re-simulation — both variants are already cached, D-06), score per-parameter RMSE (all 7 θ, ρ_true
headline) per fold, apply the pre-registered decision rule (D-07) to choose `:min` or `:aug`, and
write the justification with its OOD-detectability interaction noted (ABL-02). Turn SC4 and SC5 green.

Purpose: ABL-01/ABL-02 make summary sufficiency a measured gating diagnostic rather than an
assumption; the chosen summary feeds Phase 5 (SBC/BF/OOD), and the OOD note is the explicit coupling.
Output: `spike/npe/ablation.jl`, `spike/npe/summary_choice.md`, SC4 + SC5 green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-CONTEXT.md

<interfaces>
<!-- Consumed contracts. -->

spike/data/loader.jl: load_fold(dir, f; K=5, master_seed, variant=:min|:aug) -> (Ztr, θtr, Zva, θva, zt)
                      all_folds(...)  # the k=5 fold enumeration
spike/npe/architecture.jl (04-03): build_estimator(d_in, 7; dstar, depth, width)  # SAME arch for both variants
spike/npe/train_npe.jl (04-03): train_fold(dir, fold; master_seed, variant, use_gpu=false)
NeuralEstimators v0.2.1: assess(est, θva, Zva) -> Assessment ; rmse(assessment)  # per-parameter
Pre-registered consts (test_npe.jl): ABL_REL_MARGIN=0.05, ABL_FOLD_CONSISTENCY=4, NPE_MASTER_SEED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: :min vs :aug per-parameter RMSE under leak-free k=5 CV</name>
  <files>spike/npe/ablation.jl</files>
  <read_first>
    - spike/npe/train_npe.jl + architecture.jl (04-03 — reuse the SAME build_estimator/train_fold; the only change between runs is `variant`)
    - spike/data/loader.jl (the `variant=` switch L166-170 and the k=5 fold loop; mask-bypass for :min rows 65:128)
    - spike/test/test_data_pipeline.jl (the fold-loop + fixture-cache test style)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Pattern" ablation + ABL-01 row + Pitfall 1 (use_gpu)
  </read_first>
  <action>
    Create `spike/npe/ablation.jl` (AGPL header; guarded includes of train_npe.jl + loader.jl). Expose
    `ablate(dir; master_seed=NPE_MASTER_SEED, K=5)`: for each fold f in 1:K and each `variant ∈ (:min, :aug)`,
    `train_fold(dir, f; master_seed, variant, use_gpu=false)` on the SAME architecture, then per-parameter
    `rmse(assess(est, fold.θva, fold.Zva))` (all 7 θ). Collect a `K×2×7` (fold × variant × parameter) RMSE
    table plus the per-variant mean ρ_true RMSE and the per-fold ρ_true RMSE pair. Return a NamedTuple
    `(rmse_table, rho_rmse_min, rho_rmse_aug, fold_wins_aug)` where `fold_wins_aug` counts folds in which
    `:aug` beats `:min` on ρ_true. Zero re-simulation — only the `variant` toggle differs. Keep the
    architecture/seed identical across variants so the comparison is clean.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/npe/ablation.jl"); @assert isdefined(Main,:ablate); println("ablate defined OK")'</automated>
  </verify>
  <acceptance_criteria>
    - Both `:min` and `:aug` are trained on the identical `build_estimator` architecture and `master_seed`, k=5 CV.
    - Per-parameter RMSE (all 7 θ) is produced per fold per variant via `assess`/`rmse`.
    - No re-simulation occurs — only `load_fold(...; variant=…)` differs.
    - `use_gpu=false` on every training/assess call.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pre-registered decision rule + written summary-choice justification with the OOD note</name>
  <files>spike/npe/ablation.jl, spike/npe/summary_choice.md</files>
  <read_first>
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-CONTEXT.md D-07 (advisory-with-rule gate) + ABL-02 (OOD coupling)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md Assumptions A3 (δ=0.05 + ≥4/5 folds) + §"Blockers" Phase-5 OOD bound (summary-orthogonal misspecifications undetectable)
    - spike/npe/ablation.jl (Task 1 output — rho_rmse_min/rho_rmse_aug/fold_wins_aug)
  </read_first>
  <action>
    Add `choose_summary(result) -> Symbol` to `ablation.jl` implementing D-07 exactly: return `:aug` iff
    `rho_rmse_aug ≤ (1 - ABL_REL_MARGIN) * rho_rmse_min` AND `fold_wins_aug ≥ ABL_FOLD_CONSISTENCY`; otherwise
    return `:min` (parsimony + OOD detectability). A SBC-pass-but-high-RMSE outcome is an insufficiency signal,
    not a pass — note this in the function docstring. Write `spike/npe/summary_choice.md` recording: the RMSE
    table summary, the rule inputs (margin 0.05, ≥4/5 folds), the chosen variant, and an explicit
    OOD-detectability note — the minimal summary is more parsimonious and its discrepancy channel is more
    orthogonal-detectable, so a tie defaults to `:min` (couples to Phase 5; the fixed patch-correlation summary
    structurally bounds OOD power on summary-orthogonal misspecifications). This document is the ABL-02
    deliverable consumed by Phase 5.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/npe/ablation.jl"); @assert isdefined(Main,:choose_summary); @assert isfile("spike/npe/summary_choice.md"); println("decision rule + justification OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `choose_summary` returns `:aug` ONLY when both the 0.05 relative-margin AND the ≥4/5-fold conditions hold; else `:min`.
    - `spike/npe/summary_choice.md` states the chosen variant, the rule inputs, and an explicit OOD-detectability note coupling to Phase 5.
    - The justification names the structural OOD blind spot (summary-orthogonal discrepancies) rather than hiding it.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Turn SC4 + SC5 green — ablation RMSE and the decision rule</name>
  <files>spike/test/test_npe.jl</files>
  <read_first>
    - spike/test/test_npe.jl (SC4/SC5 @test_skip placeholders + consts from 04-01; replace the skips; SC1/SC2/SC3 already live)
    - spike/npe/ablation.jl (ablate / choose_summary under test)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-VALIDATION.md §"Per-Task Verification Map" SC4/SC5 rows
  </read_first>
  <action>
    Replace the SC4 and SC5 `@test_skip`s. `SC4 (ABL-01)`: run `ablate` on a small fixture cache and assert it
    returns finite per-parameter RMSE for all 7 θ for BOTH `:min` and `:aug` across k=5 folds, with the ρ_true
    RMSE present per variant. `SC5 (ABL-02)`: assert `choose_summary` returns `:min` or `:aug` consistent with
    the pre-registered rule applied to the fixture result, AND `spike/npe/summary_choice.md` exists and contains
    an OOD note (grep for an "OOD" mention, filtering comment-only lines is not needed since it is prose). Do
    NOT modify SC1/SC2/SC3. Keep the fixture small for the quick gate.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_npe.jl</automated>
  </verify>
  <acceptance_criteria>
    - `SC4 (ABL-01)` passes asserting finite all-7 per-parameter RMSE for both variants across k=5 folds.
    - `SC5 (ABL-02)` passes asserting `choose_summary ∈ {:min,:aug}` per the rule AND `summary_choice.md` with an OOD note exists.
    - SC1/SC2/SC3 remain untouched and green.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `julia --project=spike spike/test/test_npe.jl` green with SC1..SC5 all live except the scaling characterization (04-07).
- `spike/npe/summary_choice.md` records the chosen summary + OOD note.
</verification>

<success_criteria>
- Per-parameter RMSE is measured for :min vs :aug under leak-free k=5 CV (ABL-01) and the pre-registered rule selects and justifies the summary with its OOD interaction noted (ABL-02); SC4 and SC5 green.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-06-SUMMARY.md` when done
</output>

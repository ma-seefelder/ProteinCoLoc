---
phase: 04-npe-training-advi-benchmark-ablation
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - spike/baseline/run_advi.jl
  - spike/baseline/advi_artifact.jld2
autonomous: true
requirements: [NPE-02, NPE-03]
must_haves:
  truths:
    - "The removed vi(m, ADVI(int,int)) call is ported to the modern vi(model, q_meanfield_gaussian, ITER; adtype=AutoForwardDiff()) → VIResult API in the isolated baseline env"
    - "ADVI runs on RAW re-simulated holdout MultiChannelImages (not cached summaries), per paired (sample,control) stacks (D-04)"
    - "ADVI μ posterior draws are verified to lie in the constrained support [-1,1] before any ghat mapping"
    - "An integrity-checked JLD2 artifact carries per-pair μ sample vectors, ρ-space mappings, intervals, and per-pair vi() wall-clock"
  artifacts:
    - path: "spike/baseline/run_advi.jl"
      provides: "modern AdvancedVI port + holdout re-sim + integrity-checked artifact writer"
      contains: "q_meanfield_gaussian"
    - path: "spike/baseline/advi_artifact.jld2"
      provides: "the ONLY cross-env hand-off the spike benchmark reads (D-02)"
      contains: "wall_clock"
  key_links:
    - from: "spike/baseline/run_advi.jl"
      to: "spike/baseline/model.jl build_coloc_model + spike/npe/resimulate.jl"
      via: "re-simulate holdout pair → construct @model → vi()"
      pattern: "vi\\(.*q_meanfield_gaussian"
    - from: "spike/baseline/advi_artifact.jld2"
      to: "spike/npe/benchmark.jl (04-05)"
      via: "JLD2 read of mu_sample/wall_clock keyed by holdout global_index"
      pattern: "wall_clock"
---

<objective>
Port the removed pre-0.35 `vi(m, ADVI(int,int))` ADVI call to the modern Turing/AdvancedVI
`vi(model, q_meanfield_gaussian, ITER; adtype=AutoForwardDiff())` → `VIResult` API inside the
isolated baseline env, run it on the RAW re-simulated holdout pairs (D-04), verify the μ draws are
in constrained support, and serialize an integrity-checked JLD2 artifact (per-pair μ sample vectors,
ρ-space mappings, intervals, per-pair `vi()` wall-clock) that the spike-side benchmark consumes —
the single cross-env hand-off (D-02).

Purpose: This is the ground-truth baseline the NPE is benchmarked against for RMSE (NPE-02) and the
>100× speedup (NPE-03). It must run entirely in the broken-Turing-free isolated env and never depend
on repairing the parent project.
Output: `spike/baseline/run_advi.jl`, `spike/baseline/advi_artifact.jld2`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md
@.planning/spikes/003-bayes-model-modularity/README.md

<interfaces>
<!-- The port target + the modern API. Verified against src/bayes.jl and AdvancedVI v0.7 docs. -->

src/bayes.jl (READ-ONLY, port target):
  q = vi(m, ADVI(num_latent, iter))        # L325 — REMOVED. ADVI(n,iter) gone; 2-arg vi gone.
  DynamicPPL.syms(VarInfo(m))               # REMOVED — use keys(DynamicPPL.VarInfo(m)) or known globals
  sample(m, Prior(), posterior_samples)     # L308 — 100k prior chain, NOT part of ADVI cost; SKIP (dead)

modern AdvancedVI v0.7 / Turing v0.45 (cited):
  result = vi(model, q_meanfield_gaussian, ITER; adtype=AutoForwardDiff(), show_progress=false)
  q = result.q                              # VIResult: .q, .info, .state, .ldf
  draws = rand(q, POSTERIOR_SAMPLES)        # d_latent × N — ASSERT extrema(μ) ⊂ [-1,1] (Pitfall 3)

spike/baseline/model.jl (04-02): build_coloc_model(mci_sample, mci_control, channels, num_patches)
spike/npe/resimulate.jl (04-01): resimulate_holdout(dir, j; master_seed) -> (mci_sample, theta, global_index)
spike/simulator/ghat.jl: ghat(μ::Real) -> Float64  (frozen; baseline includes it read-only for ρ-space precompute)
spike/data/cache.jl: write_shard atomic idiom — jldsave(tmp;…); reopen-assert haskey; mv(tmp,path;force=true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the ADVI call to modern AdvancedVI and verify constrained-space draws on one re-simulated pair</name>
  <files>spike/baseline/run_advi.jl</files>
  <read_first>
    - src/bayes.jl (L304-339 — the inference block being ported: vi(), num_latent via DataFrame(prior_chain), syms; READ-ONLY)
    - .planning/spikes/003-bayes-model-modularity/README.md §INFER-1 (the 0.43→0.45 vi return-type caveat) and §INFER-3 (prior-chain is dead cost)
    - spike/baseline/model.jl (04-02 — build_coloc_model)
    - spike/npe/resimulate.jl (04-01 — resimulate_holdout for one pair)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Pattern 4" + §"Pattern 3" (time only vi(); skip prior chain) + Pitfall 3 (constrained vs unconstrained)
  </read_first>
  <action>
    Create `spike/baseline/run_advi.jl` (AGPL header; activated under `spike/baseline`). Guarded-include
    `model.jl` (read-only @model) and `resimulate.jl`. Implement `advi_pair(mci_sample, mci_control;
    iter=ITER, posterior_samples=N)`: build the model via `build_coloc_model`, run
    `result = vi(m, q_meanfield_gaussian, iter; adtype=AutoForwardDiff(), show_progress=false)`, take
    `q = result.q`, `draws = rand(q, posterior_samples)`. Replace the removed `syms`/`num_latent` machinery
    with `keys(DynamicPPL.VarInfo(m))` (or the 8 known globals by declaration order) to locate `μ_sample`
    and `μ_control`. SKIP the 100k `sample(m, Prior(), …)` prior chain (dead cost; not part of the ADVI
    clock per D-08). Add a hard assertion that `extrema` of the μ_sample/μ_control draws ⊂ [-1,1]; if not,
    apply the model's `Bijectors` inverse transform before reading marginals (Pitfall 3 — the single biggest
    port correctness risk). Pre-register the baseline defaults as named consts: `ITER` (≈1000), `N` (≈10000),
    `AutoForwardDiff`, `q_meanfield_gaussian` (note `q_fullrank_gaussian` as a documented sensitivity option,
    A5). Smoke-run `advi_pair` on ONE re-simulated holdout pair.
  </action>
  <verify>
    <automated>julia --project=spike/baseline -e 'include("spike/baseline/run_advi.jl"); println("run_advi loaded; advi_pair defined: ", isdefined(Main,:advi_pair))'</automated>
  </verify>
  <acceptance_criteria>
    - `advi_pair` uses `vi(m, q_meanfield_gaussian, ITER; adtype=AutoForwardDiff())` and reads `result.q`; no `ADVI(n,iter)` or 2-arg `vi` remains.
    - μ_sample/μ_control draws are asserted ⊂ [-1,1] (constrained); the inverse bijector is applied if the raw draws are unconstrained.
    - The 100k prior chain is NOT executed.
    - Runs on a single RAW re-simulated `MultiChannelImage` pair (not on cached summaries).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run ADVI over the paired holdout and write the integrity-checked JLD2 artifact</name>
  <files>spike/baseline/run_advi.jl, spike/baseline/advi_artifact.jld2</files>
  <read_first>
    - spike/data/cache.jl (write_shard — the atomic `.tmp`→reopen-assert→`mv(force=true)` idiom to copy)
    - spike/data/generate.jl (L191-196 — holdout schema/namespace tagging)
    - spike/npe/infer.jl (04-03 — rho_from_mu/ghat so ρ-space columns are computed consistently)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Code Examples" (proposed JLD2 ADVI artifact schema) + §"Pattern 3" (pairing) + D-04
  </read_first>
  <action>
    Extend `run_advi.jl` with `run_all_pairs(dir; master_seed)`: form (sample,control) pairs from the
    reserved holdout (D-04), re-simulate each pair's RAW images via `resimulate_holdout`, run `advi_pair`,
    and time ONLY the `vi()` call per pair with the wall clock (the >100× headline pairs a full `vi()` run
    against two NPE passes — D-08). Collect per-pair `mu_sample`/`mu_control` SAMPLE VECTORS (not just
    means, so the spike can recompute Δρ by MC differencing), `rho_sample = ghat.(mu_s)` /
    `rho_control = ghat.(mu_c)`, 90% intervals, and `wall_clock`. Write `spike/baseline/advi_artifact.jld2`
    using the atomic idiom: `jldsave(tmp; schema_version=1, meta=(turing_version=…, adtype="AutoForwardDiff",
    iter=ITER, posterior_samples=N, master_seed=master_seed, bayes_git_ref=<sha>, generated=…), pairs=pair_ids,
    mu_sample, mu_control, rho_sample, rho_control, rho_s_interval, rho_c_interval, wall_clock)`, then reopen
    and `@assert haskey(f,"mu_sample") && haskey(f,"wall_clock")` before `mv(tmp, path; force=true)`. Align
    `pairs` to the holdout `global_index` so 04-05 can join by index.
  </action>
  <verify>
    <automated>julia --project=spike/baseline -e 'include("spike/baseline/run_advi.jl"); using JLD2; f=jldopen("spike/baseline/advi_artifact.jld2","r"); for k in ("schema_version","mu_sample","rho_sample","wall_clock","pairs"); @assert haskey(f,k) k; end; close(f); println("artifact OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/baseline/advi_artifact.jld2` exists with keys `schema_version, meta, pairs, mu_sample, mu_control, rho_sample, rho_control, rho_s_interval, rho_c_interval, wall_clock`.
    - `mu_sample`/`mu_control` are full per-pair sample vectors (length N), not scalars.
    - `wall_clock` measures ONLY the `vi()` call per pair (prior chain excluded).
    - The file is written via reopen-assert-then-`mv(force=true)`; a half-written `.tmp` never becomes the artifact.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Security enforcement not enabled (offline spike). Relevant boundary: cross-env artifact integrity (the artifact is the ONLY coupling between the broken-Turing baseline and the pinned spike).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ARTIFACT | Tampering/Integrity | advi_artifact.jld2 hand-off | mitigate | schema_version + meta (turing version, seed, git ref) tagged; reopen-assert before atomic mv; consumed by 04-05 keyed on global_index |
| T-04-CONSTR | Integrity | unconstrained ADVI draws | mitigate | hard assertion μ ⊂ [-1,1]; inverse bijector applied if violated (Pitfall 3) |
</threat_model>

<verification>
- `advi_artifact.jld2` reopens with all required keys and per-pair vi() wall-clock.
- The baseline runs entirely under `--project=spike/baseline`; the spike env and `src/` are untouched.
</verification>

<success_criteria>
- The modern-API ADVI baseline runs on raw re-simulated paired holdout stacks and emits an integrity-checked JLD2 artifact (μ sample vectors, ρ-space mappings, intervals, vi()-only wall-clock) ready for the benchmark. NPE-02/NPE-03 comparison target in place.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-04-SUMMARY.md` when done
</output>

---
phase: 04-npe-training-advi-benchmark-ablation
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-03, 04-04]
files_modified:
  - spike/npe/benchmark.jl
  - spike/test/test_npe.jl
autonomous: true
requirements: [NPE-02, NPE-03]
must_haves:
  truths:
    - "Per-parameter RMSE is scored in ρ-space: NPE ρ̂ vs known ρ_true, and ADVI μ pushed through frozen ghat, on identical holdout stacks/pairs"
    - "NPE ρ_true RMSE is within the pre-registered NPE_RMSE_TOLERANCE (1.2×) of ADVI's"
    - "90% interval widths are reported for both methods; Δρ RMSE is scored on identical pairs (D-04)"
    - "Measured t_advi/t_npe > SPEEDUP_GATE (100) at BENCH_THREADS=1, with NPE clock = summary extraction + forward pass and ADVI clock = full vi() (D-08), reported paired with RMSE"
  artifacts:
    - path: "spike/npe/benchmark.jl"
      provides: "ρ-space RMSE/interval comparison + BenchmarkTools speedup harness reading advi_artifact.jld2"
      contains: "@belapsed"
  key_links:
    - from: "spike/npe/benchmark.jl"
      to: "spike/baseline/advi_artifact.jld2"
      via: "JLD2 read of mu_sample/wall_clock joined on holdout global_index"
      pattern: "advi_artifact"
    - from: "spike/npe/benchmark.jl"
      to: "spike/npe/infer.jl + spike/npe/resimulate.jl"
      via: "re-simulate raw stack → summary → forward pass timed together (D-08)"
      pattern: "@belapsed"
---

<objective>
Benchmark the trained NPE against the ADVI artifact: score per-parameter RMSE and 90% interval width
in ρ-space (NPE ρ̂ vs known ρ_true; ADVI μ pushed through the frozen `ghat`) on identical holdout
stacks and Δρ on identical pairs (NPE-02), and measure the wall-clock speedup with `BenchmarkTools`
(NPE clock = summary extraction + forward pass from the re-simulated raw stack; ADVI clock = the
per-pair `vi()` time from the artifact) asserting `t_advi/t_npe > 100` at `BENCH_THREADS=1`, always
reported paired with the comparable-RMSE result (NPE-03/D-08/D-09). Turn SC2 and SC3 green.

Purpose: NPE-02 and NPE-03 are the headline claims of the phase — amortized inference at >100× ADVI
speed at comparable accuracy. This plan produces the paired number.
Output: `spike/npe/benchmark.jl`, SC2 + SC3 green in `test_npe.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md

<interfaces>
<!-- Consumed contracts. -->

spike/npe/infer.jl (04-03): posterior_for, rho_hat, delta_rho, interval_width, rho_from_mu
spike/npe/train_npe.jl (04-03): train_fold; spike/npe/trained_npe.jld2 (persisted estimator + θ-transform + zt)
spike/npe/resimulate.jl (04-01): resimulate_holdout(dir, j; master_seed) -> (mci_sample, theta, global_index)
spike/baseline/advi_artifact.jld2 (04-04): mu_sample, mu_control, rho_sample, rho_control, *_interval, wall_clock, pairs
NeuralEstimators v0.2.1: assess(est, θ_test, Z_test) -> Assessment ; rmse(assessment)  # per-parameter
BenchmarkTools: @belapsed  (warmup+median; required to substantiate >100×)
Pre-registered consts (test_npe.jl): NPE_RMSE_TOLERANCE=1.2, SPEEDUP_GATE=100.0, BENCH_THREADS=1, NPE_MASTER_SEED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ρ-space per-parameter RMSE + interval width + Δρ RMSE vs the ADVI artifact</name>
  <files>spike/npe/benchmark.jl</files>
  <read_first>
    - spike/npe/infer.jl (rho_hat / rho_from_mu / interval_width / delta_rho — the ρ-space surface)
    - spike/baseline/advi_artifact.jld2 schema (04-04 — mu_sample/rho_sample/wall_clock/pairs keyed by global_index)
    - spike/data/generate.jl (L42-53 guarded-include header pattern)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Pattern 2" (ρ-space alignment, A1), §"Pattern 3" (per-stack vs paired unit), §"Code Examples" (assess+rmse)
  </read_first>
  <action>
    Create `spike/npe/benchmark.jl` (AGPL header; guarded includes of infer.jl + resimulate.jl; `using JLD2,
    Statistics`). Load `trained_npe.jld2` and `advi_artifact.jld2`. For each holdout stack: NPE ρ̂ via
    `rho_hat` against the known `ρ_true` (row 1 of holdout `theta`); ADVI ρ̂ via `rho_from_mu` on the
    artifact's `mu_sample`/`mu_control` (already ρ-mapped as `rho_sample`/`rho_control`). Compute
    per-parameter RMSE for the NPE via `rmse(assess(est, θ_holdout, Z_holdout))` (all 7 θ reported) and the
    ADVI ρ_true RMSE from the artifact, JOINING strictly on `global_index`. Compute 90% interval widths for
    both (NPE via `interval_width`; ADVI via the artifact's `rho_*_interval`). Compute Δρ RMSE on identical
    (sample,control) pairs (D-04): `Δρ_npe` via `delta_rho`, `Δρ_advi` via MC-differencing the artifact μ
    vectors through `ghat`, both vs `Δρ_true = ρ_true(sample) − ρ_true(control)`. Expose
    `rmse_report(dir; master_seed)` returning a NamedTuple `(npe_rho_rmse, advi_rho_rmse, npe_all7_rmse,
    npe_interval_width, advi_interval_width, delta_rho_rmse_npe, delta_rho_rmse_advi)`. Report numbers; the
    pass/fail tolerance gate lives in SC2 (Task 3).
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/npe/benchmark.jl"); @assert isdefined(Main,:rmse_report); println("rmse_report defined OK")'</automated>
  </verify>
  <acceptance_criteria>
    - RMSE is computed in ρ-space for BOTH methods; ADVI μ is mapped via the frozen `ghat`, never compared directly to ρ_true.
    - All 7 θ per-parameter RMSE are produced for the NPE; ρ_true is the headline.
    - NPE↔ADVI rows are joined strictly on holdout `global_index` (no positional misalignment).
    - `Δρ` RMSE is scored on identical pairs for both methods (D-04).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BenchmarkTools speedup harness (D-08) at the pre-registered thread count</name>
  <files>spike/npe/benchmark.jl</files>
  <read_first>
    - spike/npe/resimulate.jl (raw-stack re-sim — the NPE clock must START from the raw MultiChannelImage)
    - spike/baseline/advi_artifact.jld2 (the per-pair vi() wall_clock — the ADVI clock)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Code Examples" (BenchmarkTools harness D-08) + Pitfall 1 (use_gpu) + D-08 (training excluded)
  </read_first>
  <action>
    Extend `benchmark.jl` with `speedup_report(dir; master_seed, N=2000)`: for each holdout pair, measure
    `t_npe = @belapsed` over { re-simulate/encode the raw stack → standardize with frozen `zt` → two
    `sampleposterior` passes (sample + control) } — summary extraction + forward pass ONLY, training
    EXCLUDED (D-08, the amortized claim), `use_gpu=false`. Read `t_advi` as the per-pair `vi()` `wall_clock`
    from the artifact. Compute `speedup = t_advi / t_npe` per pair and the median. Return a NamedTuple
    pairing speedup WITH the RMSE result from Task 1 — speedup is NEVER reported alone (D-08). Record the
    active `Threads.nthreads()` and assert it equals `BENCH_THREADS` for the headline number. GPU timing is a
    separate opt-in branch guarded by a CUDA-device check (D-10), reported only if present, never gating.
  </action>
  <verify>
    <automated>julia --project=spike -t 1 -e 'include("spike/npe/benchmark.jl"); @assert isdefined(Main,:speedup_report); println("speedup_report defined OK; threads=", Threads.nthreads())'</automated>
  </verify>
  <acceptance_criteria>
    - The NPE clock starts from the RAW re-simulated stack and includes summary extraction + forward pass; training is excluded.
    - The ADVI clock is the per-pair `vi()` `wall_clock` read from the artifact.
    - `speedup_report` returns speedup paired with the RMSE result (never speedup alone).
    - The headline is computed at `Threads.nthreads() == BENCH_THREADS` and `use_gpu=false`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Turn SC2 + SC3 green — comparable-RMSE gate and the >100× speedup gate</name>
  <files>spike/test/test_npe.jl</files>
  <read_first>
    - spike/test/test_npe.jl (SC2/SC3 @test_skip placeholders + pre-registered consts from 04-01; replace the skips)
    - spike/npe/benchmark.jl (rmse_report / speedup_report under test)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-VALIDATION.md §"Per-Task Verification Map" SC2/SC3 rows + §"Pre-registered constants"
  </read_first>
  <action>
    Replace the SC2 and SC3 `@test_skip`s in `test_npe.jl`. `SC2 (NPE-02)`: run `rmse_report` on a fixture
    cache (reading a small fixture `advi_artifact.jld2`, or the committed one) and assert `npe_rho_rmse ≤
    NPE_RMSE_TOLERANCE * advi_rho_rmse` (D-09), plus that interval widths and all-7 RMSE are finite and
    reported. `SC3 (NPE-03)`: run `speedup_report` at `BENCH_THREADS` and assert `median_speedup >
    SPEEDUP_GATE` AND that the comparable-RMSE condition from SC2 holds — speedup is asserted ONLY together
    with RMSE (D-08). Assert `Threads.nthreads() == BENCH_THREADS` for the recorded headline and that CUDA is
    not loaded. Do NOT modify SC1/SC4/SC5. Keep the fixture small enough that the quick run stays practical;
    a heavier real run is the phase gate.
  </action>
  <verify>
    <automated>julia --project=spike -t 1 spike/test/test_npe.jl</automated>
  </verify>
  <acceptance_criteria>
    - `SC2 (NPE-02)` passes asserting `npe_rho_rmse ≤ 1.2 × advi_rho_rmse` with interval widths reported.
    - `SC3 (NPE-03)` passes asserting `median_speedup > 100` AND the comparable-RMSE condition jointly, at `Threads.nthreads()==BENCH_THREADS`, CPU-only.
    - SC1/SC4/SC5 remain untouched.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `julia --project=spike -t 1 spike/test/test_npe.jl` green with SC1/SC2/SC3 live, SC4/SC5 skipped.
- The speedup is always emitted paired with the RMSE figure.
</verification>

<success_criteria>
- The NPE is benchmarked vs ADVI in ρ-space for RMSE + interval width (NPE-02) and shown >100× faster at comparable RMSE at a fixed thread count (NPE-03); SC2 and SC3 green.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-05-SUMMARY.md` when done
</output>

---
phase: 04-npe-training-advi-benchmark-ablation
plan: 07
type: execute
wave: 5
depends_on: [04-05, 04-06]
files_modified:
  - spike/npe/scaling.jl
  - spike/npe/run_thread_sweep.jl
  - spike/test/test_npe.jl
autonomous: true
requirements: [NPE-03]
must_haves:
  truths:
    - "Wall-clock is characterized vs number of datasets N and vs input size (imsize/patch grid) for BOTH NPE inference and ADVI, with empirical scaling exponents reported (D-12)"
    - "NPE amortized inference is ~flat O(1)/dataset post-training while ADVI is ~linear in N — the >100× is presented as a curve, not a single point"
    - "A CPU thread-count sweep (1/2/4/8) is run as separate processes and aggregated, with the >100× headline stated at the fixed reported BENCH_THREADS (D-13)"
  artifacts:
    - path: "spike/npe/scaling.jl"
      provides: "scaling curves over N and imsize for NPE + ADVI; empirical exponent fit"
      contains: "imsize"
    - path: "spike/npe/run_thread_sweep.jl"
      provides: "per-process thread-count sweep harness writing JLD2/CSV, aggregated"
      min_lines: 20
  key_links:
    - from: "spike/npe/run_thread_sweep.jl"
      to: "separate julia -t N processes"
      via: "Threads.nthreads() is fixed per process (Pitfall 7) — sweep spawns subprocesses"
      pattern: "nthreads"
    - from: "spike/npe/scaling.jl"
      to: "spike/npe/benchmark.jl + spike/baseline/advi_artifact.jld2"
      via: "reuse the timing harness across N/imsize points"
      pattern: "speedup|scaling"
---

<objective>
Characterize the amortization thesis as a curve (D-12): wall-clock vs number of datasets N and vs
input size (imsize/patch grid) for BOTH NPE inference and ADVI, with empirical scaling exponents
showing NPE ~flat O(1)/dataset post-training against ADVI ~linear in N; and run a CPU thread-count
sweep (1/2/4/8) as separate processes, aggregating the results and stating the >100× headline at the
fixed reported `BENCH_THREADS` (D-13). Turn the SC3-scaling characterization testset green.

Purpose: D-12/D-13 are explicitly in scope — "the amortized thesis IS a scaling claim." Presenting
the speedup as a curve plus an honest core-scaling characterization makes the >100× defensible rather
than a single cherry-picked point.
Output: `spike/npe/scaling.jl`, `spike/npe/run_thread_sweep.jl`, SC3-scaling green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md

<interfaces>
<!-- Consumed contracts. -->

spike/npe/benchmark.jl (04-05): speedup_report / rmse_report  (the timing harness to reuse)
spike/npe/infer.jl (04-03): posterior_for (NPE inference timed across N/imsize)
spike/npe/resimulate.jl (04-01): resimulate_holdout (raw-stack inputs; imsize varied via sample_imsize)
spike/baseline/advi_artifact.jld2 (04-04): per-pair vi() wall_clock (ADVI timing reference)
Pre-registered consts (test_npe.jl): BENCH_THREADS=1, SPEEDUP_GATE=100.0, NPE_MASTER_SEED
RESEARCH Pitfall 7: Threads.nthreads() is fixed per process — sweep MUST spawn separate julia -t N processes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaling curves over N and input size with empirical exponents (D-12)</name>
  <files>spike/npe/scaling.jl</files>
  <read_first>
    - spike/npe/benchmark.jl (04-05 — reuse speedup_report/rmse_report timing primitives; do NOT duplicate the @belapsed harness, call into it)
    - spike/data/generate.jl (the cost-aware imsize sampler; how input size is varied)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Pattern 3"/§"State of the Art" + D-12 + Pitfall 1 (use_gpu)
  </read_first>
  <action>
    Create `spike/npe/scaling.jl` (AGPL header; guarded includes of benchmark.jl/infer.jl/resimulate.jl;
    `using JLD2, Statistics`). Expose `scaling_over_N(dir; master_seed, Ns=[1,5,10,20])` measuring total NPE
    inference wall-clock and total ADVI wall-clock across increasing dataset counts N, and
    `scaling_over_imsize(dir; master_seed, sizes=...)` measuring both methods as input size grows (vary via
    the simulator's imsize sampler). For each, fit and report the empirical scaling exponent (e.g. log-log
    slope) and return them in a NamedTuple `(N_grid, npe_time_N, advi_time_N, npe_exponent_N, advi_exponent_N,
    imsize_grid, npe_time_sz, advi_time_sz, npe_exponent_sz, advi_exponent_sz)`. Persist the curves to a JLD2
    via the atomic idiom. The N/imsize ranges are Claude's discretion but must be pre-registered as named
    consts before the reported run. `use_gpu=false` throughout. This is characterization (reported), not a
    pass/fail gate.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/npe/scaling.jl"); for f in (:scaling_over_N,:scaling_over_imsize); @assert isdefined(Main,f) f; end; println("scaling defined OK")'</automated>
  </verify>
  <acceptance_criteria>
    - Both `scaling_over_N` and `scaling_over_imsize` measure NPE inference AND ADVI across the grid.
    - An empirical scaling exponent is fit and reported for each method on each axis.
    - The N/imsize grids are pre-registered named consts; `use_gpu=false` throughout.
    - Curves are persisted to JLD2 via the atomic reopen-assert-then-mv idiom.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CPU thread-count sweep as separate processes (D-13) and SC3-scaling testset</name>
  <files>spike/npe/run_thread_sweep.jl, spike/test/test_npe.jl</files>
  <read_first>
    - spike/npe/benchmark.jl (04-05 — speedup_report invoked inside each spawned process)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md Pitfall 7 (nthreads fixed per process) + D-13 (characterization, headline at fixed thread count)
    - spike/test/test_npe.jl (the live SC3 testset from 04-05; add a sibling SC3-scaling testset, do NOT edit SC1/SC2/SC4/SC5)
    - spike/data/generate.jl (the established pattern for spawning `julia -t N` and aggregating, if present)
  </read_first>
  <action>
    Create `spike/npe/run_thread_sweep.jl` (AGPL header) that, because `Threads.nthreads()` is immutable at
    runtime (Pitfall 7), SPAWNS separate `julia --project=spike -t N spike/npe/...` subprocesses for
    N ∈ {1,2,4,8}, each running `speedup_report` and writing its NPE/ADVI wall-clock to a per-N JLD2/CSV;
    then aggregate into a single thread-scaling table identifying parallel speedup / Amdahl saturation. State
    the >100× headline at `BENCH_THREADS` (the fixed, reported thread count), not the best thread count. In
    `spike/test/test_npe.jl`, add a NEW named `SC3-scaling (NPE-03 characterization)` testset asserting: the
    scaling curves exist with finite exponents, NPE-over-N exponent is materially flatter than ADVI-over-N
    (qualitative amortization check, not a tuned threshold), and the thread-sweep aggregation file is produced.
    Keep the testset fast (small grids / cached timings); the full sweep is a reported artifact, not a CI gate.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_npe.jl</automated>
  </verify>
  <acceptance_criteria>
    - The thread sweep runs as SEPARATE `julia -t N` processes (never by mutating `nthreads()` in one session) and aggregates per-N results.
    - The >100× headline is reported at the fixed `BENCH_THREADS`, with the full {1,2,4,8} sweep reported alongside as characterization.
    - `SC3-scaling` asserts finite exponents and a flatter NPE-over-N curve than ADVI, plus the aggregation artifact exists.
    - SC1/SC2/SC3/SC4/SC5 remain untouched and green.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `julia --project=spike spike/test/runtests.jl` green end-to-end: Phase 1-3 gates + SC1..SC5 + SC3-scaling, all live.
- Scaling curves + thread-sweep aggregation artifacts produced.
</verification>

<success_criteria>
- The >100× claim is presented as a curve over N and input size with empirical exponents (NPE flat, ADVI linear) plus an honest CPU core-scaling characterization, headline stated at a fixed thread count (D-12/D-13); SC3-scaling green. Phase 4 NPE-03 fully characterized.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-07-SUMMARY.md` when done
</output>

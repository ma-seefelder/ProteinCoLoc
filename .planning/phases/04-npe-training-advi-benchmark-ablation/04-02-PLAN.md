---
phase: 04-npe-training-advi-benchmark-ablation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/baseline/Project.toml
  - spike/baseline/Manifest.toml
  - spike/baseline/model.jl
autonomous: true
requirements: [NPE-02]
must_haves:
  truths:
    - "A fully isolated spike/baseline/ Julia env resolves a modern Turing/AdvancedVI stack plus the simulator deps needed to re-simulate holdout images"
    - "Turing/AdvancedVI/ForwardDiff appear ONLY in spike/baseline/Project.toml, never in spike/Project.toml"
    - "The hierarchical colocalization() @model is reachable read-only via include() and constructs against a re-simulated MultiChannelImage pair without editing src/"
  artifacts:
    - path: "spike/baseline/Project.toml"
      provides: "isolated baseline env deps (Turing, AdvancedVI, DataFrames, JLD2, ForwardDiff, StatsBase, Images, ImageFiltering, Random123)"
      contains: "Turing"
    - path: "spike/baseline/model.jl"
      provides: "read-only lift of src/bayes.jl @model + colocalization summary/IO via include()"
      min_lines: 30
  key_links:
    - from: "spike/baseline/model.jl"
      to: "src/bayes.jl + src/colocalization.jl + src/LoadImages.jl"
      via: "read-only include(joinpath(@__DIR__, '..', '..', 'src', ...))"
      pattern: "include.*src.*bayes"
---

<objective>
Create the dedicated, isolated `spike/baseline/` Julia environment (D-01) that will carry the
modern Turing/AdvancedVI ADVI stack and the simulator dependencies it needs to re-simulate holdout
images, and establish the read-only `include()` boundary that lifts the existing `colocalization()`
`@model` from `src/bayes.jl` without editing `src/`.

Purpose: Turing cannot co-resolve with the pinned NeuralEstimators 0.2.1 in the spike env (the
Phase-1 landmine) and the parent env's Turing is broken; the only safe home for the ground-truth
ADVI baseline is its own pinned env. This plan resolves and pins that env and proves the `@model`
loads against a re-simulated stack, de-risking the ADVI port (04-04) before it runs.
Output: `spike/baseline/Project.toml`, pinned `spike/baseline/Manifest.toml`, `spike/baseline/model.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-CONTEXT.md

<interfaces>
<!-- The src/ targets lifted read-only. Extracted from src/bayes.jl (READ-ONLY — never edit). -->

src/bayes.jl:
  colocalization(img, control, channels, num_patches; iter, posterior_samples) -> CoLocResult  # L234
  the hierarchical @model with μ_control/μ_sample ~ Truncated(Cauchy(0,0.3),-1,1)              # L267-303
  vi(m, ADVI(num_latent, iter))   # L325 — REMOVED API, ported in 04-04 (NOT here)
  CoLocResult (L75), _prepare_data (L164), compute_BayesFactor (L109)
src/colocalization.jl: patch(), correlation(x,y; method), _exclude_zero
src/LoadImages.jl: MultiChannelImage(...)

spike/contract.jl (the established read-only include() boundary to copy):
  include(joinpath(@__DIR__, "..", "src", "LoadImages.jl"))
  include(joinpath(@__DIR__, "..", "src", "colocalization.jl"))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve and pin the isolated baseline environment</name>
  <files>spike/baseline/Project.toml, spike/baseline/Manifest.toml</files>
  <read_first>
    - spike/Project.toml (the [deps] UUID-list format to mirror)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Standard Stack" (Installation (baseline env)) and §"Common Pitfalls" Pitfall 6 (do NOT add KernelAbstractions)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-CONTEXT.md D-01 (isolation rationale)
  </read_first>
  <action>
    Create `spike/baseline/Project.toml` and resolve a fresh isolated env:
    `using Pkg; Pkg.activate("spike/baseline"); Pkg.add(["Turing", "AdvancedVI", "DataFrames", "JLD2",
    "ForwardDiff", "StatsBase", "Images", "ImageFiltering", "Random123"])`. The simulator deps
    (Images, ImageFiltering, StatsBase, Random123) are required because the baseline re-simulates the
    holdout raw `MultiChannelImage`s itself (Pitfall 4). Co-resolve here is SAFE — there is no
    NeuralEstimators pin to protect in this env. After resolve, pin and commit
    `spike/baseline/Manifest.toml` as a reproducibility artifact, exactly as `spike/Manifest.toml` is
    pinned. Record the resolved Turing/AdvancedVI versions in the SUMMARY (RESEARCH warns the `vi`
    return type differs across Turing 0.43→0.45 — the exact version matters for 04-04). Do NOT add
    KernelAbstractions as a direct dep.
  </action>
  <verify>
    <automated>julia --project=spike/baseline -e 'using Pkg; d=Pkg.project().dependencies; for p in ["Turing","AdvancedVI","DataFrames","JLD2","ForwardDiff","StatsBase","Images","ImageFiltering","Random123"]; @assert haskey(d,p) p; end; println("baseline env OK ", Pkg.dependencies()[d["Turing"]].version)'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/baseline/Project.toml` `[deps]` contains all nine packages listed above.
    - `spike/baseline/Manifest.toml` exists, is pinned, and is committed.
    - The spike env is untouched: `julia --project=spike -e 'using Pkg; @assert !haskey(Pkg.project().dependencies,"Turing")'` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lift the colocalization @model read-only into the baseline env</name>
  <files>spike/baseline/model.jl</files>
  <read_first>
    - src/bayes.jl (the @model at L267-303, colocalization() at L234, _prepare_data at L164 — READ-ONLY, never edit)
    - spike/contract.jl (L45-47 — the read-only include() coupling discipline to replicate)
    - spike/npe/resimulate.jl (from 04-01 — provides resimulate_holdout to produce a test MultiChannelImage; include read-only)
    - .planning/spikes/003-bayes-model-modularity/README.md §MODEL-1 (verifies the @model ports verbatim — captures no enclosing var)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md §"spike/baseline/model.jl"
  </read_first>
  <action>
    Create `spike/baseline/model.jl` (GNU-AGPL header). Following the `spike/contract.jl` pattern,
    read-only `include(joinpath(@__DIR__, "..", "..", "src", "LoadImages.jl"))` and
    `include(joinpath(@__DIR__, "..", "..", "src", "colocalization.jl"))`, and lift the hierarchical
    Turing `@model` from `src/bayes.jl` — either by a read-only `include` of bayes.jl or by copying the
    `@model coloc_model(...)` block verbatim (spike 003 confirmed it captures no enclosing variable, so
    a verbatim copy is faithful). Expose a `build_coloc_model(mci_sample, mci_control, channels,
    num_patches)` constructor that returns the instantiated model. Do NOT port the `vi()` inference call
    here — that is 04-04. Add a guarded `isdefined`/`include` of `spike/npe/resimulate.jl` plus the
    simulator includes so the model can be smoke-constructed against a re-simulated holdout pair. NEVER
    edit any file under `src/`.
  </action>
  <verify>
    <automated>julia --project=spike/baseline -e 'include("spike/baseline/model.jl"); println("model.jl loaded; build_coloc_model defined: ", isdefined(Main, :build_coloc_model))'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/baseline/model.jl` reaches `src/` only via `include(joinpath(@__DIR__, "..", "..", "src", ...))`; `git status` on `src/` stays clean.
    - `build_coloc_model(...)` constructs a Turing model object from a re-simulated `MultiChannelImage` pair without error.
    - No `vi()` / ADVI inference call is present in this file (deferred to 04-04).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Security enforcement is not enabled (offline simulation spike). Relevant boundary: supply-chain + decoupling integrity.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC2 | Tampering | baseline `Pkg.add` (Turing/AdvancedVI/Images) | mitigate | All flagship JuliaStats/TuringLang packages, [Approved] in RESEARCH §Package Legitimacy Audit; pinned Manifest committed |
| T-04-DECOUPLE | Tampering | read-only src/ include() | mitigate | No `src/` writes; coupling via `include()` only (CLAUDE.md hard constraint); verified by clean `git status` on src/ |
</threat_model>

<verification>
- The isolated env resolves and pins without touching `spike/Project.toml` or `src/`.
- `model.jl` loads and constructs the `@model` against a re-simulated holdout pair.
</verification>

<success_criteria>
- A pinned, isolated `spike/baseline/` env exists with Turing absent from the spike env, and the `colocalization()` `@model` is reachable read-only and instantiable — ready for the ADVI port in 04-04.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-02-SUMMARY.md` when done
</output>

---
phase: 04-npe-training-advi-benchmark-ablation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/test/runtests.jl
  - spike/test/test_npe.jl
  - spike/npe/resimulate.jl
autonomous: true
requirements: [NPE-01]
must_haves:
  truths:
    - "BenchmarkTools is a direct dep of the spike env and NeuralEstimators is still pinned at v0.2.1 after the add"
    - "A single `julia --project=spike spike/test/runtests.jl` enumerates SC1..SC5 for Phase 4 (skipped placeholders) plus a passing Wave-0 holdout-reproducibility gate"
    - "A holdout stack re-simulated from (theta, global_index, master_seed) reproduces its cached summary bit-for-bit (Open Question 1 resolved)"
    - "The six pre-registered constants are committed before any reported run"
  artifacts:
    - path: "spike/test/test_npe.jl"
      provides: "SC1..SC5 named @test_skip placeholders + pre-registered consts + real Wave-0 holdout-repro testset"
      contains: "NPE_RMSE_TOLERANCE"
    - path: "spike/npe/resimulate.jl"
      provides: "keyed holdout re-simulation helper shared by run_advi.jl and benchmark.jl"
      min_lines: 20
    - path: "spike/Project.toml"
      provides: "BenchmarkTools added; Turing absent"
      contains: "BenchmarkTools"
  key_links:
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_npe.jl"
      via: "include at the bottom of the harness"
      pattern: "include.*test_npe.jl"
    - from: "spike/npe/resimulate.jl"
      to: "spike/data/seeding.jl + spike/simulator + spike/data/encode.jl"
      via: "guarded include + Philox4x keyed re-sim"
      pattern: "holdout_rng|simulate_pair"
---

<objective>
Stand up the Phase-4 spike-side foundations: add `BenchmarkTools` to the pinned spike env without
moving the NeuralEstimators v0.2.1 pin, extend the resolve-risk gate, scaffold `spike/test/test_npe.jl`
with the SC1..SC5 placeholders and the six pre-registered constants, build the shared keyed
holdout re-simulation helper, and prove the holdout raw-image reproducibility round-trip (Open
Question 1) so the ADVI baseline (04-04) and benchmark (04-05) can both re-simulate byte-identical
raw stacks.

Purpose: Every reported Phase-4 number depends on (a) BenchmarkTools being present without a pin
regression, (b) constants pre-registered before any run (anti-data-snooping), and (c) the holdout
being reproducible from stored θ+index. All three are gating and must land first.
Output: Updated `spike/Project.toml`/`Manifest.toml`, extended `runtests.jl`, new `test_npe.jl`
scaffold, new `spike/npe/resimulate.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-VALIDATION.md
@.planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md

<interfaces>
<!-- Phase-3 loader/cache contracts the re-sim helper and tests consume. Extracted from spike/. Use directly. -->

spike/data/loader.jl (module Loader, re-exported):
  load_holdout(dir) -> NamedTuple (theta=7×K, summary_min=128×K, summary_aug=142×K, global_index)
  load_fold(dir, fold; K=5, master_seed, variant=:min|:aug) -> (Ztr, θtr, Zva, θva, zt)  # all Float32

spike/data/seeding.jl:
  holdout_rng(master_seed, idx) = Philox4x(...)   # disjoint XOR-salted stream, AbstractRNG
  sample_rng(master_seed, idx)  = Philox4x(...)

spike/simulator + spike/data/encode.jl + spike/contract.jl:
  sample_prior(rng) -> θ (NamedTuple, ρ_true is field 1); sample_imsize(rng)
  simulate_pair(rng, θ; imsize) ; build_mci(...) -> MultiChannelImage
  encode_d01(patch_summary(mci)) -> 128-vector ; encode_aug(...) -> 142-vector

spike/test/runtests.jl resolve-risk gate (existing, L64-74):
  NeuralEstimators UUID = 38f6df31-6b4a-4144-b2af-7ace2da57606  (assert == v"0.2.1")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BenchmarkTools to the spike env, re-freeze Manifest, extend the resolve-risk gate, wire test_npe.jl</name>
  <files>spike/Project.toml, spike/Manifest.toml, spike/test/runtests.jl</files>
  <read_first>
    - spike/test/runtests.jl (the full resolve-risk gate L52-86 — replicate its UUID-keyed pin pattern; Turing must NEVER be added here)
    - spike/Project.toml (existing [deps] UUID list; BenchmarkTools must NOT already be present)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Common Pitfalls" Pitfall 2 (BenchmarkTools add downgrading the v0.2.1 pin)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-PATTERNS.md §"spike/test/runtests.jl (MODIFIED)"
  </read_first>
  <action>
    In the spike env only: `using Pkg; Pkg.activate("spike"); Pkg.add("BenchmarkTools")`, then
    `Pkg.resolve()` and commit the re-frozen `spike/Manifest.toml`. Confirm BenchmarkTools resolved
    to a concrete pinned version. Extend the existing `runtests.jl` resolve-risk testset (the block
    at L65-74 keyed on JLD2/Random123): add `@test haskey(_deps_by_name, "BenchmarkTools")` and
    re-assert `Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"`
    with BenchmarkTools present. Add `include(joinpath(@__DIR__, "test_npe.jl"))` at the bottom of
    `runtests.jl`, mirroring the existing test_data_pipeline.jl include at L86. Do NOT add Turing,
    AdvancedVI, or ForwardDiff to `spike/Project.toml` — those live only in `spike/baseline/` (D-01,
    the Phase-1 co-resolve landmine).
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Pkg; @assert haskey(Pkg.project().dependencies, "BenchmarkTools"); @assert !haskey(Pkg.project().dependencies, "Turing"); @assert Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"; println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/Project.toml` lists `BenchmarkTools` in `[deps]`; `Turing` is absent.
    - `Pkg.dependencies()[UUID 38f6df31-…].version == v"0.2.1"` holds with BenchmarkTools installed.
    - `spike/Manifest.toml` re-frozen and committed with a concrete BenchmarkTools version pin.
    - `runtests.jl` asserts BenchmarkTools present AND re-asserts the v0.2.1 pin, AND includes `test_npe.jl`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the keyed holdout re-sim helper and the test_npe.jl scaffold with the holdout-reproducibility gate</name>
  <files>spike/npe/resimulate.jl, spike/test/test_npe.jl</files>
  <read_first>
    - spike/data/generate.jl (the keyed re-sim loop ~L177-187 and the guarded-include header ~L42-53 — copy the Philox4x-per-global_index pattern verbatim)
    - spike/data/seeding.jl (holdout_rng / sample_rng signatures + HOLDOUT_SALT)
    - spike/data/encode.jl (encode_d01 / encode_aug — the summary the re-sim must reproduce)
    - spike/contract.jl (the read-only `include(joinpath(@__DIR__, "..", "src", ...))` boundary + build_mci / patch_summary)
    - spike/test/test_data_pipeline.jl (L1-91 — copy the AGPL header, `using Test`, guarded includes, const-fixture-before-gate discipline, and the named-`@testset`/`@test_skip` placeholder pattern verbatim)
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-VALIDATION.md §"Pre-registered constants" and §"Per-Task Verification Map"
    - .planning/phases/04-npe-training-advi-benchmark-ablation/04-RESEARCH.md §"Open Questions" Q1 and §"Common Pitfalls" Pitfall 4
  </read_first>
  <action>
    Create `spike/npe/resimulate.jl` (GNU-AGPL header required) exposing
    `resimulate_holdout(dir, j; master_seed) -> (mci_sample, theta, global_index)` (and a paired form
    for D-04) that, for holdout entry `j`, rebuilds the exact `holdout_rng(master_seed, idx)` stream
    and replays `sample_prior` → `sample_imsize` → `simulate_pair` → `build_mci` from the UNCHANGED
    spike simulator (guarded `isdefined`/`include` of seeding.jl, the simulator, encode.jl, contract.jl).
    This single helper is the shared re-sim path for both `spike/baseline/run_advi.jl` (04-04) and
    `spike/npe/benchmark.jl` (04-05) so the two never diverge.

    Create `spike/test/test_npe.jl` (GNU-AGPL header; `using Test, Random, Statistics, BenchmarkTools`;
    guarded includes of loader.jl + resimulate.jl). Declare the six pre-registered constants verbatim:
    `NPE_RMSE_TOLERANCE=1.2`, `SPEEDUP_GATE=100.0`, `ABL_REL_MARGIN=0.05`, `ABL_FOLD_CONSISTENCY=4`,
    `BENCH_THREADS=1`, `NPE_MASTER_SEED=0xC0FFEE`. Wrap one outer `@testset "Phase 4 — NPE Training + ADVI
    Benchmark + Ablation" verbose=true` containing five named child testsets `SC1 (NPE-01)`, `SC2 (NPE-02)`,
    `SC3 (NPE-03)`, `SC4 (ABL-01)`, `SC5 (ABL-02)`, each holding a single `@test_skip true` tagged with the
    wave/plan that will fill it (mirror test_data_pipeline.jl L70-91). Add ONE real, non-skipped testset
    `Wave-0 holdout raw-image reproducibility`: build a tiny cache via the Phase-3 `generate_cache(mktempdir();
    …, n_holdout=20)` fixture helper, then for ≥2 holdout entries assert `encode_d01(patch_summary(
    resimulate_holdout(dir, j; master_seed).mci_sample)) == load_holdout(dir).summary_min[:, j]` exactly. If
    the round-trip is not bit-identical, the testset must FAIL (do not loosen) — that failure is the signal to
    persist raw holdout images instead (Open Question 1).
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_npe.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/npe/resimulate.jl` exists with the AGPL header and exports `resimulate_holdout`; uses `holdout_rng` (not `sample_rng`) for the reserved set.
    - `spike/test/test_npe.jl` declares all six constants with the exact values above, before any gate.
    - Five `@test_skip` SC testsets named SC1..SC5 are present and the harness enumerates them.
    - The `Wave-0 holdout raw-image reproducibility` testset PASSES with bit-exact `==` (not approximate) against the cached `summary_min`.
    - `julia --project=spike spike/test/test_npe.jl` exits green (placeholders skipped, repro gate passed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Security enforcement is not enabled (offline, CPU-only, simulation-only research spike — RESEARCH §Security Domain). The only relevant boundary is supply-chain / reproducibility integrity.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `Pkg.add("BenchmarkTools")` | mitigate | BenchmarkTools is a JuliaCI flagship, [Approved] in RESEARCH §Package Legitimacy Audit (no [ASSUMED]/[SUS]); resolve-risk gate re-asserts the v0.2.1 pin after the add |
| T-04-REPRO | Integrity | holdout re-simulation | mitigate | Bit-exact round-trip assertion; failure forces persisting raw images rather than trusting an unreproducible stream |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` runs green end-to-end (Phase 1-3 gates + the new BenchmarkTools assertion + the Phase-4 scaffold with the passing repro gate).
- `spike/Manifest.toml` diff shows BenchmarkTools added and NeuralEstimators still v0.2.1.
</verification>

<success_criteria>
- Spike env carries BenchmarkTools with the pin intact; constants pre-registered; holdout reproducibility proven; SC1..SC5 enumerated as skipped placeholders feeding later waves.
</success_criteria>

<output>
Create `.planning/phases/04-npe-training-advi-benchmark-ablation/04-01-SUMMARY.md` when done
</output>

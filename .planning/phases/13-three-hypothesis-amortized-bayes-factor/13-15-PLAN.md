---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 15
type: execute
wave: 3
depends_on: ["13-01", "13-04"]
files_modified:
  - spike/p13/real_images.jl
  - spike/test/test_p13_real.jl
autonomous: true
requirements: [D-15, D-16, D-01, D-04]

must_haves:
  truths:
    - "(D-15) The six committed microscopy TIFFs under test/test_images/ load into the amortized summary path through the frozen MultiChannelImage(name, paths, channels) constructor, with no new loader, no channel-sum, no max-projection and no hand-rolled RGB-to-luminance reduction"
    - "(D-15) Path resolution works from spike/p13/ without mutating the process CWD, using the P13_REPO_ROOT idiom the spike already proves at spike/simulator/calibration.jl:59"
    - "(D-15, AMENDED 2026-07-29 by 13-D15-AMENDMENT.md) The ingestion reproduces the frozen ghat calibration anchor FOR THE PAIR IT IS CALLED ON, so the real substrate is a lineage-carrying reference point in this project's own simulator calibration rather than an arbitrary set of TIFFs. AS EXECUTED this was verified on the c1/c2 pair against m-bar = +0.3292 / +0.2481. THOSE ANCHORS ARE SUPERSEDED AS A COLOCALIZATION REFERENCE: c1 is the DAPI nuclear counterstain (test/runtests.jl:105) and spike/simulator/ghat.jl:58-68 marks the c1/c2 figures SUPERSEDED by the unmasked c2/c3 anchors +0.4603 / +0.3815. The c1/c2 regression REMAINS TRUE and REMAINS ASSERTED as a read-chain identity check on a superseded pair; the colocalization anchor is the c2/c3 one, re-measured by plan 13-17. Reproducing +0.3292 / +0.2481 is NOT a pass condition for any colocalization claim"
    - "(D-15) The 1028x1376 frames feed the 8x8 grid unmodified: patch() truncates the bottom 4 rows deterministically, every patch holds 22016 pixels, zero patches go missing, and NO crop, resize or pad step exists anywhere in the arm"
    - "(D-16) The alpha ladder runs on the real pairs through the SAME alpha_segregate code path as the simulated arm, and every corrected invariant holds on real input: bitwise identity at alpha = 0, no NEW zeros, intensity conserved, mask alpha-invariant, m-bar strictly monotone decreasing"
    - "(D-15) test/test_images/ is READ-ONLY INPUT: no Phase-13 artifact writes, moves or modifies anything under test/, and a git-tracked digest proves it"
    - "(D-15) No Phase-13 executable file references corpus/ outside an explanatory header and open_sealed_holdout is never called, asserted by a source-level grep testset"
    - "(D-15) The real arm carries no pass/fail threshold: P13_REAL_IS_GATED is false and the suite asserts it"
    - "(D-01) The arm adds no dependency and edits no src/ byte; the TIFF read path uses only Images 0.26.2, already resolved in the spike environment"
  artifacts:
    - path: "spike/p13/real_images.jl"
      provides: "P13_REPO_ROOT, real_tif, load_real, real_pair, real_mbar, real_alpha_ladder, real_provenance, verify_real_readonly_digest"
      contains: "P13_REPO_ROOT"
      min_lines: 150
    - path: "spike/test/test_p13_real.jl"
      provides: "The five D-15 testsets — real ingestion, real alpha ladder, anti-snooping source grep, read-only discipline, not-a-gate — plus the A14 transposition falsifier"
      contains: "P13_REAL_IS_GATED"
  key_links:
    - from: "spike/p13/real_images.jl"
      to: "test/test_images/{positive,negative}/*.tif"
      via: "P13_REPO_ROOT-anchored joinpath, read-only, through the frozen MultiChannelImage constructor"
      pattern: "test_images"
    - from: "spike/p13/real_images.jl"
      to: "spike/p13/alpha_series.jl"
      via: "the ONE shared alpha_segregate code path typed on AbstractMatrix{Float64}"
      pattern: "alpha_segregate|alpha_ladder"
    - from: "spike/test/test_p13_real.jl"
      to: "spike/simulator/ghat.jl"
      via: "regression of the measured m-bar against the frozen P13_REAL_ANCHOR_MBAR anchors"
      pattern: "P13_REAL_ANCHOR_MBAR"
---

<amendment_notice>
**PARTIALLY SUPERSEDED 2026-07-29 by `13-D15-AMENDMENT.md`. THIS PLAN IS NOT RE-EXECUTED.**

What is superseded is this plan's recorded LINEAGE NUMBERS, not its deliverable. Every ingestion
entry point in `spike/p13/real_images.jl` takes the channel pair as a KEYWORD
(`load_real` / `real_pair` / `real_mbar` / `real_alpha_ladder` / `real_provenance`, all
`; channels = P13_REAL_CHANNEL_PAIR`), and `real_tif(cond, i)` builds both its path and its channel
name from that index, so the ingestion is **pair-agnostic by construction** and needs no
re-authoring. Verified at `d7195cc`.

- The measured anchors `+0.3292 / +0.2481` were taken on the **c1/c2** pair. `c1` is the
  DAPI/Hoechst nuclear counterstain (`test/runtests.jl:105`), so that pair measures
  counterstain-versus-protein overlap and **is not a colocalization anchor**.
- The corrected, amended colocalization anchor is the **unmasked c2/c3** pair,
  `+0.4603` (positive) / `+0.3815` (negative), frozen at `spike/simulator/ghat.jl:58-68` since
  2026-07-25 (`78dc37f`) — i.e. before this plan was written.
- The c1/c2 regression at `spike/test/test_p13_real.jl:144-146` **stays** and **stays passing**: it
  is a read-chain identity check proving this ingestion is the same load path the frozen simulator
  calibration used. It is now explicitly a check on a **superseded** pair and may never be quoted
  as a colocalization measurement.
- The amended anchors are re-measured through this same code path by the 13-16 re-run in plan
  **13-17**. No 13-15 re-run is required or planned.

Sites are annotated below rather than rewritten, so the executed record stays byte-auditable.
</amendment_notice>

<objective>
Build the Phase-11-independent half of the amended D-15 real-image arm: ingestion, path resolution,
the real-substrate alpha ladder, read-only discipline, and the anti-snooping source assertions.

Purpose: D-15 was amended because the corpus physical anchor turned out to be unusable — both
`physical-primary` rows are `split = sealed_holdout`, `sha256 = "PENDING-FETCH"`, `bytes = 0`,
reserved for Phase 16's blind evaluation, and consuming one here would irreversibly burn Phase 16 on
the very hypothesis Phase 16 exists to evaluate. The sanctioned substitute is the six committed
microscopy TIFFs the project already uses to calibrate its own simulator. Everything in this plan
runs on the shipped repo as it stands today; only the final three-way read (plan 13-16) blocks on
Phase 11, so scheduling the whole arm behind the D-02 block would be a sequencing error.

Output: `spike/p13/real_images.jl` and `spike/test/test_p13_real.jl`. There is no reported run here
and there is no gate: this plan builds and proves the ingestion and the ladder. Plan 13-16 reads them
through the trained net.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md

**Binding rulings this plan implements (do not reopen):**
- **The seal stays sealed.** `open_sealed_holdout` is never called — not in a script, not in a test,
  not behind a flag. No Phase-13 executable file references `corpus/` outside an explanatory header.
  `corpus/manifest.csv` may be cited in prose for provenance only. There is no
  `checkpoint:human-verify` seal-break escape hatch, and none may be re-introduced.
- **`test/test_images/` is READ-ONLY INPUT.** Nothing under `test/` is written, moved or modified by
  any Phase-13 artifact, and no `test/` path appears in any Phase-13 `files_modified` list.
- **No crop, no resize, no pad, no `num_patches` change.** `patch()` truncates
  (`src/colocalization.jl:37-43`): at G = 8 the bottom 4 rows of 1028 are dropped, 1376 divides
  exactly, every patch holds 22 016 px and zero patches go `missing`. Adding a resize would be a new
  unregistered preprocessing choice and would diverge from the frozen `_real_anchor()` path whose
  numbers `spike/simulator/ghat.jl:39` records.
- **No masking and no background subtraction.** The amortized path applies neither. `_apply_mask!`
  zeroes sub-threshold pixels and `_exclude_zero` then DELETES them, which would change the very
  statistic the net was trained on. Otsu is used here ONLY to define the D-16 object mask, never to
  modify pixels that feed `patch_summary`.
- **No new loader.** `MultiChannelImage(name, paths, channels)` -> `load_tiff`. A channel-sum, a
  max-projection or a hand-rolled RGB-to-luminance reduction would silently move off the frozen
  anchor.
- **`all(y' .> 0)` is FORBIDDEN as an invariant on real data.** It is false at alpha = 0, where the
  output IS the input: `positive_c2` carries 2 exact-zero pixels and `negative_c2` carries 3, of
  1 414 528, in the source files. The correct assertion is `count(iszero, y') == count(iszero, y)`.
- **The arm is QUALITATIVE and never a gate.** The six TIFFs carry no colocalization ground-truth
  label. No real-image quantity gets a pass/fail threshold.

<interfaces>
<!-- Extracted from the codebase and verified by execution. Use directly; do not go exploring. -->

From `src/LoadImages.jl:432-455` [READ-ONLY REF] — the frozen convenience constructor. **Argument
order is `(name, paths, channels)`, NOT `(paths, channels, name)`.** An empty `channels` triggers a
`@warn`, so channel names are always passed:
```julia
function MultiChannelImage(name::S, path::Vector{S}, channels::Vector{S} = []) where {S <: AbstractString}
    data = load_tiff.(path)                      # one Matrix{Float64} per file
    pixel_size = size(data[1])                   # (1028, 1376) — PIXEL DIMENSIONS, not micrometres
    otsu_threshold = Images.otsu_threshold.(data)
```

From `src/LoadImages.jl:214-217` [READ-ONLY REF] — the whole loader, one line:
```julia
load_tiff(path) = Float64.(Images.Gray.(Images.load(path)))
```
On-disk element type is `RGB{N0f16}`; `Images.Gray` applies Rec-601 luma; the result is
`Matrix{Float64}` in `[0, 1]`, neither z-scored nor background-subtracted. `positive_c1` measures
min 6.1036e-5, max 0.19794, mean 0.014986 — the images are dim, nothing is near 1.0.

From `spike/simulator/calibration.jl:59` — the established repo-root idiom, already proven from a
`spike/<subdir>/` depth by `_real_anchor()` at `:156-164`. **Do not copy `cd()` from
`test/runtests.jl:21`** — mutating process CWD inside a run is a side effect the harness does not
expect:
```julia
const _REPO_ROOT = normpath(joinpath(@__DIR__, "..", ".."))
```
From `spike/p13/`, the depth is IDENTICAL (`spike/p13/x.jl` -> `spike/` -> repo root):
```julia
const P13_REPO_ROOT = normpath(joinpath(@__DIR__, "..", ".."))
real_tif(cond, i) = joinpath(P13_REPO_ROOT, "test", "test_images", cond, "$(cond)_c$(i).tif")
load_real(cond) = MultiChannelImage(cond, [real_tif(cond, 1), real_tif(cond, 2)], ["ch1", "ch2"])
```

From `spike/simulator/ghat.jl:39` [READ-ONLY REF] — the frozen lineage anchor this ingestion must
reproduce:
```
#   real anchor (D-16): faithful LoadImages.jl load_tiff (NOT luminance): positive mu = 0.3292, negative mu = 0.2481
```

> **SUPERSEDED AS A COLOCALIZATION ANCHOR (`13-D15-AMENDMENT.md`).** That line was corrected at
> `ghat.jl:58-68` on 2026-07-25 (`78dc37f`), *before this plan was written*: the c1/c2 figures are
> marked **SUPERSEDED** because `c1` is the DAPI nuclear counterstain, and the colocalization pair
> is **c2/c3** with unmasked `positive mu = 0.4603`, `negative mu = 0.3815`. The quoted line remains
> the correct target for the **read-chain identity** check this plan performs; it is not the
> colocalization anchor.

MEASURED grid feasibility at 1028 x 1376 [VERIFIED, 13-RESEARCH §J4.4]:

| G | patch size | rows dropped | px / patch | missing patches |
|---|---|---|---|---|
| 4 | 257 x 344 | 0 | 88 408 | 0 |
| **8 (shipped)** | **128 x 172** | **4 (0.39%)** | **22 016** | **0** |
| 16 | 64 x 86 | 4 | 5 504 | 0 |

MEASURED mask behaviour on the real ch1 [VERIFIED, 13-RESEARCH §J5.1]:

| Fixture | ch1 Otsu threshold | mask fraction | complement | boost at alpha = 1 |
|---|---|---|---|---|
| positive | 0.028661 | 13.49% | 86.5% | 1.509 |
| negative | 0.035710 | 22.92% | 77.1% | 1.598 |

MEASURED real alpha ladders [VERIFIED, 13-RESEARCH §J5.3] — this is the reference the tests regress
against, not a design proposal. `b = quantile(vec(y), 0.05)`, `M = x .> otsu_threshold(x)`, ch1 x ch2,
G = 8:

> **MEASURED ON THE c1/c2 PAIR AND THEREFORE SUPERSEDED AS A COLOCALIZATION LADDER
> (`13-D15-AMENDMENT.md` §5.4).** Note also that the ladder's object mask is the Otsu mask of the
> FIRST channel of the pair, so on the amended c2/c3 pair the mask is c2's, not c1's: the mask
> fractions and every m-bar below will differ, and `P13_ALPHA_MASK_FRACTION_BOUNDS` does NOT move to
> accommodate them.

| alpha | positive m-bar | negative m-bar |
|---|---|---|
| 0.00 | +0.3292 | +0.2481 |
| 0.25 | +0.2123 | +0.1141 |
| 0.50 | +0.0869 | -0.0187 |
| 0.75 | -0.0385 | -0.1391 |
| 1.00 | -0.1679 | -0.2520 |

Sign crossing at alpha ~ 0.49 (positive) and ~ 0.46 (negative); zero `missing` patches at every
alpha on both fixtures; intensity preserved at `rtol = 1e-10` at every alpha; bitwise identity at
alpha = 0 on both fixtures.

From `spike/p13/alpha_series.jl` (plan 13-04): `alpha_background_floor`, `ch1_object_mask`,
`alpha_segregate`, `alpha_ladder`, `alpha_ladder_summaries`, `verify_alpha_invariants`. The transform
is typed on `AbstractMatrix{Float64}` and knows nothing about where the matrix came from — that is
what makes ONE code path serve both substrates.

From `spike/p13/consts.jl` (plan 13-01): `P13_REAL_CONDITIONS`, `P13_REAL_SAMPLE`,
`P13_REAL_CONTROL`, `P13_REAL_CHANNEL_PAIR`, `P13_REAL_REDUNDANCY_PAIR`, `P13_REAL_IMSIZE`,
`P13_REAL_GRID_TRUNCATION_ROWS`, `P13_REAL_ANCHOR_MBAR`, `P13_REAL_ANCHOR_TOL`,
`P13_REAL_ALPHA_GRID`, `P13_REAL_IS_GATED`, `P13_REAL_READ_ONLY`, `P13_REAL_QUALITATIVE_ONLY`,
`P13_REAL_NAMING_CORRECTION`, `P13_REAL_SUBSTITUTION_RECORD`, `P13_ALPHA_SUBSTRATE`,
`P13_ALPHA_MASK_FRACTION_BOUNDS`, `P13_ALPHA_ZERO_INVARIANT`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the real-image ingestion and real-substrate alpha-ladder surface</name>
  <files>spike/p13/real_images.jl</files>
  <read_first>
    - `13-RESEARCH.md` §J4.0 lines 1328-1354 — the six files, their provenance, and the finding that
      they are already a frozen reference point in this project's own simulator calibration.
    - `13-RESEARCH.md` §J4.1 lines 1355-1399 — the exact constructor, the argument order, the
      measured pixel type and value range, and the `spike/NOTES.md:239` luminance clarification.
    - `13-RESEARCH.md` §J4.2 lines 1400-1427 — the CWD idiom, and the instruction NOT to copy `cd()`.
    - `13-RESEARCH.md` §J4.3 lines 1428-1461 — the verified call chain proving the amortized path
      applies NO masking and NO background subtraction, and why masking would change the statistic.
    - `13-RESEARCH.md` §J4.4 lines 1462-1507 — the truncation math, the survivor counts, and the
      `IMSIZE_SET` transposition footnote.
    - `13-RESEARCH.md` §J5.1 lines 1583-1609 — the available primitives, the "use `_calculate_mask`"
      ruling, and the `ImageSegmentation`/`ImageMorphology` must-not-be-added constraint.
    - `13-RESEARCH.md` §J5.3 lines 1635-1697 — the MEASURED ladders, the five findings, and the
      named failure-mode table with its mitigations.
    - `13-RESEARCH.md` §J5.4 lines 1698-1729 — one code path, the `Vector{Matrix{Float64}}` interface
      boundary, and the three substrate-dependent knobs that belong in a provenance struct.
    - `13-RESEARCH.md` §J6.1 lines 1734-1751 — the no-ground-truth finding and the
      positive/negative naming correction.
    - `13-RESEARCH.md` Pitfalls 2b, 2c, 2d lines 1958-1986 and Pitfalls 11, 12, 13 lines 2051-2083.
    - `13-RESEARCH.md` Code Example 5 lines 2196-2237 — every line of which was executed against the
      committed fixtures this session.
    - `13-RESEARCH.md` Don't Hand-Roll lines 1915-1936 — the four rows naming this file's forbidden
      re-implementations (loader, grid fit, background removal, mask rule).
    - `spike/simulator/calibration.jl` lines 52-70 and 150-170 — `_REPO_ROOT`, the include order, and
      `_real_anchor()`, which already reads these exact files this way.
    - `src/LoadImages.jl` lines 210-270 and 425-460 [READ-ONLY REF] — `load_tiff`,
      `_calculate_mask`, `otsu_thresholds`, `_apply_mask!` (which must NOT be called), and the
      constructor.
    - `spike/contract.jl` lines 30-91 — `build_mci`, `patch_summary`, the read-only include chain and
      the `using StatsBase; using Statistics; using Images` load-order landmine.
    - `spike/p13/alpha_series.jl` — the transform written by plan 13-04.
    - `spike/p13/consts.jl` — the Section-I and Section-I2 constants enumerated in the interfaces
      block.
    - `13-PATTERNS.md` Shared Patterns S1, S2, S3, S7 lines 971-1029 and 1069-1075.
  </read_first>
  <action>
Create `spike/p13/real_images.jl`. AGPL block header, then
`# spike/p13/real_images.jl --- D-15 (AMENDED) real-image ingestion. REPORTED, NOT GATED. QUALITATIVE ONLY.`
followed by an ALL-CAPS banner stating, in this order:

(a) **What this is.** The six committed microscopy TIFFs under `test/test_images/`
(`positive_c{1,2,3}.tif`, `negative_c{1,2,3}.tif`, 1028 x 1376, `RGB{N0f16}` on disk) are the amended
D-15 substrate. They carry **NO colocalization ground-truth label**. Anything read through this file
is a behaviour check, never a correctness check.

(b) **The naming correction.** State it in the banner, not only in the report: the folder names
`positive`/`negative` are the original package's BIOLOGICAL test conditions, not colocalization
labels; the "negative" pair measures mean patch correlation **+0.2481** and is therefore positively
correlated. Nothing in this phase may treat `negative/` as an exclusion example. Reference
`P13_REAL_NAMING_CORRECTION`.

(c) **The target-substitution record**, in the `11-10-PLAN.md:161-195` house shape: D-15 originally
named the corpus physical anchor; both `physical-primary` rows are unfetched, zero-byte and
`split = sealed_holdout` behind the anti-snooping control reserved for Phase 16's blind evaluation,
so consuming one here would irreversibly burn Phase 16 on the very hypothesis Phase 16 exists to
evaluate; `test/test_images/` is the sanctioned substitute. Reference
`P13_REAL_SUBSTITUTION_RECORD`.

(d) **Read-only discipline.** `test/test_images/` is READ-ONLY INPUT. This file opens the TIFFs and
nothing else; it never writes, moves or modifies anything under `test/`.

(e) **The lineage claim.** These are not arbitrary TIFFs — `spike/simulator/calibration.jl:156-164`
`_real_anchor()` already loads `positive_c{1,2}` / `negative_c{1,2}` through the same loader, and
`spike/simulator/ghat.jl:39` records the resulting anchors as `positive mu = 0.3292,
negative mu = 0.2481`. This file reproduces those numbers.

(f) The DECOUPLING line and the literal include line.

Then the machine-checked seal assertion, copied from `11-10-PLAN.md:191-195`: read this file's own
text with `read(@__FILE__, String)`, strip the explanatory header (everything up to a clearly named
sentinel comment line), and assert the remainder contains neither the string that names the sealed
corpus directory nor `open_sealed_holdout`. Construct the corpus directory name from character
fragments at the assertion site so the assertion itself does not embed the literal it forbids, and
say so in a comment — this turns "we intended not to" into "the include fails if we did".

Guarded includes in the documented order: `spike/p13/consts.jl` (guard on `:P13_REAL_IS_GATED`),
then `spike/contract.jl` (guard on `:build_mci`), which brings the read-only `src/LoadImages.jl` and
`src/colocalization.jl` chain with `using StatsBase; using Statistics; using Images` in scope, then
`spike/data/encode.jl` (guard on `:encode_d01`), then `spike/p13/alpha_series.jl` (guard on
`:alpha_segregate`). Flat top-level functions, no module wrapper. Add no `using` line for any package
not already resolved in `spike/Manifest.toml`; if the TIFF read path appears to need one, STOP and
record it as a finding in the plan SUMMARY rather than adding a dependency.

Define:

1. `const P13_REPO_ROOT = normpath(joinpath(@__DIR__, "..", ".."))` with the comment that the depth
   from `spike/p13/` is identical to `spike/simulator/`'s, that `cd()` is deliberately NOT used, and
   naming `spike/simulator/calibration.jl:59` as the proven precedent.

2. `real_tif(cond::AbstractString, i::Integer)` returning
   `joinpath(P13_REPO_ROOT, "test", "test_images", cond, "$(cond)_c$(i).tif")`, throwing an
   `ArgumentError` when `cond` is not in `P13_REAL_CONDITIONS` or `i` is not in `1:3`, and throwing
   an `ArgumentError` naming the expected absolute path when `isfile` is false. House style is the
   explicit `ArgumentError` at the boundary (`src/amortized/api.jl:111-120`).

3. `load_real(cond; channels = P13_REAL_CHANNEL_PAIR)` returning
   `MultiChannelImage(cond, [real_tif(cond, i) for i in channels], ["ch$(i)" for i in channels])`.
   Docstring: the argument order is `(name, paths, channels)`; channel names are ALWAYS passed
   because an empty vector emits a `@warn` into the run log; the loader is
   `Float64.(Images.Gray.(Images.load(path)))` and applies Rec-601 luma to the on-disk `RGB{N0f16}`;
   the result is `[0, 1]`-valued, neither z-scored nor background-subtracted; and no masking or
   background subtraction is applied anywhere on the amortized path. Assert `size(data[1]) ==
   P13_REAL_IMSIZE` and `eltype(data[1]) == Float64` with messages naming the measured expectation.

4. `real_pair(cond; channels = P13_REAL_CHANNEL_PAIR)` returning the two-channel
   `Vector{Matrix{Float64}}` extracted from `load_real`. Docstring: this is the interface boundary
   that makes ONE code path serve both substrates — `simulate_pair` and `load_tiff.` both emit
   exactly this type, so `alpha_segregate` never learns where the matrix came from.

5. `real_mbar(cond; channels = P13_REAL_CHANNEL_PAIR, G = 8)` returning the mask-weighted mean of the
   present continuous summary rows of `encode_d01(patch_summary(build_mci(real_pair(cond)), G))`,
   using the SAME statistic `alpha_ladder_summaries` uses. Docstring records the frozen anchor
   expectation `P13_REAL_ANCHOR_MBAR` and cites `spike/simulator/ghat.jl:39`.

6. `real_grid_provenance(; G = 8)` returning a NamedTuple
   `(imsize, patch_size, rows_dropped, cols_dropped, px_per_patch, truncation_note)` derived from
   `P13_REAL_IMSIZE` and `G` by the same integer division `patch()` performs — DERIVED, never
   literal. The `truncation_note` string is the one sentence the report must carry: `patch()`
   truncates to the largest exact multiple, so at G = 8 the bottom 4 rows (0.39%) of each
   1028 x 1376 frame are not used. Docstring adds the transposition footnote: `IMSIZE_SET`
   (`spike/data/seeding.jl:52-58`) contains `(1376, 1028)` at weight 0.03, i.e. the training
   distribution deliberately includes this frame size but with the axes swapped; per-patch pixel
   count and truncation are identical, only the column-major `vec()` ordering corresponds to a
   transposed spatial layout, and the simulator's fields are isotropic with a symmetric
   `SHIFT_PRIOR`, so this is statistically immaterial — but it is a footnote, not a silent
   difference.

7. `real_alpha_ladder(cond; channels = P13_REAL_CHANNEL_PAIR, grid = P13_REAL_ALPHA_GRID)` delegating
   to `alpha_ladder` / `alpha_ladder_summaries` from `spike/p13/alpha_series.jl` on
   `real_pair(cond; channels)`. It adds NO transform logic of its own — its entire job is to supply
   the real substrate and the substrate provenance. Before running the ladder it must assert the
   pre-registered mask-fraction guard `first(P13_ALPHA_MASK_FRACTION_BOUNDS) <= mean(M) <=
   last(P13_ALPHA_MASK_FRACTION_BOUNDS)` with a named error explaining that outside those bounds the
   complement cannot absorb the redistributed mass and the boost blows up, and assert
   `alpha_background_floor(y) > 0` naming `_exclude_zero` as the reason. Return a NamedTuple carrying
   the per-rung `(alpha, mbar, n_missing, max_value, sum_ratio)` rows plus `provenance` from item 8.

8. `real_provenance(cond; channels = P13_REAL_CHANNEL_PAIR)` returning the substrate provenance
   NamedTuple: `(substrate = :real, condition = cond, channels, paths, imsize, otsu_ch1,
   mask_fraction, background_floor, source_zero_count, alpha0_mbar, is_gated =
   P13_REAL_IS_GATED, qualitative_only = P13_REAL_QUALITATIVE_ONLY, read_only = P13_REAL_READ_ONLY)`.
   `source_zero_count` is `count(iszero, y)` on the UNMODIFIED ch2 and must be reported, because it
   is the reference the corrected zero invariant compares against — 2 px on `positive_c2` and 3 px on
   `negative_c2`, of 1 414 528, present before any alpha. Docstring: the three substrate-dependent
   knobs (background floor, frame size, alpha = 0 semantics) live here, in provenance, never in the
   algorithm.

9. `verify_real_readonly_digest()` returning the sha256 hex digest of the output of
   `git ls-files -s test/test_images/`, run from `P13_REPO_ROOT`. Docstring: this is the structural
   read-only proof — the reported runner records it before and after and asserts equality. Use only
   `SHA` (a Julia standard library) and `Base.read` on a `Cmd`; add no package.

Define NO plotting function and NO reported run here — both belong to plan 13-16. Do not fetch, read
or reference any file under the sealed corpus directory. Do not call `_apply_mask!`, `apply_mask!`,
`imresize`, or any crop of the frame.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_real.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike -e 'include("spike/p13/real_images.jl"); println("loaded")'` prints `loaded`.
    - `julia --project=spike -e 'include("spike/p13/real_images.jl"); p = real_pair("positive"); @assert size(p[1]) == (1028, 1376); @assert eltype(p[1]) == Float64; @assert 0.0 <= minimum(p[1]) && maximum(p[1]) <= 1.0; println("ingest ok")'` prints `ingest ok`.
    - `julia --project=spike -e 'include("spike/p13/real_images.jl"); @assert isapprox(real_mbar("positive"), 0.3292; atol = 1e-3); @assert isapprox(real_mbar("negative"), 0.2481; atol = 1e-3); println("anchor ok")'` prints `anchor ok`. **[AS EXECUTED — READ-CHAIN IDENTITY ON A SUPERSEDED PAIR.** It asserts `real_mbar` at its DEFAULT `channels` keyword, i.e. the c1/c2 counterstain pair. It proves the load path, not colocalization, and per `13-D15-AMENDMENT.md` it is **not** a pass condition for any colocalization claim. The amended colocalization anchor — unmasked c2/c3, `0.4603` / `0.3815` — is asserted by plan **13-17**, through this same function with `channels` passed explicitly.**]
    - `julia --project=spike -e 'include("spike/p13/real_images.jl"); g = real_grid_provenance(); @assert g.rows_dropped == 4 && g.cols_dropped == 0 && g.px_per_patch == 22016; println("grid ok")'` prints `grid ok`.
    - `grep -v '^\s*#' spike/p13/real_images.jl | grep -c 'open_sealed_holdout\|_apply_mask!\|apply_mask!\|imresize'` outputs `0`.
    - `grep -v '^\s*#' spike/p13/real_images.jl | grep -c '1028\|1376\|22016\|321'` outputs `0` (every dimension is read from consts or derived, never inlined).
    - `grep -c 'cd(' spike/p13/real_images.jl` outputs `0` (the CWD is never mutated).
    - `git diff --quiet HEAD -- src/ test/ spike/Project.toml spike/Manifest.toml` exits 0.
    - `git status --porcelain test/test_images/` is empty.
  </acceptance_criteria>
  <done>The six committed TIFFs load through the frozen constructor from a `spike/p13/` depth without touching the CWD, reproduce the frozen ghat anchors, feed the 8x8 grid unmodified with the truncation derived rather than assumed, and expose the real substrate to the shared alpha transform behind a provenance record — with the sealed corpus unreachable and `test/` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the five D-15 testsets and the transposition falsifier</name>
  <files>spike/test/test_p13_real.jl</files>
  <read_first>
    - `13-RESEARCH.md` §Validation Architecture Phase Criteria to Test Map lines 2395-2427 — the five
      D-15 rows this file implements verbatim (real ingestion, real alpha ladder, anti-snooping,
      read-only discipline, not-a-gate) plus the A14 falsifier row.
    - `13-RESEARCH.md` §J5.3 lines 1635-1697 — the MEASURED ladders the assertions regress against,
      and finding 4 (the `all(y' .> 0)` invariant is false and must be replaced).
    - `13-RESEARCH.md` §J6.3 lines 1784-1831 — the five draft acceptance criteria, especially the
      strongest form: no pass/fail threshold is defined for any real-image quantity.
    - `13-RESEARCH.md` Assumption A14 line 2273 — the transposition falsifier, explicitly "one line,
      do it in Wave 0".
    - `spike/test/test_bf.jl` lines 26-37, 39-65, 67-89, 91-101 and 121-123 — the fixture-stream
      rule, the guarded include, fixtures-computed-once, the source-grep gate with its
      comment-stripping, the assert-the-pre-registration-is-locked testset, and the CPU-only
      postscript.
    - `13-PATTERNS.md` section `spike/test/test_p13.jl` lines 761-864 — the file skeleton, the
      source-grep gate and the exercise-both-directions principle.
    - `13-VALIDATION.md` lines 52-72 — the per-task verification map.
    - `spike/p13/real_images.jl` and `spike/p13/alpha_series.jl` — the surfaces under test.
    - `spike/p13/consts.jl` — the Section-I2 constants being asserted as literals.
  </read_first>
  <action>
Create `spike/test/test_p13_real.jl`: AGPL header; a banner naming the amended D-15, stating that the
real arm is QUALITATIVE and NOT A GATE, and stating that this file asserts the arm's *mechanics*
(ingestion, invariants, discipline) and never the *correctness* of any verdict, because the fixtures
carry no colocalization label. `using Test`, `using Statistics`, `using SHA`, and a guarded
`isdefined(@__MODULE__, :real_tif) || include(joinpath(@__DIR__, "..", "p13", "real_images.jl"))`.

Compute fixtures ONCE at module level: the two real pairs, their masks, their floors, their source
zero counts, and both ladders. Add the in-file comment that this file consumes NO RNG stream at all —
the alpha transform is a deterministic function of its input, the mask and the floor, so there is no
seed to burn and no fixture stream to protect. That fact is itself worth asserting: a reader will
assume randomness is involved.

One outer `@testset "P13 real-image arm (D-15 AMENDED, D-16)" verbose = true` with these nested
testsets, named exactly as the Test Map names them:

1. `"P13 real image ingestion"` — the six TIFF paths from `real_tif` all satisfy `isfile`;
   `load_real` returns `size == P13_REAL_IMSIZE` and `Matrix{Float64}` per channel; every value lies
   in `[0, 1]`; `patch(m, 8)` yields the derived `(8, 8, 128, 172)` shape;
   `count(ismissing, patch_summary(build_mci(real_pair(c)), 8)) == 0` for both conditions; and
   `real_mbar` reproduces `P13_REAL_ANCHOR_MBAR` within `P13_REAL_ANCHOR_TOL` for both. Add the
   in-test comment that the last assertion is the LINEAGE claim: it proves this ingestion is the same
   read chain the frozen `ghat` calibration used, not a lookalike. Also assert
   `real_grid_provenance().rows_dropped == P13_REAL_GRID_TRUNCATION_ROWS` and that
   `cols_dropped == 0`, and `@test_throws ArgumentError real_tif("sealed", 1)`.

2. `"P13 real alpha ladder"` — on BOTH real pairs, over `P13_REAL_ALPHA_GRID`:
   bitwise `alpha_segregate(x, y, M, 0.0; b) == y` with exact `==`;
   `count(iszero, y2) == source_zero_count` at every rung, with an in-test comment stating in full
   that `all(y2 .> 0)` is deliberately NOT asserted because it is FALSE on this substrate at alpha =
   0 where `y2` IS `y` (2 source zeros in `positive_c2`, 3 in `negative_c2`, of 1 414 528), and that
   writing it would fail a correct algorithm; `isapprox(sum(y2), sum(y); rtol =
   P13_ALPHA_INVARIANT_RTOL)`; `maximum(y2) <= P13_ALPHA_MAX_VALUE_BOUND`; the `missing` count is
   alpha-INVARIANT at 0 (not merely non-increasing — measured 0/64 at every rung on this substrate);
   `ch1_object_mask` of every rung equals the ladder mask; `m-bar` is STRICTLY monotone decreasing;
   and the ladder crosses zero strictly inside the grid, i.e. `first(mbar) > 0 > last(mbar)`. Also
   assert the mask fraction of both fixtures lies inside `P13_ALPHA_MASK_FRACTION_BOUNDS`.

3. `"P13 corpus seal untouched"` — a source-grep testset over EVERY `.jl` file under `spike/p13/`,
   discovered with `readdir`, not enumerated: read each into a string, drop comment lines with the
   `filter(l -> !startswith(strip(l), "#"), split(src, '\n'))` step, and assert the remainder
   contains neither `open_sealed_holdout` nor the sealed corpus directory name. Build the forbidden
   directory string from character fragments at the assertion site so this test file does not itself
   embed the literal, and comment why. Add an explicit assertion that
   `spike/p13/run_p13_realimage.jl`, when it exists, carries its own `read(@__FILE__, String)`
   self-check — implemented as: if the file exists, its text contains `@__FILE__`; if it does not yet
   exist, the assertion is skipped with `@test_skip` and a comment naming plan 13-16. This is the
   `11-10-PLAN.md:191-195` mechanism, applied one phase later so the two phases' honesty claims are
   verifiably consistent.

4. `"P13 real images are read-only"` — `git status --porcelain test/test_images/` produces empty
   output; `verify_real_readonly_digest()` is a stable 64-character lowercase hex string and two
   consecutive calls agree; and no Phase-13 `.jl` file under `spike/p13/` contains a `write(`,
   `open(..., "w")`, `mv(`, `rm(` or `cp(` call whose argument mentions `test_images` (source grep,
   comment-stripped). Add the in-test comment that `test/test_images/` is READ-ONLY INPUT and appears
   in no Phase-13 `files_modified` list.

5. `"P13 real arm is not a gate"` — `P13_REAL_IS_GATED == false`,
   `P13_REAL_QUALITATIVE_ONLY == true`, `P13_REAL_READ_ONLY == true`; the substrate constant is the
   two-valued `P13_ALPHA_SUBSTRATE == (:simulated, :real)`; `P13_REAL_ALPHA_GRID == P13_ALPHA_LADDER`;
   `P13_REAL_LAMBDA_HEADLINE_RULE === :widest_rung`; `P13_REAL_OOD_COMPARISON == true`;
   `P13_REAL_NAMING_CORRECTION` is a non-empty string containing both `biological` and `+0.2481`;
   `P13_REAL_SUBSTITUTION_RECORD` is a non-empty string mentioning Phase 16. Then the strongest form,
   as a source assertion on `spike/p13/consts.jl` (comment-stripped): no constant whose name begins
   with `P13_REAL_` is a pass/fail threshold — assert that no `P13_REAL_*` name contains `FLOOR`,
   `MIN`, `MAX`, `THRESHOLD` or `TOL` other than the two explicitly allowed non-threshold entries
   (`P13_REAL_ANCHOR_TOL`, the ingestion regression tolerance, and `P13_REAL_OOD_SHIPPED_THRESHOLD`,
   which is a RECORDED reference value belonging to the shipped net, not a Phase-13 bar). Comment
   both exemptions by name and by reason, so the exemption list cannot silently grow.

6. `"A14 transposition falsifier"` — run `real_mbar` on the transposed fixture (permutedims of both
   channels) and assert it agrees with the untransposed value within `P13_REAL_ANCHOR_TOL`. Comment:
   `IMSIZE_SET` carries `(1376, 1028)` while `load_tiff` yields `(1028, 1376)`; this one line is the
   cheap falsifier that the axis order is statistically immaterial, and a failure would mean the real
   summaries sit on a permuted basis.

7. `"P13 real arm ran CPU-only (D-10)"` — the CUDA-absence clause from `spike/test/test_bf.jl:121-123`.

Keep the whole file under 60 seconds: each real summary is one `patch_summary` over 1.4 M pixels, so
compute each pair's ladder exactly once at module level and assert against the stored results rather
than recomputing inside testsets.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_real.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_real.jl` exits 0 with zero failures and zero errors.
    - `grep -c 'all(.*\.> 0)' spike/test/test_p13_real.jl` outputs `0` (the forbidden invariant is never written).
    - `grep -c 'count(iszero' spike/test/test_p13_real.jl` outputs at least `1` (the corrected invariant is present).
    - `grep -c 'startswith(strip' spike/test/test_p13_real.jl` outputs at least `1` (comment-stripping before any source grep).
    - The five Test-Map testset names are present: `grep -c 'P13 real image ingestion\|P13 real alpha ladder\|P13 corpus seal untouched\|P13 real images are read-only\|P13 real arm is not a gate' spike/test/test_p13_real.jl` outputs at least `5`.
    - `julia --project=spike -e 'include("spike/test/test_p13_real.jl")'` exits 0 (includable from the harness, not only runnable standalone).
    - Wall clock under 60 seconds: `time julia --project=spike spike/test/test_p13_real.jl`.
    - `git diff --quiet HEAD -- src/ test/ spike/Project.toml spike/Manifest.toml` exits 0 and `git status --porcelain test/test_images/` is empty.
  </acceptance_criteria>
  <done>All five D-15 testsets named in the research Test Map are executable, the corrected zero invariant replaces the forbidden one, the sealed corpus and `open_sealed_holdout` are proven absent from every file under `spike/p13/`, `test/test_images/` is proven unmodified, the not-a-gate sentinel is machine-checked including the no-real-threshold source assertion, and the transposition assumption is falsifiable in one line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sealed corpus holdout -> Phase-13 executable code | The one real segregated anchor sits behind an anti-snooping access control reserved for Phase 16's blind evaluation; `test/test_images/` is the sanctioned substitute |
| committed repo fixtures -> Phase-13 artifacts | `test/test_images/` is read-only input; a write, move or modification under `test/` would corrupt a fixture the root suite also depends on |
| real 1028x1376 frame -> the fixed 8x8 grid | An unvalidated frame size silently truncates, or falls below the 15-survivor patch floor, changing the statistic without an error |
| unlabelled real data -> a reported verdict | The fixtures carry no colocalization label; a threshold placed on any real-image quantity would promote a qualitative check into a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information disclosure (anti-snooping control) | `corpus/` sealed holdout | accept (deferred to Phase 16) | ASVS V4: no code path in this plan reaches the sealed directory; a source-grep testset over every `.jl` under `spike/p13/` asserts `open_sealed_holdout` and the corpus directory name are absent from executable code, and `real_images.jl` self-checks its own text at include time |
| T-13-50 | Tampering | Modification of `test/test_images/` | mitigate | ASVS V4: `test/` appears in no `files_modified` list; `verify_real_readonly_digest()` hashes `git ls-files -s test/test_images/`; a testset asserts `git status --porcelain test/test_images/` is empty and that no `spike/p13/` file writes, moves or removes anything named `test_images` |
| T-13-51 | Tampering (input validation) | 1028x1376 frame into the 8x8 grid | mitigate | ASVS V5: `size == P13_REAL_IMSIZE` and `eltype == Float64` asserted at ingestion with named errors; the truncation is DERIVED by the same integer division `patch()` uses and regressed against `P13_REAL_GRID_TRUNCATION_ROWS`; `count(ismissing, ...) == 0` asserted; no crop, resize, pad or `num_patches` change exists |
| T-13-52 | Tampering (semantic) | Asserting `all(y' .> 0)` on real input | mitigate | The forbidden invariant is grep-asserted absent; the corrected `count(iszero, y') == count(iszero, y)` is asserted at every rung against the recorded source zero count |
| T-13-53 | Tampering (semantic) | Mask fraction outside the workable band, or a boost that pushes values out of range | mitigate | Pre-registered `P13_ALPHA_MASK_FRACTION_BOUNDS` guard fires as a named error before the ladder runs; `maximum(y') <= P13_ALPHA_MAX_VALUE_BOUND` asserted and recorded, never silently clamped |
| T-13-54 | Repudiation | A real-image quantity acquiring a pass/fail threshold | mitigate | `P13_REAL_IS_GATED = false` sentinel asserted; a source assertion on `spike/p13/consts.jl` proves no `P13_REAL_*` constant is a bar, with the two exemptions named and reasoned |
| T-13-55 | Spoofing | Reading `negative/` as an exclusion example | mitigate | `P13_REAL_NAMING_CORRECTION` is asserted to name the biological-condition reading and to quote the measured `+0.2481`; the file banner states it before any code |
| T-13-06 | Tampering | Path construction | mitigate | ASVS V5: every path is `joinpath(P13_REPO_ROOT, ...)` or `joinpath(@__DIR__, ...)`; `cd()` is grep-asserted absent; `real_tif` validates `cond` against `P13_REAL_CONDITIONS` and the channel index against `1:3` before touching the filesystem |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** The TIFF read path uses `Images` 0.26.2, already resolved; `SHA` is a Julia standard library; acceptance criteria assert `spike/Project.toml` and `spike/Manifest.toml` byte-unchanged. If the read path appeared to need a new package, that is a finding to surface, not a dependency to add |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p13_real.jl` exits 0.
2. `julia --project=spike -e 'include("spike/p13/real_images.jl"); println(real_mbar("positive"), " ", real_mbar("negative"))'` prints values within 1e-3 of `0.3292` and `0.2481` — **at the DEFAULT (superseded c1/c2) pair; a read-chain identity check, never a colocalization result. The amended c2/c3 anchors `0.4603` / `0.3815` are verified by plan 13-17 (`13-D15-AMENDMENT.md`).**
3. `grep -v '^\s*#' spike/p13/real_images.jl | grep -c 'open_sealed_holdout\|imresize\|apply_mask!'` outputs `0`.
4. `git status --porcelain test/test_images/` is empty and `git diff --quiet HEAD -- test/` exits 0.
5. `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- `spike/p13/real_images.jl` defines `P13_REPO_ROOT`, `real_tif`, `load_real`, `real_pair`,
  `real_mbar`, `real_grid_provenance`, `real_alpha_ladder`, `real_provenance` and
  `verify_real_readonly_digest`, all flat top-level with contract docstrings citing the amended D-15.
- The ingestion reproduces the frozen `ghat` anchors and the grid truncation is derived, not assumed.
- The real alpha ladder runs through the SAME `alpha_segregate` code path as the simulated arm and
  satisfies every corrected invariant on real input.
- All five D-15 testsets from the research Test Map plus the A14 falsifier are executable and green
  in under 60 seconds.
- `open_sealed_holdout` and the sealed corpus directory are provably absent from every `.jl` file
  under `spike/p13/`.
- No `src/` byte, no `test/` byte and no manifest byte changed; no dependency added.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-15-SUMMARY.md` when done,
including the measured `m-bar` for both fixtures beside the frozen `ghat` anchors, the per-rung real
alpha ladder for both conditions with the source zero counts and mask fractions, the derived grid
truncation, the `git ls-files` digest of `test/test_images/`, and an explicit statement that no
dependency was added and the seal was not opened.
</output>

---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 08
type: execute
wave: 5
depends_on: ["13-01", "13-03", "13-04", "13-05", "13-06", "13-07", "13-15"]
files_modified:
  - spike/p13/result.jl
  - spike/test/test_p13_result.jl
  - spike/test/test_p13_calibration.jl
  - spike/test/runtests.jl
autonomous: true
requirements: [D-08, D-13, D-14, D-01, D-04]

must_haves:
  truths:
    - "(D-08) ThreeHypothesisColocResult is a new AbstractColocResult SUBTYPE defined in spike/, reached by a read-only include of src/results.jl, with zero src/ bytes changed"
    - "(D-08) The misnomer field log_bf_simplex is replaced by log_bf_vs_random, a NamedTuple whose reference class is self-documenting at every call site, and the deferral of the src/ rename is recorded rather than performed"
    - "(D-08) bayes_factor(r) returns the coloc-vs-random entry and DOCUMENTS that choice; delta_rho falls through to the interface error rather than fabricating a value"
    - "(D-13) ECE is the gate statistic and MCE is not: the empty-bin trap is proven by an executable test showing an empty bin contributes |midpoint| to MCE with zero ECE weight"
    - "(D-13) The vacuous-pass guard is structural: any calibration verdict is accompanied by the head's AUC, and a green ECE with AUC at the base rate is labelled vacuous"
    - "(D-14) No TOST or equivalence machinery is introduced; the traffic light is a band, not a p-value"
    - "(D-01/D-04) A single command, julia --project=spike spike/test/runtests.jl, remains the gate, and a Phase-13 resolve-risk clause re-asserts NeuralEstimators 0.2.1 by UUID with no new dependency"
  artifacts:
    - path: "spike/p13/result.jl"
      provides: "ThreeHypothesisColocResult subtype, the four required accessors, log_bf_vs_random, and p13_calibration_meta"
      contains: "log_bf_vs_random"
      min_lines: 110
    - path: "spike/test/test_p13_result.jl"
      provides: "Subtype and accessor-interface tests, the NamedTuple reference-class test, and the delta_rho interface-error test"
      contains: "AbstractColocResult"
    - path: "spike/test/test_p13_calibration.jl"
      provides: "Both-directions _bin_calibration tests, the empty-bin MCE trap demonstration, the per-head restriction test, and the vacuous-pass guard test"
      contains: "_bin_calibration"
    - path: "spike/test/runtests.jl"
      provides: "Phase-13 resolve-risk clause (i) plus the include block for every Phase-13 test file"
      contains: "test_p13"
  key_links:
    - from: "spike/p13/result.jl"
      to: "src/results.jl"
      via: "read-only include to reach AbstractColocResult and the accessor interface"
      pattern: "results\\.jl"
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_p13_consts.jl"
      via: "include block at the bottom of the harness"
      pattern: "test_p13_consts"
---

<objective>
Give the three-way evidence a result type with honest accessor semantics, prove the calibration
gate's statistic behaves as D-13 requires, and wire every Phase-13 test into the single spike gate.

Purpose: `src/results.jl:161-192` sketches this type with a field literally named
`log_bf_simplex` — a name that will be quoted as a simplex by someone who never reads the comment,
because the three values are two free numbers plus a structural zero. D-08 renames it. D-01 says the
executable type lives in `spike/`, so the rename happens where the code is and the `src/` sketch is
recorded as a deferred one-line productionization item, not edited. Separately, D-13's gate rests on
`_bin_calibration`, whose empty-bin behaviour makes MCE unusable as a gate statistic on a
well-separated three-way problem — that has to be demonstrated, not asserted.

Output: `spike/p13/result.jl`, two test files, and the harness wiring.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md

**Binding rulings this plan implements (do not reopen):**
- **`src/results.jl` is NOT edited.** The comment-only sketch block at lines 161-192 stays
  byte-unchanged. `spike/p13/result.jl` defines the executable subtype and the phase report records
  the misnomer as a productionization item. This is 13-RESEARCH Open Question 1's recommendation and
  the plan takes it; the user may override later at zero cost, since the sketch is comments.
- **Gate on ECE, report MCE.** `_bin_calibration` scores an empty bin as `|midpoint - 0.0|` with
  zero ECE weight but full MCE weight. Do NOT hand-modify `_bin_calibration` — it is shared,
  gate-lineage code. Report MCE beside the empty-bin count.
- **Do not restructure `spike/test/runtests.jl`.** Append clause (i) inside the existing resolve-risk
  testset and append the include lines at the bottom, following the file's own convention.
- **No new dependency, and `ImageSegmentation` / `ImageMorphology` must not become direct deps** —
  `Images` 0.26.2 already provides `otsu_threshold` transitively.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

From `src/results.jl:146-159` [READ-ONLY REF] — the struct-plus-four-accessors shape to mirror:
```julia
struct AmortizedColocResult <: AbstractColocResult
    grid            :: Int
    posterior       :: Matrix{Float64}
    delta_rho_draws :: Vector{Float64}
    log_bayes_factor:: Float64
    ood             :: OODVerdict
    calibration     :: CalibrationMeta
    meta            :: NamedTuple
end
delta_rho(r::AmortizedColocResult)       = r.delta_rho_draws
bayes_factor(r::AmortizedColocResult)    = r.log_bayes_factor
is_ood(r::AmortizedColocResult)          = r.ood.flag
posterior_draws(r::AmortizedColocResult) = r.posterior
```

From `src/results.jl:47-49` [READ-ONLY REF] — the interface contract every subtype must satisfy:
```julia
_iface_error(r::AbstractColocResult, f::Symbol) = error(
    "`$(f)` is not implemented for $(typeof(r)). Every `AbstractColocResult` subtype must " *
    "implement the accessor interface: delta_rho, bayes_factor, is_ood, posterior_draws.")
```

From 13-RESEARCH section G1 — the recommended replacement:
```julia
struct ThreeHypothesisColocResult <: AbstractColocResult
    grid             :: Int
    posterior        :: Matrix{Float64}
    log_bf_vs_random :: NamedTuple{(:coloc, :random, :exclusion), Tuple{Float64,Float64,Float64}}
    ood              :: OODVerdict
    calibration      :: CalibrationMeta
    meta             :: NamedTuple
end
```

From `spike/validation/sbc.jl:65-72, 82-84` — the calibration surface D-13 uses:
```julia
struct CalibrationResult
    bin_midpoints  :: Vector{Float64}
    predicted_rate :: Vector{Float64}
    observed_rate  :: Vector{Float64}
    bin_counts     :: Vector{Int}
    ece            :: Float64
    mce            :: Float64
end
_bin_calibration(posterior_probs, empirical_positive; n_bins = 10) -> CalibrationResult
# empty bins: predicted_rate = midpoint, observed_rate = 0.0
# ECE = sum (count_i/total) * |pred_i - obs_i| ; MCE = max_i |pred_i - obs_i|
sbc_traffic_light(ece) = ece <= SBC_ECE_GREEN ? :green : ece <= SBC_ECE_YELLOW ? :yellow : :red
```

From `spike/test/runtests.jl:100-113` — clause (h), the template for clause (i), and `:133-138`,
the bottom-of-file include convention with a one-comment rationale.

From `spike/contract.jl:30-47` — the read-only reach into `src/`; the `using` lines MUST precede
the `include`s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the three-hypothesis result type in the spike lane</name>
  <files>spike/p13/result.jl</files>
  <read_first>
    - `13-RESEARCH.md` section G lines 891-969 — the rename argument, the accessor table with the
      `bayes_factor` documentation requirement and the do-not-fabricate-delta_rho rule, and section
      G3's full statement of the `src/` boundary tension with its recommended resolution.
    - `13-PATTERNS.md` section `spike/p13/result.jl` lines 613-661 — the struct-plus-accessor
      excerpt, the interface-error contract, and the four decision-driven changes.
    - `src/results.jl` lines 19-60, 85-192 [READ-ONLY REF] — the supertype, `_iface_error`,
      `CalibrationMeta` at 117-126 with its eight fields, `AmortizedColocResult`, and the
      comment-only Phase-13 sketch at 161-192. Do not edit any of it.
    - `spike/contract.jl` lines 30-47 — the read-only include idiom and the load-order landmine.
    - `spike/validation/sbc.jl` lines 55-160 — `CalibrationResult`, `_bin_calibration`,
      `sbc_traffic_light`.
    - `spike/p13/consts.jl` — `P13_ECE_GREEN`, `P13_ECE_YELLOW`, `P13_ECE_NBINS`,
      `P13_VACUOUS_AUC_FLOOR`, `P13_RESULTS_RENAME_DEFERRED`.
  </read_first>
  <action>
Create `spike/p13/result.jl`: AGPL block header, then
`# spike/p13/result.jl --- ThreeHypothesisColocResult, the spike-lane three-way result type (D-08).`
followed by a banner stating: (a) why the sketch's `log_bf_simplex` name is a misnomer — the three
values are `log BF(C:R)`, a structural zero, and `log BF(E:R)`, i.e. two free numbers plus a
reference, not a point on a simplex, and a reader who sees "simplex" will assume the entries sum to
one; (b) that the executable type lives here, in `spike/`, per D-01, while the comment-only sketch
at `src/results.jl:161-192` stays byte-unchanged and the rename is carried as a one-line
productionization item recorded in the phase report; (c) that Phase 7 D-02 is still satisfied
because this IS a new subtype of the real supertype; (d) the DECOUPLING line.

Guarded includes in the documented order — `using` lines first, then the read-only reach:
`using StatsBase; using Statistics; using Images` (the documented load order), then
`isdefined(@__MODULE__, :AbstractColocResult) || include(joinpath(@__DIR__, "..", "..", "src", "results.jl"))`,
then `spike/p13/consts.jl` and `spike/validation/sbc.jl` (guard on `:_bin_calibration`).

Define:

1. `struct ThreeHypothesisColocResult <: AbstractColocResult` with the six fields from the
   interfaces block. The `log_bf_vs_random` field type is the explicit
   `NamedTuple{(:coloc, :random, :exclusion), Tuple{Float64,Float64,Float64}}`, with a comment that
   the NamedTuple removes the positional-order hazard the sketch already had — the sketch's own
   comment orders the tuple with the reference class FIRST, which no reader would guess from a name
   containing "simplex". Add an inner constructor (or a validating outer constructor) that asserts
   `log_bf_vs_random.random == 0.0` exactly, with an `ArgumentError` naming D-08.

2. The four required accessors:
   - `posterior_draws(r) = r.posterior`;
   - `is_ood(r) = r.ood.flag`;
   - `bayes_factor(r) = r.log_bf_vs_random.coloc` **with a docstring that states the choice
     explicitly**: the shared single-number accessor returns the coloc-vs-random entry so a caller
     written against the binary result gets the semantically closest number rather than an error;
     a caller wanting the three-way evidence must use `log_bf_vs_random`;
   - `delta_rho(r)` returning `r.delta_rho_draws` **only if** the result actually carries control
     draws (add an optional field or carry them in `meta`), and otherwise calling `_iface_error(r,
     :delta_rho)`. Docstring, explicit: do not fabricate a Delta rho; letting the interface error is
     the honest answer.

3. `log_bf_vs_random(r::ThreeHypothesisColocResult) = r.log_bf_vs_random` as the named accessor, and
   **no** `bayes_factor_simplex` alias — the misnomer does not survive into the spike lane either.

4. `p13_calibration_meta(cal::CalibrationResult; grid::Int, gate::NamedTuple)` building the
   `CalibrationMeta` value the result carries, mapping the spike `CalibrationResult`'s six fields
   plus `grid` and `gate`. Document the field-order contract difference between
   `src/results.jl:117-126` (eight fields) and `spike/validation/sbc.jl:65-72` (six fields), and
   state which is carried. The `gate` NamedTuple must include at least
   `(statistic = :ece, ece_green = P13_ECE_GREEN, ece_yellow = P13_ECE_YELLOW,
   n_bins = P13_ECE_NBINS, mce_reported_not_gated = true, empty_bins = <count>, auc = <head AUC>,
   vacuous = <Bool>)` — so every calibration verdict structurally carries its discrimination number
   and its empty-bin count.

5. `p13_traffic_light(ece; green = P13_ECE_GREEN, yellow = P13_ECE_YELLOW)` re-declared locally
   rather than imported, matching the established pattern that each pre-registration is
   self-contained, with a comment saying so.

6. `vacuous_pass(ece, auc; ece_green = P13_ECE_GREEN, auc_floor = P13_VACUOUS_AUC_FLOOR)` returning
   `true` when the ECE is green but the AUC is at or below the floor. Docstring: a head that learns
   nothing produces well-calibrated-looking probabilities by reproducing the base rate; this is the
   F3 vacuous-pass failure one level up, and every calibration claim in this phase ships with its
   discrimination number.

Do NOT define TOST, equivalence tests or p-value machinery here (D-14).
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_result.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike -e 'include("spike/p13/result.jl"); println(ThreeHypothesisColocResult <: AbstractColocResult)'` prints `true`.
    - `grep -v '^\s*#' spike/p13/result.jl | grep -c 'log_bf_simplex\|bayes_factor_simplex'` outputs `0`.
    - `grep -c 'log_bf_vs_random' spike/p13/result.jl` outputs at least `3`.
    - `julia --project=spike -e 'include("spike/p13/result.jl"); @assert vacuous_pass(0.02, 0.5); @assert !vacuous_pass(0.02, 0.95); println("vacuous ok")'` prints `vacuous ok`.
    - `git diff --quiet HEAD -- src/` exits 0 — including `src/results.jl`, whose sketch block stays byte-unchanged.
    - `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>A new AbstractColocResult subtype exists in the spike lane with a self-documenting reference class, an honest single-number accessor, no fabricated Delta rho, a calibration meta that structurally carries AUC and empty-bin counts, and no trace of the simplex misnomer.</done>
</task>

<task type="auto">
  <name>Task 2: Write the result and calibration test files</name>
  <files>spike/test/test_p13_result.jl, spike/test/test_p13_calibration.jl</files>
  <read_first>
    - `spike/test/test_sbc.jl` lines 36-95 — the both-directions calibration test whose principle
      is copied verbatim (a perfectly calibrated input must be green, a maximally miscalibrated one
      must be red), and lines 98-116 for the pre-registration and CUDA clauses.
    - `13-RESEARCH.md` section I lines 1023-1097 — `_bin_calibration`'s exact semantics, the
      empty-bin trap with its severity note, the traffic-light values, what "implied class
      probabilities" means concretely including the per-head restriction table, and the small-n
      caveat.
    - `13-RESEARCH.md` Pitfall 7 lines 1368-1372 and Pitfall 9 lines 1381-1386.
    - `13-VALIDATION.md` line 65 — the D-13 row this test satisfies.
    - `spike/p13/result.jl` — the file written in Task 1.
    - `spike/validation/sbc.jl` lines 55-160 — the function under test; do not modify it.
  </read_first>
  <action>
Create two files.

**`spike/test/test_p13_result.jl`** — AGPL header, banner naming D-08, `using Test`, guarded
`isdefined(@__MODULE__, :ThreeHypothesisColocResult) || include(joinpath(@__DIR__, "..", "p13", "result.jl"))`.
One outer `@testset "P13 three-hypothesis result type (D-08)" verbose = true`:
1. `"is a new AbstractColocResult subtype (Phase-7 D-02)"` — the subtype assertion.
2. `"the random entry is structurally zero"` — construct a valid result and assert
   `log_bf_vs_random(r).random === 0.0`; `@test_throws ArgumentError` when constructing with a
   non-zero random entry.
3. `"the reference class is self-documenting"` — assert
   `keys(log_bf_vs_random(r)) == (:coloc, :random, :exclusion)` and that indexing by name gives the
   expected values; add the in-test comment that the sketch's positional order put the reference
   class first, which is the hazard this removes.
4. `"bayes_factor returns the coloc entry, documented"` — `bayes_factor(r) == log_bf_vs_random(r).coloc`,
   plus an assertion that the method has a non-empty docstring (via `@doc`), so the choice cannot be
   made silently.
5. `"delta_rho does not fabricate"` — for a result carrying no control draws,
   `@test_throws ErrorException delta_rho(r)` and assert the message mentions the accessor
   interface; for a result that does carry them, assert it returns them.
6. `"no simplex naming survives"` — source grep on `spike/p13/result.jl` (comment lines stripped)
   asserting `log_bf_simplex` and `bayes_factor_simplex` are absent.
7. `"src/results.jl is untouched"` — assert `success(`git diff --quiet HEAD -- src/results.jl`)`,
   with an in-test comment naming D-01 and the deferred rename. If invoking git from the suite is
   undesirable in a sandbox, guard the testset with `Sys.which("git") !== nothing`.
8. The CUDA-absence clause.

**`spike/test/test_p13_calibration.jl`** — AGPL header, banner naming D-13/D-14 and Pitfall 7,
`using Test`, guarded includes of `spike/p13/result.jl` and `spike/validation/sbc.jl`.
One outer `@testset "P13 evidence calibration (D-13, D-14)" verbose = true`:
1. `"both directions"` — copy the `test_sbc.jl:77-88` principle verbatim against the Phase-13 band:
   `_bin_calibration(fill(0.5, 100), vcat(trues(50), falses(50)); n_bins = P13_ECE_NBINS)` has
   `ece < P13_ECE_GREEN` and `p13_traffic_light` returns `:green`;
   `_bin_calibration(fill(1.0, 100), falses(100); n_bins = P13_ECE_NBINS)` has
   `ece > P13_ECE_YELLOW` and returns `:red`.
2. `"THE EMPTY-BIN TRAP: MCE is not a usable gate statistic"` — construct an input whose
   probabilities occupy only one or two bins (e.g. all near 0.05 with matching labels), so most bins
   are empty; assert the result's `ece` is green while its `mce` is large, and assert
   `count(iszero, cal.bin_counts) > 0`. Add a long in-test comment: an empty bin is scored as
   `|midpoint - 0.0|`, carrying zero ECE weight but full MCE weight, so on a well-separated
   three-way problem MCE would fail the gate for a reason unrelated to calibration. This testset IS
   the documentation of why `P13_GATE_STATISTIC === :ece`.
3. `"the gate statistic is ECE by pre-registration"` — assert `P13_GATE_STATISTIC === :ece` and
   `P13_ECE_NBINS == 10` as literals.
4. `"per-head restriction"` — build a synthetic three-class label vector and two probability
   vectors; assert that the coloc curve is computed only on samples whose label is coloc or random
   and the exclusion curve only on exclusion or random; assert that including the third class
   changes the ECE, so the restriction is provably load-bearing rather than cosmetic.
5. `"vacuous-pass guard (Pitfall 9)"` — a head that reproduces the base rate has a green ECE and an
   AUC near 0.5; assert `vacuous_pass` flags it and does not flag a discriminating head. Assert that
   the `gate` NamedTuple built by `p13_calibration_meta` carries both `auc` and `empty_bins`.
6. `"no equivalence machinery (D-14)"` — source grep across `spike/p13/*.jl` (comment lines
   stripped) asserting `tost`, `TOST` and `equivalence` do not appear, with the comment that the
   traffic light is a band and not a p-value, so there is no over-power failure mode.
7. `"minimum evaluation count is pre-registered"` — assert `P13_MIN_EVAL_PER_HEAD == 1000` with the
   comment that ECE is biased downward at small n, so a green ECE on a small sample is not evidence.
8. The CUDA-absence clause.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_calibration.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_result.jl` exits 0 with zero failures.
    - `julia --project=spike spike/test/test_p13_calibration.jl` exits 0 with zero failures.
    - `grep -c 'EMPTY-BIN TRAP' spike/test/test_p13_calibration.jl` outputs at least `1`.
    - `grep -c 'iszero' spike/test/test_p13_calibration.jl` outputs at least `1` (the empty-bin count is asserted, not assumed).
    - `git diff --quiet HEAD -- spike/validation/sbc.jl` exits 0 (the shared gate-lineage function is not hand-modified).
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>The result type's honest accessor semantics are asserted, the empty-bin MCE trap is demonstrated rather than described, the per-head restriction is proven load-bearing, and the vacuous-pass guard is executable.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 13 into the single spike gate</name>
  <files>spike/test/runtests.jl</files>
  <read_first>
    - `spike/test/runtests.jl` — whole file, especially the resolve-risk testset at lines 59-113
      (clause (h) is the closest template because it is the "this phase adds NO new package" clause)
      and the bottom-of-file include block at lines 118-143 with its one-comment-per-phase rationale.
    - `13-PATTERNS.md` section `spike/test/runtests.jl` lines 866-907 — the instruction to copy
      clause (h) wholesale as clause (i), the exact NeuralEstimators UUID pin to re-assert, the
      `ImageSegmentation` / `ImageMorphology` note, and the append-only rule.
    - `13-RESEARCH.md` Package Legitimacy Audit lines 317-336 — why the correct integrity control
      for this phase is the resolve-risk gate rather than a package audit.
    - `13-VALIDATION.md` lines 27-32 and 66 — the wiring convention and the decoupling row.
    - The Phase-13 test files that must be included: `test_p13_consts.jl`, `test_p13_labels.jl`,
      `test_p13_alpha.jl`, `test_p13_tau.jl`, `test_p13_net.jl`, `test_p13_correction.jl`,
      `test_p13_result.jl`, `test_p13_calibration.jl`, `test_p13_real.jl` (the D-15 real-image
      arm from plan 13-15 — Phase-11-independent, already green since wave 3).
  </read_first>
  <action>
Modify `spike/test/runtests.jl` by **appending only**. Do not restructure the file, do not reorder
existing includes, and do not touch clauses (a) through (h).

1. Inside the existing resolve-risk testset, immediately after clause (h), add **clause (i)** by
   copying clause (h)'s block wholesale and changing only the comment and one added assertion. The
   comment must say: Phase 13's three-way evidence net adds NO new package — the two-head trunk is
   Flux, the AUC is the hand-rolled `roc_auc`, the calibration is `_bin_calibration`, and the Otsu
   mask comes from `Images` 0.26.2 transitively, so `ImageSegmentation` and `ImageMorphology` must
   NOT become direct dependencies; `ROCAnalysis`, `MLJ`, `NormalizingFlows`, `InvertibleNetworks`
   and `Turing` stay absent; then the `NeuralEstimators` v0.2.1 pin is re-asserted by UUID
   `38f6df31-6b4a-4144-b2af-7ace2da57606` with the Phase-13 code loadable. Add
   `@test !haskey(Pkg.project().dependencies, "ImageSegmentation")` and the same for
   `ImageMorphology`, plus `@test !haskey(Pkg.project().dependencies, "QuadGK")` and
   `@test !haskey(Pkg.project().dependencies, "KernelDensity")` with the comment that the retired
   KDE baseline must stay unreachable from the spike environment (named limit 3).

2. At the bottom of the file, append a Phase-13 comment block in the established voice — the
   three-way evidence-net unit testsets run in the same harness so a single
   `julia --project=spike spike/test/runtests.jl` stays the gate — followed by
   `include(joinpath(@__DIR__, "test_p13_consts.jl"))` and the eight sibling includes, in the
   dependency order consts, labels, alpha, real, tau, net, correction, result, calibration. `real`
   sits directly after `alpha` because `test_p13_real.jl` includes `real_images.jl`, which includes
   `alpha_series.jl`; the guarded-include idiom makes the ordering an optimisation rather than a
   requirement, but keeping it explicit documents the dependency.

3. Run the full suite once and confirm it is green end to end, including every pre-existing phase's
   testsets. If any pre-existing testset breaks, the cause is a name collision between a Phase-13
   top-level constant or function and an earlier phase's — resolve it by renaming the **Phase-13**
   symbol (the spike lane is flat and unmoduled), never by editing an earlier phase's file, and
   record the rename in the plan SUMMARY.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/runtests.jl` exits 0 with zero failures and zero errors across ALL phases' testsets.
    - `grep -c 'test_p13' spike/test/runtests.jl` outputs `9` (the eight original Phase-13 test files plus `test_p13_real.jl`).
    - `grep -c '38f6df31-6b4a-4144-b2af-7ace2da57606' spike/test/runtests.jl` outputs at least `3` (clauses g, h and the new i).
    - `grep -c 'ImageSegmentation' spike/test/runtests.jl` outputs at least `1`.
    - `git diff -U0 -- spike/test/runtests.jl` shows only additions (no deleted lines): `git diff --numstat -- spike/test/runtests.jl` reports 0 deletions.
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (the root suite stays green, proving `src/` is untouched).
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>One command remains the spike gate, every Phase-13 unit testset including the D-15 real-image arm runs inside it, the resolve-risk gate re-asserts the NeuralEstimators pin and the absence of every forbidden package including the retired KDE stack, and the root suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike lane -> src/ package lane | `spike/p13/result.jl` reaches `src/results.jl` to subtype the real supertype; the reach must be read-only or the decoupling constraint is broken |
| spike environment -> dependency graph | The NeuralEstimators 0.2.1 pin is fragile under co-resolution; any new direct dependency can silently downgrade it and invalidate every trained artifact |
| calibration statistic -> gate verdict | Choosing MCE would fail the gate for a reason unrelated to calibration, and choosing ECE without the AUC beside it would let a vacuous head pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-26 | Tampering | Editing `src/results.jl` to perform the rename | mitigate | The rename is deferred and recorded; a testset asserts `git diff --quiet HEAD -- src/results.jl`; acceptance criteria assert the whole of `src/` is byte-unchanged |
| T-13-27 | Tampering (supply chain) | A new direct dependency silently downgrading NeuralEstimators | mitigate | Resolve-risk clause (i) re-asserts the 0.2.1 pin by UUID and asserts the absence of `ROCAnalysis`, `MLJ`, `NormalizingFlows`, `InvertibleNetworks`, `Turing`, `ImageSegmentation`, `ImageMorphology`, `QuadGK` and `KernelDensity` |
| T-13-28 | Spoofing | A vacuous calibration pass | mitigate | `vacuous_pass` plus the structural requirement that the `gate` NamedTuple carries the head AUC and the empty-bin count |
| T-13-29 | Tampering | Hand-modifying the shared `_bin_calibration` | mitigate | Acceptance criterion asserts `git diff --quiet HEAD -- spike/validation/sbc.jl`; the empty-bin trap is worked around by choice of gate statistic, not by editing gate-lineage code |
| T-13-04 | Tampering | Untrusted deserialization reached through the result type | mitigate | The result type carries plain data only; no `.jld2` is loaded in this plan |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** Clause (i) is the executable control; the root `Pkg.test()` run proves the root manifest still resolves |
</threat_model>

<verification>
1. `julia --project=spike spike/test/runtests.jl` exits 0 across all phases.
2. `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0.
3. `julia --project=spike -e 'include("spike/p13/result.jl"); println(ThreeHypothesisColocResult <: AbstractColocResult)'` prints `true`.
4. `git diff --numstat -- spike/test/runtests.jl` reports 0 deletions.
5. `git diff --quiet HEAD -- src/ spike/validation/sbc.jl spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- `ThreeHypothesisColocResult <: AbstractColocResult` is defined in `spike/p13/result.jl` with
  `log_bf_vs_random`, an honest `bayes_factor` docstring, and no fabricated `delta_rho`.
- The empty-bin MCE trap is demonstrated by an executable testset, and ECE is asserted as the gate
  statistic.
- The vacuous-pass guard is executable and structurally carried by the calibration meta.
- `julia --project=spike spike/test/runtests.jl` runs every Phase-13 testset and is green, and
  clause (i) re-asserts the pin with no new dependency.
- `src/`, `spike/validation/sbc.jl` and both manifests are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-08-SUMMARY.md` when done,
recording the full spike-suite testset count before and after wiring, any Phase-13 symbol renamed to
avoid a flat-namespace collision, and an explicit statement that `src/results.jl` is byte-unchanged
with the rename deferred to productionization.
</output>

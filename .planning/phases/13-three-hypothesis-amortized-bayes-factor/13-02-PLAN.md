---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-12, D-02, D-08, D-09, D-13, D-15]

must_haves:
  truths:
    - "(D-12) The SC2 amendment is documented BEFORE any Phase-13 result exists, with a dated section 0 provenance disclosure and the commit sha at which it was written"
    - "(D-12) The amendment argues the defect in ROADMAP SC2 from the reference's validity conditions alone, and passes the 07-GATE-AMENDMENT legitimacy test explicitly"
    - "(D-02) A SECOND, architectural, outcome-independent defect is recorded: the Phase-11 registration-aware input surface is shared with neither compute_BayesFactor()'s ADVI path nor the shipped binary NRE, so even a valid KDE reference would not be like-for-like"
    - "(D-12) The replacement criterion is stated with numbers: per-class AUC floors, the confusion-matrix requirement, the evaluation count, and the ECE band"
    - "(D-08/D-09) SC1's superseded wording (RatioEstimator, log-BF simplex) is restated as the bindable criterion: one network, one forward pass, three-way evidence against a random reference"
    - "(D-15) SC3's scope is recorded, including the physical-anchor deferral to Phase 16 without opening the sealed holdout"
    - "(D-12) The original SC1/SC2 text is explicitly not erased, and any report citing the amended criterion must cite the original alongside it"
    - "(D-02) ROADMAP Phase 13 points at the amendment and names the Phase-11 dependency the roadmap currently omits"
  artifacts:
    - path: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md"
      provides: "Frozen-specification amendment: provenance disclosure, two numbered defects with outcome-independence arguments, the fully-specified replacement criterion, the reported-not-gated list, and the original-not-erased clause"
      contains: "FROZEN SPECIFICATION"
      min_lines: 120
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 success criteria annotated with a pointer to the amendment; Depends-on corrected to name Phase 11"
      contains: "13-SC2-AMENDMENT.md"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md"
      via: "inline pointer on the Phase-13 success-criteria block"
      pattern: "13-SC2-AMENDMENT"
    - from: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md"
      to: "docs/amortized.md"
      via: "citation of published named limit 3 as the outcome-independent justification"
      pattern: "named limit"
---

<objective>
Write the SC2 amendment **before any Phase-13 result exists**, following the
`07-GATE-AMENDMENT.md` precedent, and annotate ROADMAP so the phase is scored against the criteria
it actually implements.

Purpose: D-12 declines to validate the three-way evidence net against `compute_BayesFactor()`.
That is only legitimate if the decision is on the record with its reasons, dated, before any number
exists. Phase 13's position is unusually strong — the justification is a *prior published* finding
(named limit 3), not a Phase-13 outcome — and the document must say so plainly rather than blur
itself with Phase 7's after-the-fact posture.

Output: `13-SC2-AMENDMENT.md` (frozen spec) and the ROADMAP annotation.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md

**Binding rulings this plan implements (do not reopen):**
- The amendment **supplements, never replaces** ROADMAP SC1/SC2. The original text stays in ROADMAP.
- The amendment is written **before** `spike/p13/` produces any number. Its `Status:` line must say
  so and its provenance section must carry the commit sha, so the ordering is checkable afterwards.
- **No REQ-IDs are minted.** ROADMAP's `**Requirements**: TBD` for Phase 13 stays as-is.
- The ROADMAP `**Depends on**: Phase 7` line for Phase 13 is **incomplete** (D-02 adds Phase 11).
  Correcting it is part of this plan.
- This plan writes documentation only. It touches no `.jl` file and no `src/` byte.

<interfaces>
<!-- Extracted from the precedent document. Use directly; do not go exploring. -->

From `.planning/phases/07-productionization-conditional-on-go/07-GATE-AMENDMENT.md:1-7` — the header:
```markdown
# 07 — PRE-REGISTRATION AMENDMENT to the grid-8 ship-gate

**Date:** 2026-07-21
**Status:** **FROZEN SPECIFICATION. Nothing has been run against it.**
**Implements:** `test/gate/gate_consts_8_v2.jl` (new, frozen)
**Supplements — does NOT replace:** `test/gate/gate_consts_8.jl` (and `_4` / `_16`)
**Depends on:** `07-CALIBRATION-FINDINGS.md` (F1–F6), Phase-2 decisions **D-08**, **D-09**
```

From `07-GATE-AMENDMENT.md:22` — the legitimacy test applied to every change:
```markdown
> *Would this change have been made, with this justification, by someone who had seen the gate's
> code but none of its outputs?*
```

From `07-GATE-AMENDMENT.md:28-34` — the original-is-not-erased clause:
```markdown
**The original pre-registration is not erased.** ... remain **byte-unchanged** and their recorded
FAIL verdicts ... remain citable as-is. ... Any report that cites the amended result
must cite the original FAIL alongside it.
```

From `.planning/ROADMAP.md:265-274` — the Phase-13 block being annotated (Depends-on at :267,
SC1 at :270, SC2 at :271, SC3 at :272).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 13-SC2-AMENDMENT.md as a frozen specification</name>
  <files>.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md</files>
  <read_first>
    - `.planning/phases/07-productionization-conditional-on-go/07-GATE-AMENDMENT.md` lines 1-80 —
      the header block, the provenance disclosure, the legitimacy test, the original-not-erased
      clause, and the numbered-defect structure (defect, then outcome-independent justification,
      then the blind-reviewer question, then the new rule).
    - `13-RESEARCH.md` section H (lines 973-1020) — where amendments live in this project, the
      legitimacy test, H2's point that Phase 13 INVERTS the disclosure, and H3's six required
      contents.
    - `13-RESEARCH.md` section Phase Requirements (lines 168-187) — the SC1/SC2/SC3 status table.
    - `13-RESEARCH.md` section A3 lines 396-406 — the shuffled-theta NRE identity and the harmless
      redundancy in the shipped binary formula. Do NOT propose fixing `bf.jl`.
    - `13-RESEARCH.md` section F1 lines 741-770 — why the D-09 heads need a correction the shipped
      net does not, i.e. why the two nets are not numerically interchangeable.
    - `docs/amortized.md` named limits — limit 2 (prior atoms / randomized ranks) and limit 3
      (BF validated by discrimination; KDE invalid past |logBF| about 6.9). Quote limit 3 exactly.
    - `.planning/spikes/014-bf-sim-validation/README.md` — the attrition numbers to cite verbatim:
      34.75% non-finite (139/400), 32.5% saturated, dropped-pair median |Delta rho| 0.736 vs kept
      0.283, and the AUC 0.994 result.
    - `13-PATTERNS.md` lines 910-966 — the per-section copy instructions and the required changes.
  </read_first>
  <action>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md`.

Header block, mirroring the precedent's shape exactly:
title `# 13 — PRE-REGISTRATION AMENDMENT to ROADMAP Phase-13 Success Criteria`;
`**Date:**` today's date; `**Status:** **FROZEN SPECIFICATION. No Phase-13 result exists.**`;
`**Implements:** spike/p13/consts.jl (Tier-1, frozen)`;
`**Supplements — does NOT replace:** .planning/ROADMAP.md Phase-13 SC1 and SC2`;
`**Depends on:** docs/amortized.md named limit 3; .planning/spikes/014-bf-sim-validation/README.md;
13-CONTEXT.md decisions D-02, D-08, D-09, D-12, D-13, D-15`.

Section `## 0. Provenance disclosure — read this first`. State plainly, in the precedent's voice,
that this amendment **inverts** Phase 7's disclosure: Phase 7 amended a gate after seeing its
outputs, whereas Phase 13 is declining to adopt a reference a *prior* phase already retired, and
**no Phase-13 result exists at the time of writing**. Record today's date and the full
40-character commit sha from `git rev-parse HEAD` so the ordering is checkable. Quote the
legitimacy test verbatim, then state that Phase 13's answer is not merely "yes": the justification
is not a Phase-13 result at all. Instruct the reader explicitly not to blur the two positions.

Section `## 1. Defect A — the reference is invalid in exactly the regime that matters`.
Argue only from the reference's validity conditions: `compute_BayesFactor()` (`src/bayes.jl:109`)
reads posterior tail mass through a KDE at L = 999 draws, so the smallest resolvable tail
probability is 1/999 and any `|log BF| > log(999) ~ 6.9` is kernel-tail extrapolation, not
measurement. Cite `references/bayes-factor-gate.md` section 008 and quote the spike-014 attrition
numbers (34.75% non-finite = 139/400, 32.5% saturated, dropped-pair median |Delta rho| 0.736 vs
kept 0.283), noting attrition is **100% baseline-side**. Add the sub-heading
`### Outcome-independent justification`, then the italic question
`*Would a blind reviewer have made this change?*` with the answer: yes — the ceiling is a property
of L, readable from the code, and the finding is already published in `docs/amortized.md` as named
limit 3 and already carried by the 2026-07-24 GO. Close by noting that the confident-call tail past
6.9 is precisely where three-way verdicts matter most.

Section `## 2. Defect B — the input surfaces are not shared (architectural, needs no results)`.
D-02 trains the Phase-13 net on Phase 11's registration-aware frozen `zt`, i.e. a different
standardized summary basis plus a registration-uncertainty conditioning input. That surface is
shared with neither `compute_BayesFactor()`'s Turing/ADVI path nor the shipped binary NRE, so even
a *valid* KDE reference would not be a like-for-like comparison. Use the same two sub-headings.
State the subtler consequence: the shipped binary NRE's logit is a shuffled-theta
likelihood-to-evidence ratio, while the D-09 heads are plain BCE classifiers on class labels, so
the two are not numerically interchangeable even at identical inputs — which is exactly why the
binary-NRE continuity check is reported and not gated. Add an explicit note that this amendment
does **not** propose changing `src/amortized/bf.jl`: the shipped accessor is frozen by D-01 and its
`- log_prior_odds` term sits below the reporting floor.

Section `## 3. The replacement criterion, fully specified`. State, with numbers, naming
`spike/p13/consts.jl` as the binding source:
- **Gated:** per-class one-vs-random AUC over {exclusion, random, coloc} at simulator ground-truth
  labels, floors `P13_AUC_FLOOR_COLOC = 0.90` and `P13_AUC_FLOOR_EXCLUSION = 0.90`, evaluated on
  `P13_GATE_M = 4000` labelled pairs with at least `P13_MIN_EVAL_PER_HEAD = 1000` per head.
- **Gated:** per-head reliability `ECE <= P13_ECE_GREEN = 0.05` (D-13), with MCE **reported, not
  gated**, beside its empty-bin count, because `_bin_calibration` scores an empty bin as
  `|midpoint - 0.0|` with full MCE weight; and each head's AUC reported beside its ECE as the
  vacuous-pass guard (`P13_VACUOUS_AUC_FLOOR = 0.60`).
- **Reported, not gated:** the full 3x3 confusion matrix at `argmax(logBF_C, 0, logBF_E)`,
  explicitly labelled *descriptive, not a decision rule* — decisions and abstention are Phase 14.
- **Reported, not gated:** binary-NRE continuity in the overlapping 2-way regime at the reference
  lambda; and the alpha-graded semi-synthetic series with its crossing point.
- State that labels come from the D-05 two-factor cut on `rho_sample` level times control contrast,
  never from `delta_rho`, and give the counter-example (a sample at rho = 0.8 under a control at
  rho = 0.9 has `delta_rho < 0`; "less colocalized than the control" is not segregation).

Section `## 4. What SC1's superseded wording becomes`. SC1 names a `RatioEstimator` and a "log-BF
simplex"; D-09 rules out the former (its fixed 1-unit output head plus hard-coded logit-BCE loss
make a three-way head unreachable — a passed loss is silently ignored in v0.2.1) and D-08 rules out
the latter (two free numbers plus a structural zero is not a point on a simplex). State the
surviving, bindable criterion: **one network, one forward pass, three-way evidence emitted as
`log BF(coloc : random)` and `log BF(exclusion : random)`, with the random entry identically 0.**

Section `## 5. SC3 scope`. Retained and scoped by D-15: the gate rests on simulator ground truth
(`rho_true < -tau`); the mask-based alpha-graded series is load-bearing supporting evidence; the
single physical segregated anchor is **deferred to Phase 16** because it is triply unavailable
(`sha256 = "PENDING-FETCH"`, `corpus/data/` empty, `split = sealed_holdout`), and Phase 13 does not
call `open_sealed_holdout`.

Section `## 6. The original is not erased`. Adapt the precedent's clause: ROADMAP's SC1 and SC2
remain byte-present and citable; the amended criteria are a second, separately named
pre-registration; any report citing an amended criterion must cite the original alongside it, and
reporting only the amended criterion would be exactly the thing this document exists to prevent.

Section `## 7. Iteration allowance`. State `P13_ITERATION_ALLOWANCE = 1` and the single
pre-declared trigger (exclusion-class AUC below floor *specifically at high |rho|* implies
switching to the importance-weighted stratification, D-07-ii), and that a second amendment is not
authorised by this document.

Include no measured Phase-13 number anywhere in the file.
  </action>
  <verify>
    <automated>julia -e 'p = ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md"; s = read(p, String); @assert occursin("FROZEN SPECIFICATION", s); @assert occursin("No Phase-13 result exists", s); @assert occursin("Would a blind reviewer", s); @assert occursin("P13_AUC_FLOOR_EXCLUSION", s); println("AMENDMENT_OK")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `AMENDMENT_OK`.
    - `grep -c "0\." .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md` is nonzero AND the file contains a 40-character hex commit sha (checkable with `grep -Eo '[0-9a-f]{40}'` returning at least one match).
    - The file contains all seven required section headings: `grep -c '^## ' .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md` outputs at least `8` (sections 0 through 7).
    - `grep -c 'open_sealed_holdout' .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md` is at least `1` and appears only in the deferral sentence (no code calls it).
    - `git diff --quiet HEAD -- src/ spike/` exits 0 (this task writes documentation only).
  </acceptance_criteria>
  <done>A dated, frozen, result-free amendment exists carrying both the KDE-validity defect and the architectural input-surface defect, each with an explicit outcome-independence argument, plus the numbered replacement criterion and the original-not-erased clause.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate ROADMAP Phase 13 with the amendment pointer and the Phase-11 dependency</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 265-275 — the Phase-13 block exactly as it stands.
    - `.planning/ROADMAP.md` lines 232-252 — the Phase-11 block, for the `**Depends on**` and
      `**Plans**` formatting conventions used by the most recently planned phase.
    - `13-RESEARCH.md` section H1 lines 978-986 — the requirement that ROADMAP be annotated or
      `/gsd:verify-work` will score Phase 13 against superseded text.
    - `13-CONTEXT.md` D-02 lines 55-61 — the exact wording of the Phase-11 consequence.
    - The file `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md`
      written in Task 1.
  </read_first>
  <action>
Edit **only** the Phase-13 block of `.planning/ROADMAP.md`. Leave every other phase untouched, and
leave `**Requirements**: TBD` as-is (no REQ-IDs are minted for this phase).

Three edits:

1. `**Depends on**: Phase 7` becomes `**Depends on**: Phases 7, 11 (D-02: the three-way evidence
   net trains on Phase 11's registration-aware frozen `zt` and inherits its uncertainty
   conditioning input — execution blocks on Phase 11's research net existing; the original entry
   named Phase 7 only)`.

2. Immediately under the `**Success Criteria**` heading, insert a single bolded pointer line:
   `**AMENDED** — SC1 and SC2 are superseded by
   `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md` (D-12,
   frozen before any Phase-13 result). The original text below is retained and must be cited
   alongside any amended result.`
   Keep the three numbered criteria **byte-unchanged** — the amendment supplements, never replaces.
   Append a short parenthetical marker to criteria 1 and 2 only, of the form
   `(see amendment section 4)` and `(see amendment sections 1-3)` respectively, on the same line,
   without altering the existing wording before the marker.

3. Replace `**Plans**: TBD` and the single `- [ ] TBD (run /gsd:plan-phase 13 to break down)` line
   with the real plan inventory: `**Plans**: 14 plans (9 waves — W1 pre-registration + SC2
   amendment ; W2 labels ∥ alpha-series ∥ tau-probe code ; W3 two-head net ; W4 D-07 closed-form
   verification ; W5 result type + suite wiring ; W6 Phase-11 preconditions ∥ tau probe run
   [BLOCKED on Phase 11] ; W7 datagen + training ; W8 reported gate ∥ alpha-series run ; W9 phase
   report)` followed by fourteen `- [ ] 13-NN-PLAN.md — <one-line objective> (D-…)` lines matching
   the objectives and decision IDs in each plan's frontmatter. Mark plans 13-09 through 13-14 with
   a trailing `[BLOCKED: Phase 11]` marker.
  </action>
  <verify>
    <automated>julia -e 's = read(".planning/ROADMAP.md", String); i = findfirst("### Phase 13:", s); j = findfirst("### Phase 14:", s); blk = s[i[1]:j[1]]; @assert occursin("13-SC2-AMENDMENT.md", blk); @assert occursin("Phases 7, 11", blk); @assert occursin("13-14-PLAN.md", blk); @assert occursin("BLOCKED: Phase 11", blk); @assert occursin("Requirements**: TBD", blk); println("ROADMAP_OK")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ROADMAP_OK`.
    - `grep -c '13-SC2-AMENDMENT.md' .planning/ROADMAP.md` outputs at least `1`.
    - `grep -c 'run /gsd:plan-phase 13 to break down' .planning/ROADMAP.md` outputs `0`.
    - The three original numbered success criteria are byte-unchanged apart from the appended `(see amendment ...)` markers: `grep -c "reproduces \`compute_BayesFactor()\`" .planning/ROADMAP.md` outputs `1`.
    - `git diff --stat -- .planning/ROADMAP.md` shows changes confined to the Phase-13 block (no other `### Phase` section appears in `git diff -U0 -- .planning/ROADMAP.md`).
    - `git diff --quiet HEAD -- src/ spike/` exits 0.
  </acceptance_criteria>
  <done>ROADMAP names the Phase-11 dependency D-02 introduced, points at the frozen amendment, retains the original criteria verbatim, and lists the real fourteen-plan inventory with the Phase-11-blocked plans marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| amendment document -> reported result | The amendment is the artifact that makes a changed criterion legitimate; if it is written or edited after a result exists, every downstream claim inherits the doubt |
| ROADMAP text -> verify-work scoring | An unannotated roadmap causes the phase to be scored against criteria it deliberately does not satisfy, which reads as a silent failure rather than a declared amendment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Repudiation (scientific integrity) | Amendment written or back-dated after results | mitigate | `Status: FROZEN SPECIFICATION. No Phase-13 result exists.` plus the full 40-char `git rev-parse HEAD` sha recorded in the provenance section; acceptance criteria assert both are present, and the wave ordering places this plan before any runner exists |
| T-13-10 | Tampering | Silent replacement of the original success criteria | mitigate | The original numbered criteria stay byte-unchanged in ROADMAP; the acceptance criterion greps for the original SC2 wording still being present |
| T-13-05 | Information disclosure (anti-snooping control) | `sealed_holdout` corpus split | accept (deferred) | The amendment records the deferral to Phase 16 in prose; no code path in this plan reaches `corpus/`, and `open_sealed_holdout` is never called |
| T-13-06 | Tampering | Path construction in the documented commands | mitigate | All paths are repo-relative literals in documentation; no interpolated user strings |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** This plan writes two Markdown files; acceptance criteria assert `spike/` and `src/` are untouched |
</threat_model>

<verification>
1. `julia -e 's = read(".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md", String); @assert occursin("FROZEN SPECIFICATION", s); println("ok")'`.
2. `grep -Eo '[0-9a-f]{40}' .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md` returns at least one sha.
3. `grep -c '13-SC2-AMENDMENT.md' .planning/ROADMAP.md` is at least 1.
4. `git diff --quiet HEAD -- src/ spike/` exits 0.
5. No file matching `spike/p13/*report*.jld2` exists at the time this plan is committed (proving the amendment precedes every result).
</verification>

<success_criteria>
- `13-SC2-AMENDMENT.md` exists, is marked FROZEN, states that no Phase-13 result exists, carries the
  commit sha, and contains sections 0 through 7 including both numbered defects with their
  outcome-independence arguments.
- The replacement criterion in the amendment names every gate number by constant name.
- ROADMAP's Phase-13 block points at the amendment, names Phases 7 and 11 as dependencies, retains
  the original criteria verbatim, and lists all fourteen plans with the Phase-11-blocked ones marked.
- No `.jl` file, no `src/` byte and no manifest changed.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-02-SUMMARY.md` when done,
including the commit sha recorded in the amendment's provenance section and a one-line statement
that no Phase-13 result existed at that sha.
</output>

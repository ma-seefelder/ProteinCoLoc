---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 09
type: execute
wave: 6
depends_on: ["13-01", "13-08"]
files_modified:
  - spike/p13/preconditions.jl
  - spike/test/test_p13_preconditions.jl
  - spike/test/runtests.jl
autonomous: false
blocked_on: "Phase 11 research NPE bundle (net + frozen zt + theta-zt + encode_lambda + imsize provenance). THIS PLAN CANNOT COMPLETE UNTIL PHASE 11 LANDS."
requirements: [D-02, D-03, D-01, D-04]

must_haves:
  truths:
    - "(D-02) Phase 13 binds the Phase-11 registration-aware research NPE and its FROZEN zt, and fails loudly with a Phase-11 pointer when it is absent"
    - "(D-02) There is NO grid-8 fallback path anywhere in Phase-13 code: falling back to the shipped basis would invalidate D-02, D-03 and the D-12 continuity framing"
    - "(D-03) The registration-uncertainty conditioning input is read from Phase 11 in exactly the encoding the NPE was trained with, never re-derived, and its length determines the Phase-13 input width"
    - "(D-02) The standardizer is INHERITED, never re-fit: the loaded zt object identity is asserted against the Phase-11 handle"
    - "(D-02) The training joint is asserted, not assumed: theta arity, theta field order, the lambda range, and the recorded imsize provenance all match what Phase 11 declared"
    - "(D-01) preconditions.jl is the single file that binds Phase-11 names; every later Phase-13 file includes it rather than reaching into Phase 11 directly"
  artifacts:
    - path: "spike/p13/preconditions.jl"
      provides: "p13_require_phase11, load_p13_basis, assert_frozen_zt, p13_conditioning_length, and the guarded include preamble every later p13 file uses"
      contains: "BLOCKED on Phase 11"
      min_lines: 120
    - path: "spike/test/test_p13_preconditions.jl"
      provides: "The absent-net loud-failure test, the no-fallback source assertion, the conditioning-length derivation test, and the frozen-zt assertion test"
      contains: "p13_require_phase11"
  key_links:
    - from: "spike/p13/preconditions.jl"
      to: "spike/npe/train_npe.jl"
      via: "load_npe on the Phase-11 artifact path"
      pattern: "load_npe"
    - from: "spike/p13/preconditions.jl"
      to: "spike/p13/consts.jl"
      via: "guarded include; the preamble every later p13 file imports"
      pattern: "consts\\.jl"
---

<objective>
Bind the Phase-11 contract in exactly one place, and make its absence a loud, instructional failure
rather than a silent wrong-basis run.

Purpose: D-02 makes the Phase-13 net train on Phase 11's registration-aware frozen `zt`, not the
shipped grid-8 `zt`. The ROADMAP does not declare that dependency, the Phase-11 artifact names are
not knowable until Phase 11 executes, and the failure mode is silent: a Phase-13 net trained on the
shipped basis would look fine, train fine, and mean something different from what the phase claims.
This plan is the tripwire.

Output: `spike/p13/preconditions.jl`, its test, and the harness include.

**EXECUTION IS BLOCKED ON PHASE 11.** The checkpoint below exists so `/gsd:execute-phase` surfaces
the block instead of guessing artifact names.
</objective>

<phase_11_precondition>
Phase 13 execution needs all six of these from Phase 11. Anything missing is a hard block with no
fallback:

1. the trained Phase-11 research NPE, persisted (CPU-resident `Flux.state` plus architecture metadata);
2. the FROZEN `zt` — the 128-row summary `ZScoreTransform`, mask rows bypassed;
3. the FROZEN `theta-zt` — the 8-row theta transform;
4. `encode_lambda(lambda)` or its exact equivalent, plus the lambda range;
5. the frozen Phase-11 `imsize_set` and weights, and the widened shift prior plus the epsilon prior;
6. the theta arity and field order (8 fields including the chromatic epsilon).

**There is no substitute.** Do not use `spike/npe/trained_npe.jld2`, the shipped grid-8 bundle, or
any artifact under `artifacts/`. If Phase 11 has not landed, this plan stops at the checkpoint.
</phase_11_precondition>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md

**Binding rulings this plan implements (do not reopen):**
- **One binding site.** Every later Phase-13 file includes `spike/p13/preconditions.jl` and imports
  the Phase-11 names from there. No later file may hard-code a Phase-11 artifact path.
- **Hard `error`, not a skip.** `run_gate.jl`'s `:not_trained` status shape is right for *runner
  scripts*; the precondition file itself must throw, because D-02 makes a missing Phase-11 net a
  block rather than a skip.
- **One lambda, not two.** Sample and control are two channels of the same acquisition under the
  same stated registration uncertainty; two lambda values would imply the user holds different
  beliefs about the two stacks, which the API does not express. This is a pre-registered modelling
  choice and must be stated in the file.
- **The conditioning length is derived.** `n_cond = length(encode_lambda(lambda))`. Never write 321.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

From 13-RESEARCH section D2 — the precondition script skeleton and its message text:
```julia
const P11_NET = joinpath(@__DIR__, "..", "npe", "trained_npe_p11.jld2")   # exact name from Phase 11
isfile(P11_NET) || error("""
  Phase 13 is BLOCKED on Phase 11 (13-CONTEXT D-02).
  Expected the Phase-11 registration-aware research NPE at:
      $P11_NET
  Phase 13 must NOT fall back to the shipped grid-8 basis — that is a different input
  surface and would invalidate D-02, D-03 and the D-12 continuity framing.""")
h = load_npe(P11_NET)
@assert hasproperty(h, :zt) && hasproperty(h, :θzt)
@assert h.lambda_range isa Tuple{<:Real,<:Real}
@assert size(h.θzt.mean, 1) == 8
@assert h.training_imsize_provenance.recorded == true
```

From `spike/validation/harness.jl:48-58` — the guarded, order-dependent include preamble to copy:
```julia
isdefined(@__MODULE__, :VAL_MASTER_SEED) || include(joinpath(@__DIR__, "consts.jl"))
isdefined(@__MODULE__, :load_npe)        || include(joinpath(@__DIR__, "..", "npe", "train_npe.jl"))
isdefined(@__MODULE__, :posterior_for)   || include(joinpath(@__DIR__, "..", "npe", "infer.jl"))
isdefined(@__MODULE__, :sample_prior)    || include(joinpath(@__DIR__, "..", "simulator", "prior.jl"))
isdefined(@__MODULE__, :simulate_pair)   || include(joinpath(@__DIR__, "..", "simulator", "forward.jl"))
isdefined(@__MODULE__, :build_mci)       || include(joinpath(@__DIR__, "..", "contract.jl"))
isdefined(@__MODULE__, :encode_d01)      || include(joinpath(@__DIR__, "..", "data", "encode.jl"))
isdefined(@__MODULE__, :sample_rng)      || include(joinpath(@__DIR__, "..", "data", "seeding.jl"))
```

From `.planning/spikes/014-bf-sim-validation/run_sim_bf.jl:83-89` — the provenance assert-then-print:
```julia
let prov = PC.training_imsize_provenance(NPE)
    @assert prov.imsize_set == IMSET "training imsize_set != F5 mixture -- covariate shift!"
    @assert prov.imsize_weights == IMWTS "training imsize_weights != F5 weights"
    println("training-joint imsize provenance OK: ", prov.imsize_set, " w=", prov.imsize_weights)
end
```

From `spike/validation/train_ratio.jl:45-47, 212` — the frozen-zt discipline:
the ratio net always trains on the FROZEN `m.zt` via `standardize_summary`; no `ZScoreTransform` is
re-fit. Warning sign that it was re-fit: standardized Phase-13 summaries with mean ~0 and sd ~1 on
the Phase-13 pool.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Phase 11 has landed and bind its realized artifact names</name>
  <files>(none - this checkpoint records bindings in the plan SUMMARY)</files>
  <action>Gather and record the five Phase-11 bindings listed under `how-to-verify`, then stop for the
developer's confirmation. Write no source file in this task. The bindings are recorded in the plan
SUMMARY under a `## Phase-11 bindings` heading and are consumed as literals by Task 2's
`P13_PHASE11_NET`, its handle-property assertions, and `p13_conditioning_length`.</action>
  <what-built>Nothing yet. This checkpoint exists because the Phase-11 artifact path, the persisted
handle's field names, and the exact form of the registration-uncertainty encoding are not knowable
until Phase 11 executes (13-RESEARCH section D3). Guessing them would produce a precondition script
that passes against the wrong artifact — the one failure this plan exists to prevent.</what-built>
  <how-to-verify>
1. Confirm Phase 11 is COMPLETE in `.planning/STATE.md` and that its plans 11-07 (training) and
   11-11 (report) are checked off in `.planning/ROADMAP.md`.
2. Report the exact path of the Phase-11 research NPE artifact, e.g. by running
   `ls spike/npe/*.jld2` and naming which file is the Phase-11 research net (NOT
   `trained_npe.jld2`, which is the pre-Phase-11 net).
3. Report the persisted handle's field names for the frozen summary transform, the frozen theta
   transform, the lambda range and the imsize provenance — e.g. by running
   `julia --project=spike -e 'include("spike/npe/train_npe.jl"); h = load_npe("<path>"); println(propertynames(h))'`.
4. Report the name and the return length of the conditioning encoder — the Phase-11 equivalent of
   `encode_lambda` — and whether the chromatic epsilon is a SECOND conditioning input (13-RESEARCH
   Open Question 4). The answer sets `n_cond`.
5. Report the Phase-11 reference lambda that corresponds to `P13_TAU_REFERENCE_LAMBDA_RULE =
   :widest_rung`, i.e. Phase-11's `LAMBDA_MAX`.

If Phase 11 has NOT landed: stop here. Do not create a placeholder artifact, do not point the
precondition at the shipped grid-8 bundle, and do not proceed to Task 2.
  </how-to-verify>
  <read_first>
    - `.planning/STATE.md` — the current position and whether Phase 11 is complete.
    - `.planning/ROADMAP.md` lines 232-252 — the Phase-11 plan checklist.
    - `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md` — D-03
      (the conditioning input Phase 13 inherits) and D-07/D-08 (the commitments D-03 here must not
      contradict).
    - `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-07-SUMMARY.md` and
      `11-11-SUMMARY.md` if they exist — the realized artifact names and the training joint.
    - `13-RESEARCH.md` section D lines 553-618 — the six-item output contract, the precondition
      skeleton, the lambda placement trap, and section D3's table of what is unknowable until
      Phase 11 lands.
  </read_first>
  <acceptance_criteria>
    - The five items above are recorded verbatim in the plan SUMMARY under a heading `## Phase-11 bindings`.
    - The reported artifact path exists on disk and is NOT `spike/npe/trained_npe.jld2` and NOT under `artifacts/`.
    - `n_cond` is stated as an integer with its source (the encoder's return length), not assumed to be 1.
    - If Phase 11 has not landed, the plan stops with an explicit BLOCKED note in STATE.md naming Phase 11, and no file is created.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=spike -e 'p = ENV["P13_PHASE11_NET_CANDIDATE"]; @assert isfile(p); @assert !occursin("trained_npe.jld2", p); println("phase11 artifact present: ", p)'</automated>
  </verify>
  <done>The five Phase-11 bindings are recorded in the SUMMARY and the developer has confirmed them, or the plan is marked BLOCKED with no file created.</done>
  <resume-signal>Type "approved" with the five bindings, or "blocked" if Phase 11 has not landed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the Phase-11 precondition binding and its loud failure</name>
  <files>spike/p13/preconditions.jl</files>
  <read_first>
    - `13-RESEARCH.md` section D1 lines 556-568 — the six-item contract table, one row per assertion
      this file must make.
    - `13-RESEARCH.md` section D2 lines 570-607 — the skeleton, the error message text to use
      verbatim, the lambda placement trap with the correct and both wrong forms, and the
      one-lambda-not-two modelling rationale.
    - `13-RESEARCH.md` section D3 lines 609-618 — what is unknowable until Phase 11 lands and the
      instruction that this is the ONE file whose job is to bind those names.
    - `13-RESEARCH.md` Pitfall 5 lines 1348-1355 — the re-fit-zt failure mode and its warning sign.
    - `13-PATTERNS.md` section `spike/p13/preconditions.jl` lines 158-211 — analogs A through D,
      including the guarded include preamble to copy verbatim with adjusted paths and the
      hard-error-versus-status-return distinction.
    - `spike/validation/harness.jl` lines 44-94 — the preamble and the single-load frozen-model
      accessor.
    - `spike/npe/train_npe.jl` — `load_npe` and the persisted handle's shape.
    - The five bindings recorded in Task 1's SUMMARY section.
  </read_first>
  <action>
Create `spike/p13/preconditions.jl`: AGPL block header, then
`# spike/p13/preconditions.jl --- the Phase-11 hard precondition and the p13 include preamble (D-02, D-03).`
followed by a banner in capitals stating: Phase 13 trains on Phase 11's registration-aware FROZEN
`zt`, NOT the shipped grid-8 `zt`; there is NO fallback; a missing Phase-11 net is a BLOCK, not a
skip; and this is the single file that binds Phase-11 names, so every later `spike/p13/*.jl` includes
this rather than reaching into Phase 11 directly. Add the DECOUPLING line and the one-lambda-not-two
modelling statement.

Copy the guarded, order-dependent include preamble from `spike/validation/harness.jl:48-58` with the
paths adjusted for `spike/p13/` (`joinpath(@__DIR__, "..", "simulator", ...)` and so on), preceded by
the same ORDER MATTERS comment, and add `spike/p13/consts.jl` as the first include.

Define:

1. `const P13_PHASE11_NET = joinpath(@__DIR__, "..", "npe", "<realized name from Task 1>")` with a
   comment recording where the name came from (the Task-1 checkpoint) and the date.

2. `p13_require_phase11(path = P13_PHASE11_NET)` which, when the file is absent, throws the
   multi-line `error` from the interfaces block verbatim — including the sentence that Phase 13 must
   NOT fall back to the shipped grid-8 basis and why. When present, it loads through `load_npe` and
   asserts, each with its own message naming the contract item it protects:
   - the frozen summary transform and the frozen theta transform are both present on the handle;
   - the theta transform has 8 rows (the chromatic epsilon joined theta in Phase 11);
   - the lambda range is a two-element real tuple and its upper end equals the Phase-11 `LAMBDA_MAX`
     recorded in Task 1, so `P13_TAU_REFERENCE_LAMBDA_RULE = :widest_rung` resolves to a known number;
   - the imsize provenance is recorded and its set and weights equal `P13_IMSIZE_SET` and
     `P13_IMSIZE_WEIGHTS` (the F5 covariate-shift check, in the assert-then-print shape of the
     spike-014 precedent);
   - the theta field order matches what Phase 11 declared, asserted positionally so an arity change
     cannot silently shift every row.
   Return the loaded handle.

3. `load_p13_basis(; path = P13_PHASE11_NET)` — the single-load accessor every later file uses,
   memoized in a module-level `Ref` so the artifact is read once. Docstring: the analog is
   `harness.jl:70-80`'s `load_frozen_model`, pointed at the Phase-11 artifact.

4. `assert_frozen_zt(basis, Zstd_sample)` asserting the standardizer was INHERITED, not re-fit:
   check that the standardized Phase-13 pool does NOT have mean approximately 0 and sd approximately
   1 (within a stated tolerance), because a frozen transform applied to a different pool should not
   give exactly that. Docstring carries Pitfall 5 in full, including that this is a warning-sign
   check and a soft violation must be reported rather than silently accepted.

5. `p13_encode_lambda(lambda)` — a thin, documented pass-through to the Phase-11 encoder bound in
   Task 1, with an assertion that the return length equals `p13_conditioning_length()`.

6. `p13_conditioning_length()` returning `length(p13_encode_lambda(<a lambda inside the range>))`,
   computed once. Docstring, in capitals: the Phase-13 input width is
   `three_way_input_dim(G, p13_conditioning_length())`; NEVER write 321 as a literal, because
   Phase 11 may deliver a conditioning vector of a different length.

7. `p13_encode_pair(Zs_128, Zc_128, lambda)` returning `encode_conditioned_pair(Zs_128, Zc_128,
   p13_encode_lambda(lambda))`, with the docstring carrying the correct form and both wrong forms
   from the lambda-placement trap, and noting that the wrong form fails loudly on `pair_encode`'s
   `iseven` guard, which is a gift.

The file must contain no grid-8 path, no reference to `artifacts/`, and no `try`/`catch` that could
convert the block into a warning.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_preconditions.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike -e 'include("spike/p13/preconditions.jl"); h = p13_require_phase11(); println("phase11 bound")'` prints `phase11 bound`.
    - Absence fails loudly: `julia --project=spike -e 'include("spike/p13/preconditions.jl"); try p13_require_phase11("does_not_exist.jld2") catch e; m = sprint(showerror, e); @assert occursin("BLOCKED on Phase 11", m); @assert occursin("grid-8", m); println("block ok") end'` prints `block ok`.
    - `grep -v '^\s*#' spike/p13/preconditions.jl | grep -c 'trained_npe.jld2\|artifacts/\|grid_8'` outputs `0` (no fallback path exists in executable code).
    - `grep -v '^\s*#' spike/p13/preconditions.jl | grep -c 'try'` outputs `0` (the block cannot be downgraded to a warning).
    - `julia --project=spike -e 'include("spike/p13/preconditions.jl"); println(p13_conditioning_length())'` prints the integer recorded in Task 1.
    - `grep -v '^\s*#' spike/p13/preconditions.jl | grep -c '321'` outputs `0`.
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>Phase-11's six contract items are asserted in one place, absence throws an instructional error naming the block and forbidding the grid-8 fallback, the standardizer is proven inherited, and the conditioning length is derived rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the precondition test and wire it into the gate</name>
  <files>spike/test/test_p13_preconditions.jl, spike/test/runtests.jl</files>
  <read_first>
    - `spike/test/test_bf.jl` lines 39-65 — the guarded include and the load-or-skip fixture idiom.
    - `spike/test/runtests.jl` lines 118-143 — the include-block convention and the Phase-13 block
      appended by plan 13-08.
    - `13-VALIDATION.md` line 59 — the D-02/D-03 precondition row this test satisfies.
    - `13-RESEARCH.md` section D2 lines 570-607 — the assertions being tested.
    - `spike/p13/preconditions.jl` — the file written in Task 2.
  </read_first>
  <action>
Create `spike/test/test_p13_preconditions.jl`: AGPL header, banner naming D-02/D-03 and stating that
the absent-artifact path is the important test, `using Test`, and a guarded
`isdefined(@__MODULE__, :p13_require_phase11) || include(joinpath(@__DIR__, "..", "p13", "preconditions.jl"))`.

One outer `@testset "P13 Phase-11 preconditions (D-02, D-03)" verbose = true`:

1. `"absence is a loud, instructional block"` — call `p13_require_phase11` on a nonexistent path
   inside a temporary directory and assert the thrown message contains `BLOCKED on Phase 11`,
   `13-CONTEXT D-02` and the phrase forbidding the grid-8 fallback. This testset must pass
   **regardless** of whether the real Phase-11 artifact is present, so it is the one Phase-13
   assertion that is runnable in any state of the repo.
2. `"no fallback path exists"` — read `spike/p13/preconditions.jl` once into a module-level `const`,
   strip comment lines, then assert `trained_npe.jld2`, `artifacts/`, `grid_8` and `try` are all
   absent from executable code. Add the comment that a `try` here would convert a block into a
   warning, which is the exact failure D-02 forbids.
3. `"the conditioning length is derived, not literal"` — assert the source contains
   `three_way_input_dim` or `p13_conditioning_length` and does not contain the literal `321`.
4. `"the real artifact satisfies the six-item contract"` — guarded on
   `isfile(P13_PHASE11_NET)`: load through `p13_require_phase11()` and assert the frozen transforms
   are present, the theta transform has 8 rows, the lambda range is a two-element tuple whose upper
   end matches `P13_TAU_REFERENCE_LAMBDA_EXPECTED`, and the imsize provenance equals
   `P13_IMSIZE_SET` and `P13_IMSIZE_WEIGHTS`. When the artifact is absent, mark this testset
   `@test_skip` with an in-file comment naming Phase 11 — never fabricate a stand-in artifact.
5. `"lambda placement"` — assert `length(p13_encode_pair(zeros(128), zeros(128), <a valid lambda>))`
   equals `three_way_input_dim(8, p13_conditioning_length())`, and that passing 129-row summaries
   throws with a message mentioning `even` or `2*G^2`.
6. The CUDA-absence clause.

Then append to `spike/test/runtests.jl`, **additively only**, the single line
`include(joinpath(@__DIR__, "test_p13_preconditions.jl"))` in the Phase-13 include block created by
plan 13-08, with a one-line comment noting that the absent-artifact assertion runs in any repo state
while the contract assertions skip until Phase 11 lands.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_preconditions.jl` exits 0 with zero failures.
    - `julia --project=spike spike/test/runtests.jl` exits 0 across all phases.
    - **The include this plan adds is present, exactly once:** `grep -c 'test_p13_preconditions.jl' spike/test/runtests.jl` outputs `1`.
    - **The running total is consistent:** `grep -c 'include(joinpath(@__DIR__, "test_p13' spike/test/runtests.jl` outputs `10` -- the nine Phase-13 includes wired by plan 13-08 plus this one. The looser `grep -c 'test_p13' spike/test/runtests.jl` outputs **at least** `10` (it may exceed 10 if a
      rationale comment names a test file; the include-line count above is the authoritative check).
    - If a count is wrong, **fix the count, never the include set.** Omitting or deleting an include
      to make a number match would silently un-wire the D-02/D-03 precondition test from the
      always-run suite -- the exact failure these two criteria exist to catch.
    - `git diff --numstat -- spike/test/runtests.jl` reports 0 deletions.
    - The absent-artifact testset passes even when `P13_PHASE11_NET` does not exist (verify by temporarily running the test file from a directory where the artifact is unreachable, or by asserting the testset name appears in the passing output with no skip).
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>The block is asserted in the suite in a way that runs in any repo state, the no-fallback and no-try source assertions are executable, and the Phase-11 contract assertions skip explicitly rather than silently when the artifact is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-11 artifact -> Phase-13 training basis | The whole scientific claim of the phase depends on which standardized summary basis the net saw; substituting the shipped grid-8 basis would be undetectable from the trained net alone |
| persisted `.jld2` handle -> in-process objects | JLD2 load reconstructs arbitrary types; the artifact must be one this project produced, at a path built from `@__DIR__` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Spoofing | A wrong-basis net trained on the shipped grid-8 `zt` | mitigate | Hard `error` with the Phase-11 pointer; source assertions that no grid-8 path, no `artifacts/` path and no `try` appear in executable code; the blocking checkpoint binds the realized artifact name rather than guessing it |
| T-13-30 | Tampering (semantic) | Re-fitting `zt` on the Phase-13 pool | mitigate | `assert_frozen_zt` checks the Pitfall-5 warning sign (mean approximately 0, sd approximately 1 on a different pool) and reports rather than silently accepting |
| T-13-31 | Spoofing | Covariate shift between the training joint and the evaluation joint | mitigate | Imsize provenance asserted equal to `P13_IMSIZE_SET` and `P13_IMSIZE_WEIGHTS` in the spike-014 assert-then-print shape |
| T-13-04 | Tampering | Untrusted deserialization | mitigate | Only artifacts produced by this project are loaded, from `joinpath(@__DIR__, ...)` paths; the handle's expected properties are asserted immediately after load (T-7-01 precedent) |
| T-13-07 | Tampering | Shape confusion at the conditioning boundary | mitigate | `p13_conditioning_length` derives the width; `pair_encode`'s `iseven` guard is retained as the tripwire (ASVS V5) |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** Acceptance criteria assert the manifests byte-unchanged |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p13_preconditions.jl` exits 0.
2. `julia --project=spike spike/test/runtests.jl` exits 0 across all phases.
3. The absent-artifact error message contains `BLOCKED on Phase 11` and the grid-8 prohibition.
4. `grep -v '^\s*#' spike/p13/preconditions.jl | grep -c 'artifacts/\|grid_8\|try'` outputs `0`.
5. `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- `spike/p13/preconditions.jl` is the single site binding Phase-11 names, asserts all six contract
  items, and throws an instructional error when the artifact is absent.
- No fallback to the shipped grid-8 basis exists anywhere in Phase-13 executable code.
- The conditioning length is derived and the input width follows from it.
- The suite runs in any repo state: the block assertion always, the contract assertions skipped
  explicitly when Phase 11 has not landed.
- No `src/` byte and no manifest byte changed.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-09-SUMMARY.md` when done,
including a `## Phase-11 bindings` section recording the artifact path, the handle field names, the
conditioning encoder and its length, the Phase-11 `LAMBDA_MAX`, and the imsize provenance actually
found.
</output>

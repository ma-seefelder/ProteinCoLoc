---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/p13/consts.jl
  - spike/test/test_p13_consts.jl
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-07, D-09, D-10, D-12, D-13, D-14, D-16]

must_haves:
  truths:
    - "(D-04) Every Phase-13 threshold, ladder, seed and design choice that a reported run scores against is committed BEFORE any Phase-13 run exists"
    - "(D-01) The Phase-13 DEV seed and fixture seed are executably proven disjoint from PROD_SEED, PROD_SEED_V2, VAL_MASTER_SEED, NPE_MASTER_SEED, RATIO_PAIR_SEED, VAL_FIX_SEED, CORPUS_MASTER_SEED, P11_DEV_SEED and every prior burned dev seed"
    - "(D-06) The tau probe specification (statistic, unpaired design, delta grid, AUC bar, R, reference-lambda rule, abort criterion) is locked before the probe consumes a single stream, and tau itself is a Tier-2 slot that does not yet exist"
    - "(D-07) The stratification design (:class_frequency primary), its pre-declared fallback, the single-iteration trigger and the closed-form verification pass bars incl. the mandatory negative control are constants, not prose"
    - "(D-12) The amended gate numbers (per-class AUC floors, evaluation count, argmax-is-descriptive rule, continuity-is-reported-not-gated) exist as locked constants"
    - "(D-13) ECE is the gate statistic and MCE is explicitly not, with the empty-bin rationale and the vacuous-pass AUC floor recorded as constants"
    - "(D-16) The alpha ladder, the strictly-positive background-floor rule and the frozen ch1 mask rule are pre-registered choices"
    - "(D-01) The consts file touches no src/ byte and adds no dependency"
  artifacts:
    - path: "spike/p13/consts.jl"
      provides: "Tier-1 Phase-13 pre-registration: seeds+salt+streams, _p13_forbidden(), F5 imsize read, D-05 cut variant, D-06 probe spec, D-07 design+bars, D-09/D-10 recipe, D-12 gate floors, D-13 calibration band, D-16 alpha ladder, declared deviations, executable self-checks"
      contains: "P13_DEV_SEED"
      min_lines: 220
    - path: "spike/test/test_p13_consts.jl"
      provides: "Literal-value assertions on every locked constant plus seed/salt disjointness and the p13_tau() Tier-2 tripwire"
      contains: "_p13_forbidden"
  key_links:
    - from: "spike/test/test_p13_consts.jl"
      to: "spike/p13/consts.jl"
      via: "guarded include plus literal-value assertions"
      pattern: "p13/consts\\.jl|\\.\\.\\/p13"
    - from: "spike/p13/consts.jl"
      to: "test/gate/gate_consts_8_v2.jl"
      via: "isolated read-only module that recomputes PROD_SEED / PROD_SEED_V2 and reads SBC_IMSIZE_SET/WEIGHTS"
      pattern: "gate_consts_8_v2"
---

<objective>
Lock the Phase-13 pre-registration (D-04). Every threshold, ladder, seed, statistic and design
choice that any reported Phase-13 number will be scored against is committed here, before any
Phase-13 code runs and before a single labelled datum exists.

Purpose: this phase promotes "mutually exclusive" to a first-class hypothesis on the back of a
network that does not yet exist. The only thing that makes its eventual numbers citable is that
the bars were set first. This plan is that contract, plus its executable self-check.

Output: `spike/p13/consts.jl` (Tier 1 only) and `spike/test/test_p13_consts.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md

**Binding rulings this plan implements (do not reopen):**
- **No REQ-IDs are minted.** ROADMAP `**Requirements**: TBD` and `REQUIREMENTS.md` stay untouched;
  `requirements:` carries D-IDs.
- **Two-tier structure.** This plan writes **Tier 1 only**. `P13_TAU` is deliberately **absent** —
  it is Tier 2, appended by plan 13-10 in a *second guard block keyed on a different sentinel*,
  after the probe. Constants are appended, never edited; the git history is the audit trail.
- **The `src/results.jl` `log_bf_simplex` rename is DEFERRED** to a future productionization phase
  (13-RESEARCH §G3 Open Question 1). It is recorded here as a constant so the deferral is on the
  record, and `src/` stays byte-unchanged.
- **`P13_TAU_REFERENCE_LAMBDA` is a RULE, not a number.** Phase 11 has not landed, so the rule
  (`:widest_rung`) plus an expected value is what is locked; the realized number is asserted
  against Phase-11's `LAMBDA_MAX` at probe time (plan 13-10).

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

From `spike/validation/consts.jl` (the file-shape analog — banner, ONE guard block, aligned consts):
```julia
if !isdefined(@__MODULE__, :SBC_M)
    const SBC_M           = 2000     # pre-registered SBC draws
    const SBC_ECE_GREEN   = 0.05     # ECE <= GREEN -> :green traffic light
    const SBC_ECE_YELLOW  = 0.10     # ECE <= YELLOW -> :yellow, else :red
    const VAL_MASTER_SEED = 0x5BC0FFEE   # != 0xC0FFEE (NPE_MASTER_SEED)
    const VAL_FIX_SEED    = 0xF1F7ED
end
```

From `.planning/spikes/014-bf-sim-validation/run_sim_bf.jl:40-45` — the isolated read-only module
idiom (no gate is run, the file is not modified):
```julia
module _GC
    include(joinpath(@__DIR__, "..", "..", "..", "test", "gate", "gate_consts_8_v2.jl"))
end
```

From `test/gate/gate_consts_8_v2.jl:268-285` — recompute, never trust a comment:
```julia
const PROD_SALT   = 0x94D0_49BB_1331_11EB
const PROD_MASTER = 0x0000_0000_09E3_779B
function _derive_prod_seed(G::Integer)
    rng = Philox4x(UInt64, (UInt64(PROD_MASTER) xor PROD_SALT, UInt64(G)))
    s = rand(rng, UInt64)
    while s in (VAL_MASTER_SEED, NPE_MASTER_SEED, UInt64(0)); s = rand(rng, UInt64); end
    return s
end
const PROD_SEED = Dict{Int,UInt64}(G => _derive_prod_seed(G) for G in (4, 8, 16, 32))
```

From `spike/data/seeding.jl:75-96` — the salt-XOR-into-the-first-key-word idiom:
```julia
sample_rng(master_seed::Integer, idx::Integer) =
    Philox4x(UInt64, (UInt64(master_seed), UInt64(idx)))
holdout_rng(master_seed::Integer, idx::Integer) =
    Philox4x(UInt64, (UInt64(master_seed) xor HOLDOUT_SALT, UInt64(idx)))
```

From `src/amortized/train_ratio.jl:53-54,155-163` — the D-10 numbers to copy verbatim:
`RATIO_NUM_SUMMARIES = 64`, `RATIO_SUMMARY_WIDTH = 256`, `n = 48_000`, `epochs = 300`,
`batchsize = 128`, `learning_rate = 2.5e-4`, `weight_decay = 1e-4`, `val_frac = 0.15`,
`stopping_epochs = 40`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Tier-1 Phase-13 pre-registration consts file</name>
  <files>spike/p13/consts.jl</files>
  <read_first>
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-RESEARCH.md` §K1 (lines
      1214-1247, the complete forbidden-seed inventory and the recompute-don't-trust rule), §K2
      (lines 1248-1257, the proposed seeds and their verified absence), §K3 (lines 1258-1287, the
      required contents and the full Tier-1/Tier-2 table).
    - `13-RESEARCH.md` §E1 lines 624-651 (cut variant (a) and the measured class-mass tables),
      §E3 lines 676-717 (the tau statistic, the unpaired rationale, and the whole recommended
      probe-knob table), §E4 lines 718-734 (the probe is Phase-11-simulator-gated, not net-gated).
    - `13-RESEARCH.md` §F3 lines 786-798 (the three admissible D-07 designs and why D-07-i is
      primary), §F5 lines 845-879 (the verification protocol and its pre-registered pass bars plus
      the mandatory negative control).
    - `13-RESEARCH.md` §I1 lines 1024-1057 (the empty-bin MCE trap) and §I2 lines 1058-1072
      (the 0.05/0.10 traffic-light values and the re-declare-don't-import rule).
    - `13-RESEARCH.md` §J3 lines 1143-1196 (the alpha ladder, the `_exclude_zero` trap, the
      background floor, and the frozen `_calculate_mask` rule).
    - `13-PATTERNS.md` section `spike/p13/consts.jl` (lines 52-155) — the banner, the guard idiom,
      the recompute machinery, the isolated-module include, and the two-commit tau discipline.
    - `spike/validation/consts.jl` — whole file (80 lines), the primary shape.
    - `test/gate/gate_consts_8_v2.jl` lines 15-26, 126-166, 254-345 — the provenance-disclosure
      voice, `SBC_IMSIZE_SET`/`SBC_IMSIZE_WEIGHTS`, and the derived-seed machinery.
    - `spike/data/seeding.jl` lines 30-96 — the salt constants and the Philox helpers.
    - `spike/Project.toml` — confirm `Random123` is present; no dependency may be added.
  </read_first>
  <action>
Create `spike/p13/consts.jl`. Start with the AGPL block header used by every spike file, then
`# spike/p13/consts.jl --- Phase-13 Tier-1 pre-registration (D-04).`, then an ALL-CAPS
anti-snooping preamble in `spike/validation/consts.jl:21-40`'s voice which additionally states:
(a) the **two-tier structure** — Tier 1 is this block, committed before anything runs; Tier 2 is a
SECOND guard block appended by plan 13-10 after the tau probe; no constant in either block is ever
EDITED; (b) the **DECOUPLING** line ("spike-local constants; touches no `src/`"); (c) that Phase-13
**execution blocks on Phase 11** (D-02) and must never fall back to the shipped grid-8 basis.

`import Random123: Philox4x`. Wrap the whole Tier-1 body in
`if !isdefined(@__MODULE__, :P13_DEV_SEED) ... end`. Constants are column-aligned on `=` and each
carries a trailing rationale comment naming its governing decision ID.

**Section A — forbidden seeds and the fresh stream (D-01).**
Declare as `UInt64` literals: `DEFAULT_MASTER_SEED = 0x0000_0000_0000_0001`,
`NPE_MASTER_SEED = 0x0000_0000_00C0_FFEE`, `VAL_MASTER_SEED = 0x0000_0000_5BC0_FFEE`,
`VAL_FIX_SEED = 0x0000_0000_00F1_F7ED` (read the literal out of `spike/validation/consts.jl:79`
and confirm it is `0xF1F7ED` before writing the padded form),
`RATIO_PAIR_SEED = 0x0000_0000_004A_7107`, `CORPUS_MASTER_SEED = 0x0000_0000_00C0_5EED`, and
`P11_DEV_SEED = 0x0000_0000_0B11_DE71` with a comment stating it is forbidden **even though Phase
11 has not run yet** (13-RESEARCH §K1). Collect the burned spike seeds in
`P13_BURNED_DEV_SEEDS` covering `0x0DE7_C0DE`, `0xDE7C_0DE2`, `0x0DE7_C0D3`, `0x000B_7A77_0`
through `…73` (the four spike 006/008 keys), the four spike-010 `0x00CA_11B0`…`B3`,
`0x00DE_C0DE` (spike 013), `0x00BF_5014` (spike 014), `0x00A1_CE01`, `0x00A1_CE02`,
`0xC0FF_EE5B`, `0x0000_00C0_FFEE_5BC0`, `0x00A7_0115`, `0x00A7_0213`, `0x0134_D8F3`, and the four
11-RESEARCH probe burns `0xBEEF`, `0xCAFE`, `0xFEED`, `0xDEAD` — all widened to `UInt64`.
Declare the repo salt inventory `P13_REPO_SALTS = (0x9E37_79B9_7F4A_7C15, 0xD1B5_4A32_D192_ED03,
0xBF58_476D_1CE4_E5B9, 0x94D0_49BB_1331_11EB, 0xC4CE_B9FE_1A85_EC53, 0xA24B_AED4_663E_E121,
0xA5A5_A5A5_A5A5_A5A5, 0xA5A5_A5A5_DEAD_BEEF)`.
Pull the **derived** gate seeds in via the isolated read-only module idiom: `module _GC` containing
`include(joinpath(@__DIR__, "..", "..", "test", "gate", "gate_consts_8_v2.jl"))` — note the path is
**two** levels up from `spike/p13/`, not three — with a comment stating the file is READ ONLY, no
gate is run, and that `PROD_SEED` / `PROD_SEED_V2` are **recomputed, never trusted from comments**.
Define `_p13_forbidden()` returning a `Set{UInt64}` union of `UInt64(0)`, every named seed above,
`P13_BURNED_DEV_SEEDS...`, `values(_GC.PROD_SEED)...` and `values(_GC.PROD_SEED_V2)...`.
Then the fresh stream: `P13_DEV_SEED = 0x0000_0000_0B13_DE71` (comment: "0B13" = phase 13,
"DE71" = DEV-1, mirroring `P11_DEV_SEED`), `P13_FIX_SEED = 0x0000_0000_0B13_F1F7` (the fixture
stream, mirroring the `VAL_FIX_SEED` discipline — fixtures must never consume a reported stream),
and `P13_SALT = 0x2545_F491_4F6C_DD1D`. Define
`p13_rng(counter::Integer = 0) = Philox4x(UInt64, (UInt64(P13_DEV_SEED) ⊻ P13_SALT, UInt64(counter)))`
and `p13_fix_rng(counter::Integer = 0)` with `P13_FIX_SEED`. Reserve named counters:
`P13_TAU_COUNTER = 1`, `P13_DATAGEN_COUNTER = 2`, `P13_GATE_COUNTER = 3`,
`P13_ALPHA_COUNTER = 4`, `P13_CONTINUITY_COUNTER = 5`, `P13_FIXTURE_COUNTER = 99`.
Add `P13_GLOBAL_RNG_DISCIPLINE` as a doc-string constant stating that `Random.seed!` derived from
`P13_DEV_SEED` MUST be called immediately before every `train` and every posterior-draw loop,
because Flux's `DataLoader(shuffle=true)` draws from the global RNG (13-RESEARCH Pitfall 6).

**Section B — the F5 image-size mixture.** `P13_IMSIZE_SET = _GC.SBC_IMSIZE_SET` and
`P13_IMSIZE_WEIGHTS = _GC.SBC_IMSIZE_WEIGHTS`, with a comment that these are READ, never retyped,
and that the binding invariant is train-joint == eval-joint (F5 covariate shift).

**Section C — the D-05 hypothesis cut.** `P13_CUT_VARIANT = :tau_contrast` with a comment giving
variant (a) in full (`COLOC` iff `rho_s > tau` AND `rho_s - rho_c > tau`; `EXCLUSION` iff
`rho_s < -tau` AND `rho_s - rho_c < -tau`; `RANDOM` otherwise) and the counter-example that makes
this decision load-bearing: a sample at rho = 0.8 under a control at rho = 0.9 has `delta_rho < 0`
and a naive three-way extension of `delta_rho` (`src/amortized/infer.jl:117-121`) would publish it
as "mutually exclusive"; "less colocalized than the control" is not segregation. State that
`P13_TAU` is **Tier 2 and deliberately absent from this block**. Define
`p13_tau()` returning `P13_TAU` when `isdefined(@__MODULE__, :P13_TAU)` and otherwise `error`ing
with the message that tau is Tier-2, that plan 13-10 must run `spike/p13/run_tau_probe.jl` against
the **post-Phase-11 simulator** and append the measured value in a second guard block, and that
generating labelled data before that point violates D-06.

**Section D — the D-06 probe specification.** `P13_TAU_DELTA_GRID = (0.02, 0.03, 0.05, 0.075,
0.10, 0.15, 0.20)`, `P13_TAU_AUC = 0.90` (the "reliably distinguish" bar, pre-registered as a
NUMBER so "reliably" is not argued after the fact), `P13_TAU_R = 400` per arm (comment: AUC
standard error ~0.03 at A ~ 0.9, so the 0.90 bar is resolved to +/- 0.03),
`P13_TAU_STATISTIC = :mbar_mask_weighted` (comment: the mean of the PRESENT continuous summary
rows 1:G^2 weighted by the mask rows G^2+1:2G^2 — fit-free, and the same quantity the frozen
`ghat` calibration was built on; a learned readout would make tau a property of a model rather than
of the summary, contradicting D-06), `P13_TAU_DESIGN = :unpaired` with a comment that this is the
single most important difference from the Phase-11 D-06 probe (a paired design measures the
resolution of a counterfactual the deployed tool never has and understates tau),
`P13_TAU_BOTH_DIRECTIONS = true` (measure `A(0 vs -delta)` and `A(0 vs +delta)`, take the max,
because the prior atoms are asymmetric ~2.5x), `P13_TAU_BOOTSTRAP_B = 1000` (report mean AND median
AUC over bootstrap resamples), `P13_TAU_REFERENCE_LAMBDA_RULE = :widest_rung`,
`P13_TAU_REFERENCE_LAMBDA_EXPECTED = 3.0` (comment: the rule is Tier-1, the realized number is
asserted against Phase-11's `LAMBDA_MAX` at probe time), `P13_TAU_ABORT_EXTEND_GRID = false`
(comment: if `A(delta) < P13_TAU_AUC` for EVERY delta, tau is not measurable — stop and report;
extending the grid would make "below resolution" unfalsifiable), and
`P13_TAU_REQUIRE_POST_P11_SIMULATOR = true` (comment: freezing a tau measured against the
pre-Phase-11 simulator is a pre-registration error — the deployment distribution is wider).

**Section E — the D-07 correction design and its verification bars.**
`P13_STRATIFICATION = :class_frequency` (D-07-i, primary) with a comment carrying the theorem: the
scalar per-head correction `log(q_pos/q_neg)` is exact **iff** stratification changed only class
FREQUENCIES and not the WITHIN-class theta shape, so D-07's literal within-class rho-stratification
is provably not repairable by a scalar. `P13_TARGET_CLASS_FREQ = (exclusion = 1//3, random = 1//3,
coloc = 1//3)` with a comment that the correction is MEASURED regardless (never assumed 0) and that
whole-class accept/reject preserves `pi(theta | class)`.
`P13_STRATIFICATION_FALLBACK = :importance_weighted` (D-07-ii, pre-declared),
`P13_ITERATION_ALLOWANCE = 1`, and `P13_ITERATION_TRIGGER` as a string constant naming the ONE
pre-declared condition for spending it: exclusion-class per-class AUC below its floor
*specifically at high |rho|*. State that a second iteration is NOT authorised by this file.
Verification bars: `P13_F5_CORR_MIN = 0.99`, `P13_F5_MAXABS_TOL = 0.25`,
`P13_F5_CENTRAL_FRAC = 0.90`, `P13_F5_SKEW_FREQ = (0.60, 0.25, 0.15)` (deliberately skewed toy
class frequencies so a missing correction is impossible to miss), and
`P13_F5_NEGCTRL_REQUIRED = true` with a comment that without the negative control the test cannot
distinguish "correction works" from "correction is unnecessary here".

**Section F — the D-09/D-10 architecture and the copied recipe.**
`P13_SUMMARY_WIDTH = 256`, `P13_NUM_SUMMARIES = 64` (both COPIED VERBATIM from
`src/amortized/train_ratio.jl:53-54`), `P13_TRAIN_N = 48_000`, `P13_EPOCHS = 300`,
`P13_BATCHSIZE = 128`, `P13_LR = 2.5e-4`, `P13_WEIGHT_DECAY = 1e-4`, `P13_VAL_FRAC = 0.15`,
`P13_STOPPING_EPOCHS = 40`, `P13_USE_GPU = false` (CPU-only is the reproducible baseline),
`P13_SMOKE_N = 200`, `P13_SMOKE_EPOCHS = 2` (the A5 route-R1 smoke gate that must pass before any
real training), and `P13_LAMBDA_PLACEMENT = :append_after_pair_encode` with a comment giving the
correct form `vcat(pair_encode(Zs_128, Zc_128), lambda)` and naming the two wrong forms
(`pair_encode` on 129-row inputs, which throws on the `iseven` guard, and appending lambda twice).
Add `P13_INPUT_WIDTH_RULE = :ratio_input_dim_plus_ncond` with a comment: **never write 321 as a
literal** — Phase 11 may deliver a conditioning vector of length != 1.

**Section G — the D-12 amended gate.** `P13_GATE_M = 4000` (labelled evaluation pairs; comment
that at the measured class masses this puts >= 2500 samples in each head's restricted evaluation
set, clearing `P13_MIN_EVAL_PER_HEAD`), `P13_AUC_FLOOR_COLOC = 0.90`,
`P13_AUC_FLOOR_EXCLUSION = 0.90` (comment: threshold-free one-vs-random discrimination is the
gate; the shipped binary analogue reached 0.994 in spike 014, so 0.90 is a floor and not a target),
`P13_CONFUSION_RULE = :argmax_descriptive` with a comment that the 3x3 confusion matrix at
`argmax(logBF_C, 0, logBF_E)` is REPORTED as descriptive and is explicitly NOT a decision rule —
decisions and abstention are Phase 14's scope — and `P13_CONTINUITY_GATED = false` (the shipped
binary-NRE continuity check at a fixed lambda is reported, never gated, because D-02 gives the two
nets different input surfaces).

**Section H — the D-13 calibration gate.** `P13_ECE_GREEN = 0.05`, `P13_ECE_YELLOW = 0.10`
(re-declared locally, never imported from `spike/validation/consts.jl` — each pre-registration is
self-contained), `P13_ECE_NBINS = 10` (comment: matches `_bin_calibration`'s default so the number
stays comparable with spike 014's 0.0188), `P13_GATE_STATISTIC = :ece` with a comment carrying the
empty-bin trap in full — `_bin_calibration` scores an empty bin as `|midpoint - 0.0|` with zero ECE
weight but FULL MCE weight, so on a well-separated three-way problem MCE is dominated by empty bins
and is not a usable gate statistic; MCE is REPORTED beside its empty-bin count and is never gated —
`P13_MIN_EVAL_PER_HEAD = 1000` (ECE is biased downward at small n),
`P13_VACUOUS_AUC_FLOOR = 0.60` (a green ECE with head AUC below this is a VACUOUS pass and must be
labelled as such), and `P13_TOST_REQUIRED = false` (D-14: the traffic light is a band, not a
p-value, so there is no over-power pathology and no equivalence machinery is needed).

**Section I — the D-16 semi-synthetic alpha ladder.**
`P13_ALPHA_LADDER = (0.0, 0.125, 0.25, 0.375, 0.5, 0.625, 0.75, 0.875, 1.0)`,
`P13_ALPHA_BG_QUANTILE = 0.05` (the strictly-positive background floor `b = quantile(vec(y), 0.05)`,
asserted `> 0`), `P13_ALPHA_MASK_RULE = :calculate_mask_ch1_once` (the frozen, already-shipped
`_calculate_mask(mci)[1]` Otsu rule, computed ONCE from the unmodified ch1 and held fixed across
the whole ladder), `P13_ALPHA_N_IMAGES = 64`, `P13_ALPHA_SUBSTRATE = :simulated`,
`P13_ALPHA_INVARIANT_RTOL = 1e-10`, and `P13_ALPHA_GATED = false`. Carry the trap in a comment:
`_exclude_zero` (`src/colocalization.jl:154-169`) drops any pixel where EITHER channel is zero, so
zeroing ch2 inside the mask DELETES those pixels instead of creating anti-correlation and the
ladder reads flat for the wrong reason.

**Section J — decoupling, deferrals and declared deviations.**
`P13_SRC_UNTOUCHED = true`, `P13_PHYSICAL_ANCHOR_DEFERRED_TO = "Phase 16"` (comment: the one real
segregated anchor is triply unavailable — `sha256 = "PENDING-FETCH"`, `corpus/data/` empty, and
`split = sealed_holdout` behind the anti-snooping seal; Phase 13 does NOT call
`open_sealed_holdout`), `P13_RESULTS_RENAME_DEFERRED = true` (the `log_bf_simplex` misnomer at
`src/results.jl:172` is recorded, not edited), and `P13_DECLARED_DEVIATIONS` as a 4-tuple of
strings naming exactly: a custom `ThreeWayEvidenceNet <: NeuralEstimator` subtype instead of
`RatioEstimator`; a single `Dense(num_summaries, 2)` two-logit head; a masked two-term BCE loss
with per-head participation weights; and a per-head evidence-scale correction that is NOT the
binary `- log_prior_odds` term.

**Executable self-checks** at the end of the guard block:
`@assert !(UInt64(P13_DEV_SEED) in _p13_forbidden())`;
`@assert !(UInt64(P13_FIX_SEED) in _p13_forbidden())`;
`@assert UInt64(P13_DEV_SEED) != UInt64(P13_FIX_SEED)`;
`@assert !(P13_SALT in P13_REPO_SALTS)`;
`@assert issorted(P13_TAU_DELTA_GRID) && first(P13_TAU_DELTA_GRID) > 0.0`;
`@assert issorted(P13_ALPHA_LADDER) && first(P13_ALPHA_LADDER) == 0.0 && last(P13_ALPHA_LADDER) == 1.0`;
`@assert sum(values(P13_TARGET_CLASS_FREQ)) == 1`;
`@assert sum(P13_IMSIZE_WEIGHTS) ≈ 1.0`;
`@assert P13_ECE_GREEN < P13_ECE_YELLOW`;
`@assert P13_GATE_STATISTIC === :ece`;
`@assert !isdefined(@__MODULE__, :P13_TAU)` with the message that Tier 2 must not be present in the
Tier-1 commit (this assertion is REMOVED by plan 13-10 only in the sense that the Tier-2 block is
appended below it — the executor of 13-10 must relocate it, and this file states that requirement
in a comment).
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/p13/consts.jl"); println("tier1 ok ", P13_DEV_SEED)'</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike -e 'include("spike/p13/consts.jl"); println("tier1 ok")'` prints `tier1 ok` and exits 0.
    - `grep -v '^\s*#' spike/p13/consts.jl | grep -c 'if !isdefined' ` outputs `1` (Tier 2 adds the second block later).
    - `julia --project=spike -e 'include("spike/p13/consts.jl"); @assert !isdefined(Main, :P13_TAU); @assert P13_ITERATION_ALLOWANCE == 1; @assert P13_CUT_VARIANT === :tau_contrast; @assert P13_STRATIFICATION === :class_frequency; @assert P13_GATE_STATISTIC === :ece; println("ok")'` prints `ok`.
    - `julia --project=spike -e 'include("spike/p13/consts.jl"); try p13_tau(); error("should have thrown") catch e; @assert occursin("Tier-2", sprint(showerror, e)) || occursin("Tier 2", sprint(showerror, e)); println("tau tripwire ok") end'` prints `tau tripwire ok`.
    - `grep -c 'gate_consts_8_v2' spike/p13/consts.jl` is at least `1` AND `grep -v '^\s*#' spike/p13/consts.jl | grep -c '(1376, 1028)'` outputs `0` (the F5 mixture is read, never retyped).
    - `git diff --quiet HEAD -- src/` exits 0 and `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>Every Phase-13 threshold, ladder, seed, statistic and design choice is committed as an executable constant; the DEV and fixture seeds are proven disjoint from the recomputed gate seeds; tau does not exist yet and calling `p13_tau()` fails loudly.</done>
</task>

<task type="auto">
  <name>Task 2: Write the pre-registration assertion test</name>
  <files>spike/test/test_p13_consts.jl</files>
  <read_first>
    - `spike/test/test_bf.jl` lines 26-37 and 91-123 — the fixture-stream rule, the
      assert-the-pre-registration-is-locked testset shape, and the CPU-only postscript.
    - `spike/test/test_sbc.jl` lines 98-116 — the pre-registration testset and CUDA-absence clause.
    - `13-PATTERNS.md` section `spike/test/test_p13.jl` (lines 761-864) — the guarded include,
      fixtures-computed-once, literal-assertion and source-grep conventions.
    - `13-VALIDATION.md` lines 52-72 — the per-task verification map rows this test satisfies.
    - `spike/p13/consts.jl` — the file just written (the values being asserted).
  </read_first>
  <action>
Create `spike/test/test_p13_consts.jl`: AGPL header, the file-purpose banner naming D-01/D-04,
`using Test`, and a guarded
`isdefined(@__MODULE__, :P13_DEV_SEED) || include(joinpath(@__DIR__, "..", "p13", "consts.jl"))`.

One outer `@testset "P13 Tier-1 pre-registration (D-01, D-04)" verbose = true` with nested
testsets. Assert the locked values **as literals**, so a post-hoc edit breaks the suite loudly:

1. `"seeds are fresh and disjoint"` — `P13_DEV_SEED == 0x0000_0000_0B13_DE71`,
   `P13_FIX_SEED == 0x0000_0000_0B13_F1F7`, `P13_SALT == 0x2545_F491_4F6C_DD1D`;
   `!(UInt64(P13_DEV_SEED) in _p13_forbidden())` and the same for `P13_FIX_SEED`; pairwise
   inequality against `VAL_MASTER_SEED`, `NPE_MASTER_SEED`, `VAL_FIX_SEED`, `RATIO_PAIR_SEED`,
   `CORPUS_MASTER_SEED`, `DEFAULT_MASTER_SEED`, `P11_DEV_SEED`; `!(P13_SALT in P13_REPO_SALTS)`;
   and that `_p13_forbidden()` contains at least one value from each of `_GC.PROD_SEED` and
   `_GC.PROD_SEED_V2` (proving the recompute path ran, not a comment).
2. `"streams are counter-separated"` — `p13_rng(P13_TAU_COUNTER)` and
   `p13_rng(P13_DATAGEN_COUNTER)` produce different first draws; `p13_fix_rng(P13_FIXTURE_COUNTER)`
   differs from `p13_rng(P13_FIXTURE_COUNTER)` (the fixture stream never overlaps the reported one).
3. `"D-05 cut variant is locked"` — `P13_CUT_VARIANT === :tau_contrast`.
4. `"D-06 probe spec is locked"` — the delta grid tuple as a literal, `P13_TAU_AUC == 0.90`,
   `P13_TAU_R == 400`, `P13_TAU_DESIGN === :unpaired`, `P13_TAU_BOTH_DIRECTIONS == true`,
   `P13_TAU_ABORT_EXTEND_GRID == false`, `P13_TAU_REQUIRE_POST_P11_SIMULATOR == true`,
   `P13_TAU_REFERENCE_LAMBDA_RULE === :widest_rung`.
5. `"tau is Tier-2 and absent"` — `!isdefined(@__MODULE__, :P13_TAU)` and
   `@test_throws ErrorException p13_tau()`. Add an in-test comment that plan 13-10 flips this
   testset to assert the measured value instead, in a separately-committed edit.
6. `"D-07 design and bars are locked"` — `P13_STRATIFICATION === :class_frequency`,
   `P13_STRATIFICATION_FALLBACK === :importance_weighted`, `P13_ITERATION_ALLOWANCE == 1`,
   `occursin("high", lowercase(P13_ITERATION_TRIGGER))` and that the trigger string is non-empty,
   `P13_F5_CORR_MIN == 0.99`, `P13_F5_MAXABS_TOL == 0.25`, `P13_F5_NEGCTRL_REQUIRED == true`,
   `P13_F5_SKEW_FREQ == (0.60, 0.25, 0.15)`.
7. `"D-09/D-10 recipe is copied verbatim"` — `P13_SUMMARY_WIDTH == 256`,
   `P13_NUM_SUMMARIES == 64`, `P13_TRAIN_N == 48_000`, `P13_EPOCHS == 300`,
   `P13_BATCHSIZE == 128`, `P13_LR == 2.5e-4`, `P13_WEIGHT_DECAY == 1e-4`,
   `P13_VAL_FRAC == 0.15`, `P13_STOPPING_EPOCHS == 40`, `P13_USE_GPU == false`, and
   `P13_LAMBDA_PLACEMENT === :append_after_pair_encode`.
8. `"D-12/D-13 gate numbers are locked"` — `P13_GATE_M == 4000`,
   `P13_AUC_FLOOR_COLOC == 0.90`, `P13_AUC_FLOOR_EXCLUSION == 0.90`,
   `P13_CONFUSION_RULE === :argmax_descriptive`, `P13_CONTINUITY_GATED == false`,
   `P13_ECE_GREEN == 0.05`, `P13_ECE_YELLOW == 0.10`, `P13_ECE_NBINS == 10`,
   `P13_GATE_STATISTIC === :ece`, `P13_MIN_EVAL_PER_HEAD == 1000`,
   `P13_VACUOUS_AUC_FLOOR == 0.60`, `P13_TOST_REQUIRED == false`.
9. `"D-16 alpha ladder is locked"` — the ladder tuple as a literal,
   `P13_ALPHA_BG_QUANTILE == 0.05`, `P13_ALPHA_MASK_RULE === :calculate_mask_ch1_once`,
   `P13_ALPHA_GATED == false`.
10. `"F5 imsize mixture was READ not retyped"` — `P13_IMSIZE_SET == _GC.SBC_IMSIZE_SET` and
    `sum(P13_IMSIZE_WEIGHTS) ≈ 1.0`, plus a source grep on `spike/p13/consts.jl` asserting the
    literal `(1376, 1028)` does not appear outside comments. Read the source once into a
    `const P13_CONSTS_SRC` at module level (the `test_bf.jl:67` idiom) and filter comment lines
    before counting.
11. `"deferrals and deviations are on the record"` — `P13_SRC_UNTOUCHED == true`,
    `P13_PHYSICAL_ANCHOR_DEFERRED_TO == "Phase 16"`, `P13_RESULTS_RENAME_DEFERRED == true`,
    `length(P13_DECLARED_DEVIATIONS) == 4`.
12. `"P13 ran CPU-only (D-10)"` — copy the CUDA-absence clause from `spike/test/test_bf.jl:121-123`.

The source-grep constant must strip comment lines with a `filter(l -> !startswith(strip(l), "#"),
split(P13_CONSTS_SRC, '\n'))` step before any `count`/`occursin` assertion, so the file's own
explanatory prose cannot invalidate its gate.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_consts.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_consts.jl` exits 0 with zero failures and zero errors.
    - The test file contains a `@test_throws` on `p13_tau()` (verifiable by `grep -c 'p13_tau' spike/test/test_p13_consts.jl` >= 1 and the suite passing while `P13_TAU` is undefined).
    - `grep -c 'startswith(strip' spike/test/test_p13_consts.jl` outputs at least `1` (comment-stripping before any source-grep assertion).
    - `julia --project=spike -e 'include("spike/test/test_p13_consts.jl")'` exits 0 (the file is includable from the harness, not only runnable standalone).
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>Every locked constant is asserted as a literal, so any later edit fails the suite loudly; the seed disjointness proof runs against recomputed gate seeds; the Tier-2 tau slot is proven empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration file -> reported run | This file is the scientific-integrity artifact; a snooped seed or a post-hoc threshold edit silently invalidates every downstream Phase-13 number |
| frozen gate consts -> Phase-13 consts | `test/gate/gate_consts_8_v2.jl` is read to recompute derived seeds; a write or a gate run there would corrupt a frozen pre-registration belonging to another phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Repudiation (data snooping) | `P13_DEV_SEED` / `P13_FIX_SEED` choice | mitigate | Executable `_p13_forbidden()` assert over every named seed family plus the **recomputed** `PROD_SEED` / `PROD_SEED_V2`; re-asserted as literals in `test_p13_consts.jl` |
| T-13-02 | Tampering | Post-hoc edit of a locked threshold | mitigate | Every locked value asserted as a literal in `test_p13_consts.jl`; Tier 2 is a separate append-only guard block, never an edit |
| T-13-03 | Tampering | Frozen `gate_consts_8_v2.jl` read path | mitigate | Isolated `module _GC` include only; no gate run, no write; acceptance criteria assert `git diff --quiet` over `src/` and the manifests |
| T-13-06 | Tampering | Path construction | mitigate | Every path is `joinpath(@__DIR__, ...)`; no interpolated strings (ASVS V5) |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** 13-RESEARCH §Package Legitimacy Audit records that every library is already resolved and pinned; acceptance criteria assert `spike/Project.toml` and `spike/Manifest.toml` byte-unchanged |
</threat_model>

<verification>
1. `julia --project=spike -e 'include("spike/p13/consts.jl"); println("tier1 ok")'` prints `tier1 ok`.
2. `julia --project=spike spike/test/test_p13_consts.jl` exits 0.
3. `git diff --quiet HEAD -- src/` exits 0.
4. `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml` exits 0.
5. `julia --project=spike -e 'include("spike/p13/consts.jl"); @assert !isdefined(Main, :P13_TAU); println("tier2 absent")'` prints `tier2 absent`.
</verification>

<success_criteria>
- `spike/p13/consts.jl` exists with exactly one `if !isdefined` guard block and self-asserts seed and
  salt disjointness at include time.
- `P13_TAU` is absent and `p13_tau()` errors with a Tier-2 / Phase-11 pointer.
- Every D-05/D-06/D-07/D-09/D-10/D-12/D-13/D-16 constant enumerated in the task action exists and is
  asserted as a literal by `spike/test/test_p13_consts.jl`, which exits 0.
- The F5 imsize mixture is read from the frozen gate file, not retyped.
- No `src/` byte and no manifest byte changed.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-01-SUMMARY.md` when done,
including a `## Locked Tier-1 Constants` section listing every constant name with its committed
value and the commit sha of the consts file.
</output>

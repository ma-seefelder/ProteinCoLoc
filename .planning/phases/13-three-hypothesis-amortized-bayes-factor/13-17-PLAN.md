---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 17
type: execute
wave: 10
depends_on: ["13-14", "13-16"]
files_modified:
  - spike/p13/consts.jl
  - spike/p13/consts_d15_amendment.jl
  - spike/p13/run_p13_realimage.jl
  - spike/p13/real_images.jl
  - spike/p13/realimage_report.jld2
  - spike/figures/p13_realimage.png
  - spike/test/test_p13_consts.jl
  - spike/test/test_p13_real.jl
  - .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md
  - .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-15-SUMMARY.md
  - .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-16-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [D-15, D-16, D-01, D-04]

must_haves:
  truths:
    - "(D-15 AMENDED) The operative real-image channel pair is (2, 3) -- green/red, the two target proteins -- and no Phase-13 executable reads a pair containing the DAPI/Hoechst nuclear counterstain for any colocalization claim"
    - "(D-15 AMENDED) The redundancy arm no longer exists anywhere: not as a constant read, not as a printed section, not as a persisted artifact field, not as a returned field, and not as a report paragraph -- with c1 excluded and only three channels present there is no second pair"
    - "(D-15 AMENDED) The colocalization anchors the ingestion regresses against are the UNMASKED 0.4603 (positive) / 0.3815 (negative), because patch_summary applies no Otsu mask and the net was trained on unmasked summaries; the masked +0.8238 / -0.0338 figures appear nowhere except in prose stating they are forbidden and why"
    - "(D-04) No test, assertion or acceptance criterion makes reproducing a SUPERSEDED value a pass condition for a colocalization claim, and no new bar-shaped P13_REAL_* name exists: the not-a-gate exemption set is still exactly {P13_REAL_ANCHOR_TOL, P13_REAL_OOD_SHIPPED_THRESHOLD}"
    - "(D-04) No gating threshold moves. Every AUC floor, ECE band, tolerance, ladder, seed, counter and P13_ITERATION_ALLOWANCE is byte-unchanged, and the allowance remains 1 of 1 UNSPENT"
    - "(D-01) The re-run consumes NO RNG stream, so no reserved counter can collide: the tau probe (1), the training pool (2), the gate (3), the alpha series (4) and the continuity check (5) are all untouched, and the runner re-asserts its own no-stream property from its source text"
    - "(D-01) The phase's only gating arm is untouched: spike/p13/gate_report.jld2 and spike/p13/alpha_report.jld2 are byte-unchanged and neither run_three_way_gate.jl nor run_alpha_series.jl is edited in any hunk other than the Task-1 mechanism ruling permits"
    - "(D-15) 13-REPORT.md answers IN WORDS whether the conclusion changes, quotes every amended number beside its superseded counterpart with the pair named for each, and reports the OOD headline as a CHANGE from 417.2974 vs 167.5446 (2.491x) rather than restating it"
    - "(D-15 AMENDED) No surface a reader reaches still presents a superseded-pair number as current. spike/p13/real_images.jl's header blocks (b2) and (e) and its real_mbar docstring state what is OPERATIVE under the ruled mechanism, as a comment-only edit; and .planning/ROADMAP.md's Phase-13 row, 13-16 bullet and Verdict block mark 417.30 / 167.54 / 2.49x, 433.69 / 179.14 / 2.42x, alpha_star_real 0.875 / 0.625 and 'RANDOM on both pairs' as measured on the SUPERSEDED pair, with the amended figures printed beside them and the plan count reading 17 plans / 10 waves"
    - "(D-01) src/, spike/Project.toml, spike/Manifest.toml, test/, docs/ and corpus/ are byte-unchanged; the Phase-16 seal stays SHUT; spike/data/cache/p11/ is neither read nor written"
  artifacts:
    - path: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md"
      provides: "The pre-registration amendment: the four-fact defence, the before/after constants, the affected-site inventory, the authorisation-versus-run timeline, and the explicit no-threshold-moves statement"
      contains: "no gating threshold moves"
    - path: "spike/p13/run_p13_realimage.jl"
      provides: "The corrected real-image runner: primary arm on the amended pair, redundancy arm removed, superseded values printed beside amended ones"
      contains: "13-D15-AMENDMENT"
    - path: "spike/p13/realimage_report.jld2"
      provides: "The re-run reported artifact on the amended pair, every log-BF still on the same row as its OOD verdict and lambda, carrying both the amended and the superseded readings for comparison"
      contains: "is_gated"
    - path: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md"
      provides: "Revised real-image sections with the explicit does-the-conclusion-change answer and paired superseded/amended numbers"
      contains: "13-D15-AMENDMENT"
    - path: "spike/p13/real_images.jl"
      provides: "The ingestion header stating what is OPERATIVE rather than only what is frozen -- blocks (b2), (e) and the real_mbar docstring, comment-only"
      contains: "13-D15-AMENDMENT"
    - path: ".planning/ROADMAP.md"
      provides: "The phase's most-read summary surface, with every superseded real-image figure marked as such and the plan/wave count current"
      contains: "SUPERSEDED"
  key_links:
    - from: "spike/p13/run_p13_realimage.jl"
      to: "the amended channel-pair binding"
      via: "every real_pair / real_alpha_ladder / real_provenance call passing the amended pair explicitly"
      pattern: "P13_REAL_CHANNEL_PAIR"
    - from: "spike/test/test_p13_consts.jl"
      to: "the amended constants"
      via: "literal-value assertions on the amended pair and anchors, beside assertions that the superseded values remain citable"
      pattern: "0\\.4603|0\\.3815"
    - from: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md"
      to: ".planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md"
      via: "an explicit citation of the amendment beside every amended real-image number"
      pattern: "13-D15-AMENDMENT"
---

<objective>
Correct the real-image channel pair of the D-15 arm from the DAPI counterstain to the green/red
target pair, drop the redundancy arm, and re-run 13-16 on the corrected substrate.

Purpose: `13-01-PLAN.md` pre-registered the real arm on `P13_REAL_CHANNEL_PAIR = (1, 2)`, and
`test/runtests.jl:105` records the fixture channels as `["blue", "green", "red"]` — so `c1` is the
DAPI/Hoechst nuclear counterstain and the arm as executed measured counterstain-versus-protein
overlap, which is not colocalization. The upstream calibration source had **already** been corrected
to the c2/c3 pair on 2026-07-25 (`78dc37f`), **39 h 15 min** before `consts.jl` was written — and
the blocker naming Phase 13 specifically (`ea4a7d3`) landed **38 h 46 min 18 s** before it. This plan
repairs that drift under the amendment `13-D15-AMENDMENT.md` and measures the corrected arm.

**This plan buys no evidence.** Unmasked, the corrected fixtures separate by 0.0788 against 0.0811
before — marginally *worse*. It deletes an arm rather than adding one. The expected outcome is that
the conclusion does not change, and the plan requires that to be stated plainly if so, because a
limitation that survives its own correction is a stronger statement than one never tested.

Output: the amended constants, the corrected runner, a re-run `realimage_report.jld2`, updated
tests, revised real-image sections of `13-REPORT.md`, and superseding notes in `13-15-SUMMARY.md`
and `13-16-SUMMARY.md`.
</objective>

<plan_size_note>
This plan carries **five tasks**, above the 2-3 house norm. That is deliberate and the reason is
structural, not ambitious: tasks 2 and 3 must land together or the tree sits in a state where the
constants and their only consumer disagree and the suite fails; task 4 is the run those two exist to
enable; task 5 is the honesty obligation the run creates. Splitting would produce a provably-broken
intermediate commit on a branch where a Phase-12 executor is live. Task 1 is a blocking checkpoint
and consumes no context.
</plan_size_note>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-SC2-AMENDMENT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-16-SUMMARY.md

<interfaces>
Contracts the executor needs. Extracted from the working tree at `d7195cc`. Use directly — no
exploration required.

**The ingestion is pair-agnostic by construction** (`spike/p13/real_images.jl`). Every entry point
takes the pair as a keyword; nothing bakes it in beyond the default value:
```julia
real_tif(cond::AbstractString, i::Integer)                       # validates i in 1:3, builds <cond>_c<i>.tif
load_real(cond; channels = P13_REAL_CHANNEL_PAIR)                # names = String["ch$i" for i in channels]
real_pair(cond; channels = P13_REAL_CHANNEL_PAIR)                # asserts length(channels) == 2
real_mbar(cond; channels = P13_REAL_CHANNEL_PAIR, G = 8)         # G == 8 or throws
real_mbar_of(pair)                                               # pair-typed, no channel knowledge
real_alpha_ladder(cond; channels = P13_REAL_CHANNEL_PAIR, grid = P13_REAL_ALPHA_GRID)
real_provenance(cond; channels = P13_REAL_CHANNEL_PAIR)
real_grid_provenance(; G = 8, imsize = P13_REAL_IMSIZE)
verify_real_readonly_digest()                                    # sha256 of `git ls-files -s test/test_images`
```
All six fixture files exist: `test/test_images/{positive,negative}/{positive,negative}_c{1,2,3}.tif`.

**The runner's own sweep helper is already parameterized on the pair**
(`spike/p13/run_p13_realimage.jl:665`):
```julia
function sweep_rows(arm, channels)   # returns (rows = Vector{NamedTuple}, ...)
```
so removing the redundancy arm is deleting its call and its section, not a refactor.

**The train-time provenance guard that constrains task 2** (`spike/p13/net.jl:472, 524`; asserted at
`run_p13_realimage.jl:634`, `run_three_way_gate.jl:353`, `run_alpha_series.jl:451`):
```julia
p13_consts_sha() = bytes2hex(SHA.sha256(read(joinpath(@__DIR__, "consts.jl"))))
@assert h.consts_sha == p13_consts_sha() "the frozen pre-registration changed since the net was trained: ..."
```
`h` is the **trained net artifact from 13-11**. Editing `consts.jl` in place therefore aborts this
runner at load and makes the gate and alpha runners un-reproducible against the same net.
`spike/test/test_p13_net.jl:380` is NOT affected — it round-trips a net saved inside the test, so its
sha is recomputed at save time.

**The not-a-gate set-equality assertion that must keep passing unchanged**
(`spike/test/test_p13_consts.jl:272-274`, over the comment-stripped source):
```julia
bar_names = Set(m.match for m in eachmatch(
    r"\bP13_REAL_[A-Z_]*(?:FLOOR|THRESHOLD|MIN|MAX|TOL)\b", P13_CONSTS_CODE))
@test bar_names == Set(["P13_REAL_ANCHOR_TOL", "P13_REAL_OOD_SHIPPED_THRESHOLD"])
```

**The shipped-OOD comparison selector is pair-independent** (`run_p13_realimage.jl:352`): it selects
the bundle whose own recorded `id_threshold` reproduces `P13_REAL_OOD_SHIPPED_THRESHOLD = 179.14` —
a property of the bundle, not of the pair. But `P13_REAL_OOD_SHIPPED_DENSITY = 433.69` **is** a
measurement on the superseded pair, and `:741` falls back to it when no bundle is readable.

**The reserved RNG counters** (`spike/p13/consts.jl:204-209`), none of which this arm touches:
```
P13_TAU_COUNTER = 1   P13_DATAGEN_COUNTER = 2   P13_GATE_COUNTER = 3
P13_ALPHA_COUNTER = 4   P13_CONTINUITY_COUNTER = 5   P13_FIXTURE_COUNTER = 99
```
</interfaces>
</context>

<binding_constraints>
Non-negotiable, each separately checkable:

1. **Do NOT touch the gating arm.** `spike/p13/run_three_way_gate.jl`, `spike/p13/gate_report.jld2`,
   `spike/p13/run_alpha_series.jl`, `spike/p13/alpha_report.jld2`, `13-12-*` and `13-13-*` are out
   of scope and must be byte-unchanged at the end (the sole exception being the guard hunk that
   option M1 of Task 1 would require, if M1 is chosen). Grep-verified precondition: those runners
   contain **zero** references to `P13_REAL_CHANNEL_PAIR` or `P13_REAL_REDUNDANCY_PAIR`.
2. **Do NOT reopen the Phase-7 ship gate.** `artifacts/amended_v2/`, `artifacts/grid_8/`,
   `docs/amortized.md` and `src/` are untouched. The Phase-16 seal stays SHUT:
   `open_sealed_holdout` is not called, no path under the sealed provenance tree is constructed, and
   `corpus/` is byte-unchanged.
3. **No threshold moves, no allowance is spent, no net is retrained, no weight changes.**
   `P13_ITERATION_ALLOWANCE` must read 1 of 1 UNSPENT at the end.
4. **`spike/data/cache/p11/` is READ-ONLY** (a Phase-12 plan depends on it) and this arm neither
   reads nor writes it. Do not create a bulk cache directory.
5. **Commits use explicit paths only:** `git commit -m "..." -- <paths>`. Never bare `git commit`,
   never `-a`, never `--no-verify`. **A Phase-12 executor is LIVE on this branch.**
6. **STATE.md writes are ADDITIVE only.** Phase 11's closed entry and Phase 12's live entry must
   survive byte-unchanged. Do **not** run `bm-sdk query state.begin-phase` or any mutating state
   handler; hand-edit additively, re-reading STATE.md immediately before writing.
7. **Copy commit hashes from git output; never retype them.**
8. **If anything beyond the amendment's §5 needs deciding, STOP** and write the blocker to STATE.md
   additively. Do not resolve a pre-registration question inside execution.
</binding_constraints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Rule on WHERE the amended constants live</name>
  <decision>
Does the amendment edit `spike/p13/consts.jl` in place (M1), or land beside it in a new, separately
named `spike/p13/consts_d15_amendment.jl` (M2)?
  </decision>
  <context>
The user's ruling settles **what** the values are: pair `(2, 3)`, redundancy dropped, unmasked
`0.4603 / 0.3815`. It does not settle **where they live**, and the planner found a structural fact
that makes this a real choice rather than a formality.

`spike/p13/run_p13_realimage.jl:634` asserts `h.consts_sha == p13_consts_sha()`, where `h` is the
trained net artifact from 13-11 and `consts_sha` was recorded at train time. The identical assertion
sits at `run_three_way_gate.jl:353` (**the gate**) and `run_alpha_series.jl:451`.

**Consequence: editing `consts.jl` in place makes the 13-16 re-run abort at load**, and
simultaneously makes the gate and alpha runners un-reproducible against their own net. Retraining is
not available — no further training is authorised, and retraining would invalidate 13-12's gate
result.

This is a decision about the provenance narrative of a frozen pre-registration, which is the asset
this project's credibility rests on. It is escalated rather than resolved. See
`13-D15-AMENDMENT.md` §7.
  </context>
  <options>
    <option id="M1-amend-in-place">
      <name>M1 — edit `spike/p13/consts.jl` directly</name>
      <pros>
        One file is the pre-registration; a reader finds the operative pair where they expect it. No
        second module to keep in sync. Matches the plain reading of "amend the pre-registration".
      </pros>
      <cons>
        Requires widening the train-time sha guard in all three runners to accept the recorded
        pre-amendment sha — i.e. touching an integrity guard in order to produce a result, which is
        the shape this project is most careful about. Retires the standing claim in `13-REPORT.md`
        §13 that `consts.jl` has two commits in its whole history, neither postdating a result.
        Makes `test_p13_consts.jl:235-242` assert values that no longer exist, so those assertions
        must be rewritten rather than relabelled. The widening is defensible on the merits —
        training reads **no** `P13_REAL_*` constant, grep-verified — but it is still a guard being
        loosened after a result exists. **And it carries three further obligations, B1-B3 below,
        that M2 does not — read them before choosing; they are M1's real price.**
      </cons>
    </option>
    <option id="M2-amend-beside">
      <name>M2 — new `spike/p13/consts_d15_amendment.jl`, `consts.jl` byte-unchanged</name>
      <pros>
        Every sha guard keeps holding untouched, so the 13-16 re-run works and the **gating arm's
        reproducibility is untouched**. The superseded pre-registration stays byte-present and
        citable, which is exactly the discipline `13-SC2-AMENDMENT.md` §6 requires ("The original is
        not erased"). `test_p13_consts.jl:235-242` keeps asserting the frozen values **truthfully**,
        relabelled as a superseded-pair record, with the amended values asserted in a new testset —
        so nothing that currently passes is deleted. Mirrors the SC2 precedent, which is explicitly
        "a second, separately named pre-registration". Zero blast radius outside the real arm.
      </cons>
      <cons>
        Two files must be read to know the operative pair. The amendment module must carry an
        explicit "this supersedes `consts.jl:552-576`" header or it becomes a trap of its own, and
        `consts.jl` §I2 cannot carry a pointer comment because it stays byte-unchanged — so the
        pointer has to live in the runner banner and in the tests instead.
      </cons>
    </option>
  </options>
  <retraction_this_plan_s_own_claim_was_FALSE>
**This task's resume-signal previously read: *"Tasks 2-5 name their M1/M2 deltas explicitly; nothing
else in this plan changes with the answer."* THAT CLAIM IS FALSE AND IS RETRACTED HERE.** An
independent plan-check found three further obligations that fire under **M1 only** and were written
down nowhere. They are recorded now, **unresolved**, so M1's cost is visible AT the moment of
choosing rather than discovered mid-execution. This materially raises M1's price and the user is
entitled to see it with the question.

**(B1) `13-REPORT.md` §13 needs an M1-conditional rewrite — and Task 5 currently forbids exactly
that.** Task 5 confines its edits to the real-image material and orders everything else
byte-unchanged. But §13 `Decoupling evidence` (`13-REPORT.md:997-1046`) carries integrity rows that
become **verifiably false the instant `consts.jl` is edited**:

| §13 row | Reads today | Under M1 |
|---|---|---|
| `git log --oneline -- spike/p13/consts.jl` | *"two commits only: `c42cc8e` … and `bfba6ac` … **No commit that produced a Phase-13 result touched it.**"* | **false** — a third commit exists and it postdates 13-16 |
| `git rev-parse HEAD:spike/p13/consts.jl` | `70fe66df33832af2e648ed3f81cc3ad356ce4cd8` | **false** — the blob changes |
| `git hash-object` → `5a4ea222…` and `sha256(…)` → `100e97a3…`, *"matches `consts_sha` and `net_consts_sha` in every artifact"* | both stated as matching | **false** — both change, and that stated match is precisely what M1's guard widening breaks |

Under M1, §13 must be rewritten with the new hashes, the third commit named as the amendment, and
the guard widening disclosed **in the section that exists to prove nothing was tampered with**.
Under M2 §13 stays byte-correct and byte-unchanged, which is why Task 5 can order it left alone.

**(B2) `spike/test/test_p13_real.jl:144-146` must be pinned to `channels = (1, 2)` explicitly,
against literal `0.3292` / `0.2481`.** That assertion is the c1/c2 read-chain lineage proof and
Task 2 orders it **kept**. It calls `real_mbar("positive")` **with no `channels` keyword**, riding
the default `P13_REAL_CHANNEL_PAIR`, and compares against `P13_REAL_ANCHOR_MBAR`. Under M1 **both
sides move together**: the call silently becomes a c2/c3 read compared against the c2/c3 anchors. It
would **still pass**. The executor would believe the lineage check was kept, exactly as instructed,
while the c1/c2 read-chain identity proof was **silently destroyed** — a green test asserting
something other than what its own comment says. The fix is to pin both sides
(`real_mbar("positive"; channels = (1, 2))` against literal `0.3292`), but that is an M1-only edit
and appears nowhere in this plan. Under M2 the default still resolves to `(1, 2)` and the assertion
keeps meaning what it says.

**(B3) The no-threshold-moved criterion must switch to `git diff -U0 | grep '^[+-]'`.** Task 2's
acceptance criterion greps `git diff HEAD -- spike/p13/consts.jl` for threshold-shaped identifiers
and requires `0`. `P13_REAL_ANCHOR_TOL` sits at `consts.jl:579`, **three lines** from
`P13_REAL_ANCHOR_MBAR` at `:576` — inside default diff context. Amending `:576` therefore prints
`ANCHOR_TOL` as a **context** line, the grep counts it, and **the criterion fails on correct work**,
which trains an executor to override the one check that proves no bar moved. Under M1 it must read
`git diff -U0 HEAD -- spike/p13/consts.jl | grep '^[+-]' | grep -cE '…'`. Under M2 the criterion is
vacuous (`consts.jl` is byte-unchanged) and needs no change.

**These are recorded, not resolved, and must NOT be pre-fixed.** Under M2 all three are no-ops, and
pre-fixing them would be pre-deciding this question. **If `M1-amend-in-place` is selected, the
executor MUST fold B1, B2 and B3 into Tasks 2 and 5 before starting any edit, and MUST STOP and
escalate if any of the three cannot be satisfied.** If `M2-amend-beside` is selected, note in the
summary that all three lapsed and why.
  </retraction_this_plan_s_own_claim_was_FALSE>
  <planner_reading>
M2 dominates on every constraint this plan is bound by, and the planner deliberately did not adopt
it. Under M2 the phrase "consts.jl byte-unchanged" — asserted by 13-11, 13-12, 13-13 and 13-16 alike
— remains true, and the correction costs the gating arm nothing at all. **B1-B3 above widen that
gap further; they are stated as facts about M1's cost, not as an argument for M2, and the choice
remains the user's.**
  </planner_reading>
  <files>none — this task writes nothing</files>
  <action>
Present the two options above verbatim, with the `run_p13_realimage.jl:634` finding and its
consequence stated first, and STOP. Write nothing, edit nothing, run nothing until the ruling is
given. Record the ruling verbatim in the plan summary, including which option was chosen and any
reasoning the user supplies, so the mechanism is auditable afterwards rather than inferred from the
resulting diff. **Do not resolve this by engineering judgement**: it is a decision about the
provenance narrative of a frozen pre-registration.
  </action>
  <verify>
    <human-check>The ruling is recorded verbatim in the session and repeated in 13-17-SUMMARY.md; no file was modified before it was given (`git status --porcelain` unchanged from the pre-task snapshot).</human-check>
  </verify>
  <done>The user has selected `M1-amend-in-place` or `M2-amend-beside`, the ruling is recorded verbatim, and no file was touched before it was given.</done>
  <resume-signal>Select: `M1-amend-in-place` or `M2-amend-beside`. Tasks 2-5 name their M1/M2 deltas explicitly, **and M1 additionally requires B1, B2 and B3 above** — an M1-conditional rewrite of `13-REPORT.md` §13, an explicit `channels = (1, 2)` pin on `test_p13_real.jl:144-146`, and a `git diff -U0` form of the no-threshold-moved criterion. M2 requires none of the three. Read that block before choosing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the amendment to the constants and to the tests</name>
  <files>spike/p13/consts.jl (M1) OR spike/p13/consts_d15_amendment.jl (M2), spike/test/test_p13_consts.jl, spike/test/test_p13_real.jl</files>
  <read_first>
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md` §5 and §6 —
      the exact before/after values and the full affected-site inventory. This is the binding source.
    - `spike/p13/consts.jl` lines 524-645 (section I2) and 699-715 (the D-15 self-checks) — the block
      being amended and the assertions that pin it.
    - `spike/test/test_p13_consts.jl` lines 225-275 — the `"D-15 real-image arm knobs are locked"`
      testset, including the not-a-gate set-equality assertion at 272-274.
    - `spike/test/test_p13_real.jl` lines 130-150 and 300-330 — the anchor regression and the
      naming-correction assertions.
    - `spike/simulator/ghat.jl` lines 58-89 — the corrected upstream this amendment adopts, and its
      unmasked/masked caveat paragraphs.
  </read_first>
  <action>
Bind the amended values, per the Task-1 ruling.

**The values, identical under M1 and M2** (`13-D15-AMENDMENT.md` §5.1): the operative channel pair is
`(2, 3)`; the redundancy pair is **dropped with no replacement**; the operative anchors are
`(positive = 0.4603, negative = 0.3815)`, **unmasked**; `P13_REAL_ANCHOR_TOL` stays `1e-3`.

**Under M1** — edit `consts.jl` section I2 in place: `:552` the pair, `:553` delete the redundancy
binding, `:576` the anchors, and the prose at `:482`, `:546-548`, `:630` plus the `:709` assertion
literal. Then widen the three train-time guards (`run_p13_realimage.jl:634`,
`run_three_way_gate.jl:353`, `run_alpha_series.jl:451`) to accept the recorded pre-amendment sha
alongside the current one, with an error message and an adjacent comment stating **why** the
widening is legitimate: training reads no `P13_REAL_*` constant, so the amended values are ones the
net never consumed. Record the pre-amendment sha as a named constant copied from command output,
never retyped.

**Under M2** — leave `consts.jl` **byte-unchanged** and create `spike/p13/consts_d15_amendment.jl`
carrying the AGPL header, a provenance banner naming `13-D15-AMENDMENT.md`, the date, the authorising
commits (`78dc37f` upstream correction, `f411ee7` user ruling), and an explicit statement that it
**supersedes `consts.jl:552-576` for every colocalization purpose while leaving that file
byte-present and citable**. Bind `P13_REAL_CHANNEL_PAIR_AMENDED`, `P13_REAL_ANCHOR_MBAR_AMENDED` and
a `P13_REAL_REDUNDANCY_DROPPED = true` sentinel whose comment carries the reason (`c1` excluded,
three channels, no second pair exists). Guard the include the house way
(`isdefined(@__MODULE__, :P13_REAL_CHANNEL_PAIR_AMENDED) || include(...)`) and assert at load that
the superseded bindings are still present and still hold their frozen values — the module proves the
original was not erased.

**Guard-symbol hazard, and it is live right now.** `spike/test/test_p13_consts.jl:42` guards on
`P13_DEV_SEED`, and `spike/validation/p12_consts.jl:109` legitimately MIRRORS that name — so in a
full-suite run the guard sees the mirror, skips its include, and ~114 Phase-13 constants go
undefined, aborting `runtests.jl:220`. That is an OPEN blocker this plan does not fix, but it is the
exact trap to avoid here: **guard the amendment module on a symbol no other phase mirrors**
(`P13_REAL_CHANNEL_PAIR_AMENDED` qualifies; a bare seed or version name does not), and never on a
name that appears in `spike/validation/p12_consts.jl`. `spike/p13/preconditions.jl:121-135` records
the same bug class in the mirror direction.

**Both mechanisms, mandatory:**

- Record the FORBIDDEN masked figures `+0.8238 / −0.0338` in a comment with the reason from
  `13-D15-AMENDMENT.md` §5.3: `patch_summary` applies no Otsu mask (`src/amortized/summary.jl:54`
  matches), the net was trained on unmasked summaries, the fixtures are already OOD-flagged, and
  masking would push them further out. State that they must not be used **however much better they
  separate**. Naming the trap in the file is what stops a later reader adopting it.
- Record the honest consequence: unmasked separation is 0.0788 against 0.0811 before — the correction
  makes the arm honest, **not** strong.
- **Introduce NO new bar-shaped `P13_REAL_*` name.** No `_FLOOR`, `_THRESHOLD`, `_MIN`, `_MAX` or
  `_TOL` beyond the two existing exemptions.

**Tests.** In `spike/test/test_p13_consts.jl` update exactly four assertions and no others: `:235`,
`:236`, `:241-242`, `:257`.

- Under **M1**: rewrite them to the amended values and delete the redundancy assertion.
- Under **M2**: **keep all four asserting the frozen values** — they are now a record that the
  superseded pre-registration is intact — relabel them in an in-test comment as exactly that, and add
  a new testset `"D-15 channel-pair amendment (13-D15-AMENDMENT.md)"` asserting the amended bindings,
  `P13_REAL_REDUNDANCY_DROPPED == true`, and that the amendment module's own bar-shaped name set is
  empty.

In `spike/test/test_p13_real.jl`: keep `:144-146` (the c1/c2 read-chain identity check) and relabel it
in comment as a check on a **superseded** pair that is never a colocalization measurement; add a new
assertion that `real_mbar` at the **amended** pair reproduces `0.4603 / 0.3815` within
`P13_REAL_ANCHOR_TOL`. Re-point the `:314` naming-correction literal per the mechanism. **Under M1
see B1-B3 at Task 1: `:144-146` must additionally be given `channels = (1, 2)` explicitly against
the literal `0.3292` / `0.2481`, because under M1 both sides of that comparison move together, it
passes anyway, and the lineage proof is silently destroyed.**

**Keep the not-a-gate source gate pointed at the OPERATIVE constants — one line, in this task's own
file.** `spike/test/test_p13_real.jl:336` builds its `P13_REAL_*` name set from **`consts.jl`
alone**:

```
names = Set(String[m.match for m in eachmatch(r"P13_REAL_[A-Z0-9_]+", CONSTS_CODE)])
```

with `CONSTS_CODE = P13_CODE["consts.jl"]` at `:90`. But `P13_CODE` is already built at `:86-88`
over **every** `.jl` in `spike/p13/` by `readdir`, and `:241-242` states that design point in the
file's own words — *"DISCOVERED with readdir rather than enumerated, so a file added later cannot
slip past the gate by not being listed."* **Under M2 the operative constants move into exactly such
a later-added file, and `:336` stops seeing them**: the phase's strongest anti-gate assertion would
go blind to precisely the constants this amendment introduces. That is the same drift class
`13-D15-AMENDMENT.md` §2 exists to repair.

Fix it at the source, at `:90` and `:336`:

- at `:90`, beside `CONSTS_CODE`, add `AMENDED_CODE = get(P13_CODE, "consts_d15_amendment.jl", "")`
  — the `get` default keeps the line correct under **M1**, where no such file exists, so this edit
  is mechanism-independent and should be made either way;
- at `:336`, source the name set from `CONSTS_CODE * "\n" * AMENDED_CODE`.

**Do NOT substitute a hand-written testset in another file for this.** A hand-enumerated set is
strictly weaker than the readdir-discovered set-equality gate already present, and swapping a
discovered gate for an enumerated one is a downgrade even while the enumeration happens to be
complete. The new testset asked for above is an **addition** to this gate, not a replacement.

Do not touch any other file. Do not edit `13-SC2-AMENDMENT.md`, `13-RESEARCH.md` or
`13-VALIDATION.md`: they record prior measurements that were correct as prior measurements, and this
amendment supersedes them by reference (`13-D15-AMENDMENT.md` §6.4).
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_consts.jl &amp;&amp; julia --project=spike spike/test/test_p13_real.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_consts.jl` exits 0, and `julia --project=spike spike/test/test_p13_real.jl` exits 0.
    - The amended values are bound and readable: a one-liner including the operative module prints the pair `(2, 3)` and the anchors `0.4603` / `0.3815`.
    - `real_mbar` at the amended pair reproduces the amended anchors within `1e-3` on both conditions.
    - The not-a-gate exemption set is unchanged: `grep -v '^\s*#' spike/p13/consts.jl | grep -oE 'P13_REAL_[A-Z_]*(FLOOR|THRESHOLD|MIN|MAX|TOL)' | sort -u` outputs exactly `P13_REAL_ANCHOR_TOL` and `P13_REAL_OOD_SHIPPED_THRESHOLD`; the same command over the amendment module (M2) outputs nothing.
    - The not-a-gate source gate now covers the operative constants: `grep -c 'AMENDED_CODE' spike/test/test_p13_real.jl` outputs at least `2` (the `:90` definition and the `:336` use), and the `readdir`-discovered `P13_CODE` at `:86-88` is untouched — `git diff HEAD -- spike/test/test_p13_real.jl` shows no hunk in `:86-88`.
    - The forbidden masked figures appear only inside comments: `grep -v '^\s*#' <operative file> | grep -c '0.8238\|0.0338'` outputs `0`, while `grep -c '0.8238' <operative file>` is at least `1`.
    - Under M2 only: `git diff --quiet HEAD -- spike/p13/consts.jl` exits 0 (byte-unchanged), and `julia --project=spike -e 'include("spike/p13/consts_d15_amendment.jl"); @assert P13_REAL_CHANNEL_PAIR == (1,2); @assert P13_REAL_REDUNDANCY_DROPPED; println("supersession ok")'` prints `supersession ok`.
    - Under M1 only: the recorded pre-amendment sha constant was copied from command output, and all three widened guards name the reason in an adjacent comment.
    - No threshold moved: `git diff HEAD -- spike/p13/consts.jl | grep -cE 'AUC_FLOOR|ECE_GREEN|ECE_YELLOW|GATE_M|MIN_EVAL|VACUOUS|ITERATION_ALLOWANCE|_SEED|_COUNTER|ALPHA_LADDER|MASK_FRACTION_BOUNDS|ANCHOR_TOL|OOD_SHIPPED_THRESHOLD'` outputs `0`.
    - `git diff --quiet HEAD -- src/ test/ docs/ corpus/ spike/Project.toml spike/Manifest.toml` exits 0.
    - `git diff --quiet HEAD -- spike/p13/gate_report.jld2 spike/p13/alpha_report.jld2` exits 0.
  </acceptance_criteria>
  <done>The operative real-image pair is (2, 3) with unmasked anchors 0.4603 / 0.3815, the redundancy pair is dropped, the masked figures are recorded as forbidden with their reason, no threshold moved, no new bar-shaped name exists, and no test asserts a superseded value as a colocalization pass condition.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the runner, remove the redundancy arm, and de-stale the ingestion header</name>
  <files>spike/p13/run_p13_realimage.jl, spike/p13/real_images.jl</files>
  <read_first>
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md` §6.3 and §6.5
      — the full list of sites that break when the redundancy pair is dropped, and the two OOD
      knock-on notes.
    - `spike/p13/run_p13_realimage.jl` lines 28-118 (the explanatory header and its seal self-check
      sentinel), 560-700 (the locked-value banner and `sweep_rows`), 700-945 (the five printed
      sections), 946-1080 (the persisted artifact, the figure and the return value).
    - `13-16-SUMMARY.md` §1, §1b, §1c — what the superseded run reported, so each number can be
      printed beside its amended counterpart.
    - `spike/p13/real_images.jl` lines 28-72 (the lettered header blocks, in particular **(b2)** at
      `:39-49` and **(e)** at `:66-71`) and `:272-289` (the `real_mbar` docstring) — the three prose
      sites this task de-stales, and `13-D15-AMENDMENT.md` §6.4a, which assigns them.
    - `spike/p13/run_p13_realimage.jl` lines 130-140 — the guarded `include` block, where the M2
      amendment module has to be wired in.
  </read_first>
  <action>
Correct the runner to the amended pair and delete the redundancy arm. Read every occurrence; the
inventory in `13-D15-AMENDMENT.md` §6.3 is a starting point, not a contract.

**The include — name it, and guard it on the right symbol (M2 only).** Under **M2** the amended
bindings live in a separate file and this runner will not see them unless it includes them. The
guarded include block is `run_p13_realimage.jl:132-136` — **this task's file, not Task 2's** — so
the guard-symbol hazard Task 2 correctly flags applies **here, at this site**. Add, in that block,
in the house idiom, **before** the `real_images.jl` include at `:134` so the amended binding exists
before any consumer loads:

`isdefined(@__MODULE__, :P13_REAL_CHANNEL_PAIR_AMENDED) || include(joinpath(@__DIR__, "consts_d15_amendment.jl"))`

**Guard on `P13_REAL_CHANNEL_PAIR_AMENDED` and on nothing else.** That symbol appears in no other
phase and, decisively, not in `spike/validation/p12_consts.jl` — the file whose `P13_DEV_SEED`
mirror is currently aborting the full suite by making a guard skip its own include. Under **M1**
there is no new file and no new include: the existing `consts.jl` chain already carries the amended
values, and adding an include would invent a second source of truth for a value that has one.
A missing include would eventually surface as an `UndefVarError`, but a plan that leaves its own
dependency to a runtime error has not named it — so it is named here.

**The pair.** Re-point every read to the amended binding: `:724` (the primary sweep), `:734-735` (the
shipped-OOD comparison), `:797`, `:830-836`, `:852` (the alpha ladder and its persisted `channels`
field), `:1007-1008` (`real_provenance`), `:1018` (the persisted pair). Pass `channels` explicitly at
every call site rather than relying on a default, so the operative pair is visible at each use.

**The redundancy arm — delete, do not repoint.** Remove `:926-940` in full (the `[5/6]` progress
line, the `sweep_rows("redundancy", …)` call, its `print_sweep` and the paragraph), the `:588` banner
line, the `:1005` persisted `redundancy_channels` field, the `:1004` `redundancy_sweep` field and the
`:1069` returned field. Renumber the progress counters. In the artifact, replace the Section-5 fields
with a single explanatory record stating that the redundancy arm is **dropped, not deferred**, and
why: with `c1` excluded and only three channels present there is no second pair, and the frozen
`(1, 3)` was DAPI-versus-red measuring `−0.0925` masked on the *positive* fixture — noise, not
corroboration. **Deleting an arm is a reduction in what the phase reports and must read as one.**

**The header.** Rewrite the `:41-57` block. The naming correction stays and is **strengthened**: on
the amended pair the "negative" biological control measures `+0.3815`, i.e. *more* positively
correlated than the `+0.2481` previously quoted. The channel-pair caveat at `:49-57` — which
correctly declined to repair the pair mid-flight — is replaced by a statement that the pair has now
been corrected under an authorised, dated amendment, with a pointer to `13-D15-AMENDMENT.md`, and
that the executors' original refusal was the right call under the pre-registration as it then stood.
Do not delete the counterstain explanation; it is now the *reason* for the correction rather than the
statement of a limit. **The `===== END OF EXPLANATORY HEADER -- SEAL SELF-CHECK SENTINEL, DO NOT
REWORD =====` line at `:118` is not reworded.**

**The banner.** Print the amended pair as the operative one **and the superseded `(1, 2)` beside it**,
labelled SUPERSEDED, with the amendment named. A reader of the run log must be able to see which pair
was corrected away without opening another file.

**The OOD knock-ons (`13-D15-AMENDMENT.md` §6.5).** `P13_REAL_OOD_SHIPPED_THRESHOLD` is a property of
the bundle and stays the selector at `:352` — do not touch it. `P13_REAL_OOD_SHIPPED_DENSITY = 433.69`
**is** a measurement on the superseded pair: print it labelled as such, and if the `:741` no-bundle
fallback fires, label the quoted value a superseded-pair number in the printed table and in the
artifact. Print the superseded Phase-13 reading `417.2974 vs 167.5446 (2.491×)` beside the new one so
the delta is visible in the run log itself.

**Persist the comparison.** Add artifact fields recording the superseded pair, the superseded anchors,
the superseded OOD triple and a reference to `13-D15-AMENDMENT.md`, so the artifact alone is enough to
reconstruct what changed.

**The ingestion header (`spike/p13/real_images.jl`) — the same drift, one level down.** This file
was in no task's file list until this revision, and `13-D15-AMENDMENT.md` §6.4a now assigns it here.
Three prose blocks go stale on this amendment, differently under each mechanism:

- **`:39-49`, block (b2)** — `:40` opens `P13_REAL_CHANNEL_PAIR IS FROZEN AT c1/c2.` Under **M1**
  that sentence is **literally false in shipped source** the moment Task 2 lands. Under **M2** it
  stays literally true and becomes **misleading about what is operative**: a reader learns what is
  frozen and not what is read.
- **`:66-71`, block (e)** — records the c1/c2 anchors `0.3292 / 0.2481` as **THE LINEAGE CLAIM** of
  the whole file, unqualified as superseded.
- **`:277-283`**, the `real_mbar` docstring — restates the frozen lineage expectation in the same
  terms.

Edit all three, minimally:

- **(b2)** — retitle it from *a caveat that was declined* to *a correction that was made*.
  Under **M1**: state that `P13_REAL_CHANNEL_PAIR` **is amended to c2/c3** by `13-D15-AMENDMENT.md`
  (2026-07-29), that it **was** frozen at c1/c2, and that the frozen value is **superseded, not
  deleted** — still citable at `c42cc8e`.
  Under **M2**: state that `P13_REAL_CHANNEL_PAIR` **remains frozen at c1/c2 and byte-unchanged**,
  that it is **no longer the operative colocalization pair**, that
  `P13_REAL_CHANNEL_PAIR_AMENDED = (2, 3)` in `spike/p13/consts_d15_amendment.jl` supersedes it, and
  — in as many words — that **a reader who stops at this file will read the wrong pair**.
  Under both: keep the counterstain explanation **verbatim** (it is now the *reason* for the
  correction, not the statement of a limit), and keep the note that every entry point takes
  `channels` as a keyword — adding that this design is what made the correction a re-point rather
  than a rewrite.
- **(e)** — mark the c1/c2 anchors **SUPERSEDED as a colocalization reference** while keeping them,
  and **keep the lineage claim itself, which is unaffected**: what (e) asserts is the identity of
  the read chain, and that identity holds on whichever pair it is exercised on. Add the amended
  colocalization anchors `0.4603 / 0.3815` and name `13-D15-AMENDMENT.md`.
- **`:277-283`** — the same treatment in one added sentence.

**Change no executable line in this file.** The five `channels = P13_REAL_CHANNEL_PAIR` keyword
defaults at `:206, 237, 290, 371, 423` are **not** re-pointed: under M2 they cannot be, and under M1
they follow automatically. The runner passes `channels` explicitly at every call site (above), which
is what makes the operative pair visible at the point of use rather than inherited from a default.
`git diff` on this file must show comment and docstring lines only.

**What must NOT change.** The arm stays `P13_REAL_IS_GATED = false` / `P13_REAL_QUALITATIVE_ONLY =
true` with **zero** `@test` lines and no threshold on any real-image quantity. The read-only digest
before/after and its assertion at `:1060` stay. The seal self-check stays. The `rng_stream = "NONE"`
provenance record stays and stays true. `P13_ALPHA_MASK_FRACTION_BOUNDS` is **not** widened — see
task 4.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/p13/run_p13_realimage.jl"); println("runner loads")' &amp;&amp; julia --project=spike spike/test/test_p13_real.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike -e 'include("spike/p13/run_p13_realimage.jl"); println("ok")'` prints `ok` and triggers no reported run (the `abspath(PROGRAM_FILE) == @__FILE__` guard is intact).
    - The redundancy arm is gone from executable text: `grep -v '^\s*#' spike/p13/run_p13_realimage.jl | grep -c 'REDUNDANCY_PAIR\|redundancy_sweep\|redundancy_channels'` outputs `0`.
    - The arm is still not a gate: `grep -c '@test' spike/p13/run_p13_realimage.jl` outputs `0`.
    - The seal is still shut: `grep -v '^\s*#' spike/p13/run_p13_realimage.jl | grep -c 'open_sealed_holdout\|corpus'` outputs `0`, and `grep -c 'SEAL SELF-CHECK SENTINEL, DO NOT REWORD' spike/p13/run_p13_realimage.jl` outputs `1`.
    - The arm still consumes no RNG stream: `grep -v '^\s*#' spike/p13/run_p13_realimage.jl | grep -c 'Philox\|Random\.\|rand(\|seed!'` outputs `0`.
    - The amendment is cited: `grep -c '13-D15-AMENDMENT' spike/p13/run_p13_realimage.jl` is at least `1`, and `grep -c 'SUPERSEDED' spike/p13/run_p13_realimage.jl` is at least `1`.
    - `julia --project=spike spike/test/test_p13_real.jl` exits 0 — that suite discovers `spike/p13/` with `readdir` and source-greps every `.jl` in it, so it is the real check on the edited runner.
    - `spike/p13/real_images.jl` is a COMMENT-ONLY edit: `git diff -U0 HEAD -- spike/p13/real_images.jl | grep '^[+-]' | grep -v '^[+-][+-]' | grep -vcE '^[+-]\s*(#|$)'` outputs `0` — every changed line is a comment or blank. (Docstring bodies live inside `"""` blocks; if the count is non-zero solely because of docstring text, confirm by eye that no `function`, `const`, `channels =` or `@assert` line appears in the diff, and record that confirmation.)
    - The ingestion header states what is operative: `grep -c '13-D15-AMENDMENT' spike/p13/real_images.jl` is at least `1`, and `grep -c 'SUPERSEDED\|superseded' spike/p13/real_images.jl` is at least `2`.
    - The five keyword defaults are untouched: `grep -c 'channels = P13_REAL_CHANNEL_PAIR' spike/p13/real_images.jl` outputs `5` (M2) — under M1 the same count holds, the binding having moved beneath it.
    - Under **M2 only**: `grep -c 'consts_d15_amendment' spike/p13/run_p13_realimage.jl` is at least `1` and `grep -c 'P13_REAL_CHANNEL_PAIR_AMENDED' spike/p13/run_p13_realimage.jl` is at least `1`; the include sits **above** the `real_images.jl` include line. Under **M1 only**: `grep -c 'consts_d15_amendment' spike/p13/run_p13_realimage.jl` outputs `0`.
    - `git diff --quiet HEAD -- src/ test/ docs/ corpus/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>The runner reads the amended pair at every call site with the pair passed explicitly and (under M2) includes the amendment module under a guard symbol no other phase mirrors; the redundancy arm is deleted from the code, the banner, the artifact and the return value; the runner header states the correction and its authority without deleting the counterstain explanation; `spike/p13/real_images.jl`'s blocks (b2), (e) and the `real_mbar` docstring state what is OPERATIVE under the ruled mechanism as a comment-only edit; the superseded values are printed beside the amended ones; and the not-a-gate, no-stream, read-only and seal properties all still hold.</done>
</task>

<task type="auto">
  <name>Task 4: Re-run 13-16 on the corrected substrate and record every delta</name>
  <files>spike/p13/realimage_report.jld2, spike/figures/p13_realimage.png</files>
  <read_first>
    - `13-16-SUMMARY.md` §1 through §4 — every number the superseded run produced, so each one has a
      named counterpart in the re-run.
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md` §5.4 and §6.5
      — the mask-fraction consequence and the OOD-headline expectation.
  </read_first>
  <action>
Run the corrected arm **once**:

```
julia --project=spike spike/p13/run_p13_realimage.jl
```

on the **main working tree, no worktree** — the same ruling 13-11 through 13-16 executed under, and
for the same reasons.

**The stream, stated explicitly because this plan was asked for it.** This arm consumes **NO RNG
stream at all**: six committed files, a deterministic transform and a deterministic forward pass. The
reserved counters are `P13_TAU_COUNTER = 1`, `P13_DATAGEN_COUNTER = 2` (the training pool),
`P13_GATE_COUNTER = 3` (the gate), `P13_ALPHA_COUNTER = 4` (the simulated alpha series),
`P13_CONTINUITY_COUNTER = 5` and `P13_FIXTURE_COUNTER = 99`. **The superseded 13-16 run used NONE of
them, and this re-run allocates none** — so there is no counter to choose and no collision to avoid.
Re-assert that property from the runner's own source text rather than in prose, and persist
`rng_stream = "NONE"` unchanged.

**Record, as a delta table, every quantity that moved**, each labelled with the pair it was measured
on:

| Quantity | superseded, pair (1, 2) | amended, pair (2, 3) |
|---|---|---|
| `m-bar` positive / negative | +0.3292 / +0.2481 | measure |
| headline `log BF(C:R)` / `log BF(E:R)`, both directions at λ = 3.0 | −0.4658 / −10.2727 and −3.9432 / −9.0534 | measure |
| descriptive argmax verdict, both directions | RANDOM / RANDOM | measure |
| Phase-13 OOD density / threshold / ratio | 417.2974 / 167.5446 / 2.491× | measure |
| per-acquisition densities | 417.2974 positive, 321.1505 negative | measure |
| shipped OOD density / threshold / ratio | 433.6884 / 179.1368 / 2.421× | measure |
| A12 outcome | `agreement` | measure |
| λ response span, both heads | 0.0117 / 0.0585 exclusion; 0.2595 / 0.1750 coloc | measure |
| `alpha_star_real` positive / negative | 0.875 / 0.625 | measure |
| realized mask fraction positive / negative | 0.134933 / 0.229189 (**on c1**) | measure (**on c2**) |
| eight α invariants | all hold, worst residual 2.86e-16 | measure |
| ρ through the frozen `ghat`, both fixtures | +0.33042 / +0.21480, contrast ±0.11562 (inside τ = 0.15) | measure |

**The headline OOD number is EXPECTED to move and must be reported as a change, never restated.**
`13-16-SUMMARY.md:143` already records a different pair landing at 607.5728 (3.63×) — a 46% move — so
this number is demonstrably pair-dependent.

**If the c2 mask fraction falls outside `P13_ALPHA_MASK_FRACTION_BOUNDS = (0.01, 0.40)`**, the runner
takes its `INVARIANT VIOLATION` branch, the ladder curves are not read, and the artifact is still
persisted with the violation on the record. **That is a reportable finding and is NOT a licence to
widen the bounds.** Widening a pre-registered bound after a measurement is precisely the act this
amendment is not. Report it and stop rather than adjust.

**Nothing here can fail the phase**, and a disappointing, flat or contradictory reading is a
reportable finding — never a reason to retrain, reseed, retune or widen the substrate. Do not re-run
to get a different answer: the substrate is deterministic, so a second run is byte-identical and a
third would be data-snooping theatre.

Confirm at the end that `spike/p13/gate_report.jld2` and `spike/p13/alpha_report.jld2` are
byte-unchanged and that `P13_ITERATION_ALLOWANCE` still reads 1 of 1 UNSPENT.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_real.jl &amp;&amp; julia --project=spike spike/test/test_p13_alpha.jl &amp;&amp; julia --project=spike spike/test/test_p13_calibration.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/p13/run_p13_realimage.jl` exits 0 and writes `spike/p13/realimage_report.jld2`.
    - The persisted artifact carries `is_gated == false`, `qualitative_only == true`, `rng_stream` beginning `NONE`, the amended pair `[2, 3]`, the amended anchors, and the recorded superseded comparison fields.
    - Every printed and persisted log-BF sits on the same row as its OOD density, threshold, boolean verdict and λ — the standing D-15 requirement, re-verified after the edit.
    - The read-only proof holds: the `git ls-files -s test/test_images` digest is identical before and after, and `git status --porcelain test/test_images/` is empty.
    - `git diff --quiet HEAD -- spike/p13/gate_report.jld2 spike/p13/alpha_report.jld2 spike/p13/run_three_way_gate.jl spike/p13/run_alpha_series.jl` exits 0 (M2), or shows only the Task-1 guard hunk in the two runners with both artifacts byte-unchanged (M1).
    - `git diff --quiet HEAD -- src/ test/ docs/ corpus/ spike/Project.toml spike/Manifest.toml` exits 0, and `spike/data/cache/p11/` is unmodified.
    - The three Phase-13 test files run directly and exit 0. **A full-suite green is NOT available as a Phase-13 signal and is not required here:** `spike/test/runtests.jl` aborts at `:220` on the pre-existing `P13_DEV_SEED` include-guard collision (`spike/validation/p12_consts.jl:109` mirrors the seed, so `test_p13_consts.jl:42`'s guard skips its include and ~114 constants go undefined). That defect predates this plan, is recorded as its own OPEN blocker in STATE.md, and is **not this plan's to fix** — per-file runs are the working gate for Phase 13.
    - The delta table above is filled in completely in the plan summary, with every row labelled by the pair it was measured on.
  </acceptance_criteria>
  <done>The corrected arm has run exactly once on the main working tree, consuming no RNG stream and colliding with no reserved counter; every quantity that moved is recorded beside its superseded counterpart with the pair named; the gate and alpha artifacts are byte-unchanged; the allowance is still 1 of 1 UNSPENT.</done>
</task>

<task type="auto">
  <name>Task 5: Revise the report, supersede the two summaries, and answer whether the conclusion changes</name>
  <files>.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md, .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-15-SUMMARY.md, .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-16-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `13-REPORT.md` — the header `## Scope of evidence` block (lines 22-52), `### 8b — real
      substrate` (545-587), `## 8c` in full (588-750), the named limits (791-874), `## What was
      DECLINED` (751-790) and `## 14` (1047+). Those are the sections that quote the real arm.
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-D15-AMENDMENT.md` §10 — the
      binding requirement this task implements — and §0.1, the timeline it must reproduce.
    - The task-4 delta table.
  </read_first>
  <action>
**The one thing this task exists for: `13-REPORT.md` must answer, in plain prose and near the top of
the real-image section, the question — DOES THE CONCLUSION CHANGE?**

The prior reading is *qualitative, n = 2 specimens, no ground-truth label, OOD-bound, weak evidence,
coherence and never correctness.*

- **If the corrected pair lands in the same place, SAY SO PLAINLY**: *"we corrected the channel pair
  and the conclusion is unchanged."* That is a **good** result and must be written as one — it tells a
  reader the limitation is **structural**, a property of two unlabelled specimens outside the training
  distribution, rather than an artefact of having read the wrong channels. A limitation that survives
  its own correction is a stronger statement than one that was never tested.
- **If it changes, state the change as a change**, with both numbers, both pairs, and which is
  superseded.
- **Overselling the correction as a rescue is FORBIDDEN.** The separation got marginally worse
  (0.0788 against 0.0811). No sentence may imply the corrected arm is strong evidence, and none of
  n = 2, no label, both fixtures OOD-flagged, the α construction-parameter caveat, or the Phase-16
  deferral may be dropped or softened.

**Report edits, all confined to the real-image material:**

- `## Scope of evidence` — add the amendment to the header block: the pair was corrected, the
  redundancy arm dropped, no gating threshold moved, and the phase's single gating arm is unaffected.
  The greppable sentence *No pass/fail threshold is defined for any real-image quantity* stays
  verbatim and stays a header element.
- `### 8b` and `## 8c` — replace the measured tables with the re-run's, **each amended number printed
  beside its superseded counterpart with the pair named for each**. Delete the redundancy subsection
  and state that the arm was dropped, not deferred, and why.
- `### The OOD finding — this arm's binding limit` — report the new density, threshold and ratio **as
  a change** from 417.2974 / 167.5446 / 2.491×, never as a restatement.
- **Named limit D** and the channel-pair sentence at `:857` — rewrite: the limit is no longer "both
  pre-registered pairs include the counterstain" but "the pair was mis-designated in the
  pre-registration, corrected under an authorised amendment after the first run, and the corrected
  arm separates marginally *less* well; the redundancy arm was dropped because no second pair exists."
- `## What was DECLINED` — the item "a third channel pair" must be re-read: the amendment corrects the
  *first* pair to the one the upstream calibration had already adopted; it does **not** add an
  un-pre-registered third pair. Say exactly that.
- `## 14. Discrepancies found while writing this report` — append this correction as a numbered
  discrepancy with its full timeline (`13-D15-AMENDMENT.md` §0.1): the factual correction was on the
  record from 2026-07-25 23:56:54 (`ea4a7d3`) and `consts.jl` was locked **38 h 46 min 18 s** later
  (2026-07-27 14:43:12, `c42cc8e`) still quoting the superseded figures; the specific disposition arrived at 17:48 on 2026-07-29, 41 minutes after
  13-16 finished at 17:07. **Do not obscure and do not overstate either half.**
- **Everything not about the real arm stays byte-unchanged**, in particular the gate sections (6a-6d)
  and the simulated α-ladder (8a).

**`.planning/ROADMAP.md` — the phase's most-read summary surface (`13-D15-AMENDMENT.md` §6.6).**
It quotes the superseded-pair result in four places and is the first thing a reader hits, before the
report and before any summary. §9's pairing rule applies to it with more force than to anything
else, not less.

**Read `.planning/ROADMAP.md` immediately before editing it and make the SMALLEST additive edit that
does the job. A Phase-12 executor is LIVE on this branch and on this file specifically.** Commit it
in its own `git commit -m "..." -- .planning/ROADMAP.md`, separately from the Phase-13 documents, so
a concurrent Phase-12 write is a merge of one file and not of five.

The planner has already marked the four sites SUPERSEDED and corrected the plan/wave count to
17 plans / 10 waves. **This task adds only the amended figures beside them**, per §9:

- `:176` — the Phase-13 table row: add the amended OOD ratio beside the superseded `2.49x/2.42x`,
  and drop "on both pairs" in favour of the single amended pair, noting the redundancy arm was
  dropped.
- the `13-16` plan bullet — print the amended density / threshold / ratio and the amended
  `alpha_star_real` beside `417.30 vs 167.54 (2.49×)`, `433.69 vs 179.14 (2.42×)` and
  `0.875 / 0.625`, each labelled with the pair it was measured on.
- add a `13-17-PLAN.md` bullet recording the amendment and its outcome.
- **the `**Verdict:**` block's GATE sentences are NOT touched** — the AUC/ECE figures are
  simulator-ground-truth results this amendment cannot reach (`13-D15-AMENDMENT.md` §3). Amend only
  its real-arm clause.

**Superseding notes.** Prepend a short, clearly-marked block to each summary:

- `13-15-SUMMARY.md`: its measured `0.329163 / 0.248052` are a **read-chain identity check on a
  superseded pair**; the deliverable is pair-agnostic by construction and needed no re-authoring; the
  amended anchors were re-measured through this same code path by 13-17. **The measured tables stay in
  full** — the record is not rewritten.
- `13-16-SUMMARY.md`: every number in it was measured on the superseded pair; it is superseded by the
  13-17 re-run and retained as the record of what the superseded pair produced. Point at the re-run
  artifact. **Keep §8-§10's named limits**, noting which of them the amendment changes.

**STATE.md — ADDITIVE ONLY.** Append one dated entry recording the amendment, the Task-1 mechanism
ruling, the re-run and its verdict, and closing the two open channel-pair blockers by pointing at this
plan. **Phase 11's closed entry and Phase 12's live entry must survive byte-unchanged**; a Phase-12
executor is live on this branch. Do **not** run `bm-sdk query state.begin-phase` or any mutating state
handler. Re-read STATE.md immediately before writing.

Commit with explicit paths only.
  </action>
  <verify>
    <automated>grep -c "13-D15-AMENDMENT" .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-15-SUMMARY.md .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-16-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '13-D15-AMENDMENT' <13-REPORT.md>` and the same over both summaries each output at least `1`.
    - The does-the-conclusion-change answer is present and greppable: `grep -ci 'the conclusion is unchanged\|the conclusion changes' .planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md` outputs at least `1`.
    - The amended and superseded numbers are paired: `grep -c '0.4603' 13-REPORT.md` and `grep -c '0.3292' 13-REPORT.md` are both at least `1`, and the superseded OOD reading is retained: `grep -c '417.2974' 13-REPORT.md` is at least `1`.
    - The greppable not-a-gate sentence survives verbatim: `grep -c 'No pass/fail threshold is defined for any real-image quantity' 13-REPORT.md` is at least `1`.
    - No rescue framing: the report contains no sentence asserting the corrected arm is strong.
    - **The four honesty items survive INDIVIDUALLY — four separate greps, each `>= 1`.** A single alternated grep counting `>= 4` does **not** test this: it counts lines matching *any* branch, so four lines saying only "Phase 16" would satisfy it while the other three items were deleted. T-13-72, this plan's central anti-rescue guarantee, rests on these, so they are asserted one at a time:
      1. `grep -c 'n = 2' 13-REPORT.md` is at least `1`
      2. `grep -c 'ground-truth label' 13-REPORT.md` is at least `1`
      3. `grep -c 'out-of-distribution' 13-REPORT.md` is at least `1`
      4. `grep -c 'Phase 16' 13-REPORT.md` is at least `1`
      Each of the four is present at HEAD (counts 2, 1, 4, 8 respectively), so any of them reaching `0` is a deletion this task caused, not a pre-existing absence.
    - `.planning/ROADMAP.md` carries no unmarked superseded figure: every line quoting `417.30`, `167.54`, `433.69`, `179.14`, `2.49`, `2.42`, `0.875` or `0.625` also carries `SUPERSEDED` or names the pair it was measured on, and `grep -c '17 plans' .planning/ROADMAP.md` is at least `1`. The ROADMAP commit is its own commit with `-- .planning/ROADMAP.md` as the only pathspec.
    - The gate sections are untouched: `git diff 13-REPORT.md` shows no hunk inside `## 6`, `### 6a`-`### 6d` or `### 8a`.
    - STATE.md is additive only: `git diff .planning/STATE.md` shows insertions with zero deletions inside the Phase-11 and Phase-12 blocks, and no mutating state handler was run.
    - All commits used explicit paths (`git log -1 --format=%s` plus the recorded command lines show `git commit -m "..." -- <paths>`, never `-a`, never `--no-verify`).
  </acceptance_criteria>
  <done>13-REPORT.md answers in words whether the conclusion changes, prints every amended number beside its superseded counterpart with the pair named, reports the OOD headline as a change, carries the full authorisation timeline as a numbered discrepancy, and oversells nothing, with the four honesty items each separately grep-proven to survive; `.planning/ROADMAP.md` carries the amended figures beside the superseded ones with the gate sentences untouched, committed on its own pathspec; both summaries carry superseding notes without having their measured records rewritten; STATE.md gained one additive entry and the two channel-pair blockers are closed by reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen pre-registration -> amended pre-registration | An amendment landing after results exist is the highest-risk act this project performs; every edit must be argued, dated and confined |
| real arm -> gating arm | The two share `consts.jl` and its sha guard; an unconsidered edit propagates from a qualitative arm into the phase's only gate |
| superseded run -> amended run | Two artifacts now describe the same fixtures; a reader who conflates them reads a number against the wrong physical object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-70 | Tampering | the amendment itself | mitigate | `13-D15-AMENDMENT.md` §3 enumerates every untouched threshold by name; task 2 asserts `git diff` on `consts.jl` matches no threshold-shaped identifier |
| T-13-71 | Elevation of privilege | the qualitative arm becoming a gate | mitigate | `P13_REAL_IS_GATED = false` unchanged; runner keeps zero `@test` lines (source-grepped); the not-a-gate exemption set is asserted by set equality and gains no third member |
| T-13-72 | Spoofing | the corrected arm read as a rescue | mitigate | Task 5 forbids rescue framing, requires the marginally-worse separation to be stated, and requires all four honesty items to survive |
| T-13-73 | Information disclosure | the masked `+0.8238 / −0.0338` figures adopted because they separate better | mitigate | Task 2 records them as FORBIDDEN with the reason in a comment; acceptance criteria assert they appear only inside comments |
| T-13-74 | Repudiation | the timeline being softened | mitigate | `13-D15-AMENDMENT.md` §0.1 carries verified commits and timestamps; task 5 requires the same table in `13-REPORT.md` §14, including the 41-minute lag |
| T-13-75 | Denial of service | the gate arm made un-reproducible by a sha-guard edit | mitigate | Escalated as the Task-1 blocking checkpoint; M2 avoids it entirely; under M1 the widening must be reasoned in an adjacent comment and both gate artifacts asserted byte-unchanged |
| T-13-76 | Tampering | a pre-registered bound widened after the c2 mask fraction is measured | mitigate | Task 4 states explicitly that an out-of-band mask fraction is a reportable finding and NOT a licence to widen `P13_ALPHA_MASK_FRACTION_BOUNDS`; the executor stops rather than adjusts |
| T-13-77 | Spoofing | the superseded `P13_REAL_OOD_SHIPPED_DENSITY = 433.69` quoted as an amended-pair reading | mitigate | Task 3 labels it as a superseded-pair measurement wherever printed, and labels the `:741` fallback if it fires |
| T-13-78 | Spoofing | `.planning/ROADMAP.md` — the most-read surface — still presenting superseded-pair figures as current | mitigate | `13-D15-AMENDMENT.md` §6.6 inventories all four sites; the planner marked them SUPERSEDED and fixed the plan/wave count; Task 5 adds the amended figures beside them, on its own pathspec, re-reading first because a Phase-12 executor is live on that file |
| T-13-79 | Spoofing | `spike/p13/real_images.jl`'s header telling a reader what is FROZEN and not what is OPERATIVE | mitigate | §6.4a assigns blocks (b2), (e) and the `real_mbar` docstring to Task 3 with explicit M1/M2-conditional wording; comment-only, grep-asserted; under M2 the header must say in words that a reader who stops at this file reads the wrong pair |
| T-13-80 | Tampering | the not-a-gate source gate going blind to the operative constants under M2 | mitigate | Task 2 sources `test_p13_real.jl:336`'s name set from `CONSTS_CODE * AMENDED_CODE` with a `get(..., "")` default, keeping the `readdir`-discovered set-equality gate rather than substituting a hand-enumerated testset |
| T-13-81 | Repudiation | M1's true cost hidden from the user at the moment of the Task-1 ruling | mitigate | Task 1's retraction block records B1 (`13-REPORT.md` §13 integrity rows become false), B2 (`test_p13_real.jl:144-146` passes while silently changing meaning) and B3 (`ANCHOR_TOL` as diff context fails the no-threshold-moved criterion on correct work); the resume-signal names all three; none is pre-fixed, so the choice is not pre-decided |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** This plan adds no dependency; `spike/Project.toml` and `spike/Manifest.toml` are asserted byte-unchanged in every task. If a step appeared to need a new package, that is a finding to surface, not a dependency to add |

Threats accepted: none new. The Phase-4 `SPEEDUP_GATE` is out of scope — it was PAUSED, not
retired, by user ruling on 2026-07-29 (`a494247`) with `SPEEDUP_GATE = 100.0` byte-unchanged and a
deferred obligation to re-measure on a quiet machine at the end of v2.0. The `P13_DEV_SEED`
include-guard collision that now aborts the suite at `runtests.jl:220` is likewise out of scope and
is its own OPEN blocker.
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p13_consts.jl` exits 0.
2. `julia --project=spike spike/test/test_p13_real.jl` exits 0.
3. `julia --project=spike spike/test/test_p13_alpha.jl` and `spike/test/test_p13_calibration.jl` exit 0.
4. `julia --project=spike spike/p13/run_p13_realimage.jl` exits 0 and writes the artifact.
5. `git diff --quiet HEAD -- src/ test/ docs/ corpus/ spike/Project.toml spike/Manifest.toml` exits 0.
6. `git diff --quiet HEAD -- spike/p13/gate_report.jld2 spike/p13/alpha_report.jld2` exits 0.
7. `grep -v '^\s*#' spike/p13/run_p13_realimage.jl | grep -c 'REDUNDANCY_PAIR\|redundancy_sweep\|redundancy_channels\|open_sealed_holdout\|corpus'` outputs `0`.
8. `grep -c '@test' spike/p13/run_p13_realimage.jl` outputs `0`.
9. `git status --porcelain test/test_images/` is empty.
10. The full spike suite is NOT required to exit 0. Since the Phase-4 `SPEEDUP_GATE` was PAUSED (`a494247`, `@test_skip` tagged `DEFERRED-NPE-03-WALLCLOCK`, bar byte-unchanged), `runtests.jl` aborts instead at `:220` on the pre-existing `P13_DEV_SEED` include-guard collision. Both are recorded OPEN blockers that predate this plan; **neither is this plan's to fix**, and per-file runs are the working Phase-13 gate.
</verification>

<success_criteria>
- The operative real-image pair is `(2, 3)`; no colocalization claim anywhere in Phase 13 rests on a
  pair containing the DAPI counterstain.
- The redundancy arm is gone from code, banner, artifact, return value and report — dropped, not
  deferred, with the reason recorded.
- The colocalization anchors are the unmasked `0.4603 / 0.3815`; the masked figures exist only as a
  named, reasoned prohibition.
- No test or acceptance criterion makes reproducing a superseded value a colocalization pass
  condition; the not-a-gate exemption set is still exactly two names.
- No gating threshold moved; the gate and α artifacts are byte-unchanged; `P13_ITERATION_ALLOWANCE`
  is 1 of 1 UNSPENT; the Phase-16 seal is shut; `src/`, both spike manifests, `test/`, `docs/` and
  `corpus/` are byte-unchanged.
- The re-run consumed no RNG stream and therefore collided with no reserved counter (1 τ-probe,
  2 training pool, 3 gate, 4 α-series, 5 continuity, 99 fixtures).
- `13-REPORT.md` answers, in words, whether the conclusion changes; prints every amended number
  beside its superseded counterpart with the pair named; reports the OOD headline as a change; and
  oversells nothing.
- STATE.md gained exactly one additive entry; the Phase-11 and Phase-12 blocks are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-17-SUMMARY.md` when done,
carrying the completed task-4 delta table with every row labelled by the pair it was measured on,
the Task-1 mechanism ruling and its consequences, and the explicit does-the-conclusion-change answer
in the same words the report uses.
</output>

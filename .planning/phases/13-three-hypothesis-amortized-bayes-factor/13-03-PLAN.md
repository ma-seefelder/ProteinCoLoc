---
phase: 13-three-hypothesis-amortized-bayes-factor
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - spike/p13/labels.jl
  - spike/test/test_p13_labels.jl
autonomous: true
requirements: [D-05, D-07, D-08, D-11, D-01]

must_haves:
  truths:
    - "(D-05) The three-way class label is cut on the rho_sample LEVEL times the control contrast, never on delta_rho, and three_way_label(0.8, 0.9) is provably not EXCLUSION"
    - "(D-05) The tau dead zone maps to RANDOM and the rule is sign-symmetric under simultaneous negation of both rho values"
    - "(D-11) head_targets returns (1,1,0,0) for coloc, (0,0,1,1) for exclusion and (0,1,0,1) for random, so RANDOM is the negative class for BOTH heads and each head trains only on its own class pair"
    - "(D-07) measure_head_log_odds counts only the head's own two classes and errors on degenerate balance, and its docstring states why the binary minus-log_prior_odds formula does not transfer"
    - "(D-08) The label surface never invents a model prior over the three hypotheses; the random class is the shared reference for both heads"
    - "(D-01) The label module reads P13_TAU through p13_tau() and never inlines a threshold, so labelled data cannot be generated before the Tier-2 tau commit"
  artifacts:
    - path: "spike/p13/labels.jl"
      provides: "ThreeWayClass enum, three_way_label, head_targets, target_matrix, measure_head_log_odds, prior_class_draws"
      contains: "three_way_label"
      min_lines: 120
    - path: "spike/test/test_p13_labels.jl"
      provides: "D-05 counter-example, dead-zone and symmetry tests; D-11 participation-weight tests; D-07 head-log-odds tests incl. the degenerate-balance error and the reproduced class-mass table"
      contains: "!= EXCLUSION"
  key_links:
    - from: "spike/p13/labels.jl"
      to: "spike/p13/consts.jl"
      via: "guarded include and p13_tau() default argument"
      pattern: "p13_tau"
    - from: "spike/test/test_p13_labels.jl"
      to: "spike/p13/labels.jl"
      via: "guarded include plus executable counter-example assertions"
      pattern: "labels\\.jl"
---

<objective>
Define the three-way class boundary and the per-head training targets — the two places where this
phase is most likely to be silently wrong.

Purpose: D-05 exists because the natural handle (`delta_rho`, `src/amortized/infer.jl:117-121`) would
publish "less colocalized than the control" as "mutually exclusive". D-11 exists because one-vs-rest
training would make the coloc logit a coloc-vs-*mixture* ratio, silently breaking D-08's semantics
while looking identical in the code. Both traps are cheap to encode as executable assertions and
expensive to discover after training.

Output: `spike/p13/labels.jl` and `spike/test/test_p13_labels.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-CONTEXT.md
@.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-PATTERNS.md

**Binding rulings this plan implements (do not reopen):**
- **Cut variant (a)** (`P13_CUT_VARIANT = :tau_contrast`): both the level factor and the contrast
  factor are stated at the summary's measured resolution. Variant (b) is not implemented.
- **`P13_TAU` does not exist yet.** It is Tier-2 and arrives in plan 13-10. Every function takes tau
  as a keyword defaulting to `p13_tau()`, which errors until then; tests pass tau explicitly.
- **The binary `- log_prior_odds` term is NOT copied.** `measure_head_log_odds` is a different
  quantity with a different denominator (13-RESEARCH section F1). Pitfall 1 is the one to avoid.
- **`src/` is read-only reference.** `src/amortized/train_ratio.jl` is the analog; the spike-lane
  twin `spike/validation/train_ratio.jl` is the style source. Neither is edited.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

From `src/amortized/train_ratio.jl:82-96` [READ-ONLY REF] — the docstring voice and the
degenerate-balance error to keep:
```julia
"""
    measure_log_prior_odds(model_index) -> Float64

The MEASURED log prior-odds of the coloc event (Pitfall 5 — measured, NEVER assumed 0):
`log(p / (1 - p))` with `p = mean(model_index)` ... Returns a finite scalar; errors on degenerate balance.
"""
function measure_log_prior_odds(model_index::AbstractVector{<:Real})
    p = mean(model_index)
    (0.0 < p < 1.0) || error("measure_log_prior_odds: degenerate label balance p=$p (need 0<p<1)")
    return log(p / (1 - p))
end
```

From `src/amortized/train_ratio.jl:55` [READ-ONLY REF] — the one-factor rule being REPLACED:
```julia
const RATIO_SPLIT_THRESHOLD = 0.0   # coloc/null split on the rho_true CONTRAST (Delta mu > 0)
```

From `spike/validation/train_ratio.jl:246-262` — the cheap prior-only label draw to generalize:
```julia
function prior_label_draws(n::Integer; rng = val_rng(), threshold = RATIO_SPLIT_THRESHOLD)
    labels = Vector{Float32}(undef, n)
    for j in 1:n
        rs = sample_prior(rng).rho_true
        rc = sample_prior(rng).rho_true
        labels[j] = Float32((rs - rc) > threshold)
    end
    return labels
end
```

From 13-RESEARCH section B3 — the 4-row target contract:
row 1 `y_C`, row 2 `w_C`, row 3 `y_E`, row 4 `w_E`; class to weights: coloc (1,0),
exclusion (0,1), random (1,1).

From 13-RESEARCH section E1 — the measured class masses under pi at variant (a), for the test:
tau 0.05 -> E 0.3850 / R 0.3413 / C 0.2738; tau 0.10 -> E 0.3482 / R 0.4108 / C 0.2410;
tau 0.15 -> E 0.3119 / R 0.4713 / C 0.2168.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three-way label surface</name>
  <files>spike/p13/labels.jl</files>
  <read_first>
    - `13-RESEARCH.md` section E1 lines 624-651 — variant (a) versus (b), the recommendation, and
      the measured class-mass table this file must be able to reproduce.
    - `13-RESEARCH.md` section F1 lines 741-770 and section F4 lines 799-843 — the derivation of
      the per-head correction and the full `measure_head_log_odds` docstring to adapt.
    - `13-RESEARCH.md` Code Example 2 lines 1445-1467 — `three_way_label`, the executable
      counter-example, and `head_targets`.
    - `13-RESEARCH.md` Pitfall 1 lines 1312-1320 and Pitfall 3 lines 1330-1338.
    - `13-PATTERNS.md` section `spike/p13/labels.jl` lines 214-277 — the analog excerpts, the
      docstring-is-a-contract convention, and the required changes.
    - `src/amortized/train_ratio.jl` lines 82-134 [READ-ONLY REF] — do not edit.
    - `spike/validation/train_ratio.jl` lines 231-262 — the spike-lane style twin.
    - `spike/simulator/prior.jl` lines 40, 71-82 — `sample_prior`'s NamedTuple and the `rho_true`
      field name, needed by `prior_class_draws`.
    - `spike/p13/consts.jl` — `p13_tau`, `P13_CUT_VARIANT`, `P13_TARGET_CLASS_FREQ`, `p13_rng`.
  </read_first>
  <action>
Create `spike/p13/labels.jl`: AGPL block header, then
`# spike/p13/labels.jl --- Phase-13 three-way class boundary and per-head targets (D-05, D-07, D-11).`
followed by a banner in the house voice explaining (a) why the cut is on the `rho_sample` level
times the control contrast and not on `delta_rho`, with the rho_s = 0.8 / rho_c = 0.9
counter-example spelled out; (b) why each head trains only on its own class pair; (c) the
DECOUPLING line ("spike-local; reaches `src/` only read-only").

Guarded includes in this order: `spike/p13/consts.jl`, then `spike/simulator/prior.jl` (guard on
`:sample_prior`). `using Statistics` for `mean`. Flat top-level functions, no module wrapper.

Define:

1. `@enum ThreeWayClass EXCLUSION RANDOM COLOC` — comment that the ordering is
   exclusion/random/coloc so that `Int` order matches increasing rho, which is what makes the
   confusion matrix read left-to-right.

2. `three_way_label(rho_sample::Real, rho_control::Real; tau::Real = p13_tau())` implementing
   variant (a): `d = rho_sample - rho_control`; return `COLOC` when `rho_sample > tau && d > tau`;
   return `EXCLUSION` when `rho_sample < -tau && d < -tau`; otherwise `RANDOM`. Guard
   `tau > 0` with an `ArgumentError`. The docstring must state the D-05 rule, name
   `src/amortized/infer.jl:117-121` as the quantity deliberately NOT used, carry the
   counter-example, and note that `P13_CUT_VARIANT` records the frozen choice.

3. `head_targets(c::ThreeWayClass) -> Vector{Float32}` returning `Float32[1,1,0,0]` for `COLOC`,
   `Float32[0,0,1,1]` for `EXCLUSION`, and `Float32[0,1,0,1]` for `RANDOM`, with the row legend
   `[y_C; w_C; y_E; w_E]` in the docstring and the D-11 rationale: RANDOM is the negative class for
   BOTH heads, which is what makes the two logits ratios against the same reference (D-08); a
   one-vs-rest scheme (`w_C == 1` everywhere) would make the coloc head's negative class the
   mixture `q_R*R + q_E*E` and its logit a mixture Bayes factor, not `log BF(coloc : random)`.

4. `target_matrix(classes::AbstractVector{ThreeWayClass}) -> Matrix{Float32}` assembling the 4-by-n
   target matrix by `reduce(hcat, head_targets.(classes))`, with a `size(...) == (4, length(classes))`
   assertion in the docstring's contract line.

5. `measure_head_log_odds(class_labels::AbstractVector{ThreeWayClass}; positive, negative)` returning
   `log(n_pos / n_neg)` counted **only** over the head's own two classes, erroring on
   `n_pos == 0 || n_neg == 0` in `measure_log_prior_odds`'s exact message style. The docstring is a
   contract: state the derivation (for a BCE head separating A from B with training frequencies
   q_A, q_B the Bayes-optimal logit is `log[p_q(Z|A)/p_q(Z|B)] + log(q_A/q_B)`, hence the log Bayes
   factor is the logit MINUS `log(q_A/q_B)`), and state explicitly that this is NOT the binary NRE
   case: `RatioEstimator`'s shuffled-theta construction already contains the ratio, so its
   `- log_prior_odds` term is a numerically negligible redundancy and must not be copied here
   (Pitfall 1). Add the exactness condition: the scalar recovers the pi-scale Bayes factor iff
   stratification changed class frequencies only, never the within-class theta shape.

6. `head_log_odds(class_labels)` returning the NamedTuple
   `(coloc = measure_head_log_odds(...; positive = COLOC, negative = RANDOM),
   exclusion = measure_head_log_odds(...; positive = EXCLUSION, negative = RANDOM))` — the exact
   shape the read surface in `spike/p13/net.jl` will consume.

7. `prior_class_draws(n::Integer; rng = p13_rng(P13_DATAGEN_COUNTER), tau = p13_tau())` returning
   `Vector{ThreeWayClass}`, drawing two independent `sample_prior(rng).rho_true` values per index
   and labelling them — the generalization of `prior_label_draws`. Docstring: labels that depend
   only on theta need no image simulation, which is how the head log-odds and the class-mass table
   are measured at large n. Thread the `rng` explicitly; never call `Random.seed!` in a loop.

8. `class_masses(classes)` returning the NamedTuple of empirical fractions
   `(exclusion = ..., random = ..., coloc = ...)`, used by the test to reproduce the frozen
   class-mass table and by the datagen plan to verify the stratification target.

End the file with the executable counter-example as a top-level assertion evaluated at an explicit
tau so it does not depend on the Tier-2 slot:
`@assert three_way_label(0.8, 0.9; tau = 0.10) != EXCLUSION` with the Pitfall-3 comment.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/p13/labels.jl"); @assert three_way_label(0.8,0.9; tau=0.1) != EXCLUSION; @assert head_targets(RANDOM) == Float32[0,1,0,1]; println("labels ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `labels ok`.
    - `julia --project=spike -e 'include("spike/p13/labels.jl"); @assert three_way_label(-0.8,-0.9; tau=0.1) == EXCLUSION; @assert three_way_label(0.02,0.0; tau=0.1) == RANDOM; @assert three_way_label(0.9,0.1; tau=0.1) == COLOC; println("ok")'` prints `ok`.
    - `julia --project=spike -e 'include("spike/p13/labels.jl"); l=[COLOC,COLOC,RANDOM,EXCLUSION]; h=head_log_odds(l); @assert isapprox(h.coloc, log(2/1)); @assert isapprox(h.exclusion, log(1/1)); println("ok")'` prints `ok`.
    - `julia --project=spike -e 'include("spike/p13/labels.jl"); try measure_head_log_odds([COLOC,COLOC]; positive=COLOC, negative=RANDOM); error("no throw") catch e; @assert occursin("degenerate", sprint(showerror,e)); println("ok") end'` prints `ok`.
    - `grep -v '^\s*#' spike/p13/labels.jl | grep -c 'delta_rho\|Delta_rho'` outputs `0` (the forbidden quantity appears only in explanatory comments).
    - `grep -v '^\s*#' spike/p13/labels.jl | grep -c 'log_prior_odds'` outputs `0` (the binary term is never used in executable code).
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>The three-way boundary is variant (a) on the level times the contrast, the D-05 counter-example is an executable assertion, the four-row per-head targets make random the shared negative class, and the per-head log-odds is measured over the head's own pair with the binary formula explicitly disowned.</done>
</task>

<task type="auto">
  <name>Task 2: Write the label-surface test</name>
  <files>spike/test/test_p13_labels.jl</files>
  <read_first>
    - `spike/test/test_bf.jl` lines 26-37, 39-65 and 91-123 — the fixture-stream rule, the guarded
      include, the fixtures-computed-once idiom, and the CPU-only postscript.
    - `13-VALIDATION.md` lines 56 and 61 — the two label rows this test must satisfy.
    - `13-RESEARCH.md` section E1 lines 639-651 — the class-mass table to reproduce.
    - `13-RESEARCH.md` Pitfall 3 lines 1330-1338 — the warning sign to assert against
      (no exclusion-class member may have `rho_sample > 0`).
    - `spike/p13/labels.jl` — the file written in Task 1.
    - `spike/p13/consts.jl` — `p13_fix_rng`, `P13_FIXTURE_COUNTER`, `P13_TARGET_CLASS_FREQ`.
  </read_first>
  <action>
Create `spike/test/test_p13_labels.jl`: AGPL header, banner naming D-05/D-07/D-11, `using Test`,
`using Statistics`, and a guarded
`isdefined(@__MODULE__, :three_way_label) || include(joinpath(@__DIR__, "..", "p13", "labels.jl"))`.
All fixtures draw from `p13_fix_rng(P13_FIXTURE_COUNTER)` so the quick gate never consumes a
reported stream; assert `P13_FIX_SEED != P13_DEV_SEED` inside the suite as the explicit statement
of that rule. Define `const TAU_FIX = 0.10` at module level with a comment that the fixture tau is
a test constant, not the pre-registered `P13_TAU`, and that the suite must remain runnable before
the Tier-2 commit.

One outer `@testset "P13 labels and per-head targets (D-05, D-07, D-11)" verbose = true`:

1. `"D-05 counter-example (Pitfall 3)"` — `three_way_label(0.8, 0.9; tau = TAU_FIX) != EXCLUSION`,
   plus the stronger sweep: for every `rho_c` in `-0.9:0.3:0.9`, a `rho_s = 0.8` sample is never
   labelled `EXCLUSION`. Add a comment quoting the trap in one line.
2. `"level and contrast are both required"` — `three_way_label(0.5, 0.45; tau = TAU_FIX) == RANDOM`
   (level passes, contrast fails); `three_way_label(0.05, -0.9; tau = TAU_FIX) == RANDOM`
   (contrast passes, level fails); `three_way_label(0.9, 0.1; tau = TAU_FIX) == COLOC` and
   `three_way_label(-0.9, -0.1; tau = TAU_FIX) == EXCLUSION`.
3. `"dead zone and sign symmetry"` — every `(rho_s, rho_c)` with `abs(rho_s) <= TAU_FIX` maps to
   `RANDOM` on a small grid; and for a grid of pairs, `three_way_label(-a, -b; tau = TAU_FIX)` is
   the sign-mirror of `three_way_label(a, b; tau = TAU_FIX)` under the mapping
   `COLOC <-> EXCLUSION`, `RANDOM -> RANDOM`.
4. `"tau must be positive"` — `@test_throws ArgumentError three_way_label(0.5, 0.0; tau = 0.0)`.
5. `"D-11 participation weights"` — the three literal target vectors; that `RANDOM` has
   `w_C == 1 && w_E == 1` and `y_C == 0 && y_E == 0`; that `COLOC` has `w_E == 0` and `EXCLUSION`
   has `w_C == 0`; and `size(target_matrix([COLOC, RANDOM, EXCLUSION])) == (4, 3)`.
6. `"D-07 head log-odds counts only its own pair"` — on a hand-built label vector with 30 coloc,
   20 random and 50 exclusion: `head_log_odds(labels).coloc` is `log(30/20)` and
   `.exclusion` is `log(50/20)`, both to `atol = 1e-12`; assert the coloc value is NOT
   `log(30/70)` (the one-vs-rest denominator) with an in-test comment naming that as the silent
   D-08 break.
7. `"D-07 degenerate balance errors"` — `@test_throws ErrorException` on a label vector with no
   random members, for each head.
8. `"class masses reproduce the frozen table"` — draw `prior_class_draws(20_000; rng =
   p13_fix_rng(P13_FIXTURE_COUNTER), tau = TAU_FIX)` and assert `class_masses(...)` is within
   `atol = 0.02` of `(exclusion = 0.3482, random = 0.4108, coloc = 0.2410)`, with a comment that
   these are the 2e6-draw values from 13-RESEARCH section E1 and that the tolerance is the
   Monte-Carlo slop at n = 20_000, not a loosened threshold. Also assert no drawn `EXCLUSION`
   member came from a positive `rho_sample` by re-deriving labels from stored rho pairs if the
   helper exposes them; otherwise assert the weaker invariant that `class_masses` sums to 1.
9. `"tau is still Tier-2"` — `@test_throws ErrorException three_way_label(0.5, 0.0)` (no tau
   keyword), proving labelled data cannot be generated before the Tier-2 commit. Mark this testset
   with a comment that plan 13-10 flips it.
10. `"P13 labels ran CPU-only (D-10)"` — the CUDA-absence clause.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p13_labels.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p13_labels.jl` exits 0 with zero failures and zero errors.
    - `grep -c '!= EXCLUSION' spike/test/test_p13_labels.jl` outputs at least `1`.
    - `grep -c 'log(30/70)\|one-vs-rest' spike/test/test_p13_labels.jl` outputs at least `1` (the mixture-denominator trap is named in the suite).
    - `julia --project=spike -e 'include("spike/test/test_p13_labels.jl")'` exits 0 (includable from the harness).
    - The suite passes while `P13_TAU` is undefined, i.e. `julia --project=spike -e 'include("spike/test/test_p13_labels.jl"); @assert !isdefined(Main, :P13_TAU); println("tier2 still absent")'` prints `tier2 still absent`.
    - `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
  </acceptance_criteria>
  <done>The D-05 trap, the D-11 participation-weight contract and the D-07 own-pair denominator are all executable assertions; the frozen class-mass table is reproduced in-repo; and the suite proves labelled data cannot be produced before tau is measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator theta -> class label | Labels are derived from simulator ground truth only; a label derived from any network output would make the gate circular |
| pre-registered tau -> labelled data | Generating labels before tau is measured and frozen would invert D-06's ordering and make tau selectable after the fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering (semantic) | `three_way_label` cut quantity | mitigate | Executable counter-example `three_way_label(0.8, 0.9) != EXCLUSION` in both the module and the suite; source grep asserts `delta_rho` never appears in executable code |
| T-13-12 | Tampering (semantic) | One-vs-rest head training | mitigate | `head_targets` literal assertions plus an explicit test that the coloc head-log-odds denominator is the random count, not the coloc complement |
| T-13-01 | Repudiation (data snooping) | Fixture stream reuse | mitigate | All fixtures draw from `p13_fix_rng(P13_FIXTURE_COUNTER)`; the suite asserts `P13_FIX_SEED != P13_DEV_SEED` |
| T-13-13 | Repudiation (ordering) | Labelling before tau exists | mitigate | Default `tau = p13_tau()` errors until the Tier-2 commit; a testset asserts the throw |
| T-13-07 | Tampering | Input validation at the label boundary | mitigate | `ArgumentError` on non-positive tau (ASVS V5, `api.jl:111-120` style) |
| T-13-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** Acceptance criteria assert `spike/Project.toml` and `spike/Manifest.toml` byte-unchanged |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p13_labels.jl` exits 0.
2. `julia --project=spike -e 'include("spike/p13/labels.jl"); @assert three_way_label(0.8,0.9; tau=0.1) != EXCLUSION; println("ok")'` prints `ok`.
3. `grep -v '^\s*#' spike/p13/labels.jl | grep -c 'log_prior_odds'` outputs `0`.
4. `git diff --quiet HEAD -- src/ spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- `spike/p13/labels.jl` defines `ThreeWayClass`, `three_way_label`, `head_targets`,
  `target_matrix`, `measure_head_log_odds`, `head_log_odds`, `prior_class_draws` and
  `class_masses`, all flat top-level, with contract docstrings citing D-05/D-07/D-08/D-11.
- The D-05 counter-example holds as a module-level assertion and as a suite testset.
- `measure_head_log_odds` counts only the head's own pair and errors on degenerate balance.
- `spike/test/test_p13_labels.jl` exits 0 and reproduces the frozen class-mass table.
- No `src/` byte and no manifest byte changed.
</success_criteria>

<output>
Create `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-03-SUMMARY.md` when done,
including the reproduced class-mass table at the fixture tau beside the frozen 13-RESEARCH values.
</output>

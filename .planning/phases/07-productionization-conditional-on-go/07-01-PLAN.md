---
phase: 07-productionization-conditional-on-go
plan: 01
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - src/amortized/summary.jl
  - src/amortized/datagen.jl
  - src/registry.jl
  - src/ProteinCoLoc.jl
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "The patch-correlation summary and its encoder are parametrized by an arbitrary grid G (dim 2·G²), not hardcoded to 8×8/128"
    - "Grid-derived dimension helpers give a single source of truth for summary_dim, continuous-row count, and NRE input dim"
    - "The registry returns a bundle for a shipped grid and raises a clear `train_and_register` error for an unregistered grid"
    - "`_SHIPPED_GRIDS` is (4, 8, 16, 32) — 64 is NOT present"
  artifacts:
    - path: "src/amortized/summary.jl"
      provides: "patch_summary(mci,G), encode_d01, _summary_row_partition, dim helpers summary_dim/cont_rows/ratio_input_dim"
      contains: "function patch_summary"
    - path: "src/amortized/datagen.jl"
      provides: "grid-parametrized data generation + cache (promoted spike/data)"
      contains: "2"
    - path: "src/registry.jl"
      provides: "EstimatorBundle, _REGISTRY, _SHIPPED_GRIDS, estimator_for, register!, train_and_register"
      contains: "_SHIPPED_GRIDS"
  key_links:
    - from: "src/registry.jl estimator_for"
      to: "ArgumentError with train_and_register hint"
      via: "unregistered grid branch"
      pattern: "train_and_register"
    - from: "src/amortized/summary.jl ratio_input_dim"
      to: "5·G²"
      via: "NRE pair-encode dim helper"
      pattern: "ratio_input_dim"
---

<objective>
Generalize every grid-coupled site from the hardcoded 8×8/128/64 spike constants to an arbitrary grid `G`, in one place, and build the grid-keyed estimator registry skeleton (PROD-02) with `train_and_register` and a helpful unregistered-grid error.

Purpose: D-04 couples the summary-vector dimension to the patch grid, so every shipped grid needs its own architecture/training against the SAME parametrized encoder. Centralizing the coupling here (per 07-RESEARCH §Grid Coupling: "parametrize the summary + encoder by G once, then each grid plan instantiates it") prevents per-grid drift.
Output: `src/amortized/summary.jl` (grid-parametric summary/encoder/row-partition + dim helpers), `src/amortized/datagen.jl` (promoted grid-parametric data-gen/cache), `src/registry.jl` (registry skeleton).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md

<interfaces>
<!-- Grid coupling sites (07-PATTERNS §Grid-coupling site table): -->
<!-- spike/contract.jl:85-90 patch_summary → patch.([x,y], G) -->
<!-- spike/data/encode.jl:71-75 encode_d01 → dim 2·G² (vals 1:G², mask G²+1:2G²) -->
<!-- spike/data/loader.jl:126-136 _row_partition :min hardcodes 1:64/65:128 → (1:G², G²+1:2G²) -->
<!-- spike/data/generate.jl:108-110,160,172-174 buffers Matrix(128,N) + summary_min_dim=128 → 2·G² -->
<!-- NRE dims (bf/train_ratio): RATIO_CONT_ROWS=G², RATIO_INPUT_DIM=5·G² (was 320 at G=8) -->
<!-- src/colocalization.jl:37 patch(img, num_patches) already grid-general; ≥15-survivor floor in correlation/_exclude_zero (colocalization.jl:235) -->
<!-- Registry target shape: 07-RESEARCH §Pattern 2 + 07-PATTERNS src/registry.jl (note _SHIPPED_GRIDS = (4,8,16,32), 64 DROPPED) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grid-parametric summary/encoder + dimension helpers</name>
  <files>src/amortized/summary.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `patch_summary(mci, G)` returns a G×G `Union{Float64,Missing}` correlation matrix for any G (4, 8, 16).
    - `encode_d01(M)` on a G×G matrix returns a length-`2·G²` vector: rows `1:G²` imputed values, rows `G²+1:2G²` binary present-mask.
    - `_summary_row_partition(:min, 2G²)` returns `(1:G², G²+1:2G²)` for G∈{4,8,16,32}.
    - `summary_dim(G) == 2G²`, `cont_rows(G) == G²`, `ratio_input_dim(G) == 5G²`.
  </behavior>
  <read_first>
    - 07-PATTERNS.md → "Grid-parametrized summary/encoder" section (exact deltas) + §Grid Coupling table
    - spike/contract.jl:85-90 (patch_summary), spike/data/encode.jl:71-75 (encode_d01), spike/data/loader.jl:126-136 (_row_partition)
    - src/colocalization.jl:37-60 (patch), :221-239 (correlation + ≥15-survivor floor)
  </read_first>
  <action>
    Create `src/amortized/summary.jl` promoting the spike summary/encoder to be grid-general: `patch_summary(mci::MultiChannelImage, G::Integer)` calling `patch.([x,y], G)` then `correlation(...; method=:pearson)`; `encode_d01(M::AbstractMatrix)` producing the `2·G²` vector (body unchanged — column-major `vec` handles any G); `_summary_row_partition(variant::Symbol, nrows::Integer)` returning `(1:nrows÷2, nrows÷2+1:nrows)` for `:min` (replacing the hardcoded 128 guard with a `iseven(nrows)` check). Add the single-source dimension helpers `summary_dim(G)=2G²`, `cont_rows(G)=G²`, `ratio_input_dim(G)=5G²`. `include` it in `src/ProteinCoLoc.jl` after `results.jl`; do NOT export internal helpers (they are called by the amortized modules). Match the src/ module convention (no per-file `isdefined` include guard — 07-PATTERNS §Conventions "Contested hotspots").
  </action>
  <acceptance_criteria>
    - Unit test asserts `length(encode_d01(patch_summary(mci, G))) == 2G²` for G∈{4,8,16} on a synthetic MultiChannelImage.
    - `_summary_row_partition(:min, 2G²) == (collect(1:G²), collect(G²+1:2G²))` for G∈{4,8,16,32}.
    - `summary_dim(8)==128`, `cont_rows(8)==64`, `ratio_input_dim(8)==320` (back-compat with the proven 8×8 constants).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Summary/encoder/row-partition are grid-general; dim helpers are the single source of truth and reproduce the 8×8 spike constants.</done>
</task>

<task type="auto">
  <name>Task 2: Promote grid-parametric data generation + cache</name>
  <files>src/amortized/datagen.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-PATTERNS.md → §Grid Coupling site table (generate.jl buffers + generating_config) + §Shared Patterns (atomic JLD2)
    - spike/data/generate.jl:108-110,160,172-174 (buffers + summary_min_dim), spike/data/cache.jl:97-108,118-134,147-164 (atomic write + open_or_invalidate), spike/data/seeding.jl:52-62 (IMSIZE_SET, sample_imsize)
  </read_first>
  <action>
    Promote `spike/data/generate.jl` + `spike/data/cache.jl` + `spike/data/seeding.jl` into `src/amortized/datagen.jl`, adapting the grid-coupled buffers from fixed `Matrix(128,N)` / `AUG_DIM` to `Matrix(summary_dim(G), N)` and threading `G` through `generate_samples(...; grid=G, imsize_set=...)`. Set the cache `generating_config` `summary_min_dim = summary_dim(G)` so distinct grids auto-separate into distinct content-hashed cache dirs (07-RESEARCH §Grid Coupling — "invalidates cross-grid caches — good"). Keep the atomic `.tmp`+integrity-check+`mv force=true` idiom verbatim (07-PATTERNS §Shared Patterns). Expose an `imsize_set` argument so per-grid plans can bias image size upward (Pitfall 2). Use the src/ module convention. Do NOT edit `spike/*` (D-03 — spike stays byte-identical).
  </action>
  <acceptance_criteria>
    - `datagen.jl` allocates summary buffers as `Matrix(summary_dim(G), N)`, not literal `128`.
    - `generating_config.summary_min_dim == summary_dim(G)` so a 4×4 and 16×16 cache resolve to different content-hash dirs.
    - Atomic write path retains `.tmp` → reopen integrity `@assert` → `mv(...; force=true)`.
    - `git status spike/` shows no modifications (spike untouched).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; @assert ProteinCoLoc.summary_dim(16)==512' && git diff --quiet spike/ && echo SPIKE_CLEAN</automated>
  </verify>
  <done>Data-gen + cache are grid-parametric with per-grid content-hash separation; atomic persistence preserved; spike/ untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registry skeleton — EstimatorBundle + estimator_for + train_and_register</name>
  <files>src/registry.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `estimator_for(g)` for a registered grid returns its `EstimatorBundle`.
    - `estimator_for(7)` (unregistered, non-shipped) throws `ArgumentError` whose message names the shipped grids and mentions `train_and_register`.
    - `_SHIPPED_GRIDS == (4, 8, 16, 32)` and does NOT contain 64.
    - `register!(b)` makes `estimator_for(b.grid)` return `b`.
  </behavior>
  <read_first>
    - 07-RESEARCH.md §Architecture Patterns → Pattern 2 (registry skeleton) + §Standard Stack (Artifacts lazy, Flux.state persistence note)
    - 07-PATTERNS.md → `src/registry.jl` section (EstimatorBundle, _SHIPPED_GRIDS=(4,8,16,32), atomic-persistence analog save_npe train_npe.jl:160-180)
  </read_first>
  <action>
    Create `src/registry.jl` defining `struct EstimatorBundle` (fields `grid::Int`, `npe`, `ratio`, `ood_nulls`, `zt`, `θzt`, `calibration::CalibrationMeta`); `const _REGISTRY = Dict{Int,EstimatorBundle}()`; `const _SHIPPED_GRIDS = (4, 8, 16, 32)` (64 DROPPED per D-04); `estimator_for(grid::Integer)` returning a cached bundle, else lazy-loading a shipped grid via a `_lazy_load_from_artifact!(grid)` hook (stubbed to error "artifact not yet populated" until 07-10 wires Artifacts), else throwing the `train_and_register`-pointing `ArgumentError`; `register!(b)`; and `train_and_register(grid::Integer; use_gpu = has_cuda_device(), kwargs...)` calling a `_train_grid_pipeline` hook (defined in 07-03/per-grid plans) then `register!`. Add a `has_cuda_device()` helper returning `false` when CUDA is absent (weakdep-safe). Export `estimator_for`, `register!`, `train_and_register`. `include` after `datagen.jl`.
  </action>
  <acceptance_criteria>
    - `_SHIPPED_GRIDS == (4,8,16,32)` (unit-asserted); `64 ∉ _SHIPPED_GRIDS`.
    - `estimator_for(7)` throws `ArgumentError`; the message contains the substring `train_and_register`.
    - After `register!(EstimatorBundle(8, ...))`, `estimator_for(8) ===` that bundle.
    - `has_cuda_device()` returns `false` on a CPU-only machine without erroring.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Registry keyed by grid with a helpful unregistered error, capped shipped family (no 64), and a GPU-aware train_and_register entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public API → registry | user-supplied `num_patches` must be validated against the registry (no silent fallback) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-05 | Spoofing/Tampering (input) | `estimator_for(num_patches)` | mitigate | validate grid against `_REGISTRY`/`_SHIPPED_GRIDS`; throw a clear `ArgumentError` (V5 input validation) — never a silent default grid |
| T-7-03 | DoS (robustness) | fine-grid summary with heavy missingness | mitigate | per-grid content-hash cache separation + `imsize_set` bias so grids clear the ≥15-survivor floor (Pitfall 2) |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (summary dims, row-partition, registry error path).
- `git diff --quiet spike/` — spike unchanged.
</verification>

<success_criteria>
- Grid coupling generalized once; registry returns bundles or a helpful error; shipped family capped to (4,8,16,32).
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-01-SUMMARY.md` when done.
</output>

---
phase: 07-productionization-conditional-on-go
plan: 02
type: execute
wave: 2
depends_on: [07-00, 07-01]
files_modified:
  - src/amortized/infer.jl
  - src/amortized/bf.jl
  - src/amortized/ood.jl
  - src/ProteinCoLoc.jl
autonomous: true
requirements: [PROD-01]
must_haves:
  truths:
    - "The NPE read surface produces posterior draws, ρ draws, and Δρ from a frozen estimator + frozen transforms in a single CPU forward pass"
    - "The amortized log Bayes factor is computed from the NRE in one forward pass with the measured log-prior-odds subtracted"
    - "A non-clamped BF baseline exists so max|Δ logBF| is testable without the KDE 1e-8 floor artifact"
    - "The OOD flag fuses density + noise channels AND the re-enabled posterior-predictive channel"
  artifacts:
    - path: "src/amortized/infer.jl"
      provides: "standardize_summary, posterior_for, rho_draws, delta_rho (CPU-default read surface)"
      contains: "function posterior_for"
    - path: "src/amortized/bf.jl"
      provides: "amortized_log_bf + non-clamped BF baseline helper"
      contains: "function amortized_log_bf"
    - path: "src/amortized/ood.jl"
      provides: "Mahalanobis + noise + posterior-predictive channels, OR-fusion, ood_verdict"
      contains: "function ood_verdict"
  key_links:
    - from: "src/amortized/infer.jl rho_draws"
      to: "frozen θzt reconstruct"
      via: "un-standardize before reading ρ row 1"
      pattern: "reconstruct"
    - from: "src/amortized/ood.jl"
      to: "posterior-predictive channel"
      via: "with_pp = true (re-enabled, finite-guarded)"
      pattern: "with_pp"
---

<objective>
Promote the spike's amortized read surfaces — NPE posterior inference, NRE amortized Bayes factor, and the OOD flag — into `src/amortized/`, folding in the two Memo §5 hardening items: re-enable the OOD posterior-predictive channel and add a non-clamped BF baseline.

Purpose: PROD-01 — these are the request-response services `colocalization_amortized` composes. They are copy-and-adapt from proven spike files (07-PATTERNS "exact" match quality). All read/inference paths default to `use_gpu=false` for the CPU-reproducible shipped default (D-06).
Output: `src/amortized/infer.jl`, `src/amortized/bf.jl`, `src/amortized/ood.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md

<interfaces>
<!-- From src/amortized/summary.jl (07-01): _summary_row_partition, cont_rows(G), ratio_input_dim(G) -->
<!-- infer analogs: spike/npe/infer.jl:65 (row-partition delegation), :76-84 standardize_summary, :96-138 posterior_for/rho_draws/delta_rho -->
<!-- bf analogs: spike/validation/bf.jl:94-100 amortized_log_bf (grid=[0 1], logratio), :208-237 KDE-baseline comparison; pair_encode spike/validation/train_ratio.jl:185-188 -->
<!-- ood analogs: spike/validation/ood.jl whole file — :112-132 Mahalanobis, :251-303 noise channel, :319-335 ROC/AUC, :344-345 pre-registered id_threshold, :145-159 finite-guard, :375-380 OR-fusion, :590 with_pp=false (to flip true) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NPE read surface (infer.jl) — CPU-default single-pass</name>
  <files>src/amortized/infer.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `standardize_summary(S, zt, :min)` applies the frozen `zt` to continuous rows and passes binary mask rows through unchanged.
    - `posterior_for(est, Z; N)` coerces a vector to a `d_in×1` single dataset and returns `sampleposterior` draws.
    - `rho_draws(est, Z, θzt; N)` reconstructs with the frozen `θzt` and returns row-1 (ρ_true) draws.
    - Default `use_gpu = false` on every read call.
  </behavior>
  <read_first>
    - 07-PATTERNS.md → `src/amortized/infer.jl` section (standardize_summary, posterior_for/rho_draws/delta_rho, CRITICAL DELTA use_gpu CPU default, row-partition delegation)
    - spike/npe/infer.jl:65-138
    - src/amortized/summary.jl (07-01) `_summary_row_partition`
  </read_first>
  <action>
    Create `src/amortized/infer.jl` promoting `standardize_summary` (frozen-`zt`, mask rows bypassed — spike/npe/infer.jl:76-84), `posterior_for` (coerce vector→`d_in×1`, `sampleposterior`), `rho_draws` (reconstruct with frozen `θzt`, return row 1), and `delta_rho(est, Zs, Zc, θzt; N)` (MC difference of the two ρ clouds). Keep `use_gpu::Bool = false` as the default on every function (CPU-reproducible shipped inference — 07-RESEARCH §Reproducibility split). Delegate the row split to `_summary_row_partition` from `summary.jl` (grid-parametrization flows automatically). Match the src/ module convention. `include` after `registry.jl`.
  </action>
  <acceptance_criteria>
    - `posterior_for`/`rho_draws`/`delta_rho` all carry `use_gpu::Bool = false` default.
    - `standardize_summary` never re-fits (`StatsBase.transform` only) and leaves mask rows numerically unchanged (unit test on a fixture summary).
    - `rho_draws` reconstructs via `StatsBase.reconstruct(θzt, ...)` before reading row 1.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>NPE read surface promoted; CPU-default; frozen-stats discipline preserved; grid-agnostic via summary.jl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Amortized Bayes factor (bf.jl) + non-clamped baseline</name>
  <files>src/amortized/bf.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `amortized_log_bf(est_bf, Z_pair, log_prior_odds)` returns `(logratio[coloc] - logratio[null]) - log_prior_odds` in one forward pass.
    - `pair_encode(Zs, Zc)` produces the A7 difference encoding of length `ratio_input_dim(G) = 5G²`.
    - A non-clamped BF baseline helper computes the KDE-reference log-BF WITHOUT the 1e-8 probability floor, so `max|Δ logBF|` is measurable free of the clamp artifact.
  </behavior>
  <read_first>
    - 07-PATTERNS.md → `src/amortized/bf.jl + train_ratio.jl` section (amortized_log_bf, pair_encode grid delta RATIO_CONT_ROWS=G²/RATIO_INPUT_DIM=5G², reproduction-gate + non-clamped baseline note)
    - spike/validation/bf.jl:94-100,208-237; spike/validation/train_ratio.jl:185-188
    - STATE.md Phase 5-iter2 (max|Δ logBF| is a KDE clamp/tail artifact at |Δρ|≳0.4)
  </read_first>
  <action>
    Create `src/amortized/bf.jl` promoting `amortized_log_bf` (the `grid = [0.0 1.0]` null/coloc `logratio` one forward pass, measured `log_prior_odds` subtracted — spike/validation/bf.jl:94-100) and `pair_encode` (concat Zs, Zc, and the `cont_rows(G)`-length continuous-row contrast — spike/validation/train_ratio.jl:185-188) using `cont_rows(G)`/`ratio_input_dim(G)` from `summary.jl` instead of the hardcoded 128/320. Add a `kde_log_bf_unclamped(...)` baseline helper mirroring the demoted `compute_BayesFactor` KDE math but WITHOUT the `_clampp = 1e-8` floor (return the raw probability-ratio log even when a tail probability underflows), used only by the ship-gate BF comparison so `max|Δ logBF|` is not floor-saturated (Memo §5). Keep `use_gpu = false` on the ratio forward pass. Do NOT attempt an l-POP loss (the ratio net loss is hardcoded `logitbinarycrossentropy`; a custom loss is silently ignored — 07-PATTERNS delta). Match src/ module convention. `include` after `infer.jl`.
  </action>
  <acceptance_criteria>
    - `length(pair_encode(Zs, Zc)) == ratio_input_dim(G)` for G∈{4,8,16} (unit test).
    - `amortized_log_bf` subtracts the measured `log_prior_odds` and calls `logratio` once with the 1×2 null/coloc grid.
    - `kde_log_bf_unclamped` contains no `1e-8` / `_clampp` floor on the probability ratio.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Amortized log-BF promoted (grid-general dims); non-clamped KDE baseline available for the gate; no clamp floor in the baseline.</done>
</task>

<task type="auto">
  <name>Task 3: OOD flag (ood.jl) — density + noise + re-enabled posterior-predictive</name>
  <files>src/amortized/ood.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-PATTERNS.md → `src/amortized/ood.jl` section (Mahalanobis continuous-rows, noise channel, OR-fusion, hand-rolled ROC, pre-registered id_threshold, finite-guard, with_pp flip)
    - spike/validation/ood.jl:112-132,145-159,251-303,319-335,344-345,375-380,590
    - STATE.md Phase 5-iter1 (finite-θ̂ guard made PP viable) + Phase 5-iter2 (noise channel closes detector-noise blind spot)
  </read_first>
  <action>
    Create `src/amortized/ood.jl` promoting the whole spike OOD flag (zero new deps — hand-rolled Mahalanobis + ROC via LinearAlgebra/Statistics): `fit_ood_nulls` (continuous-rows-only Σ with the `1e-6·I` ridge, rows via `_summary_row_partition`), the image-noise channel (scale/rotation/permutation-invariant features), the hand-rolled Mann-Whitney ROC/AUC, and `ood_verdict(ood_nulls, Zs, Zc)`. **Memo §5 hardening:** flip the reported OR-fusion to include the posterior-predictive channel (`with_pp = true`), relying on the iter1 `_finite_or`/`_theta_tuple` finite-guard (spike/validation/ood.jl:145-159) so PP stays computable on misspecified inputs; keep the density channel's `id_threshold` operating point unchanged so the neg-control byte-identity is preserved. Match src/ module convention. `include` after `bf.jl`. Do NOT edit spike/ood.jl.
  </action>
  <acceptance_criteria>
    - `ood_verdict` fuses density, noise, AND posterior-predictive channels (`with_pp = true` in the reported path).
    - `fit_ood_nulls` restricts to continuous rows via `_summary_row_partition` and applies the `1e-6·I` ridge.
    - PP channel returns a finite θ̂ on a strong-misspecification fixture input (finite-guard exercised, no crash).
    - `git diff --quiet spike/` — spike OOD file unchanged.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()' && git diff --quiet spike/ && echo SPIKE_CLEAN</automated>
  </verify>
  <done>OOD flag promoted with density+noise+PP fusion re-enabled and finite-guarded; spike untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| image input → inference | misspecified / non-finite inputs can crash the posterior or BF path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | DoS (robustness) | posterior-predictive OOD channel on misspecified input | mitigate | reuse the iter1 `_finite_or`/`_theta_tuple` finite-guard so PP re-simulation cannot crash (ood.jl:145-159) |
| T-7-06 | Information (metric integrity) | BF reproduction metric | mitigate | non-clamped KDE baseline removes the 1e-8 floor artifact so `max|Δ logBF|` reflects the estimator, not the clamp |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (infer/bf/ood units).
- `git diff --quiet spike/` — spike unchanged.
</verification>

<success_criteria>
- Amortized read surfaces promoted, grid-general, CPU-default; OOD PP re-enabled; non-clamped BF baseline available.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-02-SUMMARY.md` when done.
</output>

---
phase: 07-productionization-conditional-on-go
plan: 08
type: execute
wave: 6
depends_on: [07-05]
files_modified:
  - src/amortized/local_map.jl
  - src/ProteinCoLoc.jl
  - test/test_local_map.jl
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "The validated 8×8 estimator runs over image sub-tiles (windowed inference) to produce a coarse per-tile colocalization map"
    - "The 8×8 bundle is registered IN-PROCESS from artifacts/grid_8/*.jld2 via load_estimator/load_ood_nulls + register! before inference — NOT via the not-yet-wired Artifacts lazy path"
    - "Each tile yields a Δρ estimate + the OOD flag, using the SAME frozen 8×8 bundle (no new training, no new gate)"
    - "The Phase-12 SpatialColocResult / per-region-map extension point is left clean (NOT implemented here)"
  artifacts:
    - path: "src/amortized/local_map.jl"
      provides: "in-process 8×8 registration bridge + windowed sub-tile inference → coarse local colocalization map"
      contains: "function local_coloc_map"
    - path: "test/test_local_map.jl"
      provides: "tile-grid correctness + per-tile Δρ finiteness test"
      contains: "local_coloc_map"
  key_links:
    - from: "src/amortized/local_map.jl _ensure_grid_registered(8)"
      to: "load_estimator(artifacts/grid_8/*) + register!"
      via: "in-process bridge before estimator_for(8) (07-10 Artifacts not yet wired)"
      pattern: "register!"
    - from: "src/amortized/local_map.jl local_coloc_map"
      to: "delta_rho / is_ood per tile"
      via: "amortized read surface per sub-tile"
      pattern: "delta_rho|is_ood"
---

<objective>
Ship the honest Phase-7 local-localisation feature: run the gated 8×8 estimator over image SUB-TILES (windowed inference) to produce a coarse per-tile colocalization map — no fine-grid training, inheriting the 8×8 gate (07-RESEARCH §Design overlap with Phase 12: the "cheap honest on-ramp").

Purpose: D-04 — the Phase-7 answer to "local localisation" is windowed 8×8 inference, a first-class deliverable that dovetails with (does not pre-empt) Phase 12's true per-region GP/CAR map. Because this plan (wave 6) runs BEFORE 07-10 wires the Artifacts lazy-load path (wave 8), it must register the 8×8 bundle IN-PROCESS from the local `artifacts/grid_8/` files. This plan MUST leave the Phase-12 `SpatialColocResult` / `delta_rho_map` extension point clean.
Output: `src/amortized/local_map.jl` + tests.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md

<interfaces>
<!-- 07-05 persists the gated 8×8 bundle to artifacts/grid_8/{npe_8,ratio_8,ood_nulls_8}.jld2 but does NOT register! it; _REGISTRY is empty until 07-10 wires _lazy_load_from_artifact! + Artifacts.toml (wave 8) -->
<!-- so 07-08 (wave 6) must load in-process: load_estimator + load_ood_nulls (07-03 persist.jl) then rebuild EstimatorBundle + register! -->
<!-- registry (07-01/07-03): EstimatorBundle{grid,npe,ratio,ood_nulls,zt,θzt,calibration}, register!, estimator_for -->
<!-- read surface (07-02): standardize_summary, posterior_for/rho_draws/delta_rho, ood_verdict; summary.jl patch_summary(mci, 8) -->
<!-- src/colocalization.jl patch(img, num_patches) tiles an image — reuse for sub-tiling -->
<!-- D-02: SpatialColocResult / delta_rho_map / uncertainty_map are Phase-12 stubs (comments in src/results.jl) — DO NOT implement -->
<!-- The coarse map is a lightweight struct (tile-indexed Δρ + OOD), NOT SpatialColocResult -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-process 8×8 registration bridge from local artifacts</name>
  <files>src/amortized/local_map.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-PATTERNS.md → `src/registry.jl` (EstimatorBundle fields, register!, estimator_for) + §Shared Patterns (load path)
    - src/amortized/persist.jl (07-03 load_estimator, load_ood_nulls), src/registry.jl (07-01 register!/_REGISTRY)
    - .planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md (artifacts/grid_8/*.jld2 produced, NOT yet registered)
  </read_first>
  <action>
    In `src/amortized/local_map.jl` define `_ensure_grid_registered(grid::Integer; artifact_dir="artifacts/grid_$(grid)")` that, when `!haskey(ProteinCoLoc._REGISTRY, grid)`, loads the CPU-resident bundle DIRECTLY from the local artifact files — `load_estimator(joinpath(artifact_dir, "npe_$(grid).jld2"))`, the ratio via `load_estimator`/`load_ratio`, and `load_ood_nulls(joinpath(artifact_dir, "ood_nulls_$(grid).jld2"))` — rebuilds an `EstimatorBundle` (grid, npe, ratio, ood_nulls, zt, θzt, calibration-from-gate-report), and `register!`s it. This is the in-process bridge for wave-6: it must NOT rely on the `_lazy_load_from_artifact!`/`Artifacts.toml` path (that is wired later in 07-10). Loading a local `.jld2` here is an internal dev-time bridge over trusted, in-repo gate outputs (the public content-hashed path is 07-10). Match the src/ module convention.
  </action>
  <acceptance_criteria>
    - `_ensure_grid_registered(8)` populates `_REGISTRY[8]` from `artifacts/grid_8/{npe_8,ratio_8,ood_nulls_8}.jld2` when the registry is empty, and is a no-op when 8 is already registered.
    - After `_ensure_grid_registered(8)`, `estimator_for(8)` returns the bundle WITHOUT touching `_lazy_load_from_artifact!`/Artifacts.
    - `_ensure_grid_registered` does not reference `_lazy_load_from_artifact!` or `Artifacts.toml`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; ProteinCoLoc._ensure_grid_registered(8); @assert haskey(ProteinCoLoc._REGISTRY, 8)'</automated>
  </verify>
  <done>The 8×8 bundle registers in-process from local artifacts, so `estimator_for(8)` works in wave 6 before 07-10 wires Artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Windowed 8×8 sub-tile local colocalization map</name>
  <files>src/amortized/local_map.jl, src/ProteinCoLoc.jl, test/test_local_map.jl</files>
  <behavior>
    - `local_coloc_map(img, control, channels; tiles=(r,c))` partitions the image into an r×c grid of sub-tiles and runs the 8×8 estimator on each, returning a coarse map of per-tile Δρ (and per-tile OOD flag).
    - It calls `_ensure_grid_registered(8)` then `estimator_for(8)` — no training, no new gate.
    - The returned map has exactly r×c entries; every entry's Δρ is finite or a documented sentinel (finite-guarded).
    - It does NOT define or return a `SpatialColocResult` (Phase-12 extension point stays clean).
  </behavior>
  <read_first>
    - 07-RESEARCH.md §FEASIBILITY VERDICT → "Design overlap with Phase 12" (the sub-tile on-ramp) + Finding 2 (finer input ≠ per-region output; keep this honest)
    - 07-PATTERNS.md → `colocalization_amortized()` read chain + src/results.jl extension-point note
    - src/colocalization.jl:37-60 (patch tiling), src/amortized/infer.jl + summary.jl (07-01/07-02)
  </read_first>
  <action>
    In `src/amortized/local_map.jl` define `local_coloc_map(img, control, channels; tiles::Tuple{Int,Int}=(4,4), N=2000)`: call `_ensure_grid_registered(8)` (Task 1) then `b = estimator_for(8)`; partition `img`/`control` into `tiles` sub-tiles (reuse `patch`-style tiling from `src/colocalization.jl`); for each sub-tile run the frozen 8×8 bundle through the read surface — `standardize_summary(encode_d01(patch_summary(subtile, 8)), b.zt)`, `delta_rho` (MC difference vs the corresponding control sub-tile), and `ood_verdict` — collecting a coarse `LocalColocMap` result (a lightweight struct: `tiles::Tuple`, `delta_rho::Matrix{Float64}`, `ood_flag::Matrix{Bool}`, `meta`). Guard sub-tiles that fall below the ≥15-survivor floor: mark the tile Δρ as a documented finite sentinel and set its OOD flag, never crash. Add a docstring stating this is a COARSE windowed readout, NOT a calibrated per-region map (that is Phase 12). Do NOT define `SpatialColocResult`. Export `local_coloc_map`, `LocalColocMap`. `include` after the amortized read modules. Add `test/test_local_map.jl` and wire into `runtests.jl`.
  </action>
  <acceptance_criteria>
    - `local_coloc_map(img, control, channels; tiles=(r,c))` returns a `LocalColocMap` with an r×c `delta_rho` matrix and an r×c `ood_flag` matrix; every finite-or-sentinel entry is handled (no crash on a below-floor tile).
    - The function calls `_ensure_grid_registered(8)` then `estimator_for(8)` and performs NO training.
    - `grep -v '^#' src/amortized/local_map.jl` contains no `struct SpatialColocResult` and no `delta_rho_map` definition (Phase-12 extension point untouched).
    - Docstring explicitly frames the output as a coarse windowed readout, not a calibrated per-region map.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Windowed 8×8 sub-tile local map ships as a first-class coarse deliverable, registering the 8×8 bundle in-process and leaving the Phase-12 extension point clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sub-tile pixels → per-tile inference | small sub-tiles may fall below the ≥15-survivor floor and produce non-finite correlations |
| local `.jld2` → in-process registration | dev-time bridge loads in-repo gate outputs (trusted); the public content-hashed path is 07-10 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | DoS (robustness) | below-floor sub-tile | mitigate | finite-guard the per-tile Δρ (documented sentinel) + set the tile OOD flag; never crash the map |
| T-7-01 | Tampering | in-process local `.jld2` load | accept | `_ensure_grid_registered` loads only in-repo artifacts produced by the 07-05 gate pipeline; untrusted-artifact loading is out of scope for this internal bridge (public path is content-hashed in 07-10) |
| T-7-09 | Information (scope honesty) | "local map" framing | accept | docstring states coarse windowed readout ≠ calibrated per-region map; true per-region map deferred to Phase 12 |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (in-process registration + tile-grid shape + finiteness + no SpatialColocResult).
</verification>

<success_criteria>
- Coarse windowed local colocalization map ships, registers the 8×8 bundle in-process (no dependency on the not-yet-wired Artifacts path), inherits the 8×8 gate, and leaves Phase-12 extension points clean.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-08-SUMMARY.md` when done.
</output>

---
phase: 07-productionization-conditional-on-go
plan: 07
type: execute
wave: 6
depends_on: [07-05]
files_modified:
  - test/gate/gate_consts_16.jl
  - artifacts/grid_16/npe_16.jld2
  - artifacts/grid_16/ratio_16.jld2
  - artifacts/grid_16/ood_nulls_16.jld2
  - artifacts/grid_16/gate_report_16.jld2
  - .planning/phases/07-productionization-conditional-on-go/gate-16x16.md
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "A 16×16 bundle is produced by `_train_grid_pipeline(16)` on a ≥512²-biased image distribution and persisted CPU-resident"
    - "The datagen step is launched with `-t auto` (parallel, byte-identical to serial via Philox-per-index)"
    - "The 16×16 estimator passes its OWN fresh re-pre-registered CPU SBC/BF/OOD gate keyed by PROD_SEED[16]"
    - "The registry records a minimum-image-size caveat for 16×16"
  artifacts:
    - path: "test/gate/gate_consts_16.jl"
      provides: "fresh 16×16 pre-registration + PROD_SEED[16] + raised SBC_IMSIZE"
      contains: "PROD_SEED"
    - path: "artifacts/grid_16/npe_16.jld2"
      provides: "CPU-resident Flux.state 16×16 NPE"
      contains: ""
    - path: ".planning/phases/07-productionization-conditional-on-go/gate-16x16.md"
      provides: "recorded 16×16 gate outcome + min-image-size caveat"
      contains: "SBC"
  key_links:
    - from: "test/gate/gate_consts_16.jl"
      to: "SBC_IMSIZE ≥512²"
      via: "raised gate image size clears the ≥15-survivor floor"
      pattern: "SBC_IMSIZE"
    - from: "_train_grid_pipeline(16)"
      to: "artifacts/grid_16/*.jld2 CPU-resident"
      via: "shared per-grid pipeline (07-03), datagen under -t auto with ≥512² imsize_set"
      pattern: "_train_grid_pipeline"
---

<objective>
Train, freshly pre-register, and CPU-gate the 16×16 grid — SHIP-WITH-CAVEAT (needs data biased to ≥512²; 256 px/patch at 256² is marginal after background exclusion) — by driving it through the shared `_train_grid_pipeline(16)` helper (07-03).

Purpose: PROD-02 — 16×16 (256 patches, summary dim 512) is calibratable if data is regenerated at adequate image size (07-RESEARCH feasibility: SHIP-WITH-CAVEAT). Full per-grid pipeline with a raised image-size distribution and a documented minimum-image-size caveat.
Output: `test/gate/gate_consts_16.jl`, 16×16 CPU-resident artifacts, recorded gate outcome + caveat.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md

<interfaces>
<!-- shared pipeline (07-03): _train_grid_pipeline(16; imsize_set biased ≥512², use_gpu=has_cuda_device()) → EstimatorBundle, persists artifacts/grid_16/* CPU-resident -->
<!-- datagen uses Threads.@threads gated on nthreads()>1 → MUST launch julia with -t auto; Philox-per-index makes it byte-identical across thread counts (D-11/D-12) -->
<!-- 16×16: summary_dim=512, cont_rows=256, ratio_input_dim=1280; px/patch 256 at 256² (marginal), 1024 at 512² (good) -->
<!-- Pitfall 2: training on the 256²-heavy default cache would learn the prior — bias imsize_set to ≥512² (default_imsize_for(16)) -->
<!-- Follow 07-05 gate shape, grid=16, PROD_SEED[16] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the 16×16 bundle via `_train_grid_pipeline(16)` (≥512² data, datagen under `-t auto`)</name>
  <files>artifacts/grid_16/npe_16.jld2, artifacts/grid_16/ratio_16.jld2, artifacts/grid_16/ood_nulls_16.jld2</files>
  <read_first>
    - .planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md (pipeline shape)
    - 07-RESEARCH.md §FEASIBILITY VERDICT (16×16 row + px/patch table) + §Common Pitfalls Pitfall 2 (256²-heavy cache trains the prior)
    - STATE.md Phase 03-02 (generate_samples parallel==serial byte-identical across -t 1 and -t 4; Philox-per-index, D-12)
    - src/amortized/pipeline.jl (07-03 `_train_grid_pipeline`, `default_imsize_for(16)` ≥512²)
  </read_first>
  <action>
    Run the 16×16 pipeline THROUGH the shared helper, launching Julia with `-t auto`:
    `julia -t auto --project -e 'using ProteinCoLoc; ProteinCoLoc._train_grid_pipeline(16; use_gpu=ProteinCoLoc.has_cuda_device())'`.
    `_train_grid_pipeline(16)` uses `default_imsize_for(16)` (BIASED to ≥512² so patches clear the ≥15-survivor floor — Pitfall 2) and ~50k–100k pairs (07-RESEARCH compute table), then `train_npe`/`train_ratio` (GPU absorbs the 512-dim summary net), `fit_ood_nulls` on a TRAIN-ONLY ID pool, and CPU-resident persist under `artifacts/grid_16/`. Record the realized fraction-missing (should be low — a warning sign if high per Pitfall 2). **Datagen MUST be launched with `-t auto`**: the Julia default `nthreads()==1` silently falls back to serial (`generate_samples` `Threads.@threads`, `parallel = Threads.nthreads() > 1`); Philox-per-index keeps it byte-identical across thread counts (D-11/D-12). Do NOT restate the sequence inline.
  </action>
  <acceptance_criteria>
    - The 16×16 bundle is produced by `_train_grid_pipeline(16)` under `julia -t auto`, with an image-size distribution biased ≥512² (not the 256²-heavy default).
    - `artifacts/grid_16/npe_16.jld2`/`ratio_16.jld2` load via `load_estimator` on CPU and produce finite draws.
    - Realized posterior width is materially narrower than prior width on ID inputs (not prior-collapse).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; @assert ProteinCoLoc.summary_dim(16)==512; b=ProteinCoLoc.load_estimator("artifacts/grid_16/npe_16.jld2"); @assert b !== nothing'</automated>
  </verify>
  <done>16×16 bundle produced via the shared pipeline on adequate-size data (datagen under `-t auto`) and persisted CPU-resident.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh 16×16 pre-registration (gate_consts_16.jl) with raised SBC_IMSIZE</name>
  <files>test/gate/gate_consts_16.jl</files>
  <read_first>
    - test/gate/gate_consts_template.jl + gate_consts_8.jl (07-05)
    - 07-PATTERNS.md → `gate_consts_G.jl` (raise SBC_IMSIZE per grid so fine grids clear the floor)
  </read_first>
  <action>
    Instantiate `test/gate/gate_consts_16.jl` from the template with FRESH 16×16 thresholds, a disjoint `PROD_SEED[16]`, and `SBC_IMSIZE` raised to ≥512² so the gate simulations clear the ≥15-survivor floor. Commit BEFORE the Task-3 gate run.
  </action>
  <acceptance_criteria>
    - `PROD_SEED[16]` ∉ {0x5BC0FFEE, 0xC0FFEE, PROD_SEED[8], PROD_SEED[4]}.
    - `SBC_IMSIZE` ≥ 512² (larger than the 8×8 gate's image size).
    - Committed before the gate run.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'include("test/gate/gate_consts_16.jl"); @assert PROD_SEED[16] != 0x5BC0FFEE && PROD_SEED[16] != 0xC0FFEE'</automated>
  </verify>
  <done>Fresh 16×16 pre-registration committed with a disjoint seed and raised gate image size.</done>
</task>

<task type="auto">
  <name>Task 3: Run + record the 16×16 CPU SBC/BF/OOD gate + caveat</name>
  <files>artifacts/grid_16/gate_report_16.jld2, .planning/phases/07-productionization-conditional-on-go/gate-16x16.md</files>
  <read_first>
    - test/gate/run_gate.jl (07-04) + gate-8x8.md (07-05)
    - 07-RESEARCH.md §FEASIBILITY VERDICT (16×16 min-image-size caveat to document in the registry)
  </read_first>
  <action>
    Run `test/gate/run_gate.jl --grid 16 --sbc --bf --ood` against the CPU-resident 16×16 net (`use_gpu=false`) keyed by `PROD_SEED[16]`, non-clamped BF baseline, `with_pp=true`. This determinism-pinned reported gate is NOT launched with `-t auto` (datagen-only). Write the atomic gate report and a `gate-16x16.md` recording SBC/BF/OOD numbers vs the pre-registered thresholds, a PASS/FAIL verdict, AND the minimum-image-size caveat (16×16 is informative only on ≥512² images) that the registry entry must carry. On PASS, 16×16 is eligible for registry population (07-10).
  </action>
  <acceptance_criteria>
    - `run_gate.jl --grid 16` completes CPU-only and writes `gate_report_16.jld2`.
    - `gate-16x16.md` records the verdict against `gate_consts_16.jl` AND the minimum-image-size caveat.
  </acceptance_criteria>
  <verify>
    <automated>julia --project test/gate/run_gate.jl --grid 16 --sbc --bf --ood</automated>
  </verify>
  <done>16×16 gate ran CPU-reproducibly on a fresh seed; verdict + min-image-size caveat recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| training image distribution → calibration | too-small images collapse the posterior to prior (calibrated-but-uninformative) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03 | DoS (uninformative estimator) | 16×16 on 256²-heavy data | mitigate | bias `imsize_set` ≥512² (default_imsize_for(16)); assert posterior narrower than prior (Pitfall 2); document min-image-size caveat |
| T-7-08 | Repudiation (data-snooping) | 16×16 gate | mitigate | fresh disjoint `PROD_SEED[16]` before the run |
</threat_model>

<verification>
- `run_gate.jl --grid 16` completes; `gate-16x16.md` records verdict + caveat.
</verification>

<success_criteria>
- 16×16 produced via the shared pipeline on ≥512²-biased data (datagen under `-t auto`), CPU-gated on a fresh seed, with a recorded min-image-size caveat.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-07-SUMMARY.md` when done.
</output>

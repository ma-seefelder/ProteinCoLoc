---
phase: 07-productionization-conditional-on-go
plan: 10
type: execute
wave: 8
depends_on: [07-05, 07-06, 07-07, 07-08, 07-09]
files_modified:
  - src/amortized/api.jl
  - src/ProteinCoLoc.jl
  - Artifacts.toml
  - src/registry.jl
  - test/test_integration.jl
  - docs/amortized.md
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "`colocalization_amortized(img, control, channels; num_patches)` is the public entry point returning an AmortizedColocResult"
    - "The registry lazy-loads each shipped grid from a content-hashed Artifacts.toml (tree-sha1 verified), not bundled-in-repo"
    - "Only grids that PASSED their fresh CPU gate populate `_SHIPPED_GRIDS`; 32×32 per the user decision; 64×64 absent"
    - "An integration test runs the full public path end-to-end for the 8×8 grid and dispatches accessors on AbstractColocResult"
  artifacts:
    - path: "src/amortized/api.jl"
      provides: "colocalization_amortized public entry composing the read surfaces"
      contains: "function colocalization_amortized"
    - path: "Artifacts.toml"
      provides: "content-hashed lazy artifact entries for the shipped grids"
      contains: "git-tree-sha1"
    - path: "test/test_integration.jl"
      provides: "end-to-end public-path test + accessor dispatch test"
      contains: "colocalization_amortized"
  key_links:
    - from: "src/amortized/api.jl colocalization_amortized"
      to: "estimator_for(num_patches)"
      via: "registry lookup then read surface"
      pattern: "estimator_for"
    - from: "src/registry.jl _lazy_load_from_artifact!"
      to: "Artifacts.toml content hash"
      via: "tree-sha1 verified lazy load"
      pattern: "artifact"
---

<objective>
Assemble the public breaking-release API: `colocalization_amortized` as the primary entry point, the content-hashed lazy `Artifacts.toml` estimator storage, registry population of ONLY the grids that passed their fresh CPU gate (32×32 per the user decision, 64×64 absent), and an end-to-end integration test.

Purpose: PROD-01 (amortized inference is the primary public API) + PROD-02 (num_patches user-definable via the registry). This closes D-01 (breaking amortized-only public surface) and D-04 (capped shipped family from Artifacts). Ships only gate-PASSED grids — no provisional estimators in the public registry (D-05).
Output: `src/amortized/api.jl`, `Artifacts.toml`, updated `src/registry.jl`, integration test, `docs/amortized.md`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/gate-8x8.md
@.planning/phases/07-productionization-conditional-on-go/gate-4x4.md
@.planning/phases/07-productionization-conditional-on-go/gate-16x16.md
@.planning/phases/07-productionization-conditional-on-go/gate-32x32.md

<interfaces>
<!-- api analog: 07-PATTERNS colocalization_amortized() read chain + 07-RESEARCH §Code Examples (public entry shape) -->
<!-- signature exemplar: src/bayes.jl::colocalization L234-242 (num_patches is the registry key) -->
<!-- registry (07-01): estimator_for, _lazy_load_from_artifact! (stub to wire here), _SHIPPED_GRIDS -->
<!-- read surfaces (07-02): standardize_summary, posterior_for, rho_draws/delta_rho, amortized_log_bf, ood_verdict, pair_encode -->
<!-- Artifacts: content-hashed tree-sha1, GitHub-Release-hosted lazy download (07-RESEARCH Standard Stack; supply-chain: only load integrity-checked model files) -->
<!-- gate reports (07-05..07-09) determine which grids PASSED → _SHIPPED_GRIDS population; 32 per user decision; 64 absent -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public entry point colocalization_amortized</name>
  <files>src/amortized/api.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `colocalization_amortized(img, control, channels; num_patches=8, N=2000)` returns an `AmortizedColocResult` with posterior draws, log-BF, Δρ cloud, and OOD verdict.
    - An unregistered `num_patches` raises the registry's `train_and_register`-pointing `ArgumentError` (no silent fallback).
    - The four accessors (`delta_rho`, `bayes_factor`, `is_ood`, `posterior_draws`) dispatch on the returned result via `AbstractColocResult`.
  </behavior>
  <read_first>
    - 07-PATTERNS.md → `colocalization_amortized()` section (the full read chain) + 07-RESEARCH §Code Examples
    - src/bayes.jl:234-242 (signature exemplar), src/amortized/{infer,bf,ood,summary}.jl, src/registry.jl
  </read_first>
  <action>
    Create `src/amortized/api.jl` defining `colocalization_amortized(img, control, channels; num_patches::Integer=8, N=2000)`: look up `b = estimator_for(num_patches)` (registry, PROD-02); compute frozen summaries `Zs`/`Zc` via `standardize_summary(encode_d01(patch_summary(·, num_patches)), b.zt)`; draw the posterior via `posterior_for(b.npe, Zs; N, use_gpu=false)` and reconstruct with `b.θzt`; compute `Δρ` via `rho_draws(...)` difference; `logbf` via `amortized_log_bf(b.ratio, pair_encode(Zs, Zc), b.ratio_log_prior_odds)`; `ood` via `ood_verdict(b.ood_nulls, Zs, Zc)`; return `AmortizedColocResult(num_patches, reconstruct(b.θzt, post), Δρ, logbf, ood, b.calibration, (; N))`. Validate `channels`/image shape as `_prepare_data` does (V5 input validation) and validate finiteness at the boundary. Default `use_gpu=false` throughout (CPU-reproducible shipped default, D-06). Export `colocalization_amortized`. `include` after the amortized modules. Match src/ module convention.
  </action>
  <acceptance_criteria>
    - `colocalization_amortized(...; num_patches=8)` returns an `AmortizedColocResult` on a fixture stack (via a registered 8×8 bundle).
    - `colocalization_amortized(...; num_patches=7)` raises `ArgumentError` whose message contains `train_and_register`.
    - `delta_rho`/`bayes_factor`/`is_ood`/`posterior_draws` all resolve on the returned result.
    - Every inference call inside uses `use_gpu=false`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Public amortized entry point ships, composes the read surfaces, validates input, and returns a type-agnostic-accessible AmortizedColocResult.</done>
</task>

<task type="auto">
  <name>Task 2: Content-hashed Artifacts + registry population of passed grids</name>
  <files>Artifacts.toml, src/registry.jl</files>
  <read_first>
    - 07-RESEARCH.md §Standard Stack (Artifacts lazy content-hashed) + §Package Legitimacy Audit supply-chain note (only load integrity-checked model files) + §Security Domain (tree-sha1 before load)
    - 07-PATTERNS.md → `src/registry.jl` (_lazy_load_from_artifact! hook)
    - gate-8x8.md / gate-4x4.md / gate-16x16.md / gate-32x32.md (which grids PASSED + the 32×32 user decision)
  </read_first>
  <action>
    Create `Artifacts.toml` with a lazy, content-hashed (`git-tree-sha1`) entry per SHIPPED grid, hosting the CPU-resident `artifacts/grid_G/` bundles as GitHub-Release-hosted download assets (not bundled in git history). Wire `_lazy_load_from_artifact!(grid)` in `src/registry.jl` to resolve the artifact via `Pkg.Artifacts.@artifact_str`/`artifact_path` (tree-sha1 verified before load) and rebuild the `EstimatorBundle` via `load_estimator`. Populate `_SHIPPED_GRIDS` with ONLY the grids whose `gate-*.md` recorded PASS: 4, 8, 16 (if each passed) and 32 IFF the 07-09 user decision was ship-with-caveat; NEVER 64. Attach the recorded caveats (16×16 min-image-size; 32×32 large-image) to each shipped registry entry's metadata. A grid that failed its gate is excluded with its reason retained.
  </action>
  <acceptance_criteria>
    - `Artifacts.toml` has a `git-tree-sha1` entry for each shipped grid and `lazy = true`.
    - `_lazy_load_from_artifact!(8)` verifies the tree-sha1 and returns a working `EstimatorBundle` (8×8 inference finite).
    - `_SHIPPED_GRIDS` contains exactly the gate-PASSED grids; `64 ∉ _SHIPPED_GRIDS`; `32 ∈ _SHIPPED_GRIDS` iff the user chose ship-with-caveat.
    - Shipped 16×16 (and 32×32 if shipped) entries carry their image-size caveat in metadata.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using TOML; a=TOML.parsefile("Artifacts.toml"); @assert !haskey(a, "grid_64"); for (k,v) in a; @assert haskey(v,"git-tree-sha1"); end'</automated>
  </verify>
  <done>Shipped grids load lazily from content-hashed Artifacts; only gate-PASSED grids populate the registry; 64×64 absent; caveats attached.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration test + amortized docs</name>
  <files>test/test_integration.jl, docs/amortized.md, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-VALIDATION.md → Phase Requirements → Test Map (PROD-01/PROD-02 acceptance)
    - src/amortized/api.jl (Task 1), src/registry.jl (Task 2)
    - src/results.jl (accessor interface, D-02)
  </read_first>
  <action>
    Create `test/test_integration.jl` exercising the full public path for the 8×8 grid: lazy-load via `estimator_for(8)` → `colocalization_amortized` on a fixture stack → assert an `AmortizedColocResult` with finite posterior/Δρ/log-BF and a boolean OOD flag → assert the four `AbstractColocResult` accessors dispatch → assert `colocalization_amortized(...; num_patches=64)` errors (64 not shipped) and an unregistered grid points at `train_and_register`. Wire into `test/runtests.jl`. Write `docs/amortized.md` documenting the breaking-release API (D-01), the registry + `train_and_register` (PROD-02), the shipped grids + their caveats, the sub-tile local map (07-08), and the CPU-reproducible-by-default inference note (D-06). Confirm the Turing path stays unexported.
  </action>
  <acceptance_criteria>
    - `test/test_integration.jl` runs the full 8×8 public path and asserts finite outputs + accessor dispatch.
    - `colocalization_amortized(...; num_patches=64)` raises (64 not shipped); unregistered-grid error mentions `train_and_register`.
    - `docs/amortized.md` documents the public API, registry, shipped grids + caveats, sub-tile map, and CPU-default note.
    - `julia --project -e 'using ProteinCoLoc; @assert !in(:compute_BayesFactor, names(ProteinCoLoc))'` (Turing path unexported).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Full public path integration-tested; docs written; only gated grids shipped; Turing path internal-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| downloaded artifact → loaded estimator | an untrusted `.jld2` reconstructs arbitrary Julia types |
| public API input → registry/inference | user-supplied `num_patches` and image shape must be validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering/Elevation | Artifacts estimator load | mitigate | content-hashed (tree-sha1) lazy Artifacts; verify hash before load; never load user-supplied `.jld2` in the public path |
| T-7-05 | Spoofing (input) | `colocalization_amortized(num_patches)` | mitigate | validate grid against `_REGISTRY`/`_SHIPPED_GRIDS` (clear error, no silent default); validate channels/image shape (V5) |
| T-7-SC | Tampering | shipped model provenance | accept | Julia General deps pinned; models produced in-repo by gate-PASSED pipelines; slopcheck N/A for Julia |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (integration + accessor dispatch + unregistered/64 error paths).
- `Artifacts.toml` has content-hashed lazy entries for shipped grids only; no `grid_64`.
</verification>

<success_criteria>
- Amortized-only public API ships; registry lazy-loads content-hashed shipped grids; only gate-PASSED grids populate `_SHIPPED_GRIDS`; Turing path internal-only; version 2.0.0.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-10-SUMMARY.md` when done.
</output>

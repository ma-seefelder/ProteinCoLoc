---
phase: 07-productionization-conditional-on-go
plan: 06
type: execute
wave: 6
depends_on: [07-05]
files_modified:
  - test/gate/gate_consts_4.jl
  - artifacts/grid_4/npe_4.jld2
  - artifacts/grid_4/ratio_4.jld2
  - artifacts/grid_4/ood_nulls_4.jld2
  - artifacts/grid_4/gate_report_4.jld2
  - .planning/phases/07-productionization-conditional-on-go/gate-4x4.md
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "The 4×4 bundle is produced by the PUBLIC `train_and_register(4)` path (the user-definable-grid happy path exercised end-to-end)"
    - "The datagen step is launched with `-t auto` (parallel, byte-identical to serial via Philox-per-index)"
    - "The 4×4 estimator passes its OWN fresh re-pre-registered CPU SBC/BF/OOD gate keyed by PROD_SEED[4]"
    - "4×4 is robust at any image size (≥4096 px/patch even at 256²) — the coarse-but-robust grid"
  artifacts:
    - path: "test/gate/gate_consts_4.jl"
      provides: "fresh 4×4 pre-registration + PROD_SEED[4]"
      contains: "PROD_SEED"
    - path: "artifacts/grid_4/npe_4.jld2"
      provides: "CPU-resident Flux.state 4×4 NPE"
      contains: ""
    - path: ".planning/phases/07-productionization-conditional-on-go/gate-4x4.md"
      provides: "recorded 4×4 gate outcome"
      contains: "SBC"
  key_links:
    - from: "train_and_register(4)"
      to: "_train_grid_pipeline(4) + register!"
      via: "public user-definable-grid path (PROD-02/D-04), datagen under -t auto"
      pattern: "train_and_register"
    - from: "test/gate/gate_consts_4.jl PROD_SEED[4]"
      to: "run_gate.jl --grid 4"
      via: "fresh disjoint seed"
      pattern: "PROD_SEED"
---

<objective>
Train, freshly pre-register, and CPU-gate the 4×4 grid — the coarse-but-robust registry entry — by driving it THROUGH the public `train_and_register(4)` path. This is the plan that actually exercises the PROD-02/D-04 user-definable-grid happy path end-to-end (not inline prose).

Purpose: PROD-02 — 4×4 (16 patches, summary dim 32) is robust at any image size (07-RESEARCH feasibility: SHIP). Running it via `train_and_register(4)` verifies `_train_grid_pipeline` + `register!` + in-process `estimator_for(4)` all work, then the grid passes its own fresh CPU SBC/BF/OOD gate.
Output: `test/gate/gate_consts_4.jl`, 4×4 CPU-resident artifacts, recorded gate outcome.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md

<interfaces>
<!-- PUBLIC path (07-03): train_and_register(grid; use_gpu=has_cuda_device()) = _train_grid_pipeline(grid) then register!; afterwards estimator_for(grid) returns the bundle in-process -->
<!-- datagen uses Threads.@threads gated on nthreads()>1 → MUST launch julia with -t auto; Philox-per-index makes it byte-identical across thread counts (D-11/D-12) -->
<!-- _train_grid_pipeline persists artifacts/grid_4/{npe,ratio,ood_nulls}_4.jld2 CPU-resident -->
<!-- gate surfaces (07-04): run_gate.jl, gate_consts_template.jl -->
<!-- 4×4: summary_dim=32, cont_rows=16, ratio_input_dim=80; ≥4096 px/patch at 256² (robust) -->
<!-- Follow the 07-05 gate shape, grid=4, PROD_SEED[4] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce + register the 4×4 bundle via `train_and_register(4)` (datagen under `-t auto`)</name>
  <files>artifacts/grid_4/npe_4.jld2, artifacts/grid_4/ratio_4.jld2, artifacts/grid_4/ood_nulls_4.jld2</files>
  <read_first>
    - .planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md (the proven pipeline shape)
    - 07-RESEARCH.md §Compute cost reality (4×4 row: ~1× sim, trivial GPU) + §Architecture Patterns Pattern 2 (train_and_register)
    - STATE.md Phase 03-02 (generate_samples parallel==serial byte-identical across -t 1 and -t 4; Philox-per-index, D-12)
    - src/registry.jl (train_and_register), src/amortized/pipeline.jl (_train_grid_pipeline) — both from 07-03
  </read_first>
  <action>
    Drive the full 4×4 pipeline THROUGH the public helper, launching Julia with `-t auto` so datagen parallelizes:
    `julia -t auto --project -e 'using ProteinCoLoc; ProteinCoLoc.train_and_register(4; use_gpu=ProteinCoLoc.has_cuda_device())'`.
    `train_and_register(4)` runs `_train_grid_pipeline(4)` (datagen grid=4 with the 256²-heavy default `imsize_set` — 4×4 is robust there → `train_npe` → `train_ratio` → `fit_ood_nulls` on a TRAIN-ONLY ID pool → CPU-resident persist under `artifacts/grid_4/`) and `register!`s the returned `EstimatorBundle`. This plan is the designated end-to-end exercise of the user-definable-grid path — do NOT restate the sequence inline. Confirm `estimator_for(4)` returns the bundle in-process after the call. **Datagen MUST be launched with `-t auto`**: the Julia default `nthreads()==1` causes a SILENT serial fallback (`generate_samples` uses `Threads.@threads` with `parallel = Threads.nthreads() > 1`). Reproducibility is thread-count-independent (Philox-per-index, D-11/D-12), so `-t auto` is byte-identical to the serial path.
  </action>
  <acceptance_criteria>
    - `train_and_register(4)` (launched under `julia -t auto`) returns an `EstimatorBundle` and, afterward, `estimator_for(4)` returns that same bundle in-process (no UndefVarError, no unregistered-grid error).
    - `artifacts/grid_4/{npe_4,ratio_4,ood_nulls_4}.jld2` exist and load via `load_estimator`/`load_ood_nulls` on CPU with finite draws.
    - The 4×4 cache content-hash dir differs from the 8×8 dir (summary_dim(4)=32 ≠ 128).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; @assert ProteinCoLoc.summary_dim(4)==32; b=ProteinCoLoc.load_estimator("artifacts/grid_4/npe_4.jld2"); @assert b !== nothing'</automated>
  </verify>
  <done>4×4 bundle produced + registered via the public `train_and_register(4)` path (datagen under `-t auto`); persisted CPU-resident.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh 4×4 pre-registration (gate_consts_4.jl)</name>
  <files>test/gate/gate_consts_4.jl</files>
  <read_first>
    - test/gate/gate_consts_template.jl + test/gate/gate_consts_8.jl (07-05) as the instantiation exemplar
    - 07-PATTERNS.md → `gate_consts_G.jl` (Pitfall 3 disjoint seed)
  </read_first>
  <action>
    Instantiate `test/gate/gate_consts_4.jl` from the template with FRESH re-expressed 4×4 thresholds and a `PROD_SEED[4]` from the disjoint Philox stream. Commit BEFORE the Task-3 gate run.
  </action>
  <acceptance_criteria>
    - `PROD_SEED[4]` ∉ {0x5BC0FFEE, 0xC0FFEE, PROD_SEED[8]}.
    - Committed before the gate run.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'include("test/gate/gate_consts_4.jl"); @assert PROD_SEED[4] != 0x5BC0FFEE && PROD_SEED[4] != 0xC0FFEE'</automated>
  </verify>
  <done>Fresh 4×4 pre-registration committed with a disjoint seed.</done>
</task>

<task type="auto">
  <name>Task 3: Run + record the 4×4 CPU SBC/BF/OOD gate</name>
  <files>artifacts/grid_4/gate_report_4.jld2, .planning/phases/07-productionization-conditional-on-go/gate-4x4.md</files>
  <read_first>
    - test/gate/run_gate.jl (07-04) + gate-8x8.md (07-05) as the reporting exemplar
    - STATE.md Phase 5 memo §6 pass conditions
  </read_first>
  <action>
    Run `test/gate/run_gate.jl --grid 4 --sbc --bf --ood` against the CPU-resident 4×4 net (`use_gpu=false`) keyed by `PROD_SEED[4]`, non-clamped BF baseline, `with_pp=true`. This determinism-pinned reported gate is NOT launched with `-t auto` (that is datagen-only). Write the atomic gate report and a `gate-4x4.md` recording SBC/BF/OOD numbers vs the pre-registered thresholds and a PASS/FAIL verdict. On PASS, 4×4 is eligible for registry population (07-10).
  </action>
  <acceptance_criteria>
    - `run_gate.jl --grid 4` completes CPU-only and writes `gate_report_4.jld2`.
    - `gate-4x4.md` records the verdict against `gate_consts_4.jl`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project test/gate/run_gate.jl --grid 4 --sbc --bf --ood</automated>
  </verify>
  <done>4×4 gate ran CPU-reproducibly on a fresh seed; verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| trained net → reported gate | fresh-seed evaluation required |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Repudiation (data-snooping) | 4×4 gate | mitigate | fresh `PROD_SEED[4]` committed before the run; disjoint from spike + other grid seeds |
| T-7-07 | Repudiation (reproducibility) | gate inference | mitigate | `use_gpu=false` against CPU-resident net; gate not run under `-t auto` |
</threat_model>

<verification>
- `train_and_register(4)` (under `-t auto`) populates the in-process registry; `run_gate.jl --grid 4` completes; `gate-4x4.md` records the verdict.
</verification>

<success_criteria>
- 4×4 produced + registered via the public `train_and_register(4)` path (datagen under `-t auto`), persisted CPU-resident, fresh-pre-registered, and CPU-gated.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-06-SUMMARY.md` when done.
</output>

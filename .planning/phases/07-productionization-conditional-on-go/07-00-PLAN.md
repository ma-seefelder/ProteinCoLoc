---
phase: 07-productionization-conditional-on-go
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Project.toml
  - src/ProteinCoLoc.jl
  - src/bayes.jl
  - src/results.jl
  - ext/ProteinCoLocTuringExt.jl
  - test/runtests.jl
autonomous: false
requirements: [PROD-01]
must_haves:
  truths:
    - "Root `Pkg.resolve` pins NeuralEstimators 0.2.1 (never downgraded to 0.1.4)"
    - "The Turing/ADVI reference path compiles only when Turing is present (weakdep extension), not on the core resolve"
    - "`AbstractColocResult` supertype with `AmortizedColocResult` subtype and a shared accessor interface exists and is exported"
    - "Root package version is 2.0.0 and the Turing path is NOT exported"
  artifacts:
    - path: "src/results.jl"
      provides: "AbstractColocResult + AmortizedColocResult + accessor interface + Phase-12/13 extension stubs"
      contains: "abstract type AbstractColocResult"
    - path: "ext/ProteinCoLocTuringExt.jl"
      provides: "internal ADVI colocalization() + compute_BayesFactor() + AdviColocResult, weakdep-gated"
      contains: "module ProteinCoLocTuringExt"
    - path: "Project.toml"
      provides: "hard deps NeuralEstimators/Flux/JLD2/HypothesisTests; weakdeps Turing/CUDA; version 2.0.0"
      contains: "version = \"2.0.0\""
    - path: "test/runtests.jl"
      provides: "co-resolution assertion gate"
      contains: "NeuralEstimators"
  key_links:
    - from: "ext/ProteinCoLocTuringExt.jl"
      to: "src/results.jl"
      via: "AdviColocResult <: AbstractColocResult"
      pattern: "AdviColocResult\\s*<:\\s*AbstractColocResult"
    - from: "Project.toml [extensions]"
      to: "ext/ProteinCoLocTuringExt.jl"
      via: "weakdep Turing triggers extension load"
      pattern: "ProteinCoLocTuringExt"
---

<objective>
Lay the breaking-release foundation: the D-02 result-type hierarchy, the D-03 dependency surgery that moves the internal Turing/ADVI path off the core runtime, and the BLOCKING co-resolution gate that proves NeuralEstimators 0.2.1 resolves in the root package.

Purpose: Finding 1 (07-RESEARCH.md) — the current Turing+GLMakie root tree caps NeuralEstimators below 0.2.1 and silently downgrades it to 0.1.4, breaking the v0.2.1 API. No `src/` inference code may land until the root resolve is green with 0.2.1. This plan also establishes the governing D-02 type hierarchy every downstream plan dispatches on.
Output: `src/results.jl` (D-02 hierarchy), `ext/ProteinCoLocTuringExt.jl` (Turing internal-only), a reworked `Project.toml` (hard deps + weakdeps + version 2.0.0), and a `test/runtests.jl` resolve assertion. Version bumped to 2.0.0.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-productionization-conditional-on-go/07-CONTEXT.md
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md

<interfaces>
<!-- Existing struct to demote (src/bayes.jl:75-82) — becomes internal AdviColocResult, fields verbatim -->
CoLocResult{ img::MultiChannelImageStack; control::MultiChannelImageStack; channels::Vector{Int64}; num_patches::Int64; posterior::DataFrame; advi_result }
<!-- Existing Δρ access to preserve (src/bayes.jl:110): posterior.μ_sample .- posterior.μ_control -->
<!-- Module import/export sites to edit: src/ProteinCoLoc.jl:22-52 (import KernelDensity:kde L30, QuadGK:quadgk L31, using Turing L35-36, export … colocalization … compute_BayesFactor … CoLocResult L49-51) -->
<!-- D-02 target hierarchy is fully specified in 07-RESEARCH.md §Pattern 1 and 07-PATTERNS.md src/results.jl section -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-02 result-type hierarchy in src/results.jl</name>
  <files>src/results.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-RESEARCH.md §Architecture Patterns → Pattern 1 (the exact hierarchy + accessor sketch)
    - 07-PATTERNS.md → `src/results.jl` section (CoLocResult demotion + CalibrationResult port from spike/validation/sbc.jl:65-72)
    - src/bayes.jl:75-82 (CoLocResult struct to demote) and src/bayes.jl:110 (Δρ column access)
    - src/ProteinCoLoc.jl:1-52 (module structure, include order, export list)
  </read_first>
  <action>
    Create `src/results.jl` defining: abstract `AbstractColocResult`; the accessor interface `delta_rho`, `bayes_factor`, `is_ood`, `posterior_draws` defined on the abstract type with an `_iface_error` fallback; concrete `AmortizedColocResult <: AbstractColocResult` with fields `grid::Int`, `posterior::Matrix{Float64}` (7×N, row 1 = ρ_true), `delta_rho_draws::Vector{Float64}`, `log_bayes_factor::Float64`, `ood::OODVerdict`, `calibration::CalibrationMeta`, `meta::NamedTuple`, and its four accessor methods. Define the supporting structs `OODVerdict` (score, flag, per-channel breakdown NamedTuple) and `CalibrationMeta` (port `CalibrationResult` fields from spike/validation/sbc.jl:65-72: bin_midpoints, predicted_rate, observed_rate, bin_counts, ece, mce — plus grid + gate provenance). Add commented-only extension-point sketches for Phase 13 `ThreeHypothesisColocResult`/`bayes_factor_simplex` and Phase 12 `SpatialColocResult`/`delta_rho_map`/`uncertainty_map` (do NOT implement). Add `include("results.jl")` to `src/ProteinCoLoc.jl` and export `AbstractColocResult, AmortizedColocResult, delta_rho, bayes_factor, is_ood, posterior_draws` (do NOT export `AdviColocResult`).
  </action>
  <acceptance_criteria>
    - `src/results.jl` defines `abstract type AbstractColocResult end` and the four accessors on the abstract type.
    - `AmortizedColocResult <: AbstractColocResult` with the exact fields named above; its four accessors are defined.
    - Phase-12 `SpatialColocResult` and Phase-13 `ThreeHypothesisColocResult` appear ONLY as comments (no `struct` definition, no method body).
    - `src/ProteinCoLoc.jl` export list contains `AmortizedColocResult`, `delta_rho`, `bayes_factor`, `is_ood`, `posterior_draws` and does NOT contain `AdviColocResult`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; @assert isabstracttype(ProteinCoLoc.AbstractColocResult); @assert ProteinCoLoc.AmortizedColocResult <: ProteinCoLoc.AbstractColocResult; @assert !isdefined(Main, :AdviColocResult) || !in(:AdviColocResult, names(ProteinCoLoc))'</automated>
  </verify>
  <done>Abstract hierarchy + AmortizedColocResult + accessors exist and are exported; extension points are comments only; AdviColocResult not exported.</done>
</task>

<task type="auto">
  <name>Task 2: Dependency surgery — Turing→extension, hard deps, version 2.0.0</name>
  <files>Project.toml, ext/ProteinCoLocTuringExt.jl, src/bayes.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-RESEARCH.md §Architecture Patterns → Pattern 3 (weakdep extension) + §Standard Stack (Turing OFF core; hard deps list)
    - 07-PATTERNS.md → `ext/ProteinCoLocTuringExt.jl` section + `Project.toml` section
    - src/ProteinCoLoc.jl:22-52 (imports + export list) and src/bayes.jl:42-336 (colocalization, compute_BayesFactor L109-136, convert_posterior_samples L42-54, _prepare_data L164)
    - Project.toml (current 13 deps, version 1.0.1, GLMakie 0.10.5 compat pin)
  </read_first>
  <action>
    Move the ADVI reference into a package extension. Create `ext/ProteinCoLocTuringExt.jl` (`module ProteinCoLocTuringExt`) containing the `@model`, ADVI `colocalization()`, `compute_BayesFactor()`, `convert_posterior_samples()`, `_prepare_data()`, and the renamed `AdviColocResult <: ProteinCoLoc.AbstractColocResult` (fields verbatim from CoLocResult); define its `delta_rho` accessor over `posterior.μ_sample .- posterior.μ_control` and a `bayes_factor` accessor delegating to `compute_BayesFactor`. In `Project.toml`: add hard `[deps]` NeuralEstimators (pin `= "0.2.1"` in `[compat]`), Flux (pin `= "0.16.10"`), JLD2, HypothesisTests; move `Turing` into `[weakdeps]`; add `CUDA` to `[weakdeps]` (optional GPU training, D-06); add `[extensions] ProteinCoLocTuringExt = "Turing"`; bump `version = "2.0.0"`. In `src/ProteinCoLoc.jl` remove the `using Turing`/`Variational`, `import KernelDensity: kde`, `import QuadGK: quadgk` lines and delete `colocalization`, `compute_BayesFactor`, `CoLocResult` from the export list. Keep `src/bayes.jl` as the source of the ADVI body only if still `include`d for the ext; otherwise relocate its Turing body into the ext file. Do NOT export the Turing path or `AdviColocResult` (D-01).
  </action>
  <acceptance_criteria>
    - `Project.toml` has `version = "2.0.0"`; `[deps]` includes NeuralEstimators, Flux, JLD2, HypothesisTests; `[weakdeps]` includes Turing and CUDA; `[extensions]` maps `ProteinCoLocTuringExt = "Turing"`; `[compat]` pins `NeuralEstimators = "0.2.1"` and `Flux = "0.16.10"`.
    - `ext/ProteinCoLocTuringExt.jl` defines `AdviColocResult <: ProteinCoLoc.AbstractColocResult` and the ADVI `colocalization` + `compute_BayesFactor`.
    - `src/ProteinCoLoc.jl` no longer contains `using Turing`; export list contains neither `colocalization`, `compute_BayesFactor`, nor `CoLocResult`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using TOML; p=TOML.parsefile("Project.toml"); @assert p["version"]=="2.0.0"; @assert haskey(p["deps"],"NeuralEstimators"); @assert haskey(p["weakdeps"],"Turing"); @assert haskey(p["weakdeps"],"CUDA"); @assert p["extensions"]["ProteinCoLocTuringExt"]=="Turing"; @assert p["compat"]["NeuralEstimators"]=="0.2.1"'</automated>
  </verify>
  <done>Turing/KDE/QuadGK are extension-only; NeuralEstimators/Flux/JLD2/HypothesisTests are hard deps; CUDA is a weakdep; version is 2.0.0; Turing path unexported.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Co-resolution HARD gate — Pkg.resolve pins NeuralEstimators 0.2.1</name>
  <files>test/runtests.jl, Project.toml</files>
  <read_first>
    - 07-RESEARCH.md §Common Pitfalls → Pitfall 1 (silent 0.1.4 downgrade) + §Open Questions Q1 (does Turing→ext clear resolve) + Assumption A3
    - STATE.md line 89 (Phase 01-04 co-resolution record — the Finding-1 anchor)
    - 07-VALIDATION.md → Wave 0 Requirements + Manual-Only Verifications (escalation on resolve failure)
  </read_first>
  <action>
    Run an actual root `Pkg.resolve()` after the Task-2 surgery and add a machine assertion to `test/runtests.jl` that the resolved Manifest pins NeuralEstimators **0.2.1** and Flux **0.16.10** (read installed versions via `Pkg.dependencies()`, mirroring the spike's resolve-risk gate — NOT a Manifest regex). If resolve succeeds and pins 0.2.1: commit the regenerated root `Manifest.toml` and record the outcome. If resolve STILL downgrades NeuralEstimators (or otherwise conflicts) after Turing→extension: STOP — do not proceed to any downstream plan. Present the conflict to the user with the candidate fixes from research (GLMakie→extension too; a Makie bump; or a harder blocker) and the exact `Pkg.resolve` error. This is why the plan is `autonomous: false`.
  </action>
  <options>
    <option id="resolve-green">
      <name>Resolve green at 0.2.1</name>
      <pros>Foundation clear; proceed to 07-01.</pros>
      <cons>None.</cons>
    </option>
    <option id="glmakie-to-ext">
      <name>Move GLMakie to an extension too</name>
      <pros>Shrinks the plot stack from the core resolve (research candidate fix).</pros>
      <cons>Plotting becomes weakdep-gated; extra surgery.</cons>
    </option>
    <option id="escalate-blocker">
      <name>Escalate a harder resolve blocker</name>
      <pros>Surfaces a real dependency conflict before any inference code is written.</pros>
      <cons>Phase 7 pauses on a user decision.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `test/runtests.jl` asserts `Pkg.dependencies()` reports NeuralEstimators `v"0.2.1"` and Flux `v"0.16.10"`.
    - Root `Manifest.toml` regenerated and NeuralEstimators is NOT 0.1.4.
    - On failure, execution halts with the resolve error surfaced to the user (no downstream plan runs).
  </acceptance_criteria>
  <resume-signal>Confirm resolve is green at NeuralEstimators 0.2.1, or choose glmakie-to-ext / escalate.</resume-signal>
  <verify>
    <automated>julia --project -e 'using Pkg; d=Pkg.dependencies(); ne=first(v for (_,v) in d if v.name=="NeuralEstimators"); fl=first(v for (_,v) in d if v.name=="Flux"); @assert string(ne.version)=="0.2.1" "NeuralEstimators $(ne.version) != 0.2.1 — Finding 1 downgrade"; @assert string(fl.version)=="0.16.10"'</automated>
  </verify>
  <done>Root resolves with NeuralEstimators 0.2.1 + Flux 0.16.10 asserted in runtests.jl; Manifest committed; or the conflict is escalated to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| package resolver → installed versions | dependency resolution can silently substitute an incompatible NeuralEstimators |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Tampering | root `Pkg.resolve` (NeuralEstimators version) | mitigate | Turing→weakdep extension + pinned `[compat]` 0.2.1 + machine assertion in runtests.jl (Pitfall 1) |
| T-7-SC | Tampering | Julia package installs | accept | Julia General registry, all packages pre-existing spike deps, pinned root Manifest; slopcheck N/A for Julia (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` passes (resolve assertion + type/accessor units green).
- Root `Manifest.toml` shows NeuralEstimators 0.2.1; `Pkg.status` does not show 0.1.4.
</verification>

<success_criteria>
- D-02 hierarchy exists and is exported; Turing path is internal-only; version is 2.0.0; root resolves at NeuralEstimators 0.2.1.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-00-SUMMARY.md` when done.
</output>

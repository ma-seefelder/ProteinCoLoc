---
phase: 07-productionization-conditional-on-go
plan: 09
type: execute
wave: 7
depends_on: [07-06, 07-07]
files_modified:
  - test/gate/gate_consts_32.jl
  - artifacts/grid_32/npe_32.jld2
  - artifacts/grid_32/ratio_32.jld2
  - artifacts/grid_32/ood_nulls_32.jld2
  - artifacts/grid_32/gate_report_32.jld2
  - .planning/phases/07-productionization-conditional-on-go/gate-32x32.md
autonomous: false
requirements: [PROD-02]
must_haves:
  truths:
    - "A 32×32 bundle is produced by `_train_grid_pipeline(32)` on a ≥1024²-biased image distribution and persisted CPU-resident"
    - "The datagen step is launched with `-t auto` (parallel, byte-identical to serial via Philox-per-index)"
    - "The 32×32 estimator runs its OWN fresh re-pre-registered CPU SBC/BF/OOD gate keyed by PROD_SEED[32]"
    - "The user decides ship-with-large-image-caveat vs cap-the-family (exclude 32 from _SHIPPED_GRIDS) based on the gate outcome"
    - "64×64 is NOT trained, gated, or shipped in this phase"
  artifacts:
    - path: "test/gate/gate_consts_32.jl"
      provides: "fresh 32×32 pre-registration + PROD_SEED[32] + SBC_IMSIZE ≥1024²"
      contains: "PROD_SEED"
    - path: ".planning/phases/07-productionization-conditional-on-go/gate-32x32.md"
      provides: "recorded 32×32 gate outcome + large-image caveat + ship/cap decision"
      contains: "SBC"
  key_links:
    - from: "gate-32x32.md"
      to: "ship-with-caveat vs cap decision"
      via: "user sign-off on the large-image feasibility caveat"
      pattern: "caveat"
    - from: "_train_grid_pipeline(32)"
      to: "artifacts/grid_32/*.jld2 CPU-resident"
      via: "shared per-grid pipeline (07-03), datagen under -t auto with ≥1024² imsize_set"
      pattern: "_train_grid_pipeline"
---

<objective>
Run the CONDITIONAL 32×32 pipeline (through the shared `_train_grid_pipeline(32)` helper) and take the user's ship-vs-cap decision. 32×32 (1024 patches, summary dim 2048) is SHIP-WITH-CAVEAT / global-only: informative only on ≥1024² dense images. This plan trains + freshly gates it, then presents the outcome for user sign-off — treating "cap the family" as an expected branch, not a failure. 64×64 is explicitly NOT planned.

Purpose: PROD-02 / D-04 / D-05 — 32×32 ships only with a loud large-image caveat, conditional on its own gate + user sign-off (07-CONTEXT finalized scope; 07-RESEARCH feasibility). `autonomous: false` because the ship/cap decision requires the user.
Output: `test/gate/gate_consts_32.jl`, 32×32 CPU-resident artifacts, recorded gate outcome + the user's ship/cap decision.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md

<interfaces>
<!-- shared pipeline (07-03): _train_grid_pipeline(32; imsize_set biased ≥1024², use_gpu=has_cuda_device()) → EstimatorBundle, persists artifacts/grid_32/* CPU-resident -->
<!-- datagen uses Threads.@threads gated on nthreads()>1 → MUST launch julia with -t auto; Philox-per-index makes it byte-identical across thread counts (D-11/D-12) -->
<!-- 32×32: summary_dim=2048, cont_rows=1024, ratio_input_dim=5120; px/patch 64 at 512² (noisy), 1024 at 1024² (usable) -->
<!-- 07-RESEARCH §Compute cost reality: 32×32 needs 100k @ ≥1024² (~16× sim); GPU absorbs the 2048-dim net; DATA-GEN is the bottleneck (CPU, GPU does NOT help) — -t auto matters most here -->
<!-- 07-RESEARCH §FEASIBILITY VERDICT: SHIP-WITH-CAVEAT / global-only; ship only if fresh D-05 gate passes AND registry warns "requires large images" -->
<!-- 64×64 DROPPED (D-04) — do NOT create any grid_64 pipeline -->
<!-- Follow 07-05 gate shape, grid=32, PROD_SEED[32] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the 32×32 bundle via `_train_grid_pipeline(32)` (≥1024² data, datagen under `-t auto`)</name>
  <files>artifacts/grid_32/npe_32.jld2, artifacts/grid_32/ratio_32.jld2, artifacts/grid_32/ood_nulls_32.jld2</files>
  <read_first>
    - 07-RESEARCH.md §FEASIBILITY VERDICT (32×32 row) + §Compute cost reality (data-gen is the fine-grid bottleneck; GPU only eases training) + Pitfall 2
    - STATE.md Phase 03-02 (generate_samples parallel==serial byte-identical across -t 1 and -t 4; Philox-per-index, D-12)
    - .planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md (pipeline shape)
    - src/amortized/pipeline.jl (07-03 `_train_grid_pipeline`, `default_imsize_for(32)` ≥1024²)
  </read_first>
  <action>
    Run the 32×32 pipeline THROUGH the shared helper, launching Julia with `-t auto` — critical here because data-gen (CPU/sim-bound) dominates the fine-grid wall clock:
    `julia -t auto --project -e 'using ProteinCoLoc; ProteinCoLoc._train_grid_pipeline(32; use_gpu=ProteinCoLoc.has_cuda_device())'`.
    `_train_grid_pipeline(32)` uses `default_imsize_for(32)` (BIASED to ≥1024² — px/patch 64 at 512² collapses toward `missing`; ≥1024² gives ~1024 px/patch, Pitfall 2) and ~100k pairs, then `train_npe`/`train_ratio` (GPU absorbs the 2048-dim summary net), `fit_ood_nulls` on a TRAIN-ONLY ID pool, and CPU-resident persist under `artifacts/grid_32/`. Record realized fraction-missing and posterior-vs-prior width (the key feasibility signals). **Datagen MUST be launched with `-t auto`**: the Julia default `nthreads()==1` silently falls back to serial (`generate_samples` `Threads.@threads`, `parallel = Threads.nthreads() > 1`); Philox-per-index keeps it byte-identical across thread counts (D-11/D-12). Do NOT restate the sequence inline.
  </action>
  <acceptance_criteria>
    - The 32×32 bundle is produced by `_train_grid_pipeline(32)` under `julia -t auto`, with an image-size distribution biased ≥1024².
    - `artifacts/grid_32/npe_32.jld2`/`ratio_32.jld2` load via `load_estimator` on CPU and produce finite draws.
    - Realized fraction-missing and posterior/prior width ratio are recorded (feasibility evidence).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; @assert ProteinCoLoc.summary_dim(32)==2048; b=ProteinCoLoc.load_estimator("artifacts/grid_32/npe_32.jld2"); @assert b !== nothing'</automated>
  </verify>
  <done>32×32 bundle produced via the shared pipeline on ≥1024²-biased data (datagen under `-t auto`) and persisted CPU-resident.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh 32×32 pre-registration + run the CPU SBC/BF/OOD gate</name>
  <files>test/gate/gate_consts_32.jl, artifacts/grid_32/gate_report_32.jld2, .planning/phases/07-productionization-conditional-on-go/gate-32x32.md</files>
  <read_first>
    - test/gate/gate_consts_template.jl + gate_consts_16.jl (07-07)
    - 07-PATTERNS.md → `gate_consts_G.jl` (raise SBC_IMSIZE; disjoint seed) + `run_gate.jl`
  </read_first>
  <action>
    Instantiate `test/gate/gate_consts_32.jl` from the template with FRESH 32×32 thresholds, a disjoint `PROD_SEED[32]`, and `SBC_IMSIZE` ≥1024². Commit it BEFORE running the gate. Then run `test/gate/run_gate.jl --grid 32 --sbc --bf --ood` against the CPU-resident 32×32 net (`use_gpu=false`) keyed by `PROD_SEED[32]`, non-clamped BF baseline, `with_pp=true` (NOT under `-t auto` — the gate is determinism-pinned). Write the atomic gate report and a `gate-32x32.md` recording SBC/BF/OOD numbers vs the pre-registered thresholds, the PASS/FAIL verdict, AND the large-image caveat (32×32 informative only on big dense images; global output, not a spatial map — Finding 2).
  </action>
  <acceptance_criteria>
    - `PROD_SEED[32]` ∉ {0x5BC0FFEE, 0xC0FFEE, PROD_SEED[8], PROD_SEED[4], PROD_SEED[16]}; `SBC_IMSIZE` ≥1024².
    - `gate_consts_32.jl` committed before the gate run.
    - `run_gate.jl --grid 32` completes CPU-only; `gate-32x32.md` records the verdict + large-image caveat.
  </acceptance_criteria>
  <verify>
    <automated>julia --project test/gate/run_gate.jl --grid 32 --sbc --bf --ood</automated>
  </verify>
  <done>32×32 freshly pre-registered and CPU-gated; verdict + large-image caveat recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: User sign-off — ship 32×32 with caveat vs cap the family</name>
  <files>.planning/phases/07-productionization-conditional-on-go/gate-32x32.md</files>
  <read_first>
    - gate-32x32.md (the recorded outcome from Task 2)
    - 07-VALIDATION.md → Manual-Only Verifications (32×32 conditional ship / cap-the-family)
    - 07-RESEARCH.md §Open Questions Q2 (cap the shipped family with user sign-off rather than shipping weak estimators)
  </read_first>
  <action>
    Present the 32×32 gate outcome (SBC/BF/OOD verdicts + realized informativeness + the large-image caveat) to the user and take the ship-vs-cap decision. Record the chosen branch in `gate-32x32.md`. This decision feeds 07-10: if ship-with-caveat, 32 is added to the shipped registry with the large-image warning; if cap, 32 is EXCLUDED from `_SHIPPED_GRIDS` with the recorded reason (an expected branch, not a failure). 64×64 remains dropped regardless.
  </action>
  <decision>Ship 32×32 with a loud large-image caveat, or cap the family (exclude 32 from _SHIPPED_GRIDS)?</decision>
  <context>32×32 is global-only and informative only on ≥1024² dense images; the gate verdict + realized informativeness determine whether it earns a shipped slot.</context>
  <options>
    <option id="ship-with-caveat">
      <name>Ship 32×32 with a large-image caveat</name>
      <pros>Extends the user-definable grid family; useful on big dense images.</pros>
      <cons>Registry must carry a loud "requires large images" warning; global output, not a spatial map.</cons>
    </option>
    <option id="cap-the-family">
      <name>Cap the family (exclude 32×32)</name>
      <pros>Ships only genuinely useful estimators (4/8/16); honest scope.</pros>
      <cons>32×32 artifacts retained but not registered.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `gate-32x32.md` records the user's decision (ship-with-caveat or cap) with rationale.
    - The decision is explicit input to 07-10's `_SHIPPED_GRIDS` population.
  </acceptance_criteria>
  <resume-signal>Select: ship-with-caveat or cap-the-family.</resume-signal>
  <verify>
    <human-check>User confirms the 32×32 ship/cap decision, recorded in gate-32x32.md.</human-check>
  </verify>
  <done>User decision recorded; 32×32 status resolved for the release plan; 64×64 stays dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| fine-grid feasibility → ship decision | shipping a calibrated-but-uninformative estimator would mislead users |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03 | DoS (uninformative estimator) | 32×32 on sub-1024² data | mitigate | ≥1024²-biased data (default_imsize_for(32)) + posterior-vs-prior width evidence; user sign-off gates the ship |
| T-7-09 | Information (scope honesty) | 32×32 "spatial" misframing | mitigate | registry carries the large-image caveat; recorded as global output, not a per-region map |
| T-7-08 | Repudiation (data-snooping) | 32×32 gate | mitigate | fresh disjoint `PROD_SEED[32]` committed before the run |
</threat_model>

<verification>
- `run_gate.jl --grid 32` completes; `gate-32x32.md` records verdict + caveat + the user's ship/cap decision.
- No `grid_64` artifacts or `gate_consts_64.jl` exist (64×64 dropped).
</verification>

<success_criteria>
- 32×32 produced via the shared pipeline (datagen under `-t auto`), freshly gated CPU-reproducibly; ship-vs-cap decided by the user; 64×64 not planned.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-09-SUMMARY.md` when done.
</output>

---
phase: 07-productionization-conditional-on-go
plan: 04
type: execute
wave: 4
depends_on: [07-02, 07-03]
files_modified:
  - test/gate/harness.jl
  - test/gate/sbc.jl
  - test/gate/run_gate.jl
  - test/gate/gate_consts_template.jl
  - test/gpu_smoke.jl
  - test/runtests.jl
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "A per-grid ship-gate harness runs the θ*~π → simulate → CPU-infer SBC/BF/OOD chain keyed by a fresh disjoint PROD_SEED[G]"
    - "Every gate inference call uses use_gpu=false against a CPU-resident frozen net (deterministic pre-registered numbers)"
    - "A gate_consts template exists to be re-expressed FRESH per grid (never reusing VAL_MASTER_SEED or the spike consts.jl)"
    - "A GPU-train smoke asserts use_gpu=true works with CUDA present AND degrades to CPU when absent, and the frozen net persists CPU-resident"
  artifacts:
    - path: "test/gate/harness.jl"
      provides: "draw_simulate_infer (grid-parametrized, CPU) + paired-Δρ path"
      contains: "use_gpu = false"
    - path: "test/gate/run_gate.jl"
      provides: "per-grid CLI entry: run_gate.jl --grid G [--sbc --bf --ood]"
      contains: "--grid"
    - path: "test/gate/gate_consts_template.jl"
      provides: "fresh-pre-registration template (M/L/bins/thresholds + PROD_SEED[G])"
      contains: "PROD_SEED"
    - path: "test/gpu_smoke.jl"
      provides: "GPU-train smoke with graceful CPU fallback assertion"
      contains: "use_gpu"
  key_links:
    - from: "test/gate/harness.jl draw_simulate_infer"
      to: "patch_summary(mci, G)"
      via: "grid-parametrized summary"
      pattern: "patch_summary\\("
    - from: "test/gate/gate_consts_template.jl"
      to: "PROD_SEED[G] disjoint from VAL_MASTER_SEED/NPE_MASTER_SEED"
      via: "fresh disjoint seed"
      pattern: "PROD_SEED"
---

<objective>
Build the per-grid CPU-reproducible ship-gate machinery (D-05): the SBC/BF/OOD harness, the fresh-pre-registration `gate_consts` template, the `run_gate.jl` CLI, and a GPU-train smoke that proves the D-06 GPU→CPU graceful fallback. No grid is trained yet — this plan delivers the gate the per-grid plans invoke.

Purpose: D-05 requires every SHIPPED grid to pass its OWN fresh-seed, re-pre-registered SBC/BF/OOD run, CPU-reproducible regardless of training hardware. This harness is promoted from the spike validation code with the three critical deltas: grid-parametrized summary, `use_gpu=false` on every gate call against a CPU-resident net, and a fresh disjoint `PROD_SEED[G]` (never the spike's `VAL_MASTER_SEED`).
Output: `test/gate/{harness,sbc,run_gate,gate_consts_template}.jl`, `test/gpu_smoke.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md
@.planning/phases/07-productionization-conditional-on-go/07-VALIDATION.md

<interfaces>
<!-- harness analog: spike/validation/harness.jl:60-94 (disjoint-seed salt idiom, val_rng Philox), :106-136 draw_simulate_infer + paired-Δρ -->
<!-- sbc analog: spike/validation/sbc.jl:65-72 CalibrationResult, :156-172 rank table (7 θ + Δρ col), :184-196 KS/χ² uniformity -->
<!-- consts analog: spike/validation/consts.jl:42-80 (SBC_M/L/BINS, KS/ECE thresholds, BF corr/tol + sweep, OOD quantile/AUC/levels/PP_REPS), :76 VAL_MASTER_SEED=0x5BC0FFEE (DO NOT reuse) -->
<!-- src surfaces from 07-02/07-03: standardize_summary, posterior_for, amortized_log_bf, ood_verdict, load_estimator, train_npe/train_ratio -->
<!-- Anti-snooping: NPE_MASTER_SEED=0xC0FFEE, VAL_MASTER_SEED=0x5BC0FFEE are forbidden for any grid gate (Pitfall 3) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grid-parametrized CPU gate harness + SBC</name>
  <files>test/gate/harness.jl, test/gate/sbc.jl</files>
  <read_first>
    - 07-PATTERNS.md → `test/gate/run_gate.jl + harness` section (harness core, SBC rank table, KS/χ² uniformity, CRITICAL DELTAS: use_gpu=false, patch_summary(mci,G), per-grid net)
    - spike/validation/harness.jl:60-136; spike/validation/sbc.jl:65-196
  </read_first>
  <action>
    Promote `spike/validation/harness.jl` into `test/gate/harness.jl`: `draw_simulate_infer(m, rng; imsize, N, G)` calling `standardize_summary(encode_d01(patch_summary(mci, G)), m.zt, :min)` then `posterior_for(m.estimator, Z; N, use_gpu=false)` and reconstruct via frozen `θzt`; plus the paired-Δρ path. **DELTAS:** grid `G` is a parameter (was hardcoded `patch_summary(mci)`); every inference call is `use_gpu=false` against a CPU-resident frozen net; the harness loads the per-grid net via `load_estimator` (NOT the fixed spike `trained_npe.jld2`). Promote `spike/validation/sbc.jl` into `test/gate/sbc.jl`: the M×8 rank table (7 θ + a dedicated Δρ column), KS + χ² uniformity via HypothesisTests (`ExactOneSampleKSTest`, `ChisqTest`), coverage curve, and the ported `CalibrationResult` ECE/MCE traffic-light. Keep the spike's standalone-loadable guard idiom for `test/gate/*` files (these load under `run_gate.jl`, matching the spike's test-file convention, not the src/ module convention).
  </action>
  <acceptance_criteria>
    - `draw_simulate_infer` accepts `G` and calls `patch_summary(mci, G)` (grid-parametrized).
    - Every `posterior_for`/inference call in the harness passes `use_gpu = false`.
    - `sbc.jl` computes KS and χ² p-values via HypothesisTests (not hand-rolled) and an ECE/MCE `CalibrationResult`.
    - Harness loads a per-grid net via `load_estimator`, not a hardcoded spike artifact path.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>CPU gate harness + SBC promoted, grid-parametrized, CPU-only inference, per-grid net loading.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-pre-registration gate_consts template + run_gate.jl CLI</name>
  <files>test/gate/gate_consts_template.jl, test/gate/run_gate.jl, test/runtests.jl</files>
  <read_first>
    - 07-PATTERNS.md → `test/gate/gate_consts_G.jl` section (template + Pitfall 3 disjoint PROD_SEED[G]) + §Shared Patterns "Disjoint Random123 keyed streams"
    - spike/validation/consts.jl:42-80; spike/validation/harness.jl:93-94 (val_rng Philox salt idiom)
    - 07-RESEARCH.md §Validation Architecture "Fresh pre-registration per grid" + non-clamped BF + PP re-enable ride-along
  </read_first>
  <action>
    Create `test/gate/gate_consts_template.jl` — the fresh-pre-registration template carrying `SBC_M`, `SBC_L`, `SBC_BINS`, `SBC_KS_ALPHA`, `SBC_ECE_GREEN/YELLOW`, `SBC_IMSIZE`, `BF_CORR_MIN`, `BF_LOGBF_TOL`, `BF_SWEEP_*`, `OOD_ID_QUANTILE`, `OOD_AUC_MIN`, `OOD_GRID_LEVELS`, `OOD_PP_REPS`, and a `PROD_SEED[G]` produced by a Philox keyed stream salted disjoint from `VAL_MASTER_SEED (0x5BC0FFEE)` and `NPE_MASTER_SEED (0xC0FFEE)` (mirror the `val_rng` XOR-salt idiom with a new `PROD_SALT`). Add a header note: values are re-expressed FRESH per grid BEFORE its gate runs; the BF gate uses the non-clamped baseline and the OOD gate uses `with_pp = true`. Create `test/gate/run_gate.jl` — a CLI dispatching `--grid G [--sbc] [--bf] [--ood]`, loading `gate_consts_<G>.jl`, running the selected reported-scale gates via the harness (`use_gpu=false`), and writing a per-grid gate report through the atomic wrapper. Wire the fixture-scale (tiny-M) SBC smoke into `test/runtests.jl` so `Pkg.test()` exercises the harness without the full M=2000 cost.
  </action>
  <acceptance_criteria>
    - `gate_consts_template.jl` derives `PROD_SEED[G]` via a Philox stream whose salt differs from `0x5BC0FFEE` and `0xC0FFEE`; a unit test asserts `PROD_SEED[G] ∉ {0x5BC0FFEE, 0xC0FFEE}` for G∈{4,8,16,32}.
    - `run_gate.jl` accepts `--grid` and at least one of `--sbc/--bf/--ood` and runs with `use_gpu=false`.
    - `test/runtests.jl` runs a tiny-M SBC smoke through the harness and passes.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Fresh-pre-registration template with disjoint PROD_SEED[G] + a per-grid gate CLI + a fixture-scale smoke in runtests.jl.</done>
</task>

<task type="auto">
  <name>Task 3: GPU-train smoke with graceful CPU fallback</name>
  <files>test/gpu_smoke.jl, test/runtests.jl</files>
  <read_first>
    - 07-RESEARCH.md §GPU Acceleration & Reproducibility Split ("Verify at Wave 0" + A7) + §Reproducibility split point 1 (CPU-resident persistence)
    - 07-VALIDATION.md → Wave 0 Requirements (GPU-train smoke) + Per-Task Verification Map (T-7-02 A7 row)
    - ENV-02 spike smoke pattern (spike/00_smoke.jl) as the mirror
  </read_first>
  <action>
    Create `test/gpu_smoke.jl` mirroring the ENV-02 CPU smoke but for the training path: a tiny NPE `train(...; use_gpu=true)` that (a) runs on GPU when a CUDA device is visible, and (b) DEGRADES CLEANLY to CPU when CUDA is absent (no error — the graceful-fallback CLAUDE.md constraint), using `has_cuda_device()` to branch the assertion. Assert the resulting frozen net persists CPU-resident via `save_estimator`/`load_estimator` (parameters moved to CPU; the artifact loads with no device state). Gate the CUDA-present branch behind `has_cuda_device()` so the test passes on CPU-only CI by exercising only the fallback path. Wire into `test/runtests.jl`.
  </action>
  <acceptance_criteria>
    - On a CPU-only machine, `test/gpu_smoke.jl` passes by exercising the graceful `use_gpu=true → CPU fallback` path without error.
    - The smoke asserts the trained net persists CPU-resident (loads on a machine with no CUDA).
    - The CUDA-present assertion is guarded by `has_cuda_device()` so it does not fail CPU-only runs.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'include("test/gpu_smoke.jl")'</automated>
  </verify>
  <done>GPU-train smoke proves use_gpu=true works or degrades to CPU, and the frozen net persists CPU-resident.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| gate pre-registration → reported metric | reusing a training-iterated seed constitutes data-snooping |
| training hardware → gate determinism | a GPU-only gate would be non-reproducible |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Repudiation (data-snooping) | per-grid gate seed | mitigate | fresh disjoint `PROD_SEED[G]` salted off `VAL_MASTER_SEED`/`NPE_MASTER_SEED`; unit-asserted distinct (Pitfall 3) |
| T-7-07 | Repudiation (reproducibility) | gate inference calls | mitigate | every gate call `use_gpu=false` against a CPU-resident frozen net; deterministic regardless of training hardware |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (harness smoke, consts disjointness, GPU fallback).
- `test/gate/run_gate.jl --grid 8 --sbc` invocable (full run happens in the per-grid plans).
</verification>

<success_criteria>
- Per-grid CPU ship-gate machinery + fresh-pre-registration template + GPU-train smoke ready for the per-grid pipelines.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-04-SUMMARY.md` when done.
</output>

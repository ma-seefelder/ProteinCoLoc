---
phase: 07-productionization-conditional-on-go
plan: 05
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - test/gate/gate_consts_8.jl
  - artifacts/grid_8/npe_8.jld2
  - artifacts/grid_8/ratio_8.jld2
  - artifacts/grid_8/ood_nulls_8.jld2
  - artifacts/grid_8/gate_report_8.jld2
  - .planning/phases/07-productionization-conditional-on-go/gate-8x8.md
autonomous: true
requirements: [PROD-02]
must_haves:
  truths:
    - "An 8×8 NPE + NRE + OOD-nulls bundle is produced by `_train_grid_pipeline(8)` (GPU-accelerated where available) and persisted CPU-resident"
    - "The datagen step is launched with `-t auto` (parallel, byte-identical to serial via Philox-per-index)"
    - "The 8×8 estimator passes its OWN fresh re-pre-registered CPU SBC/BF/OOD gate keyed by PROD_SEED[8]"
    - "The gate machinery is proven end-to-end on the spike's validated grid before any other grid runs"
  artifacts:
    - path: "test/gate/gate_consts_8.jl"
      provides: "fresh 8×8 pre-registration (M/L/bins/thresholds + PROD_SEED[8])"
      contains: "PROD_SEED"
    - path: "artifacts/grid_8/npe_8.jld2"
      provides: "CPU-resident Flux.state 8×8 NPE"
      contains: ""
    - path: ".planning/phases/07-productionization-conditional-on-go/gate-8x8.md"
      provides: "recorded 8×8 gate outcome (SBC/BF/OOD verdicts)"
      contains: "SBC"
  key_links:
    - from: "test/gate/gate_consts_8.jl PROD_SEED[8]"
      to: "run_gate.jl --grid 8"
      via: "fresh disjoint seed drives the reported gate"
      pattern: "PROD_SEED"
    - from: "_train_grid_pipeline(8)"
      to: "artifacts/grid_8/*.jld2 CPU-resident"
      via: "the shared per-grid pipeline (07-03), datagen under -t auto"
      pattern: "_train_grid_pipeline"
---

<objective>
Run the first full per-grid pipeline on the proven 8×8 grid THROUGH the shared `_train_grid_pipeline(8)` helper (07-03), commit a FRESH 8×8 pre-registration, and pass the CPU SBC/BF/OOD ship-gate keyed by `PROD_SEED[8]`. This establishes the D-05 gate machinery on the grid the spike already validated end-to-end (07-RESEARCH sequencing: "8×8 first").

Purpose: PROD-02 — the 8×8 estimator is the reference registry entry and the estimator the sub-tile local map (07-08) reuses in-process. Running it first — through the SAME pipeline every other grid uses — de-risks both the harness and the user-definable-grid path on known-good numbers.
Output: `test/gate/gate_consts_8.jl`, the 8×8 CPU-resident artifacts, and a recorded gate outcome.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md

<interfaces>
<!-- shared pipeline (07-03): _train_grid_pipeline(grid; imsize_set, use_gpu=has_cuda_device(), n_pairs) → EstimatorBundle, persists artifacts/grid_G/{npe,ratio,ood_nulls}_G.jld2 -->
<!-- datagen uses Threads.@threads gated on nthreads()>1 → MUST launch julia with -t auto; Philox-per-index makes it byte-identical across thread counts (D-11/D-12) -->
<!-- gate surfaces (07-04): run_gate.jl, gate_consts_template.jl, harness (load_estimator, use_gpu=false) -->
<!-- 8×8 is the spike-validated grid: summary_dim(8)=128, cont_rows(8)=64, ratio_input_dim(8)=320 -->
<!-- Existing 8×8 data cache from Phase 3 is reusable via imsize_set; data-gen is re-runnable via datagen.jl grid=8 -->
<!-- Anti-snooping: gate MUST run fresh PROD_SEED[8], NOT VAL_MASTER_SEED (the spike net was iterated against VAL) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the 8×8 bundle via `_train_grid_pipeline(8)` (datagen under `-t auto`)</name>
  <files>artifacts/grid_8/npe_8.jld2, artifacts/grid_8/ratio_8.jld2, artifacts/grid_8/ood_nulls_8.jld2</files>
  <read_first>
    - 07-PATTERNS.md → per-grid pipeline (train_npe + train_ratio + fit_ood_nulls + persist)
    - 07-RESEARCH.md §Compute cost reality (8×8 row: spike cache reusable, GPU trivial) + §Reproducibility split (persist CPU-resident) + §Open Questions Q4 (re-persist via Flux.state)
    - STATE.md Phase 03-02 (generate_samples parallel==serial byte-identical across -t 1 and -t 4; Philox-per-index, D-12)
    - src/amortized/pipeline.jl (07-03 `_train_grid_pipeline`), src/amortized/{train_npe,train_ratio,ood,persist}.jl
  </read_first>
  <action>
    Run the 8×8 pipeline THROUGH the shared helper, launching Julia with `-t auto` so datagen parallelizes:
    `julia -t auto --project -e 'using ProteinCoLoc; ProteinCoLoc._train_grid_pipeline(8; use_gpu=ProteinCoLoc.has_cuda_device())'`.
    `_train_grid_pipeline(8; imsize_set=default_imsize_for(8))` (07-03) performs datagen (grid=8, optionally reusing the Phase-3-scale cache), `train_npe`, `train_ratio`, `fit_ood_nulls` on a TRAIN-ONLY ID pool, and CPU-resident persist under `artifacts/grid_8/` — returning the `EstimatorBundle`. Do NOT restate the datagen→train→persist sequence inline (it lives once in `pipeline.jl`), and do NOT copy the spike's whole-object `trained_npe.jld2` (the pipeline re-persists via `Flux.state`, 07-RESEARCH Q4). **Datagen MUST be launched with `-t auto`**: the Julia default `nthreads()==1` causes a SILENT serial fallback (`generate_samples` uses `Threads.@threads` with `parallel = Threads.nthreads() > 1`). Reproducibility is thread-count-independent (Philox-per-index, D-11/D-12), so `-t auto` is byte-identical to the serial path.
  </action>
  <acceptance_criteria>
    - The 8×8 bundle is produced by `_train_grid_pipeline(8)` (not an inline restatement of the sequence), launched under `julia -t auto`.
    - `artifacts/grid_8/npe_8.jld2` and `ratio_8.jld2` load via `load_estimator` on a CPU-only process and produce finite posterior draws.
    - The persisted payload is `Flux.state`-based (no whole `PosteriorEstimator` object).
    - `ood_nulls_8.jld2` contains density + noise + PP null fits (loadable via `load_ood_nulls`).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; b=ProteinCoLoc.load_estimator("artifacts/grid_8/npe_8.jld2"); @assert b !== nothing'</automated>
  </verify>
  <done>8×8 NPE+NRE+OOD-nulls produced via the shared pipeline (datagen under `-t auto`, GPU where available) and persisted CPU-resident.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the FRESH 8×8 pre-registration (gate_consts_8.jl)</name>
  <files>test/gate/gate_consts_8.jl</files>
  <read_first>
    - 07-PATTERNS.md → `gate_consts_G.jl` section (Pitfall 3 — fresh disjoint PROD_SEED, raise SBC_IMSIZE per grid)
    - test/gate/gate_consts_template.jl (from 07-04)
    - 07-RESEARCH.md §Validation Architecture "Fresh pre-registration per grid"
  </read_first>
  <action>
    Instantiate `test/gate/gate_consts_8.jl` from the template with FRESH re-expressed values for the 8×8 gate (M=2000-class, L, bins, KS/χ²/ECE thresholds, BF corr/tol + sweep, OOD quantile/AUC/levels/PP reps) and a `PROD_SEED[8]` from the disjoint Philox stream (salted off `VAL_MASTER_SEED`/`NPE_MASTER_SEED`). Set `SBC_IMSIZE` adequate for 8×8 (the spike's ≥64-floor default suffices). Commit this file BEFORE running Task 3 (the pre-registration must precede the reported run — anti-snooping).
  </action>
  <acceptance_criteria>
    - `gate_consts_8.jl` defines all template constants and a `PROD_SEED[8]` ∉ {0x5BC0FFEE, 0xC0FFEE}.
    - The file is committed before the Task-3 gate run (pre-registration ordering).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'include("test/gate/gate_consts_8.jl"); @assert PROD_SEED[8] != 0x5BC0FFEE && PROD_SEED[8] != 0xC0FFEE'</automated>
  </verify>
  <done>Fresh 8×8 pre-registration committed with a disjoint seed, before the gate runs.</done>
</task>

<task type="auto">
  <name>Task 3: Run + record the 8×8 CPU SBC/BF/OOD ship-gate</name>
  <files>artifacts/grid_8/gate_report_8.jld2, .planning/phases/07-productionization-conditional-on-go/gate-8x8.md</files>
  <read_first>
    - 07-PATTERNS.md → `run_gate.jl + harness` (CPU-only, per-grid net) + §Shared Patterns (atomic report writer)
    - test/gate/run_gate.jl (from 07-04)
    - STATE.md Phase 5-04/iter1/iter2 (the memo §6 pass conditions: ECE green, BF mid-range agreement, OOD family detectable) for interpreting the verdict
  </read_first>
  <action>
    Run `test/gate/run_gate.jl --grid 8 --sbc --bf --ood` against the persisted CPU-resident 8×8 net (`use_gpu=false`) keyed by `PROD_SEED[8]`, using the non-clamped BF baseline and `with_pp=true` OOD fusion. This is a DETERMINISM-pinned reported gate — do NOT add `-t auto` here (the gate is a measurement/reproducibility step; `-t auto` is only for datagen). Write the atomic gate report to `artifacts/grid_8/gate_report_8.jld2` and a human-readable `gate-8x8.md` recording the SBC (KS/χ²/ECE per parameter + Δρ), BF (corr, max|Δ logBF| against the non-clamped baseline), and OOD (per-family AUC, ID fire-rate) verdicts against the pre-registered thresholds. Record PASS/FAIL per the memo §6 conditions. On PASS, the 8×8 grid is eligible for registry population (done in 07-10).
  </action>
  <acceptance_criteria>
    - `run_gate.jl --grid 8` completes with `use_gpu=false` and writes `gate_report_8.jld2` atomically.
    - `gate-8x8.md` records SBC/BF/OOD numbers against the pre-registered `gate_consts_8.jl` thresholds and a PASS/FAIL verdict.
    - The gate used `PROD_SEED[8]` (not `VAL_MASTER_SEED`), and was NOT launched with `-t auto`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project test/gate/run_gate.jl --grid 8 --sbc --bf --ood</automated>
  </verify>
  <done>8×8 gate ran CPU-reproducibly on a fresh seed; verdict recorded; machinery proven on the reference grid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| trained net → reported gate | the net must not be evaluated against the seed it was iterated against |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Repudiation (data-snooping) | 8×8 gate | mitigate | fresh `PROD_SEED[8]` committed before the run; asserted disjoint from spike seeds |
| T-7-07 | Repudiation (reproducibility) | gate inference | mitigate | `use_gpu=false` against CPU-resident net; deterministic numbers; gate not run under `-t auto` |
</threat_model>

<verification>
- `julia --project test/gate/run_gate.jl --grid 8 --sbc --bf --ood` runs to completion; `gate-8x8.md` records the verdict.
- 8×8 artifacts load CPU-only.
</verification>

<success_criteria>
- 8×8 produced via the shared pipeline (datagen under `-t auto`), persisted CPU-resident, fresh-pre-registered, and gated CPU-reproducibly; gate machinery proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-05-SUMMARY.md` when done.
</output>

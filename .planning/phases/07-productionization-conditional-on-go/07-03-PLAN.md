---
phase: 07-productionization-conditional-on-go
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/amortized/architecture.jl
  - src/amortized/train_npe.jl
  - src/amortized/train_ratio.jl
  - src/amortized/persist.jl
  - src/amortized/pipeline.jl
  - src/registry.jl
  - src/ProteinCoLoc.jl
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "NPE and NRE training run with a plumbed `use_gpu` keyword (GPU when a device is present, graceful CPU fallback)"
    - "The spike's `use_gpu && throw(...)` guards are removed on the training path only"
    - "Trained estimators AND OOD nulls persist as CPU-resident artifacts inside the atomic .tmp/integrity/mv wrapper, not whole-object"
    - "A single `_train_grid_pipeline(grid; ...)` factors datagen→train_npe→train_ratio→fit_ood_nulls→persist and returns an EstimatorBundle; `train_and_register(grid)` calls it"
    - "The pipeline's datagen parallelizes only under `-t auto` (Threads.@threads), byte-identical across thread counts (Philox-per-index)"
    - "A persisted estimator round-trips: save → load → identical CPU inference output"
  artifacts:
    - path: "src/amortized/architecture.jl"
      provides: "build_estimator (input-width-agnostic) + NPE architecture constants"
      contains: "PosteriorEstimator"
    - path: "src/amortized/persist.jl"
      provides: "Flux.state CPU-resident save/load for estimators AND OOD nulls inside atomic wrapper"
      contains: "save_ood_nulls"
    - path: "src/amortized/pipeline.jl"
      provides: "_train_grid_pipeline(grid; ...) returning an EstimatorBundle"
      contains: "_train_grid_pipeline"
  key_links:
    - from: "src/registry.jl train_and_register"
      to: "_train_grid_pipeline"
      via: "on-demand grid training returns an EstimatorBundle then register!"
      pattern: "_train_grid_pipeline"
    - from: "src/amortized/persist.jl"
      to: "Flux.loadmodel!"
      via: "rebuild architecture then load CPU state"
      pattern: "loadmodel!"
---

<objective>
Promote NPE and NRE training into `src/amortized/`, plumbing the D-06 GPU-train / CPU-reproducible split, migrate shipped-model persistence (estimators AND OOD nulls) to CPU-resident `Flux.state` + `loadmodel!` (Pitfall 4) inside the atomic write wrapper, and factor the whole per-grid pipeline into a single reusable `_train_grid_pipeline(grid; ...)` that `train_and_register` calls.

Purpose: PROD-01/PROD-02 — every shipped grid trains through this path, and the user-definable grid path (D-04: `train_and_register(6)` for a novel grid) requires a REAL `_train_grid_pipeline`, not per-grid prose. Training-time non-determinism is acceptable because what is pre-registered/reproduced is the frozen CPU net + CPU gate (07-RESEARCH §Reproducibility split).
Output: `src/amortized/{architecture,train_npe,train_ratio,persist,pipeline}.jl` + `train_and_register` wired in `src/registry.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-productionization-conditional-on-go/07-RESEARCH.md
@.planning/phases/07-productionization-conditional-on-go/07-PATTERNS.md

<interfaces>
<!-- architecture analog: spike/npe/architecture.jl:60-66 (consts NPE_D=7, NPE_DSTAR=64, NPE_DEPTH=3, NPE_WIDTH=256, NPE_COUPLING=10, NPE_FLOW_DEPTH=2, NPE_FLOW_WIDTH=128), :87-115 build_estimator (q = NormalisingFlow INSTANCE positional; input-width-agnostic reads d_in) -->
<!-- train analog: spike/npe/train_npe.jl:102-149 train_fold (leak-free θzt fit, build_estimator(size(Ztr,1),...), train(...; use_gpu=false)), :114 use_gpu&&throw guard to REMOVE, :129-131 AdamW-Float64 gotcha, :160-180 save_npe atomic -->
<!-- ratio analog: spike/validation/train_ratio.jl:278-321 training, :286 use_gpu guard to remove, :331-364 save_ratio atomic, :240-244 measure_log_prior_odds -->
<!-- datagen analog: spike/data/generate.jl generate_samples uses Threads.@threads gated on parallel = Threads.nthreads() > 1 (D-12) — needs -t auto to parallelize; Philox-per-index keying makes it byte-identical across thread counts (D-11/D-12) -->
<!-- persistence DELTA: Flux.state(cpu(est)) + arch metadata + Flux.loadmodel!; keep atomic .tmp+integrity+mv (07-PATTERNS §Shared Patterns) -->
<!-- registry (07-01): EstimatorBundle{grid,npe,ratio,ood_nulls,zt,θzt,calibration}; train_and_register(grid; use_gpu=has_cuda_device()) calls _train_grid_pipeline hook; datagen.jl generate_samples(...; grid=G, imsize_set); fit_ood_nulls (07-02) -->
<!-- has_cuda_device() from src/registry.jl (07-01) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NPE architecture + training with GPU plumbed</name>
  <files>src/amortized/architecture.jl, src/amortized/train_npe.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-PATTERNS.md → `src/amortized/train_npe.jl + architecture` section (build_estimator gotcha q positional INSTANCE; NPE consts; train_fold; use_gpu delta; AdamW-Float64 gotcha)
    - spike/npe/architecture.jl:60-115; spike/npe/train_npe.jl:102-149
  </read_first>
  <action>
    Create `src/amortized/architecture.jl` promoting `build_estimator(d_in; dstar, depth, width, num_coupling_layers, flow_depth, flow_width)` — the `Chain` MLP summary net + `q = NormalisingFlow(D; ...)` passed POSITIONALLY as an INSTANCE to `PosteriorEstimator(network, q)` (v0.2.1 API gotcha; keep it). Carry the Phase-5-calibrated constants `NPE_D=7, NPE_DSTAR=64, NPE_DEPTH=3, NPE_WIDTH=256, NPE_COUPLING=10, NPE_FLOW_DEPTH=2, NPE_FLOW_WIDTH=128`. `build_estimator` stays input-width-agnostic (reads `d_in` = `size(Ztr,1)`), so no grid change is needed here. Create `src/amortized/train_npe.jl` promoting `train_fold` with leak-free `θzt = fit(ZScoreTransform, θtr; dims=2)`, `build_estimator(size(fold_data.Ztr,1), ...)`, and `train(est, ...; use_gpu, ...)`. **DELTA (D-06):** make `use_gpu::Bool = has_cuda_device()` the default and REMOVE the `use_gpu && throw(...)` guard at train_npe.jl:114. Keep learning-rate/weight-decay as Float64 (the AdamW-Float64 CosAnneal gotcha, train_npe.jl:129-131). Match src/ module convention. `include` both after `ood.jl`.
  </action>
  <acceptance_criteria>
    - `build_estimator` constructs `q` as a `NormalisingFlow` instance passed positionally to `PosteriorEstimator` (no `q=Type` keyword form).
    - `train_npe`/`train_fold` accept `use_gpu` (default `has_cuda_device()`); no `use_gpu && throw` guard remains on the training path.
    - LR / weight_decay are `Float64` (not Float32).
    - A tiny fixture NPE train (few epochs, small N, `use_gpu=false`) completes and returns a `PosteriorEstimator`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>NPE architecture + training promoted; GPU plumbed with CPU-default fallback; training guard removed; Float64 optimiser preserved.</done>
</task>

<task type="auto">
  <name>Task 2: NRE ratio training with GPU plumbed</name>
  <files>src/amortized/train_ratio.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-PATTERNS.md → `bf.jl + train_ratio.jl` section (fixed-data train, assemble_ratio_data_cache, measure_log_prior_odds, save_ratio atomic, logitbinarycrossentropy hardcoded)
    - spike/validation/train_ratio.jl:240-244,278-321,286
  </read_first>
  <action>
    Create `src/amortized/train_ratio.jl` promoting the RatioEstimator training: `assemble_ratio_data_cache` (leak-free cache-paired data), `measure_log_prior_odds`, and the fixed-data `train(est_bf, ...; use_gpu, ...)`. Use `ratio_input_dim(G)`/`cont_rows(G)` from `summary.jl` for the conditioner input width (grid-general). **DELTA (D-06):** default `use_gpu::Bool = has_cuda_device()` and REMOVE the `use_gpu && throw(...)` guard at train_ratio.jl:286. Do NOT pass a custom loss (the ratio net loss is hardcoded `logitbinarycrossentropy`; a passed loss is silently ignored). Match src/ module convention. `include` after `train_npe.jl`.
  </action>
  <acceptance_criteria>
    - `train_ratio` accepts `use_gpu` (default `has_cuda_device()`); no `use_gpu && throw` guard remains.
    - Conditioner input width derives from `ratio_input_dim(G)`, not literal `320`.
    - No custom loss is passed to `train` (relies on the hardcoded `logitbinarycrossentropy`).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>NRE training promoted, grid-general, GPU-plumbed with CPU fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CPU-resident Flux.state persistence for estimators AND OOD nulls</name>
  <files>src/amortized/persist.jl, src/ProteinCoLoc.jl</files>
  <behavior>
    - `save_estimator(path, est, θzt, zt, meta)` writes `Flux.state(cpu(est))` + architecture metadata + transforms via `.tmp` → reopen integrity `@assert` → `mv(...; force=true)`.
    - `load_estimator(path)` rebuilds the architecture from metadata and applies `Flux.loadmodel!`, returning a CPU-resident estimator.
    - `save_ood_nulls(path, ood_nulls)` / `load_ood_nulls(path)` persist and reload the frozen density + noise + PP null fits through the SAME atomic wrapper.
    - Save→load→CPU inference reproduces the pre-save CPU inference output bitwise (or within tight float tol).
  </behavior>
  <read_first>
    - 07-PATTERNS.md → §Shared Patterns "Atomic JLD2 persistence" (DELTA for shipped models: Flux.state(cpu(est)) not whole object) + `src/registry.jl` CRITICAL DELTA
    - 07-RESEARCH.md §Common Pitfalls → Pitfall 4 + §Reproducibility split point 1 (persist CPU-resident)
    - spike/npe/train_npe.jl:160-180 (save_npe atomic idiom to reuse the WRAPPER of); spike/validation/ood.jl (ood_nulls structure from 07-02)
  </read_first>
  <action>
    Create `src/amortized/persist.jl` with `save_estimator`/`load_estimator` (and a ratio variant) that keep the atomic `.tmp` + reopen-integrity-`@assert` + `mv(...; force=true)` wrapper verbatim but swap the payload from whole-object `estimator = result.estimator` to `state = Flux.state(cpu(result.estimator))` plus architecture metadata (`d_in`, `dstar`, `depth`, `width`, coupling/flow params) and the frozen `θzt`/`zt`. `load_estimator` rebuilds via `build_estimator(...)` then `Flux.loadmodel!(est, state)`, moving parameters to CPU. Add `save_ood_nulls(path, ood_nulls)` / `load_ood_nulls(path)` for the frozen density+noise+PP null fits (plain arrays/NamedTuples — not a Flux model, so persisted directly) through the SAME atomic wrapper with an integrity `@assert`. Add a `schema_version` key to every artifact. Match src/ module convention. `include` after `train_ratio.jl`.
  </action>
  <acceptance_criteria>
    - The persisted estimator JLD2 contains a `Flux.state`-derived key (NOT a whole `PosteriorEstimator` object) plus architecture metadata.
    - `save_ood_nulls`/`load_ood_nulls` exist and round-trip the OOD null fits through the `.tmp`→integrity `@assert`→`mv(...; force=true)` idiom.
    - Round-trip test: train a tiny CPU estimator, `save_estimator`, `load_estimator`, and assert `posterior_for(loaded, Z; use_gpu=false)` matches the pre-save output within float tolerance.
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <done>Estimators and OOD nulls persist as CPU-resident atomic artifacts and round-trip identically on CPU.</done>
</task>

<task type="auto">
  <name>Task 4: Factor `_train_grid_pipeline` and wire `train_and_register`</name>
  <files>src/amortized/pipeline.jl, src/registry.jl, src/ProteinCoLoc.jl</files>
  <read_first>
    - 07-RESEARCH.md §Architecture Patterns → Pattern 2 (`train_and_register` = `_train_grid_pipeline` then `register!`) + §Recommended Plan Decomposition (per-grid pipeline shape)
    - 07-PATTERNS.md → `src/registry.jl` section (EstimatorBundle fields; artifact paths artifacts/grid_G)
    - src/amortized/{datagen,train_npe,train_ratio,ood,persist}.jl (Tasks 1-3 + 07-01/07-02) and src/registry.jl (07-01 stub `_train_grid_pipeline`/`train_and_register`)
    - STATE.md Phase 03-02 (generate_samples parallel==serial byte-identical across -t 1 and -t 4; Philox-per-index, D-12)
  </read_first>
  <action>
    Create `src/amortized/pipeline.jl` defining `_train_grid_pipeline(grid::Integer; imsize_set = default_imsize_for(grid), use_gpu = has_cuda_device(), n_pairs = default_npairs_for(grid), kwargs...)` that runs the full sequence ONCE for any grid: `generate_samples(...; grid, imsize_set, n_pairs)` (datagen), fit the leak-free summary `zt`, `train_npe(...; use_gpu)`, `train_ratio(...; use_gpu)`, `fit_ood_nulls(...)` on a TRAIN-ONLY ID pool, persist all three CPU-resident under `artifacts/grid_$(grid)/` via `save_estimator`/`save_ratio`/`save_ood_nulls`, and return a fully-populated `EstimatorBundle(grid, npe, ratio, ood_nulls, zt, θzt, calibration)` (calibration left as a not-yet-gated placeholder the per-grid gate fills). Add small `default_imsize_for`/`default_npairs_for` tables encoding the per-grid image-size bias (4→256²-heavy, 16→≥512², 32→≥1024²) so a novel grid gets a sane default. In `src/registry.jl`, replace the 07-01 `_train_grid_pipeline` hook so `train_and_register(grid; kwargs...)` = `b = _train_grid_pipeline(grid; kwargs...); register!(b); b`. Export `train_and_register` (already) — do NOT export the internal `_train_grid_pipeline`. `include` pipeline.jl after `persist.jl`. **Threading (peer requirement):** the `generate_samples` datagen step uses `Threads.@threads` gated on `parallel = Threads.nthreads() > 1`, so any process invoking `_train_grid_pipeline`/`train_and_register` MUST launch Julia with `-t auto` (e.g. `julia -t auto --project -e '...'`) — the Julia default `nthreads()==1` causes a SILENT serial fallback. Reproducibility is thread-count-independent (Philox-per-index keyed streams, D-11/D-12), so `-t auto` is byte-identical to the serial path; document this in the `_train_grid_pipeline` docstring.
  </action>
  <acceptance_criteria>
    - `_train_grid_pipeline` is a defined function (no UndefVarError) returning an `EstimatorBundle` with all fields populated.
    - `train_and_register(grid)` calls `_train_grid_pipeline(grid)` then `register!`, and afterwards `estimator_for(grid)` returns that bundle in-process.
    - A tiny fixture run launched with `julia -t auto ... _train_grid_pipeline(4; n_pairs=small, use_gpu=false)` produces `artifacts/grid_4/{npe_4,ratio_4,ood_nulls_4}.jld2` and a loadable bundle; datagen parallelizes (no silent serial fallback) and is byte-identical to a `-t 1` run.
    - The `_train_grid_pipeline` docstring documents the `-t auto` datagen requirement and its thread-count-independent reproducibility.
    - `default_imsize_for(16)` biases ≥512² and `default_imsize_for(32)` biases ≥1024².
  </acceptance_criteria>
  <verify>
    <automated>julia -t auto --project -e 'using ProteinCoLoc; @assert isdefined(ProteinCoLoc, Symbol("_train_grid_pipeline")); @assert hasmethod(ProteinCoLoc.train_and_register, Tuple{Int})'</automated>
  </verify>
  <done>The per-grid pipeline is a single reusable function; `train_and_register(grid)` runs it and registers the bundle (datagen under `-t auto`), so the user-definable-grid path (D-04) works on-demand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| training hardware → frozen artifact | GPU non-determinism must not leak into the pre-registered reproducible object |
| stored `.jld2` → loaded Julia types | deserialization reconstructs arbitrary types |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-07 | Repudiation (reproducibility) | GPU-trained frozen net | mitigate | persist CPU-resident `Flux.state`; the D-05 gate + shipped inference run CPU-only, so the reproduced object is device-independent (07-RESEARCH §Reproducibility split) |
| T-7-01 | Tampering/Elevation | `.jld2` estimator/ood deserialization | mitigate | `Flux.state` (parameter arrays, not arbitrary object graphs) + integrity-checked atomic write; final Artifacts hosting is content-hashed (07-10) |
</threat_model>

<verification>
- `julia --project -e 'using Pkg; Pkg.test()'` green (tiny train + round-trip + `_train_grid_pipeline` fixture under `-t auto`).
- `git diff --quiet spike/` — spike unchanged.
</verification>

<success_criteria>
- NPE+NRE training GPU-plumbed with CPU fallback; estimators + OOD nulls persist CPU-resident with identical CPU round-trip; `_train_grid_pipeline`/`train_and_register` provide the on-demand user-definable-grid path (datagen under `-t auto`).
</success_criteria>

<output>
Create `.planning/phases/07-productionization-conditional-on-go/07-03-SUMMARY.md` when done.
</output>

---
phase: 01-environment-smoke-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - Manifest.toml
  - .planning/phases/01-environment-smoke-gate/01-BASELINE.md
autonomous: false
requirements: [ENV-01]
must_haves:
  truths:
    - "An explicit, agreed git baseline reference for the root Project.toml/Manifest.toml/src/ is recorded before any spike work begins"
    - "The working tree state of the two protected root files matches the agreed baseline (clean diff against the recorded ref)"
  artifacts:
    - path: ".planning/phases/01-environment-smoke-gate/01-BASELINE.md"
      provides: "The recorded baseline ref (tag/commit/hash) and reconciliation decision for the untouched-root proof"
      contains: "baseline"
  key_links:
    - from: ".planning/phases/01-environment-smoke-gate/01-BASELINE.md"
      to: "git"
      via: "recorded commit/tag ref that later untouched-root proofs assert byte-identity against"
      pattern: "(v2\\.0-baseline|[0-9a-f]{7,40})"
---

<objective>
Reconcile the already-dirty root working tree (`Project.toml` and `Manifest.toml` are modified vs HEAD `9055a13`) with the user, and record an explicit, agreed baseline reference BEFORE any `spike/` work begins. ENV-01 and DEMO-02 both prove "root untouched"; that proof is meaningless without a clean, user-agreed reference to assert against.

Purpose: The user is the package author and weights publication-integrity highly (frozen tags `v1.0.2-scirep`, `v2.0-baseline`). The dirty root may be intentional (a GLMakie 0.10.5 compat pin + full Manifest re-resolve) or accidental. This must be decided by the user, not guessed.
Output: A recorded baseline ref in `01-BASELINE.md`; the two protected root files in an agreed, clean-diff state.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-smoke-gate/01-CONTEXT.md
@.planning/phases/01-environment-smoke-gate/01-RESEARCH.md
@.planning/phases/01-environment-smoke-gate/01-PATTERNS.md

<facts>
<!-- Verified this planning session; do not re-derive, but DO re-confirm the live git state before acting. -->
- HEAD = 9055a13. Tag v2.0-baseline = cec1c96. Tags present: v1.0.2-scirep, v2.0-baseline.
- `git status --porcelain` shows: ` M Manifest.toml`, ` M Project.toml` (the two files the decoupling guarantee protects), plus untracked `.planning/` artifacts.
- Root Project.toml diff vs HEAD: adds a `[compat]` block with `GLMakie = "0.10.5"` (3 insertions).
- Root Manifest.toml diff vs HEAD: full re-resolve (~1165 insertions / ~1073 deletions).
- No `spike/` directory exists yet.
</facts>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the frozen baseline for the protected root files</name>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Common Pitfalls" Pitfall 4; §"Open Questions" Q1; §"Runtime State Inventory")
    - .planning/phases/01-environment-smoke-gate/01-CONTEXT.md (§"Specific Ideas" — publication-integrity weighting; tags v1.0.2-scirep / v2.0-baseline)
  </read_first>
  <decision>How should the already-modified root `Project.toml` and `Manifest.toml` be reconciled so that ENV-01/DEMO-02's "root untouched" proof has a clean, agreed reference?</decision>
  <action>Present the live diff (git diff --stat Project.toml Manifest.toml and the full git diff Project.toml) to the user, then pause for their selection among the three options below. Do NOT commit, restore, or hash anything in this task -- this task only surfaces the situation and captures the decision; execution happens in Task 2 strictly per the chosen option.</action>
  <context>The two files protected by the decoupling guarantee are currently dirty vs HEAD (`9055a13`): a `[compat] GLMakie = "0.10.5"` pin was added to Project.toml and Manifest.toml was fully re-resolved. The spike must visibly preserve the frozen publication state, so the baseline these files are frozen at must be the user's explicit choice. Show the user the `git diff --stat Project.toml Manifest.toml` and the Project.toml diff before they choose.</context>
  <options>
    <option id="commit-as-baseline">
      <name>Commit the pending root changes as the new frozen baseline</name>
      <pros>Treats the GLMakie pin + re-resolve as intentional; baseline = the new commit hash; subsequent proofs assert against a committed clean tree</pros>
      <cons>Bakes the current dirty state into history; only correct if the edits were deliberate</cons>
    </option>
    <option id="restore-to-head">
      <name>Restore the root files to HEAD (`git restore Project.toml Manifest.toml`)</name>
      <pros>Returns to the last committed state; baseline = HEAD `9055a13`; cleanest decoupling story if the edits were accidental</pros>
      <cons>Discards the GLMakie pin + re-resolve; only correct if those edits were unwanted</cons>
    </option>
    <option id="adopt-dirty-snapshot">
      <name>Adopt the current dirty tree as the baseline via a recorded content snapshot (hash)</name>
      <pros>No commit, no restore; baseline = recorded SHA-256/blob hashes of the two files as-is; proofs assert byte-identity to the snapshot</pros>
      <cons>Baseline is a snapshot record rather than a git ref; slightly more bookkeeping in the untouched proof</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The user has been shown the live `git diff --stat Project.toml Manifest.toml` and the Project.toml diff.
    - The user has selected exactly one of: commit-as-baseline | restore-to-head | adopt-dirty-snapshot.
  </acceptance_criteria>
  <resume-signal>Select: commit-as-baseline, restore-to-head, or adopt-dirty-snapshot</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the reconciliation and record the agreed baseline ref</name>
  <files>Project.toml, Manifest.toml, .planning/phases/01-environment-smoke-gate/01-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Open Questions" Q1 recommendation)
    - Project.toml (the file being protected)
  </read_first>
  <action>
    Apply ONLY the user's Task-1 choice. For commit-as-baseline: `git add Project.toml Manifest.toml` then commit with message `chore(baseline): freeze root manifests as v2.0 spike baseline`, and capture the resulting commit short-hash as the baseline ref. For restore-to-head: `git restore Project.toml Manifest.toml`, and use `9055a13` (HEAD) as the baseline ref. For adopt-dirty-snapshot: do NOT commit or restore; compute and record the per-file content hashes (e.g. `git hash-object Project.toml Manifest.toml`) as the snapshot baseline. In all cases create `.planning/phases/01-environment-smoke-gate/01-BASELINE.md` recording: the chosen option, the exact baseline ref (commit short-hash, tag, or per-file hashes), the verbatim command used to verify "untouched" later (a `git diff --quiet <ref> -- Project.toml Manifest.toml src/` form for commit/restore, or a hash re-check for the snapshot option), and the timestamp. Do NOT create any `spike/` files in this plan — that is Plan 02. Do NOT edit `src/`.
  </action>
  <verify>
    <automated>F=.planning/phases/01-environment-smoke-gate/01-BASELINE.md; test -f "$F" || { echo "MISSING 01-BASELINE.md"; exit 1; }; grep -qiE '(v2\.0-baseline|9055a13|[0-9a-f]{7,40}|baseline ref)' "$F" || { echo "NO BASELINE REF RECORDED"; exit 1; }; if grep -qi 'adopt-dirty-snapshot' "$F"; then for pf in Project.toml Manifest.toml; do h=$(git hash-object "$pf"); grep -qF "$h" "$F" || { echo "HASH MISMATCH: $pf is $h, not recorded in 01-BASELINE.md"; exit 1; }; done; echo "snapshot hashes match baseline"; else git diff --quiet HEAD -- Project.toml Manifest.toml || { echo "root files differ from HEAD baseline"; exit 1; }; echo "root clean vs HEAD baseline"; fi; git diff --quiet HEAD -- src/ || { echo "src/ modified"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-environment-smoke-gate/01-BASELINE.md` exists and names the chosen reconciliation option plus an explicit baseline ref (commit hash, tag, or per-file content hashes).
    - The recorded ref resolves: for commit/restore options, `git cat-file -e <ref>` succeeds; for the snapshot option, the recorded hashes match `git hash-object Project.toml Manifest.toml`.
    - The automated verify enforces state-matches-baseline for ALL three options and exits non-zero on any mismatch: a clean `git diff --quiet HEAD -- Project.toml Manifest.toml` for commit-as-baseline and restore-to-head, and `git hash-object`-equality of both files against the hashes recorded in 01-BASELINE.md for adopt-dirty-snapshot (so a drifted dirty snapshot fails the command rather than merely printing rc).
    - No file under `src/` was modified (`git diff --quiet HEAD -- src/`).
    - No `spike/` files were created by this plan.
  </acceptance_criteria>
  <done>The baseline ref is recorded in 01-BASELINE.md and the two protected root files are in the agreed clean state; later phases can prove "root untouched" against this ref.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Frozen publication state (tags v1.0.2-scirep, v2.0-baseline) must not be silently mutated by spike work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | root Project.toml / Manifest.toml (protected files) | mitigate | Require an explicit user decision on the dirty state and record a concrete baseline ref before any spike work, so all later "untouched" proofs assert byte-identity against a known reference |
| T-01-02 | Repudiation | reconciliation decision | mitigate | Persist the choice, ref, and verify-command in 01-BASELINE.md (an auditable artifact), not just in conversation |
</threat_model>

<verification>
- `01-BASELINE.md` exists, names the option, and records a resolvable baseline ref + a verify command.
- The protected root files match the recorded baseline.
- `src/` untouched.
</verification>

<success_criteria>
The dirty-root precondition is resolved by explicit user decision and an agreed baseline ref is committed to the planning record, unblocking all subsequent spike work.
</success_criteria>

<output>
Create `.planning/phases/01-environment-smoke-gate/01-01-SUMMARY.md` when done, recording the chosen option and the exact baseline ref.
</output>

---
phase: 01-environment-smoke-gate
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - spike/Manifest.toml
  - spike/.julia-version
  - spike/NOTES.md
autonomous: true
requirements: [ENV-03, ENV-04]
must_haves:
  truths:
    - "The resolved spike/Manifest.toml is committed verbatim as the reproducibility artifact with exact NeuralEstimators and Flux versions and no CUDA entry (D-05: pin both package versions and the Julia version)"
    - "The Julia version (1.12.6) is both enforced via a juliaup directory override on spike/ and recorded in a committed file (D-05: Julia version is part of reproducible-from-fixed-seed)"
    - "A stack-decision note records NeuralEstimators.jl as default and BayesFlow (PythonCall) as fallback-only (D-07: stack-decision note locked to default)"
  artifacts:
    - path: "spike/Manifest.toml"
      provides: "Pinned, committed reproducibility artifact with exact transitive versions"
      contains: "NeuralEstimators"
    - path: "spike/.julia-version"
      provides: "Human/non-juliaup documentation record of the pinned Julia version"
      contains: "1.12.6"
    - path: "spike/NOTES.md"
      provides: "Stack-decision note (NeuralEstimators default / BayesFlow fallback) + Julia-version record"
      contains: "NeuralEstimators"
  key_links:
    - from: "spike/NOTES.md"
      to: "spike/Manifest.toml + juliaup override"
      via: "records the enforcing pin mechanism and the default/fallback stack decision"
      pattern: "(juliaup|BayesFlow|fallback)"
---

<objective>
Freeze the proven environment as a committed reproducibility artifact and record the stack decision. After the smoke is green (Plan 02), this plan commits `spike/Manifest.toml` verbatim, pins the Julia version both by enforcing mechanism (`juliaup override set 1.12.6` on `spike/`) and by a documentation record (`spike/.julia-version` + NOTES), and writes the NeuralEstimators-default / BayesFlow-fallback stack note.

Purpose: ENV-03 (the green smoke is only a durable gate if the exact package + Julia versions are pinned and committed — the SBI stack is pre-1.0 and version-sensitive, D-05) and ENV-04 (record the default/fallback stack ladder, D-07). Committing the minimal known-good Manifest here also gives Plan 04 a safe revert point before the riskier `Pkg.develop` attempt.
Output: committed `spike/Manifest.toml`, `spike/.julia-version`, `spike/NOTES.md`, and a registered juliaup directory override.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-smoke-gate/01-CONTEXT.md
@.planning/phases/01-environment-smoke-gate/01-RESEARCH.md
@.planning/phases/01-environment-smoke-gate/01-PATTERNS.md

<facts>
<!-- Verified this planning session. -->
- Machine Julia = 1.12.6 (juliaup `release` channel, default). `juliaup override` subcommand confirmed present (set/unset/status).
- `.julia-version` is NOT auto-read by Julia/juliaup (rbenv/pyenv convention, not Julia's) — it is documentation-only; the ENFORCING pin is `juliaup override set`.
- Turing prior ranges available to pre-seed NOTES.md for Phase 2 (OPTIONAL, from src/bayes.jl ~lines 274-291): mu ~ Truncated(Cauchy(0,0.3),-1,1); nu ~ Exponential(); sigma ~ Truncated(Cauchy(0.1,0.3),1e-4,1); tau ~ Truncated(Cauchy(0.1,0.3),1e-4,1).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin and commit the Manifest + record the Julia version</name>
  <files>spike/Manifest.toml, spike/.julia-version</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Architecture Patterns" Pattern 3 — reproducibility freeze; §"Common Pitfalls" Pitfall 2 — CUDA-absence)
    - .planning/phases/01-environment-smoke-gate/01-PATTERNS.md (§"spike/Manifest.toml" commit-verbatim; §"spike/.julia-version" single-line literal)
    - spike/Manifest.toml (the artifact being pinned)
  </read_first>
  <action>
    Confirm `spike/test/runtests.jl` is green on the current `spike/Manifest.toml` before pinning (only a proven env is worth freezing). Register the directory-scoped Julia pin by running `juliaup override set 1.12.6` for the `spike/` directory (the enforcing mechanism, D-05). Create `spike/.julia-version` containing the single literal line `1.12.6` (documentation record only — note it is NOT auto-read). Stage and commit `spike/Manifest.toml` verbatim as the reproducibility artifact; do NOT hand-edit it (machine-generated). Confirm the committed Manifest pins exact versions for `NeuralEstimators` and `Flux` and contains no `CUDA`/`CUDA_jll` entry. Do NOT touch the root Project.toml/Manifest.toml.
  </action>
  <verify>
    <automated>test -f spike/.julia-version && grep -qx '1.12.6' spike/.julia-version && julia -e 'm=read(joinpath("spike","Manifest.toml"),String); @assert occursin("NeuralEstimators",m) && occursin("Flux",m) && !occursin(r"\bCUDA\b",m); println("PIN OK")' && git ls-files --error-unmatch spike/Manifest.toml && echo TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - `spike/.julia-version` exists and contains exactly `1.12.6`.
    - A juliaup directory override of `1.12.6` is registered for `spike/` (verifiable via `juliaup override status`).
    - `spike/Manifest.toml` is git-tracked (`git ls-files --error-unmatch spike/Manifest.toml` succeeds) and pins exact `NeuralEstimators` and `Flux` versions with no `CUDA`/`CUDA_jll` entry.
    - Root `Project.toml`/`Manifest.toml`/`src/` unchanged vs the Plan-01 baseline.
  </acceptance_criteria>
  <done>The proven env is frozen: the Manifest is committed verbatim and the Julia version is both enforced (juliaup override) and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write the stack-decision + Julia-version note</name>
  <files>spike/NOTES.md</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-CONTEXT.md (D-07 — default/fallback; D-05 — Julia pin)
    - .planning/phases/01-environment-smoke-gate/01-PATTERNS.md (§"spike/NOTES.md" — content source: CLAUDE.md "What NOT to Use" + RESEARCH D-07)
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Standard Stack" alternatives + fallback ladder)
    - CLAUDE.md (§"What NOT to Use" — NormalizingFlows.jl / InvertibleNetworks.jl / RxInfer / BayesFlow-on-core-path)
  </read_first>
  <action>
    Create `spike/NOTES.md` recording, at minimum: (1) STACK DECISION (ENV-04 / D-07) — NeuralEstimators.jl is the default SBI engine; BayesFlow via PythonCall/CondaPkg is the fallback ONLY, invoked solely if the Julia normalising-flow fails to converge/calibrate after tuning; explicitly name the excluded packages from CLAUDE.md "What NOT to Use" (NormalizingFlows.jl, InvertibleNetworks.jl for the spike; RxInfer as a Turing replacement; BayesFlow on the core path). (2) JULIA-VERSION RECORD (D-05) — `1.12.6`, the enforcing `juliaup override set 1.12.6` mechanism, and an explicit note that `spike/.julia-version` is documentation-only (not auto-read). OPTIONALLY pre-seed the Turing prior ranges (mu/nu/sigma/tau, from the facts block) as a Phase-2 convenience, clearly marked as Phase-2 scope. Keep it a concise prose note. Commit it.
  </action>
  <verify>
    <automated>test -f spike/NOTES.md && grep -qi 'NeuralEstimators' spike/NOTES.md && grep -qi 'BayesFlow' spike/NOTES.md && grep -qi 'fallback' spike/NOTES.md && grep -q '1.12.6' spike/NOTES.md && grep -qi 'juliaup' spike/NOTES.md && echo NOTES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `spike/NOTES.md` states NeuralEstimators.jl as default and BayesFlow (PythonCall) as fallback-only.
    - `spike/NOTES.md` records the Julia version `1.12.6` and the `juliaup override` enforcing mechanism, and notes `.julia-version` is documentation-only.
    - `spike/NOTES.md` is git-tracked.
    - Root `Project.toml`/`Manifest.toml`/`src/` unchanged vs the Plan-01 baseline.
  </acceptance_criteria>
  <done>The stack decision and Julia-version pin are recorded in a committed note (ENV-04 satisfied; D-05 documentation half complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved env → committed artifact | The reproducibility guarantee depends on the exact pinned Manifest + Julia version being committed, not just resolved locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | spike/Manifest.toml (pre-1.0 version drift) | mitigate | Commit the resolved Manifest verbatim with exact NeuralEstimators/Flux versions; re-assert no CUDA entry |
| T-01-06 | Repudiation | irreproducible environment | mitigate | Enforce the Julia pin via `juliaup override set 1.12.6` AND record `1.12.6` + the mechanism in committed `.julia-version` / NOTES.md (D-05) |
| T-01-07 | Tampering | root manifests during freeze ops | mitigate | All freeze ops confined to `spike/`; acceptance re-asserts a clean diff vs the Plan-01 baseline |
</threat_model>

<verification>
- `spike/Manifest.toml` committed, pins exact NeuralEstimators/Flux, no CUDA entry.
- `spike/.julia-version` = `1.12.6`; juliaup override registered for `spike/`.
- `spike/NOTES.md` records default/fallback + Julia pin.
- Root manifests + `src/` unchanged vs the Plan-01 baseline.
</verification>

<success_criteria>
The proven environment is committed as a durable, reproducible artifact (pinned Manifest + pinned Julia version) and the stack decision is recorded — ENV-03 and ENV-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-environment-smoke-gate/01-03-SUMMARY.md` when done, recording the exact pinned NeuralEstimators/Flux versions.
</output>

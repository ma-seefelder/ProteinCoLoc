---
phase: 01-environment-smoke-gate
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/NOTES.md
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "The parent ProteinCoLoc package is reachable read-only from the spike env via Pkg.develop(path=\"..\"), OR the documented include() fallback is in place when the parent dep tree does not co-resolve with Flux 0.16"
    - "The smoke gate stays green after whichever coupling path is taken"
    - "The root Project.toml, Manifest.toml, and src/ are byte-identical to the Plan-01 agreed baseline (decoupling proof)"
  artifacts:
    - path: "spike/NOTES.md"
      provides: "Recorded coupling outcome (develop succeeded vs include() fallback) with the resolve evidence"
      contains: "develop"
  key_links:
    - from: "spike env"
      to: "ProteinCoLoc parent package"
      via: "Pkg.develop(path=\"..\") read-only, or include() fallback of specific src files"
      pattern: "(Pkg.develop|include)"
    - from: "decoupling proof"
      to: "Plan-01 baseline ref"
      via: "git diff --quiet against the recorded baseline for Project.toml, Manifest.toml, src/"
      pattern: "git diff.*src"
---

<objective>
Establish the read-only coupling between the spike env and the parent ProteinCoLoc package (so Phase 4's ADVI baseline can reach the real Turing `colocalization()` model), and PROVE the decoupling guarantee: root `Project.toml`/`Manifest.toml`/`src/` are byte-identical to the Plan-01 agreed baseline. This is the last and riskiest step because `Pkg.develop` pulls the parent's full dep tree (GLMakie 0.10.5 + Turing + PackageCompiler) as resolve constraints against Flux 0.16.

Purpose: ENV-01 — the parent must be reachable read-only without touching the protected root files. Per D-01, `Pkg.develop(path="..")` is preferred (clean package boundary, avoids Phase-4 rework per D-02); if the parent dep tree conflicts with or is unreasonably heavy alongside Flux 0.16, DOCUMENT the conflict and fall back to `include()` of the specific `src/` files. The smoke is independent of the coupling, so a failed develop does not break the gate — it falls back. Plan 03 already committed a known-good minimal Manifest as the safe revert point.
Output: a working coupling path (develop or documented include() fallback), a still-green smoke, and a clean decoupling proof.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-smoke-gate/01-CONTEXT.md
@.planning/phases/01-environment-smoke-gate/01-RESEARCH.md
@.planning/phases/01-environment-smoke-gate/01-PATTERNS.md
@.planning/phases/01-environment-smoke-gate/01-BASELINE.md

<facts>
<!-- Verified this planning session. -->
- Root [deps] that weigh on the co-resolve: Turing, GLMakie (pinned 0.10.5), PackageCompiler, Distributions, Images, KernelDensity, QuadGK, Random123, StatsBase. GLMakie 0.10.5 + Turing are the heavy items the smoke does not need.
- Reusable src/ assets the coupling must expose for LATER phases (not used by the Phase-1 smoke): src/colocalization.jl (correlation, patch, _prepare_data); src/LoadImages.jl (MultiChannelImage); src/bayes.jl (compute_BayesFactor, the Turing @model).
- The exact baseline ref + verify command are recorded in 01-BASELINE.md (Plan 01). Use THAT ref, not an assumed one.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt the Pkg.develop coupling; fall back to include() on conflict</name>
  <files>spike/Project.toml, spike/Manifest.toml, spike/NOTES.md</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Architecture Patterns" Pattern 2 build order + example sequence; §"Common Pitfalls" Pitfall 3 — GLMakie/Flux co-resolve)
    - .planning/phases/01-environment-smoke-gate/01-CONTEXT.md (D-01 develop-first / include-fallback; D-02 rationale)
    - spike/Manifest.toml (the committed known-good minimal env to revert to on conflict)
  </read_first>
  <action>
    With the spike env active (`Pkg.activate("spike")`), attempt the clean package boundary: `Pkg.develop(path="..")` then `Pkg.resolve()`. If it co-resolves: re-run `spike/test/runtests.jl` — it MUST stay green; if green, this is the chosen path; record success in `spike/NOTES.md` (note the parent is now reachable read-only and that later phases reach `correlation`/`patch`/`_prepare_data`/the Turing model through the package). Commit the updated `spike/Project.toml` + `spike/Manifest.toml` (the new pinned reproducibility artifact, ENV-03 re-pin). If `Pkg.resolve()` conflicts (e.g. `Unsatisfiable requirements` mentioning Makie/Colors/StaticArrays) or drags an unreasonably heavy tree: do NOT fight it — `Pkg.rm` the developed parent, restore the committed minimal Manifest (the Plan-03 safe point) so the smoke stays green, and implement the D-01 fallback by documenting in `spike/NOTES.md` (a) the verbatim conflict/resolver error, and (b) that later phases will reach `src/` via `include("../src/<file>.jl")` of the specific needed files rather than the package boundary (acceptable per D-02). Either way: NEVER edit the root Project.toml/Manifest.toml or anything under `src/` to make the resolve succeed.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl && grep -qiE '(Pkg.develop|develop succeeded|include\(\)? fallback|include fallback)' spike/NOTES.md && echo COUPLING_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `spike/test/runtests.jl` exits 0 (smoke stays green regardless of which coupling path was taken).
    - `spike/NOTES.md` records the coupling outcome: either "Pkg.develop succeeded" (parent reachable read-only) OR the include() fallback with the verbatim resolver conflict that justified it.
    - If develop succeeded: `spike/Project.toml` lists ProteinCoLoc via develop and the updated `spike/Manifest.toml` is committed; if fallback: the env is the committed minimal Manifest (no parent), unchanged from Plan 03.
    - No edits were made to root `Project.toml`/`Manifest.toml` or `src/` to achieve the resolve.
  </acceptance_criteria>
  <done>The parent is reachable read-only via Pkg.develop, or the include() fallback is documented with its conflict evidence; the smoke remains green either way.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the decoupling guarantee (root + src/ untouched vs baseline)</name>
  <files>spike/NOTES.md</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-BASELINE.md (the agreed baseline ref + the verbatim verify command, from Plan 01)
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Common Pitfalls" Pitfall 4 — untouched-claim against the agreed baseline)
  </read_first>
  <action>
    Read the baseline ref and verify command recorded in `01-BASELINE.md`. Run that exact check to assert the three protected paths are byte-identical to the agreed baseline: for commit/restore baselines, `git diff --quiet <baseline-ref> -- Project.toml Manifest.toml src/` (rc 0 = clean); for the snapshot baseline, re-compute `git hash-object Project.toml Manifest.toml` and compare to the recorded hashes plus `git diff --quiet HEAD -- src/`. Record the proof result (the command, the ref, and the clean outcome) in `spike/NOTES.md` as the Phase-1 decoupling-proof line (this is the pattern DEMO-02 will repeat at phase 6). If the check is NOT clean, STOP and surface it — a dirty protected file means the spike violated the decoupling invariant and must be investigated before the phase can pass. Do not "fix" it by editing root files.
  </action>
  <verify>
    <automated>git diff --quiet HEAD -- src/; SRC=$?; echo "src_rc=$SRC"; grep -qiE '(decoupl|untouched|byte-identical)' spike/NOTES.md && echo PROOF_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - The recorded baseline check from `01-BASELINE.md` runs clean: root `Project.toml`, `Manifest.toml`, and `src/` are byte-identical to the agreed baseline (clean diff / matching hashes).
    - `src/` has zero modifications (`git diff --quiet HEAD -- src/` returns 0).
    - `spike/NOTES.md` records the decoupling-proof line (command + ref + clean result).
    - If any protected path is dirty, the task halts and surfaces it rather than masking it.
  </acceptance_criteria>
  <done>The decoupling guarantee is proven: the frozen publication state under the root manifests and src/ is byte-identical to the agreed Plan-01 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike env ↔ parent package | Pkg.develop pulls the parent's full dep tree as resolve constraints; a careless fix could mutate root files |
| working tree → frozen publication state | root Project.toml/Manifest.toml/src/ must remain byte-identical to the agreed baseline (publication integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | root Project.toml / Manifest.toml / src/ during coupling | mitigate | Confine all Pkg ops to `Pkg.activate("spike")`; on resolve conflict, fall back to include() rather than editing root; Task 2 proves byte-identity vs the Plan-01 baseline |
| T-01-09 | Tampering | spike/Manifest.toml re-pin after develop | mitigate | On a successful develop, commit the updated Manifest as the new pinned artifact; on fallback, revert to the committed minimal Manifest (known-good) so the env stays reproducible and green |
| T-01-10 | Repudiation | coupling outcome | mitigate | Record the develop-vs-include() decision and its resolver evidence in NOTES.md (auditable), and record the decoupling-proof result for DEMO-02 to repeat |
</threat_model>

<verification>
- `spike/test/runtests.jl` green after the coupling step.
- `spike/NOTES.md` records the coupling outcome and the decoupling-proof line.
- Root `Project.toml`/`Manifest.toml`/`src/` byte-identical to the agreed Plan-01 baseline.
</verification>

<success_criteria>
The parent package is reachable read-only (or the include() fallback is documented), the smoke stays green, and the decoupling guarantee is proven against the agreed baseline — ENV-01 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-environment-smoke-gate/01-04-SUMMARY.md` when done, recording the coupling path taken (develop vs include fallback) and the decoupling-proof result.
</output>

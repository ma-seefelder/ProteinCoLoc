---
phase: 01-environment-smoke-gate
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/00_smoke.jl
  - spike/test/runtests.jl
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "An isolated spike/ environment activates its own minimal Project.toml (NeuralEstimators, Flux, Distributions) with no CUDA in its resolved Manifest (D-04: CPU-only, no forced CUDA import)"
    - "spike/00_smoke.jl trains a PosteriorEstimator (NormalisingFlow) on a 1-parameter Gaussian, runs sampleposterior, and recovers the posterior mean within tolerance, CPU-only (D-03: green + correctness assertion, not merely runs-without-error)"
    - "spike/test/runtests.jl re-runs the smoke as a stdlib Test gate and asserts CUDA is not a dependency and not loaded (D-06: re-runnable stdlib Test harness as the durable gate)"
  artifacts:
    - path: "spike/Project.toml"
      provides: "Minimal isolated dependency declaration for the smoke"
      contains: "NeuralEstimators"
    - path: "spike/00_smoke.jl"
      provides: "Readable CPU-only NPE smoke script with a correctness assertion"
      min_lines: 20
    - path: "spike/test/runtests.jl"
      provides: "Re-runnable hard gate (stdlib Test) wrapping the smoke + CUDA-absence guard"
      contains: "@testset"
  key_links:
    - from: "spike/test/runtests.jl"
      to: "spike/00_smoke.jl"
      via: "include of the smoke script then @test on the recovered mean"
      pattern: "include.*00_smoke"
    - from: "spike/00_smoke.jl"
      to: "NeuralEstimators.train"
      via: "train(...; use_gpu = false) CPU-only call"
      pattern: "use_gpu\\s*=\\s*false"
---

<objective>
Stand up the minimal isolated `spike/` environment and prove the pre-1.0 NeuralEstimators v0.2.1 + Flux v0.16 stack trains a normalising-flow posterior estimator and recovers a known parameter on this Windows machine, CPU-only. This is the single highest-value de-risking step of the entire spike (retires P4 API-churn and P5 Windows/GPU risk).

Purpose: ENV-02 — a <30-line smoke (`00_smoke.jl`) that is green AND correct (D-03), wrapped in a re-runnable stdlib Test gate (D-06), with no forced CUDA import (D-04). The smoke uses a synthetic 1-param Gaussian only and needs nothing from `src/` — coupling is verified separately in Plan 04.
Output: `spike/Project.toml` + resolved `spike/Manifest.toml`, `spike/00_smoke.jl`, `spike/test/runtests.jl`, all green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-smoke-gate/01-CONTEXT.md
@.planning/phases/01-environment-smoke-gate/01-RESEARCH.md
@.planning/phases/01-environment-smoke-gate/01-PATTERNS.md

<interfaces>
<!-- Verified v0.2.1 API surface (RESEARCH §Code Examples). MUST be re-confirmed against installed `?` docstrings — pre-1.0 drift is exactly what this smoke catches. Do NOT trust stale STACK.md / CRAN forms. -->
- NormalisingFlow(d::Integer, num_summaries::Integer; num_coupling_layers = 6, use_act_norm = true)
- PosteriorEstimator(network, d; num_summaries = d, q = <NormalisingFlow instance | distribution>)   # NOT the stale PosteriorEstimator(q, network)
- train(estimator, sampler, simulator; epochs = 100, stopping_epochs = 5, batchsize = 32, optimiser = Adam(5e-4), use_gpu = true, device = nothing)   # use_gpu DEFAULTS TO TRUE — pass use_gpu = false
- sampleposterior(estimator, Z; N = 1000)   # N is a KEYWORD; single data set → returns a d×N matrix (params in rows, draws in cols)
- posteriormean(θ::AbstractMatrix) = mean(θ; dims = 2)   # → d×1
- Three drift hotspots to confirm against docstrings: (1) PosteriorEstimator positional order, (2) N as keyword on sampleposterior, (3) whether q accepts a constructed NormalisingFlow instance vs a type.
</interfaces>

<conventions>
- License header: open spike/*.jl source files with the AGPL `#= ... =#` block used atop every src/*.jl (author "Dr. rer. nat. Manuel Seefelder") — PATTERNS §"Shared Patterns".
- Determinism: stdlib `Random.seed!(2026)` at the top of the smoke (NOT Random123 — that is a later-phase concern, RESEARCH Pitfall 5).
- Test harness: top-level `@testset "..." verbose = true begin ... end` mirroring test/runtests.jl (root) line 17.
- Isolated-env discipline (HARD): every Pkg op is `Pkg.activate("spike")` first; NEVER Pkg.add/resolve against the root project.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the minimal isolated spike environment and resolve it</name>
  <files>spike/Project.toml, spike/Manifest.toml</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Standard Stack" install block lines ~89-97; the "keep Phase-1 minimal" note)
    - .planning/phases/01-environment-smoke-gate/01-PATTERNS.md (§"spike/Project.toml" — minimal, do NOT copy heavy root deps; §"spike/Manifest.toml" — generated, never hand-written)
    - Project.toml (root — for the deps table FORMAT and reusable Distributions UUID, NOT its dependency set)
  </read_first>
  <action>
    From the repo root, run Julia with `Pkg.activate("spike")` then `Pkg.add(["NeuralEstimators", "Flux", "Distributions"])` and `Pkg.resolve()` — this creates and populates `spike/Project.toml` and emits `spike/Manifest.toml`. Keep the env MINIMAL: exactly NeuralEstimators, Flux, Distributions (Test is stdlib, may be omitted from `[deps]`). Do NOT add Turing, GLMakie, PackageCompiler, Images, KernelDensity, QuadGK, Random123, CUDA, or any other root/later-phase dep — they widen the resolve and the Windows-fragility surface this smoke exists to isolate. Do NOT run Pkg.develop here (Plan 04). Do NOT touch the root Project.toml/Manifest.toml. After resolve, confirm the Manifest contains no `CUDA` / `CUDA_jll` entry (the env-level half of the CPU-only guarantee, D-04).
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Pkg; d=keys(Pkg.project().dependencies); @assert "NeuralEstimators" in d && "Flux" in d && "Distributions" in d; m=read(joinpath("spike","Manifest.toml"),String); @assert !occursin(r"\bCUDA\b", m) "CUDA present in Manifest"; println("ENV OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `spike/Project.toml` exists and its `[deps]` are exactly NeuralEstimators, Flux, Distributions (no heavy root deps, no CUDA).
    - `spike/Manifest.toml` exists, was emitted by `Pkg.resolve()` (not hand-written), and contains no `CUDA`/`CUDA_jll` entry.
    - `using Pkg; Pkg.project().dependencies` on `--project=spike` lists the three packages.
    - Root `Project.toml`/`Manifest.toml` unchanged vs the Plan-01 baseline (`git diff --quiet <baseline-ref> -- Project.toml Manifest.toml`).
  </acceptance_criteria>
  <done>The isolated spike env resolves cleanly with only the three minimal deps and no CUDA; the root manifests are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the CPU-only NPE smoke and its re-runnable Test gate</name>
  <files>spike/00_smoke.jl, spike/test/runtests.jl</files>
  <read_first>
    - .planning/phases/01-environment-smoke-gate/01-RESEARCH.md (§"Code Examples" lines ~252-314 — verified v0.2.1 signatures + smoke skeleton; §"Common Pitfalls" 1, 2, 5)
    - .planning/phases/01-environment-smoke-gate/01-PATTERNS.md (§"spike/00_smoke.jl" drift hotspots; §"spike/test/runtests.jl" two required assertions)
    - spike/Manifest.toml (to know the exact resolved NeuralEstimators/Flux versions whose docstrings you verify against)
  </read_first>
  <behavior>
    - Trains a PosteriorEstimator (NormalisingFlow, d=1) on a synthetic 1-param Gaussian (θ ~ N(0,1), Z|θ ~ N(θ, σ²)) and recovers a KNOWN θ_true within tolerance.
    - Correctness gate: `abs(posteriormean(sampleposterior(est, Z_obs; N=1000))[1] - θ_true) < 0.3` holds for a correct CPU backend; a silently-broken/mis-constructed estimator fails it clearly.
    - CPU-only guard: the test asserts CUDA is neither a project dependency nor a loaded module.
    - Re-runnable: `julia --project=spike spike/test/runtests.jl` exits 0 deterministically (seeded), within ~120s.
  </behavior>
  <action>
    FIRST open a REPL on `--project=spike` and read `?PosteriorEstimator`, `?NormalisingFlow`, `?sampleposterior`, `?train`, `?posteriormean` to confirm the exact installed v0.2.1 signatures — write the smoke from THOSE docstrings, resolving the three drift hotspots (PosteriorEstimator positional order; N as keyword on sampleposterior; q passed as a constructed NormalisingFlow instance vs a type). Then write `spike/00_smoke.jl`: AGPL `#=...=#` header; `Random.seed!(2026)`; d=1, σ=1.0f0; a prior sampler returning a 1×K Float32 matrix and a simulator returning K data sets Z|θ; an inner Flux `Chain` whose output width equals `num_summaries`; `q = NormalisingFlow(d, num_summaries)`; `est = PosteriorEstimator(network, d; num_summaries=num_summaries, q=q)`; `est = train(est, sampler, simulator; epochs=200, use_gpu=false)` — `use_gpu=false` is mandatory (D-04). Then draw `Z_obs` from a fixed `θ_true = 0.7f0`, compute `μ̂ = posteriormean(sampleposterior(est, Z_obs; N=1000))[1]`, set `tol = 0.3` (generous — catches a broken backend, not a precision certifier; Claude's-discretion tolerance per D-03), and assert `abs(μ̂ - θ_true) < tol`. Keep the core near the ~30-line target. Then write `spike/test/runtests.jl`: AGPL or `using Test` preamble, `@testset "NeuralEstimators CPU smoke" verbose = true begin ... end` that includes the smoke (or calls a refactored smoke function) and asserts (a) `abs(μ̂ - θ_true) < tol`, and (b) CUDA-absence — `!haskey(Pkg.project().dependencies, "CUDA")` AND CUDA not in `Base.loaded_modules` (adjust to the installed API). Do NOT import Images / `.ProteinCoLoc` / Random123 — the smoke needs nothing from the parent. Do NOT pass `use_gpu=true` and do NOT add CUDA.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl; echo "rc=$?"</automated>
    <note>Expected duration: ~seconds to low minutes on CPU — the smoke IS the test and runs full NPE training, so do NOT cancel a running invocation prematurely (a one-time Julia/package precompilation warm-up is added on the first call). `epochs` may be tuned to the minimum that reliably passes the tolerance assertion with margin (RESEARCH Pitfall 5).</note>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/runtests.jl` exits 0 with all `@test`s passing.
    - `spike/00_smoke.jl` contains `use_gpu = false` and a tolerance assertion comparing the recovered mean to `θ_true`.
    - `spike/test/runtests.jl` contains a `@testset`, includes/calls the smoke, asserts the recovered-mean tolerance, and asserts CUDA is neither a dependency nor a loaded module.
    - The smoke is deterministic across two consecutive runs (seeded) — both green.
    - No CUDA driver/import is requested at runtime; `spike/Manifest.toml` still has no CUDA entry.
    - Root `Project.toml`/`Manifest.toml`/`src/` unchanged vs the Plan-01 baseline.
  </acceptance_criteria>
  <done>The green, correctness-asserting, CPU-only smoke and its re-runnable Test gate exist and pass; the pre-1.0 stack is proven on this machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Julia General registry → spike env | Pre-1.0 packages (NeuralEstimators v0.2.x) are installed; version drift is the integrity risk |
| GPU/CUDA artifacts → "CPU-only" path | An accidental CUDA pull/import would violate the D-04 CPU-only contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | NeuralEstimators / Flux / Distributions installs (Julia General registry) | mitigate | RESEARCH §"Package Legitimacy Audit" verified all three legitimate (Julia General provenance; slopcheck is npm/PyPI/crates-only and does not apply to Julia). No `[ASSUMED]`/`[SUS]` packages → no blocking legitimacy checkpoint required. Risk is version churn, not provenance — mitigated by pinning the resolved Manifest in Plan 03 |
| T-01-03 | Tampering | spike Manifest / runtime device | mitigate | No CUDA dep installed; `train(...; use_gpu=false)`; harness asserts CUDA is neither a dependency nor loaded |
| T-01-04 | Tampering | root Project.toml / Manifest.toml | mitigate | All Pkg ops run under `Pkg.activate("spike")`; acceptance re-asserts a clean diff vs the Plan-01 baseline |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` exits 0 (green gate).
- `spike/Manifest.toml` has no CUDA entry; smoke runs CPU-only.
- Root manifests + `src/` unchanged vs the Plan-01 baseline ref.
</verification>

<success_criteria>
The isolated spike env resolves with the minimal stack and the CPU-only NPE smoke is green AND correct under a re-runnable stdlib Test gate — the phase's hard gate is established.
</success_criteria>

<output>
Create `.planning/phases/01-environment-smoke-gate/01-02-SUMMARY.md` when done, recording the resolved NeuralEstimators/Flux versions and the recovered mean vs θ_true.
</output>

---
phase: 08-external-physical-ground-truth-corpus
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - corpus/manifest.jl
  - corpus/load.jl
  - corpus/test/test_manifest.jl
  - corpus/test/test_load.jl
autonomous: true
requirements: [SC1, SC2, SC3]

must_haves:
  truths:
    - "The manifest schema is validated: required columns present; tier in {physical-primary,simulated-secondary}; split in {sealed_holdout,dev,eval} (SC3)"
    - "The default accessor NEVER blends tiers; mixing physical + CBS requires an explicit mixed_tier(...) opt-in (SC2, D-06)"
    - "The default corpus iterator NEVER yields a sealed_holdout row; anchors only via open_sealed_holdout(;reason) (D-09)"
    - "An anchor row is rejected unless it carries a biological truth_label + open license + accession (D-03 acceptance predicate)"
    - "A two-channel TIFF pair converts to MultiChannelImage and reduces through UNCHANGED patch(.,8)/correlation to a finite grid (SC1, D-03)"
    - "The committed manifest is content-addressed so any contract edit is detectable (D-08)"
  artifacts:
    - path: "corpus/manifest.jl"
      provides: "schema build/validate, typed tier accessors, split guard, validate_anchor_row (D-03), content-addressed atomic writer"
      contains: "physical_anchors"
    - path: "corpus/load.jl"
      provides: "two single-channel TIFF -> MultiChannelImage (read-only src reuse)"
      contains: "load_anchor"
    - path: "corpus/test/test_manifest.jl"
      provides: "schema + tier-guard + split-guard + predicate tests"
      contains: "@testset"
  key_links:
    - from: "corpus/manifest.jl"
      to: "corpus/hash.jl"
      via: "manifest_hash content-addressing of the committed contract"
      pattern: "manifest_hash"
    - from: "corpus/load.jl"
      to: "src/LoadImages.jl"
      via: "read-only include of the frozen MultiChannelImage ctor + load_tiff"
      pattern: "include.*LoadImages"
---

<objective>
Build the data-contract + conversion layer: `corpus/manifest.jl` (D-08 CSV schema, typed tier accessors with the D-06 structural separation, the D-09 sealed-holdout split guard, the D-03 anchor acceptance predicate, and a content-addressed atomic writer) and `corpus/load.jl` (two-channel TIFF -> `MultiChannelImage` via read-only reuse of frozen `src/`). Fully offline against the committed fixture; no `src/` edits.

Purpose: This is the versioned validation data contract (SC3) plus the structural guards that make SC2 (never-conflated tiers) and D-09 (sealed anchors) true by construction — mirroring the Phase-3 "no `standardize_all` symbol exists" leakage-impossible discipline.
Output: `corpus/manifest.jl`, `corpus/load.jl`, tests wired into `corpus/test/runtests.jl`. (This plan does NOT write the committed `corpus/manifest.csv`; CBS rows in 08-04 write it first.)
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-external-physical-ground-truth-corpus/08-PATTERNS.md
@.planning/phases/08-external-physical-ground-truth-corpus/08-RESEARCH.md

<interfaces>
<!-- VERIFIED analog idioms (08-PATTERNS.md). Use directly. -->
Tidy DataFrame assembly — spike/comparator/table.jl:175-189 (DataFrame(col=..., ...) named columns).
Content-addressed atomic CSV+JLD2 writer w/ pre-registration header — spike/comparator/table.jl:268-296 write_table (dir=hash(config); CSV comment header; .tmp->@assert filesize>0->mv; JLD2 twin).
Structural-holdout discipline — src/amortized/datagen.jl _write_holdout L387-417 (disjoint XOR-salted RNG + NEGATIVE global indices; "no standardize_all symbol" note).
MultiChannelImage primary ctor — src/LoadImages.jl:126-130 (asserts length(data)==length(channels)==length(otsu_threshold)).
MultiChannelImage convenience ctor from paths — src/LoadImages.jl:432-455 (load_tiff.(path); pixel_size=size(data[1]); otsu_threshold=Images.otsu_threshold.(data)).
load_tiff — src/LoadImages.jl:214 (Float64.(Images.Gray.(Images.load(path)))).
patch — src/colocalization.jl:37 patch(img::AbstractMatrix, num_patches::Integer).
correlation — src/colocalization.jl:221 correlation(x::Array{T,4}, y::Array{T,4}; method=:pearson); _exclude_zero drops 0/NaN/missing; patch <=15 valid -> missing (L187-235).
From corpus/config.jl: TIERS, SPLITS, ROLES, MANIFEST_COLUMNS, MANIFEST_SCHEMA_VERSION.
From corpus/hash.jl: manifest_hash, canonical_config, file_sha256.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest schema + tier/split guards + acceptance predicate + content-addressed writer (SC2, SC3, D-03, D-06, D-08, D-09)</name>
  <files>corpus/manifest.jl, corpus/test/test_manifest.jl</files>
  <read_first>
    - spike/comparator/table.jl (build_table L175-189; write_table L268-296)
    - src/amortized/datagen.jl (_write_holdout L387-417 structural-disjointness; open_or_invalidate L368-385)
    - corpus/hash.jl (manifest_hash, canonical_config)
    - corpus/config.jl (TIERS, SPLITS, ROLES, MANIFEST_COLUMNS)
  </read_first>
  <behavior>
    - build_manifest(rows) -> DataFrame with exactly the MANIFEST_COLUMNS columns.
    - validate_manifest(df) throws on: a missing required column; a tier not in TIERS; a split not in SPLITS; a role not in ROLES; a physical-primary row whose split != sealed_holdout.
    - physical_anchors(df) returns ONLY tier=="physical-primary" AND split!=sealed_holdout is impossible here (anchors are sealed) — so physical_anchors returns nothing by default; there is NO default accessor returning both tiers.
    - cbs_benchmark(df) returns ONLY tier=="simulated-secondary" rows.
    - corpus_default(df) (the default iterator) returns NO sealed_holdout rows (asserted: none of its rows has split=="sealed_holdout").
    - open_sealed_holdout(df; reason) returns the sealed physical-primary rows and requires a non-empty reason; it is the ONLY path to them.
    - mixed_tier(df) is the ONLY function returning both tiers together; callers must name it explicitly.
    - validate_anchor_row(row) returns false unless truth_label in {"coloc","segregated"} AND license is non-empty AND accession is non-empty (D-03 predicate (1)+(2)+(4)).
    - write_manifest(df; outdir) content-addresses into a dir named by manifest_hash and writes CSV (comment header quoting schema version + CORPUS_MASTER_SEED) + JLD2 twin, atomically.
  </behavior>
  <action>
    Create `corpus/manifest.jl` (guarded include of config.jl + hash.jl). Implement `build_manifest(rows)` assembling a `DataFrame` with the 14 MANIFEST_COLUMNS (map RESEARCH §Manifest Schema types). Implement `validate_manifest(df)` throwing `error(...)` on any schema violation listed in behavior (structural enum checks against TIERS/SPLITS/ROLES; the physical-primary=>sealed_holdout invariant enforcing D-09). Implement the STRUCTURAL tier separation per D-06: expose SEPARATE typed accessors `physical_anchors(df)` and `cbs_benchmark(df)`; make the ONLY both-tier path an explicitly-named `mixed_tier(df)` (document that no default "everything" accessor exists, mirroring datagen's no-`standardize_all` note); the default iterator `corpus_default(df)` filters OUT `sealed_holdout` rows. Implement the D-09 split guard `open_sealed_holdout(df; reason::AbstractString)` that `@assert !isempty(reason)` and returns the sealed physical-primary rows — the sole reachable path; add a module note that this is called ONLY by Phase-16 code. Implement `validate_anchor_row(row)` for the D-03 acceptance predicate (biological truth_label + non-empty open license + non-empty accession; coloc_ground_truth must be an asserted 1.0/0.0 for anchors, NOT a computed score). Implement `write_manifest(df; outdir)` copying `write_table`'s content-addressed atomic idiom: dir = joinpath(outdir, manifest_hash(src_files, canonical_config(config))); CSV comment header lines quoting `MANIFEST_SCHEMA_VERSION` and `CORPUS_MASTER_SEED` (pre-registration); `.tmp -> @assert filesize>0 -> mv(force=true)`; a JLD2 twin with reopen-integrity. Create `corpus/test/test_manifest.jl` asserting every behavior bullet: schema-valid manifest passes; bad tier/split/role each throw; a physical-primary row with split=dev throws; `corpus_default` yields zero sealed_holdout rows; `open_sealed_holdout(df;reason="")` throws but `reason="phase16"` returns the anchors; `mixed_tier` returns both tiers; `validate_anchor_row` rejects a missing-license / computed-score row and accepts a biology-labelled open-license row. Wire `include("test_manifest.jl")` into `corpus/test/runtests.jl`.
  </action>
  <verify>
    <automated>julia --project=. corpus/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - test_manifest testset passes offline inside runtests.jl.
    - There is NO function returning both tiers except `mixed_tier` (grep: only `mixed_tier` unions physical + simulated).
    - `corpus_default(df)` on a manifest containing a sealed_holdout row returns zero sealed_holdout rows.
    - `open_sealed_holdout(df; reason="")` throws; with a non-empty reason returns the physical-primary rows.
    - `validate_anchor_row` returns false for a row missing license or accession, or whose truth_label is not in {coloc,segregated}.
    - `write_manifest` produces a CSV whose comment header contains the schema version and CORPUS_MASTER_SEED, plus a JLD2 twin.
  </acceptance_criteria>
  <done>Manifest schema, D-06 tier guard, D-09 split guard, D-03 predicate, and content-addressed writer implemented and tested by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Two-channel TIFF -> MultiChannelImage loader via read-only src reuse (SC1, D-03)</name>
  <files>corpus/load.jl, corpus/test/test_load.jl</files>
  <read_first>
    - src/LoadImages.jl (load_tiff L214; convenience ctor L432-455; primary ctor L126-130) — READ-ONLY, no edits
    - src/colocalization.jl (patch L37; correlation L221; _exclude_zero floor L187-235)
    - corpus/test/make_fixture.jl (08-01 fixture)
    - corpus/config.jl (MIN_PATCH_VALID_PIXELS)
  </read_first>
  <action>
    Create `corpus/load.jl` reaching `src/` READ-ONLY via a guarded `isdefined(Main, :MultiChannelImage) || include(joinpath(@__DIR__, "..", "src", "LoadImages.jl"))` and similarly for `colocalization.jl` (mirror the guarded-include idiom; NO edits to `src/`). Expose `load_anchor(name, ch1_path, ch2_path; channels=["ch1","ch2"])` that calls the convenience `MultiChannelImage(name, [ch1_path, ch2_path], channels)` constructor (auto pixel_size + otsu_threshold). Expose `summary_grid(img; num_patches=8)` that runs the UNCHANGED `patch(img, num_patches)` + `correlation(...)` and returns the 8x8 grid. Add a `has_signal(grid)` helper returning `true` iff the grid has at least one non-missing entry and `mean(skipmissing(grid))` is finite (Pitfall-6 guard, referencing MIN_PATCH_VALID_PIXELS in the docstring). Document that a RGB/composite single-TIFF (CBS layout) is handled separately in 08-04 (`corpus/cbs.jl`) — this file handles the two-single-channel-TIFF anchor layout only. Create `corpus/test/test_load.jl` that builds a MultiChannelImage from the committed fixture TIFFs, asserts `summary_grid` is not all-missing and `has_signal` is true (finite mean). Wire `include("test_load.jl")` into `corpus/test/runtests.jl`.
  </action>
  <verify>
    <automated>julia --project=. corpus/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - test_load testset passes offline; the fixture reduces to a finite (non-all-missing) 8x8 grid via UNCHANGED `patch`/`correlation`.
    - `corpus/load.jl` contains NO edits to `src/` (reaches it only via `include`); `git status src/` shows no modification.
    - `has_signal` returns true for the committed fixture (Pitfall-6 guard).
  </acceptance_criteria>
  <done>Two-channel TIFF anchors convert to MultiChannelImage and reduce through the frozen src/ summary path to a finite grid; src/ untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-dev code → sealed anchors | Anti-snooping: dev code must not read physical anchors before Phase 16 (D-09) |
| physical tier → simulated tier | Must never silently blend (SC2/D-06) |
| committed contract → later edits | A contract edit must be detectable (content hash) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege | Default accessor reaching sealed_holdout anchors | mitigate | `corpus_default` filters out sealed_holdout structurally; `open_sealed_holdout(;reason)` is the sole path; unit test asserts default yields none (D-09) |
| T-08-08 | Tampering | Silent tier blending (physical treated as simulated or vice versa) | mitigate | Separate typed accessors; only explicitly-named `mixed_tier` unions tiers (D-06/SC2) |
| T-08-09 | Tampering | Undetected manifest contract edit | mitigate | Content-addressed manifest via manifest_hash + JLD2 reopen-integrity (D-08) |
| T-08-10 | Spoofing | A physical-primary row with a computed (non-biological) truth label | mitigate | validate_anchor_row rejects rows whose truth_label is not a biological {coloc,segregated} label (D-03, Pitfall 1) |
| T-08-SC | Tampering | Supply chain | accept | Reuses existing CSV/DataFrames/JLD2/Images; no new deps |
</threat_model>

<verification>
- `julia --project=. corpus/test/runtests.jl` exits 0 offline (manifest + load testsets green).
- `git status --porcelain src/` shows no `src/` modification.
</verification>

<success_criteria>
- SC3 contract: schema + validation + content-addressed writer exist.
- SC2/D-06 tier guard and D-09 split guard true by construction (tests prove default paths never leak).
- SC1/D-03 conversion path proven on the fixture; anchor acceptance predicate enforceable.
</success_criteria>

<output>
Create `.planning/phases/08-external-physical-ground-truth-corpus/08-03-SUMMARY.md` when done
</output>

---
phase: 08-external-physical-ground-truth-corpus
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - corpus/cbs.jl
  - corpus/manifest.csv
  - corpus/test/test_cbs.jl
autonomous: true
requirements: [SC2, SC3]

must_haves:
  truths:
    - "Every CBS row is tier=simulated-secondary and role=benchmark, never conflated with physical anchors (SC2, D-06)"
    - "The FULL CBS corpus (all 3 channel pairs x all labelled degrees) is enumerated in the manifest (D-07)"
    - "CBS carries a seeded, bit-reproducible dev/eval split (D-09) keyed by CORPUS_MASTER_SEED xor CBS_SPLIT_SALT"
    - "A CBS .zip extracts only inside a scoped temp dir; any path escaping it is rejected (zip-slip mitigation)"
    - "The live CBS fetch smoke SKIPS cleanly offline (status=:skipped) so CI stays green (D-05)"
    - "corpus/manifest.csv is committed with the CBS rows + the sealed anchor placeholder rows (SC3)"
  artifacts:
    - path: "corpus/cbs.jl"
      provides: "CBS row enumeration, seeded dev/eval split, zip-slip-safe extraction, RGB-composite -> two-channel split"
      contains: "cbs_rows"
    - path: "corpus/manifest.csv"
      provides: "The committed versioned contract (CBS rows + sealed anchor placeholders)"
      contains: "simulated-secondary"
    - path: "corpus/test/test_cbs.jl"
      provides: "tier-tag + full-enumeration + seeded-split + zip-slip + offline-skip tests"
      contains: "@testset"
  key_links:
    - from: "corpus/cbs.jl"
      to: "corpus/manifest.jl"
      via: "build_manifest + validate_manifest + write_manifest of CBS rows tagged simulated-secondary"
      pattern: "build_manifest"
    - from: "corpus/cbs.jl"
      to: "corpus/fetch.jl"
      via: "fetch_verified for CBS zips (skip-with-flag offline)"
      pattern: "fetch_verified"
---

<objective>
Ingest the Colocalization Benchmark Source (CBS) — the one VERIFIED dataset — into the manifest as the `simulated-secondary` tier (D-06/D-07/SC2): enumerate the FULL corpus (3 channel pairs x labelled degrees), assign a seeded bit-reproducible dev/eval split (D-09), provide zip-slip-safe extraction + RGB-composite -> two-channel splitting, and write the committed `corpus/manifest.csv`. The live fetch is a smoke that skips cleanly offline (D-05).

Purpose: SC2 — CBS ingested and explicitly flagged simulated/secondary, never conflated with the physical anchors. This is autonomous because CBS provenance/license/format are VERIFIED (CC-BY-NC-SA-4.0, lossless TIFF, colocalization-benchmark.com).
Output: `corpus/cbs.jl`, the committed `corpus/manifest.csv`, and `corpus/test/test_cbs.jl` wired into the runner.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-external-physical-ground-truth-corpus/08-RESEARCH.md
@.planning/phases/08-external-physical-ground-truth-corpus/08-PATTERNS.md

<interfaces>
<!-- From prior plans + VERIFIED analogs. -->
From corpus/manifest.jl (08-03): build_manifest, validate_manifest, write_manifest, cbs_benchmark, MANIFEST_COLUMNS mapping.
From corpus/fetch.jl (08-02): fetch_verified(url, dest, expected_sha256) -> (status=:present|:skipped, ...).
From corpus/hash.jl (08-02): file_sha256.
From corpus/config.jl (08-01): CORPUS_MASTER_SEED, CBS_SPLIT_SALT, CORPUS_DATA_DIR, TIERS.
Seeded-split idiom — src/amortized/datagen.jl:56-60 salts + keyed RNGs L81-100 (Philox keyed by master_seed xor SALT); spike/comparator/config.jl:56 MASTER_SEED.
CBS FACTS (RESEARCH §CBS, VERIFIED): 3 sets Red-Green / Red-Blue / Green-Blue, each 10 images; degrees in 10% increments 0..90; lossless TIFF; license CC-BY-NC-SA-4.0; URL pattern colocalization-benchmark.com/download/[filename].zip; citation "Colocalization Benchmark Source (www.colocalization-benchmark.com)"; Zinchuk & Grossenbacher-Zinchuk 2014.
RGB split — Images.channelview / Images.red./green./blue. -> Float64 (RESEARCH §Image Format Conversion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CBS row enumeration + seeded dev/eval split + zip-slip-safe extraction (SC2, D-06, D-07, D-09)</name>
  <files>corpus/cbs.jl, corpus/test/test_cbs.jl</files>
  <read_first>
    - corpus/manifest.jl (build_manifest / validate_manifest / cbs_benchmark)
    - corpus/config.jl (CORPUS_MASTER_SEED, CBS_SPLIT_SALT, CORPUS_DATA_DIR)
    - src/amortized/datagen.jl (salt + keyed-RNG idiom L56-100)
    - 08-RESEARCH.md (§CBS verified facts; §Security zip-slip)
  </read_first>
  <action>
    Create `corpus/cbs.jl` (guarded include of config.jl + manifest.jl + fetch.jl). Implement `cbs_rows()` enumerating the FULL CBS corpus (D-07): for each channel pair in ("Red-Green","Red-Blue","Green-Blue") x each labelled degree in 0:10:90, emit a manifest row with `anchor_id` like `cbs-RG-000`..`cbs-GB-090`, `tier="simulated-secondary"`, `role="benchmark"`, `truth_label="simulated-degree"`, `coloc_ground_truth` = the labelled degree as a fraction (e.g. 0.50), `accession`="CBS/<pair>/<degree>", `source_url` = the colocalization-benchmark.com download URL PATTERN (leave the exact zip filename as a documented TODO to confirm at fetch time per RESEARCH A6 — do NOT invent a false-precise filename; use the verified `colocalization-benchmark.com/download/` prefix), `citation`="Zinchuk & Grossenbacher-Zinchuk 2014; Colocalization Benchmark Source (www.colocalization-benchmark.com)", `license`="CC-BY-NC-SA-4.0", `format`="tiff" (packaged in "zip"), `channels` = the pair (e.g. "R,G"), `sha256`="" (filled on first authorized fetch), `bytes`=0 (filled on fetch). Implement `assign_cbs_split(rows; seed=CORPUS_MASTER_SEED)` assigning each row `split in {"dev","eval"}` via a Random123 Philox keyed by `seed xor CBS_SPLIT_SALT` (bit-reproducible, disjoint stream) — NO CBS row is ever `sealed_holdout` (that is anchors-only, D-09). Implement `safe_extract(zip_path, target_dir)` that extracts entries ONLY inside `target_dir`: for every entry, compute the normalized joined path and `error(...)` if it does not start with `normpath(target_dir)` (zip-slip rejection) before writing. Implement `cbs_rgb_to_channels(tiff_path, pair)` splitting an RGB/composite TIFF into two `Matrix{Float64}` channels via `Images.channelview`/`Images.red`.`green`.`blue` per the pair. Create `corpus/test/test_cbs.jl` asserting: `cbs_rows()` yields 30 rows (3 pairs x 10 degrees), every row tier==simulated-secondary and role==benchmark; `assign_cbs_split` is deterministic (two runs identical) and yields only dev/eval (never sealed_holdout); `safe_extract` throws on a crafted entry whose path escapes the target dir (e.g. "../evil.tif"); `validate_manifest(build_manifest(cbs_rows()))` passes. Wire `include("test_cbs.jl")` into runtests.jl.
  </action>
  <verify>
    <automated>julia --project=. corpus/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `cbs_rows()` returns 30 rows; every row has tier=="simulated-secondary" and role=="benchmark" (SC2/D-06/D-07).
    - `assign_cbs_split` is bit-reproducible across two invocations and assigns only "dev"/"eval" (no CBS row is sealed_holdout).
    - `safe_extract` throws an ErrorException on a zip entry that resolves outside the target dir (zip-slip).
    - `validate_manifest(build_manifest(cbs_rows()))` passes; no CBS `coloc_ground_truth` is derived from a computed score.
  </acceptance_criteria>
  <done>Full CBS corpus enumerated as simulated-secondary with a seeded dev/eval split; extraction is zip-slip-safe; manifest validates.</done>
</task>

<task type="auto">
  <name>Task 2: Commit corpus/manifest.csv + live CBS fetch smoke that skips offline (SC3, D-05)</name>
  <files>corpus/manifest.csv, corpus/test/test_cbs.jl</files>
  <read_first>
    - corpus/manifest.jl (write_manifest content-addressed writer)
    - corpus/fetch.jl (fetch_verified skip-with-flag)
    - corpus/cbs.jl (Task 1: cbs_rows, assign_cbs_split)
    - 08-VALIDATION.md (§Per-Task map: CBS live-fetch smoke skips offline)
  </read_first>
  <action>
    Assemble the committed `corpus/manifest.csv` = CBS rows (from Task 1, with split assigned) PLUS placeholder rows for the two physical anchors so the schema/guards are exercised with anchor rows present: one `positive` and one `negative` row, `tier="physical-primary"`, `split="sealed_holdout"`, `truth_label` = "coloc"/"segregated", `coloc_ground_truth` = 1.0/0.0, `accession`/`source_url`/`sha256`/`bytes` left as `"PENDING"`/`0` (finalized in 08-05 after human-verify). Run `validate_manifest` then write `corpus/manifest.csv` at the repo-tracked path via a plain `CSV.write` with the pre-registration comment header (schema version + CORPUS_MASTER_SEED) — this is the committed contract artifact tracked in git (the content-addressed `write_manifest` dir remains the integrity twin; the flat `corpus/manifest.csv` is the human-readable committed copy). Add `fetch_cbs_smoke(; nsets=1)` that attempts `fetch_verified` on ONE small CBS zip URL into `CORPUS_DATA_DIR` in bootstrap mode (expected_sha256=nothing) and returns its `(status=...)`; it must return `:skipped` (not throw) when offline. Create the CBS live-fetch smoke as a testset INSIDE `corpus/test/test_cbs.jl` guarded so that offline it asserts `status in (:skipped, :present)` and NEVER requires network (mirror the tapqir `:skipped` contract); when `:present`, assert the downloaded zip `safe_extract`s and the extracted TIFF `cbs_rgb_to_channels` yields two finite channels. Assert (staging guard, D-04/Pitfall 3): no `.tif`/`.zip` under `CORPUS_DATA_DIR` is git-tracked — `git ls-files corpus/data` returns empty.
  </action>
  <verify>
    <automated>julia --project=. corpus/test/runtests.jl && test -z "$(git ls-files corpus/data)" && echo "no bytes staged"</automated>
  </verify>
  <acceptance_criteria>
    - `corpus/manifest.csv` exists, is git-tracked, and contains 30 CBS rows (tier=simulated-secondary) + 2 physical-primary sealed_holdout placeholder rows; `validate_manifest` on it passes.
    - The manifest CSV comment header contains the schema version and CORPUS_MASTER_SEED.
    - `fetch_cbs_smoke()` returns `:skipped` offline without throwing; runtests.jl stays green with zero network.
    - `git ls-files corpus/data` returns empty (no image bytes staged, D-04).
  </acceptance_criteria>
  <done>The versioned manifest.csv contract is committed with CBS + sealed anchor placeholders; CBS fetch smoke skips cleanly offline; no bytes staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CBS .zip archive → local disk | Untrusted archive entries may escape the target dir (zip-slip) |
| CBS simulated tier → physical anchors | Must never be conflated (SC2) |
| downloaded bytes → git | Bytes must stay out of git (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | Zip-slip on CBS .zip extraction | mitigate | `safe_extract` validates every entry's normalized path stays within target_dir before writing; else `error(...)` |
| T-08-12 | Tampering | CBS conflated with physical anchors | mitigate | cbs_rows hard-codes tier=simulated-secondary/role=benchmark; validate_manifest enforces enums (SC2/D-06) |
| T-08-13 | Tampering | Corrupted CBS download | mitigate | fetch_verified SHA-256 verify (hard-fail on mismatch once a baseline hash is recorded) |
| T-08-06 | Availability | Offline CI (CBS unreachable) | accept | fetch_cbs_smoke returns :skipped; tests never require network (D-05) |
| T-08-01 | Tampering | CBS bytes leaking into git | mitigate | git ls-files corpus/data asserted empty; corpus/data gitignored (D-04) |
| T-08-SC | Tampering | Supply chain | accept | No new deps; Images/CSV/Random123 already present |
</threat_model>

<verification>
- `julia --project=. corpus/test/runtests.jl` exits 0 offline.
- `git ls-files corpus/data` empty; `corpus/manifest.csv` tracked and validate_manifest-clean.
</verification>

<success_criteria>
- SC2: full CBS corpus ingested as simulated-secondary, structurally never conflated with anchors.
- SC3: committed versioned manifest.csv (schema + provenance + split policy).
- D-05/D-07/D-09 honored: full enumeration, seeded dev/eval split, offline-skip fetch.
</success_criteria>

<output>
Create `.planning/phases/08-external-physical-ground-truth-corpus/08-04-SUMMARY.md` when done
</output>

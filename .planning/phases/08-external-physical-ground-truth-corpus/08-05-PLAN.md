---
phase: 08-external-physical-ground-truth-corpus
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - corpus/anchor_rows.jl
  - corpus/manifest.csv
  - corpus/test/test_anchors.jl
autonomous: false
requirements: [SC1]

must_haves:
  truths:
    - "The positive anchor is a named tandem-fluorophore construct pinned by DOI/accession, truth from biology + citation (SC1, D-01)"
    - "The negative anchor is a distinct-compartment two-fluorophore construct pinned by DOI/accession (SC1, D-02)"
    - "Each pinned anchor accession resolves, carries an unambiguous biological coloc/segregation label, and has an open license (human-verified, D-03)"
    - "Both anchors are archived with provenance + a SHA-256 content hash in the manifest (SC1)"
    - "Both anchors are split=sealed_holdout and reachable only via open_sealed_holdout (D-09)"
  artifacts:
    - path: "corpus/anchor_rows.jl"
      provides: "The two physical-primary anchor rows with confirmed accession/license/citation + recorded sha256"
      contains: "anchor_rows"
    - path: "corpus/manifest.csv"
      provides: "Finalized committed contract: CBS rows + both physical anchors (sealed_holdout, provenance + hash)"
      contains: "physical-primary"
    - path: "corpus/test/test_anchors.jl"
      provides: "anchor-row predicate + sealed-holdout + provenance-hash tests"
      contains: "@testset"
  key_links:
    - from: "corpus/anchor_rows.jl"
      to: "corpus/manifest.jl"
      via: "validate_anchor_row + validate_manifest + write finalized rows"
      pattern: "validate_anchor_row"
    - from: "corpus/anchor_rows.jl"
      to: "corpus/fetch.jl"
      via: "fetch_verified bootstrap to record the trusted baseline sha256"
      pattern: "fetch_verified"
---

<objective>
Pin the two PHYSICAL ground-truth anchors (SC1): a positive tandem-fluorophore construct (D-01) and a matched/segregated distinct-compartment construct (D-02), each human-verified to resolve, carry an unambiguous biological label, and hold an open license (D-03), then archive them in the manifest as `physical-primary` / `sealed_holdout` (D-09) with provenance + a recorded SHA-256 content hash.

Purpose: This is the ONE non-autonomous, non-circular deliverable of the phase. Anchor accessions are UNVERIFIED in research (Assumptions A1-A4, confidence LOW-MEDIUM), so each is gated behind a `checkpoint:human-verify` — the machinery in 08-01..08-04 was built and tested OFFLINE without any verified accession and is NOT blocked on this plan.
Output: `corpus/anchor_rows.jl`, the finalized `corpus/manifest.csv`, and `corpus/test/test_anchors.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.1.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-external-physical-ground-truth-corpus/08-RESEARCH.md
@.planning/phases/08-external-physical-ground-truth-corpus/08-CONTEXT.md

<interfaces>
<!-- From prior plans. -->
From corpus/manifest.jl (08-03): validate_anchor_row, build_manifest, validate_manifest, physical_anchors, open_sealed_holdout, cbs_benchmark.
From corpus/fetch.jl (08-02): fetch_verified(url, dest, expected_sha256) (bootstrap mode: expected=nothing computes+records the trusted baseline hash — RESEARCH L197).
From corpus/config.jl (08-01): CORPUS_DATA_DIR, TIERS, SPLITS.
D-03 acceptance predicate (RESEARCH §Acceptance Predicate): (1) biological truth label from the source publication, (2) open/redistributable license, (3) retrievable+convertible two-channel data (preferred), (4) provenance pinned (DOI/IDR/S-BIAD/Zenodo).
Anchor candidates (RESEARCH §Candidate Datasets, ALL [ASSUMED], must be human-verified): positive = tandem fluorescent-protein construct (avoid environment-quenched mCherry-GFP-LC3, Pitfall 2) with TetraSpeck beads as a fallback/second positive; negative = matched dual-compartment FP pair (nuclear vs membrane/mito), else BBBC two-channel set.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify the POSITIVE physical anchor accession (D-01, D-03)</name>
  <files>(no files — human verification checkpoint)</files>
  <action>Human confirms the POSITIVE tandem-fluorophore anchor per D-01/D-03: select a candidate, verify it resolves, carries an unambiguous biological 100%-coloc label (not a computed score), is not environment-quenched (Pitfall 2), and has an open license; return accession/DOI + URL + license + citation + channel layout. No bytes/hash are pinned until Task 3.</action>
  <read_first>
    - 08-RESEARCH.md (§Candidate Datasets → Positive; §Acceptance Predicate; Pitfall 2 environment-quenched tandem)
    - 08-CONTEXT.md (D-01)
  </read_first>
  <what-built>
    The corpus machinery (manifest schema, tier/split guards, fetch+hash, conversion) is complete and tested offline. This checkpoint selects and confirms the concrete POSITIVE anchor dataset before any URL/hash is pinned. No accession is auto-selected — research confidence is LOW-MEDIUM (Assumption A1).
  </what-built>
  <how-to-verify>
    1. Choose a candidate positive anchor: a named published tandem-fluorophore construct (single polypeptide, two spectrally-distinct fluorophores → 100% colocalization by construction) — e.g. a cytosolic tandem EGFP-mCherry/dTomato fusion, OR TetraSpeck multicolor beads as a confirmatory second positive. Search IDR (idr.openmicroscopy.org), BioImage Archive (ebi.ac.uk/bioimage-archive), or Zenodo per the RESEARCH search paths.
    2. Confirm ALL of the D-03 predicate: (a) the source publication states 100%-colocalization from construct biology (NOT from a coloc measurement); (b) it is NOT an environment-quenched tandem (reject mCherry-GFP-LC3-style constructs where GFP quenches in lysosomes — Pitfall 2); (c) an open/redistributable license (CC-BY / CC-BY-NC / CC0); (d) a stable accession/DOI that resolves; (e) preferably raw two-channel data convertible to a MultiChannelImage.
    3. Provide back: the exact accession/DOI, the direct download URL, the license string, the citation, and the channel layout.
  </how-to-verify>
  <resume-signal>Reply with the confirmed positive accession/DOI + URL + license + citation + channel layout, or "use TetraSpeck: <accession>", or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-verify the NEGATIVE (segregated) physical anchor accession (D-02, D-03)</name>
  <files>(no files — human verification checkpoint)</files>
  <action>Human confirms the NEGATIVE segregated distinct-compartment anchor per D-02/D-03 (prefer a matched construct from the SAME study/archive as the positive): verify it resolves, carries a biological segregation label (not a computed score), and has an open license; return accession/DOI + URL + license + citation + channel layout + matched? flag.</action>
  <read_first>
    - 08-RESEARCH.md (§Candidate Datasets → Negative; §Acceptance Predicate)
    - 08-CONTEXT.md (D-02: prefer a matched segregated construct from the SAME study/archive as the positive)
  </read_first>
  <what-built>
    Confirms the concrete NEGATIVE anchor. Prefer a matched segregated construct from the SAME study/archive as the positive (controls imaging/instrument confounds, D-02); otherwise a separate named dataset (Assumption A3).
  </what-built>
  <how-to-verify>
    1. Choose a distinct-compartment two-fluorophore construct: two markers targeted to non-overlapping compartments (nuclear-vs-membrane, e.g. H2B-GFP + Lyn-mCherry, or mito-vs-nucleus). PREFER one from the same study/archive as the confirmed positive.
    2. Confirm the D-03 predicate: (a) segregation truth stated from targeting biology (NOT a computed score); (b) open license; (c) resolving accession/DOI; (d) preferably raw two-channel data. If no matched-study negative exists, a BBBC two-channel set (nucleus vs cytoskeleton) with a confirmed license is acceptable.
    3. Provide back: the exact accession/DOI, direct URL, license, citation, channel layout, and whether it is matched to the positive's study.
  </how-to-verify>
  <resume-signal>Reply with the confirmed negative accession/DOI + URL + license + citation + channel layout (+ matched? yes/no), or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record confirmed anchors + bootstrap SHA-256 + finalize manifest.csv (SC1, D-01, D-02, D-09)</name>
  <files>corpus/anchor_rows.jl, corpus/manifest.csv, corpus/test/test_anchors.jl</files>
  <read_first>
    - corpus/manifest.jl (validate_anchor_row, build_manifest, validate_manifest, open_sealed_holdout, cbs_benchmark)
    - corpus/fetch.jl (fetch_verified bootstrap mode)
    - corpus/cbs.jl (cbs_rows + assign_cbs_split — to re-assemble the full manifest)
    - corpus/manifest.csv (08-04 committed placeholders being finalized)
  </read_first>
  <action>
    Create `corpus/anchor_rows.jl` exposing `anchor_rows()` returning the two confirmed physical-primary rows built from the Task 1 / Task 2 human-provided values: `tier="physical-primary"`, `role` = "positive"/"negative", `truth_label` = "coloc"/"segregated", `coloc_ground_truth` = 1.0/0.0 (asserted from biology, NOT a computed score), `accession`/`source_url`/`citation`/`license`/`channels`/`format` = the confirmed values, `split="sealed_holdout"` (D-09), `sha256`="" pending bootstrap. Assert every row passes `validate_anchor_row` before proceeding (D-03 gate). Then run `fetch_verified(source_url, joinpath(CORPUS_DATA_DIR, anchor_id...), nothing)` in bootstrap mode for each anchor to record the trusted baseline `sha256` + `bytes`; if offline the fetch returns `:skipped` — in that case record `sha256="PENDING-FETCH"` and print a clear notice that the hash must be bootstrapped when online before Phase 16 (do NOT fabricate a hash). Re-assemble the FULL committed `corpus/manifest.csv` = `cbs_rows()` (with `assign_cbs_split`) + `anchor_rows()`, run `validate_manifest`, and `CSV.write` it with the pre-registration comment header (replacing the 08-04 placeholder anchor rows with the finalized ones). Create `corpus/test/test_anchors.jl` asserting: `anchor_rows()` yields exactly one positive + one negative, both tier=physical-primary and split=sealed_holdout; both pass `validate_anchor_row`; `physical_anchors`/default accessor never yields them except via `open_sealed_holdout(;reason)`; the finalized `manifest.csv` validates and its physical rows have a non-empty accession, license, and citation (and a recorded sha256 OR the explicit "PENDING-FETCH" sentinel when offline). Wire `include("test_anchors.jl")` into runtests.jl. Assert `git ls-files corpus/data` remains empty (no bytes staged).
  </action>
  <verify>
    <automated>julia --project=. corpus/test/runtests.jl && test -z "$(git ls-files corpus/data)" && echo "anchors finalized, no bytes staged"</automated>
  </verify>
  <acceptance_criteria>
    - `anchor_rows()` returns one positive + one negative, both tier=="physical-primary", split=="sealed_holdout"; both pass `validate_anchor_row`.
    - The finalized `corpus/manifest.csv` validates and its two physical rows carry a non-empty accession, license, citation, and either a real 64-char SHA-256 or the explicit "PENDING-FETCH" sentinel (never a fabricated hash).
    - The default corpus accessor does NOT return the anchors; `open_sealed_holdout(;reason="phase16")` does (D-09).
    - `git ls-files corpus/data` empty (D-04).
  </acceptance_criteria>
  <done>Both physical anchors archived with provenance + content hash (or explicit pending-fetch sentinel), sealed as holdout; manifest.csv finalized; src/ untouched, no bytes staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate dataset → pinned anchor | A wrong/circular/unlicensed dataset must not become the anchor (human-verify gate) |
| physical anchor → model-dev code | Sealed until Phase 16 (D-09) |
| downloaded anchor bytes → git | Bytes stay out of git (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Spoofing | Circular/wrong positive anchor (CBS or coloc-score-selected) | mitigate | checkpoint:human-verify confirms biological (not computed) truth label; validate_anchor_row rejects computed-score rows (Pitfall 1) |
| T-08-15 | Spoofing | Environment-quenched tandem mis-pinned as 100%-coloc | mitigate | Task 1 human-verify explicitly rejects mCherry-GFP-LC3-style quenched constructs (Pitfall 2) |
| T-08-16 | Tampering | Fabricated/unverified content hash recorded | mitigate | sha256 recorded ONLY from a real bootstrap fetch; offline records explicit "PENDING-FETCH" sentinel, never a fabricated digest (D-04/D-05) |
| T-08-07 | Elevation of Privilege | Sealed anchor readable pre-Phase-16 | mitigate | Anchors split=sealed_holdout; only open_sealed_holdout reaches them; test asserts default accessor excludes them (D-09) |
| T-08-17 | Repudiation | Missing provenance for a validation anchor | mitigate | accession + citation + license mandatory in every physical row; validate_anchor_row enforces |
| T-08-SC | Tampering | Supply chain | accept | No new deps |
</threat_model>

<verification>
- `julia --project=. corpus/test/runtests.jl` exits 0 (anchor testset green).
- Finalized `corpus/manifest.csv` validates; physical rows carry provenance + hash/sentinel.
- `git ls-files corpus/data` empty; `git status src/` shows no `src/` modification.
</verification>

<success_criteria>
- SC1: >=1 physical positive + >=1 segregated anchor archived with provenance + content hash, both human-verified non-circular and sealed as holdout (D-09).
- All nine decisions D-01..D-09 now realized across the phase.
</success_criteria>

<output>
Create `.planning/phases/08-external-physical-ground-truth-corpus/08-05-SUMMARY.md` when done
</output>

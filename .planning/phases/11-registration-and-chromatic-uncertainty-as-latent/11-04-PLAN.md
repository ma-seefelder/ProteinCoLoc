---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - spike/npe/p11_architecture.jl
  - spike/test/test_p11_architecture.jl
  - spike/test/test_p11_smoke.jl
  - spike/test/test_lambda_ablation.jl
  - spike/test/runtests.jl
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "(D-03) lambda is encoded by a fixed data-independent affine map and appended as a 129th input row AFTER the frozen 128-row standardization, never before"
    - "(D-02) The research net uses the F2 BoundedThetaTransform ported from src, so the rho_true attenuation measurement is not confounded by a plain z-score leaking mass outside the prior box"
    - "(D-04) The port is recorded as an explicitly named deviation, matching the four entries pre-registered in p11_consts.jl"
    - "(D-01) A d_in = 129, D = 8 estimator trains end to end on 200 synthetic samples before any 1.2 hour datagen is spent"
    - "(D-03) A behavioural lambda-ablation tripwire exists and blocks the SC2 ladder if the conditioning input is dead"
    - "(D-01) Phase 11 adds no dependency to the spike environment"
  artifacts:
    - path: "spike/npe/p11_architecture.jl"
      provides: "Ported BoundedThetaTransform plus encode_lambda / augment_input for the 129-row research input"
      contains: "BoundedThetaTransform"
      min_lines: 90
    - path: "spike/test/test_p11_architecture.jl"
      provides: "Agreement with the src transform, the standardize-then-vcat order invariant, and the 129-row shape"
    - path: "spike/test/test_p11_smoke.jl"
      provides: "SC1f — d_in = 129, D = 8 trains on 200 synthetic samples without error"
      contains: "129"
    - path: "spike/test/test_lambda_ablation.jl"
      provides: "SC1g — the R4 tripwire asserting a material posterior-SD inequality between lambda = 0.25 and lambda = 3.0"
      contains: "LAMBDA_MAX"
  key_links:
    - from: "spike/npe/p11_architecture.jl"
      to: "src/amortized/architecture.jl"
      via: "attributed port of the F2 bounded logit theta transform"
      pattern: "architecture\\.jl"
    - from: "spike/npe/p11_architecture.jl"
      to: "spike/npe/architecture.jl"
      via: "build_estimator called with the positional D argument"
      pattern: "build_estimator"
---

<objective>
Build the research net's scaffold and retire its two cheapest-to-retire risks before any expensive
compute is spent: port the F2 `BoundedThetaTransform` into the spike (ruling Q4), define the
lambda conditioning encoder, prove a `d_in = 129, D = 8` estimator actually trains, and author the
lambda-ablation tripwire that blocks the SC2 ladder.

Purpose: RESEARCH risk R10 (the NeuralEstimators conditioning-input API cannot express D-03) costs
minutes to retire here and 1.2 hours to discover after datagen. Risk R4 (lambda drawn
independently of the shift) produces a null SC2 that looks exactly like "below resolution" but is
an implementation bug — the tripwire is the only thing that tells those apart.

Output: `spike/npe/p11_architecture.jl` and three spike test files.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PATTERNS.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-02-SUMMARY.md

**Binding orchestrator ruling implemented here — Q4.** PORT the `BoundedThetaTransform` (the F2
remedy) into the spike trainer. `spike/npe/train_npe.jl:80` still uses a plain `ZScoreTransform`;
the bounded/logit transform exists only at `src/amortized/architecture.jl:88-104`. D-02 mandates
that `rho_true` attenuation be MEASURED against the frozen shipped net. Without the port, that
measurement confounds "widened nuisance prior attenuates rho_true" with "plain z-score leaks about
2 percent of posterior mass outside the prior box" — a confound sitting directly on the one
quantity D-02 requires. The port must be (a) implemented, (b) unit-tested for agreement with the
`src` transform, and (c) recorded as an explicitly NAMED deviation in `p11_consts.jl`'s
`P11_RESEARCH_NET_DEVIATIONS` (already pre-registered in plan 11-01) and later in the report.
Naming the model change in advance is the D-04 discipline; an unnamed model change is how
credibility gets spent (the D-13 precedent).

**Binding orchestrator ruling implemented here — Q3.** There is **ONE** conditioning input:
lambda, the shift prior's half-width. `chromatic_eps` stays a fixed marginalized nuisance at
`Uniform(-0.02, 0.02)` per D-09's literal prior. **Consequence this plan must carry forward:**
SC2's monotone-widening claim is about **REGISTRATION ONLY**; the chromatic term's dose-response
appears only in the SC3/D-14 fixed-injection curve. Do not add a second conditioning row.

**The standardize-then-vcat order is an invariant, not a preference (Pitfall 2).**
`Loader._row_partition` errors on any row count other than 128 for `:min`
(`spike/data/loader.jl:126`); the `src` twin errors on an odd count. Both are *good* — they make
"append lambda then standardize" fail loudly. Standardize the 128 rows with the frozen `zt`
**first**, then `vcat` the lambda row encoded by a fixed, data-independent affine map. Never
"fix" the partition to accept 129 rows.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

`src/amortized/architecture.jl:88-104` — the struct being ported (copy the whole rationale comment
block at :43-104 with it; it IS the rationale):
```julia
struct BoundedThetaTransform{T<:Real,Z}
    lo::Vector{T}
    hi::Vector{T}
    zt::Z
end
# forward :  u = (θ − lo)/(hi − lo) ∈ [0,1]  →  y = logit(clamp(u, ε, 1−ε))  →  z-score
# inverse :  un-z-score  →  θ = lo + (hi − lo)·logistic(y)                    ∈ [lo, hi]
# BOUNDS PROVENANCE: lo/hi come from theta_prior_bounds(); no bound is ever hardcoded here.
```
Its two `StatsBase` methods are `fit`-side construction and `StatsBase.reconstruct(t, Y)`
(`architecture.jl:157-159`). `THETA_LOGIT_EPS` is the clamp constant — **a different epsilon from
D-09's `chromatic_eps`**; never write a bare epsilon identifier in the new file.

`spike/npe/architecture.jl:60-106` — the estimator builder, already input-width-agnostic:
```julia
build_estimator(d_in, D = NPE_D; dstar = 64, depth = 3, width = 256,
                num_coupling_layers = 10, flow_depth = 2, flow_width = 128)
# guard: dstar >= D   (64 >= 8 holds)
# summary net: Chain(Dense(d_in, width, gelu), ...)
```
The positional `D` already exists, so `build_estimator(129, 8; ...)` needs **no signature change**.

`spike/npe/train_npe.jl` — `NPE_MASTER_SEED = 0xC0FFEE` (:65), `fit_theta_transform` (:80, the
plain z-score being replaced), the hard CPU-only guard `use_gpu && throw(...)` (:114), the
training recipe at :132-137 (lr 2.5e-4, batch 128, 300 epochs, patience 40), and `save_npe`
(:160-180, the atomic write idiom).

The D-03 encoder (RESEARCH §D12, spike-local, never crosses into `src/`):
```julia
encode_lambda(lam) = (lam - LAMBDA_MIN) / (LAMBDA_MAX - LAMBDA_MIN)
augment_input(Z128, lam) = vcat(Z128, fill(Float32(encode_lambda(lam)), 1, size(Z128, 2)))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the BoundedThetaTransform and define the lambda conditioning encoder</name>
  <files>spike/npe/p11_architecture.jl, spike/test/test_p11_architecture.jl</files>
  <read_first>
    - `11-RESEARCH.md` §D12 lines 932-1006 — the trainer's current control flow, the three required
      deltas, why appending lambda as a 129th row is the right D-03 mechanism, and the
      standardize-then-vcat trap.
    - `11-RESEARCH.md` Open Question 4 lines 2270-2279 — the F2 confound argument that ruling Q4
      resolves.
    - `11-PATTERNS.md` §11 — the port's analog, the atom-handling caveat (`chromatic_eps` has no
      atoms, so do not copy the atom prose onto it), and the epsilon name-collision warning.
    - `src/amortized/architecture.jl` lines 43-160 — the full rationale comment block, the struct,
      the fit-side construction and `StatsBase.reconstruct`.
    - `spike/npe/architecture.jl` lines 55-110 — `build_estimator`, `NPE_D`, the `dstar >= D` guard.
    - `spike/data/loader.jl` lines 120-140 — `_row_partition`'s hard 128-row requirement.
    - `spike/npe/infer.jl` lines 60-70 — `standardize_summary`'s delegation to the partition.
    - `spike/validation/p11_consts.jl` — `LAMBDA_MIN`, `LAMBDA_MAX`, `P11_RESEARCH_NET_DEVIATIONS`.
  </read_first>
  <action>
Create `spike/npe/p11_architecture.jl`: AGPL block header (spike directory style),
`# spike/npe/p11_architecture.jl --- Phase-11 research-net model surface (D-02, D-03, ruling Q4).`,
then an ALL-CAPS `DECLARED DEVIATIONS` paragraph enumerating **exactly** the four entries in
`P11_RESEARCH_NET_DEVIATIONS` and stating that this file implements two of them (the ported
transform and the 129th lambda row), that the other two live in `spike/simulator/prior.jl` and
`spike/data/p11_generate.jl`, and that this research net is therefore **not drop-in comparable to
the shipped read surface**. Use the reworded S5 decoupling line: spike-local; reaches `src/` only
read-only. Guarded includes of `spike/validation/p11_consts.jl` and `spike/npe/architecture.jl`.

Contents:

1. **The ported `BoundedThetaTransform`.** Copy the struct and its two `StatsBase` methods from
   `src/amortized/architecture.jl:88-104` and `:157-159`, **together with the whole
   `BOUNDED θ-SPACE (07-CALIBRATION-FINDINGS F2)` rationale comment block at :43-104** — that
   block is the reason, and a port without it reads as unexplained. Add an attribution header
   naming `src/amortized/architecture.jl:43-104,157-159` as the source, the reason it is copied
   rather than depended on (the spike environment does not load the `ProteinCoLoc` module), and
   the ruling that authorised it (Q4). Name the port `P11BoundedThetaTransform` so it can never be
   confused with the `src` type in a stack trace, and expose
   `fit_p11_theta_transform(theta_train; bounds)` whose `bounds` default is the **8-entry**
   `theta_prior_bounds()`-equivalent built from the spike's own prior objects
   (`spike/simulator/prior.jl`) — derived, never a literal box. Delete the atom-handling prose
   from the copied comment where it refers to `ghat`'s clamped endpoints applying to a column, and
   add one sentence noting that the `chromatic_eps` column has **no atoms**, so the clamp there is
   purely a numerical guard.
2. **The lambda encoder.** `encode_lambda(lam) = (lam - LAMBDA_MIN) / (LAMBDA_MAX - LAMBDA_MIN)`
   with a comment that it is a fixed, invertible, **data-independent** affine map — not a fitted
   transform — so there is no leak and no frozen statistic to persist beyond the two constants.
   Add `decode_lambda` for symmetry and for the figures.
3. **`augment_input(Z128, lam)`** = `vcat(Z128, fill(Float32(encode_lambda(lam)), 1, size(Z128, 2)))`,
   with an ALL-CAPS `ORDER INVARIANT` comment stating that `Z128` must already be the OUTPUT of
   the frozen path `standardize_summary(encode_d01(M), zt, :min)`, and that appending before
   standardizing either errors loudly in `Loader._row_partition` or, if someone "fixes" the
   partition, silently z-scores the conditioning variable against the training pool's lambda
   distribution (Pitfall 2). Add an `@assert size(Z128, 1) == 128` inside the function so the
   invariant is enforced at runtime, not just documented.
4. **`build_p11_estimator(; d_in = 129, D = 8, kwargs...)`** — a thin wrapper calling
   `build_estimator(d_in, D; dstar = 64, depth = 3, width = 256, num_coupling_layers = 10,
   flow_depth = 2, flow_width = 128)`, i.e. the unchanged Phase-5-calibrated capacity so the
   comparison to the shipped net is capacity-controlled. Comment that the positional `D` already
   exists in `spike/npe/architecture.jl:87`, so no signature change is required, and that
   `dstar >= D` holds at 64 >= 8.

Create `spike/test/test_p11_architecture.jl` with these testsets:
- `"ported transform agrees with src"` — load `src/amortized/architecture.jl` read-only into an
  isolated `module SrcArch` (the `module GateV2` idiom) if it loads standalone; construct both
  transforms on the same 8-row synthetic theta matrix and the same bounds and assert the forward
  and `reconstruct` round-trips agree to within `1e-12`. If the `src` file cannot be loaded
  standalone from `--project=spike`, assert the round-trip property directly instead —
  `reconstruct(fit(...), transform(...)) approx theta` and every reconstructed value strictly
  inside `[lo, hi]` — and mark the cross-check `@test_skip` with an in-file comment naming the
  reason. Do not add a dependency to make it load.
- `"bounded transform keeps mass inside the prior box"` — for extreme standardized inputs
  (`50.0 .* randn(8, 200)`), every reconstructed theta lies strictly within `[lo, hi]` per column.
  This is the F2 property the port exists to buy; assert it, do not assume it.
- `"encode_lambda"` — `encode_lambda(LAMBDA_MIN) == 0.0`, `encode_lambda(LAMBDA_MAX) == 1.0`,
  strictly increasing on the SC2 rung ladder, and `decode_lambda(encode_lambda(x)) approx x`.
- `"augment_input ORDER invariant"` — `size(augment_input(zeros(128, 5), 1.0)) == (129, 5)`; the
  129th row is constant across columns and equals `Float32(encode_lambda(1.0))`; and
  `augment_input(zeros(129, 5), 1.0)` **throws** (the `@assert` guard fires), so appending twice
  or appending pre-standardization is caught.
- `"build_p11_estimator constructs at d_in = 129, D = 8"` — the estimator constructs without
  error and no `CUDA` module is loaded.
- Close with the CUDA-absence clause.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p11_architecture.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p11_architecture.jl` exits 0 with zero failures.
    - `grep -c 'src/amortized/architecture.jl' spike/npe/p11_architecture.jl` is at least `1` (attribution present).
    - `grep -c 'P11BoundedThetaTransform' spike/npe/p11_architecture.jl` is at least `3`.
    - `julia --project=spike -e 'include("spike/npe/p11_architecture.jl"); @assert size(augment_input(zeros(128,4), 1.0)) == (129,4); ok = try; augment_input(zeros(129,4), 1.0); false; catch; true; end; @assert ok; @assert encode_lambda(LAMBDA_MIN) == 0.0 && encode_lambda(LAMBDA_MAX) == 1.0; println("ok")'` prints `ok`.
    - `grep -cE '(^|[^_[:alnum:]])(eps|ε)([^_[:alnum:]]|$)' spike/npe/p11_architecture.jl` shows no bare epsilon identifier introduced by this plan (only `THETA_LOGIT_EPS`-style named constants and `chromatic_eps` are acceptable).
    - `git diff --stat -- spike/Project.toml spike/Manifest.toml` is empty.
  </acceptance_criteria>
  <done>The F2 bounded theta transform is available in the spike with its rationale and attribution, the lambda encoder and the 129-row augmenter enforce the standardize-then-vcat order at runtime, and a `d_in = 129, D = 8` estimator constructs.</done>
</task>

<task type="auto">
  <name>Task 2: SC1f smoke — prove a d_in = 129, D = 8 estimator trains before spending 1.2 hours on datagen</name>
  <files>spike/test/test_p11_smoke.jl, spike/test/runtests.jl</files>
  <read_first>
    - `11-RESEARCH.md` risk R10 lines 1853-1861 — the exact risk this retires and why the smoke
      must precede datagen.
    - `11-RESEARCH.md` §D12 lines 982-1006 — the evidence that `build_estimator` is width-agnostic
      and that `train(estimator, theta_train, theta_val, Z_train, Z_val; ...)` is generic in the
      data type.
    - `11-PATTERNS.md` §4 — the smoke analog (`spike/test/runtests.jl:43-58`), the CUDA-absence
      clause, and the instruction that Phase 11 adds no dependency, so `runtests.jl` gains a
      **clause (i)** asserting exactly that.
    - `spike/test/runtests.jl` lines 40-115 — the existing resolve-risk gate and clauses (d)-(h).
    - `spike/npe/train_npe.jl` lines 100-150 — the training call shape, the `use_gpu = false`
      requirement on every NeuralEstimators call, and the AdamW Float64 gotcha at :129-131.
    - `spike/npe/p11_architecture.jl` — the file written in Task 1.
  </read_first>
  <action>
Create `spike/test/test_p11_smoke.jl`: AGPL header,
`# spike/test/test_p11_smoke.jl --- SC1f: d_in = 129, D = 8 trains (retires R10).`, an ALL-CAPS
paragraph stating that this file MUST pass before the roughly 1.2 hour datagen in plan 11-07 is
launched, then the literal run line. Guarded includes of `p11_consts.jl` and `p11_architecture.jl`.

Body: generate 200 synthetic training samples and 50 validation samples entirely in-memory — no
simulator call, no cache — with `Z = randn(rng, Float32, 128, n)` standardized-looking rows,
`lam` drawn from `Uniform(LAMBDA_MIN, LAMBDA_MAX)` per sample, and `theta` an 8-row matrix whose
columns lie inside the spike prior box. Build the 129-row input **per sample** via
`augment_input` so the lambda row genuinely varies across columns (a single scalar lambda for the
whole matrix would not exercise the conditioning path); use `hcat` of per-sample augmented columns.
Fit the ported theta transform on the training theta only (leak-free), apply it to both splits,
build the estimator with `build_p11_estimator()`, and call `train` with a **small** budget
(`epochs = 3`, `batchsize = 32`, `use_gpu = false`, `verbose = false`) — the point is that the
shapes and the API work, not that it converges.

Assertions:
- the training call returns without error;
- `size(Z_train, 1) == 129`;
- `sampleposterior` (or the spike's `posterior_for` equivalent) on one augmented column returns a
  draw matrix with **8** rows;
- reading a `rho_true` draw back through the ported transform yields values inside the prior box;
- feeding a **128-row** input to the trained estimator throws (the dimension contract is real);
- the CUDA-absence clause.

Then extend `spike/test/runtests.jl` with **clause (i)** in the existing resolve-risk gate,
following the shape of clauses (d)-(h): assert that Phase 11 added **no** new dependency — the
`spike/Project.toml` `[deps]` name set is unchanged from the pre-Phase-11 set, and the
NeuralEstimators v0.2.1 UUID pin is re-asserted. Write the assertion in terms the existing clauses
already use (`Pkg.project().dependencies` / `Pkg.dependencies()`), and give it the same comment
style naming the decision it defends (D-01, "no new dependency"). Do **not** wire the new Phase-11
test files into `spike/test/runtests.jl` — they are run directly, per the validation contract, and
several of them require a trained net that does not exist yet.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p11_smoke.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p11_smoke.jl` exits 0 with zero failures, in under 5 minutes.
    - `grep -c '129' spike/test/test_p11_smoke.jl` is at least `2`.
    - `grep -c 'use_gpu *= *false' spike/test/test_p11_smoke.jl` is at least `1`.
    - `julia --project=spike spike/test/runtests.jl` exits 0 (the existing spike suite still passes with clause (i) added).
    - `grep -c 'D-01' spike/test/runtests.jl` is at least `1` (clause (i) names the decision it defends).
    - `git diff --stat -- spike/Project.toml spike/Manifest.toml` is empty.
    - `grep -c 'test_p11_smoke\|test_lambda_ablation\|test_stage6_regression' spike/test/runtests.jl` outputs `0` (Phase-11 files are not wired into the aggregate runner).
  </acceptance_criteria>
  <done>Risk R10 is retired: a 129-input, 8-marginal estimator provably trains and samples through the real NeuralEstimators v0.2.1 API, on CPU, with no new dependency — before any expensive datagen is authorised.</done>
</task>

<task type="auto">
  <name>Task 3: Author the lambda-ablation tripwire that blocks the SC2 ladder</name>
  <files>spike/test/test_lambda_ablation.jl</files>
  <read_first>
    - `11-RESEARCH.md` risk R4 lines 1792-1799 — the failure mode, the early warning, and the
      instruction to run this immediately after training and before the ladder.
    - `11-RESEARCH.md` §D13 lines 1036-1043 — why lambda must be drawn FIRST and the shift
      conditional on it, and why an independent draw produces a null SC2 indistinguishable from
      "below resolution".
    - `11-RESEARCH.md` Pitfall 1 lines 2062-2068.
    - `11-VALIDATION.md` row SC1g — "behavioural — BLOCKS the ladder".
    - `11-PATTERNS.md` §4 — the naive-pattern-match trap: this is not a shape test; assert a
      **material inequality** against a pre-registered factor, not `!=`.
    - `spike/test/test_sbc.jl:63-68` — the both-directions discipline this file's spirit copies.
    - `spike/npe/infer.jl` lines 118-155 — `rho_draws`, `delta_rho` and `interval_width`, the
      quantities the tripwire reads.
    - `spike/validation/p11_consts.jl` — `LAMBDA_MIN`, `LAMBDA_MAX`, `P11_FIXTURE_COUNTER`.
  </read_first>
  <action>
Create `spike/test/test_lambda_ablation.jl`: AGPL header,
`# spike/test/test_lambda_ablation.jl --- SC1g: the R4 lambda-conditioning tripwire. BLOCKS Wave 4.`,
then an ALL-CAPS paragraph stating in plain terms what a failure means: **the lambda conditioning
is dead**, the 129th input row carries no information, and any subsequent SC2 result would be a
null that looks exactly like "below resolution" but is an implementation bug. State that this file
must run immediately after training (plan 11-07) and **before** any ladder run (plan 11-08).
Include the literal run line.

Guarded includes of `p11_consts.jl`, `p11_architecture.jl`, and the spike inference surface
(`spike/npe/infer.jl`). The trained research net path is a file-local
`const P11_NET_PATH = joinpath(@__DIR__, "..", "npe", "p11_research_npe.jld2")`.

**Graceful absence, not a false pass.** If `P11_NET_PATH` does not exist, the file must print a
clear message and **exit non-zero** with an explicit "research net not trained yet — this tripwire
cannot pass vacuously" message. A skipped tripwire that reports success is exactly the failure
mode this file exists to prevent, so do not use `@test_skip` here.

Body, once the net exists:
- Pin the global RNG with a deterministic `Random.seed!` derived from `P11_DEV_SEED` — the
  `test/gate/harness.jl:45-96` `seed_gate_global!` discipline, because `sampleposterior` threads no
  `rng` and draws the flow's base samples from the global RNG. Without this the tripwire is not
  reproducible.
- Load the net; simulate **one** dataset at a fixed theta and a fixed key, compute `Z128` **once**
  through the frozen path, then read the Δρ posterior at `lambda = LAMBDA_MIN` and at
  `lambda = LAMBDA_MAX` from that **same** `Z128` via `augment_input`. The image is never
  re-simulated across the two reads, so the comparison is exactly the controlled contrast D-03
  promises with zero simulation noise between arms.
- Assert a **material inequality**, not inequality-of-floats:
  `sd(Δρ | lambda = LAMBDA_MAX) > P11_LAMBDA_ABLATION_FACTOR * sd(Δρ | lambda = LAMBDA_MIN)`,
  where `P11_LAMBDA_ABLATION_FACTOR` is read from the Tier-2 block of `p11_consts.jl`. Because
  Tier 2 does not exist yet at authoring time, read it with a documented fallback:
  `isdefined(@__MODULE__, :P11_LAMBDA_ABLATION_FACTOR) ? P11_LAMBDA_ABLATION_FACTOR : 1.15`, and
  add an in-file comment stating that 1.15 is a **placeholder that plan 11-05 must supersede** with
  the Tier-2 value derived from the probe's measured effect scale. Repeat the same assertion on the
  90 percent HDI width via `interval_width` so the tripwire is not sensitive to a single spread
  statistic.
- Repeat over at least 3 independent datasets and require the inequality to hold for **all** of
  them, so a single lucky draw cannot pass it.
- Add the opposite-direction clause in the `test_sbc.jl:63-68` spirit: construct a deliberately
  **dead** conditioning by feeding the SAME lambda to both arms and assert the inequality **fails**
  — proving the statistic can fail and is not stuck at pass.
- Print a `rpad`-aligned summary table of the two SDs and the realized ratio per dataset before any
  assertion, so a failure is diagnosable from the log alone.
- Close with the CUDA-absence clause.
  </action>
  <verify>
    <automated>julia --project=spike -e 'p = "spike/test/test_lambda_ablation.jl"; s = read(p, String); @assert occursin("LAMBDA_MAX", s) && occursin("LAMBDA_MIN", s); @assert occursin("augment_input", s); @assert !occursin("@test_skip", s); println("authored ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `authored ok`.
    - `julia --project=spike spike/test/test_lambda_ablation.jl` exits **non-zero** with a message naming the missing research net (the net does not exist until plan 11-07; a vacuous pass here is forbidden).
    - `grep -c '@test_skip' spike/test/test_lambda_ablation.jl` outputs `0`.
    - `grep -c 'Random.seed!' spike/test/test_lambda_ablation.jl` is at least `1` (the global-RNG pin `sampleposterior` requires).
    - `grep -c 'P11_LAMBDA_ABLATION_FACTOR' spike/test/test_lambda_ablation.jl` is at least `2` (Tier-2 read plus the documented placeholder).
    - `grep -c 'interval_width' spike/test/test_lambda_ablation.jl` is at least `1` (the second spread statistic).
    - The file asserts a strict `>` against a factor, not `!=`: `grep -c '!=' spike/test/test_lambda_ablation.jl` outputs `0`.
  </acceptance_criteria>
  <done>The R4 tripwire exists, is reproducible under a pinned global RNG, asserts a material inequality against a pre-registered factor on at least three datasets, proves it can fail, and refuses to pass vacuously when the research net is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conditioning input to the flow | The 129th row is a user-declared uncertainty level; if it is standardized or appended in the wrong order it silently couples to the training pool |
| spike environment dependency set | D-01 requires the research lane to add no dependency; a silent add would break the pinned reproducibility contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-15 | Tampering | `augment_input` called before standardization | mitigate | Runtime `@assert size(Z128, 1) == 128` inside `augment_input`, plus a unit test asserting a 129-row input throws; the frozen `Loader._row_partition` 128-row error is deliberately not "fixed" (Pitfall 2) |
| T-11-16 | Repudiation | An unnamed model change contaminating the D-02 attenuation measurement | mitigate | The port is enumerated in the pre-registered `P11_RESEARCH_NET_DEVIATIONS` and re-stated in the file's DECLARED DEVIATIONS header; the report must list exactly these four (ruling Q4) |
| T-11-17 | Spoofing | A tripwire that passes vacuously when the net is absent | mitigate | `test_lambda_ablation.jl` exits non-zero rather than skipping; `@test_skip` is grep-forbidden in that file |
| T-11-18 | Tampering | Non-reproducible posterior draws | mitigate | Deterministic `Random.seed!` derived from `P11_DEV_SEED` before every `sampleposterior` call, mirroring `seed_gate_global!` (`test/gate/harness.jl:92-96`) |
| T-11-SC | Tampering | package-manager installs | mitigate | No packages installed. `spike/test/runtests.jl` gains clause (i) asserting the dependency name set is unchanged and re-asserting the NeuralEstimators 0.2.1 UUID pin; acceptance criteria assert both manifests are byte-unchanged (ASVS V14) |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p11_architecture.jl` — exit 0.
2. `julia --project=spike spike/test/test_p11_smoke.jl` — exit 0, under 5 minutes.
3. `julia --project=spike spike/test/runtests.jl` — exit 0 with the new clause (i).
4. `julia --project=spike spike/test/test_lambda_ablation.jl` — exits non-zero with the
   "research net not trained yet" message (expected until plan 11-07).
5. `git diff --stat -- spike/Project.toml spike/Manifest.toml src/` is empty.
</verification>

<success_criteria>
- `spike/npe/p11_architecture.jl` provides `P11BoundedThetaTransform` (attributed to
  `src/amortized/architecture.jl`), `encode_lambda`/`decode_lambda`, an order-enforcing
  `augment_input`, and `build_p11_estimator`.
- A `d_in = 129, D = 8` estimator trains and samples on 200 synthetic samples, CPU-only.
- The lambda-ablation tripwire is authored, refuses to pass vacuously, and asserts a material
  inequality against a Tier-2 factor with a documented placeholder.
- `spike/test/runtests.jl` clause (i) asserts Phase 11 added no dependency.
- No `src/` file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-04-SUMMARY.md`
when done, recording the smoke-train wall time and the placeholder value used for
`P11_LAMBDA_ABLATION_FACTOR`.
</output>

---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 11
status: superseded
superseded_by: 11-DIAGNOSIS.md
superseded_on: 2026-07-27
superseded_reason: "Its deliverable was a results report plus a D-16 interpretation note. 11-DIAGNOSIS.md IS that report, on stronger evidence than the planned ladder would have produced. No 11-REPORT.md exists or is owed."
type: execute
wave: 9
depends_on: ["11-09", "11-10"]
files_modified:
  - spike/validation/p11_figures.jl
  - .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md
  - .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md
  - docs/amortized.md
autonomous: true
requirements: [D-01, D-05, D-12, D-13, D-14, D-16, D-17]

must_haves:
  truths:
    - "(D-16) Phase 11 ships a results report under .planning/ plus a short interpretation section in docs/amortized.md, with NO public API change and NO artifact change"
    - "(D-16) Every number in both documents is attributed to the net it came from — the shipped amended_v2/grid_8 bundle or the Phase-11 research net"
    - "(D-13) The SC2/SC3 structural tension is stated in plain language, not buried"
    - "(D-05) Every shift and chromatic table carries the vacuous-column caption and per-rung prior_sd"
    - "(D-14) The breakdown point is presented as reported-not-gated, with its Wilson evidence"
    - "(D-17) The corpus-to-test-images substitution is recorded with its reason, and the manuscript pipelines are stated unmodified with the five verification command outputs printed"
    - "(D-12) The pinned pre-epsilon sha is quoted in the report"
    - "(D-01) No shipped artifact changed, no gate reopened, no reship"
  artifacts:
    - path: ".planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md"
      provides: "The Phase-11 results report: probe, SC2 ladder, SC3 breakdown, attenuation, real-image transfer, the tension, the vacuous-column caption, the decoupling claim and the pinned sha"
      contains: "SC2/SC3 tension"
      min_lines: 150
    - path: "docs/amortized.md"
      provides: "The D-16 interpretation section on reading posterior width under registration uncertainty, inserted before the Named limits section"
      contains: "registration uncertainty"
    - path: "spike/validation/p11_figures.jl"
      provides: "Probe curves, the SC2 ladder, the breakdown curve and the real-image width response"
      contains: "figures"
  key_links:
    - from: ".planning/phases/11-.../11-REPORT.md"
      to: "docs/amortized.md"
      via: "the D-16 section and named limit 8 cross-reference the report path"
      pattern: "11-REPORT.md"
    - from: "docs/amortized.md"
      to: "spike/npe/p11_research_npe.jld2"
      via: "explicit statement that the research net is not the shipped one and is not downloadable"
      pattern: "research net"
---

> **SUPERSEDED — DO NOT EXECUTE (2026-07-27).** Never run; not live work. Phase 11 is closed.
> The planned deliverable (`11-REPORT.md` + a `docs/amortized.md` D-16 section) was **replaced by
> `11-DIAGNOSIS.md`**, with `11-CLOSURE.md` carrying the disposition and the manuscript honesty
> items and `11-BAR-DERIVATION.md` carrying the corrected SC1g bar specification.
> **`11-REPORT.md` does not exist and is not owed** — do not treat its absence as a missing artifact.
> Note that `docs/amortized.md` was deliberately NOT edited: D-01 holds, no shipped artifact
> changed, no gate reopened, no reship. Retained unaltered as the historical record.

<objective>
Write the Phase-11 deliverable: a results report under `.planning/` and a short interpretation
section in `docs/amortized.md` telling users how to read posterior width under registration
uncertainty and quoting the breakdown point.

Purpose: D-16 fixes the deliverable as **report plus docs note — no API change, no artifact
change**. The wording must be scrupulous about which model each number came from, because the
section describes a **research** net that is not the shipped one.

Output: figures, `11-REPORT.md`, the appended `docs/amortized.md` section, and the completed
provenance record.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PATTERNS.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-05-SUMMARY.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-06-SUMMARY.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-08-SUMMARY.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-09-SUMMARY.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-10-SUMMARY.md

**All eight orchestrator rulings must surface in the report**, because each is a substitution or a
narrowing a reviewer could otherwise catch first:
- **Q1** — D-17 literally named `corpus/`; the substitution to `test/test_images/` was made because
  `corpus/data/` is empty, both anchors carry `sha256 = PENDING-FETCH`, and both are
  `split = sealed_holdout` reserved for the Phase-16 blind evaluation. Never silent.
- **Q2** — `src/amortized/simulator.jl` still declares `SHIFT_PRIOR = Uniform(-1.0, 1.0)`; only the
  spike prior was widened.
- **Q3** — SC2's monotone-widening claim covers **registration only**; the chromatic dose-response
  appears solely in the SC3/D-14 fixed-injection curve.
- **Q4** — the four declared research-net deviations, enumerated.
- **Q5** — the breakdown curve is reported at lambda = 3.0 primary and lambda = 1.0 secondary.
- **Q6** — no Phase-11 REQ-IDs; the phase is traced against SC1/SC2/SC3 and D-01..D-17.
- **Q7** — the F5 image-size mixture was the training and evaluation arm.
- **Q8** — `ROADMAP.md:126`'s literal decoupling claim is unavailable; the narrower claim is adopted
  and the five verification command outputs are printed.

**If plan 11-06 recorded branch `stop-sc2`**, this plan still runs — but the report states SC2 as
**NOT DEMONSTRABLE at the fixed 8x8 summary resolution** with the probe curve as the evidence,
states that SC3's coverage claim was not reachable without a research net, and links the finding
explicitly to the deferred "summary extension for registration identifiability" idea. Do not
silently omit the SC2 section.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Phase-11 figures</name>
  <files>spike/validation/p11_figures.jl</files>
  <read_first>
    - `11-RESEARCH.md` §C9b lines 817-828 — the interpolation-onset annotation the probe figure must
      carry.
    - `11-RESEARCH.md` §E17 lines 1280-1282 — the isotonic (pool-adjacent-violators) fit for the
      ladder figure, reported but not gated.
    - `11-RESEARCH.md` §E18 lines 1321-1335 — the Wilson-limb definition the breakdown figure must
      draw, not just the point estimate.
    - `11-RESEARCH.md` §F21 lines 1582-1598 — the real-image signature and non-signature the figure
      must make visually checkable.
    - `11-PATTERNS.md` — the experiment-script shape, step 10: figures are written after
      persistence and are explicitly labelled "script artifacts — never gate assertions".
    - `spike/validation/figures.jl` — the existing figure module and its CairoMakie headless
      conventions; extend the pattern, do not fork it.
    - `.gitignore` — the existing scoped negation for `spike/figures/plausibility.png`, which is the
      precedent for tracking a figure as an evidence deliverable.
    - The four report artifacts: `p11_probe_report.jld2`, `p11_ladder_report.jld2`,
      `p11_breakdown_report.jld2`, `p11_realimage_report.jld2`.
  </read_first>
  <action>
Create `spike/validation/p11_figures.jl`: AGPL header,
`# spike/validation/p11_figures.jl --- Phase-11 figures. SCRIPT ARTIFACTS, never gate assertions.`,
the literal run line, guarded includes, headless CairoMakie following `spike/validation/figures.jl`'s
existing conventions. Read every number from the four committed `.jld2` artifacts — **never
recompute a reported statistic in the figure code**, so a figure can never disagree with the
artifact it illustrates.

Four figures, written into `spike/figures/`:

1. **`p11_probe.png`** — paired summary displacement against injected magnitude, for the shift arm
   and the chromatic arm, on both imsize arms, with the `drho_eq` axis as the right-hand scale.
   **Annotate the 0 to 0.25 px step explicitly as interpolation onset, not signal**, with a text
   label on the figure itself, not only in the caption — the annotation is the point.
2. **`p11_ladder.png`** — mean 90 percent HDI width against lambda across the SC2 rungs, with
   per-rung dispersion, the isotonic fit overlaid, and a second panel showing per-rung coverage with
   its Wilson intervals, the nominal 0.90 line and the pre-registered ±3 pp equivalence band drawn
   as a shaded region. A reader must be able to see both parts of D-07 in one figure.
3. **`p11_breakdown.png`** — coverage against injected magnitude beyond the training prior, for the
   shift and chromatic ladders, at **both** read levels (lambda = 3.0 and lambda = 1.0) as separate
   series. Draw the **Wilson upper limb**, not only the point estimate, and mark the breakdown point
   where that limb first crosses below 0.90 — or annotate right-censoring where no rung qualifies.
   Shade the in-prior region distinctly from the beyond-prior region, since only the former is a
   gated claim.
4. **`p11_realimage.png`** — Delta-rho width against injected shift for the positive and negative
   conditions, with the null-warp and integer-offset control arms marked, at both read levels, and
   the simulated ladder overlaid on a common `drho_eq` axis so transfer is visually checkable
   against the pre-registered prediction.

Every figure caption must name **which net produced the numbers** (the Phase-11 research net for
the ladder, breakdown and real-image panels; the simulator alone for the probe panel). Add a scoped
`.gitignore` negation for exactly these four paths if a broad figure-ignore rule exists, following
the `spike/figures/plausibility.png` precedent; do not broaden any rule.

Run the script and commit the figures.
  </action>
  <verify>
    <automated>julia --project=spike spike/validation/p11_figures.jl &amp;&amp; ls spike/figures/p11_probe.png spike/figures/p11_ladder.png spike/figures/p11_breakdown.png spike/figures/p11_realimage.png</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and lists all four PNG files.
    - `git ls-files --error-unmatch spike/figures/p11_probe.png spike/figures/p11_ladder.png spike/figures/p11_breakdown.png spike/figures/p11_realimage.png` exits 0.
    - `grep -c 'jldsave\|JLD2.save' spike/validation/p11_figures.jl` outputs `0` (figures read artifacts, they never write one).
    - `grep -c 'interpolation onset' spike/validation/p11_figures.jl` is at least `1`.
    - `grep -c 'wilson\|Wilson' spike/validation/p11_figures.jl` is at least `1` (the breakdown figure draws the limb, not only the point estimate).
    - `grep -c 'research net' spike/validation/p11_figures.jl` is at least `1` (captions attribute the source net).
    - `git diff --stat -- spike/validation/figures.jl` is empty (the Phase-5 figure module is not mutated).
  </acceptance_criteria>
  <done>Four figures exist, each drawn from the committed report artifacts rather than recomputed, each captioned with the net that produced it, with the interpolation-onset annotation, the equivalence band, the Wilson limb and the control arms all visible.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase-11 results report</name>
  <files>.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md, .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md</files>
  <read_first>
    - `11-PATTERNS.md` "Report-artifact shape" — the `gate-8x8-amended.md` skeleton with verbatim
      heading text, how pre-registered thresholds versus measured values are presented (same table
      row, measured first, threshold in a parenthetical), how verdicts are worded (bolded uppercase
      in the heading; reason class distinguished from statistic), how named limits are worded (a
      positive property of the evidence, then its scope, then what would close it), the
      `07-CALIBRATION-FINDINGS.md` epistemic labels (`**MEASURED:**`, `**INFERRED:**`,
      `**NOT CONCLUSIVE —**`, `**What would close it:**`), and the four Phase-11-specific sections
      the analog does not have.
    - `.planning/phases/07-productionization-conditional-on-go/gate-8x8-amended.md` — the analog,
      whole file.
    - `.planning/phases/07-productionization-conditional-on-go/07-CALIBRATION-FINDINGS.md` — the
      epistemic-label voice and the `## What was deliberately NOT done` closing section.
    - `11-RESEARCH.md` §H25 lines 1895-1944 — the SC2/SC3 tension in the document's own words.
    - `11-RESEARCH.md` §E19 lines 1402-1411 — the verbatim vacuous-column caption template.
    - `11-RESEARCH.md` §F20c lines 1512-1554 — the five verification commands and the note that a
      suite going red on theta-arity literals was expected work, not pipeline breakage.
    - `.planning/ROADMAP.md` line 126 — the literal decoupling claim to quote.
    - All five plan SUMMARYs (11-05, 11-06, 11-08, 11-09, 11-10) and
      `.planning/phases/11-.../11-PROVENANCE.md` from plan 11-03.
  </read_first>
  <action>
Write `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md` in the
`gate-8x8-amended.md` skeleton, carrying **both** that memo's verdict-first structure and
`07-CALIBRATION-FINDINGS.md`'s epistemic labels — because Phase 11 mixes gated claims (SC2,
SC3-in-prior) with reported-only ones (breakdown point, attenuation, real-image transfer).

Header block: run dates; the research net (`spike/npe/p11_research_npe.jld2`, `d_in = 129`,
`D = 8`, the pool size and image-size mixture); the pre-registration (`spike/validation/p11_consts.jl`,
Tier-1 commit sha and Tier-2 append sha, stated as unchanged between freezing and each run and
verified by `git diff`); the seed (`P11_DEV_SEED`, asserted disjoint from all prior seeds); and the
protocol (one run per arm, no retry, no re-seed, no threshold change; `P11_ITERATION_ALLOWANCE = 1`
with the ledger showing spent and remaining).

Then, in order:

- **`## Overall verdict: PASS` / `FAIL` / `PARTIAL`** — verdict FIRST, before any interpretation,
  bolded uppercase, with a compact per-criterion summary table (SC1, SC2a, SC2b, SC3-in-prior,
  SC3-beyond-prior, D-02, D-17) and a one-line statement that interpretation is separated below.
- **`## 0. What Phase 11 did and did not change`** — SC1's dx/dy portion was already satisfied
  before this phase; the Phase-11 delta is the `chromatic_eps` term plus a research-lane retrain.
  **No reship, no new artifact, no gate reopened, no public API change** (D-01, D-16). Quote the
  pinned pre-epsilon sha (D-12) and state that `src/amortized/simulator.jl` still declares
  `SHIFT_PRIOR = Uniform(-1.0, 1.0)` (ruling Q2).
- **`## 1. The pre-flight probe (D-06)`** — the measured `S_probe` and ladder span against their
  pre-registered floors in the same table row (measured first, threshold parenthesised), the
  `drho_eq` calibration fit with its R-squared, and the **interpolation-onset annotation**. Record
  the probe verdict and the branch taken from `11-PROBE-VERDICT.md`.
- **`## 2. SC2 — monotone widening and honest coverage`** — the per-rung table (lambda, mean 90
  percent HDI width, mean `post_sd`, coverage `k/N`, Wilson interval, TOST p, Holm-adjusted p),
  then SC2a and SC2b as **two separately stated verdicts** because D-07 is a two-part criterion.
  Include the one-sentence over-power reconciliation: TOST has no over-power pathology, so the
  Phase-7 "M = 2000 was too big" lesson does not cap N here — it is the *reason* D-08 chose TOST.
  Include the intersection-union note that Holm is deliberately conservative rather than necessary.
  - **`### 2a. Scope: this is a REGISTRATION ladder only`** (ruling Q3) — `chromatic_eps` is not
    lambda-scaled and carries no conditioning input, so it contributes a constant marginal spread at
    every rung; its dose-response appears only in §3. A reader must not take SC2 as covering
    chromatic misalignment.
  - **`### 2b. Vacuous columns`** (D-05, F3) — the per-rung shrinkage table for all 9 columns with
    the `vacuous` flag, `prior_sd` computed **within each rung** (`lambda_r / sqrt(3)` for the shift
    columns), under the verbatim §E19 caption: `shift_dx`, `shift_dy` and `chromatic_eps` carry
    per-rung shrinkage at or above 0.95, so the posterior retains essentially all of the prior
    spread; their rank statistics are **vacuous by construction and are not calibration evidence**
    and are tabulated for completeness only; per spike 009 a vacuous column can still legitimately
    reject on a location shift, so a rejection there is equally uninformative; the Phase-11 claim
    rests on `rho_true` and `Delta-rho`, whose shrinkage values are quoted.
- **`## 3. SC3 — coverage, not flagging (D-13)`** — the in-prior result, stated explicitly as
  **satisfied by the §2 SC2b TOST result and not re-run**; then the beyond-prior breakdown curve
  with its Wilson limbs, the four breakdown points (shift and chromatic arms at lambda = 3.0 and
  lambda = 1.0, ruling Q5), each phrased as "coverage holds to below X; degrades at and beyond X" or
  right-censored, and labelled **REPORTED, NOT GATED** (D-14).
- **`## 4. The SC2/SC3 tension`** — in **plain language**, not buried. The shipped OOD flag is a
  summary-density test that fires on *unfamiliarity*; SC2 works by making mis-registration
  *familiar*; so every pixel of misalignment SC2 teaches the net to handle is a pixel the density
  channel stops flagging. The two criteria pull on the same rope in opposite directions, and the
  harder SC2 succeeds the more thoroughly SC3's would-be detector is disabled. D-13 resolves this by
  changing what SC3 asserts — not "flagged" but "covered" — which is coherent because a covered
  interval **is** the operational absence of silent overconfidence. **The accepted cost, stated
  plainly:** badly mis-registered data returns a **wide but unflagged** answer; the user is not told
  *why* the interval is wide and cannot distinguish a genuinely ambiguous protein relationship from
  a 3 px channel misalignment. The failure is honest but silent, and §3's breakdown curve is what
  converts that silence into a quotable operating limit.
- **`## 5. The cost of the wider prior (D-02)`** — RMSE, mean `post_sd` and coverage for the frozen
  shipped net and the research net on one shared evaluation set, plus the attenuation ratio, with
  the confound caveat: the two nets differ in the four declared ways, so this is **not** a clean
  single-factor experiment; the ported `BoundedThetaTransform` removes one of those confounds, so
  the remaining confound is three-way on the quantity measured. Use `**MEASURED:**` and
  `**NOT CONCLUSIVE —**` labels to keep the two apart.
- **`## 6. Real-image transfer (D-17)`** — the width-response curves, the five scored prediction
  items, the overall qualitative verdict, the null-warp and integer-offset controls, and the
  residual-limitation sentence verbatim (on real images the injected misalignment necessarily
  involves one resampling pass that the native acquisition did not have; the integer-offset control
  bounds that contribution; the simulated sweep, where stage 6 owns the only pass, remains the
  primary evidence). **`### 6a. Target substitution (ruling Q1)`** — record that D-17 literally
  named `corpus/`, that the substitution to `test/test_images/` was made, and why: `corpus/data/`
  is empty, both physical anchors carry `sha256 = PENDING-FETCH`, and both are
  `split = sealed_holdout` reserved by `corpus/config.jl:55-58` for the Phase-16 blind evaluation,
  so opening them would irreversibly burn Phase 16. State that no `corpus/` accessor was called.
- **`## 7. Decoupling and provenance (rulings Q8, Q2; D-12)`** — quote `ROADMAP.md:126`'s literal
  claim verbatim; state plainly that D-11 deliberately edits `src/amortized/simulator.jl` so the
  literal claim is no longer available; adopt the narrower wording; and print the output of all
  five §F20c verification commands (four empty diffs plus the green suite), plus the
  `test/test_images/` digest, the pinned pre-epsilon sha and the
  `git show "<SHA>":src/amortized/simulator.jl | grep -c chromatic_eps` result of `0`. Note that
  the root suite going red on theta-arity literals during the epsilon commit was **expected work,
  not evidence of pipeline breakage**, and that `_train_grid_pipeline`'s `skip_if_done` branch
  checks artifact integrity rather than the datagen digest, so the digest change is invisible at
  that call site — silence there must not be read as "nothing changed".
- **`## 8. Declared research-net deviations (ruling Q4)`** — the four, enumerated, each with the
  reason it was declared in advance and its effect on comparability. State that this net is **not**
  the shipped one, **not** drop-in comparable to the shipped 128-row read surface, and **not** a
  distributable artifact (it persists the whole estimator, unlike the shipped `Flux.state` form).
- **`## 9. What this run establishes`** — a numbered, quotable, one-claim-per-item list.
- **`## 10. What was deliberately NOT done`** — the deferred ideas restated as scope boundaries:
  no summary extension for registration identifiability; no dedicated registration OOD channel; no
  staged marginalize-then-identify comparison; no off-axis optical centre; no bead acquisition; no
  API-level width advisory; no productionization of the research net. For the first of these, note
  that a below-resolution probe would have been direct evidence *for* it, and state whether the
  probe was.

Then append a `## Final decoupling verification` section to
`.planning/phases/11-.../11-PROVENANCE.md` re-running the five §F20c commands against
`PHASE11_BASE_SHA` at end of phase, with their outputs, so the claim is verified at the state the
phase actually ships in rather than only mid-phase.
  </action>
  <verify>
    <automated>julia --project -t auto -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project -t auto -e 'using Pkg; Pkg.test()'` exits 0.
    - `11-REPORT.md` opens with an `## Overall verdict:` heading before any interpretation section.
    - It contains all of: `SC2/SC3 tension`, `wide but unflagged`, `vacuous by construction`, `REPORTED, NOT GATED`, `sealed_holdout`, `registration ladder only`, and the pinned 40-character pre-epsilon sha.
    - It quotes `ROADMAP.md:126` verbatim and states the narrower adopted claim: `grep -c 'only `src/amortized/`' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md` is at least `1`.
    - SC2a and SC2b appear as two separately worded verdicts: `grep -cE 'SC2a|SC2b' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md` is at least `4`.
    - The four declared deviations are enumerated: `grep -c 'BoundedThetaTransform' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md` is at least `1`.
    - `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- src/bayes.jl src/colocalization.jl src/LoadImages.jl src/plot.jl Artifacts.toml test/test_images/` is empty, and that empty output is recorded in `11-PROVENANCE.md` under `## Final decoupling verification`.
    - `git diff --name-only "$PHASE11_BASE_SHA"..HEAD -- src/ | grep -v '^src/amortized/'` is empty, recorded likewise.
  </acceptance_criteria>
  <done>The report leads with its verdict, presents every measured value beside its pre-registered threshold, states the SC2/SC3 tension and the vacuous-column non-evidence caption in the house voice, records the D-17 substitution and the narrower decoupling claim with their reasons, quotes the pinned sha, and closes with what was deliberately not done.</done>
</task>

<task type="auto">
  <name>Task 3: Add the D-16 interpretation section to docs/amortized.md</name>
  <files>docs/amortized.md</files>
  <read_first>
    - `11-PATTERNS.md` "docs/amortized.md named-limit shape" and the heading-order table — the
      D-16 section is inserted as a new `##` **between line 105 and line 106**, i.e. after
      `## Windowed sub-tile local map (coarse)` and immediately before
      `## Named limits (non-negotiable honesty — carry these into the manuscript)`. The rationale
      is structural: everything before the Named-limits heading is *how to read the tool*;
      everything from it onward is *what not to claim*.
    - `docs/amortized.md` lines 95-155 — the `## Windowed sub-tile local map (coarse)` section
      (the nearest tonal analog: describe a capability, then immediately and bluntly bound what it
      is not), the Named-limits intro, and named limit #8 as written in plan 11-02.
    - `11-RESEARCH.md` §H25 lines 1939-1944 — the recommended one-liner for the docs note.
    - `11-CONTEXT.md` D-16 — the deliverable definition and the scrupulousness requirement.
    - `.planning/phases/11-.../11-REPORT.md` — the numbers to quote, so the docs section and the
      report cannot disagree.
  </read_first>
  <action>
Insert a new `##` section into `docs/amortized.md`, titled
`## Reading posterior width under registration uncertainty (Phase 11, research net)`, positioned
**after** `## Windowed sub-tile local map (coarse)` and **immediately before**
`## Named limits (non-negotiable honesty — carry these into the manuscript)`. Match the tonal
analog: describe the capability, then immediately and bluntly bound what it is not. Second person
is never used; the subject is the artifact or the evidence; em-dashes carry the concessions;
emphasis is bold, never italics.

Content, kept short — this is an interpretation note, not a second report:

1. **Which net every number came from.** Open by stating that the numbers in this section come from
   a **Phase-11 research net** — `d_in = 129`, `D = 8`, a widened shift prior, a chromatic
   parameter, a lambda conditioning input and a ported bounded theta transform — trained on a fresh
   DEV seed in `spike/`. It is **not** the shipped `amended_v2/grid_8` bundle, is **not** drop-in
   comparable to the shipped 128-row read surface, and is **not** downloadable. Every number the
   rest of `docs/amortized.md` quotes comes from the shipped bundle; every number in this section
   does not. Say that explicitly.
2. **How to read a wide posterior.** State the SC2 result in one or two sentences: under increasing
   declared registration uncertainty the Delta-rho posterior widens, and its 90 percent
   credible-interval coverage stays equivalent to nominal within the pre-registered band while it
   widens — quoting the measured numbers from the report. Then the operational reading: a wide
   Delta-rho posterior under registration uncertainty is the **honest** answer, not a defect.
3. **The quotable operating limit.** Quote the D-14 breakdown point at both read levels
   (lambda = 3.0 and lambda = 1.0) in the report's own phrasing — "coverage holds to below X px;
   degrades at and beyond X px" — and label it explicitly as **reported, not gated**.
4. **The blunt bound (D-13).** Use the RESEARCH-recommended one-liner: *a wide `Delta-rho` posterior
   under registration uncertainty is the honest answer, not a flagged error — the OOD flag does not
   fire on misalignment the model was trained to expect, and by design cannot.* Follow it with the
   accepted cost in one sentence: badly mis-registered data returns a wide but **unflagged** answer,
   so the user is not told why the interval is wide. Cross-reference limit #8 and the Phase-11
   report path.
5. **The registration-only scope (ruling Q3).** One sentence: the monotone-widening result is about
   **registration** uncertainty; the chromatic parameter carries no conditioning input, and its
   dose-response is characterised only by the fixed-injection curve in the Phase-11 report.
6. **What is NOT implemented here.** In the `Windowed sub-tile local map` register: none of this
   changes the shipped public API, the shipped artifact, or the shipped OOD channel. There is no
   width advisory in `colocalization_amortized(...)`, no registration OOD channel, and no
   productionized registration-aware estimator — those are future phases.

Also extend the existing `**Findings and scripts:**` closing line's path list with the Phase-11
report path if plan 11-02 did not already do so, and verify named limit #8 still reads correctly
beside the new section.

**Do not change any public API, any exported symbol, any artifact, or `Artifacts.toml`.** This task
edits documentation only. Run the full root suite afterwards to confirm the docs edit did not break
a content-asserting test.
  </action>
  <verify>
    <automated>julia --project -t auto -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project -t auto -e 'using Pkg; Pkg.test()'` exits 0.
    - The new section sits between the two named headings: `grep -n '^## ' docs/amortized.md` shows `## Reading posterior width under registration uncertainty (Phase 11, research net)` with a line number greater than that of `## Windowed sub-tile local map (coarse)` and less than that of `## Named limits`.
    - The section states the net provenance: `grep -c 'not the shipped' docs/amortized.md` is at least `1`, and `grep -c 'research net' docs/amortized.md` is at least `2`.
    - It carries the D-13 one-liner: `grep -c 'by design cannot' docs/amortized.md` is at least `1`.
    - It quotes a breakdown point at both read levels: `grep -c 'coverage holds to' docs/amortized.md` is at least `1`.
    - `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- Artifacts.toml src/api.jl src/amortized/api.jl` is empty (no API or artifact change).
    - `grep -n '^8\. \*\*' docs/amortized.md` still returns exactly one line and it is still inside the Named-limits list.
    - `grep -c '11-REPORT.md' docs/amortized.md` is at least `1`.
  </acceptance_criteria>
  <done>`docs/amortized.md` carries a short interpretation section, correctly positioned between "how to read the tool" and "what not to claim", that attributes every number to the research net, states the honest-width reading, quotes the breakdown point as reported-not-gated, names the unflagged cost bluntly, bounds the claim to registration only, and changes no API or artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research-net numbers to published documentation | `docs/amortized.md` otherwise describes only the shipped bundle; an unattributed research-net number would be read as a shipped-estimator property |
| report claims to the evidence artifacts | Every quoted number must trace to a committed `.jld2`, or the report becomes a stronger claim than the evidence supports |
| documentation edit to shipped surface | D-16 forbids any API or artifact change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-50 | Spoofing | A research-net number read as a shipped-estimator property | mitigate | The D-16 section opens by naming the net and stating it is not the shipped bundle and not downloadable; every figure caption names its source net; acceptance criteria grep for both |
| T-11-51 | Repudiation | Silent target substitution or silently narrowed decoupling claim | mitigate | Report sections 6a and 7 record ruling Q1 and ruling Q8 explicitly, quote `ROADMAP.md:126` verbatim, and print the five verification command outputs |
| T-11-52 | Spoofing | A vacuous rank result quoted as calibration evidence in the manuscript | mitigate | The verbatim §E19 non-evidence caption is carried into report section 2b, alongside the per-rung shrinkage table |
| T-11-53 | Tampering | Documentation edit altering the shipped API or artifact | mitigate | Acceptance criteria assert `Artifacts.toml` and the API files are byte-unchanged across the whole phase, and the full root suite is green after the edit (ASVS V6) |
| T-11-54 | Tampering | A figure disagreeing with the artifact it illustrates | mitigate | `p11_figures.jl` reads every number from the committed `.jld2` artifacts and is grep-asserted to write no artifact of its own |
| T-11-55 | Repudiation | Named limit #8 damaged while inserting the new section | mitigate | Acceptance criterion re-asserts exactly one `^8\. \*\*` item still inside the Named-limits list after the edit |
| T-11-SC | Tampering | package-manager installs | mitigate | No packages installed; CairoMakie is already a resolved spike dependency |
</threat_model>

<verification>
1. `julia --project=spike spike/validation/p11_figures.jl` — exit 0; four PNGs written and tracked.
2. `julia --project -t auto -e 'using Pkg; Pkg.test()'` — exit 0 after the docs edit.
3. `11-REPORT.md` contains the verdict-first heading, the tension section, the vacuous-column
   caption, the substitution record, the narrower decoupling claim and the pinned sha.
4. The five §F20c commands are re-run at end of phase and recorded in `11-PROVENANCE.md` under
   `## Final decoupling verification`, all producing their expected empty output.
5. `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- Artifacts.toml` is empty.
6. The D-16 section sits between `## Windowed sub-tile local map (coarse)` and `## Named limits`.
</verification>

<success_criteria>
- Four figures exist, drawn from committed artifacts, each captioned with its source net.
- `11-REPORT.md` leads with its verdict, reports SC2a and SC2b separately, presents the breakdown
  curve as reported-not-gated at both read levels, states the SC2/SC3 tension and the accepted
  "wide but unflagged" cost in plain language, carries the vacuous-column non-evidence caption,
  records the D-17 substitution and the narrower decoupling claim with their reasons, quotes the
  pinned pre-epsilon sha, and closes with what was deliberately not done.
- `docs/amortized.md` carries the D-16 interpretation section in the correct position, scrupulously
  attributed to the research net, with no API and no artifact change.
- `11-PROVENANCE.md` carries the end-of-phase re-run of all five verification commands.
- The full root suite is green.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-11-SUMMARY.md`
when done, listing the figure paths, the report path, the docs section heading and line position,
and the end-of-phase decoupling command outputs.
</output>

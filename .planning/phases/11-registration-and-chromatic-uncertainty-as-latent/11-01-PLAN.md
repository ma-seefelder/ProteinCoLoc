---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/validation/p11_consts.jl
  - spike/test/test_p11_consts.jl
  - spike/validation/p11_stats.jl
  - spike/test/test_p11_tost.jl
  - spike/test/test_p11_forced_theta.jl
  - spike/test/capture_p11_golden.jl
  - spike/test/fixtures/p11_stage6_golden.jld2
autonomous: true
requirements: [D-01, D-04, D-07, D-08, D-10, D-14]

must_haves:
  truths:
    - "(D-04) Every SC2/SC3 threshold, ladder and seed governing a reported Phase-11 run is committed BEFORE any run exists"
    - "(D-01) The Phase-11 DEV seed is executably proven disjoint from PROD_SEED_V2, VAL_MASTER_SEED, NPE_MASTER_SEED, VAL_FIX_SEED, CORPUS_MASTER_SEED and every prior dev/burned seed"
    - "(D-08) The equivalence family's Holm direction is all(<=(fwer), ...) and is proven inverted relative to sbc_holm_pass by a hand-computed vector"
    - "(D-10) A pre-edit golden fixture of simulate_pair exists, captured before any stage-6 byte changes, so the epsilon=0 regression check is verifiable"
    - "(D-14) The beyond-prior breakdown ladder and its two read levels are locked as reported-not-gated constants"
    - "(D-07) The two-part SC2 criterion (monotone width AND per-rung coverage) is expressed as locked constants, not as prose"
  artifacts:
    - path: "spike/validation/p11_consts.jl"
      provides: "Tier-1 pre-registration: P11_DEV_SEED/P11_SALT, forbidden-seed set, ladders, N_PER_RUNG, TOST band, abort criterion, iteration allowance, F5 imsize by reference"
      contains: "P11_DEV_SEED"
      min_lines: 150
    - path: "spike/test/test_p11_consts.jl"
      provides: "Executable assertions that the locked values are the locked values"
      contains: "_p11_forbidden"
    - path: "spike/validation/p11_stats.jl"
      provides: "wilson_ci / tost_pvalue / p11_holm_adjusted / p11_tost_pass / p11_perm_spearman / p11_shrinkage / breakdown_point"
      contains: "p11_tost_pass"
    - path: "spike/test/test_p11_tost.jl"
      provides: "Hand-computed Holm-direction test plus Wilson/TOST both-directions tests"
    - path: "spike/test/test_p11_forced_theta.jl"
      provides: "merge(sample_prior(rng), forced) field-order / row-alignment invariant"
    - path: "spike/test/fixtures/p11_stage6_golden.jld2"
      provides: "Pre-edit simulate_pair golden bytes at 4 Philox keys plus the PHASE11_BASE_SHA it was captured at"
      contains: "phase11_base_sha"
  key_links:
    - from: "spike/test/test_p11_consts.jl"
      to: "spike/validation/p11_consts.jl"
      via: "guarded include plus literal-value assertions"
      pattern: "p11_consts\\.jl"
    - from: "spike/validation/p11_consts.jl"
      to: "test/gate/gate_consts_8_v2.jl"
      via: "isolated module GateV2 read of SBC_IMSIZE_SET/WEIGHTS"
      pattern: "module GateV2"
    - from: "spike/test/capture_p11_golden.jl"
      to: "spike/simulator/forward.jl"
      via: "simulate_pair on an explicit 7-field theta literal (never sample_prior)"
      pattern: "simulate_pair"
---

<objective>
Lock the Phase-11 pre-registration (D-04) and capture the two artifacts whose ordering is
load-bearing: the pre-edit `simulate_pair` golden fixture (D-10 regression check) and the
Phase-11 base commit sha (the §F20c decoupling diff anchor).

Purpose: every downstream reported number in this phase is credible only if the thresholds,
ladders and seed were committed before any run. This plan is the anti-snooping contract, plus
the statistical primitives whose *direction* the phase can silently get wrong (D-08 Holm).

Output: `spike/validation/p11_consts.jl` (Tier-1 only), `spike/validation/p11_stats.jl`, three
spike unit-test files, and `spike/test/fixtures/p11_stage6_golden.jld2`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PATTERNS.md

**Binding orchestrator rulings this plan implements** (do not reopen):
- **Q5** — the beyond-prior breakdown curve is read at **lambda = 3.0 primary, lambda = 1.0
  secondary**. Both are Tier-1 constants, locked here, before any run. Neither is gated (D-14).
- **Q6** — no Phase-11 REQ-IDs are minted. `requirements:` carries D-IDs. `REQUIREMENTS.md` and
  `ROADMAP.md`'s `**Requirements**: TBD` stay untouched.
- **Q7** — the research net trains AND the ladder evaluates on the **F5 image-size mixture**.
  This is a Tier-1 design choice, locked here. `SBC_IMSIZE_SET`/`SBC_IMSIZE_WEIGHTS` are **read**
  from `test/gate/gate_consts_8_v2.jl:152-153` via the isolated `module GateV2` idiom, **never
  retyped**. Tier-2 records probe-derived THRESHOLDS ONLY, never the imsize choice. A datagen
  wall-clock ceiling is pre-registered here; exceeding it is a BLOCKER written to STATE.md, never
  a silent downgrade to 256 squared.
- **Q4** — the `BoundedThetaTransform` port is a **declared** research-net deviation, enumerated
  in this consts file so it is named in advance (the D-04 discipline).

**The Tier-1 / Tier-2 split (RESEARCH §G23).** This plan writes **Tier 1 only**. Tier 2
(`SC2_SPEARMAN_FLOOR`, the `drho_eq` calibration coefficients) is *appended* by plan 11-05 after
the probe, in a **second guard block keyed on a different sentinel const**. Constants are
appended, never edited — the git history is the pre-registration audit trail.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

From `spike/validation/consts.jl` (the file-shape analog; AGPL block header, one guard block):
```julia
if !isdefined(@__MODULE__, :SBC_M)
    const SBC_M           = 2000
    const VAL_MASTER_SEED = 0x5BC0FFEE   # != 0xC0FFEE (NPE_MASTER_SEED)
    const VAL_FIX_SEED    = 0xF1F7ED
end
```

From `test/gate/gate_consts_8_v2.jl:152-153` (READ, never retype):
```julia
const SBC_IMSIZE_SET     = ((512, 512), (1024, 1024), (1376, 1028), (2048, 2048))
const SBC_IMSIZE_WEIGHTS = (0.40, 0.25, 0.25, 0.10)
```

From `test/gate/sbc.jl:352-379` — the adjuster to copy, and the direction to INVERT:
```julia
sbc_holm_adjusted(p::AbstractVector{<:Real})                   # Holm step-down
sbc_holm_pass(p; fwer) = all(>(fwer), sbc_holm_adjusted(p))    # pass iff Holm rejects NOTHING
```

From `test/gate/sbc.jl:243-250` — already generic in the column count:
```julia
sbc_shrinkage(post_sd::AbstractMatrix, prior_draws::AbstractMatrix)
  # -> (shrinkage, post_sd_mean, prior_sd, vacuous);  SBC_VACUOUS_SHRINKAGE = 0.95
```

From `spike/simulator/prior.jl:73-81` — today's 7-field `sample_prior` NamedTuple order:
`(rho_true, spillover, autofluorescence, label_efficiency, shift_dx, shift_dy, noise)`

From `scripts/train_grid8_amended_v2.jl:25-29` — the isolated-include idiom:
```julia
module GateV2
    include(joinpath(@__DIR__, "..", "test", "gate", "gate_consts_8_v2.jl"))
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Tier-1 pre-registration consts file and its test</name>
  <files>spike/validation/p11_consts.jl, spike/test/test_p11_consts.jl</files>
  <read_first>
    - `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-RESEARCH.md`
      §G22 (lines 1604-1717) and §G23 (lines 1718-1758) — the seed inventory, the executable
      disjointness block, and the exact Tier-1 vs Tier-2 split.
    - `11-RESEARCH.md` §E16 lines 1236-1256 (the N_min = 271 to N_PER_RUNG = 500 derivation),
      §E17 lines 1302-1303 (the SC2 rung ladder), §E18 lines 1336-1345 (the beyond-prior ladder),
      §C10 lines 829-898 (the abort criterion and the band arithmetic), §D14 lines 1094-1103
      (the training budget).
    - `11-PATTERNS.md` §1 (the two-guard-block trap and the provenance-disclosure voice) and §2
      (the test shape, the `module GateV2` idiom).
    - `spike/validation/consts.jl` — whole file, 80 lines, the primary shape.
    - `test/gate/gate_consts_8_v2.jl` — lines 15-26, 126-166, 254-343, 434-453.
    - `spike/test/test_sbc.jl:98-116` — the pre-registration test shape and the CUDA-absence clause.
    - `scripts/train_grid8_amended_v2.jl:25-35` — the isolated-include idiom.
  </read_first>
  <action>
Create `spike/validation/p11_consts.jl`: AGPL block header (spike directory style), then
`# spike/validation/p11_consts.jl --- Phase-11 Tier-1 pre-registration (D-04).`, then an
ALL-CAPS anti-snooping preamble in `consts.jl:21-40`'s voice that additionally states the
two-tier structure: **Tier 1 is this block, committed before the probe; Tier 2 is a SECOND guard
block appended after the probe; no constant in either block is ever EDITED.** Use the
`gate_consts_8_v2.jl:15-26` provenance-disclosure voice for that note.

Wrap the Tier-1 body in `if !isdefined(@__MODULE__, :P11_DEV_SEED) ... end`. Constants are
column-aligned with `=` and each carries a trailing rationale comment naming its governing
decision ID.

Contents:

1. **Forbidden-seed machinery (D-01).** `import Random123: Philox4x`. Declare
   `NPE_MASTER_SEED = 0x0000_0000_00C0_FFEE`, `VAL_MASTER_SEED = 0x0000_0000_5BC0_FFEE`,
   `VAL_FIX_SEED` — **read the literal from `spike/validation/consts.jl:79` and confirm it is
   `0xF1F7ED`, i.e. `0x0000_0000_00F1_F7ED`**; RESEARCH §G22 deliberately printed a wrong-looking
   placeholder to force this check. Also `DEFAULT_MASTER_SEED = 0x0000_0000_0000_0001`,
   `CORPUS_MASTER_SEED = 0x0000_0000_00C0_5EED`,
   `F2_DEV_SEEDS = (0x0000_0000_0DE7_C0DE, 0x0000_0000_DE7C_0DE2)`,
   `SPIKE_DEV_SEEDS = (0x0000_0000_00CA_11B0, 0x0000_0000_00CA_11B1, 0x0000_0000_00CA_11B2,
   0x0000_0000_00CA_11B3, 0x0000_0000_00DE_C0DE, 0x0000_0000_0DE7_C0D3)`, and
   `P11_RESEARCH_BURNED = (0x…BEEF, 0x…CAFE, 0x…FEED, 0x…DEAD)` as `UInt64`s with a comment naming
   them as keys the 11-RESEARCH indicative probe consumed on 2026-07-25 (burned, not reusable).
   Recompute the derived gate seeds rather than trusting comments:
   `_derive(master, salt, G) = rand(Philox4x(UInt64, (UInt64(master) xor salt, UInt64(G))), UInt64)`
   with `PROD_MASTER = 0x0000_0000_09E3_779B`, `PROD_SALT = 0x94D0_49BB_1331_11EB`,
   `AMEND_MASTER = 0x0000_0000_C0DE_2026`, `AMEND_SALT = 0xC4CE_B9FE_1A85_EC53`, over
   `G in (4, 8, 16, 32)`, producing `PROD_SEED` and `PROD_SEED_V2` as `Dict`s. Expose
   `_p11_forbidden()` returning the flattened tuple including `UInt64(0)`.
2. **The fresh stream.** `P11_SALT = 0xA24B_AED4_663E_E121` (mx3 constant; comment naming the
   five repo salts it differs from), `P11_DEV_SEED = 0x0000_0000_0B11_DE71`, and
   `p11_rng(counter::Integer = 0) = Philox4x(UInt64, (UInt64(P11_DEV_SEED) xor P11_SALT, UInt64(counter)))`.
   Reserve named counters as consts: `P11_PROBE_COUNTER = 1`, `P11_DATAGEN_COUNTER = 2`,
   `P11_LADDER_COUNTER = 3`, `P11_BREAKDOWN_COUNTER = 4`, `P11_ATTENUATION_COUNTER = 5`,
   `P11_REALIMAGE_COUNTER = 6`, `P11_FIXTURE_COUNTER = 99` — with the `VAL_FIX_SEED` discipline
   comment that fixtures never consume a reported counter.
3. **Lambda conditioning (D-03).** `LAMBDA_MIN = 0.25` — comment: never 0, because the 0 to 0.25
   step is interpolation onset, not misalignment sensitivity (RESEARCH §C9b, Pitfall 4).
   `LAMBDA_MAX = 3.0` — comment: equals D-02's widened `SHIFT_PRIOR` half-width, so the widest
   rung *is* the stated prior.
4. **Ladders.** `SC2_RUNGS = (0.25, 0.5, 1.0, 1.5, 2.0, 2.5, 3.0)`;
   `SC3_SHIFT_RUNGS = (3.5, 4.0, 5.0, 6.0, 8.0)` with a comment that the injection is along the
   diagonal, `dx = dy = x/sqrt(2)`; `SC3_EPS_RUNGS = (0.025, 0.03, 0.04, 0.05)`;
   `SC3_READ_LAMBDA_PRIMARY = 3.0` and `SC3_READ_LAMBDA_SECONDARY = 1.0` per ruling Q5, with a
   comment stating lambda = 3.0 is the maximal-declared-uncertainty (most damning) reading and
   lambda = 1.0 the realistic under-declaration user story, and that **neither is gated** (D-14).
5. **Coverage / equivalence (D-07, D-08).** `SC2_COVERAGE_NOMINAL = 0.90`,
   `SC2_TOST_DELTA = 0.03`, `SC2_TOST_ALPHA = 0.05` (per side), `P11_HOLM_FWER = 0.05`,
   `PERM_B = 10_000`, `N_PER_RUNG = 500`, `N_MIN_DERIVED = 271`, `Z_TWO_SIDED_90 = 1.644853627`.
   Record the §C10(b) derivation verbatim as a comment block — the reported-width-factor table
   (c = 0.92 gives coverage 0.8698, minus 3.0 pp; c = 1.00 gives 0.9000; c = 1.10 gives 0.9260,
   plus 2.6 pp) and the sentence that a plus/minus 3 pp coverage band equals roughly a minus 8
   percent / plus 10 percent error in reported interval width, approximately 1/30 of the effect
   the ladder demonstrates — in the way `gate_consts_8_v2.jl:189-198` records the Fisher-z
   derivation. Also record the `N_min` arithmetic `0.49346/sqrt(N) <= 0.03 => N >= 270.6`.
6. **The D-06 probe specification and abort criterion.** `P11_PROBE_THETA_BASE_N = 5`,
   `P11_PROBE_R = 32`, `P11_PROBE_SHIFT_RUNGS = (0.0, 0.25, 0.5, 1.0, 1.5, 2.0, 3.0)`,
   `P11_PROBE_SHIFT_EXT = (4.0, 5.0, 6.0, 8.0)`,
   `P11_PROBE_EPS_RUNGS = (0.0, 0.0025, 0.005, 0.01, 0.02)`,
   `P11_PROBE_EPS_EXT = (0.03, 0.05)`, `P11_PROBE_DRHO_RUNGS = (0.02, 0.05, 0.10, 0.20)`,
   `P11_PROBE_METRIC = :paired_l2_rows_1_64` with a comment fixing it to the mean **and** median
   paired 2-norm of the summary difference over the 64 continuous rows only — never the mask rows
   65:128, which are near-constant and are exactly the rows `_summary_row_partition` excludes.
   **The abort criterion is Tier 1 and must be immune to the probe's own result:**
   `P11_PROBE_S_FLOOR = 0.9`, `P11_PROBE_SPAN_FLOOR = 0.02` (drho_eq units), and
   `SC2_SPEARMAN_ATTENUATION = 0.5` with a comment naming the 0.5 as a **judgment allowance fixed
   before the run** (the same move `07-NUISANCE-SBC-SPEC-DRAFT.md:91-94` makes for delta = 0.10 SD)
   — Tier 2 supplies only the measured `S_probe`, and `SC2_SPEARMAN_FLOOR` is defined there as
   `SC2_SPEARMAN_ATTENUATION * S_probe`.
7. **F5 image-size arm (ruling Q7).** Load `test/gate/gate_consts_8_v2.jl` into an **isolated
   `module GateV2`** exactly as `scripts/train_grid8_amended_v2.jl:25-29` does, then
   `const P11_IMSIZE_SET = GateV2.SBC_IMSIZE_SET` and
   `const P11_IMSIZE_WEIGHTS = GateV2.SBC_IMSIZE_WEIGHTS`. Comment that this is READ, never
   retyped, that the binding invariant is train-joint == eval-joint (F5), and that the isolation
   is required because the gate file defines the same const names as `gate_consts_8.jl`.
   Add `P11_PROBE_COMPARABILITY_IMSIZE = (256, 256)` — the extra probe arm kept only for
   comparability with the RESEARCH indicative run.
8. **Budget (ruling Q7).** `P11_N_PAIRS = 50_000` — comment: matches every prior run
   (`train_grid8_amended_v2.jl:11`), so the comparison to the shipped net is capacity-controlled.
   `P11_DATAGEN_WALLCLOCK_CEILING_MIN = 150` — comment: expected about 1.2 h at 32 threads
   (`gate_consts_8_v2.jl:148`, E[cost] 0.08346 s/pair); **exceeding this is a BLOCKER recorded in
   STATE.md, NOT a downgrade to 256 squared**, which would recreate the exact 07-05 compute-budget
   artifact `gate_consts_8_v2.jl:130-134` exists to prevent.
9. **Regression tolerance (D-10).** `P11_STAGE6_EXACT = true` and
   `P11_STAGE6_TOLERANCE_FALLBACK = 1e-13`, with a comment that exact `==` is MEASURED to hold
   (RESEARCH §A3, twelve keys plus four shift pairs) and that any use of the fallback must be
   recorded as a deviation in the report.
10. **The D-04 novelty.** `P11_ITERATION_ALLOWANCE = 1` with a comment that the allowance is
    declared in advance, that a second iteration is NOT authorised by this file, and that the
    single allowance is cheapest spent on a re-parameterised probe (RESEARCH §C10(c) step 2)
    before training rather than after.
11. **Declared research-net deviations (ruling Q4).** `P11_RESEARCH_NET_DEVIATIONS` as a
    4-tuple of strings naming exactly: the `chromatic_eps` 8th theta column; `SHIFT_PRIOR`
    widened to `Uniform(-3, 3)` (spike only); lambda appended as a 129th input row; and the
    `BoundedThetaTransform` ported from `src/amortized/architecture.jl`. Comment that naming the
    model changes in advance is the D-04 discipline, that D-13's precedent is that unnamed model
    changes are how credibility gets spent, and that the report must enumerate exactly these four.
12. **Executable self-checks** at the end of the block:
    `@assert !(UInt64(P11_DEV_SEED) in _p11_forbidden())`;
    `@assert P11_SALT not-in (0x9E37_79B9_7F4A_7C15, 0xD1B5_4A32_D192_ED03, 0xBF58_476D_1CE4_E5B9, PROD_SALT, AMEND_SALT)`;
    `@assert issorted(SC2_RUNGS) && first(SC2_RUNGS) == LAMBDA_MIN && last(SC2_RUNGS) == LAMBDA_MAX`;
    `@assert N_PER_RUNG >= N_MIN_DERIVED`;
    `@assert sum(P11_IMSIZE_WEIGHTS) approx 1.0`;
    `@assert length(unique(_p11_forbidden())) == length(_p11_forbidden())`.

Then create `spike/test/test_p11_consts.jl`: AGPL header, guarded
`include(joinpath(@__DIR__, "..", "validation", "p11_consts.jl"))`, `using Test`, one outer
`@testset "P11 Tier-1 pre-registration (D-04)" verbose = true` with nested testsets asserting the
locked values **as literals**, so a post-hoc edit breaks the suite loudly:
the seed and salt literals; `!(UInt64(P11_DEV_SEED) in _p11_forbidden())`; pairwise disjointness
against `VAL_MASTER_SEED`, `NPE_MASTER_SEED`, `VAL_FIX_SEED`, `CORPUS_MASTER_SEED`,
`DEFAULT_MASTER_SEED` and every value of `PROD_SEED` / `PROD_SEED_V2`;
`length(_p11_forbidden()) == length(unique(_p11_forbidden()))`; `N_PER_RUNG == 500` and
`N_PER_RUNG >= 271`; `SC2_TOST_DELTA == 0.03`; `SC2_RUNGS == (0.25,0.5,1.0,1.5,2.0,2.5,3.0)`;
`first(SC2_RUNGS) > 0.0` (the no-lambda-zero rule); `issorted(SC3_SHIFT_RUNGS)`;
`SC3_READ_LAMBDA_PRIMARY == 3.0 && SC3_READ_LAMBDA_SECONDARY == 1.0`;
`P11_ITERATION_ALLOWANCE == 1`; `length(P11_RESEARCH_NET_DEVIATIONS) == 4`;
`P11_IMSIZE_SET == ((512,512),(1024,1024),(1376,1028),(2048,2048))` **with an in-test comment
saying this literal is the ASSERTION that the read succeeded, not a second source of truth**;
and `P11_DATAGEN_WALLCLOCK_CEILING_MIN == 150`. Close with the house
`@testset "... ran CPU-only (D-10)"` clause asserting no `CUDA` in `Base.loaded_modules`, copied
from `spike/test/test_sbc.jl:114-116`.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p11_consts.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p11_consts.jl` exits 0 with zero failures.
    - `grep -c 'if !isdefined' spike/validation/p11_consts.jl` outputs `1` (Tier-2 adds the second block later).
    - `grep -q 'module GateV2' spike/validation/p11_consts.jl` succeeds AND `grep -c '(1376, 1028)' spike/validation/p11_consts.jl` outputs `0` (the mixture is read, never retyped).
    - `julia --project=spike -e 'include("spike/validation/p11_consts.jl"); @assert P11_ITERATION_ALLOWANCE == 1; @assert first(SC2_RUNGS) == 0.25; @assert N_PER_RUNG >= N_MIN_DERIVED; @assert length(P11_RESEARCH_NET_DEVIATIONS) == 4; println("ok")'` prints `ok`.
    - `grep -c 'P11_RESEARCH_BURNED' spike/validation/p11_consts.jl` is at least `2` (declared and consumed by `_p11_forbidden`).
    - `git diff --stat -- spike/Project.toml spike/Manifest.toml` is empty (no dependency change).
  </acceptance_criteria>
  <done>The Tier-1 anti-snooping contract is committed and executably self-checked; the DEV seed is proven disjoint from all forbidden families; the F5 mixture is read from the frozen gate file; the one-iteration allowance and the four declared research-net deviations are constants, not prose.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase-11 statistical primitives with the INVERTED Holm direction</name>
  <files>spike/validation/p11_stats.jl, spike/test/test_p11_tost.jl, spike/test/test_p11_forced_theta.jl</files>
  <read_first>
    - `11-RESEARCH.md` §E16 lines 1164-1260 — the Wilson CI, the two one-sided tests, the exact
      `p11_tost_pass` definition, the intersection-union honesty note, the N-per-rung arithmetic,
      and the "no over-power pathology for an equivalence test" reconciliation.
    - `11-RESEARCH.md` §E17 lines 1262-1304 — the permutation-Spearman design and why the
      permutation is over **rung labels**.
    - `11-RESEARCH.md` §E18 lines 1321-1335 — the exact `breakdown_point` definition.
    - `11-RESEARCH.md` §E19 lines 1347-1411 — `sbc_shrinkage` and the per-rung `prior_sd` rule.
    - `11-RESEARCH.md` Pitfall 3 lines 2078-2083 and risk R7 lines 1834-1839.
    - `11-PATTERNS.md` §5 — the copy-with-attribution convention and the single most dangerous
      naive-pattern-match trap in this phase.
    - `test/gate/sbc.jl:243-250` and `:352-379` — the two functions being copied and the
      direction being inverted.
    - `spike/simulator/prior.jl:73-81` — the current `sample_prior` field order.
    - `spike/Project.toml` — confirm `StatsBase` and `Distributions` are present; no new dep.
  </read_first>
  <action>
Create `spike/validation/p11_stats.jl`: AGPL header,
`# spike/validation/p11_stats.jl --- Phase-11 equivalence / monotonicity statistics (D-07, D-08).`,
the reworded S5 decoupling line ("spike-local; reaches `src/` only read-only"), a guarded include
of `p11_consts.jl`, and `using Statistics, StatsBase, Random` plus `import Distributions: Normal, cdf`.
Define, as flat top-level functions (no `module` wrapper — the spike house style):

- `wilson_ci(k::Integer, n::Integer; z::Real = Z_TWO_SIDED_90)` returning `(lo = ..., hi = ...)`,
  exactly the RESEARCH §E16 form. Comment that Wald is deliberately NOT used because its coverage
  is poor near p = 0.9 at N in the low hundreds.
- `tost_pvalue(k::Integer, n::Integer; p0 = SC2_COVERAGE_NOMINAL, delta = SC2_TOST_DELTA)`
  returning `max(p1, p2)` from the two one-sided normal-approximation tests against `p0 - delta`
  and `p0 + delta`. Use `cdf(Normal(), x)` — do not hand-roll `erf`. Guard the standard error
  against a degenerate `p_hat` in {0, 1} with `max(p_hat * (1 - p_hat), 1e-12)`.
- `p11_holm_adjusted(p::AbstractVector{<:Real})` — a **copy** of `sbc_holm_adjusted`
  (`test/gate/sbc.jl:352-362`) carrying an attribution comment naming the source file and line
  range and the reason it is copied rather than depended on (`spike/Project.toml` cannot load the
  gate file's dependency chain; RESEARCH "Don't Hand-Roll" forbids a fresh implementation).
- **`p11_tost_pass(p_tost::AbstractVector; fwer::Real = P11_HOLM_FWER) =
  all(<=(fwer), p11_holm_adjusted(p_tost))`** — preceded by a bold comment block stating that
  this is the **MIRROR IMAGE** of `sbc_holm_pass` (`test/gate/sbc.jl:374`, `all(>(fwer), ...)`):
  for a uniformity null rejection is bad, for an equivalence null rejection is **good**, so
  copying `holm_pass` verbatim silently inverts the SC2b verdict. State that the name
  `holm_pass` must never be reused here. Add the intersection-union note: requiring every
  component TOST to reject at alpha already has family-wise size at most alpha, so Holm is
  deliberately conservative in the right direction, not necessary — the same direction
  `gate_consts_8_v2.jl:213-218` argues for the BF arm.
- `p11_perm_spearman(lam::AbstractVector, w::AbstractVector; B::Integer = PERM_B, rng)` returning
  `(S = corspearman(lam, w), p_perm = (1 + count of S_b >= S) / (B + 1))`, shuffling the **rung
  labels** rather than the widths, so the null is exactly "lambda has no effect" under the
  within-rung dependence an asymptotic Spearman would misstate. Thread an explicit `rng`; never
  call `Random.seed!` in a loop.
- `p11_shrinkage(post_sd::AbstractMatrix, prior_draws::AbstractMatrix; cutoff = 0.95)` — a copy
  of `sbc_shrinkage` (`test/gate/sbc.jl:243-250`) with attribution, returning
  `(shrinkage, post_sd_mean, prior_sd, vacuous)`. Add a **Pitfall 5** comment: callers MUST pass
  `prior_draws` restricted to a single rung, because under D-13's hierarchical sampler the shift
  columns' prior SD within a rung is `lambda_r / sqrt(3)`; pooling would inflate `prior_sd` and
  make the shift columns look informative, contradicting D-05.
- `breakdown_point(ks::AbstractVector{<:Integer}, n::Integer, rungs)` returning the **first**
  rung magnitude whose `wilson_ci(k, n).hi < SC2_COVERAGE_NOMINAL`, or `nothing` for the
  right-censored case. Comment the "upper limb, not the point estimate" burden-of-proof rationale
  and the "first, not last" unambiguity-under-non-monotonicity rationale (RESEARCH §E18).

Create `spike/test/test_p11_tost.jl`: AGPL header, guarded include of `p11_stats.jl`,
`using Test, Random`. Testsets:
- `"Wilson CI"` — at `k = 450, n = 500` the interval brackets 0.90 and its half-width is
  `< SC2_TOST_DELTA` (proving the pre-registered N is decidable); at `k = n` the `hi` does not
  exceed 1.0 by more than floating slop.
- `"TOST p-value (both directions)"` — `tost_pvalue(450, 500) < SC2_TOST_ALPHA` (equivalent) AND
  a deliberately non-equivalent `tost_pvalue(400, 500) > SC2_TOST_ALPHA`, mirroring the
  both-directions discipline of `test_sbc.jl:63-68` so a stuck always-pass statistic is caught.
- `"Holm DIRECTION (Pitfall 3 / R7)"` — **the hand-computed vector**: for `p = [0.001, 0.002, 0.04]`
  assert `p11_holm_adjusted(p)` is elementwise approximately `[0.003, 0.004, 0.04]`; assert
  `p11_tost_pass(p; fwer = 0.05) == true`; and assert the inverted reading is wrong by asserting
  `all(>(0.05), p11_holm_adjusted(p)) == false`. Add a second vector `[0.001, 0.002, 0.30]` for
  which `p11_tost_pass(...) == false`.
- `"agreement with the frozen adjuster"` — load `test/gate/sbc.jl`'s adjuster into an isolated
  `module GateSBC` using the `module GateV2` idiom and assert
  `p11_holm_adjusted(p)` is approximately `GateSBC.sbc_holm_adjusted(p)` on a fixed 8-vector
  drawn from `Random.Xoshiro(2026)`. If the gate file cannot be loaded standalone under
  `--project=spike`, mark the testset `@test_skip` with an in-file comment naming the reason —
  do not fabricate agreement and do not add a dependency to make it load.
- `"breakdown_point"` — on a synthetic `ks` vector whose coverage drops at the 3rd rung the
  function returns the 3rd rung magnitude; on an all-covered vector it returns `nothing`.
- Close with the CUDA-absence clause.

Create `spike/test/test_p11_forced_theta.jl`: AGPL header, `using Test, Random123`, guarded
includes of `p11_consts.jl` then `spike/simulator/prior.jl`. Testsets:
- `"NamedTuple merge field-order invariant"` — `keys(merge((a=1,b=2,c=3), (b=9,))) == (:a,:b,:c)`
  (an existing key is overwritten **in place**) and `keys(merge((a=1,b=2), (z=3,))) == (:a,:b,:z)`
  (a new key **appends at the end**). These are the two facts D-15's forced-theta injection and
  D-09's append-at-end theta layout both rest on; state that in a comment.
- `"sample_prior row alignment"` — build `theta = sample_prior(p11_rng(P11_FIXTURE_COUNTER))`,
  then for `forced = (shift_dx = 1.25, shift_dy = -0.5)` assert `keys(merge(theta, forced)) ==
  keys(theta)` and that `collect(values(merge(theta, forced)))[5:6] == [1.25, -0.5]` — forcing a
  subset must not disturb `collect(values(theta))` row order, which is the house theta-to-vector map.
- `"theta arity is derived, never literal"` — assert `length(collect(values(theta))) == length(keys(theta))`
  and that `last(keys(theta)) == :noise` today, so plan 11-02's append-at-end contract for
  `chromatic_eps` is checkable before and after the edit.
- Close with the CUDA-absence clause.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_p11_tost.jl &amp;&amp; julia --project=spike spike/test/test_p11_forced_theta.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p11_tost.jl` exits 0 and `julia --project=spike spike/test/test_p11_forced_theta.jl` exits 0.
    - `grep -c 'all(<=(' spike/validation/p11_stats.jl` outputs at least `1`.
    - `grep -c 'function holm_pass\|^holm_pass' spike/validation/p11_stats.jl` outputs `0` (the name is never reused).
    - `julia --project=spike -e 'include("spike/validation/p11_stats.jl"); @assert p11_tost_pass([0.001,0.002,0.04]); @assert !p11_tost_pass([0.001,0.002,0.30]); @assert wilson_ci(450,500).hi - wilson_ci(450,500).lo < 2*SC2_TOST_DELTA; println("ok")'` prints `ok`.
    - `grep -c 'test/gate/sbc.jl' spike/validation/p11_stats.jl` is at least `2` (attribution on both copied functions).
    - `git diff --stat -- spike/Project.toml` is empty.
    - `grep -c 'atol\|isapprox' spike/test/test_p11_forced_theta.jl` outputs `0` (the row-order claims are exact).
  </acceptance_criteria>
  <done>`p11_tost_pass` provably uses `<=` and is proven inverted relative to `sbc_holm_pass` on a hand-computed vector; Wilson/TOST are decidable at the pre-registered N; the theta row-alignment invariant D-15 depends on is asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Capture PHASE11_BASE_SHA and the pre-edit stage-6 golden fixture</name>
  <files>spike/test/capture_p11_golden.jl, spike/test/fixtures/p11_stage6_golden.jld2</files>
  <read_first>
    - `11-RESEARCH.md` §A3 lines 341-398 — why exact `==` is achievable and the exact T2 fixture
      specification: four Philox keys against an **explicitly constructed** 7-field theta, never
      `sample_prior`, whose RNG consumption changes the instant `chromatic_eps` is added.
    - `11-RESEARCH.md` §F20c lines 1512-1554 — the five decoupling commands that consume
      `PHASE11_BASE_SHA`.
    - `11-RESEARCH.md` Pitfall 6 lines 2096-2101 — the sha-timing failure mode.
    - `11-PATTERNS.md` §3 — the `save_npe` atomic-write idiom and the timing-discipline trap
      (store the capture-time sha INSIDE the JLD2 so a post-edit regeneration is self-evidently
      invalid).
    - `spike/npe/train_npe.jl:160-186` — the atomic `.tmp` to reopen-assert to `mv` idiom and the
      `generated = string(Dates.now(Dates.UTC)) * "Z"` requirement (IN-07).
    - `spike/test/test_simulator.jl:90-110` — the `const theta_BASE` literal plus `merge` variant idiom.
    - `spike/simulator/forward.jl:110-180` — the current entry guards and stage 6, so the fixture
      theta is guaranteed to pass validation unchanged.
    - `.gitignore` — check whether any `*.jld2` rule would exclude the fixture.
  </read_first>
  <action>
Create `spike/test/capture_p11_golden.jl` — a one-shot capture script. AGPL header, then
`# spike/test/capture_p11_golden.jl --- PRE-EDIT golden capture for the D-10 stage-6 regression (SC1d).`
followed by an ALL-CAPS paragraph stating the **timing discipline**: this script MUST run and its
artifact MUST be committed BEFORE any byte of `spike/simulator/forward.jl` stage 6 or
`src/amortized/simulator.jl` changes; a fixture regenerated afterwards is invalid, and the
embedded `phase11_base_sha` is what makes that detectable. Include the literal run line
`#     julia --project=spike spike/test/capture_p11_golden.jl`. No `Pkg.activate`, no arg parsing.

Body:
- Guarded includes, order documented in a preceding comment: `spike/validation/p11_consts.jl`
  then `spike/simulator/forward.jl`, both via `joinpath(@__DIR__, ...)`.
- `using JLD2, Dates, Random123`.
- `const P11_GOLDEN_PATH = joinpath(@__DIR__, "fixtures", "p11_stage6_golden.jld2")`.
- `const THETA_GOLDEN = (rho_true = 0.7, spillover = 0.1, autofluorescence = 0.05,
  label_efficiency = 0.85, shift_dx = 0.37, shift_dy = -0.82, noise = 0.5)` using the repo's
  actual Unicode field names — an **explicit 7-field literal**, never `sample_prior`. State that
  reason in-file, citing RESEARCH §A4 row 3.
- Capture the sha with `readchomp(\`git rev-parse HEAD\`)` and `readchomp(\`git rev-parse --short HEAD\`)`,
  and **assert the working tree is clean for the files about to be edited**:
  `@assert isempty(readchomp(\`git status --porcelain -- spike/simulator src/amortized/simulator.jl\`))`
  with a message that a dirty tree makes the pre-edit claim already false.
- For `k in 1:4`, build `rng = p11_rng(P11_FIXTURE_COUNTER + k)` and record
  `simulate_pair(rng, THETA_GOLDEN; imsize = (256, 256))`, storing both channel matrices.
- Write atomically with the S4 idiom: `mkpath(dirname(path))`, `tmp = path * ".tmp"`,
  `jldsave(tmp; schema_version = 1, theta = THETA_GOLDEN, keys_used = collect(1:4),
  imsize = (256, 256), channels = <Vector of the four two-channel outputs>,
  phase11_base_sha = <full 40-char sha>, phase11_base_sha_short = <short sha>,
  seed = UInt64(P11_DEV_SEED), salt = UInt64(P11_SALT),
  fixture_counter = P11_FIXTURE_COUNTER, generated = string(Dates.now(Dates.UTC)) * "Z",
  caption = "PRE-EDIT simulate_pair golden for the D-10 epsilon=0 regression check (SC1d)")`,
  reopen and `@assert haskey(f, "channels") && haskey(f, "phase11_base_sha")`, then
  `mv(tmp, path; force = true)`.
- Print an `"="^78`-bannered summary including the captured sha in copy-paste-ready form.
- Bare call of the capture function as the last line (no `abspath(PROGRAM_FILE)` guard — not the
  house idiom).

Run the script. Record the printed full and short sha in the plan SUMMARY under a heading
`## PHASE11_BASE_SHA` — plans 11-03 and 11-11 consume it for the §F20c diff commands. Confirm
the fixture is trackable: if a `*.jld2` ignore rule exists, add a scoped negation for exactly
`spike/test/fixtures/p11_stage6_golden.jld2`, in the same style `.gitignore` already uses for
`spike/figures/plausibility.png`. Do not broaden any ignore rule.
  </action>
  <verify>
    <automated>julia --project=spike -e 'using JLD2; d = JLD2.load("spike/test/fixtures/p11_stage6_golden.jld2"); @assert haskey(d, "channels") && length(d["channels"]) == 4; @assert length(d["phase11_base_sha"]) == 40; @assert !haskey(d["theta"], :chromatic_eps); println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`.
    - `git ls-files --error-unmatch spike/test/fixtures/p11_stage6_golden.jld2` exits 0 (the fixture is tracked, not ignored).
    - `git diff --stat -- spike/simulator/ src/amortized/simulator.jl` is empty at capture time (the pre-edit claim holds).
    - `grep -c 'sample_prior' spike/test/capture_p11_golden.jl` outputs `0` (the fixture theta is an explicit literal).
    - `julia --project=spike -e 'using JLD2; d = JLD2.load("spike/test/fixtures/p11_stage6_golden.jld2"); @assert d["phase11_base_sha"] == readchomp(\`git rev-parse HEAD\`) || true; @assert endswith(d["generated"], "Z"); println(d["phase11_base_sha_short"])'` prints a 7-or-more-character sha.
    - The plan SUMMARY contains a `## PHASE11_BASE_SHA` section carrying the full 40-character sha.
  </acceptance_criteria>
  <done>The pre-edit golden bytes and the Phase-11 base sha exist and are committed; the fixture embeds its own capture-time sha, so a post-edit regeneration is self-evidently invalid; the D-10 regression check in plan 11-03 is now verifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration file to reported run | The consts file is the security-of-the-scientific-claim artifact; a snooped seed or a post-hoc threshold edit silently invalidates every downstream number |
| git working tree to golden fixture | The fixture asserts a property of the tree at capture time; a dirty or post-edit capture produces a false regression baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Repudiation | `spike/validation/p11_consts.jl` seed choice | mitigate | Executable `@assert !(P11_DEV_SEED in _p11_forbidden())` over six seed families plus the recomputed `PROD_SEED`/`PROD_SEED_V2` dicts; asserted again as literals in `test_p11_consts.jl` (ASVS V14) |
| T-11-02 | Tampering | Post-hoc edit of a locked threshold | mitigate | Locked values asserted **as literals** in `test_p11_consts.jl`, so an edit breaks the suite loudly; Tier 2 is a separate append-only guard block, never an edit |
| T-11-03 | Tampering | Golden fixture regenerated after the stage-6 edit | mitigate | `phase11_base_sha` is stored inside the JLD2 and the capture script asserts a clean working tree for `spike/simulator` and `src/amortized/simulator.jl` before writing |
| T-11-04 | Tampering | Torn artifact on crash during fixture write | mitigate | Atomic `.tmp` write, reopen-and-`@assert haskey`, then `mv(...; force = true)` (`spike/npe/train_npe.jl:160-180`) |
| T-11-05 | Spoofing | Retyped F5 imsize mixture drifting from the frozen gate value | mitigate | Read via isolated `module GateV2`; acceptance criterion greps that the literal tuple does NOT appear in the consts file |
| T-11-06 | Information disclosure | corpus `sealed_holdout` split | accept (out of scope here) | This plan touches no corpus path; the epistemic access control is enforced in plan 11-10 (ASVS V4) |
| T-11-SC | Tampering | package-manager installs | mitigate | **No packages are installed.** RESEARCH "Package Legitimacy Audit" records that every library is already resolved and pinned in the committed manifests. Acceptance criteria assert `spike/Project.toml` and `spike/Manifest.toml` are byte-unchanged |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_p11_consts.jl` — exit 0.
2. `julia --project=spike spike/test/test_p11_tost.jl` — exit 0.
3. `julia --project=spike spike/test/test_p11_forced_theta.jl` — exit 0.
4. `julia --project=spike -e 'using JLD2; d = JLD2.load("spike/test/fixtures/p11_stage6_golden.jld2"); @assert length(d["channels"]) == 4; println("golden ok")'`.
5. `git diff --stat -- spike/Project.toml spike/Manifest.toml src/` is empty — this plan touches no `src/` file and adds no dependency.
6. `git ls-files --error-unmatch spike/test/fixtures/p11_stage6_golden.jld2` exits 0.
</verification>

<success_criteria>
- `spike/validation/p11_consts.jl` exists with exactly one `if !isdefined` guard block, carries all
  Tier-1 constants enumerated in the task action, and self-asserts seed disjointness at include time.
- `spike/validation/p11_stats.jl` defines `p11_tost_pass` with `<=` and never defines `holm_pass`.
- All three spike unit-test files pass with zero failures.
- `spike/test/fixtures/p11_stage6_golden.jld2` is tracked in git, holds four two-channel outputs,
  a 40-character `phase11_base_sha`, and a `theta` without a `chromatic_eps` key.
- The plan SUMMARY records `PHASE11_BASE_SHA` (full and short).
- No file under `src/` is modified and no dependency is added.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-01-SUMMARY.md`
when done, including a `## PHASE11_BASE_SHA` section with the full 40-character sha.
</output>

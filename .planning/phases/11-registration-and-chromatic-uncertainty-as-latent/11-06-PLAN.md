---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 06
type: execute
wave: 5
depends_on: ["11-05"]
files_modified:
  - .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md
  - .planning/STATE.md
autonomous: false
requirements: [D-04, D-06]

must_haves:
  truths:
    - "(D-06) The abort branch is a defined, pre-registered decision with a recorded outcome, not an implicit hope"
    - "(D-04) The single declared iteration allowance is spent, or explicitly not spent, and the ledger says which"
    - "(D-06) No training compute is authorised until the probe verdict is recorded"
  artifacts:
    - path: ".planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md"
      provides: "The mechanically evaluated abort criterion, the three pre-registered branch options, and the recorded decision with its rationale"
      contains: "P11_ITERATION_ALLOWANCE"
  key_links:
    - from: ".planning/phases/11-.../11-PROBE-VERDICT.md"
      to: "spike/validation/p11_consts.jl"
      via: "criterion evaluated against the frozen Tier-1 floors and the frozen Tier-2 measurements"
      pattern: "P11_PROBE_S_FLOOR"
---

<objective>
Evaluate the pre-registered D-06 abort criterion against the frozen probe measurements, present
the three pre-registered branch options, and record the decision before any training compute is
authorised.

Purpose: "below resolution" is a real possible outcome (RESEARCH §C10(c), risk R5). A plan without
a defined branch is a plan with an implicit hope. This is also the one place the D-04 iteration
allowance is cheapest to spend — before training, not after.

Output: `11-PROBE-VERDICT.md` and an updated STATE.md carrying the authorisation (or the block).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-05-SUMMARY.md

`11-VALIDATION.md` lists the probe "below resolution" verdict as a **manual-only verification**:
the abort branch requires human-legible judgement of the probe curves against the pre-registered
floor. That is why this plan carries a blocking checkpoint. The *criterion* itself is mechanical
and frozen; the *branch choice* — in particular whether to spend the single D-04 iteration — is a
scope-versus-effort tradeoff and belongs to the developer.

**The three pre-registered branches (RESEARCH §C10(c)) — do not invent a fourth:**
1. **PROCEED.** The criterion did not fire. Training is authorised as planned.
2. **SPEND THE ITERATION.** The criterion fired. Do **not** train. Re-parameterise the *ladder*,
   not the *criterion*: raise the maximum toward the D-14 extension range (up to 8 px) and/or move
   the ladder to the F5 realistic image sizes where the chromatic response is much larger. Re-run
   the probe **once**. This consumes `P11_ITERATION_ALLOWANCE`.
3. **STOP SC2.** The criterion fired again after the single re-run, or the developer chooses not
   to spend the allowance. Report SC2 as **NOT DEMONSTRABLE at the fixed 8x8 summary resolution**,
   with the probe curve as the evidence; route the remaining budget to SC3 (a coverage claim,
   which does not require a resolvable width gradient) plus the D-16 docs note; and link the
   finding explicitly to the deferred "summary extension for registration identifiability" idea,
   since a below-resolution probe is direct evidence *for* that deferred work.

**Forbidden under every branch:** widening the ladder a second time, or relaxing
`SC2_SPEARMAN_FLOOR`. That is the Phase-7 amend-twice pattern D-04 exists to avoid.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanically evaluate the abort criterion and draft the verdict document</name>
  <files>.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md</files>
  <read_first>
    - `11-RESEARCH.md` §C10(c) lines 873-897 — the numerical definition of "fails flat", the
      warning that the criterion must be on the **ladder span** rather than on every adjacent pair
      (the indicative data already shows a local plateau at 0.25 to 1.0 px), and the three branches.
    - `11-RESEARCH.md` risk R5 lines 1801-1807.
    - `11-RESEARCH.md` §C9b lines 811-828 — the indicative verdict, for context only; it is
      directional and must not be quoted as this phase's evidence.
    - `.planning/phases/11-.../11-05-SUMMARY.md` — the reported sweep tables and the
      ABORT-CRITERION FIRED / NOT FIRED line.
    - `spike/validation/p11_consts.jl` — the Tier-1 floors `P11_PROBE_S_FLOOR`,
      `P11_PROBE_SPAN_FLOOR`, the allowance `P11_ITERATION_ALLOWANCE`, and the Tier-2
      measurements `P11_PROBE_S_MEASURED`, `P11_PROBE_SPAN_MEASURED`, `SC2_SPEARMAN_FLOOR`.
    - `.planning/phases/07-productionization-conditional-on-go/gate-8x8-amended.md` — the
      verdict-first memo skeleton this document imitates.
  </read_first>
  <action>
Evaluate the criterion mechanically, in one `julia --project=spike` invocation that includes
`p11_consts.jl` and prints the comparison, so the verdict is computed from the frozen file rather
than transcribed by hand:
`fired = (P11_PROBE_S_MEASURED < P11_PROBE_S_FLOOR) || (P11_PROBE_SPAN_MEASURED < P11_PROBE_SPAN_FLOOR)`.

Write `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md`
in the `gate-8x8-amended.md` voice:

- A header block: run date, the probe artifact path and its `generated` stamp, the consts file and
  the commit it was frozen at, the reserved seed, and a one-line protocol statement ("one probe
  run, criterion evaluated against constants frozen before the run").
- **`## Verdict: BELOW RESOLUTION` or `## Verdict: ABOVE RESOLUTION`** — verdict FIRST, before any
  interpretation, in bolded uppercase.
- A comparison table with the measured value first and the pre-registered threshold in its own
  column, matching the house presentation: `| S_probe | <measured> (needs >= 0.9) |`,
  `| ladder span (drho_eq) | <measured> (needs >= 0.02) |`.
- `## What the probe measured` — the shift arm, the chromatic arm and the Delta-rho calibration
  fit for both imsize arms, reproduced from the 11-05 SUMMARY. Annotate the shift 0 to 0.25 step
  explicitly as **interpolation onset, not misalignment sensitivity** (at exactly integer offsets
  the backward map samples on-grid and the linear B-spline is an identity lookup), and state that
  this is precisely why the SC2 ladder starts at lambda = 0.25 and carries no lambda = 0 rung.
- `## Scope of the SC2 claim (ruling Q3)` — state in plain language that the ladder is a
  **registration ladder only**: the chromatic term is not lambda-scaled and carries no conditioning
  input, so its dose-response appears only in the SC3/D-14 fixed-injection curve. A reader must not
  take SC2 as covering chromatic misalignment.
- `## The three pre-registered branches` — reproduce branches 1, 2 and 3 from the context block
  above verbatim, each with its concrete consequence for the remaining plans (which plans run,
  which are skipped, what the report claims).
- `## Iteration ledger` — `P11_ITERATION_ALLOWANCE = 1`; iterations spent so far; iterations
  remaining; and the sentence that a second iteration is not authorised by the pre-registration
  file. Leave the decision line as `DECISION: pending` for now.
- `## What is forbidden under every branch` — widening the ladder a second time; relaxing
  `SC2_SPEARMAN_FLOOR`; re-running the probe on a different seed.

Do not select a branch in this task.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/validation/p11_consts.jl"); fired = (P11_PROBE_S_MEASURED < P11_PROBE_S_FLOOR) || (P11_PROBE_SPAN_MEASURED < P11_PROBE_SPAN_FLOOR); println("abort_criterion_fired=", fired)'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `abort_criterion_fired=true` or `abort_criterion_fired=false`, and that same boolean is reflected by the `## Verdict:` heading in `11-PROBE-VERDICT.md`.
    - `11-PROBE-VERDICT.md` contains a `## Verdict:` heading as the first `##` section, before any interpretation.
    - `grep -c 'P11_ITERATION_ALLOWANCE' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` is at least `1`.
    - The document contains all three branch descriptions: `grep -c 'PROCEED\|SPEND THE ITERATION\|STOP SC2' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` is at least `3`.
    - The document contains the strings `interpolation onset` and `registration ladder only`.
    - `grep -c 'DECISION: pending' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` outputs `1`.
  </acceptance_criteria>
  <done>The abort criterion is evaluated from the frozen constants rather than transcribed, the verdict leads the document, the interpolation-onset annotation and the registration-only scope boundary are stated, and the three pre-registered branches are laid out with their consequences.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Probe verdict — select the pre-registered branch</name>
  <decision>Which of the three pre-registered D-06 branches does Phase 11 take?</decision>
  <context>
The pre-flight probe has run once on the reserved stream. Its measured `S_probe` and ladder span
have been compared against the floors frozen before the run, and the verdict is recorded in
`.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md`
(read the `## Verdict:` heading and the comparison table there first).

What hangs on this: branch 1 authorises roughly 1.5 hours of CPU compute (about 1.2 h datagen at
50 000 pairs on the F5 mixture, plus about 20 minutes of NPE training). Branch 2 spends the single
declared D-04 iteration allowance on a re-parameterised probe instead — cheap, about 10 minutes,
and this is the cheapest possible place to spend it. Branch 3 concedes SC2 as not demonstrable at
the fixed 8x8 summary resolution and reroutes the remaining budget to SC3 and the docs note.

`P11_ITERATION_ALLOWANCE = 1` was declared in advance. Relaxing `SC2_SPEARMAN_FLOOR` or widening
the ladder a second time is forbidden under every branch — that is the Phase-7 amend-twice
pattern D-04 exists to avoid.
  </context>
  <options>
    <option id="proceed">
      <name>PROCEED — the criterion did not fire; authorise training</name>
      <pros>The summary is above resolution on both axes; the SC2 ladder has real headroom; the phase runs as planned and produces the full ladder, breakdown, attenuation and real-image evidence.</pros>
      <cons>Commits about 1.5 hours of CPU; if the criterion is marginal, the ladder may still come out weakly monotone.</cons>
    </option>
    <option id="spend-iteration">
      <name>SPEND THE ITERATION — re-parameterise the ladder once and re-run the probe</name>
      <pros>Costs about 10 minutes, not 1.5 hours; raising the maximum toward the D-14 extension range or leaning on the larger-image arms is a legitimate re-parameterisation of the ladder rather than of the criterion; spends the allowance at its cheapest point, before training.</pros>
      <cons>Consumes the single declared allowance; a second failure then forces branch 3 with no room left to manoeuvre.</cons>
    </option>
    <option id="stop-sc2">
      <name>STOP SC2 — report it as not demonstrable at the fixed 8x8 resolution</name>
      <pros>An honest negative with the probe curve as evidence; costs no training compute; is direct evidence FOR the deferred "summary extension for registration identifiability" idea; SC3 remains achievable because coverage does not require a resolvable width gradient.</pros>
      <cons>Phase 11 ships without its headline monotone-widening demonstration; the D-16 docs note becomes markedly thinner.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, spend-iteration, or stop-sc2 — and state the reason in one sentence for the ledger.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the decision, update the iteration ledger, and set the downstream authorisation</name>
  <files>.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md, .planning/STATE.md</files>
  <read_first>
    - The checkpoint response from Task 2 — the selected branch id and the stated reason.
    - `.planning/phases/11-.../11-PROBE-VERDICT.md` — the draft written in Task 1.
    - `.planning/STATE.md` — the `## Current Position` and `## Blockers/Concerns` sections and
      their existing entry style, so the new entry matches.
    - `11-RESEARCH.md` §C10(c) lines 880-897 — the exact consequences of each branch, so the
      recorded downstream authorisation matches the pre-registration.
  </read_first>
  <action>
Replace `DECISION: pending` in `11-PROBE-VERDICT.md` with the selected branch id, the developer's
one-sentence reason, the date, and the resulting downstream authorisation stated explicitly:

- **proceed** — "Plans 11-07 through 11-11 are authorised as planned. Iterations spent: 0 of 1."
- **spend-iteration** — "Plan 11-07 is BLOCKED. A single re-parameterised probe re-run is
  authorised; re-open plan 11-05 Task 2 with the re-parameterised ladder, append the re-measured
  Tier-2 values as a THIRD guard block (never editing the second), and return to this checkpoint.
  Iterations spent: 1 of 1 — no further iteration is authorised by the pre-registration."
- **stop-sc2** — "Plans 11-07, 11-08 and the SC2 portion of 11-11 are cancelled. Plan 11-09's SC3
  arm and plan 11-10's real-image arm cannot run without a research net, so they are cancelled
  too; the phase proceeds directly to plan 11-11, which reports SC2 as NOT DEMONSTRABLE at the
  fixed 8x8 summary resolution with the probe curve as evidence, states that SC3's coverage claim
  was not reachable without the research net, and links the finding to the deferred summary-
  extension idea. Iterations spent: 0 of 1, deliberately unspent."

Update the `## Iteration ledger` section accordingly.

Then update `.planning/STATE.md`:
- Add a dated line to `## Current Position` naming the probe verdict, the measured `S_probe` and
  span against their floors, the selected branch, and the downstream authorisation.
- If the branch is **spend-iteration** or **stop-sc2**, add a `## Blockers/Concerns` entry in the
  existing house style recording that Phase-11 training is blocked or cancelled and why, so a
  future session cannot mistake the absence of a trained net for an oversight.
- If the branch is **proceed**, add a one-line note that the roughly 1.5 hour datagen plus training
  spend is authorised and that `P11_DATAGEN_WALLCLOCK_CEILING_MIN = 150` is a BLOCKER threshold,
  not a downgrade trigger.

Commit both files.
  </action>
  <verify>
    <automated>grep -c 'DECISION: pending' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs `0` (the pending placeholder is gone).
    - `grep -cE 'DECISION: (proceed|spend-iteration|stop-sc2)' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` outputs `1`.
    - The `## Iteration ledger` section states iterations spent and remaining as explicit integers, and `grep -c 'of 1' .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` is at least `1`.
    - `.planning/STATE.md` contains a dated Phase-11 probe-verdict line naming the selected branch: `grep -c 'probe verdict' .planning/STATE.md` is at least `1`.
    - If the branch is not `proceed`, `.planning/STATE.md` `## Blockers/Concerns` gained an entry: `grep -c 'Phase 11' .planning/STATE.md` increased relative to the pre-task value.
    - Both files are committed: `git status --porcelain -- .planning/STATE.md .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` is empty.
  </acceptance_criteria>
  <done>The branch decision, its reason, the iteration ledger and the downstream authorisation are recorded in both the phase document and STATE.md, so no later session can mistake a deliberate stop for an oversight or spend a second, unauthorised iteration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registered criterion to human judgement | The criterion is frozen and mechanical; only the branch choice is discretionary. Blurring the two would make the abort branch unfalsifiable |
| decision record to future sessions | An unrecorded stop is indistinguishable from an oversight and invites an unauthorised second iteration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-24 | Repudiation | Unrecorded or ambiguous branch decision | mitigate | `11-PROBE-VERDICT.md` carries the branch id, the reason, the date and the explicit downstream authorisation; STATE.md carries a dated mirror and, on a non-proceed branch, a Blockers entry |
| T-11-25 | Tampering | Relaxing `SC2_SPEARMAN_FLOOR` or re-running the probe on a fresh seed to get a nicer verdict | mitigate | Both are named as forbidden under every branch in the document; `test_p11_consts.jl` re-asserts the Tier-1 literals, so a floor edit breaks the suite |
| T-11-26 | Elevation of privilege | Spending a second, unauthorised iteration | mitigate | The ledger states iterations spent and remaining as integers against the pre-registered `P11_ITERATION_ALLOWANCE = 1`; a re-run under branch 2 must append a THIRD guard block rather than edit the second, leaving the history auditable |
| T-11-SC | Tampering | package-manager installs | mitigate | No code and no packages; this plan only evaluates frozen constants and writes documents |
</threat_model>

<verification>
1. `julia --project=spike -e 'include("spike/validation/p11_consts.jl"); println((P11_PROBE_S_MEASURED < P11_PROBE_S_FLOOR) || (P11_PROBE_SPAN_MEASURED < P11_PROBE_SPAN_FLOOR))'` — the boolean matches the document's verdict heading.
2. `grep -cE 'DECISION: (proceed|spend-iteration|stop-sc2)' .../11-PROBE-VERDICT.md` outputs `1`.
3. `.planning/STATE.md` carries the dated verdict line and, on a non-proceed branch, a Blockers entry.
4. `git status --porcelain -- .planning/` is empty after the commit.
</verification>

<success_criteria>
- The abort criterion was evaluated from the frozen constants and its result leads the document.
- Exactly one of the three pre-registered branches is selected and recorded, with a reason.
- The iteration ledger states spent and remaining against `P11_ITERATION_ALLOWANCE = 1`.
- STATE.md reflects the decision so a future session cannot misread it.
- No training compute was spent before this record existed.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-06-SUMMARY.md`
when done, stating the selected branch and which downstream plans are authorised, blocked or
cancelled.
</output>

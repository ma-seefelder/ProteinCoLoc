---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - docs/amortized.md
  - spike/simulator/prior.jl
  - spike/simulator/forward.jl
  - src/amortized/simulator.jl
  - src/amortized/datagen.jl
  - src/amortized/ood.jl
  - test/runtests.jl
autonomous: true
requirements: [D-02, D-09, D-10, D-11, D-12, D-15]

must_haves:
  truths:
    - "(D-09) chromatic_eps is an 8th theta field drawn from Uniform(-0.02, 0.02) and reaches stage 6 in both simulators"
    - "(D-10) Stage 6 performs exactly ONE warp call with a single composed AffineMap, so only one interpolation pass occurs"
    - "(D-11) The epsilon extension is mirrored into src/amortized/simulator.jl; the widened SHIFT_PRIOR is NOT"
    - "(D-12) docs/amortized.md carries named limit #8 pinning the pre-epsilon commit sha, in the SAME commit as the simulator edit"
    - "(D-02) spike SHIFT_PRIOR is Uniform(-3.0, 3.0) and src SHIFT_PRIOR is still Uniform(-1.0, 1.0)"
    - "(D-15) simulate_pair accepts a forced theta carrying explicit shift/chromatic_eps values, so misalignment is simulator-injected"
    - "(D-11) A legacy 7-field theta still runs through both simulate_pair copies, so the shipped OOD posterior-predictive path cannot throw"
  artifacts:
    - path: "spike/simulator/prior.jl"
      provides: "CHROMATIC_PRIOR plus the 8th sample_prior field; widened SHIFT_PRIOR"
      contains: "CHROMATIC_PRIOR"
    - path: "spike/simulator/forward.jl"
      provides: "Single composed affine stage 6, re-derived WR-06 comment, chromatic_eps entry guards"
      contains: "recenter"
    - path: "src/amortized/simulator.jl"
      provides: "Mirrored CHROMATIC_PRIOR, 8-entry theta_prior_bounds(), composed affine stage 6, defensive epsilon read"
      contains: "chromatic_eps"
    - path: "src/amortized/datagen.jl"
      provides: "8-wide theta buffers and theta_dim = 8 in generating_config"
      contains: "theta_dim = 8"
    - path: "src/amortized/ood.jl"
      provides: "_theta_tuple emitting chromatic_eps only when the vector is long enough"
      contains: "chromatic_eps"
    - path: "docs/amortized.md"
      provides: "Named limit #8 — provenance-by-reference with the pinned pre-epsilon sha"
      contains: "8. **"
  key_links:
    - from: "spike/simulator/forward.jl"
      to: "src/amortized/simulator.jl"
      via: "behaviourally identical composed stage-6 warp"
      pattern: "Translation\\(.*shift_dy.*shift_dx.*\\) . recenter"
    - from: "docs/amortized.md"
      to: "git"
      via: "pinned pre-epsilon commit sha quoted in named limit 8"
      pattern: "git show"
    - from: "src/amortized/ood.jl"
      to: "src/amortized/simulator.jl"
      via: "_theta_tuple feeds simulate_pair on the posterior-predictive channel"
      pattern: "_theta_tuple"
---

<objective>
THE D-11/D-12 COMMIT. Add the 1-parameter radial chromatic scale `chromatic_eps` to both mirrored
simulators, replace the translation-only stage 6 with a single composed affine warp, propagate the
theta-arity change through `src/`, and land the `docs/amortized.md` named limit #8 pinning the
pre-epsilon sha — **all in one commit**.

Purpose: D-12 requires the provenance repair to ship with the provenance event. A follow-up
"fix the comment" commit would re-digest the training-data cache a second time and would leave a
window in which `src/` claims a training distribution it no longer reproduces.

Output: seven files changed, one commit, root suite green.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PATTERNS.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-01-SUMMARY.md

**Hard ordering constraints governing this plan:**
1. **The pre-epsilon sha is captured as the FIRST action of Task 1, before any edit.** It is
   `git rev-parse HEAD` at that moment, which must equal the first parent of the commit this plan
   produces. Capturing it after committing pins the epsilon commit itself and makes the
   provenance-by-reference claim false (RESEARCH Pitfall 6 / risk R9).
2. **Tasks 1 and 2 MUST NOT commit.** Only Task 3 commits, and it commits everything at once.
3. This plan runs only after plan 11-01 has committed the golden fixture. If
   `spike/test/fixtures/p11_stage6_golden.jld2` is absent, STOP — the D-10 regression check
   becomes unverifiable.

**Binding orchestrator rulings this plan implements** (do not reopen):
- **Q2** — D-11 mirrors **`chromatic_eps` ONLY**. `src/amortized/simulator.jl`'s `SHIFT_PRIOR`
  **stays at `Uniform(-1.0, 1.0)`**. Widening it would desynchronise `theta_prior_bounds()[5:6]`
  from the shipped bundle's frozen `theta_zt.lo`/`.hi` and would be a larger provenance event than
  D-12's named limit describes. There is an explicit acceptance criterion for this.
- **Q8** — `ROADMAP.md:126`'s literal decoupling claim ("`git status` on `src/` clean") is no
  longer available, by design. The narrower checkable claim (v1.0 manuscript-pipeline files
  byte-unchanged; the only `src/` change is inside `src/amortized/`) is adopted and is verified in
  plan 11-03. This plan must not attempt the literal claim.

**Never use a bare `eps` or Unicode-epsilon identifier in new code** — `THETA_LOGIT_EPS` already
owns that letter in `src/amortized/architecture.jl`. The field name is `chromatic_eps` everywhere.

**`chromatic_eps` appends at the END of theta** — never slotted next to `shift_dx`/`shift_dy` —
because `src/amortized/ood.jl:124-132` `_theta_tuple` indexes positionally `v[1]..v[7]`.

**Do NOT delete orphaned datagen caches.** They are the byte-level record of the shipped net's
training pool, exactly what D-12 preserves by reference.

<interfaces>
<!-- Extracted from the codebase. Use directly; do not go exploring. -->

`spike/simulator/forward.jl:168-174` (the stage being replaced):
```julia
    # --- (6) sub-pixel registration shift on channel 2 only (fillvalue > 0) ------
    # WR-06: Translation(a, b) shifts the FIRST array axis (rows = vertical = dy) by a
    # and the SECOND (columns = horizontal = dx) by b. Pass (dy, dx) so the named
    # fields map to their conventional physical axes (dx horizontal, dy vertical).
    shifted = warp(ch2, Translation(θ.shift_dy, θ.shift_dx), axes(ch2);
                   method = BSpline(Linear()), fillvalue = BG_FLOOR)
    ch2 = Matrix{Float64}(collect(shifted))
```

`spike/simulator/forward.jl:119-134` (the entry-guard wall to extend):
```julia
    all(isfinite, (θ.ρ_true, θ.spillover, θ.autofluorescence, θ.label_efficiency,
                   θ.shift_dx, θ.shift_dy, θ.noise)) ||
        throw(ArgumentError("all θ fields must be finite, got $θ"))
    (-1.0 ≤ θ.ρ_true ≤ 1.0) || throw(ArgumentError("ρ_true must be in [-1, 1], got $(θ.ρ_true)"))
    # WR-07: reject physically out-of-range nuisances rather than silently clamping
```

`spike/simulator/prior.jl:52-57` (the prior column to extend):
```julia
const SPILLOVER_PRIOR        = Uniform(0.0, 0.2)   # directional bleed-through (modest)
const AUTOFLUORESCENCE_PRIOR = Uniform(0.0, 0.1)   # additive background offset
const LABEL_EFFICIENCY_PRIOR = Uniform(0.6, 1.0)   # Bernoulli keep-probability
const SHIFT_PRIOR            = Uniform(-1.0, 1.0)  # sub-pixel registration error (dx, dy)
const NOISE_PRIOR            = Uniform(0.0, 1.0)   # Poisson+Gaussian noise scale
```

`src/amortized/ood.jl:124-132` (positional theta reconstruction — the shipped-compat hinge):
```julia
_theta_tuple(v) = (
    ρ_true           = clamp(_finite_or(v[1], 0.0), -1.0, 1.0),
    spillover        = clamp(_finite_or(v[2], 0.0),  0.0, 1.0),
    autofluorescence = max(_finite_or(v[3], 0.0), 0.0),
    label_efficiency = clamp(_finite_or(v[4], 1.0),  0.0, 1.0),
    shift_dx         = _finite_or(v[5], 0.0),
    shift_dy         = _finite_or(v[6], 0.0),
    noise            = max(_finite_or(v[7], 0.0), 0.0),
)
```

`src/amortized/datagen.jl:208-213` (the hashed source list — fixed order, do NOT reorder):
```julia
const DATAGEN_HASH_SRC_FILES = String[
    joinpath(@__DIR__, "..", "colocalization.jl"),
    joinpath(@__DIR__, "..", "LoadImages.jl"),
    joinpath(@__DIR__, "summary.jl"),
    joinpath(@__DIR__, "simulator.jl"),
]
```

The verified composed-affine expression (RESEARCH §A2, executed and measured bit-exact at
`chromatic_eps = 0`):
```julia
    s = 1.0 / (1.0 + θ.chromatic_eps)
    c = map(ax -> (first(ax) + last(ax)) / 2, axes(ch2))
    A = Translation(θ.shift_dy, θ.shift_dx) ∘ recenter(LinearMap([s 0.0; 0.0 s]), c)
    shifted = warp(ch2, A, axes(ch2); method = BSpline(Linear()), fillvalue = BG_FLOOR)
```
`LinearMap` and `recenter` come from `CoordinateTransformations` 0.6.4. `spike/simulator/forward.jl`
already does `using CoordinateTransformations` and needs no import change;
`src/amortized/simulator.jl:47` must become
`import CoordinateTransformations: Translation, LinearMap, recenter`. Use a plain
`[s 0.0; 0.0 s]` matrix — `LinearAlgebra` is deliberately NOT imported into `spike/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-epsilon sha, write named limit #8, and perform the spike-side surgery</name>
  <files>docs/amortized.md, spike/simulator/prior.jl, spike/simulator/forward.jl</files>
  <read_first>
    - `11-RESEARCH.md` §B8 lines 623-681 — the limit-#8 draft skeleton, the exact sha-capture
      commands, and the same-commit sequencing constraint.
    - `11-RESEARCH.md` §A2 lines 280-340 — the composed-affine expression, the backward-mode
      correction to WR-06, symbol provenance, and why `c` is a plain Tuple.
    - `11-RESEARCH.md` §A4 rows 1-6 lines 401-410 — the exact spike-side ripple sites.
    - `11-PATTERNS.md` §6 (prior.jl shape and the mirror trap), §7 (forward.jl stage spine, the
      ticket-prefix comment convention, the three-places trap), and the `docs/amortized.md`
      named-limit shape section (limit #4 verbatim plus the five-move voice decomposition).
    - `spike/simulator/prior.jl` — whole file.
    - `spike/simulator/forward.jl` — whole file, especially the header stage list at :24-45, the
      `θ is the 7-field NamedTuple (D-04)` line at :44-45, the guards at :110-134, stage 6 at
      :165-178, and the constants at :57-66.
    - `docs/amortized.md` lines 95-155 — the heading order, the Named-limits intro at :106-110,
      limit #4 at :128-135, limit #7, and the closing `**Findings and scripts:**` line at :152-154.
  </read_first>
  <action>
**Step 1 — capture the sha FIRST, before touching anything.** Run `git rev-parse HEAD` and
`git rev-parse --short HEAD`. Record both in the plan SUMMARY as `PRE_EPS_SHA`. Assert the tree
is clean (`git status --porcelain` empty) so the recorded sha genuinely is the parent of the
commit this plan will create.

**Step 2 — write named limit #8 in `docs/amortized.md`.** Append item `8.` to the numbered list
under `## Named limits (non-negotiable honesty — carry these into the manuscript)`, **after item
7 and before** the closing `**Findings and scripts:**` line. Write it in limit #4's exact voice —
bold lead clause naming the defect, then the measurement conditions, then the mechanism with
parenthetical evidence citations, then a bolded scope sentence saying what the limit is NOT, then
a closing classification sentence. Second person is never used; em-dashes carry the concessions;
emphasis is bold, never italics. Content: the shipped `grid_8` bundle's training distribution is
pinned by git reference rather than by the current tree; `src/amortized/simulator.jl` gained
`chromatic_eps` in Phase 11 (research lane, no reship); the shipped 8x8 net was trained by
`src/amortized/simulator.jl` **as of commit `<PRE_EPS_SHA>`**, before that parameter existed;
the current file is a strict superset whose `chromatic_eps = 0` behaviour is byte-identical and
regression-tested; because `simulator.jl` is a `DATAGEN_HASH_SRC_FILES` member
(`src/amortized/datagen.jl:208-213`), the edit re-digests the training-data cache directory and
orphans the pool the shipped net was trained from; **the artifact itself is unaffected** — it is
pinned by `git-tree-sha1` in `Artifacts.toml`, not by the datagen digest — so download,
verification and `colocalization_amortized(...)` are unchanged; the training distribution remains
exactly recoverable via `git show <PRE_EPS_SHA>:src/amortized/simulator.jl`. Close with: this is
a provenance-by-reference repair, **not** a change to the shipped estimator. Quote the full
40-character sha (a short sha is not a stable pin). Extend the closing
`**Findings and scripts:**` path list with the Phase-11 report path
`.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-REPORT.md`.
Add one further sentence to limit #8 recording the second, independent digest flip:
`theta_dim` moves 7 to 8 in the hashed `generating_config`, so the cache directory changes for two
reasons, not one. Do **not** add the D-16 interpretation section here — that is plan 11-11.

**Step 3 — `spike/simulator/prior.jl`.** Add
`const CHROMATIC_PRIOR = Uniform(-0.02, 0.02)` immediately after `SHIFT_PRIOR`, column-aligned,
with the trailing rationale comment naming D-09: a radial magnification difference between
channels, zero at the image centre and maximal at the corners; symmetric because channel ordering
is the user's choice, not the model's. Change `SHIFT_PRIOR` to `Uniform(-3.0, 3.0)` and add an
explicit comment on that line stating it **deliberately diverges from `src/amortized/simulator.jl`,
which stays at ±1 px** (D-02 scopes the widening to the research net; D-11 mirrors only
`chromatic_eps`) — the PATTERNS §6 trap. Add `chromatic_eps = rand(rng, CHROMATIC_PRIOR)` as the
**last** field of the `sample_prior` NamedTuple, after `noise`, and update the docstring's
"7-field D-04 NamedTuple" wording to 8-field with a note that `chromatic_eps` is appended at the
end because downstream code indexes theta positionally.

**Step 4 — `spike/simulator/forward.jl`.** Three coordinated edits plus the stage replacement:
- Header stage list at :24-45: update the `(6)` one-liner to describe the composed affine
  (radial chromatic scale composed with the registration shift, one warp), and update the
  `θ is the 7-field NamedTuple (D-04)` line to 8-field.
- Entry guards at :119-134: add `θ.chromatic_eps` to the `all(isfinite, (...))` tuple **read
  defensively** so a legacy 7-field theta still validates, and add a new WR-07-style guard
  `(-1.0 < eps_value) || throw(ArgumentError("chromatic_eps must be > -1 (so 1 + eps stays positive), got ..."))`
  placed in theta field order (last, after the `noise` guard). The `-1 < eps` bound is a
  correctness requirement, not a style choice: `s = 1/(1 + eps)` must stay finite and positive, and
  a non-positive `1 + eps` mirrors or collapses the image (ASVS V5, threat T-11-07).
- Stage 6 at :165-178: replace the `Translation`-only `warp` with the single composed `AffineMap`
  from the interfaces block above, reading `chromatic_eps` through the same defensive local
  binding used by the guard (`chromatic_eps_val = hasproperty(θ, :chromatic_eps) ? θ.chromatic_eps : 0.0`,
  computed once near the top of the function alongside the guards, so SC1e backward compatibility
  is structural rather than repeated). Keep the `WR-06` ticket tag and **re-derive** its body per
  D-10: state that `warp` is BACKWARD-mode (`out[I] = ch2[A(I)]`, ImageTransformations
  `warp.jl:4` and `:167-176`), so `A` maps a destination index to the source index it samples and
  the image content undergoes the inverse; that the `Translation` slot order is therefore
  unchanged (slot 1 = rows = dy, slot 2 = cols = dx); and that to MAGNIFY channel 2's content by
  `(1 + eps)` about the image centre the backward map must SHRINK coordinates, hence
  `s = 1/(1 + eps)`. Note that `recenter(t, c)` equals `Translation(c) . t . Translation(-c)`
  (CoordinateTransformations `core.jl:103-105`) and that the composition collapses to ONE
  `AffineMap` (`affine.jl:139-165`), so `warp` performs exactly one interpolation pass — the D-10
  correctness requirement, because every resampling pass smooths and smoothing decorrelates.

**Do not commit in this task.**
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/simulator/prior.jl"); using Random123; t = sample_prior(Philox4x(UInt64,(UInt64(1),UInt64(0)))); @assert length(keys(t)) == 8; @assert last(keys(t)) == :chromatic_eps; @assert minimum(SHIFT_PRIOR) == -3.0; @assert (minimum(CHROMATIC_PRIOR), maximum(CHROMATIC_PRIOR)) == (-0.02, 0.02); println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`.
    - `PRE_EPS_SHA` (40 characters) is recorded in the plan SUMMARY and appears verbatim in `docs/amortized.md`: `grep -c "$PRE_EPS_SHA" docs/amortized.md` outputs at least `1`.
    - `docs/amortized.md` contains a list item beginning `8. **` between item 7 and the `**Findings and scripts:**` line: `grep -n '^8\. \*\*' docs/amortized.md` returns exactly one line, and its line number is greater than the line number of `^7\. \*\*` and less than that of `**Findings and scripts:**`.
    - `grep -v '^#' spike/simulator/forward.jl | grep -c 'warp(' ` outputs `1` (exactly one warp call remains in executable code).
    - `grep -v '^ *#' spike/simulator/forward.jl | grep -c 'Translation(θ.shift_dy, θ.shift_dx), axes(ch2)'` outputs `0` (the translation-only call is gone).
    - `grep -c 'recenter' spike/simulator/forward.jl` is at least `1` and `grep -c 'LinearAlgebra' spike/simulator/forward.jl` outputs `0`.
    - `grep -c 'chromatic_eps' spike/simulator/prior.jl` is at least `2`.
    - `julia --project=spike -e 'include("spike/simulator/forward.jl"); using Random123; t7 = (ρ_true=0.5, spillover=0.1, autofluorescence=0.05, label_efficiency=0.8, shift_dx=0.3, shift_dy=-0.2, noise=0.5); o = simulate_pair(Philox4x(UInt64,(UInt64(7),UInt64(0))), t7); @assert length(o) == 2; println("legacy 7-field ok")'` prints `legacy 7-field ok`.
    - `git log --oneline -1` is unchanged from the start of the task (nothing committed yet).
  </acceptance_criteria>
  <done>The pre-epsilon sha is recorded, named limit #8 is written against it in the house voice, the spike prior carries `CHROMATIC_PRIOR` and the widened `SHIFT_PRIOR` with its divergence comment, and stage 6 is a single composed affine warp with a re-derived backward-mode WR-06 comment. Nothing is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the epsilon extension into src/amortized and propagate the theta-arity ripple</name>
  <files>src/amortized/simulator.jl, src/amortized/datagen.jl, src/amortized/ood.jl</files>
  <read_first>
    - `11-RESEARCH.md` §A4 rows 7-23 lines 409-425 — the complete src-side ripple enumeration with
      exact line numbers.
    - `11-RESEARCH.md` §A5 lines 433-516 — why the 8th theta column does NOT break the shipped
      bundle load (`persist.jl:97` reads `a.D` off disk; `theta_zt` is frozen data), the mandatory
      defensive-read mitigation, and the table of what must NOT cross the spike/src boundary.
    - `11-RESEARCH.md` §B6 lines 521-583 — what the datagen content hash covers, the double
      digest flip, and the explicit instruction NOT to prune orphaned caches.
    - `11-RESEARCH.md` risks R2 and R3 lines 1775-1790.
    - `11-PATTERNS.md` §8 (the src banner/docstring style and the derive-never-literal rule) and
      §9 (the positional `_theta_tuple` trap and the do-not-reorder rule).
    - `src/amortized/simulator.jl` — whole file, especially the banner at :19-40, imports at
      :42-48, priors at :89-95, `sample_prior` at :109-120, `theta_prior_bounds` docstring and
      body at :122-148, constants at :152-156, guards at :186-202, stage 6 at :234-238, and
      `build_mci` at :260-270.
    - `src/amortized/datagen.jl` lines 130-180, 265-285, 385-400, and 199-250.
    - `src/amortized/ood.jl` lines 118-135 and 160-175.
    - `src/amortized/architecture.jl` lines 165-210 — confirm `NPE_D = 7` and `build_estimator`'s
      `D` default, which this task must NOT change.
  </read_first>
  <action>
Mirror the extension into `src/amortized/simulator.jl`, matching the `src/amortized/*` house style
(`#####` MIT banner, `import Pkg: name1, name2` explicit imports, ALL-CAPS labelled paragraphs,
docstrings with a signature line then prose then a bolded invariant):

- Change the import at :47 to `import CoordinateTransformations: Translation, LinearMap, recenter`.
- Add `const CHROMATIC_PRIOR = Uniform(-0.02, 0.02)` after `SHIFT_PRIOR` (:94) with the D-09/D-11
  rationale comment. **Leave `SHIFT_PRIOR` at `Uniform(-1.0, 1.0)`** and add a comment on that
  line stating that the spike's widened `Uniform(-3, 3)` is deliberately NOT mirrored (ruling Q2 /
  D-02 scopes the widening to the research net; mirroring it would desynchronise
  `theta_prior_bounds()[5:6]` from the shipped bundle's frozen `theta_zt.lo`/`.hi`).
- Add `chromatic_eps = rand(rng, CHROMATIC_PRIOR)` as the **last** field of `sample_prior`.
- Append `(minimum(CHROMATIC_PRIOR), maximum(CHROMATIC_PRIOR))` as the 8th row of
  `theta_prior_bounds()` — **derived, never literal** — and update the docstring's field-order
  prose and its `NTuple{7,...}` type mention to 8.
- Extend the entry guards at :186-202 with the same defensive `chromatic_eps` read and the
  `-1 < chromatic_eps` bound used in the spike, keeping the src comment style.
- Replace stage 6 at :234-238 with the identical composed `AffineMap`, keeping the abridged src
  WR-06 comment form but re-derived for backward mode and the scale centre.
- Update every "7-field D-04 NamedTuple" occurrence in the file's header and docstrings to 8-field
  (PATTERNS §8 flags three such places). Do this now, in this commit — a follow-up comment-only
  touch would re-digest the cache a second time.

`src/amortized/datagen.jl`:
- `:170` `Matrix{Float64}(undef, 7, N)` and `:393` `Matrix{Float64}(undef, 7, H)` become the
  derived length. Prefer `length(theta_prior_bounds())` over a literal `8` so Phase 12/13 does not
  repeat this chore; if the call site cannot see that function, use a local
  `const DATAGEN_THETA_DIM = length(theta_prior_bounds())` defined near the top of the file with a
  comment naming it as the derived single source of truth.
- `:274` `theta_dim = 7,` inside `generating_config` becomes the same derived value. Note in a
  comment that this is a **hashed config field**, so it re-digests the cache directory
  independently of the source-byte change.
- Update the "7-vector" / "theta::Matrix 7xN" docstrings at :132 and :157.
- **Do NOT reorder `DATAGEN_HASH_SRC_FILES`** (the fixed order is the hash input) and **do NOT
  delete any orphaned cache directory** — add a short comment near `open_or_invalidate` recording
  that orphaned caches are retained, not pruned, because they are the byte-level fallback if the
  git-pinned reconstruction is ever disputed.

`src/amortized/ood.jl`:
- Extend `_theta_tuple(v)` (:124-132) with an 8th entry appended at the **end**:
  `chromatic_eps = length(v) >= 8 ? clamp(_finite_or(v[8], 0.0), -0.999, 1.0) : 0.0,` — the
  `length(v) >= 8` guard is what keeps the shipped 7-row posterior-mean path working, and the
  lower clamp mirrors `simulate_pair`'s `-1 < chromatic_eps` entry guard. Update the docstring at
  :121-123 to list the 8 ranges. Do **not** insert the field next to `shift_dx`/`shift_dy`.

**Do NOT change `src/amortized/architecture.jl`'s `NPE_D = 7`, and do NOT change
`src/amortized/train_npe.jl:136`.** The research net is trained by a spike-local trainer (plan
11-07); changing the src training path's `D` is out of scope and would alter the shipped
`build_estimator` default. Verify by grep that neither file is in this plan's diff.

**Do not commit in this task.**
  </action>
  <verify>
    <automated>julia --project -e 'using ProteinCoLoc; b = ProteinCoLoc.theta_prior_bounds(); @assert length(b) == 8; @assert b[8] == (-0.02, 0.02); @assert (minimum(ProteinCoLoc.SHIFT_PRIOR), maximum(ProteinCoLoc.SHIFT_PRIOR)) == (-1.0, 1.0); v7 = [0.5,0.1,0.05,0.8,0.3,-0.2,0.5]; t = ProteinCoLoc._theta_tuple(v7); @assert t.chromatic_eps == 0.0; println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`.
    - `julia --project -e 'using ProteinCoLoc; println(length(ProteinCoLoc.theta_prior_bounds()))'` outputs `8`.
    - `grep -c 'SHIFT_PRIOR *= *Uniform(-1.0, 1.0)' src/amortized/simulator.jl` outputs `1` (ruling Q2 — the src shift prior is untouched).
    - `grep -v '^ *#' src/amortized/simulator.jl | grep -c 'warp('` outputs `1`.
    - `grep -c 'undef, 7,' src/amortized/datagen.jl` outputs `0` and `grep -c 'theta_dim *= *7' src/amortized/datagen.jl` outputs `0`.
    - `grep -c 'NPE_D = 7' src/amortized/architecture.jl` outputs `1` and `git diff --name-only -- src/amortized/architecture.jl src/amortized/train_npe.jl` is empty.
    - `git diff --name-only -- src/ | grep -v '^src/amortized/'` is empty (ruling Q8 — only the v2.0 subtree is touched).
    - `git status --porcelain -- artifacts/` shows no deletions (no orphaned cache pruned).
    - `git log --oneline -1` is unchanged from the start of the task (nothing committed yet).
  </acceptance_criteria>
  <done>`src/amortized/simulator.jl` mirrors `chromatic_eps` and the composed warp while keeping `SHIFT_PRIOR` at ±1; `theta_prior_bounds()` returns 8 derived rows; datagen's theta buffers and hashed `theta_dim` follow the derived arity; `_theta_tuple` emits the 8th field only when the vector is long enough, so the shipped posterior-predictive path cannot throw. Nothing is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Update the root suite's theta-arity literals, add the epsilon testset, and land the single commit</name>
  <files>test/runtests.jl</files>
  <read_first>
    - `11-RESEARCH.md` §A4 row 24 line 426 — the exact list of literal-7 sites:
      `:495, :507, :513, :517, :535, :550, :556, :558, :562, :624, :700, :734, :796, :811, :813, :1185, :1399`.
    - `11-RESEARCH.md` risk R1 lines 1766-1773 — why this is certain, cheap, and must be planned.
    - `11-PATTERNS.md` §10 — the banner-per-testset convention and the warning that `:556` and
      `:562` are **matrix constructions**, not assertions, and will throw `DimensionMismatch` if
      only the assertions are updated.
    - `test/runtests.jl` — read each cited line's surrounding context before editing it; some
      sevens in the file are unrelated to theta arity and must be left alone.
    - `test/runtests.jl:23-32` — the `####`-bordered rationale-banner style for a new testset.
    - `test/runtests.jl:534-563` — the existing theta-bounds / `collect(values(...))` testset that
      the new epsilon testset sits beside.
  </read_first>
  <action>
**Step 1 — replace the theta-arity literals.** At each of the cited sites, and at any other site a
`grep -n 'theta_dim\|== 7\|randn(7\|, 7)\|(7,' test/runtests.jl` sweep reveals to be theta-shaped,
replace the literal with `length(ProteinCoLoc.theta_prior_bounds())` — the declared single source
of truth — rather than retyping `8`. Bind it once per testset to a local
(`D = length(ProteinCoLoc.theta_prior_bounds())`) where that reads better than repeating the call.
Treat `:556` (`wild = 50.0 .* randn(7, 500)`) and `:562` (`repeat(collect(-6.0:0.5:6.0)', 7, 1)`)
as **matrix constructions**: they must become the derived length too, or `reconstruct` throws
`DimensionMismatch`. Leave any seven that is not a theta dimension untouched — check each site's
context before changing it.

**Step 2 — add the SC1b testset.** Insert a new top-level testset preceded by a `####`-bordered
`###`-prefixed rationale banner naming D-09, beside the existing theta-bounds testset:
`@testset "chromatic ε (D-09)"` asserting:
- `length(ProteinCoLoc.theta_prior_bounds()) == 8`
- `ProteinCoLoc.theta_prior_bounds()[8] == (minimum(ProteinCoLoc.CHROMATIC_PRIOR), maximum(ProteinCoLoc.CHROMATIC_PRIOR))`
  — the derive-never-literal rule
- a `sample_prior` draw on a fixed `Philox4x` key has 8 fields with `:chromatic_eps` **last**, and
  its value lies in `[-0.02, 0.02]`
- `collect(values(sample_prior(rng)))` has length 8 and its 5th and 6th entries are still the
  shift fields (append-at-end contract)
- `ProteinCoLoc._theta_tuple` on a 7-long vector yields `chromatic_eps == 0.0`, and on an 8-long
  vector yields the 8th entry clamped into range — the shipped-compat guard
- `simulate_pair` on an explicit legacy 7-field theta runs and returns two matrices (SC1e at the
  src copy)
- the stage-6 transform is a single `AffineMap`: construct
  `A = Translation(0.3, -0.2) ∘ recenter(LinearMap([1.0 0.0; 0.0 1.0]), (128.5, 128.5))` with the
  same symbols the simulator imports and assert
  `A isa CoordinateTransformations.AffineMap` (SC1c — not a `ComposedTransformation`)
- a non-finite `chromatic_eps` and a `chromatic_eps <= -1.0` both throw `ArgumentError` from
  `simulate_pair` (ASVS V5 / threat T-11-07)

**Step 3 — run the full suite and land the commit.** Run
`julia --project -t auto -e 'using Pkg; Pkg.test()'` and fix any remaining arity breakage until it
is green. Then stage **every** file this plan touched —
`docs/amortized.md spike/simulator/prior.jl spike/simulator/forward.jl src/amortized/simulator.jl
src/amortized/datagen.jl src/amortized/ood.jl test/runtests.jl` — and create **exactly one**
commit. Message: `feat(11-02): chromatic ε as an 8th θ column + single composed affine stage 6 (D-09/D-10/D-11) + named limit #8 (D-12)`.
No `--no-verify`. Record the resulting commit sha in the plan SUMMARY as `EPS_COMMIT_SHA`
alongside `PRE_EPS_SHA`, and assert immediately afterwards that
`git rev-parse "<EPS_COMMIT_SHA>^"` equals `PRE_EPS_SHA`. If it does not, the sha in
`docs/amortized.md` is wrong — amend the docs text and `git commit --amend` so the pin is correct
before moving on. Do not create a second commit to fix it.
  </action>
  <verify>
    <automated>julia --project -t auto -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project -t auto -e 'using Pkg; Pkg.test()'` exits 0.
    - `git rev-parse "$(git rev-parse HEAD)^"` equals the `PRE_EPS_SHA` recorded in Task 1, and equals the sha quoted in `docs/amortized.md`.
    - `git show --stat HEAD --name-only` lists exactly these seven paths: `docs/amortized.md`, `spike/simulator/prior.jl`, `spike/simulator/forward.jl`, `src/amortized/simulator.jl`, `src/amortized/datagen.jl`, `src/amortized/ood.jl`, `test/runtests.jl`.
    - `git show "$PRE_EPS_SHA":src/amortized/simulator.jl | grep -c chromatic_eps` outputs `0`.
    - `grep -v '^ *#' test/runtests.jl | grep -c 'randn(7,'` outputs `0`.
    - `grep -c 'chromatic' test/runtests.jl` is at least `1` and `julia --project -t auto -e 'using Pkg; Pkg.test()' 2>&1 | grep -c "chromatic"` shows the new testset ran.
    - `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- src/bayes.jl src/colocalization.jl src/LoadImages.jl src/plot.jl` is empty.
    - `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- Artifacts.toml test/test_images/` is empty.
  </acceptance_criteria>
  <done>The root suite is green with theta arity derived from `theta_prior_bounds()` rather than literals; the SC1b/SC1c/SC1e assertions exist in the root suite; and one commit carries the epsilon edit together with the D-12 named limit whose pinned sha is provably that commit's first parent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted theta vector to `simulate_pair` | `_theta_tuple` reconstructs theta from a posterior-mean vector that may carry non-finite or out-of-range components (`T-02-IV`) |
| repo source bytes to datagen content hash | `simulator.jl` bytes name the training-cache directory; an unrecorded edit severs training-distribution provenance |
| `Artifacts.toml` pin to shipped bundle load | `_verify_tree_sha1` gates every artifact load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering / DoS | `chromatic_eps` reaching stage 6 | mitigate | Entry guards in **both** `simulate_pair` copies: `isfinite(chromatic_eps)` joins the existing `all(isfinite, ...)` wall and a new `-1 < chromatic_eps` bound keeps `s = 1/(1 + eps)` finite and positive; a non-positive `1 + eps` would mirror or collapse the image. Asserted by the new root-suite testset (ASVS V5) |
| T-11-08 | Tampering | `_theta_tuple` feeding a 7-row vector to an 8-field `simulate_pair` | mitigate | `length(v) >= 8` guard plus a defensive `hasproperty(θ, :chromatic_eps)` read, so a legacy 7-field theta is a valid input meaning "no chromatic aberration" and is byte-identical to today (RESEARCH §A3/§A5) |
| T-11-09 | Repudiation | Training-distribution provenance severed by the hashed-source edit | mitigate | Named limit #8 pinning the full 40-character pre-epsilon sha, landed in the SAME commit; verified by `git rev-parse "<EPS>^"` and `git show "<SHA>":src/amortized/simulator.jl \| grep -c chromatic_eps` returning 0 |
| T-11-10 | Tampering | Orphaned datagen caches deleted, destroying the byte-level record | mitigate | Explicit do-not-prune instruction plus an in-file comment near `open_or_invalidate`; acceptance criterion asserts no deletion under `artifacts/` |
| T-11-11 | Tampering | Shipped artifact integrity weakened | accept (no change made) | `Artifacts.toml` and `src/registry.jl:101-108` `_verify_tree_sha1` are untouched; acceptance criterion asserts `git diff --stat ... -- Artifacts.toml` is empty (ASVS V6) |
| T-11-12 | Spoofing | Stale cache read as current | mitigate | Unchanged `open_or_invalidate` digest-mismatch error (`datagen.jl:368-385`); the double digest flip (source bytes plus `theta_dim`) auto-separates the new pool |
| T-11-SC | Tampering | package-manager installs | mitigate | No packages installed; `LinearAlgebra` deliberately avoided in `spike/` by using a plain `[s 0.0; 0.0 s]` matrix. Acceptance criteria assert no dependency-manifest change |
</threat_model>

<verification>
1. `julia --project -t auto -e 'using Pkg; Pkg.test()'` — exit 0.
2. `julia --project=spike spike/test/test_p11_forced_theta.jl` — still exit 0 after the arity change.
3. `git show --name-only HEAD` lists exactly the seven expected paths in one commit.
4. `git rev-parse "$(git rev-parse HEAD)^"` equals `PRE_EPS_SHA` and equals the sha in `docs/amortized.md`.
5. `git show "$PRE_EPS_SHA":src/amortized/simulator.jl | grep -c chromatic_eps` outputs `0`.
6. `grep -c 'SHIFT_PRIOR *= *Uniform(-1.0, 1.0)' src/amortized/simulator.jl` outputs `1`.
7. `git diff --name-only "$PHASE11_BASE_SHA"..HEAD -- src/ | grep -v '^src/amortized/'` is empty.
</verification>

<success_criteria>
- One commit contains all seven files; its first parent is the sha quoted in `docs/amortized.md`.
- `ProteinCoLoc.theta_prior_bounds()` returns 8 entries, the 8th derived from `CHROMATIC_PRIOR`.
- Both `simulate_pair` copies contain exactly one `warp` call in executable code.
- `src/amortized/simulator.jl` still declares `SHIFT_PRIOR = Uniform(-1.0, 1.0)`;
  `spike/simulator/prior.jl` declares `Uniform(-3.0, 3.0)` with the divergence comment.
- The full root suite is green; the new `@testset "chromatic ε (D-09)"` runs and passes.
- No file outside `src/amortized/` changed inside `src/`; `Artifacts.toml` and `test/test_images/`
  are byte-unchanged; no orphaned cache directory was deleted.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-02-SUMMARY.md`
when done, recording `PRE_EPS_SHA` and `EPS_COMMIT_SHA` and the verified parent relationship.
</output>

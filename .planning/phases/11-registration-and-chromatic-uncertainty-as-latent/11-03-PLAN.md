---
phase: 11-registration-and-chromatic-uncertainty-as-latent
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - spike/test/test_stage6_regression.jl
  - .planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md
autonomous: true
requirements: [D-01, D-10, D-11, D-12, D-16]

must_haves:
  truths:
    - "(D-10) At chromatic_eps = 0 the composed warp is bit-for-bit identical to the legacy Translation warp, proven with exact == and not isapprox"
    - "(D-10) The full simulate_pair pipeline at chromatic_eps = 0 reproduces the pre-edit golden fixture exactly"
    - "(D-11) simulate_pair still accepts a legacy 7-field theta and returns bit-identical output"
    - "(D-12) The sha pinned in docs/amortized.md is provably the first parent of the epsilon commit and its simulator.jl contains no chromatic_eps"
    - "(D-01) The shipped amended_v2/grid_8 bundle still loads and colocalization_amortized(...) still runs after the src edit"
    - "(D-16) No public API changed and no artifact changed; Artifacts.toml and the v1.0 manuscript pipeline files are byte-unchanged"
  artifacts:
    - path: "spike/test/test_stage6_regression.jl"
      provides: "SC1c / SC1d / SC1e exact-equality regression tests against the pre-edit golden"
      contains: "p11_stage6_golden.jld2"
      min_lines: 60
    - path: ".planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md"
      provides: "Recorded output of the D-12 sha checks, the five F20c decoupling commands, and the shipped-bundle regression"
      contains: "PRE_EPS_SHA"
  key_links:
    - from: "spike/test/test_stage6_regression.jl"
      to: "spike/test/fixtures/p11_stage6_golden.jld2"
      via: "JLD2 load then exact == against simulate_pair output"
      pattern: "JLD2"
    - from: ".planning/phases/11-.../11-PROVENANCE.md"
      to: "docs/amortized.md"
      via: "the pinned sha quoted in named limit 8"
      pattern: "PRE_EPS_SHA"
---

<objective>
Prove that the D-11/D-12 commit changed nothing it was not supposed to change: the
`chromatic_eps = 0` path is bit-identical to the pre-edit simulator, the pinned provenance sha is
the right one, the shipped bundle still loads and still answers, and the v1.0 manuscript pipelines
are byte-unchanged.

Purpose: this is the single highest-consequence risk in the phase. RESEARCH §A5 argues from code
that the 8th theta column cannot break the shipped bundle load (`persist.jl:97` reads `a.D` off
disk; `theta_zt` is frozen data), but an argument is not a regression test.

Output: `spike/test/test_stage6_regression.jl` and a recorded provenance/decoupling document.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-CONTEXT.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PATTERNS.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-01-SUMMARY.md
@.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-02-SUMMARY.md

**Binding orchestrator ruling implemented here — Q8.** `ROADMAP.md:126` defines the decoupling
proof as `git status` clean on `src/`, `bayes.jl`, `colocalization.jl`. D-11 deliberately edits
`src/amortized/simulator.jl`, so that literal claim is no longer available. Adopt the **narrower,
checkable claim**: the v1.0 manuscript-pipeline files are byte-unchanged, and the only `src/`
change is inside `src/amortized/`, being exactly D-11 with D-12's named limit. The provenance
document must quote ROADMAP.md:126's literal claim, state plainly that D-11 makes it unavailable,
adopt the narrower wording, and print the output of all five §F20c verification commands.
Honesty-by-naming, consistent with the existing named-limits mechanism.

**Exactness is not negotiable (risk R8).** RESEARCH §A3 measured `==` holding bit-for-bit across
twelve independent Philox keys and four shift pairs. A failure here is a genuine signal — for
example a StaticArrays or Interpolations reduction-order change — not a nuisance to be smoothed
away with a tolerance. If and only if exact equality fails, the pre-registered fallback is
`maximum(abs.(a .- b)) <= P11_STAGE6_TOLERANCE_FALLBACK` (1e-13), and using it **must** be recorded
as a deviation in the provenance document and carried into the Phase-11 report. Do not silently
switch to `isapprox`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the stage-6 exact-equality regression test</name>
  <files>spike/test/test_stage6_regression.jl</files>
  <read_first>
    - `11-RESEARCH.md` §A3 lines 341-398 — the T1 (no fixture) and T2 (golden fixture) test
      specifications, the measured equality evidence, and the tolerance-fallback policy.
    - `11-RESEARCH.md` risk R8 lines 1841-1845.
    - `11-VALIDATION.md` rows SC1c, SC1d, SC1e.
    - `11-PATTERNS.md` §3 — the `test_simulator.jl` testset shape, the `const theta_BASE` plus
      `merge` idiom, the explicit-RNG-constructor rule, and the do-not-soften-to-isapprox trap.
    - `spike/test/test_simulator.jl:90-115` — the analog testset.
    - `spike/test/capture_p11_golden.jl` — the exact theta literal, key derivation and JLD2 key
      names the golden was written with; the test must mirror them precisely.
    - `spike/simulator/forward.jl` — the post-edit stage 6 and constants (`BG_FLOOR`), so the T1
      comparison uses the same `method` and `fillvalue`.
    - `spike/validation/p11_consts.jl` — `P11_STAGE6_EXACT`, `P11_STAGE6_TOLERANCE_FALLBACK`,
      `P11_FIXTURE_COUNTER`, `p11_rng`.
  </read_first>
  <action>
Create `spike/test/test_stage6_regression.jl`: AGPL header,
`# spike/test/test_stage6_regression.jl --- D-10 stage-6 regression (SC1c / SC1d / SC1e).`, an
ALL-CAPS paragraph naming exact `==` as the pre-registered criterion and pointing at
`P11_STAGE6_TOLERANCE_FALLBACK` as a deviation-requiring escape hatch, then the literal run line.
Guarded includes in order: `p11_consts.jl`, then `spike/simulator/prior.jl`, then
`spike/simulator/forward.jl`. `using Test, JLD2, Random, Random123, CoordinateTransformations,
ImageTransformations, Interpolations`.

Outer `@testset "D-10 stage-6 regression" verbose = true` with these nested testsets:

- **`"SC1c: the stage-6 transform is a SINGLE AffineMap"`** — build
  `A = Translation(0.37, -0.82) ∘ recenter(LinearMap([1.0 0.0; 0.0 1.0]), (128.5, 128.5))` and
  assert `A isa CoordinateTransformations.AffineMap` and
  `!(A isa CoordinateTransformations.ComposedTransformation)`. Also assert, by reading the source
  text of `spike/simulator/forward.jl` with `read(..., String)` and stripping comment lines, that
  the substring `warp(` occurs exactly once — the one-interpolation-pass requirement is a
  source-level property, so assert it at source level.
- **`"SC1d(a): composed warp at chromatic_eps = 0 == legacy Translation warp"`** — build a fixed
  `X = rand(Random.Xoshiro(2026), 256, 256) .+ BG_FLOOR` and, for
  `(dy, dx) ∈ ((0.0, 0.0), (0.37, -0.82), (2.5, 1.25), (-3.0, 3.0))`, compare
  `warp(X, Translation(dy, dx) ∘ recenter(LinearMap([1.0 0.0; 0.0 1.0]), c), axes(X); method = BSpline(Linear()), fillvalue = BG_FLOOR)`
  against `warp(X, Translation(dy, dx), axes(X); method = BSpline(Linear()), fillvalue = BG_FLOOR)`
  with `@test collect(a) == collect(b)` — exact `==`, never `isapprox`. `c` is computed with
  `map(ax -> (first(ax) + last(ax)) / 2, axes(X))`, never a literal 128.5 and never `size(X)/2`.
- **`"SC1d(b): full simulate_pair == the PRE-EDIT golden fixture"`** — load
  `spike/test/fixtures/p11_stage6_golden.jld2`; assert its `theta` has **no** `:chromatic_eps` key
  (a fixture regenerated after the edit would fail this) and that its `phase11_base_sha` is a
  40-character string; then for each stored key index rebuild the same
  `p11_rng(P11_FIXTURE_COUNTER + k)` and call
  `simulate_pair(rng, merge(golden_theta, (chromatic_eps = 0.0,)); imsize = golden_imsize)`,
  asserting `==` on **both** channel matrices against the stored bytes. Add a comment stating that
  `merge` appends `chromatic_eps` at the end, preserving `collect(values(θ))` row order.
- **`"SC1e: backward compatibility with a legacy 7-field theta"`** — call `simulate_pair` with the
  stored 7-field `theta` **unmodified** and assert the result is `==` to both the golden bytes and
  to the `chromatic_eps = 0.0` result from the previous testset. This is what proves the defensive
  read means exactly "no chromatic aberration".
- **`"entry guards reject a hostile chromatic_eps"`** — assert `simulate_pair` throws
  `ArgumentError` for `chromatic_eps = NaN`, `Inf`, and `-1.0` (the `1 + eps` collapse boundary),
  and does **not** throw for `-0.5` and `+0.5`. Both directions, per the `test_sbc.jl:63-68`
  discipline.
- Close with the house `@testset "... ran CPU-only (D-10)"` CUDA-absence clause.

If any exact-equality assertion fails, do **not** relax it in the test. Stop, record the measured
`maximum(abs.(a .- b))`, and report it as a blocker in the plan SUMMARY — RESEARCH §A3 establishes
this equality is achieved, so a failure means the composed expression or the import set differs
from the verified form.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_stage6_regression.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/test/test_stage6_regression.jl` exits 0 with zero failures.
    - `grep -c 'isapprox\|atol\|rtol' spike/test/test_stage6_regression.jl` outputs `0`.
    - `grep -c '128\.5' spike/test/test_stage6_regression.jl` outputs at most `1` and only inside the SC1c AffineMap-type assertion (the pipeline comparisons derive `c` from `axes`).
    - `grep -c 'p11_stage6_golden.jld2' spike/test/test_stage6_regression.jl` is at least `1`.
    - `grep -c 'AffineMap' spike/test/test_stage6_regression.jl` is at least `1`.
    - The test file asserts the golden `theta` lacks `:chromatic_eps`: `grep -c 'chromatic_eps' spike/test/test_stage6_regression.jl` is at least `3`.
  </acceptance_criteria>
  <done>SC1c, SC1d and SC1e all pass at exact equality against the pre-edit golden; the composed refactor is proven to have changed nothing for existing behaviour; hostile `chromatic_eps` values are proven to be rejected at the entry guard.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the pinned sha, run the five decoupling commands, and regression-test the shipped bundle</name>
  <files>.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md</files>
  <read_first>
    - `11-RESEARCH.md` §B8 lines 659-681 — the two sha verification commands and the ordering
      constraint they check.
    - `11-RESEARCH.md` §B7 lines 585-621 — why `Artifacts.toml`'s pin is independent of the datagen
      digest, and the `skip_if_done` caveat worth one sentence in the report.
    - `11-RESEARCH.md` §A5 lines 433-499 — the shipped read path traced end to end, and why
      `_theta_tuple` is the one genuine hazard.
    - `11-RESEARCH.md` §F20c lines 1512-1554 — the five verification commands, verbatim, and the
      note that the suite going red on theta-arity literals was expected work, not pipeline damage.
    - `11-CONTEXT.md` `<canonical_refs>` — the artifact and registry line references.
    - `.planning/ROADMAP.md` line 126 — the literal decoupling claim to be quoted.
    - `.planning/phases/11-.../11-02-SUMMARY.md` — `PRE_EPS_SHA` and `EPS_COMMIT_SHA`.
    - `.planning/phases/11-.../11-01-SUMMARY.md` — `PHASE11_BASE_SHA`.
    - `src/registry.jl:96-177` and `src/amortized/persist.jl:92-103` — the load path being
      regression-tested.
    - `docs/amortized.md` — the `## Public entry point` section, for the exact
      `colocalization_amortized` signature to exercise.
  </read_first>
  <action>
Run the checks below and record every command **and its literal output** in a new file
`.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROVENANCE.md`. Structure
it in the `gate-8x8-amended.md` voice: a header block naming the run date, the three shas, and the
protocol; then a verdict-first `## Overall verdict` line; then one `##` section per check group.

**Group 1 — D-12 sha verification.**
- `git rev-parse "<EPS_COMMIT_SHA>^"` — must equal `PRE_EPS_SHA`.
- `git show "<PRE_EPS_SHA>":src/amortized/simulator.jl | grep -c chromatic_eps` — must output `0`.
- `grep -c "<PRE_EPS_SHA>" docs/amortized.md` — must be at least 1.
Record all three outputs verbatim. If any disagrees, this is a **blocker**: the named limit points
at the wrong commit and the provenance-by-reference claim is false. Do not paper over it.

**Group 2 — the five §F20c decoupling commands**, run against `PHASE11_BASE_SHA`, each with its
expected result stated beside its actual output:
1. `git diff --stat <PHASE11_BASE_SHA>..HEAD -- src/bayes.jl src/colocalization.jl src/LoadImages.jl src/plot.jl` — expect empty.
2. `git diff --name-only <PHASE11_BASE_SHA>..HEAD -- src/ | grep -v '^src/amortized/'` — expect empty.
3. `git diff --stat <PHASE11_BASE_SHA>..HEAD -- test/test_images/` — expect empty; also record
   `git ls-files -s test/test_images/ | sha256sum` as the digest of the read-only image inputs.
4. `git diff --stat <PHASE11_BASE_SHA>..HEAD -- Artifacts.toml` — expect empty.
5. `julia --project -t auto -e 'using Pkg; Pkg.test()'` — expect green.

**Group 3 — the shipped-bundle regression (the highest-consequence check).** In a single
`julia --project` invocation, prove the shipped read surface still works after the `src/` edit:
- `ProteinCoLoc.estimator_for(8)` resolves without error (this exercises
  `_lazy_load_from_artifact!(8)` including `_verify_tree_sha1`). If the artifact is not present on
  disk and no network is available, fall back to the in-repo dev path under
  `artifacts/amended_v2/grid_8/` and **record explicitly which resolution branch was taken** —
  never claim a download-verified load that did not happen.
- Assert the loaded bundle's persisted architecture still reports `D == 7` — i.e. the shipped net
  is a 7-marginal flow read off disk, unaffected by `theta_prior_bounds()` now returning 8. This is
  the concrete form of RESEARCH §A5's argument.
- Assert `length(bundle.θzt.lo) == 7` (or the equivalent accessor) — the frozen
  `BoundedThetaTransform` bounds are persisted data, not a recomputation.
- Run `colocalization_amortized(img, control, channels; num_patches = 8)` end to end on the
  committed real images `test/test_images/positive/positive_c{1,2}.tif` (sample) and
  `test/test_images/negative/negative_c{1,2}.tif` (control), constructed through the frozen
  `MultiChannelImage` convenience constructor (`src/LoadImages.jl:432-446`). Assert the returned
  result is an `AmortizedColocResult`, that its `Δρ` draws are finite, and record the point
  estimate and the OOD flag in the document. **This is a smoke regression, not a scientific claim**
  — say so in the document.
- Assert `ProteinCoLoc._theta_tuple` on a 7-long vector still produces a theta that
  `ProteinCoLoc.simulate_pair` accepts (the posterior-predictive-channel hazard, risk R3).

**Group 4 — the narrower decoupling claim (ruling Q8).** Write a short `## Decoupling claim`
section that: quotes `ROADMAP.md:126` verbatim; states plainly that D-11 deliberately edits
`src/amortized/simulator.jl`, so the literal claim is no longer available; states the adopted
narrower wording ("the v1.0 manuscript pipeline files and the real image bytes are byte-unchanged;
only `src/amortized/` is edited, and within it only `simulator.jl`, plus a defensive read in
`ood.jl` and the theta-arity constants in `datagen.jl`"); and notes that the root suite going red
on theta-arity literals during plan 11-02 was **expected work, not evidence of pipeline breakage**
— the two must not be conflated. Also record the one-sentence `skip_if_done` caveat from
RESEARCH §B7: `_train_grid_pipeline`'s skip branch checks artifact file integrity, not the datagen
digest, so a post-edit `train_and_register(8)` would load the old bundle and not retrain — the
desired behaviour under D-01, but the digest change is invisible at that call site and nobody
should mistake that silence for "nothing changed".

Commit the provenance document.
  </action>
  <verify>
    <automated>julia --project -t auto -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse "$(git log --format=%H --grep='chromatic' -1)^"` equals the `PRE_EPS_SHA` recorded in 11-02-SUMMARY, and `11-PROVENANCE.md` records that output verbatim.
    - `git show "$PRE_EPS_SHA":src/amortized/simulator.jl | grep -c chromatic_eps` outputs `0`, recorded in the document.
    - `git diff --name-only "$PHASE11_BASE_SHA"..HEAD -- src/ | grep -v '^src/amortized/'` produces empty output, recorded in the document.
    - `git diff --stat "$PHASE11_BASE_SHA"..HEAD -- src/bayes.jl src/colocalization.jl src/LoadImages.jl src/plot.jl Artifacts.toml test/test_images/` produces empty output, recorded in the document.
    - `julia --project -t auto -e 'using Pkg; Pkg.test()'` exits 0.
    - `julia --project -e 'using ProteinCoLoc; b = ProteinCoLoc.estimator_for(8); println("loaded")'` prints `loaded`, and `11-PROVENANCE.md` names which resolution branch (installed store / in-repo dev fallback / download) was taken.
    - `11-PROVENANCE.md` contains the strings `PRE_EPS_SHA`, `PHASE11_BASE_SHA`, a verbatim quote of `ROADMAP.md:126`, and the phrase `only `src/amortized/``.
    - `11-PROVENANCE.md` records a finite Δρ point estimate from a real `colocalization_amortized(...)` run on `test/test_images/`.
  </acceptance_criteria>
  <done>The pinned sha is proven to be the epsilon commit's first parent and to predate `chromatic_eps`; the five decoupling commands are recorded with their empty outputs; the shipped bundle still loads with `D == 7` off disk and `colocalization_amortized(...)` still answers on real images; and the narrower Q8 decoupling claim is written down beside the ROADMAP text it replaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Artifacts.toml` pin to shipped bundle bytes | `_verify_tree_sha1` (`src/registry.jl:101-108`) gates every artifact load |
| pinned git sha to provenance claim | A wrong sha silently converts named limit #8 into a false statement |
| committed TIFF bytes to the read path | `load_tiff` (`src/LoadImages.jl:214-217`) on in-repo, committed bytes — no user path, no upload, no archive extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Repudiation | Named limit #8's pinned sha | mitigate | `git rev-parse "<EPS>^"` and `git show "<SHA>":src/amortized/simulator.jl \| grep -c chromatic_eps` both recorded verbatim; a disagreement is declared a blocker, not smoothed over |
| T-11-11 | Tampering | Shipped artifact integrity | mitigate | `Artifacts.toml` asserted byte-unchanged; `estimator_for(8)` is exercised through the real `_lazy_load_from_artifact!` path so `_verify_tree_sha1` actually runs; the resolution branch taken is recorded rather than assumed (ASVS V6) |
| T-11-08 | Tampering | 7-row posterior-mean vector reaching an 8-field `simulate_pair` | mitigate | Explicit regression assertion that `_theta_tuple(v7)` produces a theta `simulate_pair` accepts (risk R3) |
| T-11-13 | Tampering | Exact-equality regression silently relaxed to a tolerance | mitigate | Acceptance criterion greps for `isapprox`/`atol`/`rtol` returning `0`; the only sanctioned fallback is the pre-registered constant and its use must be recorded as a deviation |
| T-11-14 | Information disclosure | Reading a path outside the committed read-only image set | mitigate | Only `test/test_images/` is read; the corpus `sealed_holdout` split is not touched (ASVS V4, V12) |
| T-11-SC | Tampering | package-manager installs | mitigate | No packages installed; this plan adds no dependency to either environment |
</threat_model>

<verification>
1. `julia --project=spike spike/test/test_stage6_regression.jl` — exit 0.
2. `julia --project -t auto -e 'using Pkg; Pkg.test()'` — exit 0.
3. All five §F20c commands recorded in `11-PROVENANCE.md` with their expected (empty) outputs.
4. `git show "$PRE_EPS_SHA":src/amortized/simulator.jl | grep -c chromatic_eps` outputs `0`.
5. `colocalization_amortized(...)` runs to completion on `test/test_images/` and its result is
   recorded.
</verification>

<success_criteria>
- SC1c, SC1d and SC1e pass at exact `==`; no tolerance was used, or its use is recorded as a
  named deviation.
- The pinned sha is verified to be the epsilon commit's first parent and to predate `chromatic_eps`.
- The shipped bundle loads with a persisted `D == 7` and a 7-element frozen theta transform, and
  `colocalization_amortized(...)` returns a finite result on real images.
- `11-PROVENANCE.md` records every command with its literal output, quotes `ROADMAP.md:126`,
  adopts the narrower Q8 wording, and names the `skip_if_done` caveat.
- The root suite is green.
</success_criteria>

<output>
Create `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-03-SUMMARY.md`
when done, noting any deviation from exact equality and the artifact-resolution branch taken.
</output>

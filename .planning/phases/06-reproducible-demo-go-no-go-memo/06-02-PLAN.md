---
phase: 06-reproducible-demo-go-no-go-memo
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md
  - spike/demo.jl
autonomous: true
requirements: [DEMO-03]
tags: [memo, decision, go-no-go, spike]

must_haves:
  truths:
    - "A 2–3-page Go/No-Go memo exists at .planning/phases/06-.../06-GO-NO-GO-MEMO.md"
    - "The memo renders a Clean Go (recommend Phase 7) AND states explicitly the literal pre-registered gates did NOT pass as written (D-01)"
    - "The memo reports BOTH number sets in order: Set 1 pre-registered FAIL (primary, verbatim from 05-04-SUMMARY.md) then Set 2 post-iteration (post-hoc) (D-03)"
    - "The memo states the explicit falsification condition that would have forced a No-Go (D-02)"
    - "The memo names the data-snooping exposure + the three mitigations (consts.jl vs e9c91d3, DEV seed 0xDE7C0DE, single confirmatory VAL run) (D-04)"
    - "The memo commits Phase 7 to the independent fresh-seed re-pre-registered confirmation ship-gate (D-05)"
    - "The memo commits to the Phase 8–16 DAG with Phase 11 ∥ 13 lead, Phase 12 following 11 (D-07/D-08)"
    - "The memo frames SBC as 'calibrated under the simulator' paired with the OOD result in the same breath"
    - "demo.jl hard-asserts the memo file is present and contains the required keyword set (DEMO-03 gate)"
  artifacts:
    - path: ".planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md"
      provides: "The 2–3-page Go/No-Go decision memo"
      min_lines: 60
    - path: "spike/demo.jl"
      provides: "Memo-presence + content-keyword assertion (DEMO-03 gate)"
  key_links:
    - from: ".planning/phases/06-.../06-GO-NO-GO-MEMO.md"
      to: ".planning/phases/05-.../05-04-SUMMARY.md"
      via: "verbatim transcription of Set 1 pre-registered FAIL numbers"
      pattern: "0.8861|0.730|2.81e-40"
    - from: "spike/demo.jl"
      to: ".planning/phases/06-.../06-GO-NO-GO-MEMO.md"
      via: "isfile + content-keyword @assert"
      pattern: "GO-NO-GO-MEMO"
---

<objective>
Author the 2–3-page Go/No-Go memo (DEMO-03) and wire its presence + content gate into `spike/demo.jl`. The memo renders a Clean Go, reports BOTH number sets honestly (pre-registered FAIL primary, post-iteration post-hoc), states the falsification condition, names the data-snooping exposure with its three mitigations, commits Phase 7 to an independent confirmation ship-gate, and commits to the Phase 8–16 build-out DAG with Phase 11 ∥ 13 as lead axes.

Purpose: Deliver the falsifiable, honest SC3 verdict that closes the spike — a decision memo whose credibility rests on pre-registration discipline plus a hard ship-gate, not on post-hoc numbers.
Output: `.planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md` + a memo-gate assertion added to `spike/demo.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-CONTEXT.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md
@.planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-04-SUMMARY.md

<numbers>
<!-- The exact figures the memo transcribes. Set 1 is NOT reproducible from current artifacts — cite verbatim from 05-04-SUMMARY.md. Set 2 is on-disk / STATE.md, labeled post-hoc. -->

Set 1 (PRIMARY, pre-registered, ALL THREE FAIL — from 05-04-SUMMARY.md):
  SBC (M=2000, L=999, bins=50, imsize 256×256, VAL_MASTER_SEED): ρ_true KS p=2.81e-40, χ² p=5.27e-146, ECE 0.189 (red); Δρ ECE 0.192 (red); 6/8 params green.
  BF: corr(amortized,KDE)=0.8861, max|Δ logBF|=3.598 (both D-08a≥0.95 and D-08b≤0.5 FAIL); log_prior_odds=−0.0294.
  OOD: pooled AUC 0.730 (<0.80 FAIL); noise family AUC 0.0 (named blind spot); held-out ID fire-rate 0.05.

Set 2 (POST-HOC, confirmatory — on-disk *_report.jld2 + STATE.md iter1/iter2):
  SBC (iter1 retrain, same 50k cache, higher-capacity flow): ECE green on all 8 params (ρ_true 0.0164, was 0.189 red); KS passes 3/8; strict all-8 KS∧χ² conjunction still FAILS (χ² over-powered at M=2000; label_efficiency/shift genuinely non-uniform).
  BF (iter2 retrain, NRE on same 50k cache + A7 difference encoding): corr 0.9358 (↑ from 0.886), max|Δ logBF| 10.95 — diagnosed as a clamped-KDE baseline tail artifact (baseline hits its _clampp=1e-8 floor → ±18.41 at |Δρ|≳0.4 while the bounded NRE stays ~±8; mid-range agrees within ~1–2).
  OOD (iter2, added image-noise Channel 3, OR-fused): all 4 families best-AUC 1.0, combined pooled AUC 1.0, ID fire-rate 0.05, negative controls KS-invariant + density-quiet.

Mitigations (D-04): consts.jl byte-unchanged vs commit e9c91d3; model selection on disjoint DEV seed 0xDE7C0DE; single confirmatory VAL run per iteration (no retry-to-pass).
Thesis restatement (NPE-03): amortized >100× faster than per-dataset ADVI (median 325× forward-pass clock, ~16× full posterior-sample workload) at comparable RMSE; ADVI baseline ~0.5 s/pair; ρ recovery corr 0.983.
Build-out DAG (D-07/D-08): Wave A {8,9,10} (9 & 10 complete); Wave B {11 ∥ 13, then 12}; Wave C {14,15}; Wave D {16}. Lead axes: Phase 11 (registration/chromatic uncertainty as latent) ∥ Phase 13 (three-hypothesis amortized BF); Phase 12 (spatial coloc map) follows 11. Longer horizon: hierarchy/3D/multi-channel.
</numbers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Go/No-Go memo (2–3 pages, both number sets, verdict, falsification, ship-gate, DAG)</name>
  <read_first>
    - .planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-04-SUMMARY.md (SOURCE OF TRUTH for Set 1 pre-registered FAIL numbers — transcribe verbatim; NOT reproducible from current artifacts)
    - .planning/STATE.md (Accumulated Context → Decisions [Phase 05-iter1]/[Phase 05-iter2] — Set 2 post-iteration numbers + the three mitigations)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-CONTEXT.md (D-01..D-08 locked decisions)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md (§"Go/No-Go Memo — Verbatim Numbers" Set 1 & Set 2; §Common Pitfalls 1)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md (memo section: analogs 05-04-SUMMARY.md + spike/NOTES.md; required-content checklist)
    - .planning/ROADMAP.md (§Phase 7 contract; §Progress Execution Order — the Phase 8–16 DAG)
    - spike/validation/consts.jl (the byte-locked pre-registration anchor referenced by the honesty framing)
  </read_first>
  <action>
    Create .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md, 2–3 pages of prose modeled on 05-04-SUMMARY.md + spike/NOTES.md decision-record style (labeled tables + honest tradeoff rows). Do NOT touch src/. Required sections:
    (1) VERDICT — Clean Go, recommend Phase 7 (D-01). State EXPLICITLY that the literal pre-registered gates did NOT pass as written; justify the Clean Go by reading each residual failure to a characterized non-method cause: χ²-over-power at M=2000, residual data-scale gap, clamped-KDE baseline hitting its 1e-8 floor at sweep tails. Do NOT claim a pass.
    (2) SET 1 — PRIMARY pre-registered result (all three gates FAIL): transcribe verbatim from 05-04-SUMMARY.md the SBC table (ρ_true KS 2.81e-40 / χ² 5.27e-146 / ECE 0.189 red; Δρ ECE 0.192 red; 6/8 green), BF (corr 0.8861, max|Δ| 3.598, log_prior_odds −0.0294), OOD (pooled AUC 0.730 FAIL, noise family 0.0 blind spot, ID fire-rate 0.05). Label these as the pre-registered numbers from the frozen summary — NOT reproducible from current artifacts.
    (3) SET 2 — post-hoc confirmatory (D-03/D-04): SBC ECE green all 8 (ρ_true 0.0164), KS 3/8, conjunction still fails; BF corr 0.9358 + the clamped-KDE tail-artifact diagnosis; OOD combined AUC 1.0. Frame explicitly as post-hoc. Name the data-snooping exposure plainly (frozen net retrained twice AFTER the pre-registered result was seen) + the three mitigations (consts.jl byte-unchanged vs e9c91d3; disjoint DEV seed 0xDE7C0DE; single confirmatory VAL run each, no retry-to-pass). Do NOT present Set 2 as the pre-registered result.
    (4) FALSIFICATION CONDITION (D-02): state explicitly what would have forced a No-Go — SBC ECE staying red after capacity+data iteration, OR BF mid-range disagreement, OR an OOD family undetectable by any summary-orthogonal channel.
    (5) SHIP-GATE (D-05): commit Phase 7 to an independent, fresh-seed, re-pre-registered SBC/BF/OOD confirmation run (new locked consts, new disjoint seed, single run) that must reproduce the calibration/OOD/BF story BEFORE amortized inference ships into src/ — a HARD ship-gate, the credibility backbone of the Clean Go.
    (6) SBC framing (carried-forward hard requirement): report SBC as "calibrated under the simulator" paired with the OOD result in the SAME breath — SBC alone is never a real-data guarantee.
    (7) THESIS (NPE-03): amortized >100× faster than ADVI (median 325× forward-pass, ~16× full workload) at comparable RMSE, ρ recovery corr 0.983; restate the honest ADVI ~0.5 s/pair caveat.
    (8) BUILD-OUT DECISION (D-07/D-08): commit to the Phase 8–16 DAG — Wave A {8,9,10}, Wave B {11 ∥ 13, then 12}, Wave C {14,15}, Wave D {16}; lead axes Phase 11 (registration/chromatic uncertainty as latent) ∥ Phase 13 (three-hypothesis amortized BF); Phase 12 (spatial coloc map) follows 11; hierarchy/3D/multi-channel named as longer horizon.
    May cite (not required) RxInfer BACK-01 as a post-Go paper nice-to-have. Select supporting figures from spike/validation/figures/ by filename reference only (do not regenerate).
  </action>
  <verify>
    <automated>grep -l "Clean Go" .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md && grep -c "0.8861\|0.730\|0.9358\|falsification\|0xDE7C0DE\|Phase 11\|Phase 13\|ship-gate" .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md</automated>
  </verify>
  <acceptance_criteria>
    - file .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md exists and is 60+ lines (≈2–3 pages)
    - contains the string "Clean Go" and an explicit statement that the literal pre-registered gates did NOT pass as written
    - contains Set 1 verbatim figures (0.8861, 3.598, 0.730, 2.81e-40, 0.189) labeled pre-registered/primary
    - contains Set 2 figures (0.0164, 0.9358, combined AUC 1.0) labeled post-hoc/confirmatory
    - contains an explicit "falsification" condition, the three mitigations (e9c91d3, 0xDE7C0DE, single confirmatory VAL run), and the D-05 ship-gate
    - contains "Phase 11" and "Phase 13" as lead axes and "Phase 12" following 11
    - contains an SBC "calibrated under the simulator" phrase paired with OOD
  </acceptance_criteria>
  <done>The memo renders a defensible Clean Go with both number sets correctly labeled, an explicit falsification condition, the named data-snooping exposure + mitigations, the D-05 ship-gate, and the Phase 8–16 DAG with Phase 11 ∥ 13 lead.</done>
</task>

<task type="auto">
  <name>Task 2: Wire memo-presence + content-keyword assertion into demo.jl and re-verify the full demo</name>
  <read_first>
    - spike/demo.jl (created in plan 06-01 — add the DEMO-03 memo gate to its close)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-GO-NO-GO-MEMO.md (the file being asserted — created in Task 1)
    - spike/02_simulator_demo.jl (lines 148-152 self-assert idiom)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-VALIDATION.md (row 6-02-01: memo content assertion)
  </read_first>
  <action>
    Extend spike/demo.jl's close (the self-assert section from plan 06-01). Define `const MEMO_PATH = joinpath(@__DIR__, "..", ".planning", "phases", "06-reproducible-demo-go-no-go-memo", "06-GO-NO-GO-MEMO.md")` using @__DIR__ + joinpath (never a bare relative path). Hard-gate DEMO-03: `@assert isfile(MEMO_PATH) "demo FAILED: missing memo $MEMO_PATH"`. Then read the memo once (`memo = read(MEMO_PATH, String)`) and content-gate with `@assert` that it `occursin` each required keyword: "Clean Go", "falsification", both number-set anchors (e.g. "0.8861" for Set 1 and "0.9358" for Set 2), the ship-gate, and "Phase 11"/"Phase 13". This makes the memo's required content machine-checked, not just its presence. Update the STEP 3 success-criteria table SC3 row so it now reports the memo gate as a real pass/fail (was presence-reported in plan 06-01). Keep the verify command the bare `julia --project=spike spike/demo.jl` (no POSIX test -f; Windows-tauglich). Do NOT edit consts.jl/Project.toml/Manifest.toml/src/.
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 with the memo present (DEMO-03 gate passes)
    - spike/demo.jl source contains `@assert isfile(MEMO_PATH)` and `occursin`-based content asserts for "Clean Go", "falsification", "Phase 11", "Phase 13"
    - the success-criteria table SC3 row reports the memo gate as pass
    - `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` clean
    - `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` empty
    - `julia --project=spike spike/test/runtests.jl` stays green
  </acceptance_criteria>
  <done>demo.jl hard-gates the memo's presence and required content; the full fast-tier demo exits 0 with all three DEMO criteria green and locked files byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo.jl → memo file | Read-only `read()` of the memo for the content assertion; no write-back |
| memo authoring → repo | Prose file under .planning/; never touches src/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-02 | Tampering | accidental src/ mutation | mitigate | Memo lives under .planning/; demo.jl memo-read is read-only; decoupling assert from plan 06-01 still gates src/ cleanliness |
| T-6-04 | Repudiation/Information | mislabeling post-hoc numbers as pre-registered | mitigate | Set 1 transcribed verbatim from 05-04-SUMMARY.md and labeled primary/pre-registered; Set 2 explicitly labeled post-hoc (D-03/D-04); content assert checks both anchors present |
| T-6-03 | Tampering/Supply chain | spike env deps | mitigate | No installs; `git diff --exit-code spike/Project.toml spike/Manifest.toml` empty at the gate |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — nothing to audit |
</threat_model>

<verification>
- memo file exists, 2–3 pages, both number sets correctly labeled (Set 1 verbatim vs 05-04-SUMMARY.md, Set 2 post-hoc)
- `julia --project=spike spike/demo.jl` exits 0 with the memo-presence + content gate passing (DEMO-03)
- `git status --porcelain -- src/` empty; `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` clean
- `julia --project=spike spike/test/runtests.jl` still green
</verification>

<success_criteria>
A 2–3-page Go/No-Go memo renders a defensible Clean Go, reports both number sets honestly in the required order, states the falsification condition, names the data-snooping exposure with its three mitigations, commits Phase 7 to the independent confirmation ship-gate, and commits to the Phase 8–16 DAG with Phase 11 ∥ 13 lead — and demo.jl machine-gates the memo's presence and required content.
</success_criteria>

<output>
Create `.planning/phases/06-reproducible-demo-go-no-go-memo/06-02-SUMMARY.md` when done
</output>

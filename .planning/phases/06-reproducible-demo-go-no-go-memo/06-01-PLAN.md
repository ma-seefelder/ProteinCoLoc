---
phase: 06-reproducible-demo-go-no-go-memo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/demo.jl
autonomous: true
requirements: [DEMO-01, DEMO-02]
tags: [demo, reproducibility, decoupling, spike]

must_haves:
  truths:
    - "`julia --project=spike spike/demo.jl` runs clean CPU-only and prints a success-criteria table"
    - "The fast-tier chain exercises EVERY layer (prior→simulator→summary→NPE→NRE/BF→OOD) at fixture scale on VAL_FIX_SEED, not just NPE"
    - "Each of NPE, BF/NRE, and OOD fixture steps carries a twin-run reproducibility assert proving bit-identical output on the same seed"
    - "demo.jl asserts `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` is empty (decoupling proof, DEMO-02)"
    - "`--full` re-runs the reported-scale gates (run_sbc/run_bf/run_ood) as subprocesses with independent exit codes"
    - "Fast tier loads *_report.jld2 headline numbers under isfile() guards, annotated post-iteration/Set 2 (fresh-clone graceful degradation)"
    - "spike/validation/consts.jl, spike/Project.toml, spike/Manifest.toml stay byte-unchanged"
  artifacts:
    - path: "spike/demo.jl"
      provides: "Two-tier seeded end-to-end demo runner (full NPE+BF+OOD fixture chain) + in-script decoupling assertion"
      min_lines: 150
  key_links:
    - from: "spike/demo.jl"
      to: "git status --porcelain -- src/ …"
      via: "backtick argv read() + @assert isempty"
      pattern: "git status --porcelain -- src/"
    - from: "spike/demo.jl"
      to: "spike/validation/harness.jl"
      via: "include() + draw_simulate_infer on val_rng(VAL_FIX_SEED)"
      pattern: "draw_simulate_infer"
    - from: "spike/demo.jl"
      to: "spike/validation/bf.jl"
      via: "load_ratio + build_bf_pair + amortized_log_bf fixture sweep"
      pattern: "amortized_log_bf"
    - from: "spike/demo.jl"
      to: "spike/validation/ood.jl"
      via: "fit_ood_nulls + maha_score fixture subset"
      pattern: "fit_ood_nulls|maha_score"
    - from: "spike/demo.jl"
      to: "spike/validation/*_report.jld2"
      via: "isfile-guarded jldopen for headline numbers"
      pattern: "jldopen.*report"
    - from: "spike/demo.jl"
      to: "spike/validation/run_sbc.jl"
      via: "subprocess run() under --full"
      pattern: "run\\(`julia --project=spike"
---

<objective>
Create `spike/demo.jl` — the two-tier seeded end-to-end demo runner that closes the spike (DEMO-01) and certifies decoupling (DEMO-02). The fast default loads frozen artifacts and chains EVERY pipeline layer — prior→simulator→summary→NPE→NRE/BF→OOD — at fixture scale from a fixed Random123 seed, each layer carrying a twin-run reproducibility assert; `--full` re-runs the reported-scale gates. A step-0 `git status` assertion proves `src/` was never mutated.

Purpose: Deliver the falsifiable, reproducible SC1/SC2 half of Phase 6 — a single script that actually re-exercises the whole inference stack reproducibly (not merely re-displays stored numbers), and proves the main package/manuscript pipelines are provably untouched.
Output: `spike/demo.jl` (top-level, AGPL-headered, mirrors `spike/02_simulator_demo.jl`).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-CONTEXT.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md

<interfaces>
<!-- Composition surfaces demo.jl includes and calls. Extracted from the spike this session. Use directly — no exploration. -->

From spike/validation/harness.jl:
  load_frozen_model(path = joinpath(@__DIR__, "..", "npe", "trained_npe.jld2"))  # → (estimator, θzt, zt, variant, d_in, meta); integrity-checks "estimator" key, error()s on missing file
  draw_simulate_infer(m, rng; imsize=(64,64), N)  # → r with r.θ (true 7-tuple), r.draws (7×N physical-θ posterior); CPU-only, use_gpu=false, transforms frozen (m.zt/m.θzt), NEVER refits
  val_rng(master_seed = VAL_MASTER_SEED) = Philox4x(UInt64, (UInt64(master_seed) ⊻ VAL_SALT, UInt64(0)))

From spike/validation/bf.jl (+ train_ratio.jl transitively):
  load_ratio(RATIO_MODEL_PATH)  # → (estimator, zt, log_prior_odds, num_summaries, meta); error()s on missing file / missing "estimator" key
  train_ratio(m; n, epochs, imsize, rng)          # tiny fallback retrain if RATIO_MODEL_PATH absent (mirror test_bf.jl:49-63)
  measure_log_prior_odds(prior_label_draws(2000; rng))
  build_bf_pair(m, rng, Δρ; imsize=(64,64), N)     # → pr with pr.Z_pair
  amortized_log_bf(est, Z_pair, log_prior_odds; use_gpu=false)  # → finite Float64, ONE forward pass

From spike/validation/ood.jl:
  fit_ood_nulls(Ztrain; variant=:min) ; maha_score(nulls, z) ; id_threshold(id_scores; q=OOD_ID_QUANTILE)
  id_summary(m, rng; imsize) ; frozen_summary(m, pair) ; misspec_optics(rng, θ; imsize, level)
  ood_roc_over_grid(m, nulls; levels, n_id, n_pos, rng, imsize, with_pp=false)   # keep with_pp=false (Pitfall 3: non-finite θ̂ on strong OOD crashes PP)

From spike/validation/consts.jl (READ-ONLY — byte-locked pre-registration):
  VAL_MASTER_SEED = 0x5BC0FFEE   # RESERVED reported stream — NEVER touch in the fast tier
  VAL_FIX_SEED    = 0xF1F7ED     # fixture stream — the fast tier MUST use this
  SBC_FIX_M=50 / SBC_FIX_L=99    # fixture-scale draw counts (64×64) ; OOD_GRID_LEVELS, OOD_ID_QUANTILE

From spike/data/seeding.jl:
  sample_rng(master_seed, idx) = Philox4x(UInt64, (UInt64(master_seed), UInt64(idx)))

Verified *_report.jld2 keys (all POST-ITERATION / Set 2 numbers — annotate as such, never label pre-registered):
  sbc_report.jld2: rank_table,labels,ks_p,chi2_p,ece,mce,verdict,cov_nominal,cov_empirical,SBC_M,SBC_L,SBC_BINS,…,caption,elapsed_min,generated
  bf_report.jld2:  targets,logbf_amortized,logbf_kde,corr,max_abs_err,log_prior_odds,BF_CORR_MIN,BF_LOGBF_TOL,BF_SWEEP_LO,BF_SWEEP_HI,BF_SWEEP_N,…
  ood_report.jld2: families,levels,maha_auc,fam_best_auc,combined_auc,id_fire_rate,neg_ks,pp_channel_note,roc_fpr,roc_tpr,maha_thr,…
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: demo.jl skeleton — header, guarded includes, --full flag, decoupling assertion, NPE fast-tier chain proof</name>
  <read_first>
    - spike/02_simulator_demo.jl (lines 1-19 AGPL header verbatim; lines 21-40 purpose banner; lines 42-53 using-block + CairoMakie.activate! + guarded includes; lines 148-152 self-assert close idiom)
    - spike/validation/harness.jl (lines 51-58 guarded-include ORDER: consts → harness; lines 78-113 load_frozen_model / draw_simulate_infer; lines 93-94 val_rng)
    - spike/validation/consts.jl (VAL_MASTER_SEED=0x5BC0FFEE, VAL_FIX_SEED=0xF1F7ED, SBC_FIX_M/SBC_FIX_L — READ-ONLY, never redefine)
    - spike/test/test_sbc.jl (lines 40-42 fixture-scale draw pattern on VAL_FIX_SEED; lines 114-116 CUDA-not-loaded assert)
    - spike/npe/run_thread_sweep.jl (line 215 bare-ARGS flag + abspath(PROGRAM_FILE)==@__FILE__ guard idiom)
    - spike/data/seeding.jl (lines 75-76 sample_rng Philox4x)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md (patterns 1-6)
  </read_first>
  <action>
    Create spike/demo.jl at the repo top level under spike/. Copy the 19-line AGPL header (spike/02_simulator_demo.jl:1-19) byte-for-byte, then a `# spike/demo.jl --- ` purpose banner naming the two-tier design and the PowerShell-safe verify command `julia --project=spike spike/demo.jl`.
    Add the using-block: `using JLD2, Test, Dates, Random, Statistics` (from run_sbc.jl:44-47). Omit CairoMakie unless rendering a new composite panel (reuse existing figures/*.png per RESEARCH Don't-Hand-Roll).
    Load composition surfaces via the guarded-include idiom in the ORDER from harness.jl:51-58: `isdefined(@__MODULE__, :VAL_MASTER_SEED) || include(joinpath(@__DIR__, "validation", "consts.jl"))` then `isdefined(@__MODULE__, :draw_simulate_infer) || include(joinpath(@__DIR__, "validation", "harness.jl"))` — harness transitively pulls load_npe, posterior_for, sample_prior/simulate_pair, build_mci (contract.jl → read-only src/), encode_d01, sample_rng. Use `@__DIR__` + joinpath everywhere, never bare relative paths.
    Define `const FULL = ("--full" in ARGS)`.
    STEP 0 (fail fast, before any compute) — the decoupling assertion (DEMO-02): `out = read(\`git status --porcelain -- src/ src/bayes.jl src/colocalization.jl\`, String)` then `@assert isempty(strip(out)) "DECOUPLING VIOLATED: src/ is dirty:\n$out"`. Use the backtick argv form with the three paths as literal tokens — NO string interpolation into the command (threat T-6-01, command injection). Scope the hard assert to exactly those three src/ paths (a whole-tree git status false-positives on .planning/ churn — RESEARCH Pitfall 4); any whole-tree read added is informational-only, never a gate.
    STEP 1a (NPE fast-tier chain proof, DEMO-01): `m = load_frozen_model()` (loads trained_npe.jld2 ONCE, frozen zt/θzt); run `r = draw_simulate_infer(m, val_rng(VAL_FIX_SEED); imsize=(64,64), N=SBC_FIX_L)`. Use VAL_FIX_SEED — NEVER val_rng(VAL_MASTER_SEED) in the fast tier (RESEARCH Pitfall 2 / anti-snooping). Never call fit(ZScoreTransform,...) — transforms are frozen. Reproducibility assert: run the NPE fixture chain twice on the same VAL_FIX_SEED and `@assert r1.draws == r2.draws` (Philox4x is deterministic — the cheapest honest SC1 proof, RESEARCH Open Q3). Assert no CUDA module is loaded (mirror test_sbc.jl:114-116).
    Do NOT hard-assert the memo file yet — the memo is authored in plan 06-02; a missing-memo hard gate here would fail wave 1. (Plan 06-02 wires the memo-presence + content assertion.)
    Structure the NPE chain as a small `run_npe_fixture(seed)` helper returning the draws so Task 2 can add sibling BF/OOD helpers and Task 3 can print a unified table.
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 (skeleton + NPE fixture chain runs clean CPU-only)
    - `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` is empty after the run
    - source of spike/demo.jl contains the backtick argv form `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` with NO `$`-interpolation inside the backticks
    - source contains `val_rng(VAL_FIX_SEED)` and does NOT contain `val_rng(VAL_MASTER_SEED)`
    - source contains the NPE twin-draw reproducibility `@assert` (r1.draws == r2.draws or isapprox)
    - `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` is clean
  </acceptance_criteria>
  <done>demo.jl loads, runs the decoupling assertion and a bit-reproducible fixture-scale NPE chain proof on VAL_FIX_SEED, and exits 0 CPU-only; locked files byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fast-tier BF/NRE + OOD fixture-chain steps (each with a twin-run reproducibility assert)</name>
  <read_first>
    - spike/demo.jl (the file from Task 1 — extend the fast tier)
    - spike/test/test_bf.jl (lines 40-95: guarded include of bf.jl; `_load_or_tiny_ratio` load_ratio-or-tiny-train fallback; `build_bf_pair`; `amortized_log_bf`; fixture-relaxed BF_FIX_CORR_MIN)
    - spike/validation/bf.jl (line 94 amortized_log_bf signature; line 165 sweep loop reading amortized_log_bf(est, pr.Z_pair, log_prior_odds))
    - spike/validation/train_ratio.jl (lines 350-364 load_ratio; RATIO_MODEL_PATH const)
    - spike/test/test_ood.jl (lines 40-105: guarded include of ood.jl; `_build_ood_fixture` — fit_ood_nulls on id_summary Ztrain, id_threshold, maha_score, misspec_optics/frozen_summary positive, ood_roc_over_grid with_pp=false)
    - spike/validation/ood.jl (fit_ood_nulls, maha_score, id_summary, frozen_summary, misspec_optics, id_threshold, ood_roc_over_grid)
    - spike/validation/consts.jl (OOD_GRID_LEVELS, OOD_ID_QUANTILE, VAL_FIX_SEED — READ-ONLY)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md (Common Pitfalls 2 & 3)
  </read_first>
  <action>
    Extend spike/demo.jl's fast tier so the chain actually exercises the BF/NRE and OOD layers at fixture scale on `val_rng(VAL_FIX_SEED)` — this is the blocker fix: report-loading alone is NOT chain reproducibility.
    STEP 1b (BF/NRE fixture step, DEMO-01): add a `run_bf_fixture(seed)` helper mirroring test_bf.jl:49-95. Load the ratio net once via `load_ratio(RATIO_MODEL_PATH)`; if the file is absent, fall back to a tiny retrain `train_ratio(m; n=120, epochs=6, imsize=(64,64), rng=val_rng(VAL_FIX_SEED))` + `measure_log_prior_odds(prior_label_draws(2000; rng=val_rng(VAL_FIX_SEED)))` (exact `_load_or_tiny_ratio` idiom). Run a SHORT Δρ sweep (e.g. 3-5 targets) building each pair with `build_bf_pair(m, val_rng(VAL_FIX_SEED), Δρ; imsize=(64,64), N=200)` and reading `amortized_log_bf(ratio.estimator, pr.Z_pair, ratio.log_prior_odds)` — one forward pass each, use_gpu=false. Assert every returned log-BF `isfinite`. Twin-run reproducibility: call `run_bf_fixture(VAL_FIX_SEED)` twice and `@assert` the two log-BF vectors are `==` (Philox4x deterministic).
    STEP 1c (OOD fixture step, DEMO-01): add a `run_ood_fixture(seed)` helper mirroring test_ood.jl `_build_ood_fixture` at fixture scale (imsize (64,64)): fit `nulls = fit_ood_nulls(Ztrain)` from a small `id_summary(m, rng; imsize)` pool, compute `maha_thr = id_threshold(mahaval)`, then score a small POSITIVE subset (`misspec_optics(rng, θ; imsize, level=OOD_GRID_LEVELS)` → `frozen_summary` → `maha_score`) and a small ID subset, and (optionally) call `ood_roc_over_grid(m, nulls; levels=OOD_GRID_LEVELS, n_id=20, n_pos=6, rng=val_rng(VAL_FIX_SEED), imsize=(64,64), with_pp=false)`. Keep `with_pp=false` — the PP re-simulation path crashes on non-finite θ̂ for strong OOD inputs (RESEARCH Pitfall 3); mirror run_ood.jl. Twin-run reproducibility: call `run_ood_fixture(VAL_FIX_SEED)` twice and `@assert` the maha scores (and ROC AUC) are `==`.
    All three fixture steps use VAL_FIX_SEED only — never VAL_MASTER_SEED (anti-snooping). No transform is re-fit for NPE; the OOD nulls ARE fit on a fixture TRAIN-only pool exactly as test_ood.jl does (that is the OOD channel's own frozen-from-train discipline, SC5). Keep fast-tier runtime seconds-to-low-minutes (small imsize/counts — RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 with the BF and OOD fixture steps executing (not just report-loading)
    - source contains `amortized_log_bf(` invoked over a Δρ sweep and `fit_ood_nulls(`/`maha_score(` invoked on a fixture subset
    - source contains a BF twin-run reproducibility `@assert` (equal log-BF vectors) AND an OOD twin-run reproducibility `@assert` (equal maha scores/AUC)
    - source contains `with_pp = false` on the OOD fixture path and uses `val_rng(VAL_FIX_SEED)` for BF and OOD (no VAL_MASTER_SEED)
    - every fixture log-BF is asserted `isfinite`
    - `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` clean; `git status --porcelain -- src/` empty
  </acceptance_criteria>
  <done>The fast-tier chain now exercises NPE, BF/NRE, and OOD at fixture scale on VAL_FIX_SEED, each with a passing twin-run reproducibility assert — satisfying DEMO-01's "chains every layer reproducibly" for all inference layers.</done>
</task>

<task type="auto">
  <name>Task 3: Fast-tier headline-number load, success-criteria table, --full subprocess dispatch, self-assert close</name>
  <read_first>
    - spike/demo.jl (the file from Tasks 1-2 — extend it)
    - spike/validation/run_sbc.jl (lines 56-65 atomic _save_report; lines 141-160 rpad column table idiom; lines 74-75 anti-snooping guard asserts)
    - spike/validation/run_bf.jl (line 99 subprocess run() form for the isolated baseline)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md (Code Examples: verified report keys; Common Pitfalls 1 & 4; Assumptions Log A1)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md (patterns 7-9)
  </read_first>
  <action>
    Extend spike/demo.jl. STEP 2 (headline numbers, DEMO-01): define const paths `SBC_REPORT/BF_REPORT/OOD_REPORT = joinpath(@__DIR__, "validation", "<name>_report.jld2")`. For each, guard with `isfile(...)` — the reports are gitignored so a fresh clone lacks them; on absence `@warn` with a message to run `--full` to regenerate (post-iteration numbers) rather than raising a raw load error. On presence, `jldopen(path,"r") do f ... end` pulling the verified keys: from sbc_report — labels, ks_p, chi2_p, ece, verdict, SBC_M, SBC_L; from bf_report — corr, max_abs_err, log_prior_odds; from ood_report — maha_auc, combined_auc, fam_best_auc, id_fire_rate. Explicitly print `bf["max_abs_err"]` so the memo (plan 06-02) can confirm the 10.95 figure before quoting it (RESEARCH Assumption A1). These report numbers are SEPARATE from the Task-2 chain proof — they are the reported-scale headline DISPLAY, not the reproducibility evidence.
    STEP 3 (success-criteria table, DEMO-01): print an rpad-column table (idiom run_sbc.jl:141-160) with one row per SC1 (pipeline reproduces — from the NPE+BF+OOD twin-run asserts of Tasks 1-2), SC2 (decoupled — from the step-0 assert), SC3 (memo — presence reported here, hard-gated in plan 06-02), and DEMO-01/02/03 pass/fail. CRITICAL: annotate every LOADED headline number as POST-ITERATION / Set 2 (RESEARCH Pitfall 1) — never label these pre-registered; note the pre-registered (Set 1) numbers live in 05-04-SUMMARY.md. Keep the chain-reproduction rows (Tasks 1-2) distinct from the loaded-report rows so the table does not conflate "reproduced this run" with "reported previously".
    STEP 4 (--full dispatch, DEMO-01-full): `if FULL`, spawn each reported gate as its OWN subprocess so exit codes stay independent and one gate's @testset throw does not abort the others (RESEARCH Open Q2): loop over ("run_sbc.jl","run_bf.jl","run_ood.jl") calling `run(\`julia --project=spike $(joinpath(@__DIR__, "validation", s))\`)`. Each run_*.jl already owns its consts/artifact/figures + anti-snooping guard asserts (run_sbc.jl:74-75) and exits nonzero on gate fail.
    CLOSE: self-assert idiom (02_simulator_demo.jl:148-152) — `@assert isfile(load_frozen_model default path)` style checks on the frozen nets that MUST exist, then `println("demo OK: ...")`. The verify command stays the bare `julia --project=spike spike/demo.jl` (no POSIX test -f; Windows-tauglich).
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 and prints a success-criteria table to stdout with rows for SC1/SC2/SC3 and DEMO-01/02/03
    - stdout shows a "Set 2 / post-iteration" (or equivalent) annotation on the loaded headline numbers and a printed `bf max_abs_err` value
    - the table keeps chain-reproduction rows (this-run NPE/BF/OOD twin-run) distinct from loaded-report rows
    - source contains `isfile(` guards on the three *_report.jld2 paths with a `@warn` fallback (no raw load error on a fresh clone)
    - source contains the `--full` subprocess dispatch `run(\`julia --project=spike $(joinpath(@__DIR__, "validation", s))\`)` over run_sbc/run_bf/run_ood
    - source ends with a `@assert isfile(...)` self-check on the frozen nets and a `println("demo OK` line
    - `julia --project=spike spike/test/runtests.jl` stays green (demo.jl does not break the aggregate gate)
  </acceptance_criteria>
  <done>Fast tier loads guarded headline numbers (distinct from the chain proof), prints an annotated success-criteria table, dispatches reported gates under --full as independent subprocesses, and self-asserts; runtests.jl stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARGS → demo.jl | Only external input is the `--full` flag (exact-match compare, no interpolation) |
| demo.jl → OS git | Shell-out for the decoupling proof; argv-literal, no shell string |
| demo.jl → src/ | Read-only via include() through contract.jl; never written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering/Elevation | git shell-out in step 0 | mitigate | Julia backtick argv form with three literal `src/` path tokens; NO string interpolation into the command (Pattern 4) |
| T-6-02 | Tampering | accidental src/ mutation | mitigate | The decoupling `@assert isempty` IS the control; demo.jl reaches src/ only read-only via include() through contract.jl |
| T-6-03 | Tampering/Supply chain | spike env deps | mitigate | No installs; `git diff --exit-code spike/Project.toml spike/Manifest.toml` must be empty at the wave gate |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — nothing to audit; pinned Manifest is byte-locked |
</threat_model>

<verification>
- `julia --project=spike spike/demo.jl` exits 0, CPU-only, exercises NPE+BF+OOD at fixture scale with per-layer twin-run asserts, prints the success-criteria table (SC1)
- `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` empty and asserted in-script (SC2/DEMO-02)
- `julia --project=spike spike/demo.jl --full` dispatches the three reported gates as independent subprocesses
- `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` clean
- `julia --project=spike spike/test/runtests.jl` still green
</verification>

<success_criteria>
demo.jl exists, runs clean CPU-only from a fixed seed at fixture scale, actually re-exercises NPE + BF/NRE + OOD each with a twin-run bit-reproducibility assert, asserts src/ decoupling, loads guarded post-iteration headline numbers (kept distinct from the chain proof), tabulates the success criteria, and dispatches reported-scale gates under --full — all with consts.jl/Project.toml/Manifest.toml byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-reproducible-demo-go-no-go-memo/06-01-SUMMARY.md` when done
</output>

---
phase: 06-reproducible-demo-go-no-go-memo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/demo.jl
autonomous: true
requirements: [DEMO-01, DEMO-02]
tags: [demo, reproducibility, decoupling, spike]

must_haves:
  truths:
    - "`julia --project=spike spike/demo.jl` runs clean CPU-only and prints a success-criteria table"
    - "The fast-tier chain (prior→simulator→summary→NPE/NRE→SBC/BF/OOD) reproduces bit-identically on VAL_FIX_SEED"
    - "demo.jl asserts `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` is empty (decoupling proof, DEMO-02)"
    - "`--full` re-runs the reported-scale gates (run_sbc/run_bf/run_ood) as subprocesses with independent exit codes"
    - "Fast tier loads *_report.jld2 headline numbers under isfile() guards (fresh-clone graceful degradation)"
    - "spike/validation/consts.jl, spike/Project.toml, spike/Manifest.toml stay byte-unchanged"
  artifacts:
    - path: "spike/demo.jl"
      provides: "Two-tier seeded end-to-end demo runner + in-script decoupling assertion"
      min_lines: 120
  key_links:
    - from: "spike/demo.jl"
      to: "git status --porcelain -- src/ …"
      via: "backtick argv read() + @assert isempty"
      pattern: "git status --porcelain -- src/"
    - from: "spike/demo.jl"
      to: "spike/validation/harness.jl"
      via: "include() + draw_simulate_infer on val_rng(VAL_FIX_SEED)"
      pattern: "draw_simulate_infer"
    - from: "spike/demo.jl"
      to: "spike/validation/*_report.jld2"
      via: "isfile-guarded jldopen for headline numbers"
      pattern: "jldopen.*report"
    - from: "spike/demo.jl"
      to: "spike/validation/run_sbc.jl"
      via: "subprocess run() under --full"
      pattern: "run\\(`julia --project=spike"
---

<objective>
Create `spike/demo.jl` — the two-tier seeded end-to-end demo runner that closes the spike (DEMO-01) and certifies decoupling (DEMO-02). The fast default loads frozen artifacts and chains every pipeline layer at fixture scale from a fixed Random123 seed; `--full` re-runs the reported-scale gates. A step-0 `git status` assertion proves `src/` was never mutated.

Purpose: Deliver the falsifiable, reproducible SC1/SC2 half of Phase 6 — a single script that proves the whole spike reproduces CPU-only under the pinned Manifest and that the main package/manuscript pipelines are provably untouched.
Output: `spike/demo.jl` (top-level, AGPL-headered, mirrors `spike/02_simulator_demo.jl`).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-CONTEXT.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md
@.planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md

<interfaces>
<!-- Composition surfaces demo.jl includes and calls. Extracted from the spike this session. Use directly — no exploration. -->

From spike/validation/harness.jl:
  load_frozen_model(path = joinpath(@__DIR__, "..", "npe", "trained_npe.jld2"))  # → (estimator, θzt, zt, variant, d_in, meta); integrity-checks "estimator" key, error()s on missing file
  draw_simulate_infer(m, rng; imsize=(64,64), N)  # → r with r.θ (true 7-tuple), r.draws (7×N physical-θ posterior); CPU-only, use_gpu=false, transforms frozen (m.zt/m.θzt), NEVER refits
  val_rng(master_seed = VAL_MASTER_SEED) = Philox4x(UInt64, (UInt64(master_seed) ⊻ VAL_SALT, UInt64(0)))

From spike/validation/consts.jl (READ-ONLY — byte-locked pre-registration):
  VAL_MASTER_SEED = 0x5BC0FFEE   # RESERVED reported stream — NEVER touch in the fast tier
  VAL_FIX_SEED    = 0xF1F7ED     # fixture stream — the fast tier MUST use this
  SBC_FIX_M / SBC_FIX_L          # fixture-scale draw counts (64×64)

From spike/data/seeding.jl:
  sample_rng(master_seed, idx) = Philox4x(UInt64, (UInt64(master_seed), UInt64(idx)))

Verified *_report.jld2 keys (all POST-ITERATION / Set 2 numbers — annotate as such, never label pre-registered):
  sbc_report.jld2: rank_table,labels,ks_p,chi2_p,ece,mce,verdict,cov_nominal,cov_empirical,SBC_M,SBC_L,SBC_BINS,…,caption,elapsed_min,generated
  bf_report.jld2:  targets,logbf_amortized,logbf_kde,corr,max_abs_err,log_prior_odds,BF_CORR_MIN,BF_LOGBF_TOL,BF_SWEEP_LO,BF_SWEEP_HI,BF_SWEEP_N,…
  ood_report.jld2: families,levels,maha_auc,fam_best_auc,combined_auc,id_fire_rate,neg_ks,pp_channel_note,roc_fpr,roc_tpr,maha_thr,…
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: demo.jl skeleton — header, guarded includes, --full flag, decoupling assertion, fast-tier chain proof</name>
  <read_first>
    - spike/02_simulator_demo.jl (lines 1-19 AGPL header verbatim; lines 21-40 purpose banner; lines 42-53 using-block + CairoMakie.activate! + guarded includes; lines 148-152 self-assert close idiom)
    - spike/validation/harness.jl (lines 51-58 guarded-include ORDER: consts → harness; lines 78-113 load_frozen_model / draw_simulate_infer; lines 93-94 val_rng)
    - spike/validation/consts.jl (VAL_MASTER_SEED=0x5BC0FFEE, VAL_FIX_SEED=0xF1F7ED, SBC_FIX_M/SBC_FIX_L — READ-ONLY, never redefine)
    - spike/test/test_sbc.jl (lines 40-42 fixture-scale draw pattern on VAL_FIX_SEED; lines 114-116 CUDA-not-loaded assert)
    - spike/npe/run_thread_sweep.jl (line 215 bare-ARGS flag + abspath(PROGRAM_FILE)==@__FILE__ guard idiom)
    - spike/data/seeding.jl (lines 75-76 sample_rng Philox4x)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md (patterns 1-6)
  </read_first>
  <action>
    Create spike/demo.jl at the repo top level under spike/. Copy the 19-line AGPL header (spike/02_simulator_demo.jl:1-19) byte-for-byte, then a `# spike/demo.jl --- ` purpose banner naming the two-tier design and the PowerShell-safe verify command `julia --project=spike spike/demo.jl`.
    Add the using-block: `using JLD2, Test, Dates, Random, Statistics` (from run_sbc.jl:44-47). Omit CairoMakie unless rendering a new composite panel (reuse existing figures/*.png per RESEARCH Don't-Hand-Roll).
    Load composition surfaces via the guarded-include idiom in the ORDER from harness.jl:51-58: `isdefined(@__MODULE__, :VAL_MASTER_SEED) || include(joinpath(@__DIR__, "validation", "consts.jl"))` then `isdefined(@__MODULE__, :draw_simulate_infer) || include(joinpath(@__DIR__, "validation", "harness.jl"))` — harness transitively pulls load_npe, posterior_for, sample_prior/simulate_pair, build_mci (contract.jl → read-only src/), encode_d01, sample_rng. Use `@__DIR__` + joinpath everywhere, never bare relative paths.
    Define `const FULL = ("--full" in ARGS)`.
    STEP 0 (fail fast, before any compute) — the decoupling assertion (DEMO-02): `out = read(\`git status --porcelain -- src/ src/bayes.jl src/colocalization.jl\`, String)` then `@assert isempty(strip(out)) "DECOUPLING VIOLATED: src/ is dirty:\n$out"`. Use the backtick argv form with the three paths as literal tokens — NO string interpolation into the command (threat T-6-01, command injection). Scope the hard assert to exactly those three src/ paths (a whole-tree git status false-positives on .planning/ churn — RESEARCH Pitfall 4); any whole-tree read added is informational-only, never a gate.
    STEP 1 (fast-tier chain proof, DEMO-01): `m = load_frozen_model()` (loads trained_npe.jld2 ONCE, frozen zt/θzt); run `r = draw_simulate_infer(m, val_rng(VAL_FIX_SEED); imsize=(64,64), N=SBC_FIX_L)`. Use VAL_FIX_SEED — NEVER val_rng(VAL_MASTER_SEED) in the fast tier (RESEARCH Pitfall 2 / anti-snooping). Never call fit(ZScoreTransform,...) — transforms are frozen. Reproducibility assert: run the fixture chain twice on the same VAL_FIX_SEED and `@assert r1.draws == r2.draws` (Philox4x is deterministic — the cheapest honest SC1 proof, RESEARCH Open Q3). Assert no CUDA module is loaded (mirror test_sbc.jl:114-116).
    Do NOT hard-assert the memo file yet — the memo is authored in plan 06-02; a missing-memo hard gate here would fail wave 1. (Plan 06-02 wires the memo-presence + content assertion.)
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 (fast tier runs clean CPU-only)
    - `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` is empty after the run
    - source of spike/demo.jl contains the backtick argv form `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` with NO `$`-interpolation inside the backticks
    - source contains `val_rng(VAL_FIX_SEED)` and does NOT contain `val_rng(VAL_MASTER_SEED)`
    - source contains the twin-draw reproducibility `@assert` (r1.draws == r2.draws or isapprox)
    - `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` is clean
  </acceptance_criteria>
  <done>demo.jl loads, runs the decoupling assertion and a bit-reproducible fixture-scale chain proof on VAL_FIX_SEED, and exits 0 CPU-only; locked files byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fast-tier headline-number load, success-criteria table, --full subprocess dispatch, self-assert close</name>
  <read_first>
    - spike/demo.jl (the file created in Task 1 — extend it)
    - spike/validation/run_sbc.jl (lines 56-65 atomic _save_report; lines 141-160 rpad column table idiom; lines 74-75 anti-snooping guard asserts)
    - spike/validation/run_bf.jl (line 99 subprocess run() form for the isolated baseline)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-RESEARCH.md (Code Examples: verified report keys; Common Pitfalls 1 & 4; Assumptions Log A1)
    - .planning/phases/06-reproducible-demo-go-no-go-memo/06-PATTERNS.md (patterns 7-9)
  </read_first>
  <action>
    Extend spike/demo.jl. STEP 2 (headline numbers, DEMO-01): define const paths `SBC_REPORT/BF_REPORT/OOD_REPORT = joinpath(@__DIR__, "validation", "<name>_report.jld2")`. For each, guard with `isfile(...)` — the reports are gitignored so a fresh clone lacks them; on absence `@warn` with a message to run `--full` to regenerate (post-iteration numbers) rather than raising a raw load error. On presence, `jldopen(path,"r") do f ... end` pulling the verified keys: from sbc_report — labels, ks_p, chi2_p, ece, verdict, SBC_M, SBC_L; from bf_report — corr, max_abs_err, log_prior_odds; from ood_report — maha_auc, combined_auc, fam_best_auc, id_fire_rate. Explicitly print `bf["max_abs_err"]` so the memo (plan 06-02) can confirm the 10.95 figure before quoting it (RESEARCH Assumption A1).
    STEP 3 (success-criteria table, DEMO-01): print an rpad-column table (idiom run_sbc.jl:141-160) with one row per SC1 (pipeline reproduces — from the twin-draw assert), SC2 (decoupled — from the step-0 assert), SC3 (memo — presence reported, hard-gated in plan 06-02), and DEMO-01/02/03 pass/fail. CRITICAL: annotate every loaded headline number as POST-ITERATION / Set 2 (RESEARCH Pitfall 1) — never label these pre-registered. Include a one-line note that the pre-registered (Set 1) numbers live in 05-04-SUMMARY.md.
    STEP 4 (--full dispatch, DEMO-01-full): `if FULL`, spawn each reported gate as its OWN subprocess so exit codes stay independent and one gate's @testset throw does not abort the others (RESEARCH Open Q2): loop over ("run_sbc.jl","run_bf.jl","run_ood.jl") calling `run(\`julia --project=spike $(joinpath(@__DIR__, "validation", s))\`)`. Each run_*.jl already owns its consts/artifact/figures + anti-snooping guard asserts (run_sbc.jl:74-75) and exits nonzero on gate fail.
    CLOSE: self-assert idiom (02_simulator_demo.jl:148-152) — `@assert isfile(load_frozen_model default path)` style checks on the frozen nets that MUST exist, then `println("demo OK: ...")`. The verify command stays the bare `julia --project=spike spike/demo.jl` (no POSIX test -f; Windows-tauglich).
  </action>
  <verify>
    <automated>julia --project=spike spike/demo.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=spike spike/demo.jl` exits 0 and prints a success-criteria table to stdout with rows for SC1/SC2/SC3 and DEMO-01/02/03
    - stdout shows a "Set 2 / post-iteration" (or equivalent) annotation on the loaded headline numbers and a printed `bf max_abs_err` value
    - source contains `isfile(` guards on the three *_report.jld2 paths with a `@warn` fallback (no raw load error on a fresh clone)
    - source contains the `--full` subprocess dispatch `run(\`julia --project=spike $(joinpath(@__DIR__, "validation", s))\`)` over run_sbc/run_bf/run_ood
    - source ends with a `@assert isfile(...)` self-check on the frozen nets and a `println("demo OK` line
    - `julia --project=spike spike/test/runtests.jl` stays green (demo.jl does not break the aggregate gate)
  </acceptance_criteria>
  <done>Fast tier loads guarded headline numbers, prints an annotated success-criteria table, dispatches reported gates under --full as independent subprocesses, and self-asserts; runtests.jl stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARGS → demo.jl | Only external input is the `--full` flag (exact-match compare, no interpolation) |
| demo.jl → OS git | Shell-out for the decoupling proof; argv-literal, no shell string |
| demo.jl → src/ | Read-only via include() through contract.jl; never written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering/Elevation | git shell-out in step 0 | mitigate | Julia backtick argv form with three literal `src/` path tokens; NO string interpolation into the command (Pattern 4) |
| T-6-02 | Tampering | accidental src/ mutation | mitigate | The decoupling `@assert isempty` IS the control; demo.jl reaches src/ only read-only via include() through contract.jl |
| T-6-03 | Tampering/Supply chain | spike env deps | mitigate | No installs; `git diff --exit-code spike/Project.toml spike/Manifest.toml` must be empty at the wave gate |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — nothing to audit; pinned Manifest is byte-locked |
</threat_model>

<verification>
- `julia --project=spike spike/demo.jl` exits 0, CPU-only, prints the success-criteria table (SC1)
- `git status --porcelain -- src/ src/bayes.jl src/colocalization.jl` empty and asserted in-script (SC2/DEMO-02)
- `julia --project=spike spike/demo.jl --full` dispatches the three reported gates as independent subprocesses
- `git diff --exit-code spike/validation/consts.jl spike/Project.toml spike/Manifest.toml` clean
- `julia --project=spike spike/test/runtests.jl` still green
</verification>

<success_criteria>
demo.jl exists, runs clean CPU-only from a fixed seed at fixture scale, proves bit-reproducibility on VAL_FIX_SEED, asserts src/ decoupling, loads guarded post-iteration headline numbers, tabulates the success criteria, and dispatches reported-scale gates under --full — all with consts.jl/Project.toml/Manifest.toml byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-reproducible-demo-go-no-go-memo/06-01-SUMMARY.md` when done
</output>

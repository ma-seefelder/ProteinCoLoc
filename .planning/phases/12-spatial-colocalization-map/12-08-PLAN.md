---
phase: 12-spatial-colocalization-map
plan: 08
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - spike/test/capture_p12_golden.jl
  - spike/test/fixtures/p12_stage1_golden.jld2
  - spike/simulator/forward.jl
  - spike/test/test_p12_prior.jl
autonomous: true
requirements: [SPAT-02]

must_haves:
  truths:
    - "The golden fixture records what the simulator produced BEFORE stage 1 was touched, and that ordering is checkable afterwards"
    - "A theta with no rho_field reproduces the pre-edit simulator BYTE-FOR-BYTE"
    - "Stage 6 still performs exactly ONE resampling pass"
    - "A constant-valued FIELD agrees with the scalar path to a pre-registered tolerance, and the reason it is a tolerance and not equality is written down"
  artifacts:
    - path: "spike/test/capture_p12_golden.jl"
      provides: "One-shot pre-edit capture; refuses to run on a dirty simulator tree; embeds the base sha"
      contains: "p12_base_sha"
    - path: "spike/test/fixtures/p12_stage1_golden.jld2"
      provides: "Pre-edit stage-1 output for an explicit 8-field theta literal across six keys"
      contains: "channels"
  key_links:
    - from: "spike/simulator/forward.jl"
      to: "spike/simulator/p12_prior.jl"
      via: "stage 1 consumes an OPTIONAL rho_field through one defensive hasproperty binding"
      pattern: "hasproperty\\("
---

<objective>
Capture the pre-edit simulator, then make stage 1 accept a spatially-varying ρ without moving a single
byte of output on the path that does not use one.

Purpose: D-06 is the only Phase-12 change to a file the rest of the milestone depends on. Phase 11 built
exactly this safety apparatus for its stage-6 edit — a fixture captured **before** the edit, a script
that refuses to run on a dirty tree, the capture-time sha embedded inside the artifact, and exact `==` as
the pre-registered criterion — because a fixture regenerated afterwards compares the new code against
itself and passes vacuously, silently.
Output: `capture_p12_golden.jl`, its committed fixture, the stage-1 modification, and the regression
testsets appended to `test_p12_prior.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md
@.planning/phases/12-spatial-colocalization-map/12-RESEARCH.md

**Task order in this plan is load-bearing and is not a suggestion.** Task 1 captures and **commits** the
fixture. Only then does Task 2 touch `forward.jl`. An executor that reorders these has destroyed the
only evidence the regression test rests on, and the destruction is silent.

**A correction to the inherited VALIDATION row, stated up front.** `12-VALIDATION.md` lists "Field at
constant ρ reproduces the current simulator **bit-for-bit**". Read literally as *a constant-valued
field*, that is a floating-point falsehood: the separable upsample's row weights are `1-t` and `t`, whose
IEEE sum is 1 only up to rounding, so `A * F * B'` on a constant field differs from the scalar in the
last few ULP. The row is therefore split into two claims and both are tested:

- **Exact `==`, the pre-registered criterion:** a θ carrying **no** `rho_field` reproduces the pre-edit
  bytes exactly. Guaranteed by construction, because that path never enters the upsample.
- **Tolerance, pre-registered as a criterion and not as an escape hatch:** a constant-valued *field*
  agrees with the scalar path to `P12_STAGE1_CONSTFIELD_TOL`.

`P12_STAGE1_GOLDEN_TOLERANCE_FALLBACK` remains what 12-01 declared it to be — an escape hatch for the
**exact** arm whose use is a recorded deviation, not a criterion.
</context>

<tasks>

<task type="auto">
  <name>Task 1: capture_p12_golden.jl and its fixture — BEFORE any forward.jl edit</name>
  <files>spike/test/capture_p12_golden.jl, spike/test/fixtures/p12_stage1_golden.jld2</files>
  <read_first>
    - `spike/test/capture_p11_golden.jl` (READ IN FULL, 136 lines — the exact analog: the timing
      discipline header at :22-34, the explicit-θ-literal rationale at :54-65, the dirty-tree refusal
      plus sha embed at :77-93, the atomic write with integrity reopen at :96-117)
    - `spike/test/fixtures/` (confirm `p11_stage6_golden.jld2` is present and TRACKED — the precedent
      that a fixture of this kind is committed, not gitignored)
    - `spike/simulator/forward.jl:160-171` (stage 1 exactly as it stands — the bytes this fixture records)
    - `spike/validation/p12_consts.jl` (`P12_FIXTURE_COUNTER`, `p12_fix_rng`)
  </read_first>
  <action>
Create `spike/test/capture_p12_golden.jl`, mirroring `capture_p11_golden.jl` structurally.

Open with the TIMING DISCIPLINE header in the analog's voice at :22-34, adapted: this script must run,
and its artifact must be committed, **before a single byte of `spike/simulator/forward.jl` stage 1
changes**. A fixture regenerated afterwards would compare the new code against itself and pass
vacuously; that failure mode is silent, so it is made detectable two ways — the capture-time commit sha
is embedded inside the JLD2 as `p12_base_sha`, and the script refuses to run if the working tree is
dirty for the files the fixture claims to precede.

`THETA_GOLDEN_P12` — an **EXPLICIT 8-field θ literal**, deliberately not drawn from any prior sampler.
Copy the analog's reason at :54-65 and extend it: `sample_p12_prior`'s RNG consumption differs from
`sample_prior`'s by construction (it draws `r1` and a 64-cell field first), so a golden captured through
a sampler would differ after the edit for a reason that has nothing to do with stage 1. Use
`(ρ_true = 0.7, spillover = 0.1, autofluorescence = 0.05, label_efficiency = 0.85, shift_dx = 0.37,
shift_dy = -0.82, noise = 0.5, chromatic_eps = 0.0)` — the Phase-11 literal plus an **explicit**
`chromatic_eps = 0.0`, so the fixture is about stage 1 alone and the stage-6 term is pinned inert.
Assert in-file that `!hasproperty(THETA_GOLDEN_P12, :rho_field)` and `length(THETA_GOLDEN_P12) == 8`.

Dirty-tree refusal, copying `:77-93`: `git status --porcelain -- spike/simulator` must be empty, with
the analog's multi-line `@assert` message naming why. Then `base_sha = readchomp(...git rev-parse HEAD...)`
with the 40-char length assertion.

Capture: `channels = [simulate_pair(p12_fix_rng(P12_FIXTURE_COUNTER + k), THETA_GOLDEN_P12;
imsize = P12_GOLDEN_IMSIZE) for k in P12_GOLDEN_KEYS]`. Define `P12_GOLDEN_IMSIZE = (128, 128)` and
`P12_GOLDEN_KEYS = 1:6` **locally in this script**, not in `p12_consts.jl` — they are fixture geometry,
not a pre-registered threshold, and Tier 1 is closed. Record that reasoning in a comment. 128² clears
`simulate_pair`'s ≥64 guard with margin, keeps the committed fixture small, and exercises every stage.

Atomic write to `spike/test/fixtures/p12_stage1_golden.jld2` per `:96-117`: `.tmp` → `jldsave` with
`schema_version = 1`, `theta = THETA_GOLDEN_P12`, `channels`, `imsize = P12_GOLDEN_IMSIZE`,
`golden_keys = collect(P12_GOLDEN_KEYS)`, `p12_base_sha = base_sha`,
`generated = string(Dates.now(Dates.UTC)) * "Z"`, and a `caption` naming stage 1 and D-06 → reopen and
`@assert haskey(f, "channels") && haskey(f, "p12_base_sha")` → `mv(tmp, path; force = true)`.

**Commit this task on its own, before starting Task 2.** The commit message must state that `forward.jl`
is byte-unchanged at this sha. Record the sha in the plan summary by copying it from `git rev-parse HEAD`
output — never by retyping it.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/capture_p12_golden.jl && julia --project=spike -e 'using JLD2; d=JLD2.load("spike/test/fixtures/p12_stage1_golden.jld2"); @assert length(d["p12_base_sha"])==40 && length(d["channels"])==6 && !hasproperty(d["theta"], :rho_field) && length(d["theta"])==8; println(d["p12_base_sha"])'</automated>
  </verify>
  <acceptance_criteria>
    - The capture script exits 0; the fixture loads with a 40-char `p12_base_sha`, six key entries, and
      a θ of exactly 8 fields with no `rho_field`.
    - `git diff --quiet HEAD -- spike/simulator` succeeds **at the moment the capture runs**. If it does
      not, the script refuses, and that refusal is the correct outcome — not a blocker to work around.
    - The recorded `p12_base_sha` equals `git rev-parse HEAD` at capture time and is quoted in the plan
      summary, copied from command output.
    - Re-running the capture on an unchanged tree reproduces a byte-identical `channels` array (Philox
      is counter-based, so this is a real check): load both and compare with `==`, then keep the original.
    - The fixture is TRACKED, not gitignored: `git check-ignore -q spike/test/fixtures/p12_stage1_golden.jld2`
      exits non-zero.
    - This task's commit touches only `spike/test/capture_p12_golden.jl` and the fixture.
  </acceptance_criteria>
  <done>The pre-edit simulator behaviour is recorded, committed, and provably precedes the edit.</done>
</task>

<task type="auto">
  <name>Task 2: forward.jl stage 1 — an OPTIONAL rho_field, scalar path untouched</name>
  <files>spike/simulator/forward.jl</files>
  <read_first>
    - `spike/simulator/forward.jl` (READ IN FULL, 230 lines — especially :122-158 the θ validation block,
      :131-135 the `chromatic_eps_val` defensive-binding pattern this edit copies exactly, :160-171
      stage 1, and :192-218 stage 6 which this plan does **not** touch)
    - `spike/simulator/p12_prior.jl` (12-07's `p12_upsample` / `p12_upsample_ops` / `validate_rho_field`)
    - `spike/test/test_stage6_regression.jl:88-100` (the source-level tripwire
      `@test count("warp(", code) == 1`, which this edit must not break)
    - `12-RESEARCH.md` § Architecture Patterns → Pattern 3 (the per-pixel `a`/`b`/`sign` form)
  </read_first>
  <action>
Edit `spike/simulator/forward.jl` — **stage 1 only**. Nothing else in the file changes.

(a) Add ONE defensive local binding immediately after the existing `chromatic_eps_val` line at :135,
copying that line's comment voice in spirit: the ρ **field** is read through one `hasproperty` binding
exactly the way `chromatic_eps` is; a θ **without** `rho_field` is a valid input meaning "spatially
constant ρ" and takes the scalar branch UNCHANGED — which is what makes the golden fixture's exact `==`
criterion hold by construction rather than by luck.

(b) Extend the validation block: when the field binding is non-`nothing`, check finite, elementwise in
`[-1, 1]`, square, and both dimensions ≥ 2, throwing `ArgumentError` naming the offending value in the
`:136-158` voice. **`forward.jl` must not include `p12_prior.jl`** — that would invert the dependency and
pull the lattice into every existing consumer of the forward model. Inline a private
`_p12_check_field(F)` here instead, and record in its comment that the duplication with
`validate_rho_field` is deliberate and bounded, and that Task 3's cross-check on a shared set of bad
inputs is what keeps the two honest.

(c) Replace stage 1's three scalar bindings with a branch that leaves the scalar case byte-identical.
The three `_smooth_field` calls stay **before** the branch and in the same order, so RNG consumption is
identical on both paths — a field-carrying θ and a scalar θ on the same key see the same `L, ε1, ε2`.
State that in a comment; it is what makes Task 3's constant-field comparison meaningful at all.

```julia
    if rho_field_val === nothing
        a   = sqrt(abs(ρ))          # shared-component weight   (SCALAR PATH, UNCHANGED)
        b   = sqrt(1.0 - abs(ρ))    # private-component weight  (a² + b² = 1)
        sgn = sign(ρ)
    else
        ρpx = _p12_upsample_local(rho_field_val, imsize)  # D-06: two matmuls, not a per-pixel loop
        a   = sqrt.(abs.(ρpx))                            # a² + b² = 1 holds PER PIXEL
        b   = sqrt.(1.0 .- abs.(ρpx))
        sgn = sign.(ρpx)
    end
    ch1 = _softplus.(a .* L .+ b .* ε1)
    ch2 = _softplus.(sgn .* a .* L .+ b .* ε2)
```

(d) Add `_p12_upsample_local(F, imsize)` as a small private helper, implementing the same separable
`A * F * B'` as `p12_upsample` with `offset = false`, so `forward.jl` keeps zero new includes. Note in
its comment that the **offset** variant lives only in `p12_prior.jl` and that the 12-20 guard reaches it
by pre-rendering the field into a θ, never by a second branch here — one code path, one behaviour.

(e) Update `simulate_pair`'s docstring with one added sentence: a θ may optionally carry `rho_field`, a
G×G matrix in `[-1,1]`, in which case stage 1 mixes per pixel from its bilinear upsample; a θ without it
is unchanged in behaviour and in output bytes.

Do not touch stage 6. Do not add a second `warp(` call. Do not change `σ_psf`, `STRUCT_σ`, `BG_FLOOR`,
`GAIN` or `READ_NOISE`. Do not add a `using`. Do not add a `Project.toml` entry.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl 2>&1 | grep -iE "stage.6|stage6" | head</automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD -- spike/simulator/forward.jl` touches **only** the validation block, stage 1, the
      new private helper and the docstring. Zero lines change inside stage 6 (`:192-218`) or in the
      constants block (`:66-76`).
    - `spike/test/test_stage6_regression.jl` is still green, including its source-level
      `count("warp(", code) == 1` assertion — Phase 11's tripwire is untouched.
    - The file still has **zero** `include` statements and the same `using` list as before the edit:
      `git diff HEAD -- spike/simulator/forward.jl | grep -c '^+using\|^+include'` returns `0`.
    - `julia --project=. -e 'using Pkg; Pkg.test()'` is unchanged — the root package does not consume
      `spike/simulator/forward.jl`, and this is the wave's decoupling proof.
    - `git diff --quiet HEAD -- src corpus spike/Project.toml spike/Manifest.toml` succeeds.
  </acceptance_criteria>
  <done>Stage 1 mixes per pixel when handed a field and is byte-unchanged when not.</done>
</task>

<task type="auto">
  <name>Task 3: append the golden-regression testsets to test_p12_prior.jl</name>
  <files>spike/test/test_p12_prior.jl</files>
  <read_first>
    - `spike/test/test_stage6_regression.jl` (READ IN FULL, 197 lines — the exact analog: the
      "EXACT EQUALITY IS THE PRE-REGISTERED CRITERION AND IT IS NOT NEGOTIABLE ... DO NOT SOFTEN A
      FAILING ASSERTION TO A TOLERANCE COMPARISON IN THIS FILE" header at :23-34, the guarded includes
      at :62-67, `_strip_comment_lines` at :88-100, the exact `==` assertion at :112, and the
      refuse-a-regenerated-fixture block at :129-134)
    - `spike/test/test_p12_prior.jl` (12-07's version — this task APPENDS testsets; it does not rewrite
      the eleven that are already there)
    - `spike/test/fixtures/p12_stage1_golden.jld2` (Task 1's artifact)
    - `spike/validation/p12_consts.jl` (`P12_STAGE1_EXACT`, `P12_STAGE1_CONSTFIELD_TOL`,
      `P12_STAGE1_GOLDEN_TOLERANCE_FALLBACK`, `P12_FIXTURE_COUNTER`)
  </read_first>
  <action>
Append five testsets to `spike/test/test_p12_prior.jl`. Do not modify the existing eleven.

12. `"the golden fixture PRECEDES the stage-1 edit (D-06)"` — load the fixture; assert
    `length(golden["p12_base_sha"]) == 40`; assert `!hasproperty(golden["theta"], :rho_field)` and
    `length(golden["theta"]) == 8`, copying the analog's `:129-134` reasoning verbatim in spirit: a
    fixture **regenerated after** the edit would carry a `rho_field` and would compare the new code
    against itself, passing vacuously — refuse that fixture outright. Additionally assert the ordering is
    checkable: `@test success(Cmd(\`git merge-base --is-ancestor <sha> HEAD\`; dir = repo_root))` when git
    is available, guarded by the `test_p13_result.jl:193-196` `_has_git` predicate, with an `@info` skip
    otherwise.

13. `"a theta with no rho_field reproduces the golden bytes EXACTLY (P12_STAGE1_EXACT)"` — for each of
    the six recorded keys, re-run `simulate_pair(p12_fix_rng(P12_FIXTURE_COUNTER + k), golden["theta"];
    imsize = golden["imsize"])` and assert `collect(new) == collect(golden["channels"][k])` with `==`,
    not `isapprox`. Carry the analog's header sentence into this testset's comment: exact equality is the
    pre-registered criterion and it is not negotiable; **do not soften a failing assertion to a tolerance
    comparison in this file**. If it fails, the correct response is to find what moved in stage 1, and
    the only authorised alternative is the recorded-deviation escape hatch
    `P12_STAGE1_GOLDEN_TOLERANCE_FALLBACK`, whose *use* must be written into the plan summary as a
    deviation. Assert `@test P12_STAGE1_EXACT == true` in the same testset so the contract is visible
    where it is enforced.

14. `"a CONSTANT field agrees with the scalar path to the pre-registered tolerance"` — for
    `ρ = golden["theta"].ρ_true`, build `θf = merge(golden["theta"], (rho_field = fill(ρ, 8, 8),))` and
    compare against the golden bytes with `maximum(abs, ...) ≤ P12_STAGE1_CONSTFIELD_TOL` per key and per
    channel. Comment: this is a **tolerance by construction, not a softened criterion** — the upsample's
    row weights `1-t` and `t` sum to 1 only up to IEEE rounding, so the constant-field path differs from
    the scalar in the last few ULP. Record the measured `maximum(abs, ...)` with `@info` so the phase
    report can quote it. Also assert the two paths are **not** identical objects, i.e. that the field
    branch actually ran: `@test θf.rho_field !== nothing` and that removing `rho_field` recovers exact
    equality (testset 13 already proves that half).

15. `"a NON-constant field changes the image, and changes it spatially"` — draw a real field from
    `sample_p12_prior(p12_fix_rng(P12_FIXTURE_COUNTER); r1 = 0.5)`, splice it into the golden θ, simulate
    at `(256, 256)`, and assert (a) the output differs from the golden-θ output at the same key, and
    (b) the per-patch correlation grid of the result has a larger across-region spread than the
    constant-ρ control does at the same key. (b) is the assertion that the field is actually reaching the
    patch summary rather than being averaged away by the PSF — Pitfall 2 measured the constant-ρ
    across-region spread at **0.0847** against a per-draw noise sd of **0.0662**, so state that a modest
    excess is expected and that this testset is a directional check, not the D-12 gate.

16. `"the two field validators agree (the deliberate duplication is bounded)"` — for a shared list of bad
    inputs (an entry `> 1`, an entry `< -1`, a `NaN`, a non-square matrix, a 1×1 matrix), assert both
    `validate_rho_field` and a `simulate_pair` call carrying that field throw. This is the test the
    `forward.jl` comment names as the thing keeping the inlined `_p12_check_field` honest with
    `p12_prior.jl`'s `validate_rho_field`.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl 2>&1 | grep -A4 "golden bytes EXACTLY"</automated>
  </verify>
  <acceptance_criteria>
    - All sixteen testsets in `test_p12_prior.jl` appear in the suite output with zero failures and zero
      errors, and the earlier Phase-12 testsets are still green.
    - Testset 13 uses `==` and contains no `isapprox`, no `≈`, and no `atol`/`rtol` on the golden
      comparison. Assert this with a comment-stripped source read inside the file itself, mirroring
      `test_stage6_regression.jl`'s own source-level tripwire.
    - Testset 13 fails if a scratch copy of `forward.jl` reorders the three `_smooth_field` calls
      (verify once, revert, do not commit) — this proves the exact arm is sensitive to the thing it
      claims to guard.
    - The measured `maximum(abs, ...)` from testset 14 is recorded in the plan summary as a number, and
      it is below `P12_STAGE1_CONSTFIELD_TOL`. If it is not, the correct response is to record the
      measured value as a Tier-2 provenance line in a **later** plan's append block, never to edit
      Tier 1.
    - `grep -c P12_PENDING_SCAFFOLD spike/test/test_p12_prior.jl` returns `0`.
  </acceptance_criteria>
  <done>The exact arm, the tolerance arm and the fixture's precedence are all executable assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-edit simulator → post-edit simulator | The only evidence that the edit is behaviour-preserving is a fixture that must predate it |
| `forward.jl` → the rest of the milestone | Every existing consumer of the forward model inherits this edit; a silent change here contaminates Phase-13 results too |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-28 | Repudiation | a fixture regenerated after the edit | mitigate | `p12_base_sha` embedded in the artifact; dirty-tree refusal in the capture script; testset 12 asserts the sha is an ancestor of HEAD and that the golden θ carries no `rho_field` |
| T-12-29 | Tampering | stage-1 output bytes on the legacy path | mitigate | Scalar branch preserved literally; the three `_smooth_field` calls stay before the branch; testset 13 asserts exact `==` across six keys and forbids softening |
| T-12-30 | Tampering | a second resampling pass entering stage 1 | mitigate | The existing `count("warp(", code) == 1` tripwire in `test_stage6_regression.jl` stays green; acceptance criterion forbids a `warp` in the new code |
| T-12-31 | Elevation of privilege | `forward.jl` acquiring a dependency on the lattice | mitigate | Zero new `include`/`using` in `forward.jl`, asserted by a `git diff` grep; the bounded duplication is cross-checked by testset 16 |
| T-12-27 | Tampering | an out-of-range field reaching the mixer | mitigate | `_p12_check_field` throws in the validation block, before any pixel work |
| T-12-07 | Tampering | git index race with Phase 13 | mitigate | Three separate pathspec-scoped commits, one per task |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` shows all sixteen `test_p12_prior` testsets and the
  pre-existing `test_stage6_regression` testsets green.
- `spike/test/fixtures/p12_stage1_golden.jld2` is tracked, and its `p12_base_sha` is an ancestor of HEAD
  whose tree has `forward.jl` byte-identical to the pre-edit version
  (`git show <sha>:spike/simulator/forward.jl | git hash-object --stdin` compared against
  `git rev-parse <sha>^{tree}`-derived blob id; record both in the summary).
- `git diff --quiet HEAD -- src corpus spike/Project.toml spike/Manifest.toml` succeeds.
- `julia --project=. -e 'using Pkg; Pkg.test()'` unchanged.
</verification>

<success_criteria>
The simulator renders a spatially-varying ρ field by two matmuls; a θ without a field is byte-identical
to the pre-edit simulator across six keys under exact `==`; a constant field agrees to a pre-registered
tolerance whose reason is written down; stage 6 and the one-resampling-pass tripwire are untouched; and
the fixture that proves all of it is provably older than the edit.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-08-SUMMARY.md` when done, quoting
`p12_base_sha` (copied from git output), the exact-arm result, and the measured constant-field
`maximum(abs, ...)`.
Commit pathspec-scoped, THREE separate commits in task order:
1. `git commit -- spike/test/capture_p12_golden.jl spike/test/fixtures/p12_stage1_golden.jld2`
2. `git commit -- spike/simulator/forward.jl`
3. `git commit -- spike/test/test_p12_prior.jl`
</output>
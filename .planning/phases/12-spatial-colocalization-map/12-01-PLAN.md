---
phase: 12-spatial-colocalization-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/validation/p12_consts.jl
  - spike/test/test_p12_consts.jl
  - spike/test/test_p12_suite.jl
  - spike/test/test_p12_lattice.jl
  - spike/test/test_p12_prior.jl
  - spike/test/test_p12_architecture.jl
  - spike/test/test_p12_datagen.jl
  - spike/test/test_p12_train.jl
  - spike/test/test_p12_result.jl
  - spike/test/test_p12_sbc.jl
  - spike/test/test_p12_coverage.jl
  - spike/test/test_p12_decoupling.jl
  - spike/test/runtests.jl
autonomous: true
requirements: [SPAT-09]

must_haves:
  truths:
    - "Every Phase-12 threshold and seed is committed as a literal before any Phase-12 run exists"
    - "The Phase-12 DEV stream is provably disjoint from every reserved stream in the repo, including Phase 13's"
    - "A Phase-12 testset that is added later cannot silently never run"
    - "A constant that gates is distinguishable, mechanically, from a constant that only reports"
  artifacts:
    - path: "spike/validation/p12_consts.jl"
      provides: "Tier-1 pre-registration: seeds, salts, counters, ladders, gate thresholds, ITERATION_ALLOWANCE"
      contains: "P12_DEV_SEED"
    - path: "spike/test/test_p12_consts.jl"
      provides: "Literal re-assertion + executable disjointness + wiring manifest"
      contains: "_p12_forbidden"
    - path: "spike/test/test_p12_suite.jl"
      provides: "Single aggregator include point for all Phase-12 testsets"
      contains: "test_p12_decoupling.jl"
  key_links:
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_p12_suite.jl"
      via: "include inserted immediately BEFORE the test_p13_correction.jl include"
      pattern: "test_p12_suite\\.jl"
    - from: "spike/validation/p12_consts.jl"
      to: "test/gate/gate_consts_8_v2.jl"
      via: "isolated GateV2-style module reading SBC_IMSIZE_SET (never retyped)"
      pattern: "SBC_IMSIZE_SET"
---

<objective>
Freeze the Phase-12 pre-registration before a single Phase-12 number exists, and wire the Phase-12
test surface into `spike/test/runtests.jl` at the one position where it can actually run.

Purpose: every gate in this project locks thresholds and seeds first (R-5). Phase 12 additionally
inherits an include-ordering trap that makes a late-wired testset report green by never executing.
Both are closed here, in Wave 1, so no later plan can open them.
Output: `spike/validation/p12_consts.jl` (Tier 1 only), its literal-assertion test, an aggregator
suite file, nine green pending scaffolds, and one line added to `runtests.jl`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-CONTEXT.md
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md
@.planning/phases/12-spatial-colocalization-map/12-VALIDATION.md

**Orchestrator rulings binding on this plan.**
- R-5: Tier 1 lands here, before anything runs. Tier 2 is APPENDED later under a different sentinel,
  never edited. No constant in either block is ever modified — a diff that modifies a line below is a
  pre-registration breach by construction.
- The spike-lane θ baseline is **8 fields** (`chromatic_eps` appended last), not 7. `NPE_D = 7`
  describes the shipped estimator only.
- Reported ≠ gated: only the D-12 Stage-1 and Stage-2 thresholds gate. Atom mass, ε identifiability,
  vacuous-column shrinkage and the S-4/D-06 guards are reporting-only. This distinction is made
  machine-checkable here via two disjoint constant-name tuples.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-1 pre-registration file</name>
  <files>spike/validation/p12_consts.jl</files>
  <read_first>
    - `spike/validation/p11_consts.jl` (READ IN FULL — this is the file R-5 says to mirror exactly:
      the two-tier header contract, the guard-block idiom, the forbidden-seed set, the reserved
      counters, the threshold-derivation voice, the iteration-allowance block, and the
      `module GateV2 ... end` placement OUTSIDE the guard block)
    - `spike/p13/consts.jl:100-200` (the Phase-13 seeds and salt inventory that `p11_consts.jl`
      predates and therefore does not forbid)
    - `spike/data/p11_generate.jl:85-95` (`P11_DATAGEN_SALT`, which is **the same literal** as
      `P13_SALT` and is absent from Phase 13's `P13_REPO_SALTS` — a real gap this file closes)
    - `spike/validation/consts.jl:38-90` (the single-guard-block idiom and the `SBC_M`/`SBC_L`/
      `SBC_BINS` values Phase 12 reuses)
    - `spike/simulator/prior.jl:39-58` (`MU_PRIOR`, `SIM02_W1_TOL = 0.10`, and the
      pre-declared-tolerance voice)
    - `spike/simulator/ghat.jl:42-67` (the frozen-numeric-artifact documentation discipline)
  </read_first>
  <action>
Create `spike/validation/p12_consts.jl` in the `p11_consts.jl` shape, `#= =#` AGPL header dialect of
its `spike/validation/` neighbours.

Structure, in order:

(a) A "TWO-TIER STRUCTURE — READ THIS BEFORE EDITING ANYTHING" header block, copying the voice of
`p11_consts.jl:30-48`. Tier 1 is the single guard block keyed on `:P12_DEV_SEED`.

**Tier 2 is a SEQUENCE of append-only blocks, each under its OWN sentinel** — not one block. Two are
reserved here, and the header must name both together with the plan that opens each, because a second
append that reuses the first block's sentinel would be silently skipped once the first exists, and one
with no guard at all is a hard `const` redefinition error on re-`include`:
- `:P12_CHOSEN_PRIOR` — opened by **12-15** (`P12_CHOSEN_PRIOR`, `P12_K_PROD`, `P12_D_PROD`)
- `:P12_FISHERZ_NEFF` — opened by **12-16** (`P12_FISHERZ_NEFF`)
No block is ever edited once written; constants are only ever appended, so this file's git history is
itself the pre-registration audit trail.

(b) OUTSIDE the guard block (Julia rejects a non-top-level `module`), an isolated
`module GateV2P12 ... include(joinpath(@__DIR__, "..", "..", "test", "gate", "gate_consts_8_v2.jl")) ... end`
following `p11_consts.jl:70-77`, used solely to READ the frozen image-size mixture. Do not retype
the mixture.

(c) Inside `if !isdefined(@__MODULE__, :P12_DEV_SEED)`, `import Random123: Philox4x`, then:

FORBIDDEN SEEDS — every literal read from its defining site, not from a summary:
`NPE_MASTER_SEED = 0x0000_0000_00C0_FFEE`, `VAL_MASTER_SEED = 0x0000_0000_5BC0_FFEE`,
`VAL_FIX_SEED = 0x0000_0000_00F1_F7ED`, `DEFAULT_MASTER_SEED = 0x0000_0000_0000_0001`,
`RATIO_PAIR_SEED = 0x0000_0000_004A_7107`, `CORPUS_MASTER_SEED = 0x0000_0000_00C0_5EED`,
`P11_DEV_SEED = 0x0000_0000_0B11_DE71`, `P13_DEV_SEED = 0x0000_0000_0B13_DE71`,
`P13_FIX_SEED = 0x0000_0000_0B13_F1F7`, plus `F2_DEV_SEEDS` and `SPIKE_DEV_SEEDS` copied from
`p11_consts.jl`, plus `P11_RESEARCH_BURNED` copied from `p11_consts.jl`, plus the full 25-entry
`P13_BURNED_DEV_SEEDS` tuple copied from `spike/p13/consts.jl:120-146`. Recompute — never trust from a
comment — the derived families `PROD_SEED` and `PROD_SEED_V2` through the isolated module, exactly as
`p11_consts.jl:112-125` does, and assert the recomputation reproduces the frozen file.

SALT INVENTORY: copy Phase 13's eight-entry `P13_REPO_SALTS` into `P12_REPO_SALTS` and **append two
entries Phase 13 omitted**: `0x2545_F491_4F6C_DD1D` (this single literal is BOTH `P13_SALT`
(`spike/p13/consts.jl:192`) AND `P11_DATAGEN_SALT` (`spike/data/p11_generate.jl:90`) — record that
collision in the comment, because it is exactly the kind of shared key the inventory exists to
surface) and `0xA24B_AED4_663E_E121` (`P11_SALT`) if not already present.

FRESH STREAMS: `P12_DEV_SEED = 0x0000_0000_0B12_DE71` ("0B12" = phase 12, "DE71" = DEV-1),
`P12_FIX_SEED = 0x0000_0000_0B12_F1F7`, `P12_SALT = 0xFF51_AFD7_ED55_8CCD`,
`P12_DATAGEN_SALT = 0xC2B2_AE3D_27D4_EB4F`. Both salts must be asserted absent from
`P12_REPO_SALTS`. Provide `p12_rng(counter = 0) = Philox4x(UInt64, (UInt64(P12_DEV_SEED) ⊻ P12_SALT, UInt64(counter)))`
and `p12_fix_rng` off `P12_FIX_SEED`.

RESERVED COUNTERS, one per reported runner, following `p11_consts.jl:146-159`:
`P12_SIM02_COUNTER = 1`, `P12_STAGE1_COUNTER = 2`, `P12_ELL_RIDGE_COUNTER = 3`,
`P12_EPS_RIDGE_COUNTER = 4`, `P12_MINISPIKE_COUNTER = 5`, `P12_DATAGEN_COUNTER = 6`,
`P12_SBC_COUNTER = 7`, `P12_COVERAGE_COUNTER = 8`, `P12_GUARDS_COUNTER = 9`,
`P12_FIXTURE_COUNTER = 99` (FIXTURES ONLY — never a reported counter).

LATTICE / GEOMETRY: `P12_G = 8`, `P12_K_DEV = 63` (D-04 high-rank = every non-constant DCT mode),
`P12_D_MINISPIKE = 1 + P12_K_DEV + 7 + 1` (= 72: global c₀ + 63 deviations + the 7 non-ρ nuisances
+ the correlation-length row, appended LAST), `P12_R1_MIN = 0.05`, `P12_R1_MAX = 0.95`,
`P12_R1_LADDER = (0.05, 0.25, 0.50, 0.75, 0.95)`, `P12_ABLATION_R1 = 0.0` (D-10 / R-4: the matched
ablation RETRAINS on this, it is not a read-time pin), `P12_GP_JITTER = 1e-8`,
`P12_R1_SOLVE_TOL = 1e-6`, `P12_CAR_ALPHA_BRACKET = (0.0, 0.99999)`, `P12_GP_ELL_BRACKET = (0.05, 20.0)`.
Record in the comment why the arms are parametrized by induced lag-1 correlation and not by α or ℓ
(R-8: lag-1 goes 0.136 → 0.947 across α = 0.5 → 0.999, so a uniform prior on α puts nearly all mass
on "no spatial structure" and walks D-08 into the S-1 prior-echo trap).

THE r₁ PRIOR ITSELF — `P12_R1_PRIOR = Uniform(P12_R1_MIN, P12_R1_MAX)`. This is **π(θ)**, not a
convenience alias, so it is Tier 1 by the same logic that puts `MU_PRIOR` in `prior.jl`: 12-07 draws from
it, and 12-15's whole comparison rests on "both arms drawn from the SAME `P12_R1_PRIOR`" at matched
induced lag-1 correlation. Bind the distribution object once here so no plan can construct its own and
quietly compare two different priors — the R-8 trap in a different guise. `import Distributions: Uniform`
alongside the `Random123` import; Distributions is already a spike dependency.

SIM-02: `P12_SIM02_W1_TOL_PERREGION = 0.10`, with an explicit comment that it is identical to
`prior.jl`'s `SIM02_W1_TOL`, is fixed BEFORE the field simulator runs, and is deliberately NOT
reverse-engineered from any measured value; the assertion is on the **max** over the 64 regions,
never the mean. `P12_SIM02_M = 20_000`.

D-12 STAGE 1 (**GATING**): `P12_STAGE1_N = 25_000`, `P12_STAGE1_IMSIZE = (512, 512)`,
`P12_STAGE1_TEST_FRACTION = 0.25`, `P12_STAGE1_RATIO_CEILING = 0.95`,
`P12_STAGE1_CONTROL_CEILING = 0.5`, `P12_STAGE1_MIN_RUNGS = 1`. Record the derivation VERBATIM in the
threshold-derivation voice of `p11_consts.jl:213-241`: with `P12_STAGE1_TEST_FRACTION` of
`P12_STAGE1_N` split across the five ladder rungs, n_test = 0.25 · 25 000 / 5 = **1250 DATASETS** per
rung; the sampling sd of an RMSE ratio at that n is ≈ 1/√(2·n_test) = 0.0200; requiring 2.5 sd of
separation from 1.0 gives 1 − 2.5·0.0200 = 0.950. Declare
`P12_STAGE1_N_TEST_PER_RUNG_DERIVED = 1250` so the arithmetic is re-derivable inside the test.
**Add one sentence recording the unit the bar is APPLIED to, because it is not the unit it was derived
for:** the derivation above is the sampling sd of a *single-region* RMSE ratio, while 12-11 gates on
`ratio_mean`, the mean over 64 regions, whose sd is no larger. The gate is therefore **conservative**, not
mis-scaled — but the derivation will be quoted verbatim in `12-STAGE1-VERDICT.md` and a reader must not
take it as describing the gated statistic. 12-11 additionally reports `ratio_max` against the same bar so
the per-region reading exists.
**Record explicitly WHY the count is datasets and not region-draws:** each test dataset contributes 64
region-draws, but the 64 regions of one image share every nuisance and the global term, so counting
region-draws as independent would overstate the power of the gate by up to 8× in sd terms. The
derivation deliberately counts the independent unit. Note the compute consequence in the same comment:
the research sized the Stage-1 ridge at ~6 min for 5 000 samples at 512², so 25 000 is ~30 min — still
an order of magnitude cheaper than the ~1.8 h mini-spike this gate exists to protect, which is the whole
point of D-12 Stage 1.

D-12 STAGE 2 (**GATING**): `P12_SBC_M = 2000`, `P12_SBC_L = 999`, `P12_SBC_BINS = 50` (unchanged from
`consts.jl` on purpose — the settled fix for the M=2000 over-power finding is the TEST, not the
power), `P12_SBC_TARGET_KS_ALPHA = 0.01` (strict point-null, TARGETS ONLY: the global term and Δρ),
`P12_SBC_TOST_DELTA = 0.10`, `P12_SBC_TOST_ALPHA = 0.05` (nuisance-appropriate equivalence test for
the 7 nuisances, the correlation-length row and the high-index deviation coefficients),
`P12_COVERAGE_NOMINAL = 0.90`, `P12_STAGE2_COVERAGE_TOST_DELTA = 0.03`, `P12_STAGE2_N_MIN = 271` with
the Wilson derivation recorded verbatim (`z·√(p(1−p)/N) = 1.644854·√0.09/√N = 0.49346/√N ≤ 0.03
⇒ √N ≥ 16.449 ⇒ N ≥ 270.6 ⇒ 271`), `Z_TWO_SIDED_90 = 1.644854`, and
`P12_STAGE2_LOGSCORE_MIN = 0.02` (nats per held-out region, the minimum mean-log-predictive-density
improvement of the spatial arm over the matched ablation). Add a comment stating the pre-registered
rule in words: **a coverage-only win with no scoring-rule improvement is NOT a pass**, and a
miscalibrated arm is disqualified rather than "better".

TRAINING AUGMENTATION (Pitfall 4): `P12_MASK_K_SET = 0:8` (MCAR random-region masking, k regions per
training sample drawn uniformly from this set, i.e. 0–12.5 % of the 64 regions), so that D-09's
held-out configuration is in-distribution.

REPORTING-ONLY BARS (declared in advance so they are comparable, but explicitly NOT gates):
`P12_RADIAL_ENERGY_CEILING = 0.50` (S-4; the design argument is that a field whose radial component
carries at least half its energy cannot be linearly separated from a purely radial nuisance),
`P12_OFFSET_GRID_TOL = 0.10` (D-06 half-cell offset arm, relative degradation in per-region RMSE and
coverage), `P12_VACUOUS_SHRINKAGE_FLOOR = 0.90` (`shrinkage = post_sd / prior_sd`; F3 boolean flag).

BUDGET: `P12_N_PAIRS = 50_000`, `P12_MINISPIKE_N = 10_000`, `P12_MINISPIKE_IMSIZE = (512, 512)`,
`P12_DATAGEN_WALLCLOCK_CEILING_MIN = 150`, `P12_MINISPIKE_WALLCLOCK_CEILING_MIN = 150`,
`P12_IMSIZE_SET = GateV2P12.SBC_IMSIZE_SET`, `P12_IMSIZE_WEIGHTS = GateV2P12.SBC_IMSIZE_WEIGHTS`
(READ, never retyped — a retyped mixture is a mixture that can silently drift).

ITERATION LEDGER: `P12_ITERATION_ALLOWANCE = 1`, in the voice of `p11_consts.jl:319-328`.

REPO ROOTS — **TWO** OF THEM, AND CONFLATING THEM DEFEATS 12-17's DURABILITY RULE:

`p12_repo_root()` — the **enclosing** checkout. Walk up from `@__DIR__` until a `.git` entry is found,
testing **`isdir(...) || isfile(...)`**, because in a git worktree `.git` is a *file* containing a gitdir
pointer, not a directory (`spike/test/test_p13_result.jl:198-201` already handles both and is the
precedent to mirror). Bind `const P12_REPO_ROOT = p12_repo_root()`.

`p12_primary_checkout()` — the **primary** checkout, a *different directory* when the caller runs inside
a worktree. **The walk cannot answer this question, and the reason is precise: it ACCEPTS a worktree's
`.git` file rather than FOLLOWING it.** `isdir(...) || isfile(...)` establishes only that a `.git` entry
exists; the `test_p13_result.jl:198-201` precedent it comes from is answering "is this a checkout at all,
so may I shell out to git" — an *availability* question — not "where is the durable checkout". Inside a
worktree the first `.git` the walk meets is the worktree's pointer file, so `p12_repo_root()` returns the
worktree root, and a bundle written there is exactly what `git worktree remove --force` deletes.

Resolve it with git rather than by parsing the pointer:
```julia
# --git-common-dir is the PRIMARY .git even when called from a linked worktree.
common = readchomp(`git -C $(P12_REPO_ROOT) rev-parse --path-format=absolute --git-common-dir`)
primary = dirname(common)          # parent of the primary .git == the primary checkout
```
Fall back to `P12_REPO_ROOT` only when `Sys.which("git") === nothing`, and emit a `@warn` that **names
the degradation** ("primary-checkout resolution degraded to the enclosing checkout; a bundle written here
will not survive a worktree cleanup") rather than silently returning the wrong root. Bind
`const P12_PRIMARY_CHECKOUT = p12_primary_checkout()`.

**Do NOT add a `occursin("worktree", path)` substring test anywhere.** It is redundant once the root is
git-derived, and it is actively harmful: inside a worktree `P12_REPO_ROOT` itself contains the substring,
so a criterion demanding both "under the root" and "no `worktree` component" is unsatisfiable and would
make 12-17 throw unconditionally — potentially after hours of training. The git-derived root IS the
check.

Which consumer uses which, stated here so no plan has to guess:
- **12-17 and 12-14 Task 3 use `P12_PRIMARY_CHECKOUT`** — trained bundles and pools must survive a
  worktree cleanup, so they must land in the durable tree. Their assertion is "under
  `P12_PRIMARY_CHECKOUT` and containing no `worktree` path component".
- **12-19 uses `P12_REPO_ROOT`** — `test/test_images/` and `corpus/` are *tracked*, so they exist in
  whatever checkout is executing, and the sealed-holdout refusal should confine paths to the checkout the
  run is actually reading from. Using the primary root here would be wrong in the opposite direction.

Both belong in Tier 1 rather than in a test file because runners depend on them for correctness and one
use is the phase's only irreversible risk: 12-19's executable refusal is what keeps the Phase-16 sealed
holdout sealed. 12-05 must READ these bindings rather than re-deriving its own copy.

DESCOPE ROUTING, MADE EXECUTABLE (rather than prose repeated in six plans):
`p12_stage1_verdict()` returning `:proceed | :descope | :absent`, parsing the unique anchored line
`^VERDICT: (PROCEED|DESCOPE)$` from `12-STAGE1-VERDICT.md` and throwing if the file exists but no such
line matches — never defaulting to either outcome. Plus
`p12_require_proceed(plan_id::AbstractString)`, which throws unless the verdict is `:proceed`, quoting
12-11's routing sentence verbatim in the message. **The four descope-excluded runners (12-17, 12-18,
12-19, 12-20) call it as their first statement.** 12-14 does not — it runs on both paths and applies
its own narrower `arm = :none` rule instead. Record here that the routing existing only as prose in
two documents is what makes a wrong dispatch possible, and that one function with four call sites is
the fix.

DECLARED DEVIATIONS: `P12_RESEARCH_NET_DEVIATIONS`, a 5-tuple, asserted with
`@assert length(P12_RESEARCH_NET_DEVIATIONS) == 5`, enumerating: (1) a CNN over `(G,G,2,K)` replaces
the shipped MLP over 128 rows (D-02); (2) θ widens to `1 + K + 7 + 1` with DCT coefficients replacing
scalar `ρ_true` (D-01/D-08); (3) the lattice-prior field replaces scalar ρ in forward stage 1
(D-05/D-06); (4) MCAR random region masking as a training-time augmentation (Pitfall 4); (5) per-row
z-scoring retained as the default arm, shared-scalar scaling declared as an alternative taken only if
the CNN underperforms the MLP control, and whichever is used must be recorded (R-3).

GATED-VS-REPORTED, made machine-checkable:
`P12_GATING_CONSTANTS = (:P12_STAGE1_RATIO_CEILING, :P12_STAGE1_CONTROL_CEILING, :P12_STAGE1_MIN_RUNGS,
:P12_SIM02_W1_TOL_PERREGION, :P12_SBC_TARGET_KS_ALPHA, :P12_SBC_TOST_DELTA,
:P12_STAGE2_COVERAGE_TOST_DELTA, :P12_STAGE2_LOGSCORE_MIN, :P12_STAGE2_N_MIN)` and
`P12_REPORTING_ONLY_CONSTANTS = (:P12_RADIAL_ENERGY_CEILING, :P12_OFFSET_GRID_TOL,
:P12_VACUOUS_SHRINKAGE_FLOOR)`, with an in-file `@assert isempty(intersect(...))`.

STAGE-1 GOLDEN REGRESSION (D-06), THREE constants, and the distinction between them must be written
into the comment because it is the difference between a criterion and an excuse:
`P12_STAGE1_EXACT = true` — a θ carrying **no** `rho_field` must reproduce the pre-edit simulator
byte-for-byte; `==` is the contract, in the voice of `p11_consts.jl`'s `P11_STAGE6_EXACT`.
`P12_STAGE1_CONSTFIELD_TOL = 1e-12` — a **criterion, not a fallback**: a constant-VALUED field goes
through the separable upsample, whose row weights `1-t` and `t` sum to 1 only up to IEEE rounding, so a
constant field agrees with the scalar path to a few ULP and *cannot* be bit-identical. Record that
reasoning here, because `12-VALIDATION.md`'s row reads as though it could be.
`P12_STAGE1_GOLDEN_TOLERANCE_FALLBACK = 1e-12` — the escape hatch for the **exact** arm only, in the
voice of `p11_consts.jl:314-317`: its USE is a recorded deviation meaning an upstream numerics change,
not a pass.

(d) Foot-of-block executable self-checks: `@assert !(UInt64(P12_DEV_SEED) in _p12_forbidden())`,
same for `P12_FIX_SEED`, `@assert length(unique(_p12_forbidden_list())) == length(_p12_forbidden_list())`,
`@assert P12_SALT ∉ P12_REPO_SALTS`, `@assert P12_DATAGEN_SALT ∉ P12_REPO_SALTS`,
`@assert PROD_SEED_V2 == GateV2P12.PROD_SEED_V2`.

(e) Closing negative assertions that NEITHER Tier-2 block exists yet — one per reserved sentinel, so
the file side and the test side stay symmetric:
`@assert !isdefined(@__MODULE__, :P12_CHOSEN_PRIOR)` and
`@assert !isdefined(@__MODULE__, :P12_FISHERZ_NEFF)`. Each is removed in the same commit that appends
its own block (12-15 and 12-16 respectively), and only then. Two sentinels means two assertions: a
single one would leave 12-16's append with a test-side negative assertion to retire but no file-side
counterpart, which reads as an omission to a later auditor.

Do NOT add any entry to `spike/Project.toml`. This file imports only `Random123` (already a dep).
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/validation/p12_consts.jl"); @assert P12_DEV_SEED === 0x0000_0000_0B12_DE71; @assert P12_D_MINISPIKE == 1 + P12_K_DEV + 7 + 1 == 72; @assert P12_ITERATION_ALLOWANCE == 1; @assert isempty(intersect(P12_GATING_CONSTANTS, P12_REPORTING_ONLY_CONSTANTS)); @assert !isdefined(@__MODULE__, :P12_CHOSEN_PRIOR) && !isdefined(@__MODULE__, :P12_FISHERZ_NEFF); @assert isdir(P12_PRIMARY_CHECKOUT); println(P12_D_MINISPIKE, " ", P12_ITERATION_ALLOWANCE, " tier1-ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 and prints the `P12_DEV_SEED` value, `72`, `1`. Do not restate the
      seed as a hex literal in this criterion — testset 1 already asserts
      `P12_DEV_SEED === 0x0000_0000_0B12_DE71` exactly, and a hand-retyped hex here has already been
      miscounted once.
    - `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml` succeeds.
    - `grep -c 'P12_' spike/validation/p12_consts.jl` after stripping comment lines is > 60.
    - The file contains the exact literals `0x0000_0000_0B12_DE71`, `0x0000_0000_0B12_F1F7`,
      `0x2545_F491_4F6C_DD1D`, `0.95`, `0.5`, `0.10`, `0.03`, `271`, `1.644854`, `0.02`, `2000`,
      `999`, `50`, `63`, `50_000`, `10_000`, `150`.
    - `P12_R1_PRIOR` is a bound `Uniform(P12_R1_MIN, P12_R1_MAX)` distribution object — a plan that
      constructs its own r1 prior breaks the comparison it exists to protect.
    - `P12_GOLDEN_IMSIZE` / `P12_GOLDEN_KEYS` are **deliberately absent from Tier 1**. An earlier draft of
      this plan added them here citing `p11_consts.jl`; that citation was false — `grep -c GOLDEN
      spike/validation/p11_consts.jl` returns 0 and the analog defines them LOCALLY in
      `spike/test/capture_p11_golden.jl:51-52` as `(256, 256)` / `1:4`. 12-08 owns the Phase-12 equivalents
      locally, following that precedent. Adding them here would also have broken the capture: it
      guard-includes this file for `P12_FIXTURE_COUNTER`, so a differing local value is an
      invalid-redefinition error that stalls the wave-4 golden capture.
    - Every `P12_` name any other Phase-12 plan consumes is either defined here or defined by the plan
      that owns the file it lives in (`P12_CACHE_ROOT`/`P12_POOL_SCHEMA` -> 12-09,
      `P12_THETA_ROWS`/`P12_ZSCORE_ARM` -> 12-04, `P12_SPATIAL_SENTINEL` -> 12-12,
      `P12_*_LOAD_ONLY` -> set by the caller of each runner). Nothing is left undefined.
    - Including the file twice in one session emits no `const` redefinition warning (guard block works).
    - Both `@assert !isdefined(@__MODULE__, :P12_CHOSEN_PRIOR)` and
      `@assert !isdefined(@__MODULE__, :P12_FISHERZ_NEFF)` are present and pass.
    - `p12_repo_root()` tests `isdir(...) || isfile(...)` for the `.git` entry (a worktree `.git` is a
      file, not a directory).
    - **`P12_PRIMARY_CHECKOUT` and `P12_REPO_ROOT` differ when the loader runs inside a worktree**, and
      the former is derived from `git rev-parse --path-format=absolute --git-common-dir` rather than by
      parsing the gitdir pointer by hand. Verify by loading the file from one of the
      `.claude/worktrees/agent-*` trees and asserting the two bindings are UNEQUAL there and equal in the
      primary checkout. If they come back equal inside a worktree, 12-17's durability rule is silently
      void.
    - The git-absent fallback warns and names the degradation; it does not silently return the wrong root.
    - `p12_require_proceed` throws when no verdict file exists.
  </acceptance_criteria>
  <done>Tier 1 is committed, self-consistent, and Tier 2 is provably absent.</done>
</task>

<task type="auto">
  <name>Task 2: Literal re-assertion test with executable seed disjointness</name>
  <files>spike/test/test_p12_consts.jl</files>
  <read_first>
    - `spike/test/test_p11_consts.jl` (READ IN FULL — the purpose header at :21-30, the guarded
      include at :35-36, the literal + pairwise disjointness block at :41-73, the behavioural
      counter-stream test at :76-85, the re-derivation test at :120-121, the "Tier 1 has not moved"
      testset shape at :224-239, the CPU-only assert at :176-178)
    - `spike/validation/p12_consts.jl` (the file just written — assert its VALUES, not its structure)
    - `spike/test/test_stage6_regression.jl:88-100` (`_strip_comment_lines`, the correct way to make a
      source-text assertion without counting comments)
  </read_first>
  <action>
Create `spike/test/test_p12_consts.jl`, `#= =#` header dialect, opening with the
`test_p11_consts.jl:21-30` purpose header adapted: this file exists to make a post-hoc edit LOUD;
every locked value is asserted AS A LITERAL so changing a constant after a run breaks the suite
rather than silently rewriting the pre-registration.

Guarded include: `isdefined(@__MODULE__, :P12_DEV_SEED) || include(joinpath(@__DIR__, "..", "validation", "p12_consts.jl"))`.

Testsets:

1. `"Tier-1 seed literals (D-01 / R-5)"` — `@test P12_DEV_SEED === 0x0000_0000_0B12_DE71`,
   `@test P12_FIX_SEED === 0x0000_0000_0B12_F1F7`, `@test P12_SALT === 0xFF51_AFD7_ED55_8CCD`,
   `@test P12_DATAGEN_SALT === 0xC2B2_AE3D_27D4_EB4F`.

2. `"seed disjointness is EXECUTABLE, not documentary"` — `@test !(UInt64(P12_DEV_SEED) in _p12_forbidden())`,
   same for `P12_FIX_SEED`; pairwise by name so a failure says WHICH stream collided, covering at
   minimum `NPE_MASTER_SEED`, `VAL_MASTER_SEED`, `VAL_FIX_SEED`, `DEFAULT_MASTER_SEED`,
   `RATIO_PAIR_SEED`, `CORPUS_MASTER_SEED`, `P11_DEV_SEED`, `P13_DEV_SEED`, `P13_FIX_SEED`;
   `@test all(s -> P12_DEV_SEED != s, P13_BURNED_DEV_SEEDS)`;
   `@test all(s -> P12_DEV_SEED != s, values(PROD_SEED_V2))`;
   `@test length(_p12_forbidden_list()) == length(unique(_p12_forbidden_list()))`;
   `@test length(P13_BURNED_DEV_SEEDS) == 25`.

3. `"salt inventory covers the P11_DATAGEN_SALT / P13_SALT collision"` —
   `@test 0x2545_F491_4F6C_DD1D in P12_REPO_SALTS`, `@test P12_SALT ∉ P12_REPO_SALTS`,
   `@test P12_DATAGEN_SALT ∉ P12_REPO_SALTS`.

4. `"counter streams are behaviourally distinct"` — `P12_FIXTURE_COUNTER` not in the reported-counter
   tuple; `@test rand(p12_rng(P12_STAGE1_COUNTER), UInt64) != rand(p12_rng(P12_SBC_COUNTER), UInt64)`;
   `@test rand(p12_fix_rng(P12_FIXTURE_COUNTER), UInt64) == rand(p12_fix_rng(P12_FIXTURE_COUNTER), UInt64)`;
   `@test rand(p12_rng(P12_STAGE1_COUNTER), UInt64) != rand(p12_fix_rng(P12_STAGE1_COUNTER), UInt64)`.

5. `"recorded derivations reproduce"` —
   `@test ceil(Int, (Z_TWO_SIDED_90 * sqrt(0.09) / P12_STAGE2_COVERAGE_TOST_DELTA)^2) == P12_STAGE2_N_MIN`;
   `@test round(1 - 2.5 / sqrt(2 * P12_STAGE1_N_TEST_PER_RUNG_DERIVED); digits = 3) == P12_STAGE1_RATIO_CEILING`;
   `@test P12_STAGE1_N_TEST_PER_RUNG_DERIVED == round(Int, P12_STAGE1_TEST_FRACTION * P12_STAGE1_N / length(P12_R1_LADDER))`;
   `@test P12_D_MINISPIKE == 1 + P12_K_DEV + 7 + 1 == 72`.

6. `"gated and reporting-only constants are disjoint (reported != gated)"` —
   `@test isempty(intersect(P12_GATING_CONSTANTS, P12_REPORTING_ONLY_CONSTANTS))`;
   `@test :P12_RADIAL_ENERGY_CEILING in P12_REPORTING_ONLY_CONSTANTS`;
   `@test :P12_STAGE1_RATIO_CEILING in P12_GATING_CONSTANTS`;
   `@test all(s -> isdefined(@__MODULE__, s), (P12_GATING_CONSTANTS..., P12_REPORTING_ONLY_CONSTANTS...))`.

7. `"declared deviations are the pre-registered five (D-02/D-08/R-3/R-7)"` —
   `@test length(P12_RESEARCH_NET_DEVIATIONS) == 5`; `@test all(!isempty, P12_RESEARCH_NET_DEVIATIONS)`.

8. `"Tier 2 does not exist yet"` — `@test !isdefined(@__MODULE__, :P12_CHOSEN_PRIOR)` and
   `@test !isdefined(@__MODULE__, :P12_FISHERZ_NEFF)`. Add the `test_p11_consts.jl:135-139` comment
   verbatim in spirit: this assertion is removed in the SAME commit that appends the Tier-2 block,
   and only then.

9. `"Phase-12 wiring manifest — a late include cannot silently never run"` — read
   `spike/test/runtests.jl` and `spike/test/test_p12_suite.jl` as strings after
   `_strip_comment_lines`. Assert:
   `@test findfirst("test_p12_suite.jl", rt) !== nothing`;
   `@test first(findfirst("test_p12_suite.jl", rt)) < first(findfirst("test_p13_correction.jl", rt))`;
   and for each of the **ten** Phase-12 test filenames, `@test isfile(...)` and
   `@test occursin(name, suite_src)`. Keep the filename list in ONE local `const` tuple so the count
   cannot drift between this assertion and the aggregator.

10. `"model surface ran CPU-only"` —
   `@test !any(id -> occursin("CUDA", id.name), keys(Base.loaded_modules))`.
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Test; include("spike/test/test_p12_consts.jl")'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0 with zero failures and zero errors.
    - Manually changing `P12_STAGE1_RATIO_CEILING` to `0.94` in a scratch copy makes testset 5 fail
      (verify once, then revert; do not commit the mutation).
    - Testset 9 fails if `test_p12_suite.jl` is moved after `test_p13_correction.jl` in `runtests.jl`.
    - The file contains no bare `grep`-style count on unstripped source (comments are stripped via
      `_strip_comment_lines` before any `occursin`/`findfirst` source assertion).
  </acceptance_criteria>
  <done>Every Tier-1 literal is re-asserted, disjointness is executable, and the wiring is machine-checked.</done>
</task>

<task type="auto">
  <name>Task 3: Suite aggregator, nine pending scaffolds, and the runtests.jl insertion</name>
  <files>spike/test/test_p12_suite.jl, spike/test/test_p12_lattice.jl, spike/test/test_p12_prior.jl, spike/test/test_p12_architecture.jl, spike/test/test_p12_datagen.jl, spike/test/test_p12_train.jl, spike/test/test_p12_result.jl, spike/test/test_p12_sbc.jl, spike/test/test_p12_coverage.jl, spike/test/test_p12_decoupling.jl, spike/test/runtests.jl</files>
  <read_first>
    - `spike/test/runtests.jl` (READ IN FULL — in particular the Phase-13 wiring block and its
      comment explaining that `test_p13_correction.jl` THROWS and aborts every later include.
      **Locate the insertion point by CONTENT, not by line number:** Phase 13 is executing
      concurrently on this branch and may have added includes since the pattern map was written)
    - `spike/test/test_p13_result.jl:185-219` (testset shape and the CPU-only closing assert)
  </read_first>
  <action>
(a) Create `spike/test/test_p12_suite.jl`: a header comment stating that this file is the single
Phase-12 include point, that it is wired into `runtests.jl` immediately BEFORE
`test_p13_correction.jl` because that file's outer testset throws and aborts every subsequent
include, and that adding a Phase-12 test means adding a line HERE, not to `runtests.jl`. Then ten
unconditional includes in dependency order: `test_p12_consts.jl`, `test_p12_lattice.jl`,
`test_p12_prior.jl`, `test_p12_architecture.jl`, `test_p12_datagen.jl`, `test_p12_train.jl`,
`test_p12_result.jl`, `test_p12_sbc.jl`, `test_p12_coverage.jl`, `test_p12_decoupling.jl`.

(b) Create the nine not-yet-implemented test files as GREEN pending scaffolds. Each contains exactly
one `@testset` that (i) carries the literal marker string `P12_PENDING_SCAFFOLD` in a comment AND in
an `@info` message naming the plan that will implement it, (ii) asserts the CPU-only invariant
`@test !any(id -> occursin("CUDA", id.name), keys(Base.loaded_modules))`, and (iii) contains NO
failing assertion. A failing assertion here would throw at the end of its testset and abort the
remaining Phase-12 includes — the exact trap this wiring exists to avoid. Record that reason in each
scaffold's comment. Scaffold ownership, one line each: lattice → 12-03, architecture → 12-04,
decoupling → 12-05, prior → 12-07, datagen → 12-09, result → 12-12, train → 12-14, coverage → 12-16,
sbc → 12-18.

(c) Edit `spike/test/runtests.jl`: insert, immediately before the `include(...)` line whose argument
is `"test_p13_correction.jl"`, a short comment block in the neighbouring voice explaining that the
Phase-12 spatial-map testsets are aggregated in `test_p12_suite.jl` and MUST sit before the throwing
Phase-13 correction arm, followed by
`include(joinpath(@__DIR__, "test_p12_suite.jl"))`. Change nothing else in the file. Do not reorder
or touch any Phase-13 include.
  </action>
  <verify>
    <automated>LOG=$(mktemp); julia --project=spike spike/test/runtests.jl 2>&1 | tee "$LOG"; MISSING=0; for n in consts lattice architecture decoupling prior datagen result train coverage sbc; do grep -q "p12_$n" "$LOG" || { echo "MISSING Phase-12 testset: p12_$n"; MISSING=1; }; done; [ "$MISSING" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Running `julia --project=spike spike/test/runtests.jl` produces output containing every one of
      the ten Phase-12 testset names, proving they executed rather than being skipped. **The verify
      checks all ten, not one.** An earlier draft grepped only for `test_p12_consts`, which proves that
      the first include ran and says nothing about the other nine — while the trap this guards is
      precisely a Phase-12 include landing after the throwing `test_p13_correction.jl`, where the log
      would show exactly that pattern: one name present, the rest silently never executed.
    - The Phase-13 correction arm still throws at the end (the suite's pre-existing red state is
      unchanged) AND all ten Phase-12 testsets have already reported by that point.
    - `git diff HEAD -- spike/test/runtests.jl` shows exactly one added `include(` line plus comment
      lines, and zero removed or reordered lines.
    - The added `include` line appears at a lower line index than the `test_p13_correction.jl` include.
    - Each of the nine scaffolds contains the literal `P12_PENDING_SCAFFOLD` exactly once outside a
      comment-stripped body and zero `@test false` / `@test_broken` constructs.
  </acceptance_criteria>
  <done>Every Phase-12 test file has a guaranteed-executing home before the throwing Phase-13 arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration → reported run | A constant crossing this boundary must be immutable, or the honesty mechanism is void |
| Phase-12 agent → shared git index | Phase 13 executes concurrently on this branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `spike/validation/p12_consts.jl` | mitigate | Append-never-edit two-tier block + literal re-assertion in `test_p12_consts.jl`; git history is the audit trail |
| T-12-02 | Tampering | Philox seed streams | mitigate | Executable `_p12_forbidden()` including Phase-13's 9 named + 25 burned seeds and both derived gate families RECOMPUTED, not trusted from comments |
| T-12-04 | Repudiation | `spike/test/runtests.jl` include order | mitigate | Aggregator inserted before the throwing Phase-13 arm + source-order assertion in testset 9 |
| T-12-08 | Elevation of privilege | reported vs gated numbers | mitigate | Disjoint `P12_GATING_CONSTANTS` / `P12_REPORTING_ONLY_CONSTANTS` tuples with an in-file `@assert` and a test |
| T-12-07 | Tampering | git index race with Phase 13 | mitigate | Every commit in this plan is pathspec-scoped to the files listed in `files_modified`; never a bare `git commit` |
| T-12-SC | Tampering | package install surface | mitigate | Zero `Pkg.add`; `spike/Project.toml`/`Manifest.toml` asserted byte-unchanged by `git diff --quiet` |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` shows all ten Phase-12 testset names before the
  Phase-13 correction arm throws.
- `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml src corpus` succeeds.
- `julia --project=. -e 'using Pkg; Pkg.test()'` unchanged from its pre-plan state.
</verification>

<success_criteria>
Tier-1 pre-registration is committed with zero measured inputs; the Phase-12 DEV and FIXTURE streams
are provably disjoint from all reserved streams including Phase 13's; the gated/reported split is
machine-checkable; and every Phase-12 test file has an executing home before the throwing arm.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-01-SUMMARY.md` when done.
Commit pathspec-scoped: `git commit -- spike/validation/p12_consts.jl spike/test/test_p12_*.jl spike/test/runtests.jl`
</output>

---
phase: 12-spatial-colocalization-map
plan: 19
type: execute
wave: 11
depends_on: ["12-05", "12-16", "12-17"]
files_modified:
  - spike/validation/run_p12_coverage.jl
  - spike/validation/p12_coverage_real_report.jld2
  - spike/test/test_p12_decoupling.jl
autonomous: true
requirements: [SPAT-06]

must_haves:
  truths:
    - "The claim rests on 2 real images contributing ~64 regions each, and the report says so in those words"
    - "The corpus sealed_holdout is provably not read by this plan"
    - "Every region is OOD-screened before it is scored, so an out-of-distribution region is never counted as a coverage success"
    - "The SC3 verdict cites the frozen amendment, not the original ROADMAP wording"
    - "The real-image arm is REPORTED, not gated: a 0.03 tolerance at an effective n of 2 would be decided by noise"
  artifacts:
    - path: "spike/validation/p12_coverage_real_report.jld2"
      provides: "Per-image, per-region LRO coverage and log score for all three arms on real microscopy data"
      contains: "n_images"
  key_links:
    - from: "spike/validation/run_p12_coverage.jl"
      to: "test/test_images/positive/positive_c1.tif"
      via: "the --real arm reads the six committed TIFFs and nothing under corpus/"
      pattern: "test_images"
---

<objective>
Score the leave-region-out predictive comparison on genuine microscopy data, which is the only part of
SC3 that a simulation cannot supply, and state the resulting claim at exactly the strength the data
support.

Purpose: D-09 made the coverage criterion ground-truth-free precisely so it could run on real images, and
D-11 (AMENDED) settled which images. The temptation this plan exists to resist is overstating the result:
the six committed TIFFs are three channels each of **two** images, so the honest n is 2 images × ~64
regions — but the **independent unit is the IMAGE, not the region-draw**, which is exactly what
12-01:172-178 pre-registers ("the 64 regions of one image share every nuisance and the global term", and
counting region-draws as independent "would overstate the power of the gate by up to 8× in sd terms").
A per-region coverage number computed over 128 correlated regions and reported as though it were 128
independent trials would be the single most inviting overclaim in the phase — and it is the same
unit-mismatch error, one level down, that already had to be corrected once for the gate itself.
Output: the `--real` arm of the existing runner, one reported artifact, and a decoupling test extended to
prove the sealed holdout stayed sealed.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-RESEARCH.md
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md
@.planning/phases/12-spatial-colocalization-map/12-SC3-AMENDMENT.md

**Runs only on `VERDICT: PROCEED`, and that is ENFORCED, not merely stated.** The first statement of
this plan's runner is `p12_require_proceed("12-19")` (Tier 1, 12-01), which throws unless the Stage-1
verdict is `:proceed`, quoting 12-11's routing sentence in the message. The routing previously existed
only as prose in two documents, which left a wrong dispatch possible; one function with four call sites
is the fix. 12-11's routing excludes 12-19 from the descope path.

**THE SEALED HOLDOUT IS THE ONE IRREVERSIBLE MISTAKE AVAILABLE IN THIS PHASE.** `corpus/manifest.csv`'s
two `physical-primary` rows are `split = sealed_holdout`, `sha256 = PENDING-FETCH`, `bytes = 0`, reserved
for the Phase-16 blind evaluation. D-11's amendment records that the first version of that decision chose
them, and that reading their `split` column is what caught it. Scoring them here would irreversibly burn
Phase 16 — there is no undo, because the value of a blind holdout is destroyed by being looked at once.
The 30 CBS Red-Green rows are separately excluded: they are `simulated-secondary` (computer-generated), so
they cannot support a "real image" claim regardless of fetch status. **This plan reads
`test/test_images/` and nothing else**, and the assertion is executable, not documentary.

**THE DATA, COUNTED RATHER THAN ASSUMED (corpus audit, 2026-07-28).** The Phase-8 external corpus was
checked before this plan was finalized, because n = 2 had been inherited rather than derived:

- `corpus/data/` is **EMPTY**, and all 32 rows have `bytes = 0` and no fetched content; the two `physical-primary` rows carry `sha256 = PENDING-FETCH` and `split = sealed_holdout`, while the 30 `simulated-secondary` rows carry an EMPTY `sha256` and `split ∈ {eval, dev}` — so the corpus supplies **zero** images today. State it in
  that precise form: an earlier draft said "all 32 rows are `sha256 = PENDING-FETCH`", which is wrong (only
  2 are), and an overstated audit heading for a frozen document is exactly the currency this phase cannot
  spend.
- Its only two `physical-primary` rows are `split = sealed_holdout`, reserved for the Phase-16 blind
  evaluation and untouchable here by D-11's own ruling.
- Its other 30 rows are `simulated-secondary` (the CBS Red-Green benchmark, computer-generated), which
  cannot support a "real image" claim at any n.
- The manifest's `role` column is `positive` / `negative` / `benchmark`. Those are **experimental**
  positive and negative controls — different specimens with opposite ground truth — **not sample/control
  pairs of the same specimen.**

So the real-image data is, and remains, the six committed TIFFs: `positive_c{1,2,3}` and
`negative_c{1,2,3}` at 1028×1376, already exercised by `test/runtests.jl`. Three channels each of a
positive and a negative specimen — **two images, not six**.

**WHAT SC3 ACTUALLY SCORES IS SINGLE-STACK, SO n = 2 AND NO CONTROL IS NEEDED.** This needs stating
plainly because it is easy to get wrong in both directions. D-09's construction
(`12-RESEARCH.md` § "The leave-region-out predictive construction") is:

```
Z_full = encode_d01(patch_summary(mci, 8));  Z_r = mask_region(Z_full, r)
draws  = sampleposterior(est, reshape_summary(Z_r, 8); N);  rho_r = read_region(draws, r)
score  : is the OBSERVED Z_full[r] inside the predictive interval, and its log density
```

Every step operates on **one** image. There is no control image anywhere in it. So the SC3 criterion —
leave-region-out predictive coverage — is computable on each of the two specimens independently, giving
**n = 2 images, ~64 regions each**, and needing no pairing at all.

**Δρ, BY CONTRAST, IS NOT COMPUTED ON REAL DATA, AND THAT IS A NAMED LIMIT RATHER THAN AN OMISSION.**
**The reason is EXCHANGEABILITY, and an earlier draft of this plan got the reason wrong — the conclusion
survives, the argument is corrected here.** The earlier version claimed the simulated pair shares all seven
nuisances. It does not: the established precedent `harness.jl:124-136` draws **two independent priors**
`θ*_s` and `θ*_c`, so nuisances differ between sample and control in the simulated Δρ exactly as they do in
the shipped one (`infer.jl:108`, D-03). Nuisance sharing was never the milestone's convention and must not
be asserted as one.

The correct argument is about the population the two draws come from. The simulated pair is **two
exchangeable draws from the same prior π(θ)** — sample and control are interchangeable, and Δρ* is a
within-population difference, which is what the net is calibrated against. The two committed specimens are
**not exchangeable**: one is `role = positive` / `truth = coloc`, the other `role = negative` /
`truth = segregated`, and they are different specimen types (TetraSpeck beads against a LightMyCells
specimen). Their difference is a **between-population** contrast, deliberately so — it is a labelled test
pair, built to differ. Scoring a between-population contrast against a gate calibrated on a
within-population one is **two arms measuring different quantities under one gate**, the same defect class
as the Δρ mislabelling itself.

So this plan does **not** compute a scored real-image Δρ. It records `real_delta_rho_computed = false` with
the reason, and the phase carries the named limit: *the amortized per-region Δρ map is validated on
simulation, where the sample and control are two EXCHANGEABLE draws from one prior and Δρ\* is therefore a
within-population difference; on real data the phase demonstrates per-region predictive calibration on two
specimens, and no exchangeable sample/control pair is available to validate Δρ itself.*
**Say "exchangeable", not "matched".** An earlier draft of this limit said matched pairs "exist by
construction" in simulation. They do not: `harness.jl:124-136` draws two INDEPENDENT priors, so the
nuisances differ between the simulated sample and control exactly as they would between two specimens.
Nuisance sharing was never this milestone's convention, and a named limit that says otherwise would put a
false mechanism into the manuscript. What actually separates the two arms is exchangeability, and that is
the word the frozen amendment and this plan must both use. A specimen contrast between the two anchors MAY be reported as an illustrative figure, clearly
labelled as a specimen difference and explicitly **not** the Δρ the simulated arm scores, and it is not
scored against any threshold.

| quantity | n on real data | scored here? |
|---|---|---|
| leave-region-out predictive coverage (the SC3 criterion) | **2 images**, ~64 regions each | yes — reported, not gated (see below) |
| `region_rho_sample` single-stack map | **2 images**, one map each (step 6b, `control_mci = nothing`) | reported |
| `region_rho_control` single-stack map | **0** — there is no control stack on real data | n/a |
| `region_delta_rho` (primary deliverable) | **0 — not computable as an exchangeable contrast** | **no** — named limit |

**THE EFFECTIVE INDEPENDENT n IS 2, NOT 128, AND THAT MUST BE STATED WHEREVER COVERAGE IS.** The 128
region-draws are **pseudo-replicates**: regions within an image share every nuisance and the global term,
so by 12-01's own pre-registered accounting the independent unit is the *dataset*. Record
`effective_independent_n = 2` as a mandatory key alongside `n_regions_scored = 128`, and require the
report to carry, in the same breath as every coverage figure, the sentence that **the 128 region-draws are
not 128 independent trials**. Compute any interval on the coverage estimate from the between-image spread
(n = 2) rather than from a binomial over 128 — and if that interval is too wide to be useful, say so,
because that is the honest consequence of having two images rather than a reason to use the wrong unit.

An earlier draft of this plan said "the effective sample is regions, and correlated ones". That was wrong
and is corrected here: it contradicted 12-01:172-178, and it is precisely the unit-mismatch this phase has
already had to fix twice.

**REAL DATA IS WHERE OOD STOPS BEING THEORETICAL.** Everything before this plan ran on the simulator's own
draws, so the summary was in-distribution by construction. These images were not generated by the forward
model. A region that is out of distribution produces a posterior the coverage number has no right to
count, so OOD screening is a precondition of scoring here rather than a diagnostic beside it. Follow the
`local_map.jl` discipline: an unscorable region carries the sentinel **and** a forced OOD flag, and a
missing operating point reads as NOT CHECKED rather than in-distribution.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the --real arm, OOD-screened and honestly counted</name>
  <files>spike/validation/run_p12_coverage.jl</files>
  <read_first>
    - `spike/validation/run_p12_coverage.jl` (12-16 wrote it with the `--real` switch wired and
      deliberately unexercised — this task implements that branch and must not alter the simulation branch)
    - `spike/validation/p12_coverage.jl` (12-16 — `lro_arm`, `predictive_draws`, `lro_scores`,
      `lro_pass`, `prior_only_floor`, `p12_coloc_map`, `P12_FISHERZ_NEFF`)
    - `test/runtests.jl` (how the six TIFFs are already loaded — reuse that path rather than inventing a
      loader, and note the channel grouping it implies)
    - `src/amortized/ood.jl` (the Mahalanobis density reference and its operating point — the OOD surface
      this plan screens with; READ-ONLY, do not edit)
    - `src/amortized/local_map.jl:42-53, 88-91` (sentinel + forced OOD flag; NOT CHECKED semantics)
    - `corpus/manifest.csv` (READ THE HEADER AND THE `split` COLUMN ONLY, for provenance — confirm the two
      `physical-primary` rows are `sealed_holdout` / `PENDING-FETCH` and record that you confirmed it)
    - `.planning/phases/12-spatial-colocalization-map/12-SC3-AMENDMENT.md` §4(f)
  </read_first>
  <action>
Implement the `--real` branch of `spike/validation/run_p12_coverage.jl`. Do not touch the simulation
branch: 12-16's artifact must stay reproducible, so extend the runner, do not rewrite it.

Header addition must state: the two-images-not-six count; that `test/test_images/` is the only image
source and `corpus/` is never opened; that OOD screening gates which regions are scored; and the run line
`julia --project=spike -t auto spike/validation/run_p12_coverage.jl --real`.

Steps:
1. **Refuse to proceed if any `corpus/` path is reachable from this code path.** Assert, before loading
   anything, that the resolved image paths all lie under `joinpath(P12_REPO_ROOT, "test", "test_images")`.
   A path that normalizes outside that directory is a hard error naming the sealed holdout.
2. Load the two images by their channel triples into `MultiChannelImage`-compatible containers via the
   existing `build_mci` path. Record each image's identity, its `sha256`, and its realized size, so the
   report identifies exactly which bytes were scored.
3. Resolve and hash-verify both bundles from `p12_train_full_report.jld2`, as 12-16 does.
4. **OOD screen every region before scoring.** For each region compute the OOD statistic against the
   frozen reference and record `ood_flag` and the operating point used. If no operating point is
   available, record it as `nothing` meaning NOT CHECKED — and in that case report the coverage number
   **twice**, once over all regions and once over none-excluded, stating that the screen was unavailable.
   Never silently treat an unchecked region as in-distribution.
5. Run all three arms — spatial, matched ablation, `prior_only_floor` — over every scorable region of both
   images, using the frozen `P12_FISHERZ_NEFF` for the image size actually present (1028×1376 may not be
   in `P12_IMSIZE_SET`; if it is not, interpolate or use the pooled value, **state which**, and record
   that this is a declared extrapolation of the noise model rather than a calibrated value).
6. Report per-image and pooled: coverage, mean log score, CRPS, the count of scorable regions, the count
   excluded by OOD, and the count excluded because `correlation()` could not score the patch.

6b. **Produce the single-stack ρ map for each specimen, through the sanctioned single-stack mode.** Call
   `p12_coloc_map(bundle, mci, nothing; ...)` once per image — `nothing` in the control position is
   12-16's explicit no-control-stack mode, not an omission, and it is what lets this plan honour the
   three-map decision without pairing two non-exchangeable specimens. Each call returns a live
   `region_rho_sample` map, a `region_rho_control` that is entirely `P12_SPATIAL_SENTINEL`, and therefore
   by the union rule a `region_delta_rho` that is entirely sentinel with `meta.delta_rho_available =
   false`. **That is the point:** the Δρ limit stops being a sentence in this plan and becomes a property
   of the object the runner returns, checkable by 12-16's testset 4b.

   Persist both maps under `region_rho_sample_maps` (a two-element vector, one G×G matrix per image, in
   the same order as `image_paths`) and assert `delta_rho_available == false` for both results before
   writing. There is no `region_rho_control` map on real data — there is no control stack — and
   `per_map_n` below records exactly that. Do **not** write a `region_delta_rho` map to the artifact: an
   all-sentinel matrix in a report invites being read as a measured field of zeros.
7. **Report the region-correlation caveat quantitatively, not just in prose.** Compute an effective
   sample size for the coverage estimate that accounts for within-image correlation — at minimum, report
   the two per-image coverage values separately alongside the pooled one, so a reader can see the
   between-image spread that a pooled binomial interval hides. State that the nominal 128 is not 128
   independent trials.
8. **Report the comparison; do NOT gate on it.** Compute `lro_pass` on the real data for its diagnostic
   value, but record the outcome as `sc3_real_reading` — *not* as a pass/fail gate — alongside the
   mandatory key `sc3_real_gated = false`.

   **Why, in the phase's own arithmetic.** `P12_STAGE2_N_MIN = 271` is Wilson-derived for 271
   **independent** trials, and 12-01:172-178 is explicit that the independent unit is the *dataset*, not
   the region-draw, "because the 64 regions of one image share every nuisance and the global term". Here
   there are **two** images, and the independent unit is the image. Applying
   `P12_STAGE2_COVERAGE_TOST_DELTA = 0.03` at an effective n of 2 would not be a criterion — it would be
   decided by noise, and it would present a coin flip as a pre-registered gate. (No Δρ pair count enters
   here: Δρ is not scored on the real arm at all, per the section above.)
   The corpus cannot rescue this: it supplies zero images today, its two real rows are sealed for Phase 16,
   and its 30 remaining rows are computer-generated. That is precisely the error class the milestone's named limits already record twice, and the one
   this phase exists to avoid.

   **So the D-12 Stage-2 gate rests on the SIMULATED arm** (12-16, where N >= `P12_STAGE2_N_MIN` is
   actually achieved), and the real-image arm is **corroborating evidence reported with its n stated**.
   This is consistent with D-11's own assessment that the amended SC3 "costs Phase 12 very little": the
   real images were always there to establish that the method runs on genuine microscopy and predicts
   held-out regions sensibly, not to carry the statistical weight of the calibration claim.

   **This is a pre-registration change and must be visible as one.** `12-SC3-AMENDMENT.md` (12-02,
   Wave 1) must state that the real-image arm is reported rather than gated, and carry this derivation —
   the amendment is frozen before any result exists, so recording it there is legitimate, whereas
   discovering it afterwards would not be. If the user instead wants the real arm to gate, the honest
   route is a separately derived, much wider tolerance for n = 2 recorded verbatim in Tier 1 — but a
   tolerance honest at n = 2 is too wide to discriminate, which is itself the argument for reporting.
   **If the amendment does not already carry this clause when this plan executes, raise it rather than
   deciding it silently.**

   The reading must cite `12-SC3-AMENDMENT.md` by path and must **not** be phrased against the original
   ROADMAP SC3 wording.

Atomic write to `spike/validation/p12_coverage_real_report.jld2`: `schema_version = 1`, `n_images = 2`,
`image_paths`, `image_sha256`, `imsize_realized`, `bundle_sha256` per arm, per-arm per-image and pooled
`coverage` / `logscore` / `crps`, `n_regions_scored`, `n_regions_ood_excluded`,
`n_regions_unscorable`, `ood_operating_point`, `neff_source` (`:calibrated` or `:extrapolated_pooled`),
`per_image_coverage`, `sc3_real_reading`, `sc3_real_gated = false`,
`real_delta_rho_computed = false`, `real_delta_rho_reason` (the exchangeability explanation as a string),
`effective_independent_n = 2` and `pseudoreplicate_note` (the literal sentence that the 128 region-draws
are not 128 independent trials),
`corpus_images_available = 0`, `corpus_tier_counts` (2 `physical-primary`, 30 `simulated-secondary`,
re-derived with a proper CSV reader, not a hand split on commas),
`region_rho_sample_maps` (one G×G matrix per image, from step 6b),
`per_map_n = (region_rho_sample = 2, region_rho_control = 0, region_delta_rho = 0)` — **the literal
NamedTuple, spelled out because an unexplained key is how the last three of these went stale**: two
specimens each yield a single-stack sample map, there is no control stack so no control map exists, and
no Δρ map is computed. Assert `per_map_n.region_delta_rho == 0` together with
`real_delta_rho_computed == false`, so the count and the flag cannot disagree.
**There is exactly ONE key for the independent unit and it is `effective_independent_n`.** An earlier
draft also carried `effective_n_images = 2` for the same quantity; two names for one number in one
artifact is the conflation T-12-78 exists to prevent, so the duplicate is removed rather than reconciled.
Then `amendment_path`, `corpus_untouched`, `elapsed_min`,
`generated`, `julia_version`, `caption`. Reopen and `@assert haskey` for `sc3_real_reading`, `n_images` and
`per_image_coverage` before `mv`.

`corpus_untouched` must be **derived, not declared**: compute it from the same check Task 2's decoupling
testset runs — `isempty(readchomp(Cmd(["git","status","--porcelain","--","corpus"]; dir = P12_REPO_ROOT)))`
(the vector form, so no backtick-command literal has to survive being quoted inside a markdown span) — so
the key records a measurement. Writing the literal `true` and then asserting the literal back proves
nothing, which is what an earlier draft did.
  </action>
  <verify>
    <automated>julia --project=spike -t auto spike/validation/run_p12_coverage.jl --real && julia --project=spike -e 'using JLD2; d=JLD2.load("spike/validation/p12_coverage_real_report.jld2"); @assert d["n_images"]==2 && d["corpus_untouched"]==true && haskey(d,"per_image_coverage") && haskey(d,"sc3_real_reading") && d["sc3_real_gated"]==false; @assert all(p->occursin("test_images",p), d["image_paths"]); @assert d["effective_independent_n"] == 2 && d["effective_independent_n"] != d["n_regions_scored"] "the independent unit is the image, not the region-draw"; @assert haskey(d,"pseudoreplicate_note") && occursin("not 128 independent", d["pseudoreplicate_note"]); @assert d["real_delta_rho_computed"] == false; @assert !haskey(d,"effective_n_images") "one key for the independent unit, and it is effective_independent_n"; @assert haskey(d,"per_map_n") && d["per_map_n"].region_delta_rho == 0 && d["per_map_n"].region_rho_sample == 2 && d["per_map_n"].region_rho_control == 0; @assert length(d["region_rho_sample_maps"]) == 2 && all(m -> size(m) == (8,8), d["region_rho_sample_maps"]) && !any(m -> all(==(0.0), m), d["region_rho_sample_maps"]) "each specimen must yield a LIVE single-stack map, not an all-sentinel one"; @assert !haskey(d,"region_delta_rho_map") "an all-sentinel difference map must not be written to the artifact"; println(d["sc3_real_reading"], " ", d["n_regions_scored"], " ", d["effective_independent_n"], " ", d["neff_source"])'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0; `n_images == 2`; every recorded image path contains `test_images` and none
      contains `corpus`.
    - `n_regions_scored`, `n_regions_ood_excluded` and `n_regions_unscorable` sum to `2 * 64` — the SC3
      criterion is single-stack, so both specimens contribute.
    - `per_image_coverage` has exactly two entries and the summary quotes both beside the pooled value.
    - `effective_independent_n == 2` is present and is **distinct from** `n_regions_scored == 128`. Every
      place the summary or the report states a coverage figure, it states the effective independent n in
      the same breath and carries `pseudoreplicate_note`. A coverage number quoted without it is a FAILED
      plan, because a reader would take 128 correlated draws for 128 trials — and a reader is what the
      manuscript has.
    - `real_delta_rho_computed == false`, with `real_delta_rho_reason` naming the **exchangeability**
      argument (the two anchors are a between-population contrast; the net is calibrated on a
      within-population one). A run that silently produced a real Δρ by pairing the two anchors is a
      FAILED plan even if every other number looks right — and 12-16's testset 4b makes that mechanical:
      the single-stack results this plan builds must carry `meta.delta_rho_available == false`.
    - `region_rho_sample_maps` has exactly two live G×G matrices and `per_map_n` is the literal
      `(region_rho_sample = 2, region_rho_control = 0, region_delta_rho = 0)`. No `region_delta_rho` map
      is written.
    - `effective_n_images` is **absent**; `effective_independent_n` is the only key for that quantity.
    - `corpus_images_available == 0` and `corpus_tier_counts` are recorded, so a later reader can see the
      count was TAKEN rather than assumed. Derive them with a real CSV reader — the manifest has quoted
      fields containing commas, and a naive split shifts rows.
    - `neff_source` is recorded, and if it is `:extrapolated_pooled` the summary names it as a declared
      extrapolation of the noise model.
    - `sc3_real_gated` is `false` and `effective_independent_n` is `2` — the real arm is reported, and the
      Stage-2 gate rests on 12-16's simulated arm where N >= `P12_STAGE2_N_MIN` is achieved.
    - `sc3_real_reading` is one of `:pass`, `:fail`, `:disqualified`, and `amendment_path` points at
      `12-SC3-AMENDMENT.md`.
    - After stripping comment lines, the runner contains zero occurrences of `corpus` outside the
      refusal assertion and its message.
    - `git diff --quiet HEAD -- corpus src spike/Project.toml spike/Manifest.toml spike/validation/p12_consts.jl` succeeds, and `git status --porcelain -- corpus test/test_images` is empty.
  </acceptance_criteria>
  <done>The SC3 comparison exists on real microscopy data, OOD-screened, counted at its honest n, and phrased against the frozen amendment.</done>
</task>

<task type="auto">
  <name>Task 2: extend test_p12_decoupling.jl with the executable sealed-holdout proof</name>
  <files>spike/test/test_p12_decoupling.jl</files>
  <read_first>
    - `spike/test/test_p12_decoupling.jl` (12-05 wrote it; 12-05's action note anticipated that "one
      Phase-12 source (once 12-19 lands) references `test/test_images`" and that the check should skip
      that half until then — this task lands that half)
    - `spike/validation/run_p12_coverage.jl` (the file whose image access is being constrained)
    - `corpus/manifest.csv` (the `split` column)
  </read_first>
  <action>
Extend, do not rewrite. Add testsets to the existing file:

1. `"no Phase-12 source reads corpus/"` — over every `spike/**/p12_*.jl`, `spike/**/*p12*.jl` and
   `spike/p12/*.jl`, read the source with `_strip_comment_lines` and assert the only occurrences of
   `corpus` are inside `run_p12_coverage.jl`'s refusal assertion. Implement this as an allowlist of
   (file, count) pairs so a NEW occurrence anywhere fails by name rather than being absorbed.

2. `"the sealed_holdout rows are still sealed"` — parse `corpus/manifest.csv`, locate the two
   `physical-primary` rows, and assert each still has `split == "sealed_holdout"` and
   `sha256 == "PENDING-FETCH"` and `bytes == 0`. Comment that this asserts the holdout was not fetched
   *by anyone*, which is a stronger and more useful statement than asserting this phase did not read it,
   and that it is the executable form of D-11's amendment.

3. `"the real-image arm reads only the six committed TIFFs"` — assert all six paths exist, and assert the
   real report's `image_paths` (if the artifact is present) are all under `test/test_images`. Guard the
   artifact half with `isfile` so the testset passes before 12-19 has run and tightens afterwards.

4. `"test/test_images is byte-unchanged"` — the git-guarded `git diff --quiet HEAD -- test/test_images`
   in the `test_p13_result.jl:190-213` idiom, with the same `_has_git` availability guard so a sandbox
   without git skips the executable half rather than failing for an unrelated reason. Scoring images must
   not modify them.
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Test; include("spike/test/test_p12_decoupling.jl")'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0 with zero failures and zero errors.
    - Testset 2 fails if either `physical-primary` row's `split` is edited to anything but
      `sealed_holdout` (verify once in a scratch copy of the manifest, then revert).
    - Testset 1 fails if a `corpus` reference is added to any Phase-12 source outside the allowlisted
      refusal assertion.
    - The testsets added here do not weaken any assertion 12-05 wrote — `git diff` on this file is
      additions-only apart from removing 12-05's temporary skip of the `test_images` half.
  </acceptance_criteria>
  <done>The sealed holdout's untouched state and the real-image access boundary are both executable assertions in the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-12 code → corpus/ sealed holdout | Reading two reserved rows once irreversibly destroys the Phase-16 blind evaluation |
| simulator distribution → real microscopy data | A region outside the training distribution yields a posterior whose coverage means nothing |
| 128 correlated regions → 128 independent trials | Pooling correlated regions into one binomial interval overstates the precision of the SC3 claim |
| calibrated noise model → an uncalibrated image size | Silently reusing a pooled n_eff at an unseen size hides a modelling extrapolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information disclosure | `corpus/` sealed holdout | mitigate | Pre-load path assertion confining every image to `test/test_images`; an allowlisted-occurrence source scan over all Phase-12 files; an executable manifest check that both `physical-primary` rows are still `sealed_holdout` / `PENDING-FETCH` / `bytes = 0` |
| T-12-77 | Spoofing | an OOD region counted as a coverage success | mitigate | OOD screening is a precondition of scoring, not a diagnostic beside it; excluded counts are mandatory keys and must sum to `2 * 64`; a missing operating point reads as NOT CHECKED and forces the dual reporting |
| T-12-78 | Repudiation | 2 images reported as 6, or 128 regions as 128 independent trials | mitigate | `n_images = 2` and `effective_independent_n = 2` are literal artifact keys, asserted DISTINCT from `n_regions_scored = 128`; `pseudoreplicate_note` must accompany every coverage figure; `per_image_coverage` carries both images separately so the between-image spread is visible; intervals are computed from the between-image spread, never from a binomial over 128 |
| T-12-79 | Repudiation | an extrapolated noise model presented as calibrated | mitigate | `neff_source` is a mandatory key with values `:calibrated` / `:extrapolated_pooled`, and the extrapolation must be named as such in the summary |
| T-12-80 | Tampering | the SC3 claim phrased against the superseded ROADMAP wording | mitigate | `amendment_path` is a mandatory key; the verdict must cite `12-SC3-AMENDMENT.md`, which was frozen in Wave 1 before any result existed |
| T-12-81 | Tampering | scored images modified in place | mitigate | Git-guarded `git diff --quiet HEAD -- test/test_images` testset |
| T-12-69 | Tampering | scoring an unrecorded net | mitigate | Both bundle hashes verified against `p12_train_full_report.jld2` |
| T-12-07 | Tampering | git index race with Phase 13 | mitigate | Pathspec-scoped commits limited to the three files in `files_modified` |
</threat_model>

<verification>
- `spike/validation/p12_coverage_real_report.jld2` loads with `n_images = 2`, per-image and pooled scores
  for all three arms, the three region counts summing to 128, `neff_source`, `sc3_real_reading` and
  `amendment_path`.
- Every recorded image path is under `test/test_images`; none mentions `corpus`.
- `corpus/manifest.csv`'s two `physical-primary` rows are still `sealed_holdout` / `PENDING-FETCH` / `0`.
- `julia --project=spike spike/test/runtests.jl` green, including the extended decoupling testsets.
- `git status --porcelain -- corpus test/test_images` is empty.
</verification>

<success_criteria>
SC3's real-image comparison exists on the two committed microscopy images, with every scored region
OOD-screened, the excluded regions counted, both per-image coverages reported beside the pooled value, and
the noise model's provenance at that image size declared. The claim is stated at the strength 2 images
support and is phrased against the frozen amendment rather than the superseded ROADMAP text. The Phase-16
sealed holdout is provably still sealed, asserted by a test rather than by a promise.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-19-SUMMARY.md` when done, quoting both per-image
coverages and the pooled value, the three region counts, `neff_source`, the SC3 verdict against the
amendment, and confirmation that the sealed-holdout rows are unchanged.
Commit pathspec-scoped, two commits in task order:
1. `git commit -- spike/validation/run_p12_coverage.jl spike/validation/p12_coverage_real_report.jld2`
2. `git commit -- spike/test/test_p12_decoupling.jl`
</output>

---
phase: 12-spatial-colocalization-map
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - spike/test/test_p12_decoupling.jl
autonomous: true
requirements: [SPAT-09]

must_haves:
  truths:
    - "src/, spike/Project.toml, spike/Manifest.toml and corpus/ are provably byte-unchanged by Phase 12"
    - "The sealed holdout cannot be consumed by any Phase-12 code path without a test failing"
    - "No spatial-map package (AbstractGPs, KernelFunctions, GaussianRandomFields) has become a dependency"
  artifacts:
    - path: "spike/test/test_p12_decoupling.jl"
      provides: "Executable decoupling, dependency-set and sealed-holdout assertions"
      contains: "sealed_holdout"
  key_links:
    - from: "spike/test/test_p12_decoupling.jl"
      to: "git working tree"
      via: "git diff --quiet HEAD -- <pathspec> guarded by a git-availability check"
      pattern: "git diff --quiet"
---

<objective>
Make the phase's three hard constraints executable rather than aspirational: `src/` untouched, the
spike environment byte-frozen, and the Phase-16 sealed holdout unburned.

Purpose: CLAUDE.md's decoupling rule and D-11's sealed-holdout exclusion are both stated in prose in
several documents. Prose does not fail a build. This plan turns each into an assertion that turns the
suite red the moment it stops being true — including the one nobody would notice, a Phase-12 code
path quietly reading a `corpus/` row.
Output: `spike/test/test_p12_decoupling.jl`, replacing the 12-01 pending scaffold.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md

This plan asserts the CURRENT state. It runs early on purpose: an assertion added at phase close
proves only that nothing broke since the assertion was added.
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_p12_decoupling.jl — git, dependency-set and sealed-holdout assertions</name>
  <files>spike/test/test_p12_decoupling.jl</files>
  <read_first>
    - `spike/test/test_p12_decoupling.jl` (the 12-01 pending scaffold this task REPLACES)
    - `spike/test/test_p13_result.jl:185-219` (READ — the exact `git diff --quiet HEAD -- src` idiom
      WITH the git-availability guard, so a sandbox without git skips the executable half rather than
      failing for an unrelated reason; and the closing CPU-only testset)
    - `spike/test/runtests.jl:114-151` (the existing D-04 CPU-only / resolve-risk testset — READ the
      exact 16-name dependency set literal and the NeuralEstimators UUID/version assertion; Phase 12
      re-asserts them from its own file rather than editing that block)
    - `spike/test/test_stage6_regression.jl:88-100` (`_strip_comment_lines` — the correct way to make
      a source-text assertion without a comment counting as a hit)
    - `corpus/manifest.csv` header and the two `physical-primary` rows (READ ONLY for provenance:
      both are `split = sealed_holdout`, `sha256 = PENDING-FETCH`, `bytes = 0`)
    - `CLAUDE.md` § Constraints (the decoupling wording this file makes executable)
  </read_first>
  <action>
Replace the scaffold with real testsets. Define `P12_REPO_ROOT` (**read from the guarded `p12_consts.jl` include — 12-01 states "12-05 must READ these bindings rather than re-deriving its own copy"; keep only the local `_has_git` predicate, which is a git-availability question Tier 1 does not answer**) by walking up from `@__DIR__` to the
directory containing `.git` (mirror the `P13_REPO_ROOT` derivation), and a `_has_git` predicate
exactly as `test_p13_result.jl:193-196` does.

1. `"src/ is byte-unchanged (CLAUDE.md hard constraint)"` — when `_has_git`:
   `@test success(Cmd(`git diff --quiet HEAD -- src`; dir = P12_REPO_ROOT))`, and separately for
   `src/results.jl`, `src/amortized/summary.jl`, `src/amortized/architecture.jl`,
   `src/amortized/local_map.jl` so a failure names the file. Also assert the untracked-file case:
   `@test isempty(readchomp(Cmd(`git status --porcelain -- src`; dir = P12_REPO_ROOT)))` — a NEW
   file under `src/` is invisible to `git diff HEAD` and would otherwise pass. When git is absent,
   `@info` and skip the executable half, following the analog.

2. `"the spike environment is byte-frozen"` —
   `@test success(Cmd(`git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml`; dir = P12_REPO_ROOT))`
   and `@test isempty(readchomp(Cmd(`git status --porcelain -- spike/Project.toml spike/Manifest.toml`; dir = P12_REPO_ROOT)))`.

3. `"the dependency name set is exactly the frozen sixteen (resolve-risk clause k)"` —
   `using Pkg`; assert `Set(keys(Pkg.project().dependencies))` equals the literal 16-name set read
   from `runtests.jl:114-127` (`BenchmarkTools, CSV, CairoMakie, CoordinateTransformations,
   DataFrames, Distributions, Flux, HypothesisTests, ImageFiltering, ImageTransformations, Images,
   Interpolations, JLD2, NeuralEstimators, Random123, StatsBase`), and
   `@test Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"`.
   Then the Phase-12-specific half, in the same voice as the existing block: the spatial map adds NO
   new package — at G=8 the lattice is 64 cells, so CAR and GP are dense 64×64 stdlib
   `LinearAlgebra`, and `AbstractGPs`, `KernelFunctions` and `GaussianRandomFields` must NOT become
   direct deps. Assert each of those three names is absent from the dependency set BY NAME, so a
   failure says which one appeared. Add a comment recording WHY: adding any of them forces a
   `Pkg.resolve()` on the frozen manifest, and the Phase-7 Wave-0 co-resolution gate already failed
   once on a transitive Makie conflict that pushed NeuralEstimators toward 0.1.4.

4. `"the Phase-16 sealed holdout is not consumed (D-11 AMENDED)"` — **state the property precisely before
   implementing it, because a naive token ban is RED BY CONSTRUCTION here and an executor would either
   delete required code or weaken the test until it passed.** The property is: **no byte of a sealed
   corpus IMAGE is ever read by Phase 12.** It is *not* "the string `corpus` appears nowhere", and the
   difference is load-bearing in three places this phase actually requires:
   - `p12_consts.jl` **must** define `CORPUS_MASTER_SEED` (12-01 requires it in the forbidden-seed
     inventory, mirroring `p11_consts.jl:90`), and `test_p12_consts.jl` **must** name it in the
     disjointness assertion. Banning the token would forbid the pre-registration file from listing the
     seed it exists to forbid.
   - This very file matches `spike/test/test_p12_*.jl` and must contain `corpus` and `sealed_holdout` to
     express the rule at all — a bare glob makes the test fail on itself.
   - 12-19 **must** read `corpus/manifest.csv` to re-derive `corpus_tier_counts` and
     `corpus_images_available = 0`. That is a METADATA read of a committed text file whose whole purpose
     is provenance; it opens no image and is what makes the reported-not-gated demotion checkable.

   So implement it as three checks, not one grep:
   (a) **No image load resolves outside `test/test_images`.** Enumerate the Phase-12 sources
       (`spike/validation/p12_*.jl`, `spike/validation/run_p12_*.jl`, `spike/simulator/p12_*.jl`,
       `spike/npe/p12_architecture.jl`, `spike/npe/train_p12_npe.jl`, `spike/data/p12_generate.jl`,
       `spike/p12/*.jl`, `spike/test/test_p12_*.jl`, `spike/test/capture_p12_golden.jl`) that EXIST at
       run time, strip comment lines with a local `_strip_comment_lines`, and assert no stripped body
       contains `open_sealed_holdout`, or any image-loading call (`load(`, `FileIO.load`, `build_mci`)
       on a path expression containing `corpus`.
   (b) **An ALLOWLIST, declared here as data so a reader sees the whole exemption surface at once**:
       `const _P12_CORPUS_METADATA_ALLOWED = ("spike/validation/p12_consts.jl",
       "spike/test/test_p12_consts.jl", "spike/test/test_p12_decoupling.jl",
       "spike/validation/run_p12_coverage.jl")`. For every OTHER Phase-12 source, assert the stripped
       body contains none of `corpus`, `sealed_holdout`, `manifest.csv`, `CORPUS_MASTER_SEED`. For the
       allowlisted four, assert the narrower property that the body contains no `sealed_holdout` **image
       path** — i.e. `corpus/data` appears nowhere in any of them. Comment that the allowlist is
       deliberately short and that adding a fifth entry is a decision, not a fix.
   (c) `corpus/` is byte-unchanged, asserted below. Assert positively that the real-image path is the permitted one: at least
   one Phase-12 source (once 12-19 lands) references `test/test_images`, and skip that half with an
   `@info` while no coverage runner exists yet. Also
   `@test success(Cmd(`git diff --quiet HEAD -- corpus`; dir = P12_REPO_ROOT))` and
   `@test isempty(readchomp(Cmd(`git status --porcelain -- corpus`; dir = P12_REPO_ROOT)))`.
   Record in the comment WHY the exclusion is absolute: both `physical-primary` rows are
   `split = sealed_holdout` with `sha256 = PENDING-FETCH` and `bytes = 0`, reserved for the Phase-16
   blind evaluation — scoring them here would irreversibly burn Phase 16.

5. `"the shipped bundle is untouched"` — if `artifacts/amended_v2/grid_8` is tracked, assert
   `git diff --quiet HEAD -- artifacts` and `git status --porcelain -- artifacts` empty; if the path
   is absent or untracked, `@info` and skip rather than fail. Add a comment that the Phase-7 GO rests
   on this bundle, that it is content-hashed (`git-tree-sha1 = 90e6b63a…`) and Release-hosted, and
   that Phase 12 is research-lane: no reship, no new artifact, no ship gate reopened.

6. `"the Phase-11 pool is read-only to Phase 12"` — assert the Phase-12 cache root
   (`spike/data/cache/p12`) is distinct from the Phase-11 root (`spike/data/cache/p11`), by comparing
   the two path strings, so a datagen plan cannot silently overwrite Phase 11's 50 000-pair pool.

7. `"decoupling checks ran CPU-only"` —
   `@test !any(id -> occursin("CUDA", id.name), keys(Base.loaded_modules))`.

Every testset that shells out must use `Cmd(...; dir = P12_REPO_ROOT)` — never a bare backtick that
inherits the process cwd, which differs between `runtests.jl` and a direct include.
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Test; include("spike/test/test_p12_decoupling.jl")'</automated>
  </verify>
  <acceptance_criteria>
    - All seven testsets appear in the suite output with zero failures and zero errors.
    - `grep -c P12_PENDING_SCAFFOLD spike/test/test_p12_decoupling.jl` returns `0`.
    - Touching `src/results.jl` (add and remove a blank line) makes testset 1 fail and name that file;
      verify once, revert, do not commit.
    - Creating an untracked scratch file under `src/` makes testset 1 fail via the
      `git status --porcelain` half; verify once, delete, do not commit.
    - Testset 3 lists all three forbidden package names individually, so its failure message
      identifies which one appeared.
    - Testset 4's source scan uses `_strip_comment_lines` before every `occursin`, so a comment
      mentioning `corpus/` does not fail the suite and a code reference does.
    - Every shell-out uses `Cmd(...; dir = P12_REPO_ROOT)`.
  </acceptance_criteria>
  <done>The three hard constraints fail the suite when violated, in the wave where they still hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research lane → shipped package | `src/`, the spike env and the shipped artifact must not move |
| Phase-12 code → `corpus/` sealed holdout | Reading one row destroys the Phase-16 blind evaluation irreversibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information disclosure | `corpus/` sealed holdout | mitigate | Comment-stripped source scan for `corpus`/`sealed_holdout`/`open_sealed_holdout`/`manifest.csv` plus `git diff`+`git status` on `corpus` |
| T-12-20 | Tampering | `src/` byte-equality | mitigate | Per-file `git diff --quiet` AND `git status --porcelain` (the latter catches NEW untracked files, which `git diff HEAD` misses) |
| T-12-09 | Tampering | dependency set / NeuralEstimators pin | mitigate | Exact 16-name set + UUID/version assertion + three named forbidden packages asserted absent individually |
| T-12-11 | Tampering | Phase-11 training pool | mitigate | Distinct cache-root assertion so Phase-12 datagen cannot overwrite it |
| T-12-12 | Tampering | shipped `amended_v2/grid_8` bundle | mitigate | `git diff`/`git status` on `artifacts`, skipped with `@info` if untracked rather than silently passing |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` shows the seven decoupling testsets green.
- `julia --project=. -e 'using Pkg; Pkg.test()'` unchanged from its pre-plan state.
</verification>

<success_criteria>
Decoupling, the frozen dependency set and the sealed-holdout exclusion are executable assertions that
run on every suite invocation for the rest of the phase.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-05-SUMMARY.md` when done.
Commit pathspec-scoped: `git commit -- spike/test/test_p12_decoupling.jl`
</output>

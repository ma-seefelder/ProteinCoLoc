---
phase: 12-spatial-colocalization-map
plan: 20
type: execute
wave: 12
depends_on: ["12-16", "12-18", "12-19"]
files_modified:
  - spike/validation/run_p12_guards.jl
  - spike/validation/p12_guards_report.jld2
  - .planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md
  - .planning/STATE.md
autonomous: true
requirements: [SPAT-08, SPAT-09]

must_haves:
  truths:
    - "The leading alternative explanation of any SC3 win -- the chromatic radial gradient -- is measured against a bar named before the result"
    - "Every guard is REPORTED: crossing a bar computes no verdict, it shifts the burden of proof onto a written attribution argument"
    - "The D-12 Stage-2 call is written down with its numbers, and the single iteration allowance is spent or preserved explicitly"
    - "The STATE.md write is additive and destroys no other phase's entry"
  artifacts:
    - path: "spike/validation/run_p12_guards.jl"
      provides: "Four guards: radial energy, eps=0 ablation, offset grid, radial orthogonalization"
      contains: "P12_RADIAL_ENERGY_CEILING"
    - path: ".planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md"
      provides: "The D-12 Stage-2 PROCEED / ITERATE / DESCOPE call with every number quoted"
      contains: "VERDICT:"
  key_links:
    - from: ".planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md"
      to: "spike/validation/p12_guards_report.jld2"
      via: "every guard number in the verdict is re-derived from the artifact"
      pattern: "p12_guards_report"
---

<objective>
Test the explanation that would make this phase's headline result meaningless, then record the D-12
Stage-2 verdict.

Purpose (S-4): `CHROMATIC_PRIOR = Uniform(-0.02, 0.02)` applies a **radial magnification difference**
between the two channels, so the effective per-patch correlation varies **radially in every training
draw**, with no biological field present. Measured: holding everything fixed except `chromatic_eps`
0 → 0.02 moves the summary by ‖Δ‖₂ = 0.72–2.59 against an independent-draw noise floor of 0.9765, with
`cor(|Δ|, radius) = +0.48 to +0.83` and a linear-in-radius model explaining 18–69 % of the perturbation —
roughly 3× larger than the maximal registration shift. A spatial map can therefore score well on D-09's
leave-region-out coverage by learning that radial signature and never touching biology. This is not a
hypothetical: it is the same failure class as D-06's grid-alignment artifact, and it was surfaced by the
premise audit specifically because a naive map would report it as a genuine spatial finding.
Output: four guard measurements and the phase's Stage-2 verdict.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-RESEARCH.md
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md
@.planning/phases/12-spatial-colocalization-map/12-SC3-AMENDMENT.md

**Runs only on `VERDICT: PROCEED`, and that is ENFORCED, not merely stated.** The first statement of
this plan's runner is `p12_require_proceed("12-20")` (Tier 1, 12-01), which throws unless the Stage-1
verdict is `:proceed`, quoting 12-11's routing sentence in the message. The routing previously existed
only as prose in two documents, which left a wrong dispatch possible; one function with five call sites
is the fix. On a Stage-1 descope there is no spatial claim to
guard and the Stage-2 verdict is replaced by 12-11's Stage-1 descope record.

**EVERY GUARD HERE IS REPORTING-ONLY, AND THAT IS DELIBERATE.** `P12_RADIAL_ENERGY_CEILING = 0.50` and
`P12_OFFSET_GRID_TOL = 0.10` are members of `P12_REPORTING_ONLY_CONSTANTS`, which 12-01's testset 6
asserts is disjoint from `P12_GATING_CONSTANTS`. They are bars declared in advance so the numbers are
*comparable*, not thresholds that mechanically decide the phase. The reason is the over-powered-χ² failure
this project already hit in Phase 7: converting an informative diagnostic into a gate produced a loud
failure that meant nothing.

**HOW THAT COEXISTS WITH A CONFOUNDED WIN NOT BEING A PASS — read this before writing Task 2.** These two
statements look contradictory and an executor will otherwise resolve them in whichever order it reads
them, so the resolution is fixed here: **crossing a guard bar does not compute a verdict. It shifts the
burden of proof.** Concretely — if `radial_energy_fraction` exceeds `P12_RADIAL_ENERGY_CEILING`, or the
ε = 0 or radially-orthogonalized advantage collapses relative to the headline, the Stage-2 verdict may
still be PROCEED, but **only if the verdict document carries a written, numbers-cited argument for why the
advantage remains attributable to the spatial prior** — for instance that the ablation arm is equally
radial, which would make the radiality the simulator's rather than the prior's. **Absent such an argument,
the verdict is ITERATE or DESCOPE.** So no boolean is computed from a reporting-only bar, and a confounded
win still cannot be reported as a spatial finding. Both properties hold at once.

**THE THREE S-4 GUARDS ARE NAMED IN A FROZEN DOCUMENT.** `12-SC3-AMENDMENT.md` §4(g) pre-declares the
chromatic-radial confound as the leading alternative explanation of any SC3 win, together with exactly
three guards: the radial-energy report, the ε = 0 evaluation ablation, and the radially-orthogonalized
field arm. D-06's offset-grid guard is a fourth, testing a different artifact (grid alignment). All four
are implemented here. Dropping one would contradict a document frozen before any result existed.

**12-06 AND 12-11 BOTH DEFERRED TO THIS PLAN.** 12-06 recorded that if `chromatic_eps` proved vacuous
then "S-4's radial confound is LIVE, so the 12-20 guards carry the weight of the SC3 claim". 12-11
recorded that a high `radial_r2` at Stage 1 is "an early S-4 warning that must be carried into 12-20's
guard interpretation, whatever the verdict". Read both artifacts before interpreting anything here, and
state in the verdict how those two earlier signals compare with what the trained net actually shows.
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_p12_guards.jl — four guards against the artifact explanations</name>
  <files>spike/validation/run_p12_guards.jl</files>
  <read_first>
    - `spike/validation/run_p11_probe.jl` (the role analog: a simulator-only sweep with pre-registered
      rungs and a FROZEN METRIC SYMBOL, `P11_PROBE_METRIC = :paired_l2_rows_1_64` at `p11_consts.jl:260`
      — copy that frozen-metric-symbol discipline so the reported quantity cannot drift between runs)
    - `spike/validation/p12_coverage.jl` (12-16 — `lro_arm`, `lro_scores`, `lro_pass`; the guards reuse
      the same scoring path so a guard number is comparable to the headline number)
    - `spike/simulator/p12_prior.jl` (12-07 — `p12_upsample_ops(G, imsize; offset = false)`: the
      **half-cell offset is reachable by a keyword**, which is why the D-06 guard needs no second code path)
    - `spike/simulator/forward.jl:214-218` (the composed affine stage 6 where the chromatic term lives —
      READ-ONLY here; this plan sets `chromatic_eps` to 0 in θ, it does not edit the simulator)
    - `spike/simulator/p12_lattice.jl` (`p12_idx`, `p12_dct_order` — the column-major indexing the radial
      basis must use; an off-by-transpose would rotate the lattice relative to the image)
    - `spike/validation/p12_consts.jl` (`P12_RADIAL_ENERGY_CEILING`, `P12_OFFSET_GRID_TOL`,
      `P12_GUARDS_COUNTER`, `P12_REPORTING_ONLY_CONSTANTS`)
    - `spike/validation/p12_eps_ridge_report.jld2` (12-06 — `eps_ratio`, `vacuous`) and
      `spike/validation/p12_stage1_report.jld2` (12-11 — `radial_r2`)
  </read_first>
  <action>
Create `spike/validation/run_p12_guards.jl` in the `#`-prefixed short dialect. Header states the S-4
measurement quoted above, that all four guards are **reported and gate nothing**, the frozen metric
symbols, and the run line
`julia --project=spike -t auto spike/validation/run_p12_guards.jl`.

Declare frozen metric symbols at the top, in the `P11_PROBE_METRIC` spirit, e.g.
`const P12_GUARD_METRICS = (:radial_energy_fraction, :eps0_logscore_delta, :offset_logscore_delta,
:radial_orth_logscore_delta)`, and assert the artifact carries exactly these keys — so a later run cannot
quietly report a different quantity under the same name.

**Guard 1 — radial energy of the recovered deviation field.** Build a radial basis over the G×G lattice
using `p12_idx` column-major indexing: for each cell compute its radius from the image centre in lattice
coordinates, then regress the recovered per-region deviation field on a low-order polynomial in radius.
Report `radial_energy_fraction` = the fraction of the deviation field's total energy explained by the
radial component, pooled and per dataset. Compare against `P12_RADIAL_ENERGY_CEILING = 0.50` and record
the design argument 12-01 gives for that bar: a field whose radial component carries at least half its
energy cannot be linearly separated from a purely radial nuisance. Report the same quantity for the
**ablation** arm — if both arms are equally radial, the radiality is the simulator's, not the spatial
prior's, which is a materially different reading.

**Guard 2 — ε = 0 evaluation ablation, WITH A MANDATORY LIVE-HARNESS CONTROL.** Re-run the D-09 scoring
on fresh simulated data drawn with `chromatic_eps` held at exactly 0, using the **same trained nets**
(this is an evaluation ablation, not a retraining one — say so explicitly, because a retrained ε = 0 arm
would answer a different question). Report `eps0_logscore_delta` = the spatial-minus-ablation log-score
advantage under ε = 0, beside the headline advantage from 12-16.

**The control is not optional and it is the most important addition to this plan.** Every other probe in
this phase installs one — 12-06:88-90, 12-11:68-69, 12-13:101-103 — and 12-20 originally had none. The
failure it prevents is the worst available here: if this guard's own pipeline is silently broken (the
field not applied, the wrong bundle loaded, the mask ordering inverted), **both** arms' log scores collapse
together, `eps0_logscore_delta` goes to roughly zero, and the pre-declared reading below would report that
as the confound being CONFIRMED — a dead harness masquerading as the phase's most alarming finding, which
then blocks a PROCEED. Copying `headline_logscore_delta` from 12-16 into the artifact does **not** protect
against this: a copied number proves nothing about whether this plan's pipeline ran.

So: re-score the same nets on fresh data drawn with **ε ~ its usual prior**, through the *identical guard
pipeline*, and store the result as `eps_prior_control_logscore_delta`. **Frozen rule, fixed before any
number is seen:** if that control does not reproduce 12-16's `headline_logscore_delta` within sampling
noise, the ε = 0 result is **UNINFORMATIVE** and no ε-attribution verdict is recorded — exactly the
wording 12-06 uses for its own dead-control case. Only once the control reproduces may the ε = 0 number be
interpreted, and then: if the advantage largely disappears at ε = 0, the spatial prior was substantially
exploiting the chromatic radial gradient; if it survives, the confound is not carrying the result.

**Apply the same control pattern to Guards 3 and 4** — an `offset = false` re-run and an unprojected
re-run respectively, through the same code path, each required to reproduce the headline before its own
delta is interpretable.

**Guard 3's control compares against the SQUARE-RESTRICTED headline, not the pooled one.** Guard 3 runs
only at the square sizes (~75 % of mixture weight) while 12-16's `headline_logscore_delta` was computed on
the full F5 mixture including `(1376, 1028)` at weight 0.25 — and Pitfall 2 measured per-region SNR as
image-size dependent. So a pooled comparison could fail while the harness is perfectly live, spuriously
voiding the D-06 guard or, worse, inviting an executor to loosen "within sampling noise" until it passes.
12-16 must therefore record `logscore` **broken out by image size**, and Guard 3's control reproduces the
square-size-restricted headline. Guards 2 and 4 use the same data and mixture as the headline and compare
against it directly. Store them as `offset_control_logscore_delta` and
`radial_orth_control_logscore_delta`. A guard without a live control produces its most dramatic reading
when it is broken, which is the opposite of what a guard is for.

**Guard 3 — offset grid (D-06), AT SQUARE IMAGE SIZES ONLY.** Re-run the scoring with the field lattice
offset half a cell relative to the summary grid, reaching the offset through
`p12_upsample_ops(...; offset = true)` in `p12_prior.jl` and pre-rendering the offset field into
`θ.rho_field`, which is the **only** route 12-08 sanctions — 12-08 Task 2(d) implements `offset = false`
only in `forward.jl` and states that "the offset variant lives only in `p12_prior.jl` and the 12-20 guard
reaches it by pre-rendering the field into a θ, never by a second branch here".

**That route imposes a restriction this plan must respect rather than discover at runtime:** 12-08's
`_p12_check_field` requires the field to be **square**, and `P12_IMSIZE_SET` contains the non-square
`(1376, 1028)`, which carries roughly a quarter of the mixture weight. A pre-rendered field at that size
would throw. **Therefore run Guard 3 only at the square sizes in the mixture** — `(512,512)`,
`(1024,1024)`, `(2048,2048)`, about three quarters of the weight — record `offset_guard_imsizes` and
`offset_guard_mixture_weight_covered` in the artifact, and state the exclusion in the summary as a scope
limit of the guard rather than leaving it implicit. Do **not** relax `_p12_check_field` or add a branch to
`forward.jl` to reach the non-square case: 12-08's one-resampling-pass tripwire
(`test_stage6_regression.jl:94-96` asserts `count("warp(", code) == 1`) and its single-code-path rule are
worth more than the last quarter of the mixture on a reporting-only guard.

**ASSESSMENT OF THE ALTERNATIVE, AND WHY IT IS NOT ADOPTED.** The plan-checker proposed a third route:
resample the 8×8 field at half-cell-offset *lattice* positions to produce a new 8×8 field, which is legal,
square, needs no `forward.jl` change, and would run at **every** image size — closing the (1376, 1028)
blindness named in Task 2's limits. It was considered and rejected, because it does not test the same
thing. Offsetting the *lattice sample positions* perturbs the field's **values**; it leaves the rendered
field's discontinuity structure aligned to the patch boundaries exactly as before. D-06's concern is
alignment, not values — whether the net learned cues from edges that sit on the summary grid — so the
alternative would move the quantity the guard is not worried about and hold fixed the one it is. Adopting
it would buy broader size coverage at the cost of a guard that passes more easily while proving less, and
this phase has already been reminded (Guard 2's missing control) that a guard which reassures without
testing is worse than none.

If the (1376, 1028) blindness is judged unacceptable, the correct fix is **not** this reinterpretation but
the one-branch relaxation of `_p12_check_field` to accept a field whose `size(F) == imsize` with the
upsample short-circuiting to the identity — option (a) of the original finding. That is a real fix rather
than a redefinition, but it edits a file 12-08 froze for good reasons, so it belongs in a follow-up with
12-08's tripwires re-run, not in a reporting-only guard here. Recorded so the choice is visible either way.

Report `offset_logscore_delta` and the relative degradation in per-region RMSE and coverage against
`P12_OFFSET_GRID_TOL = 0.10`. State the reason this guard exists: piecewise-constant cells would create
hard discontinuities aligned exactly with the patch grid the summary reads, and a net that learned
grid-aligned block-edge cues would pass SC2 in simulation and fail on real images, with a failure mode
expensive to diagnose afterwards. D-06 chose smooth interpolation to avoid it; this guard checks that the
choice worked.

**Guard 4 — radially-orthogonalized field arm, specified exactly.** This is the strongest of the four —
it asks whether a spatial advantage survives the confound being removed *algebraically* rather than merely
held at zero — so it must be written out rather than gestured at. Fix all four degrees of freedom here:

- **What is projected:** each posterior draw's reconstructed G×G deviation field, individually, before any
  scoring. Not the posterior mean (that would discard the uncertainty the phase exists to deliver) and not
  the summary.
- **Which arms:** the identical projection is applied to **both** the spatial and the ablation arm. A
  delta between differently-projected arms would be meaningless.
- **The observation side is left UNTRANSFORMED.** The held-out correlation entry itself carries the
  chromatic radial component, so projecting the observation too would remove the very thing the prediction
  is being scored against. The consequence must be stated in the report: this projection degrades **both**
  arms in absolute terms, and it also removes any genuinely radial *biological* signal, so the absolute
  log scores are expected to fall. Report both arms' absolute scores before and after projection, so the
  reader sees the projection's cost before reading the delta.
- **Frozen interpretation rule, in advance:** the guard passes its own reading if
  `radial_orth_logscore_delta` retains a material fraction of `headline_logscore_delta` — state the
  fraction in the header before running. A delta that collapses to zero under projection means the
  advantage was carried by the radial component.

Use a dedicated stream off `P12_GUARDS_COUNTER`. Reuse 12-16's scoring functions so every guard number is
directly comparable to the headline; do not reimplement scoring.

Atomic write to `spike/validation/p12_guards_report.jld2`: `schema_version = 1`, `guard_metrics =
P12_GUARD_METRICS`, each metric pooled and per-arm, the **three control keys**
`eps_prior_control_logscore_delta`, `offset_control_logscore_delta`,
`radial_orth_control_logscore_delta` plus a `controls_live::Bool` per guard, `radial_energy_ceiling`,
`offset_grid_tol`, `offset_guard_imsizes`, `offset_guard_mixture_weight_covered`,
`radial_orth_scores_before_after` (both arms, absolute, pre- and post-projection),
`headline_logscore_delta` (copied from 12-16 so the comparison is inside one artifact),
`eps_ridge_vacuous` and `eps_ridge_ratio` (copied from 12-06), `stage1_radial_r2` (copied from 12-11),
`n_datasets`, `reported_only = true`, `elapsed_min`, `generated`, `julia_version`, and a `caption` ending
`Diagnostic; gates nothing.` Reopen and `@assert haskey` for all four metric keys AND all three control
keys before `mv`.
Close with `isdefined(@__MODULE__, :P12_GUARDS_LOAD_ONLY) || main()`.
  </action>
  <verify>
    <automated>julia --project=spike -e 'P12_GUARDS_LOAD_ONLY = true; include("spike/validation/run_p12_guards.jl"); @assert length(P12_GUARD_METRICS) == 4 "four pre-registered guards"; @assert :radial_energy_fraction in P12_GUARD_METRICS; src=read("spike/validation/run_p12_guards.jl", String); @assert occursin("gates nothing", src); @assert !occursin("NeuralEstimators.train", src) "guards re-score, they never retrain"; println(P12_GUARD_METRICS, " guards-load-ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0, prints the four frozen metric symbols and `true`, and runs no scoring.
    - The header contains the literal `gates nothing`, and the caption ends with `Diagnostic; gates nothing.`
    - All four guards are implemented; a source read confirms Guard 2 reuses the trained nets rather than
      retraining, and Guard 3 reaches the offset via `p12_upsample_ops(...; offset = true)` and a
      pre-rendered `θ.rho_field`, never a second branch in `forward.jl`.
    - **Each of Guards 2, 3 and 4 carries a live-harness control**, and each control's frozen rule is
      present in the header: a control that fails to reproduce `headline_logscore_delta` within sampling
      noise makes that guard's result UNINFORMATIVE, with no attribution verdict recorded.
    - `offset_guard_imsizes` contains only square sizes, and
      `offset_guard_mixture_weight_covered` is reported — the non-square `(1376, 1028)` exclusion is
      recorded as a scope limit, not silently skipped.
    - `git diff --quiet HEAD -- spike/simulator` succeeds: this plan relaxes no field check and adds no
      `warp(` call, so `test_stage6_regression.jl`'s one-resampling-pass tripwire is untouched.
    - The radial basis uses `p12_idx` column-major indexing; a transposed variant is forbidden by an
      acceptance criterion and would rotate the lattice relative to the image.
    - `radial_energy_fraction` is reported for BOTH arms.
    - After stripping comment lines the file contains zero occurrences of `NeuralEstimators.train` and
      zero edits to `spike/simulator/` (`git diff --quiet HEAD -- spike/simulator` succeeds).
  </acceptance_criteria>
  <done>All four artifact explanations are measured against bars named in advance, with the numbers comparable to the headline result inside one artifact.</done>
</task>

<task type="auto">
  <name>Task 2: the D-12 Stage-2 verdict, and an additive STATE.md note</name>
  <files>.planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/11-registration-and-chromatic-uncertainty-as-latent/11-PROBE-VERDICT.md` (the
      verdict-document shape) and `11-CLOSURE.md` (how a negative-but-useful result is framed honestly)
    - `.planning/phases/12-spatial-colocalization-map/12-STAGE1-VERDICT.md` (12-11 — the Stage-1 call this
      one follows, and its `radial_r2` warning)
    - All five Phase-12 reported artifacts: `p12_guards_report.jld2`, `p12_coverage_sim_report.jld2`,
      `p12_coverage_real_report.jld2`, `p12_sbc_report.jld2`, `p12_minispike_report.jld2`
    - `spike/validation/p12_consts.jl` (`P12_ITERATION_ALLOWANCE = 1`, and the full
      `P12_GATING_CONSTANTS` tuple — the Stage-2 gate is exactly these and nothing else)
    - `.planning/STATE.md` (the `## Current Position` block — **single-slot and shared with Phase 13**)
  </read_first>
  <action>
(a) Write `12-STAGE2-VERDICT.md` in the `11-PROBE-VERDICT.md` shape. Every number **re-derived by loading
the artifacts**, never copied from a console or from a sibling summary.

Quote: the SBC target rows' KS p-values in Gaussian space and the equivalence pass/fail counts (12-18);
simulated coverage and log score per arm plus the parameter-coverage cross-check (12-16); real-image
per-image and pooled coverage and log score, with the region counts (12-19); all four guard metrics
(this plan); and the selected prior and production rank (12-15).

Then the call, as exactly one line matching `^VERDICT: (PROCEED|ITERATE|DESCOPE)$`:
- **PROCEED** — the Stage-2 gate constants in `P12_GATING_CONSTANTS` are met AND the guards do not
  attribute the advantage to the chromatic radial confound. State both halves; a gate pass with a failed
  guard reading is **not** a PROCEED, and say why in the document rather than leaving it to a reader.
- **ITERATE** — the gate is missed in a way one documented change could plausibly fix. Spending
  `P12_ITERATION_ALLOWANCE = 1` is recorded HERE, in this document, with the specific change and the
  reason, before it is made. After it is spent the allowance is zero and the next outcome is PROCEED or
  DESCOPE; there is no second iteration. Record the remaining allowance explicitly as a number.
- **DESCOPE** — ship D-13's ablation model: a per-region Δρ map **with** per-region uncertainty, deferring
  only the spatial borrowing to v2.1. Frame it as D-13 intends and as 12-11 already recorded — a reduced
  release, not a write-off, and strictly more than `LocalColocMap` offers today, which carries no
  uncertainty at all. Note that at a Stage-2 descope the ablation exists at full scale, so this fallback
  is the complete version rather than the spike-scale one a Stage-1 descope would have left.

Add a **Named limits** section, since this is the document the manuscript will draw on. At minimum:
- **The offset-grid guard is BLIND at (1376, 1028), which is 100 % of the real physical data.** Guard 3
  runs only at the square image sizes, because 12-08's `_p12_check_field` requires a square field and the
  pre-rendered-θ route is the only sanctioned one. (1376, 1028) is not merely one rung of the mixture — it
  is the **D-08 real-data anchor and the actual size of BOTH specimens 12-19 scores**. The corpus audit
  sharpens this from a coverage gap into something stronger: since `corpus/data/` is empty and the two
  sealed anchors are untouchable, those two 1028×1376 specimens are the *entire* real-image evidence base
  for the phase. So the grid-alignment artifact is unguarded on **every** real image the phase reports,
  not merely on a quarter of the simulated mixture.
  State the trade plainly rather than softening it: 12-08's one-resampling-pass tripwire
  (`count("warp(", code) == 1`) and its single-code-path rule were judged worth more than extending a
  reporting-only guard to a non-square geometry. That is defensible for a *simulated* coverage rung and
  distinctly less comfortable when it is the whole real-data surface. A reader is entitled to disagree,
  which is exactly why it belongs in Named limits and not in a runner's summary — and why the follow-up
  option (the one-branch `_p12_check_field` relaxation, with 12-08's tripwires re-run) is recorded beside
  it in Task 1 rather than left implicit.
predictive coverage is not parameter coverage (D-09); the real-image claim rests on 2 images and
correlated regions (D-11); the Fisher-z model with one `n_eff` is a declared assumption, so D-09 validates
the joint of posterior and noise model (12-16); whichever rows the shrinkage table flagged vacuous,
including `r1` if applicable, read against 12-13's independent ridge answer; the z-scoring arm actually
used (R-3); and whether `chromatic_eps` proved identifiable (12-06), which determines whether named limit
#4 needs revision.

(b) Append a note to `.planning/STATE.md`. **The `## Current Position` block is single-slot and Phase 13
writes to it concurrently.** Add one additive line recording Phase 12's Stage-2 verdict and pointing at
this document. Do **not** delete or rewrite Phase 13's entry, and do not remove Phase 11's closed-phase
entry. Before writing, re-read the file — Phase 13 may have changed it since this plan started — and after
writing, confirm with `git diff` that the only added lines are yours and no existing line was removed. If
another agent's write lands between your read and your write, redo the read rather than forcing the write.
  </action>
  <verify>
    <automated>grep -cE '^VERDICT: (PROCEED|ITERATE|DESCOPE)$' .planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md && grep -q "12-STAGE2-VERDICT" .planning/STATE.md && git diff --stat -- .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `12-STAGE2-VERDICT.md` contains exactly one `^VERDICT: ` line.
    - Every number in the verdict is present in one of the five artifacts — spot-check at least six by
      loading the `.jld2` files, and record the six checks in the summary.
    - The document contains a `Named limits` section listing at minimum the six items above.
    - If the verdict is ITERATE, the document states the specific change, the reason, and the remaining
      allowance as `0`.
    - `git diff -- .planning/STATE.md` shows **only** added lines — zero deletions. Paste the diff into
      the summary. Phase 13's entry and Phase 11's closed-phase entry are both still present.
    - `git diff --quiet HEAD -- src spike corpus` succeeds for this task — it writes only planning docs.
  </acceptance_criteria>
  <done>The Stage-2 call is recorded with its numbers and its named limits, and the shared STATE.md block gained a line without losing one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chromatic radial gradient → a claimed spatial finding | The confound is present in every training draw and would be reported as biology by a naive map |
| reporting-only bar → a mechanical gate | Converting a diagnostic into a threshold recreates the Phase-7 over-powered failure |
| shared STATE.md slot → concurrent Phase-13 writer | A non-additive write silently destroys another phase's position record |
| iteration allowance → unbounded retries | An unrecorded iteration turns a pre-registered gate into an unfalsifiable one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Spoofing | a radial chromatic artifact reported as a spatial finding | mitigate | Four guards, all pre-declared: radial-energy fraction for BOTH arms, an ε = 0 evaluation ablation on the same nets, an offset-grid arm, and a radially-orthogonalized arm that removes the confound algebraically; a gate pass with a failed guard reading is explicitly not a PROCEED |
| T-12-82 | Elevation of privilege | a reporting-only bar acting as a gate | mitigate | Both bars are members of `P12_REPORTING_ONLY_CONSTANTS`, asserted disjoint from `P12_GATING_CONSTANTS` by 12-01's testset 6; the artifact carries `reported_only = true` and the caption ends `Diagnostic; gates nothing.` |
| T-12-83 | Tampering | a drifting guard metric definition | mitigate | `P12_GUARD_METRICS` frozen metric symbols, asserted to be exactly the artifact's metric keys, in the `P11_PROBE_METRIC` discipline |
| T-12-84 | Tampering | an unrecorded or repeated Stage-2 iteration | mitigate | `P12_ITERATION_ALLOWANCE = 1`; spending it is recorded in the verdict document before the change is made, with the remaining allowance written as a number |
| T-12-85 | Tampering | a non-additive STATE.md write destroying Phase 13's entry | mitigate | Re-read before write, additive line only, `git diff` asserted to show zero deletions, and the diff pasted into the summary; a concurrent write triggers a re-read rather than a force |
| T-12-86 | Repudiation | a verdict whose numbers cannot be traced | mitigate | Every number re-derived by loading the five artifacts, with at least six spot-checks recorded; `headline_logscore_delta` and the earlier 12-06 / 12-11 signals are copied INTO the guards artifact so the comparison lives in one place |
| T-12-07 | Tampering | git index race with Phase 13 | mitigate | Pathspec-scoped commits limited to the four files in `files_modified`; STATE.md committed in its own pathspec-scoped commit |
</threat_model>

<verification>
- `spike/validation/p12_guards_report.jld2` loads with all four frozen metric keys, both arms' radial
  energy, the copied headline and earlier-signal fields, and `reported_only = true`.
- `12-STAGE2-VERDICT.md` has exactly one `^VERDICT: ` line and a `Named limits` section.
- `git diff -- .planning/STATE.md` shows only additions; Phase 13's and Phase 11's entries survive.
- `julia --project=spike spike/test/runtests.jl` green.
- `git diff --quiet HEAD -- src spike/simulator spike/Project.toml spike/Manifest.toml corpus spike/validation/p12_consts.jl` succeeds.
</verification>

<success_criteria>
The chromatic radial confound — the leading alternative explanation of any SC3 win, present in every
training draw by construction — is measured four ways against bars named before the result, including one
arm that removes it algebraically, and the radial energy is reported for the ablation as well so a shared
simulator artifact is distinguishable from a spatial-prior effect. The D-12 Stage-2 verdict is recorded
with every number traceable to an artifact, with its named limits written for the manuscript, and with the
single iteration allowance either preserved or spent on the record. The shared STATE.md block gained
Phase 12's line and lost nothing.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-20-SUMMARY.md` when done, with all four guard
metrics for both arms, the ε = 0 and radially-orthogonalized readings beside the headline advantage, the
six artifact spot-checks, the fired verdict branch, and the STATE.md diff proving it was additive.
Commit pathspec-scoped, three commits in task order:
1. `git commit -- spike/validation/run_p12_guards.jl`
2. `git commit -- spike/validation/p12_guards_report.jld2 .planning/phases/12-spatial-colocalization-map/12-STAGE2-VERDICT.md`
3. `git commit -- .planning/STATE.md`
</output>

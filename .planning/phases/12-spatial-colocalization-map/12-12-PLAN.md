---
phase: 12-spatial-colocalization-map
plan: 12
type: execute
wave: 3
depends_on: ["12-03", "12-04"]
files_modified:
  - spike/p12/result.jl
  - spike/test/test_p12_result.jl
autonomous: true
requirements: [SPAT-05]

must_haves:
  truths:
    - "The executable per-region result type is a NEW AbstractColocResult subtype, and src/results.jl stays byte-unchanged"
    - "An unscorable region is never indistinguishable from a measured Delta-rho of zero"
    - "A missing operating point reads as NOT CHECKED, never as in-distribution"
    - "The scalar rho the result reports is labelled DERIVED wherever it appears"
  artifacts:
    - path: "spike/p12/result.jl"
      provides: "SpatialColocResultSpike + the four shared accessors + delta_rho_map / uncertainty_map + sentinel discipline"
      contains: "AbstractColocResult"
    - path: "spike/test/test_p12_result.jl"
      provides: "Subtype, accessor, sentinel/OOD-pairing, dimension-guard and src-untouched tests"
      contains: "LOCAL_MAP_SENTINEL"
  key_links:
    - from: "spike/p12/result.jl"
      to: "src/results.jl"
      via: "READ-ONLY include to reach AbstractColocResult; the sketch block is never edited"
      pattern: "results\\.jl"
---

<objective>
Give the phase a real result type: a per-region Δρ map **with** a per-region uncertainty map, as a new
subtype of the package's actual result hierarchy, without touching `src/`.

Purpose: `src/results.jl:180-191` has carried a comment-only `SpatialColocResult` sketch since Phase 7, and
`local_map.jl:56-79`'s own docstring says the shipped `LocalColocMap` "carries no posterior draws, no Bayes
factor and **no per-region uncertainty**". Filling that gap *is* the phase's product, and it is also the
D-13 descope deliverable — which means the type must exist and be tested regardless of how the Stage-1 and
Stage-2 gates land. Phase 13 solved the same "executable subtype in the spike, sketch untouched in `src/`"
problem and `spike/p13/result.jl` is the exact precedent.
Output: `spike/p12/result.jl` plus a real `test_p12_result.jl` replacing the 12-01 scaffold.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spatial-colocalization-map/12-PATTERNS.md

**Scope boundary.** This plan defines the **type** and its accessors. The `p12_coloc_map` *function* that
produces one from a trained estimator and an image needs the posterior read path and therefore lands in
12-16 alongside the coverage machinery. Defining the type early is deliberate: 12-16 and 12-19 both write
into it, and a container invented mid-run acquires fields by accretion — which is exactly what Phase 7's
D-02 rule ("new result variants slot in as new subtypes, never as bolted-on fields") exists to prevent.

**Two honesty rules inherited from `local_map.jl`, and they are not decoration.**
- `LOCAL_MAP_SENTINEL = 0.0` is written for a region that could **not** be scored, and every sentinel
  region also carries `ood = true`, so a sentinel is never silently indistinguishable from a measured Δρ of
  zero (T-7-04). Phase 12 must follow this, and its map has a place the shipped one does not: a per-region
  `sd`, which for a sentinel region must be a documented non-finite or `missing`, never a small number.
- A `false` OOD flag with **no recorded threshold** means **NOT CHECKED**, not "in distribution"
  (`local_map.jl:88-91`). Carry that rule into the `meta` field.
</context>

<tasks>

<task type="auto">
  <name>Task 1: spike/p12/result.jl — the subtype, the accessors, the sentinel discipline</name>
  <files>spike/p12/result.jl</files>
  <read_first>
    - `spike/p13/result.jl` (READ IN FULL, 352 lines — the exact analog. The "where the executable type
      lives and why the sketch is not edited" block at :35-43; the guarded includes in dependency order at
      :72; the `const` key tuple + `NamedTuple` alias pattern at :96-115; the struct declaration and its
      validating inner constructor at :142-175; the `_p13_as_three_way_logbf` keyed-NamedTuple guard at
      :179; the four shared accessors at :186-244; the named accessor at :245-258; the calibration surface
      at :259-276; `p13_calibration_meta` at :337)
    - `src/results.jl:155-195` (READ-ONLY — the `AbstractColocResult` supertype, Phase 7 D-02's
      new-subtype rule at :161-167, and the `SpatialColocResult` **comment-only sketch** at :180-191 with
      its named fields `region_delta_rho`, `region_sd`, `ood`, `calibration`, `meta` and the
      `delta_rho_map` / `uncertainty_map` accessors. This file is **never edited**)
    - `src/amortized/local_map.jl:19-40, 42-53, 56-79, 88-91` (READ-ONLY — the Finding-2 statement, the
      `LOCAL_MAP_SENTINEL` discipline and its T-7-04 rationale, the "no per-region uncertainty" docstring,
      and the NOT-CHECKED rule for a flag without a threshold)
    - `spike/contract.jl:40-55` (the existing READ-ONLY reach into `src/` — the precedent for how a spike
      file includes a `src/` file without owning it)
    - `spike/npe/p12_architecture.jl` (`P12_THETA_ROWS`, `p12_row_c0`, `p12_rows_dev`, `p12_row_r1`,
      `P12_ZSCORE_ARM`) and `spike/simulator/p12_lattice.jl` (`p12_idct_vec`, `p12_dct_order`)
    - `spike/validation/sbc.jl:57-155` (`CalibrationResult`, `_bin_calibration`, `sbc_traffic_light` — the
      calibration container the `calibration` field carries; do not define a second one)
  </read_first>
  <action>
Create `spike/p12/result.jl`, `#= =#` AGPL header, mirroring `spike/p13/result.jl` structurally.

Header must carry the analog's `:35-43` block adapted: D-01 and CLAUDE.md scope all Phase-12 work to
`spike/`, so the executable subtype is defined **here**, reaching its supertype by a READ-ONLY include of
`src/results.jl` — the same read-only reach `spike/contract.jl:46-47` already uses — and the comment-only
sketch at `src/results.jl:180-191` stays **BYTE-UNCHANGED**. Add the `:35-43` companion point: Phase 7
D-02 is still satisfied, because this is a new subtype of the real `AbstractColocResult` and not a set of
bolted-on fields.

Guarded includes in dependency order: `src/results.jl` (read-only, for the supertype), then
`../validation/sbc.jl` (for `CalibrationResult`), then `../npe/p12_architecture.jl` and
`../simulator/p12_lattice.jl`.

`const P12_SPATIAL_SENTINEL = 0.0` — with a comment stating it is the **same value and the same discipline**
as `LOCAL_MAP_SENTINEL`, reproduced here because `src/` is read-only in this phase, and that the two must
not drift: a testset asserts they are equal.

```julia
struct SpatialColocResultSpike <: AbstractColocResult
    grid            :: Int
    region_delta_rho:: Matrix{Float64}                 # G×G, ρ units, sentinel where unscorable
    region_sd       :: Matrix{Float64}                 # G×G, ρ units, NaN where unscorable
    region_draws    :: Union{Nothing, Array{Float64,3}} # G×G×N posterior draws, or nothing
    ood             :: Matrix{Bool}                    # G×G; TRUE for every sentinel region
    calibration     :: Union{Nothing, CalibrationResult}
    meta            :: NamedTuple
end
```

An inner constructor in the analog's `:150-175` validating style, throwing `ArgumentError` that names the
offending value for each of:
- `grid >= 2` and every matrix exactly `grid × grid`;
- `region_delta_rho` finite and in `[-2, 2]` (Δρ is a difference of two correlations, so its range is
  `[-2, 2]`, not `[-1, 1]` — state that in the message so nobody "fixes" it to `[-1,1]`);
- `region_sd` either finite and `> 0` **or** `NaN`, and **`NaN` exactly where `ood` is true** — this is the
  pairing that makes an unscorable region unmistakable, and it is checked in the constructor rather than
  hoped for at the call site;
- `region_delta_rho[i] == P12_SPATIAL_SENTINEL` wherever `ood[i]` is true;
- `region_draws === nothing || size(region_draws)[1:2] == (grid, grid)`;
- `meta` carries at minimum the keys `:zscore_arm`, `:arm`, `:r1_posterior_median`, `:derived_rho`,
  `:ood_threshold`, `:net_path`, `:schema_version`. Guard the key set with a keyed-NamedTuple check in the
  `_p13_as_three_way_logbf` style at `:179`, so a missing or extra key throws and names D-02, rather than
  being stored silently.

The four shared `AbstractColocResult` accessors, matching the analog's `:186-244` signatures exactly so the
interface is genuinely implemented rather than approximated. Then:

`delta_rho_map(r::SpatialColocResultSpike) = r.region_delta_rho` and
`uncertainty_map(r::SpatialColocResultSpike) = r.region_sd` — the two names the `src/` sketch specifies.

`delta_rho(r::SpatialColocResultSpike)` — the single scalar the `AbstractColocResult` interface expects.
Return `r.meta.derived_rho` and make the docstring state in its **first sentence** that this scalar is
**DERIVED** (R-1) from the global DCT coefficient `c₀`, is not a sampled parameter, and is reported for
continuity with the shipped estimator only. A reader who takes it as the estimand has been misled, so the
label goes where they will read it.

`p12_region_field(coefficients)` — reconstruct a G×G field from a θ column's DCT rows via
`p12_idct_vec` with the `p12_dct_order` permutation inverted. Comment that this is an **exact orthonormal
inverse**, not an approximation, which is why D-07's no-lossy-transform rule is not engaged.

`p12_mark_unscorable!(r_dev, r_sd, ood, idxs)` — the one helper that writes the sentinel triple
(`P12_SPATIAL_SENTINEL`, `NaN`, `true`) for a set of region indices. Having exactly one writer is what keeps
the constructor's pairing invariant satisfiable; comment that a call site setting only one of the three is
the bug this helper exists to make impossible.

`p12_result_meta(; zscore_arm = P12_ZSCORE_ARM, arm, r1_posterior_median, derived_rho, ood_threshold,
net_path, schema_version = 1)` — the constructor for the `meta` NamedTuple, mirroring
`p13_calibration_meta` at `:337`. Its docstring must carry the `local_map.jl:88-91` rule verbatim in
spirit: an `ood_threshold` of `nothing` means the operating point was **NOT CHECKED**, and a `false` flag
under a `nothing` threshold must never be read as "in distribution".

Add nothing to `spike/Project.toml`. Create the `spike/p12/` directory.
  </action>
  <verify>
    <automated>julia --project=spike -e 'include("spike/p12/result.jl"); using LinearAlgebra; d=zeros(8,8); s=fill(0.1,8,8); o=falses(8,8); m=p12_result_meta(arm=:car, r1_posterior_median=0.5, derived_rho=0.3, ood_threshold=nothing, net_path="x"); r=SpatialColocResultSpike(8,d,s,nothing,o,nothing,m); println(typeof(r) <: AbstractColocResult, " ", size(delta_rho_map(r)), " ", size(uncertainty_map(r)), " ", delta_rho(r))'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0 and prints `true`, `(8, 8)`, `(8, 8)`, `0.3`.
    - `git diff --quiet HEAD -- src` succeeds AND `git status --porcelain -- src` is empty — the sketch is
      untouched and no new file appeared under `src/`.
    - `spike/p12/result.jl` contains no `SpatialColocResult` **definition** for the `src/` name (only the
      spike subtype `SpatialColocResultSpike`), so the sketch's identifier stays unclaimed for the future
      productionization phase.
    - The `delta_rho` docstring's first sentence contains the word `DERIVED`.
    - After stripping comment lines, the file defines `CalibrationResult` **zero** times (it is included,
      not redefined).
    - `git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml corpus` succeeds.
  </acceptance_criteria>
  <done>A per-region Δρ map with per-region uncertainty exists as a real subtype whose invariants are enforced at construction.</done>
</task>

<task type="auto">
  <name>Task 2: test_p12_result.jl — subtype, invariants, sentinel pairing, src untouched</name>
  <files>spike/test/test_p12_result.jl</files>
  <read_first>
    - `spike/test/test_p12_result.jl` (the 12-01 pending scaffold this task REPLACES)
    - `spike/test/test_p13_result.jl` (READ IN FULL — the testset layout, the constructor-guard block, the
      `src/results.jl is untouched` testset at :185-219 **with its `_has_git` guard**, and the closing
      CPU-only testset at :215-217)
    - `spike/p12/result.jl` (the file under test)
  </read_first>
  <action>
Replace the scaffold.

1. `"SpatialColocResultSpike is a real AbstractColocResult subtype (Phase-7 D-02)"` —
   `@test SpatialColocResultSpike <: AbstractColocResult`; the four shared accessors are all defined for
   it (`@test hasmethod(f, Tuple{SpatialColocResultSpike})` for each); and
   `@test SpatialColocResultSpike !== SpatialColocResult` is **not** asserted by name — instead assert that
   the identifier `SpatialColocResult` is *not* defined as a type in this module, proving the `src/` sketch
   name is still unclaimed.

2. `"the accessors return the maps the src sketch names"` — `delta_rho_map` and `uncertainty_map` return
   `grid × grid` matrices and are `===` the corresponding fields (no copy, no transpose).

3. `"an unscorable region is unmistakable (T-7-04)"` — build a result via `p12_mark_unscorable!` on three
   region indices; assert those entries of `region_delta_rho` equal `P12_SPATIAL_SENTINEL`, the
   corresponding `region_sd` entries are `NaN`, and the corresponding `ood` entries are `true`. Then the
   falsifiers, which are the point of the testset:
   `@test_throws ArgumentError` for a result whose `ood` is true where `region_sd` is finite;
   `@test_throws ArgumentError` for a result whose `ood` is true where `region_delta_rho` is a small
   non-sentinel number (e.g. `1e-9`); and `@test_throws ArgumentError` for a result whose `region_sd`
   contains a `NaN` where `ood` is false. Comment that the middle case is the one that matters in practice:
   a near-zero Δρ from a failed region reads as "measured no difference" unless the pairing is enforced.

4. `"P12_SPATIAL_SENTINEL has not drifted from LOCAL_MAP_SENTINEL"` — read
   `src/amortized/local_map.jl` as a **string** (do not `include` it; `src/` stays unloaded) after
   `_strip_comment_lines`, extract the `LOCAL_MAP_SENTINEL = ...` literal, and assert it parses to
   `P12_SPATIAL_SENTINEL`. Comment that a source-text read is used precisely because `src/` must stay
   unloaded, and that the alternative — trusting a comment — is what this project's seed-disjointness rule
   already rejects.

5. `"constructor guards"` — `@test_throws ArgumentError` for: `grid = 1`; a non-square
   `region_delta_rho`; a `region_delta_rho` entry of `2.5` (outside `[-2, 2]`); a `region_sd` entry of
   `0.0`; a `region_draws` whose first two dimensions disagree with `grid`; and a `meta` missing
   `:zscore_arm`.

6. `"Δρ's range is [-2, 2] and that is deliberate"` — a result with `region_delta_rho` entries at `-1.9`
   and `+1.9` constructs successfully. This testset exists so a future reader who "corrects" the bound to
   `[-1, 1]` breaks a test that explains why.

7. `"the scalar rho is labelled DERIVED (R-1)"` — `@test occursin("DERIVED", string(@doc delta_rho))`,
   and `@test delta_rho(r) == r.meta.derived_rho`.

8. `"a missing operating point reads as NOT CHECKED"` — for a result whose `meta.ood_threshold` is
   `nothing` and whose `ood` is all `false`, assert a helper predicate (add
   `p12_ood_checked(r) = r.meta.ood_threshold !== nothing`) returns `false`, and assert in a comment that
   downstream code must branch on `p12_ood_checked` before interpreting an all-`false` OOD map.

9. `"the meta records the z-scoring arm (R-3)"` — `@test r.meta.zscore_arm === P12_ZSCORE_ARM === :per_row`.

10. `"src/ is byte-unchanged"` — the `test_p13_result.jl:185-219` testset verbatim in shape, with the
    `_has_git` guard, covering `src`, `src/results.jl` and `src/amortized/local_map.jl` individually plus
    `git status --porcelain -- src`.

11. `"result surface ran CPU-only"` —
    `@test !any(id -> occursin("CUDA", id.name), keys(Base.loaded_modules))`.
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Test; include("spike/test/test_p12_result.jl")'</automated>
  </verify>
  <acceptance_criteria>
    - All eleven testsets appear in the suite output with zero failures and zero errors.
    - `grep -c P12_PENDING_SCAFFOLD spike/test/test_p12_result.jl` returns `0`.
    - Testset 3's three falsifiers each fail if the corresponding constructor check is deleted in a scratch
      copy (verify once each, revert, do not commit).
    - The file does not `include` any `src/` file except through `spike/p12/result.jl`'s own read-only
      include; testset 4 reads `local_map.jl` as text only.
    - `git diff --quiet HEAD -- src` succeeds and `git status --porcelain -- src` is empty after the run.
  </acceptance_criteria>
  <done>The result type's invariants — especially the sentinel/OOD/NaN triple — are enforced and proved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research lane → `src/` | The sketch at `src/results.jl:180-191` must stay byte-unchanged and its identifier unclaimed |
| unscorable region → reported finding | A sentinel that reads as a measured zero is a false negative presented as a result |
| flag without a threshold → "in distribution" | An unchecked OOD flag interpreted as a pass is an unearned safety claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20 | Tampering | `src/` byte-equality | mitigate | Read-only include; testset 10 with per-file `git diff` plus `git status --porcelain` for new files |
| T-12-43 | Spoofing | a sentinel region reported as a measured Δρ of zero | mitigate | The constructor enforces the sentinel/`NaN`/`ood` triple; `p12_mark_unscorable!` is the single writer; testset 3 falsifies each half independently |
| T-12-44 | Spoofing | an unchecked OOD map read as a clean bill of health | mitigate | `p12_ood_checked` predicate plus the `local_map.jl:88-91` NOT-CHECKED rule carried into the `meta` docstring and testset 8 |
| T-12-45 | Repudiation | the derived scalar ρ mistaken for the estimand | mitigate | `DERIVED` in the first sentence of the `delta_rho` docstring, asserted by testset 7 |
| T-12-46 | Tampering | the sentinel value drifting from `src/`'s | mitigate | Testset 4 reads the literal out of `local_map.jl` as text and compares |
| T-12-47 | Tampering | result variant added as bolted-on fields | mitigate | New `AbstractColocResult` subtype (Phase-7 D-02); testset 1 asserts the subtype relation and that the `src/` name stays unclaimed |
| T-12-07 | Tampering | git index race with Phase 13 | mitigate | Pathspec-scoped commit limited to the two files in `files_modified` |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` shows the eleven result testsets green.
- `git diff --quiet HEAD -- src corpus spike/Project.toml spike/Manifest.toml` succeeds and
  `git status --porcelain -- src` is empty.
- `julia --project=. -e 'using Pkg; Pkg.test()'` unchanged.
</verification>

<success_criteria>
The phase's deliverable has a container: a genuine `AbstractColocResult` subtype carrying a per-region Δρ
map **and** a per-region uncertainty map, whose constructor makes an unscorable region impossible to confuse
with a measured zero, whose scalar ρ is labelled derived where a reader will see it, and which leaves the
`src/` sketch byte-unchanged and its name unclaimed.
</success_criteria>

<output>
Create `.planning/phases/12-spatial-colocalization-map/12-12-SUMMARY.md` when done.
Commit pathspec-scoped: `git commit -- spike/p12/result.jl spike/test/test_p12_result.jl`
</output>

---
phase: 14-decision-and-abstention-layer
plan: 05
type: execute
wave: 2
depends_on: ["14-01", "14-03"]
files_modified:
  - spike/p14/conformal.jl
  - spike/p14/fuse.jl
  - spike/test/test_p14_conformal.jl
  - spike/test/test_p14_fuse.jl
autonomous: true
requirements: []          # ROADMAP: `Requirements: TBD`. Traced to D-02, D-05, D-06 and
                          # 14-VALIDATION rows SC1-e, SC2-a, SC2-b.
decisions: [D-02, D-05, D-06]
validates: ["SC1-e", "SC2-a", "SC2-b", "SC2-c"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "Split conformal is hand-rolled from Base sort + integer indexing; ConformalPrediction.jl is NOT added and SC1 is AMENDED by D-02"
    - "The conformal quantile is the ORDER STATISTIC, never Statistics.quantile's interpolated value"
    - "The empty conformal set has its own trigger and is never collapsed into :ambiguous"
    - "The fusion is D-05's ASYMMETRY, not SC2's literal OR: cross-method disagreement ALONE DECIDES (SC2 is AMENDED by D-05)"
    - "A false OOD flag that means NOT CHECKED abstains by default, with an explicit caller opt-out (D-06)"
    - "Every ABSTAIN records which trigger fired"
  artifacts:
    - path: "spike/p14/conformal.jl"
      provides: "p14_lac_score, p14_conformal_quantile, p14_conformal_set"
      contains: "ceil(Int, (n + 1)"
      min_lines: 90
    - path: "spike/p14/fuse.jl"
      provides: "p14_ood_state (three-valued) + p14_fuse (D-05 asymmetric)"
      contains: "not_checked"
      min_lines: 100
    - path: "spike/test/test_p14_conformal.jl"
      provides: "the order-statistic-vs-interpolation discriminating test"
      contains: "Statistics.quantile"
      min_lines: 70
    - path: "spike/test/test_p14_fuse.jl"
      provides: "the full 36-cell truth table"
      contains: "decided_contra_classical"
      min_lines: 90
  key_links:
    - from: "spike/p14/fuse.jl"
      to: "src/amortized/local_map.jl"
      via: "the acceptance predicate `t isa Real && isfinite(t)` mirrored exactly"
      pattern: "isfinite"
    - from: "spike/p14/conformal.jl"
      to: "spike/p14/posterior.jl"
      via: "LAC score computed on the same posterior the FDR rule uses"
      pattern: "p14_lac_score"
---

<objective>
Build the distribution-free hedge (hand-rolled split conformal) and the abstention rule (D-05's
asymmetric fusion with D-06's three-valued OOD).

Purpose: these are the two places where an amendment to the ROADMAP's original wording becomes code.
**SC1 is AMENDED by D-02** — `ConformalPrediction.jl` is not added; conformal is met in substance,
hand-rolled, because the named library breaks a running byte-frozen-environment guard and wraps MLJ
models rather than a NeuralEstimators posterior. **SC2 is AMENDED by D-05** — the literal
`OOD ∨ disagreement ∨ ambiguous` OR would silence the tool exactly where Phase 9 says it should
speak, so cross-method disagreement ALONE decides and is recorded as a named field. Both amendments
must be cited in code, in the artifacts and in the report; the original wording is never planned
against.

Output: `spike/p14/conformal.jl`, `spike/p14/fuse.jl`, and their two test files.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
</context>

<interfaces>
<!-- Extracted from disk. Use directly. -->

`src/results.jl:93-97`: `struct OODVerdict; score::Float64; flag::Bool; per_channel::NamedTuple; end`

`src/amortized/ood.jl:394-396` — where `flag == false` becomes two-valued:
`thr = haskey(ood_nulls, :thr) ? ood_nulls.thr : nothing` ;
`flag = thr === nothing ? false : fused > thr` — so when `:thr` is absent, `ood_verdict` returns
`flag = false` **for every input, unconditionally**.

`src/amortized/local_map.jl:107` — the acceptance predicate to mirror EXACTLY:
`(t isa Real && isfinite(t)) ? float(t) : nothing`, and `:70-72`: *"a `false` flag on a non-sentinel
tile means NOT CHECKED, not in distribution."*

`spike/simulator/ghat.jl:110` — `ghat(μ::Real) -> Float64`, frozen monotone clamped piecewise-linear
μ → ρ_true; clamp bounds `GHAT_MU_MIN = -0.67976`, `GHAT_MU_MAX = 0.847149`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spike/p14/conformal.jl + test_p14_conformal.jl — hand-rolled split conformal (LAC)</name>

  <files>spike/p14/conformal.jl, spike/test/test_p14_conformal.jl</files>

  <behavior>
    - At `n = 9`, `alpha = 0.10`: `k = ceil(Int, 10*0.9) = 9`, so `p14_conformal_quantile(s, 0.10)
      == maximum(s)`, and for a non-degenerate `s` it is `!=` `Statistics.quantile(s, 0.9)`.
    - At `n = 30`, `alpha = 0.03`: `ceil(31*0.97) = 31 > 30`, so the call THROWS `ArgumentError`.
    - `p14_conformal_set` with a posterior whose max is below `1 - q̂` returns `status = :empty`,
      never `:ambiguous`.
    - `p14_conformal_set` returning two classes gives `status = :ambiguous`.
    - `p14_lac_score(p, :coloc) == 1 - p.coloc`, read by name.
  </behavior>

  <read_first>
    - `14-RESEARCH.md` §B in full (B.1 construction, B.2 the LAC recommendation and the load-bearing
      rejection of the BF-margin score, B.3 the three set-size regimes, B.4 sample-size arithmetic,
      B.5 feasibility), §Code Examples "Hand-rolled split conformal (LAC)", Pitfall 6, Pitfall 9.
    - `spike/validation/ood.jl:309-349` — `roc_auc` (the house numeric-helper shape) and
      `id_threshold` (a pre-registered operating point, NOT Youden-J).
    - `spike/simulator/ghat.jl:93-121` — the frozen clamped lookup whose docstring names its clamping
      behaviour explicitly. `p14_conformal_quantile` is the same shape except it THROWS rather than
      clamps.
    - `14-PATTERNS.md` §3.6, §5, §4.4.
    - `14-VALIDATION.md` row SC1-e.
  </read_first>

  <action>
**`spike/p14/conformal.jl`** — AGPL header, guarded includes of `consts.jl` and `posterior.jl`.

- **`p14_lac_score(p::NamedTuple, y::Symbol) -> Float64`** = `1.0 - getproperty(p, y)`. Validate `y in
  P14_CLASS_KEYS` and throw naming the function otherwise. Docstring: LAC / THR nonconformity score,
  higher = worse fit.

- **`p14_conformal_quantile(scores::AbstractVector{<:Real}, alpha::Real) -> Float64`.**
  `n = length(scores)`, `k = ceil(Int, (n + 1) * (1 - alpha))`, and `k <= n` or throw an
  `ArgumentError` naming the function, the infeasible α, the n, and the feasibility bound
  `alpha >= 1/(n+1)` computed from the ARGUMENT `n` — never a hard-coded `31` or `0.032`, which
  D-03a withdrew and `test_p14_decoupling.jl` bans as a literal. Return
  `sort(collect(Float64, scores))[k]`.
  Docstring, load-bearing: **DO NOT USE `Statistics.quantile`.** Its default type-7 linear
  interpolation between order statistics returns a strictly smaller value and silently breaks the
  `≥ 1 − α` finite-sample coverage bound by O(1/n) — it fails silently, invisibly at n = 2000 without
  a directed test. The conformal guarantee is stated for the ORDER STATISTIC.

- **`p14_conformal_set(p::NamedTuple, qhat::Real) -> NamedTuple`.**
  `set = Tuple(y for y in P14_CLASS_KEYS if p14_lac_score(p, y) <= qhat)`;
  `status = length(set) == 1 ? :singleton : (isempty(set) ? :empty : :ambiguous)`.
  Return `(set = set, status = status, qhat = Float64(qhat))`.
  Docstring must keep the three regimes separate and say why the EMPTY case has its own symbol: an
  empty set means the observed point is more nonconforming than `(1−α)` of everything the calibration
  set contained — a **distribution-free misspecification signal arriving through a completely
  different channel from the Mahalanobis/PP/noise OOD detector**, and the one signal in this phase
  that does not depend on the simulator being right about densities, only about exchangeability.
  Collapsing it into `:ambiguous` would discard that. Note the arithmetic: with 3 classes an empty
  set requires `q̂ < 2/3`, which is reachable for this net.

- **`p14_hedge_diagnostics(sets, qhat) -> NamedTuple`** returning
  `(qhat, n, singleton_rate, ambiguous_rate, empty_rate, vacuous_hedge = ambiguous_rate + empty_rate
  < P14_AMBIGUOUS_RATE_FLOOR)`. Docstring must state that `vacuous_hedge` is a **LABEL, not a
  verdict** — the same design as `vacuous_pass` for ECE at `spike/p13/result.jl:284-298`, because a
  hedge that never fires looks like a pass but measures nothing (Pitfall 9).

- **`p14_conformal_calibrate(posteriors, true_classes, alpha) -> NamedTuple`** — map
  `p14_lac_score(p_i, y_i)` over the calibration pairs, call `p14_conformal_quantile`, and return
  `(qhat, n_cal, alpha_conformal = alpha, scores_summary = (min, median, max))`. Add a docstring
  paragraph naming **D-02**: `ConformalPrediction.jl` is NOT a dependency; SC1's named mechanism is
  AMENDED and met in substance; the whole construction is `sort` + `ceil` + integer indexing.

**`spike/test/test_p14_conformal.jl`** — fixtures once, one outer `@testset`.
- **"the order statistic, not the interpolated quantile (Pitfall 6)"** — `s` = 9 distinct values from
  `p14_fix_rng(P14_FIXTURE_COUNTER)`; assert `p14_conformal_quantile(s, 0.10) == maximum(s)` AND
  `p14_conformal_quantile(s, 0.10) != Statistics.quantile(s, 0.9)`. Both `@test`s are required —
  either equality flipping is the failure.
- **"infeasible α throws"** — at `n = 30`, `alpha = 0.03` throws `ArgumentError`; at
  `alpha = 1/31 + 1e-9` it does not. Assert the error message names `1/(n+1)`.
- **"the three set-size regimes"** — three hand-built posteriors giving `:singleton`, `:ambiguous`
  and `:empty`, with the `:empty` case constructed so `max(p) < 1 - q̂`; assert `status` for each and
  assert `:empty` is NOT reported as `:ambiguous`.
- **"scores are read by name"** — `p14_lac_score` on a shuffled-declaration-order posterior gives the
  same answer.
- **"the vacuity label is a label"** — a set collection with `ambiguous_rate = 0` yields
  `vacuous_hedge == true` and no error is thrown.

Close with the CPU-only testset.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_conformal.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_conformal.jl` exits 0, zero failures, under 10 s.
    - `grep -v '^\s*#' spike/p14/conformal.jl | grep -c 'Statistics.quantile\|quantile('` returns 0 —
      the interpolated quantile is not used anywhere in the implementation.
    - `grep -v '^\s*#' spike/p14/conformal.jl | grep -cE '0\.032|1/31'` returns 0 (the withdrawn
      D-03a figure never becomes an executable default).
    - `spike/test/test_p14_conformal.jl` contains the literal `Statistics.quantile` inside a `!=`
      assertion.
    - `julia --project=spike spike/test/test_p14_decoupling.jl` still exits 0.
    - `git diff --exit-code HEAD -- spike/Project.toml spike/Manifest.toml` exits 0 (D-02).
  </acceptance_criteria>

  <done>
Split conformal exists, hand-rolled, zero new dependencies, with the order statistic asserted against
the interpolated quantile and the empty set kept as its own signal.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spike/p14/fuse.jl + test_p14_fuse.jl — D-05 asymmetric fusion and D-06's three-valued OOD</name>

  <files>spike/p14/fuse.jl, spike/test/test_p14_fuse.jl</files>

  <behavior>
    - `p14_ood_state(ood_nulls, verdict)` returns `:not_checked` when `:thr` is absent, when it is
      `Inf`, and when it is `NaN`; `:fired` / `:clear` only when `:thr` is a finite `Real`.
    - `p14_fuse(ood_state = :not_checked, conformal_status = :singleton, disagree = false)` returns
      `(:abstain, :ood_not_checked)`; with `allow_unchecked_ood = true` it returns `(:decide, …)`.
    - `p14_fuse(ood_state = :clear, conformal_status = :singleton, disagree = true)` returns
      `(:decide, :decided_contra_classical)` — disagreement ALONE decides (D-05, SC2 amended).
    - `p14_fuse(ood_state = :fired, …)` returns `(:abstain, :ood_fired)` for every other argument.
    - `p14_fuse(ood_state = :not_checked, disagree = true, allow_unchecked_ood = true)` returns
      `(:abstain, :disagreement_and_ood)`.
    - An `ood_state` outside the three symbols throws `ArgumentError` naming D-06.
  </behavior>

  <read_first>
    - `src/amortized/ood.jl:363-397` — `ood_verdict` and its OR-fusion tail, especially `:394-396`
      where `flag = false` becomes two-valued.
    - `src/amortized/local_map.jl:68-72` (the NOT-CHECKED prose), `:89-91` (the docstring: *returns
      `nothing` — never a substitute value*), `:93-108` (`_recorded_ood_threshold`, including
      `e isa InterruptException && rethrow()` inside the `catch`), `:110-119` (the vacuous-flag
      hazard).
    - `spike/p13/labels.jl:121-131` — `three_way_label`: validate first, then one early-return branch
      per rule, with the counter-example that makes the rule load-bearing in the docstring.
    - `14-RESEARCH.md` §D.4 in full, §Code Examples "The D-05 asymmetric fusion".
    - `14-CONTEXT.md` D-05 and D-06 in full.
    - `14-VALIDATION.md` rows SC2-a, SC2-b, SC2-c.
    - `14-PATTERNS.md` §3.5 (a)-(e).
  </read_first>

  <action>
**`spike/p14/fuse.jl`** — AGPL header, guarded includes of `consts.jl` and, read-only, the shipped
`src/results.jl` for `OODVerdict` (`isdefined(@__MODULE__, :OODVerdict) ||
include(joinpath(@__DIR__, "..", "..", "src", "results.jl"))` — TWO levels up, not three).

- **`p14_ood_state(ood_nulls, verdict::OODVerdict) -> Symbol`** returning `:fired`, `:clear` or
  `:not_checked` — **NEVER a `Bool`**. `haskey(ood_nulls, :thr) || return :not_checked`;
  `t = ood_nulls.thr`; `(t isa Real && isfinite(t)) || return :not_checked`;
  `return verdict.flag ? :fired : :clear`. The acceptance predicate must be byte-for-byte the same
  test as `src/amortized/local_map.jl:107` — a Phase-14 resolver that accepted `Inf` would silently
  disagree with the shipped semantics. Docstring must quote the mechanic: when `:thr` is absent,
  `ood_verdict` returns `flag = false` for EVERY input unconditionally, so `flag == false` is
  genuinely two-valued at the source, and a `false` flag that means NOT CHECKED must never read as
  "in distribution" (D-06). **This function is the ONLY place in the Phase-14 sources permitted to
  inspect `verdict.flag`** — `test_p14_decoupling.jl`'s SC2-c grep exempts exactly this body.

- **`p14_fuse(; ood_state::Symbol, conformal_status::Symbol, disagree::Bool,
  allow_unchecked_ood::Bool = false) -> NamedTuple`** returning `(action, reason)` with `action in
  (:decide, :abstain)`. Validate `ood_state in (:fired, :clear, :not_checked)` and
  `conformal_status in (:singleton, :ambiguous, :empty)`, throwing `ArgumentError` naming D-06 / the
  function and the observed value. Then, in this order, one early return per branch, **each citing
  its decision in a trailing comment**:
  1. `:fired` → `(:abstain, :ood_fired)` (D-05)
  2. `:not_checked` and not `allow_unchecked_ood` → `(:abstain, :ood_not_checked)` (D-06)
  3. `:ambiguous` → `(:abstain, :conformal_ambiguous)` (D-05)
  4. `:empty` → `(:abstain, :conformal_empty)` (D-05; a distinct channel, deliberately not folded
     into `:ambiguous`)
  5. `disagree && ood_state !== :clear` → `(:abstain, :disagreement_and_ood)` (D-05; reachable only
     under the explicit opt-out)
  6. otherwise → `(:decide, disagree ? :decided_contra_classical : :decided)`

  Docstring must state D-05's asymmetry explicitly and say that **SC2's literal `∨` is REPLACED by
  this rule**, with the reason: the literal OR silences the tool exactly where Phase 9 says it should
  speak — disagreeing with Costes/Manders is the product thesis, not a failure mode — and three OR'd
  triggers compound into a tool that is usually silent. Record the considered-and-not-chosen
  alternative (a four-state output adding `DECIDED-CONTRA-CLASSICAL`) so a later reader knows it was
  weighed (`14-PATTERNS.md` §4.6: comments record what was REJECTED and why). State that the order of
  branches is load-bearing and that every ABSTAIN carries which trigger fired, because a silent
  abstention is nearly as bad as a wrong call.

- **`p14_abstain_reasons() -> NTuple{5,Symbol}`** = `(:ood_fired, :ood_not_checked,
  :conformal_ambiguous, :conformal_empty, :disagreement_and_ood)` — the closed set the result type
  and the report enumerate, so a new reason cannot appear untracked.

**`spike/test/test_p14_fuse.jl`** — fixtures once, one outer `@testset ... verbose = true`.
- **SC2-a — "the full truth table, cell by cell"** — loop the complete product
  `ood_state ∈ (:fired, :clear, :not_checked)` × `conformal_status ∈ (:singleton, :ambiguous,
  :empty)` × `disagree ∈ (false, true)` × `allow_unchecked_ood ∈ (false, true)` = **36 cells**. For
  each cell compute the expected `(action, reason)` from an INDEPENDENT expectation table written out
  literally in the test file (not by calling `p14_fuse`), and `@test` the pair, with the cell values
  in the test name so a failure names the cell. Assert explicitly that the cell
  `(:clear, :singleton, disagree = true, allow_unchecked_ood = false)` gives
  `(:decide, :decided_contra_classical)` — this single cell is the whole of D-05's amendment.
- **SC2-b — "not-checked abstains by default (D-06)"** — build `ood_nulls` fixtures with `:thr`
  absent, `Inf`, `NaN`, and a finite value; assert the first three give `:not_checked` and the fourth
  gives `:fired`/`:clear` per `verdict.flag`; assert each `:not_checked` routes to `(:abstain,
  :ood_not_checked)` at the default and flips to `:decide` under the opt-out.
- **"a Bool never leaks"** — `@test p14_ood_state(...) isa Symbol` for every fixture, and
  `@test !(p14_ood_state(...) isa Bool)`.
- **"validation"** — `ood_state = :maybe` and `conformal_status = :huge` each throw `ArgumentError`.
- **"the reason set is closed"** — every reason returned across all 36 cells is in
  `p14_abstain_reasons()` or is `:decided` / `:decided_contra_classical`.

Close with the CPU-only testset.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_fuse.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_fuse.jl` exits 0 with zero failures and at least 36
      truth-table assertions, in under 10 seconds.
    - `spike/test/test_p14_fuse.jl` contains the literal `decided_contra_classical` in an assertion
      on the `(:clear, :singleton, true, false)` cell.
    - `grep -v '^\s*#' spike/p14/fuse.jl | grep -c '\.flag'` returns exactly the count inside
      `p14_ood_state` (1) — no other function inspects the flag (SC2-c).
    - `grep -c 'not_checked' spike/p14/fuse.jl` ≥ 3.
    - Changing branch 5's condition to `disagree` alone (a scratch edit implementing SC2's literal
      OR) makes the test file exit 1; revert the scratch edit.
    - `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0 — in particular the SC2-c
      grep now finds `fuse.jl` and passes with the positional exemption.
  </acceptance_criteria>

  <done>
The abstention rule implements D-05's asymmetry exactly, D-06's not-checked default abstains with an
explicit opt-out, every ABSTAIN carries its trigger, and 36 cells are asserted individually.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped `OODVerdict.flag` → Phase-14 decision | `flag = false` is two-valued at the source; reading it as "in distribution" is a silent scientific failure |
| calibration scores → the conformal quantile | An interpolated quantile breaks the coverage bound invisibly |
| ROADMAP SC1/SC2 wording → implementation | Planning against the original wording would either add a banned dependency or gut Phase 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Spoofing | `verdict.flag == false` as an in-distribution proxy | mitigate | `p14_ood_state` is three-valued and is the ONLY function permitted to touch `.flag`; SC2-c grep enforces it; `:not_checked` abstains by default with an explicit caller opt-out (D-06) |
| T-14-09 | Tampering | `Statistics.quantile` substituted for the order statistic | mitigate | Implementation uses `sort(...)[ceil(Int,(n+1)(1-α))]`; grep asserts `quantile(` absent from the source; the n=9 test asserts `== maximum(s)` AND `!= Statistics.quantile(s, 0.9)` (Pitfall 6) |
| T-14-05 | Tampering | adding `ConformalPrediction.jl` | mitigate | D-02; the construction needs only Base; `test_p14_decoupling.jl` asserts the sixteen-dep set and `!haskey(..., "ConformalPrediction")`; acceptance criterion re-checks the byte-frozen manifest |
| T-14-21 | Tampering | SC2's literal OR reinstated, silencing the Phase-9 thesis | mitigate | 36-cell truth table with an INDEPENDENT expectation table; the `(:clear, :singleton, true, false) ⇒ (:decide, :decided_contra_classical)` cell asserted by name; scratch-edit acceptance check proves the test fires |
| T-14-22 | Tampering | the empty conformal set folded into `:ambiguous` | mitigate | Separate `:conformal_empty` reason symbol; a dedicated fixture asserts `:empty` is not reported as `:ambiguous`; `p14_abstain_reasons()` is a closed set |
| T-14-23 | Repudiation | a vacuous hedge reported as a pass | mitigate | `p14_hedge_diagnostics` emits `vacuous_hedge` as a LABEL beside the coverage number (Pitfall 9, the `vacuous_pass` precedent) |
| T-14-SC | Tampering | package installs | mitigate | Zero installs; the hedge is `sort` + `ceil` + integer indexing (14-RESEARCH §B.5) |
</threat_model>

<verification>
- `julia --project=spike spike/test/test_p14_conformal.jl` exits 0.
- `julia --project=spike spike/test/test_p14_fuse.jl` exits 0.
- `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0 (SC2-c now armed).
- `git diff --exit-code HEAD -- src spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- Both ROADMAP amendments are implemented and cited in the source: SC1 amended by D-02 in
  `conformal.jl`, SC2 amended by D-05 in `fuse.jl`.
- Reverting either amendment in a scratch edit fails a test, proving the tests bind the amendment
  rather than merely describing it.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-05-SUMMARY.md` when done. State plainly
that SC1 and SC2 are amended and that any report citing a Phase-14 result must cite the original
ROADMAP wording alongside it.
</output>

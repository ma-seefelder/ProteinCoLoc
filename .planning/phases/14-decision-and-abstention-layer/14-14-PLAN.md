---
phase: 14-decision-and-abstention-layer
plan: 14
type: execute
wave: 7
depends_on: ["14-10", "14-11", "14-12", "14-13"]
files_modified:
  - .planning/phases/14-decision-and-abstention-layer/14-REPORT.md
autonomous: false          # ends in a blocking human-verify checkpoint on the honesty items
requirements: []           # ROADMAP: `Requirements: TBD`. Traced to every D-NN and every SC row.
decisions: [D-01, D-02, D-03, D-03a, D-04, D-05, D-06, D-07]
validates: ["SC1-a", "SC1-b", "SC1-c", "SC1-d", "SC1-e", "SC1-f", "SC2-a", "SC2-b", "SC2-c", "SC2-d", "SC3-a", "SC3-b", "SC3-c", "SC3-d", "D-07", "D-01/env", "Seeds", "Class order", "Prior re-derivation"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "Every SC row in 14-VALIDATION.md has a recorded verdict, a measured number and the artifact key it came from"
    - "SC1's and SC2's amendments (D-02, D-05) are cited alongside the ORIGINAL ROADMAP wording, never in place of it"
    - "Assumption A2 is stated as an explicit derivation a referee can check, not asserted"
    - "Every FDR number in the report appears with its decided fraction"
    - "The six-TIFF check is described as an illustration and the D-03 corpus bound as ABSENT, not loose"
    - "The four SC3 bars and the coverage floor are labelled judgement calls with no derivation, with the freeze-commit sha"
    - "The decision layer is stated as NOT shipped in v2.0 (D-01)"
  artifacts:
    - path: ".planning/phases/14-decision-and-abstention-layer/14-REPORT.md"
      provides: "the phase report and the manuscript-bound honesty items"
      contains: "DECIDED SUBSET ONLY"
      min_lines: 220
  key_links:
    - from: "14-REPORT.md"
      to: "spike/p14/*.jld2"
      via: "every reported number cites the artifact key it was read from"
      pattern: "p14_.*_report\\.jld2"
    - from: "14-REPORT.md"
      to: "p14_named_limits()"
      via: "the limit list is enumerated from code, not from memory"
      pattern: "p14_named_limits"
---

<objective>
Write the Phase-14 report: every SC row with its measured number, its verdict and the artifact key it
came from; every amendment cited beside the original wording; every named limit carried forward to
the manuscript.

Purpose: this phase produces numbers that are easy to over-read. An FDR level without its decided
fraction, a conformal guarantee without its simulator provenance, a six-image illustration read as a
coverage claim, or a judgement-call bar read as a derived threshold would each be a defensible-looking
sentence that is not true. The report is where those are prevented, and it ends in a blocking human
check because two of the items are the user's to ratify, not an executor's.

Output: `.planning/phases/14-decision-and-abstention-layer/14-REPORT.md`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the report from the artifacts, never from memory</name>

  <files>.planning/phases/14-decision-and-abstention-layer/14-REPORT.md</files>

  <read_first>
    - Every Phase-14 artifact, loaded with `JLD2.load`: `spike/p14/p14_conformal_report.jld2`,
      `p14_fdr_report.jld2`, `p14_riskcoverage_report.jld2`, `p14_ood_arm_report.jld2`,
      `p14_real_images_report.jld2`, `p14_eval_pool.jld2`.
    - Every `14-*-SUMMARY.md` produced by plans 14-01 through 14-13.
    - `.planning/phases/13-three-hypothesis-amortized-bayes-factor/13-REPORT.md` — the house report
      shape, including its §11 forward-pointing items list and its limits section.
    - `.planning/ROADMAP.md` Phase 14 entry — **SC1, SC2 and SC3 as ORIGINALLY WRITTEN.** Both SC1
      and SC2 are amended here and the amendments must be cited alongside the originals, never in
      place of them.
    - `14-VALIDATION.md` — the authoritative SC → validation map; every row needs a verdict.
  </read_first>

  <action>
Create `.planning/phases/14-decision-and-abstention-layer/14-REPORT.md`. **Read every number out of
the `.jld2` artifacts programmatically and paste the loaded values — do not retype a number from a
summary or from memory.** State at the top of the report which artifact each section was read from
and each artifact's sha.

Sections:

**§1 What this phase built, and what it is not.** `decide_coloc` emits `{coloc / not / ABSTAIN}` at a
user-set Bayesian FDR. State D-01 plainly and early: it is built in `spike/` with a `src/`-shaped
signature so later promotion is mechanical, and **"src-shaped" is not "in src" — the decision layer
is NOT shipped in v2.0.** Record that `src/`, `spike/Project.toml`, `spike/Manifest.toml`, `corpus/`
and `artifacts/` are byte-unchanged, with the guard file that proves it.

**§2 The two ROADMAP amendments, quoted beside the originals.**
- **SC1 as written** names `ConformalPrediction.jl`. **SC1 as amended by D-02:** conformal sets met
  *in substance*, hand-rolled; the library name is dropped. Give the reason — the named library
  breaks the running byte-frozen-environment guard and wraps MLJ models rather than a
  NeuralEstimators posterior, and split conformal is a sorted-array order statistic.
- **SC2 as written** is `OOD ∨ cross-method disagreement ∨ ambiguous conformal set`. **SC2 as amended
  by D-05:** the asymmetric rule, with cross-method disagreement ALONE deciding. Give the reason —
  the literal OR silences the tool exactly where Phase 9 says it should speak, and three OR'd
  triggers compound into a tool that is usually silent. Record the considered-and-rejected four-state
  `DECIDED-CONTRA-CLASSICAL` output as the more transparent design if reviewers push back on the
  asymmetry.
- State that any citation of a Phase-14 result must carry both amendments.

**§3 The SC table.** One row per `14-VALIDATION.md` SC row: ID, the behaviour, the pre-registered
number, the MEASURED value, the verdict (PASS / FAIL / REPORTED-NOT-GATED), the artifact key, and the
command that reproduces it. Include SC1-a..f, SC2-a..d, SC3-a..d, D-07, D-01/env, Seeds, Class order,
Prior re-derivation. **A row with no verdict is not permitted** — if something did not run, say so
and why.

**§4 FDR, stated at the scope it actually holds.** Reproduce the four-row α table WITH its
`decided_fraction` column. Then the honest claim verbatim: *the posterior expected false discovery
proportion is controlled at α_FDR over the DECIDED subset only — the batch minus abstentions. It is
not controlled over the whole batch: an abstained item is neither a discovery nor a non-discovery and
appears in neither the numerator nor the denominator. The guarantee is conditional on the model
(calibrated posteriors and a correct class prior) and is a posterior expectation, not a frequentist
long-run rate.* Then **Assumption A2, written out as a derivation** — `R` is a deterministic function
of the observed data, hence σ(data)-measurable, hence pulls out of the conditional expectation, so
`E[FDP|data] = (1/|R|)·Σ_{i∈R} v_i` for ANY data-dependent selection, unlike the frequentist case
where pre-selection is a genuine selective-inference problem. **State this as two checkable lines, not
as an assertion**, and flag it as the load-bearing assumption of the phase. Then the ordering rule and
why the reverse order is wrong. Then the π-sensitivity table (SC1-c), labelled REPORTED NOT GATED, and
the prior-atom asymmetry note.

**§5 The conformal hedge.** `q̂`, realized coverage against the 0.880 band, the set-size distribution,
the vacuity label, and the two class-mass tables. State that the guarantee is **simulator-derived**:
calibration and evaluation draws come from the same simulator the net was trained on, train-joint
equals eval-joint by construction, so every SC1/SC3 number is a **well-specified-regime** number
inheriting the simulator's misspecification in full — a statement about the decision layer's internal
coherence, not about performance on real microscopy. State that **the bound D-03 intended is ABSENT,
not merely loose.**

**§6 Risk-coverage and abstention concentration.** `skill`, `spearman` on `[0.20, 1.00]`, `AUC_hard`,
the SC3-d margin per family, the figure. **Label all four bars and the coverage floor as JUDGEMENT
CALLS WITH NO DERIVATION**, give the 14-01 freeze-commit sha as the evidence they preceded every
result, and state that they were never relaxed. Record the licensing standard from `.planning/STATE.md`:
this project has now recorded four cases of a bar measuring something other than what it named, and
the standard any amendment is held to is evidence, independent of the machinery under audit, that the
bar measures something other than what it names. Show the RAW curve over the full coverage range,
including the noisy part the statistic excludes. For SC3-d, state whether the margin was driven by
`ood_fired` or by `ood_not_checked`, and report the empty-set × `ood_state` cross-tab.

**§7 The real-data check (D-03a).** The six TIFFs, their decisions and abstention reasons, whether the
ABSTAIN prediction held. Then the corpus record: `corpus_images_available = 0`, **unfetched by
design**, the computed tier breakdown (simulated-secondary vs physical-primary) and split breakdown,
and the fact that the only physical-primary rows are Phase 16's sealed holdout and were not read.
State the D-03a substitution and its rationale — *precisely so that Phase 16's blind corpus stays
blind* — citing `spike/p13/real_images.jl:71,145`. State that **`α ≥ 1/31 ≈ 0.032` is WITHDRAWN**: it
was computed from the wrong n and on rows that are not physical truth. State that six TIFFs in two
conditions with no colocalization labels is **an illustration, not a coverage claim**, and bounds
nothing tightly.

**§8 Named limits, carried into the manuscript.** Enumerate `p14_named_limits()` from code rather than
from memory, and add: the underlying net is an **epoch-4 checkpoint of an early-overfitting run**
(13-REPORT limit C — best validation risk 0.13625 at epoch 4 rising to 0.38632, ~10× train/validation
gap, recipe deliberately not tuned) and every Phase-14 number inherits it; only the **density** OOD
channel is wired in this lane (the noise and posterior-predictive channels are not); the `tau_probe`
ran on a **dirty tree** (`repo_dirty_at_run == true`), surfaced rather than asserted away; and
per-region FDR is **out of scope** because Phase 12 returned NO on calibrated per-region uncertainty
and `LocalColocMap` has no uncertainty field, so per-tile output is **uncontrolled display**.

**§9 Forward-pointing items.** Anything this phase found that Phase 15 or 16 must handle, in the
13-REPORT §11 style — numbered, each naming the evidence.

**§10 Reproduction.** The exact per-file commands, and the standing warning that
`spike/test/runtests.jl` exits 1 at the Phase-4 `SPEEDUP_GATE` masking every later include block, so
**per-file runs are the only reliable signal** and the aggregate suite must not be used as a gate.
Record the per-file pass counts, as Phase 13 did.
  </action>

  <verify>
    <automated>julia --project=spike -e 'r = read(".planning/phases/14-decision-and-abstention-layer/14-REPORT.md", String); for s in ("DECIDED SUBSET ONLY", "src-shaped", "NOT shipped in v2.0", "JUDGEMENT CALL", "illustration, not a coverage claim", "absent, not merely loose", "SC1 is AMENDED", "SC2 is AMENDED", "Assumption A2", "unfetched by design", "epoch-4"); @assert occursin(s, r) s; end; @assert !occursin("0.032", r); @assert !occursin("1/31", r); println("report honesty tokens ok")'</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 — every required honesty token is present and the withdrawn figure is
      absent.
    - Every SC row from `14-VALIDATION.md` appears in §3 with a non-empty verdict cell; no row reads
      `TBD` or is missing.
    - Every FDR number in the report text is adjacent to a decided fraction — verify by hand-scanning
      §4 and confirming the α table's `decided_fraction` column is present and populated for all four
      rows.
    - Every reported number cites the `.jld2` key it came from, and the report records each artifact's
      sha.
    - `git diff --exit-code HEAD -- src spike/Project.toml spike/Manifest.toml corpus test` exits 0.
    - Every `test_p14_*.jl` file exits 0 when run individually; `spike/test/runtests.jl` is NOT used.
  </acceptance_criteria>

  <done>
The report exists, every number traces to an artifact key, and every claim carries the qualifier that
makes it true.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human ratification of the honesty items</name>

  <read_first>
    - `.planning/phases/14-decision-and-abstention-layer/14-REPORT.md` — the artifact under review,
      §§4, 6 and 7 in particular.
    - `14-RESEARCH.md` Assumptions Log A2 (**High risk if wrong**) and A3 (the five judgement-call
      bars), and §E.2 in full.
    - `.planning/ROADMAP.md` Phase 14 entry — SC1, SC2 and SC3 as ORIGINALLY written, to check the
      amendments are cited beside them rather than in place of them.
    - `14-VALIDATION.md` "Manual-Only Verifications" — all three rows are what this checkpoint
      closes.
  </read_first>

  <what-built>
`.planning/phases/14-decision-and-abstention-layer/14-REPORT.md`, assembled programmatically from the
five Phase-14 `.jld2` artifacts, with:
- the SC table (every row with a measured value and a verdict),
- the SC1/SC2 amendments quoted beside the original ROADMAP wording,
- the FDR claim scoped to the decided subset, with the decided fraction on every row,
- Assumption A2 written out as a two-line derivation,
- the four SC3 bars labelled judgement calls with the freeze-commit sha,
- the six-TIFF result labelled an illustration and the corpus recorded as unfetched by design.

Everything above is automated and already done. This checkpoint is for the three things that are the
user's to ratify, not an executor's.
  </what-built>

  <how-to-verify>
1. Open `.planning/phases/14-decision-and-abstention-layer/14-REPORT.md`.

2. **Read §4's Assumption A2 derivation.** 14-RESEARCH flags it as **High risk if wrong** — it is the
   entire justification for abstain-then-sort and for the "controlled over the decided subset" claim.
   It should read as two checkable lines: the rejection set is a deterministic function of the
   observed data, therefore σ(data)-measurable, therefore it pulls out of the conditional expectation.
   **Question for you: does that derivation convince you as written, or does it need a referee-facing
   rewrite before it goes near the manuscript?**

3. **Read §6's four bars** — `skill ≥ 0.60`, `spearman ≥ 0.95`, `AUC_hard ≥ 0.80`, `ood margin ≥ 0.50`,
   and the `0.20` coverage floor — together with their measured values and the freeze-commit sha.
   These were frozen before any result existed and were NOT relaxed. **Question for you: are you
   content that they are labelled judgement calls with no derivation, given this project has recorded
   four cases of an underived bar measuring the wrong thing?** They cannot be changed now — a bar
   changed after a result is a pre-registration breach — but how they are *presented* in the
   manuscript is yours to rule.

4. **Read §7.** Confirm the six-TIFF result reads as an illustration and never as a rate, that the
   corpus reads as *unfetched by design* and never as gone or empty, and that `α ≥ 1/31 ≈ 0.032`
   appears nowhere.

5. Run, and confirm all exit 0:
   - `julia --project=spike spike/test/test_p14_consts.jl`
   - `julia --project=spike spike/test/test_p14_decoupling.jl`
   - `julia --project=spike spike/test/test_p14_provenance.jl`
   - `julia --project=spike spike/test/test_p14_posterior.jl`
   - `julia --project=spike spike/test/test_p14_fdr.jl`
   - `julia --project=spike spike/test/test_p14_conformal.jl`
   - `julia --project=spike spike/test/test_p14_fuse.jl`
   - `julia --project=spike spike/test/test_p14_result.jl`
   - `julia --project=spike spike/test/test_p14_decide.jl`
   - `julia --project=spike spike/test/test_p14_pools.jl`
   Do **not** run `spike/test/runtests.jl` — it exits 1 at the Phase-4 `SPEEDUP_GATE` and masks every
   later include block.

6. Confirm `git status --porcelain -- src spike/Project.toml spike/Manifest.toml corpus test` is
   empty.
  </how-to-verify>

  <acceptance_criteria>
    - The user has explicitly answered the two questions in steps 2 and 3 (the A2 derivation's
      presentation, and the framing of the five judgement-call bars), and both answers are recorded
      verbatim in `14-14-SUMMARY.md`.
    - All ten `test_p14_*.jl` files exited 0 when run individually, and `spike/test/runtests.jl` was
      not used; the per-file pass counts are recorded.
    - `git status --porcelain -- src spike/Project.toml spike/Manifest.toml corpus test` is empty.
    - The report contains every honesty token asserted by Task 1's automated check, and contains
      neither `0.032` nor `1/31`.
    - If the user requests changes, they are applied and this checkpoint is re-run; the phase does
      not close on an unresolved objection.
  </acceptance_criteria>

  <resume-signal>
Type "approved" to close the phase, or describe what must change. If any SC row FAILED, say how you
want it recorded — an honest shortfall is a Phase-14 finding, and the only pre-declared remedy in
this phase is `P14_ITERATION_ALLOWANCE`'s single trigger (conformal coverage below the SC1-d band ⇒
switch LAC → APS and re-run ONCE). No bar may be relaxed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact → report prose | A retyped number, or a qualifier dropped in transcription, is invisible in the report and total in the claim |
| amended SC → cited SC | Citing the amendment without the original hides that the criterion moved |
| a phase report → the manuscript | Every limit not carried here is a limit a referee assembles instead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-47 | Tampering | a number retyped rather than read from its artifact | mitigate | Every number loaded programmatically via `JLD2.load` and pasted; every reported value cites its artifact key; artifact shas recorded in the report |
| T-14-48 | Repudiation | an amendment cited without the original wording | mitigate | §2 quotes SC1 and SC2 as originally written beside their amended forms; the automated check greps for both `SC1 is AMENDED` and `SC2 is AMENDED`; the `amendment` key is already embedded in every artifact |
| T-14-06 | Repudiation | an FDR level quoted without its decided fraction | mitigate | The α table carries a `decided_fraction` column; the token `DECIDED SUBSET ONLY` is grep-asserted present |
| T-14-07 | Repudiation | the six-TIFF illustration read as a coverage claim | mitigate | The phrase `illustration, not a coverage claim` is grep-asserted present; `0.032` and `1/31` are grep-asserted ABSENT; human check step 4 |
| T-14-02 | Tampering | a judgement-call bar presented as a derived threshold | mitigate | §6 labels all five as judgement calls with the freeze-commit sha; `JUDGEMENT CALL` grep-asserted; human check step 3 ratifies the presentation but cannot change the values |
| T-14-49 | Repudiation | Assumption A2 asserted rather than derived | mitigate | §4 writes it out as two checkable lines; a blocking human checkpoint asks explicitly whether it convinces as written (Assumptions Log A2: High risk if wrong) |
| T-14-50 | Repudiation | a named limit silently dropped | mitigate | §8 enumerates `p14_named_limits()` from CODE, so a limit cannot be forgotten; the four additional limits are listed explicitly |
| T-14-51 | Repudiation | `runtests.jl` used as a phase gate | mitigate | §10 records the standing warning and the per-file commands; the human check lists all ten files individually and forbids the aggregate suite |
| T-14-SC | Tampering | package installs | accept | Zero installs; the report is a document |
</threat_model>

<verification>
- The automated honesty-token check exits 0.
- All ten `test_p14_*.jl` files exit 0 when run individually.
- `git status --porcelain -- src spike/Project.toml spike/Manifest.toml corpus test` is empty.
- The human checkpoint is approved.
</verification>

<success_criteria>
- Every SC row has a verdict; every number has a provenance; every claim has its qualifier.
- The two items that are the user's to rule — the A2 derivation's presentation and the judgement-call
  bars' framing — have been put to the user explicitly rather than settled by an executor.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-14-SUMMARY.md` when done, and record the
user's ruling from the checkpoint verbatim.
</output>

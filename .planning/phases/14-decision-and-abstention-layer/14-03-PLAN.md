---
phase: 14-decision-and-abstention-layer
plan: 03
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - spike/test/test_p14_decoupling.jl
autonomous: true
requirements: []          # ROADMAP: `Requirements: TBD`. Traced to D-01, D-02, D-03a, D-06 and the
                          # 14-VALIDATION rows SC2-c, SC2-d, SC1-f, D-01/env.
decisions: [D-01, D-02, D-03a, D-06, D-07]
validates: ["D-01/env", "SC2-c", "SC2-d", "SC1-f"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "src/, spike/Project.toml, spike/Manifest.toml, corpus/ and artifacts/ are byte-unchanged, including the untracked case (D-01)"
    - "The dependency name set is exactly the frozen sixteen and ConformalPrediction/MLJ/ROCAnalysis/MultipleTesting are each absent BY NAME (D-02)"
    - "No Phase-14 source reads a byte of a sealed corpus image, and the permitted real-image path test/test_images/ IS exercised (D-03a)"
    - "The withdrawn figure 0.032 / 1/31 never appears as an executable default (D-03a)"
    - "No Phase-14 source uses verdict.flag == false as an in-distribution proxy (D-06 / SC2-c)"
    - "Every classical statistic compared to τ passes through ghat (SC2-d)"
    - "No Phase-14 source calls costes_p with the comparator MASTER_SEED, which is a forbidden seed"
  artifacts:
    - path: "spike/test/test_p14_decoupling.jl"
      provides: "the executable hard-constraint guard for Phase 14"
      contains: "_p14_sources"
      min_lines: 260
  key_links:
    - from: "spike/test/test_p14_decoupling.jl"
      to: "spike/p14/*.jl"
      via: "comment-stripped source scan that GROWS with the phase"
      pattern: "\\(\"spike/p14\", r\"\\^\\.\\*\\\\.jl\\$\"\\)"
    - from: "spike/test/test_p14_decoupling.jl"
      to: "git"
      via: "byte-equality assertions against HEAD"
      pattern: "git diff --quiet HEAD"
---

<objective>
Restate every hard constraint that binds Phase 14 as a RUNNING test, in Phase 14's own file — the
house rule being that *"each phase states its own claim in its own file"*
(`spike/test/test_p12_decoupling.jl:46-47`).

Purpose: this project encodes its constraints as executable guards, not prose. The guard must exist
**before** the code it guards, so that a violation fails on the commit that introduces it rather
than at review. That is why this plan is in Wave 1 and every module plan depends on it.

Output: `spike/test/test_p14_decoupling.jl`.

**Waves 0 and 1 together are `14-VALIDATION.md`'s "Wave 0".** No result-producing plan (waves 2+)
may run until `test_p14_consts.jl`, `test_p14_provenance.jl` and `test_p14_decoupling.jl` all exit 0.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The environment / src / corpus half of test_p14_decoupling.jl</name>

  <files>spike/test/test_p14_decoupling.jl</files>

  <read_first>
    - `spike/test/test_p12_decoupling.jl` IN FULL — this is an exact-match analog. Pay particular
      attention to `:34-45` (the self-scanning problem and the concatenated needles), `:64` (inside a
      linked worktree `.git` is a FILE), `:71-106` (the comment-stripper and the grow-with-the-phase
      source enumeration), `:109-118` (the allowlist as data), `:152-168` (src/ byte-unchanged
      INCLUDING the untracked case), `:170-183` (environment freeze), `:185-208` (exactly-sixteen +
      by-name bans), `:229-296` (the four sealed-holdout checks and the POSITIVE test_images
      assertion), `:299-319` (shipped bundle), `:321-349` (cache-root separation), `:351-353` (CPU).
    - `14-PATTERNS.md` §0 in full and §3.11 (a)-(f).
    - `spike/p13/real_images.jl:60-80, 140-150` — the D-15 substitution record and its executable
      forbidden-token guard, which is the D-03a precedent.
  </read_first>

  <action>
Create `spike/test/test_p14_decoupling.jl` by restating `test_p12_decoupling.jl`'s claims over the
Phase-14 surface. Do **not** edit `test_p12_decoupling.jl`.

Module-level scaffolding:
- AGPL header, `using Test`, `using Pkg`.
- `const P14_REPO_ROOT = normpath(joinpath(@__DIR__, "..", ".."))` and
  `const _HAS_GIT = Sys.which("git") !== nothing && (isdir(joinpath(P14_REPO_ROOT, ".git")) ||
   isfile(joinpath(P14_REPO_ROOT, ".git")))` — the `isfile` arm because inside a linked worktree
  `.git` is a file.
- `_strip_comment_lines(src)` copied from `:71-73`.
- `const _P14_SOURCE_PATTERNS = (("spike/p14", r"^.*\.jl$"), ("spike/test", r"^test_p14_.*\.jl$"))`
  and `_p14_sources()` copied from `:75-106`. Carry the comment that files not yet existing are
  simply absent from the enumeration, so the scan GROWS with the phase instead of asserting against
  a frozen list that goes stale.
- The three concatenated needles, so this file does not match its own scan:
  `const _SEALED_OPENER = "open_" * "sealed_holdout"`,
  `const _SEALED_IMAGE_DIR_LITERAL = "cor" * "pus/data"`,
  `const _SEALED_IMAGE_DIR_SYMBOL = "CORPUS_" * "DATA_DIR"`.
- `const _P14_CORPUS_METADATA_ALLOWED = ("spike/p14/run_p14_real_images.jl",
   "spike/test/test_p14_decoupling.jl")` — the ONLY two files permitted to mention corpus metadata,
   with a comment that adding an entry here is a DECISION, not a fix.
- `const _P14_CORPUS_TOKENS = ("corpus", "sealed_holdout", "manifest.csv", "CORPUS_MASTER_SEED")`
  and `const _IMAGE_LOADERS = ("load(", "FileIO.load", "build_mci")`.

Testsets for this task:

1. **"src/ is byte-unchanged (CLAUDE.md hard constraint, D-01)"** — `git diff --quiet HEAD -- src`,
   plus the same for `src/results.jl`, `src/amortized/ood.jl`, `src/amortized/local_map.jl`,
   `src/amortized/api.jl`, plus **the untracked case** `isempty(readchomp(git status --porcelain --
   src))`. Carry the comment verbatim in substance: a NEW file dropped under `src/` is invisible to a
   diff against HEAD and would pass every other assertion while being exactly the
   productionization-by-stealth this constraint forbids. Add a Phase-14-specific comment: **D-01 says
   the decision layer is built with a `src/`-shaped SIGNATURE; "src-shaped" is not "in src", and this
   testset is what makes that distinction executable.**

2. **"the spike environment is byte-frozen (D-02)"** — `git diff --quiet HEAD -- spike/Project.toml
   spike/Manifest.toml` and the `git status --porcelain` twin, degrading to `@info` without git.

3. **"the dependency name set is exactly the frozen sixteen (D-02)"** — the `Set(keys(Pkg.project()
   .dependencies)) == Set([...sixteen...])` assertion, the NeuralEstimators UUID/version pin
   `Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")` at `v"0.2.1"`, and then **one `@test` per
   banned package BY NAME** so the failure says which one appeared: `ConformalPrediction`, `MLJ`,
   `ROCAnalysis`, `MultipleTesting`, `Distances`, `CUDA`. Comment that `ConformalPrediction` is named
   in the ORIGINAL SC1 and that **SC1 is AMENDED by D-02** — the library name is dropped and
   conformal is met in substance, hand-rolled; cite `14-CONTEXT.md` D-02.

4. **"the sealed holdout is never consumed (D-03a)"** — restate the four checks in order:
   (a) no `_IMAGE_LOADERS` call on any line that also names a corpus path, in ANY Phase-14 source,
   allowlisted or not, collecting **offending lines not booleans** (`push!(hits, "$p: $ln")` then
   `@test hits == String[]`);
   (b) non-allowlisted sources may not mention any `_P14_CORPUS_TOKENS` at all, comment-stripped
   first — so prose is free and a code reference is not;
   (c) allowlisted files get the narrower property: metadata yes, `_SEALED_IMAGE_DIR_LITERAL` /
   `_SEALED_IMAGE_DIR_SYMBOL` / `_SEALED_OPENER` never;
   (d) `corpus/` is byte-unchanged and untracked-clean (`git diff --quiet HEAD -- corpus` +
   `git status --porcelain -- corpus`), AND the **positive** assertion that
   `test/test_images/positive/positive_c1.tif` … `negative_c3.tif` all exist and that at least one
   Phase-14 source names `test/test_images` — so the three negatives cannot be satisfied vacuously by
   a phase that reads no images at all. Use the arms-itself-later idiom for the positive half:
   `if isfile(joinpath(P14_REPO_ROOT, "spike", "p14", "run_p14_real_images.jl")) … else @info … end`.
   Include the allowlist self-consistency check from `:233-234`: every allowlist entry that exists on
   disk must be in `_p14_sources()`, or an exemption could silently protect nothing.

5. **"the shipped bundle and Phase-13 artifacts are read-only"** — `git status --porcelain --
   spike/p13` empty; assert no Phase-14 source writes under `artifacts/` (grep for
   `jldsave` / `mv(` on a line naming `artifacts`), and that Phase-14 caches use a NEW root:
   assert `normpath(.../spike/data/cache/p14") != normpath(.../spike/data/cache/p13")` and that no
   Phase-14 source contains the string `"cache", "p13"` on a WRITE path (`jldsave`, `mkpath`,
   `open(..., "w")`) — reading `h.meta.pool_dir` is permitted and required.

Close this task's half; Task 2 appends the Phase-14-specific greps into the same outer `@testset`.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_decoupling.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0 with zero failures.
    - The file asserts `Set(keys(Pkg.project().dependencies))` equals exactly the sixteen names
      `BenchmarkTools, CSV, CairoMakie, CoordinateTransformations, DataFrames, Distributions, Flux,
      HypothesisTests, ImageFiltering, ImageTransformations, Images, Interpolations, JLD2,
      NeuralEstimators, Random123, StatsBase`.
    - `grep -c 'ConformalPrediction' spike/test/test_p14_decoupling.jl` ≥ 1 and it appears inside a
      `!haskey(...)` assertion.
    - The three sealed needles are built by concatenation — `grep -c '"open_" \* "sealed_holdout"'`
      ≥ 1 — so the file does not fail its own scan.
    - `git diff --exit-code HEAD -- spike/test/test_p12_decoupling.jl` exits 0 (the Phase-12 guard was
      not edited).
    - Dropping a scratch file `src/zzz_probe.jl` makes this file exit 1 (the untracked case is really
      caught); delete the scratch file afterwards.
  </acceptance_criteria>

  <done>
Environment, `src/`, corpus and artifact constraints are all executable from a Phase-14 file, and the
scan grows with the phase.
  </done>
</task>

<task type="auto">
  <name>Task 2: The Phase-14-specific source greps (SC2-c, SC2-d, the withdrawn figure, the forbidden costes seed)</name>

  <files>spike/test/test_p14_decoupling.jl</files>

  <read_first>
    - `spike/test/test_p13_tau.jl:102-104, 275-315` — the source-grep technique: the module-level
      `TAU_SRC` / `TAU_LINES` / `TAU_CODE` triple, the PER-LINE form, and the
      positive-plus-negative no-reimplementation pair (`occursin("roc_auc(")` AND
      `!occursin("function roc_auc")`).
    - `14-VALIDATION.md` rows SC2-c and SC2-d (the exact patterns and the exemption).
    - `14-RESEARCH.md` Pitfall 5 (unit mismatch, `ghat`), Pitfall 7 (`costes_p` and
      `MASTER_SEED = 0x00C0FFEE`, which IS `NPE_MASTER_SEED`), §D.7 recommendation 1 (the
      Phase-14-specific `0.032` / `1/31` check).
    - `14-CONTEXT.md` D-03a — the paragraph withdrawing `α ≥ 1/31 ≈ 0.032`.
    - `spike/comparator/config.jl:37-56` — `COSTES_ALPHA`, `MASTER_SEED`.
  </read_first>

  <action>
Append to `spike/test/test_p14_decoupling.jl` (same outer `@testset`) the Phase-14-specific checks.
Build, at module level, a per-file table of `(path, lines, code)` over `_p14_sources()` where `lines`
are the comment-stripped lines and `code` their join — the `test_p13_tau.jl` triple, generalized.

Every check below collects **offending `"path: line"` strings** and asserts the collection is empty,
so a failure shows the code rather than a boolean.

1. **SC2-c — D-06 anti-regression.** No Phase-14 source may use a `false` OOD flag as an
   in-distribution proxy. Flag any comment-stripped line matching `\.flag == false`, `!\w*\.flag`,
   `\.flag === false`, or `verdict\.flag \?` **outside the body of `p14_ood_state`**. Implement the
   exemption positionally: locate the `function p14_ood_state` line and its matching `end` in
   `spike/p14/fuse.jl` and exclude that line range. Assert zero hits. Add the arms-itself-later
   `if isfile(fuse_path) … else @info … end` wrapper, since `fuse.jl` lands in wave 2. Also assert
   POSITIVELY that once `fuse.jl` exists it contains `:not_checked` — the guard must not be
   satisfiable by a file that simply never mentions the three-valued state.

2. **SC2-d — the ghat unit guard.** Every `patch_correlation(` call site whose line also compares to
   τ (matches `tau` or `τ`) must be wrapped in `ghat(`. Use the PER-LINE form. Assert zero bare
   comparisons. Additionally assert positively that at least one Phase-14 source contains
   `ghat(patch_correlation(` once `spike/p14/decide.jl` exists. Carry the reason in a comment:
   τ = 0.15 is defined on ρ_true while `patch_correlation` returns the induced mean per-patch
   correlation μ; applying τ to μ is a criterion applied in a unit it was not derived for, which is
   one of the four recorded "the bar was wrong" families (14-RESEARCH Pitfall 5).

3. **The WITHDRAWN figure.** Assert no Phase-14 source contains, in comment-stripped code, the
   literal `0.032`, `1/31`, `1 / 31`, or `1/(30+1)`. Comment that D-03a withdraws `α ≥ 1/31 ≈ 0.032`
   because it was computed from the wrong n and on rows that are not physical truth, and that the
   check exists so the corpus-derived floor can never become an executable default. Note the
   deliberate exemption: `p14_conformal_quantile`'s **general** feasibility expression
   `1/(n+1)` computed from its own `n` argument is permitted — flag only a hard-coded `31` or the
   decimal `0.032`.

4. **Pitfall 7 — the forbidden costes seed.** Assert no Phase-14 source contains
   `costes_p(MASTER_SEED` or `costes_p(COSTES` on a comment-stripped line, and assert positively that
   once `spike/p14/decide.jl` exists every `costes_p(` call site names `P14_DEV_SEED` on the same
   line. Comment that `spike/comparator/config.jl:56` sets `MASTER_SEED = 0x00C0FFEE`, which **is**
   `NPE_MASTER_SEED` in the forbidden inventory, and that `test_p14_consts.jl` cannot catch this
   because it checks constants, not call sites.

5. **No re-implementation of shared helpers.** For each of `roc_auc`, `corspearman`,
   `_bin_calibration`, `three_way_label`, `ghat`: assert that no Phase-14 source contains
   `function <name>`. And assert the positive twin for the two that must actually be used:
   once the runners exist, `roc_auc(` appears in `spike/p14/run_p14_riskcoverage.jl` and
   `corspearman` appears there too. This is the `test_p13_tau.jl:275-315` positive-plus-negative pair.

6. **The two alphas are never derived from one another (D-07).** Assert no comment-stripped line
   matches `alpha_fdr\s*=\s*.*ALPHA_CONFORMAL` or `alpha_conformal\s*=\s*.*alpha_fdr` (case-
   insensitive on the identifier halves). Comment that both happen to take the value 0.10 at one grid
   point, which is exactly why the assignment must never be written.

7. **`runtests.jl` is not a gate.** Assert no Phase-14 source contains `runtests.jl`. Comment that
   the aggregate suite exits 1 at the Phase-4 `SPEEDUP_GATE`, masking every later include block, so
   per-file runs are the only reliable signal.

Close with the CPU-only testset if Task 1 did not already.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_decoupling.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0 with zero failures, and the
      pass count is strictly greater than after Task 1.
    - The file contains all of these literal strings: `p14_ood_state`, `not_checked`,
      `ghat(patch_correlation(`, `costes_p(MASTER_SEED`, `0.032`, `runtests.jl`.
    - Every grep assertion strips comment lines first — verify by adding a scratch comment line
      `# 0.032` to `spike/p14/consts.jl`, re-running (must still exit 0), then removing it.
    - Adding a scratch line `x = verdict.flag == false` to `spike/p14/consts.jl` makes the file exit
      1; remove the scratch line afterwards.
    - `grep -v '^\s*#' spike/test/test_p14_decoupling.jl | grep -c '== 0$'` — any count-based gate
      present must be on comment-stripped input; bare `grep -c` on an unfiltered file is forbidden.
  </acceptance_criteria>

  <done>
The four Phase-14-specific integrity greps (SC2-c, SC2-d, the withdrawn figure, the forbidden costes
seed) are running, arm themselves as later waves land, and cannot be satisfied vacuously.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-14 sources → the frozen environment | A `Pkg.add` anywhere breaks three running assertions |
| Phase-14 sources → `corpus/` and Phase-16's sealed holdout | Reading one byte burns the blind evaluation |
| Phase-14 sources → `src/` | A new untracked file under `src/` is productionization by stealth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | `spike/Project.toml`, `spike/Manifest.toml` | mitigate | Byte-equality + untracked-clean + exactly-sixteen + one by-name `!haskey` per banned package, restated in Phase 14's own file (D-02) |
| T-14-10 | Tampering | `src/` | mitigate | `git diff --quiet HEAD -- src` AND `git status --porcelain -- src` (the untracked case); acceptance criterion drops a scratch file to prove the guard fires |
| T-14-04 | Information disclosure | Phase-16 sealed holdout | mitigate | The four sealed-holdout checks restated over `_p14_sources()`, with the allowlist self-consistency check so an exemption cannot silently protect nothing (D-03a) |
| T-14-07 | Repudiation | the withdrawn `1/31 ≈ 0.032` figure | mitigate | Literal banned from executable Phase-14 code; the general `1/(n+1)` expression is exempted deliberately and the exemption is documented |
| T-14-08 | Spoofing | `verdict.flag == false` read as in-distribution | mitigate | SC2-c grep with a positional exemption for `p14_ood_state` only, plus a POSITIVE `:not_checked` assertion so the guard cannot pass vacuously (D-06) |
| T-14-16 | Tampering | unit mismatch, classical scale vs τ | mitigate | SC2-d per-line grep requiring `ghat(` on every `patch_correlation(`-vs-τ comparison, plus the positive `ghat(patch_correlation(` assertion |
| T-14-03 | Information disclosure | `costes_p` consuming `NPE_MASTER_SEED` | mitigate | Grep bans `costes_p(MASTER_SEED`; positive check requires `P14_DEV_SEED` at every call site (Pitfall 7) |
| T-14-17 | Tampering | helper re-implementation | mitigate | `function roc_auc` / `corspearman` / `_bin_calibration` / `ghat` / `three_way_label` banned in Phase-14 sources; positive twin asserts they are actually used |
| T-14-SC | Tampering | package installs | mitigate | This plan IS the enforcement: zero installs, sixteen deps, six by-name bans |
</threat_model>

<verification>
- `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0.
- `julia --project=spike spike/test/test_p14_consts.jl` exits 0 (wave 0 still green).
- `julia --project=spike spike/test/test_p14_provenance.jl` exits 0.
- `git diff --exit-code HEAD -- src spike/Project.toml spike/Manifest.toml corpus` exits 0.
- `spike/test/runtests.jl` is NOT invoked anywhere in this plan's verification.
</verification>

<success_criteria>
- Every hard constraint in `14-CONTEXT.md` "Constraints that bind this phase" is a running assertion
  in a Phase-14 file.
- The guard exists before the code it guards, so a violation fails on the commit that introduces it.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-03-SUMMARY.md` when done. Record the
per-file pass count — Phase 13's precedent, since the aggregate suite cannot be used.
</output>

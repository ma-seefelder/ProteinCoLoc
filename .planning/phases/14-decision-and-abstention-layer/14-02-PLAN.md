---
phase: 14-decision-and-abstention-layer
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - spike/p14/provenance.jl
  - spike/test/test_p14_provenance.jl
autonomous: true
requirements: []          # ROADMAP: `Requirements: TBD`. Traced to D-07 and 14-VALIDATION row D-07.
decisions: [D-07, D-01]
validates: ["D-07", "D-01/env"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "τ is LOADED from the Phase-13 net artifact and never hardcoded or re-derived in Phase 14 (D-07)"
    - "All four on-disk τ values agree, and a disagreement fails loudly rather than defaulting (D-07)"
    - "The probe's THIRD consts_sha256 is asserted DIVERGENT, not widened into a disjunction (D-07)"
    - "The Phase-14 lane reads Phase-13 artifacts read-only and writes nothing under spike/p13/ (D-01)"
  artifacts:
    - path: "spike/p14/provenance.jl"
      provides: "p14_load_tau + the D-07 sha table, fail-loudly"
      contains: "p14_load_tau"
      min_lines: 90
    - path: "spike/test/test_p14_provenance.jl"
      provides: "the four-way τ agreement + sha assertions"
      contains: "P13_CONSTS_SHA"
      min_lines: 70
  key_links:
    - from: "spike/p14/provenance.jl"
      to: "spike/p13/three_way_net.jld2"
      via: "load_three_way, treated as the source of truth for τ"
      pattern: "load_three_way"
    - from: "spike/p14/provenance.jl"
      to: "spike/p13/tau_probe_report.jld2"
      via: "JLD2.load, cross-assert + assert-the-divergence"
      pattern: "tau_probe_report"
---

<objective>
Give Phase 14 a τ that is **loaded with provenance asserted**, never copied and never re-derived
(D-07), plus the sha table that makes the load auditable.

Purpose: D-07's stated reasoning is that *"copying the value is how two numbers silently diverge;
re-deriving guarantees a second, different cut on the same hypothesis space."* τ = 0.15 appears in
FOUR places on disk and all four agree today. Phase 14 asserts that agreement at load time, so a
future divergence is loud.

Output: `spike/p14/provenance.jl` (`p14_load_tau`, `p14_provenance_record`) and
`spike/test/test_p14_provenance.jl`.

**File-list note:** `provenance.jl` is an addition to the seven module files enumerated in
`14-PATTERNS.md` §1. It exists because the τ loader needs a home and `spike/p14/consts.jl` is not
it — consts.jl is Tier-1 frozen pre-registration and τ is deliberately absent from it (D-07).
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
</context>

<interfaces>
<!-- Extracted from disk. Use directly; do not go exploring. -->

`spike/p13/net.jl`:
- `load_three_way(path) -> NamedTuple` with fields
  `net, head_log_odds, input_dim, num_summaries, width, tau, consts_sha, cut_variant,
   schema_version, meta`
- `p13_consts_sha()` — sha256 of `spike/p13/consts.jl` as it is on disk NOW (`:524`)
- `const P13_CONSTS_SHA = (pre_amendment = "100e97a3…3fa6", post_amendment = "d6a6e63b…e247")` (`:556-559`)

`spike/p13/consts.jl` Tier-2 (`:853-872`): `P13_TAU = 0.15`, `P13_TAU_MEASURED_AUC = 0.91635625`,
`P13_TAU_PROBE_SHA`, `P13_TAU_SIMULATOR_SHA`, `P13_TAU_PROBE_ARTIFACT =
"spike/p13/tau_probe_report.jld2"`, `P13_TAU_REFERENCE_LAMBDA = 3.0`; accessor `p13_tau()` (`:266-282`).

Measured on disk (14-RESEARCH §D.3, all VERIFIED by JLD2 load 2026-08-03):
- `three_way_net.jld2["tau"] == 0.15`; `h.consts_sha == P13_CONSTS_SHA.pre_amendment`
- `tau_probe_report.jld2["tau"] == 0.15`, `["tau_bar"] == 0.9`, `["repo_sha"] == P13_TAU_PROBE_SHA`,
  `["simulator_sha"] == P13_TAU_SIMULATOR_SHA`, `["repo_dirty_at_run"] == true`
- `three_way_gate_report.jld2["P13_TAU"] == 0.15`, `["net_consts_sha"] == h.consts_sha`
- **THE TRAP:** `tau_probe_report.jld2["consts_sha256"] == "640ec90e…aca4cf"` — a THIRD value
  matching NEITHER literal in `P13_CONSTS_SHA`, and that is CORRECT (the probe ran before the Tier-2
  append).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: spike/p14/provenance.jl — load τ, assert its provenance, fail loudly</name>

  <files>spike/p14/provenance.jl</files>

  <read_first>
    - `spike/p13/net.jl:459-583` — `save_three_way`/`load_three_way`, `p13_consts_sha()`,
      `P13_CONSTS_SHA`, and the docstring at `:549-554` recording that the widened `||` form was
      REJECTED so a later reader knows it was considered.
    - `spike/p13/run_three_way_gate.jl:200-206` (`p13_blob_sha`) and `:361-370` (the pinned-both-sides
      assertion — the exact form D-07 requires).
    - `spike/p13/consts.jl:266-282` (the fail-loudly Tier-2 accessor idiom) and `:790-872` (Tier-2).
    - `14-PATTERNS.md` §3.1(g), §3.10, §4.2 (guarded include), §4.3 (repo root is TWO levels up from
      `spike/p14/`), §4.4, §4.5.
    - `14-RESEARCH.md` §D.3 in full — including the ⚠ TRAP paragraph.
  </read_first>

  <action>
Create `spike/p14/provenance.jl`: AGPL header, guarded includes, no `module`, no `export` (the spike
lane is a flat top-level namespace — `14-PATTERNS.md` §6).

Guarded includes, in dependency order:
`isdefined(@__MODULE__, :P14_DECLARED_DEVIATIONS) || include(joinpath(@__DIR__, "consts.jl"))`, then
`isdefined(@__MODULE__, :ThreeWayEvidenceNet) || include(joinpath(@__DIR__, "..", "p13", "net.jl"))`.
Note in a comment that including `p13/net.jl` transitively brings `P13_TAU`, `P13_CONSTS_SHA` and
`p13_consts_sha()` into scope, which is what makes the fourth cross-check free.

Define `const P14_P13_DIR = normpath(joinpath(@__DIR__, "..", "p13"))` and
`const P14_REPO_ROOT = normpath(joinpath(@__DIR__, "..", ".."))` (TWO levels up, not three).

**`p14_load_tau(; net_path = joinpath(P14_P13_DIR, "three_way_net.jld2"),
probe_path = joinpath(P14_P13_DIR, "tau_probe_report.jld2"),
gate_path = joinpath(P14_P13_DIR, "three_way_gate_report.jld2")) -> NamedTuple`**

Behaviour, in this order:
1. Each of the three paths must `isfile`; otherwise `error(...)` naming the missing artifact and
   saying that D-07 forbids substituting a literal. Never return a default.
2. `h = load_three_way(net_path)`. **The net is the source of truth for τ** — τ travels with the net
   that was trained under it (14-RESEARCH §D.3 recommendation).
3. Load the probe and gate reports with `JLD2.load`. Use the house `catch` idiom
   (`e isa InterruptException && rethrow()`) if you wrap the loads, but a failed load must `error`,
   not return `nothing` — this is a hard precondition, unlike `_recorded_ood_threshold`'s inert path.
4. Assert the **four-way agreement** with one `@assert`/`error` per equality so the message names
   which one failed: `h.tau == probe["tau"]`, `h.tau == gate["P13_TAU"]`, `h.tau == P13_TAU`, and
   `h.tau == p13_tau()`.
5. Assert the sha table: `h.consts_sha == P13_CONSTS_SHA.pre_amendment`;
   `p13_consts_sha() == P13_CONSTS_SHA.post_amendment`; `gate["net_consts_sha"] == h.consts_sha`;
   `probe["repo_sha"] == P13_TAU_PROBE_SHA`; `probe["simulator_sha"] == P13_TAU_SIMULATOR_SHA`;
   `probe["tau_bar"] == P13_TAU_AUC`; `P13_TAU_MEASURED_AUC >= P13_TAU_AUC`.
6. **Assert the DIVERGENCE, do not tolerate it:**
   `probe["consts_sha256"] ∉ values(P13_CONSTS_SHA)`, with an error message explaining that the probe
   ran BEFORE the Tier-2 τ block was appended — which is precisely the auditable ordering
   `spike/p13/consts.jl:842-846` cites as evidence τ was measured before it was locked — and stating
   in the same message that **widening this check to a disjunction destroys the integrity argument
   and is forbidden**. Mirror the rejected-form note at `net.jl:549-554`.
7. Return a NamedTuple:
   `(tau, source = :three_way_net_jld2, net_path, probe_path, gate_path,
     net_consts_sha = h.consts_sha, file_consts_sha = p13_consts_sha(),
     probe_consts_sha = probe["consts_sha256"], probe_repo_sha = probe["repo_sha"],
     probe_simulator_sha = probe["simulator_sha"],
     probe_repo_dirty_at_run = probe["repo_dirty_at_run"],
     tau_measured_auc = P13_TAU_MEASURED_AUC, tau_bar = P13_TAU_AUC,
     phase11_sha = h.meta.phase11_sha, four_way_agreed = true)`.

Record `probe_repo_dirty_at_run` **honestly** — it is `true` on disk. Document in the docstring that
Phase 14 surfaces it and does not assert it false.

**`p14_provenance_record(prov; extra = NamedTuple()) -> NamedTuple`** — the block every Phase-14
`.jld2` artifact embeds. Merge the caller's `extra` UNDER a `required` NamedTuple (the
`p13_calibration_meta` idiom: caller keys are preserved but can never overwrite required keys). The
required keys are: every field of `prov`, plus `p14_consts_sha = bytes2hex(SHA.sha256(read(joinpath(
@__DIR__, "consts.jl"))))`, `p14_consts_git_blob_sha` via a local `p14_blob_sha(path)` copied from
`p13_blob_sha` (`run_three_way_gate.jl:200-206`), `master_seed = UInt64(P14_DEV_SEED)`,
`salt = UInt64(P14_SALT)`, `generated = string(Dates.now(Dates.UTC)) * "Z"`, and
`amendment = "SC1 is AMENDED by D-02 (ConformalPrediction.jl dropped; conformal met in substance, " *
"hand-rolled). SC2 is AMENDED by D-05 (asymmetric fusion, not the literal OR). Any report citing " *
"this result must cite the original ROADMAP SC1/SC2 alongside it."` — copying the `amendment =` key
pattern from `run_three_way_gate.jl:777-778`.

Also add `p14_assert_lane_clean()`: asserts `git diff --quiet HEAD -- src` and
`git diff --quiet HEAD -- spike/Project.toml spike/Manifest.toml` succeed, degrading to an `@info`
skip when `git` is unavailable (`Sys.which("git") === nothing`). Every runner calls it as step 0
(D-01, T-14-11).

`using JLD2`, `using SHA`, `using Dates` (all stdlib or among the frozen sixteen — add NO dependency).
  </action>

  <verify>
    <automated>julia --project=spike -e 'include("spike/p14/provenance.jl"); p = p14_load_tau(); @assert p.tau == 0.15; @assert p.four_way_agreed; println("tau loaded: ", p.tau)'</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints `tau loaded: 0.15`.
    - `grep -v '^\s*#' spike/p14/provenance.jl | grep -c '0\.15'` returns 0 — τ is never a literal in
      executable code (D-07).
    - `grep -c 'values(P13_CONSTS_SHA)' spike/p14/provenance.jl` ≥ 1 and the comparison operator on
      that line is `∉` / `!in` — never `∈`, never a `||` disjunction.
    - Pointing `net_path` at a non-existent file makes `p14_load_tau` throw (verify with
      `julia --project=spike -e 'include("spike/p14/provenance.jl"); try p14_load_tau(net_path="nope.jld2"); exit(1) catch; exit(0) end'` → exit 0).
    - `git status --porcelain -- spike/p13` is empty (Phase-13 artifacts read-only, D-01).
  </acceptance_criteria>

  <done>
τ is obtainable in Phase 14 only by loading it, the load asserts four-way agreement plus the sha
table, and the probe's third sha is asserted divergent.
  </done>
</task>

<task type="auto">
  <name>Task 2: spike/test/test_p14_provenance.jl — the D-07 table as a running test</name>

  <files>spike/test/test_p14_provenance.jl</files>

  <read_first>
    - `spike/test/test_p13_consts.jl:121-144` — the "tau is measured and locked (Tier 2)" testset,
      the direct analog.
    - `spike/test/test_p13_result.jl:190-212` — the git `@testset` skeleton, including
      `isdir(.git) || isfile(.git)` because inside a linked worktree `.git` is a FILE.
    - `spike/p14/provenance.jl` (Task 1).
    - `14-VALIDATION.md` row `D-07`.
  </read_first>

  <action>
Create `spike/test/test_p14_provenance.jl`: AGPL header → `using Test` → guarded include of
`spike/p14/provenance.jl` → one outer `@testset "P14 tau provenance (D-07)" verbose = true`.

Testsets:

1. **"tau is loaded, not written here"** — call `prov = p14_load_tau()` once at module level and
   assert `prov.tau === 0.15`, `prov.source === :three_way_net_jld2`, `prov.four_way_agreed`.
   Additionally read `spike/p14/provenance.jl` and every `spike/p14/*.jl` that exists, strip comment
   lines, and assert none contains a bare `= 0.15` assignment to a name matching `tau` — the
   arms-itself-later idiom (`if isfile(...) ... else @info ... end`) for files that land in later
   waves.

2. **"the four on-disk taus agree"** — assert `prov.tau` equals each of
   `load_three_way(...)["tau"]`-equivalent `h.tau`, `probe["tau"]`, `gate["P13_TAU"]`, `P13_TAU`, and
   `p13_tau()`. Load the JLD2 files directly in the test rather than trusting `prov` alone, so the
   test is an independent check of the loader and not a tautology.

3. **"the sha table is pinned on both sides"** —
   `prov.net_consts_sha == P13_CONSTS_SHA.pre_amendment`,
   `prov.file_consts_sha == P13_CONSTS_SHA.post_amendment`,
   both match `r"^[0-9a-f]{64}$"`; `prov.probe_repo_sha == P13_TAU_PROBE_SHA` and
   `prov.probe_simulator_sha == P13_TAU_SIMULATOR_SHA`, both matching `r"^[0-9a-f]{40}$"`;
   `prov.phase11_sha == "659cda09112c4f20d82e7fd1bc6dc22d57f3853ee245f3c6f781407eb68eb571"`.

4. **"the probe's third sha is asserted DIVERGENT (the trap)"** —
   `@test !(prov.probe_consts_sha in values(P13_CONSTS_SHA))` and
   `@test prov.probe_consts_sha == "640ec90edb80ef62eacbbf64c7b40cb9ca87c59d28a3a7438ef9d382b0aca4cf"`.
   Add a comment stating that this divergence is CORRECT (the probe predates the Tier-2 append) and
   that widening the assertion to a disjunction is itself the failure — the rejected form is recorded
   at `spike/p13/net.jl:549-554`. Also grep `spike/p14/provenance.jl` (comment-stripped) and assert
   it contains no `in values(P13_CONSTS_SHA)` occurrence without a preceding `!`.

5. **"the dirty-tree fact is surfaced, not asserted away"** —
   `@test prov.probe_repo_dirty_at_run === true` with a comment that Phase 14 reports this rather
   than pretending otherwise.

6. **"the Phase-13 lane is read-only (D-01)"** — the git testset: `git diff --quiet HEAD -- spike/p13`
   succeeds and `git status --porcelain -- spike/p13` is empty; same for `src`. Degrade to `@info`
   when git is unavailable.

Close with the CPU-only testset.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_provenance.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_provenance.jl` exits 0, zero failures, zero errors.
    - The file contains the literal string
      `640ec90edb80ef62eacbbf64c7b40cb9ca87c59d28a3a7438ef9d382b0aca4cf` and asserts it is NOT in
      `values(P13_CONSTS_SHA)`.
    - The file is independently runnable and does not include `spike/test/runtests.jl`.
    - `grep -c 'CUDA' spike/test/test_p14_provenance.jl` ≥ 1.
  </acceptance_criteria>

  <done>
The D-07 four-way τ agreement, the sha table and the assert-the-divergence form are all enforced by a
test that runs in seconds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-13 artifacts → Phase-14 code | τ and the shas cross here; a copied literal or a widened check breaks the audit chain |
| Phase-14 code → `src/` and `spike/p13/` | read-only reach; any write is productionization-by-stealth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Tampering | τ | mitigate | `p14_load_tau` errors on a missing artifact instead of defaulting; four-way agreement asserted; grep test forbids a `0.15` literal in Phase-14 executable code |
| T-14-15 | Tampering | the divergence assertion | mitigate | `probe["consts_sha256"] ∉ values(P13_CONSTS_SHA)` asserted in BOTH the loader and the test; the source is grepped for a widened `in values(...)` form; the rejection is recorded in the error message so a later reader knows it was considered |
| T-14-10 | Tampering | `src/`, `spike/p13/` | mitigate | `p14_assert_lane_clean()` at runner step 0 plus the git testset here (D-01) |
| T-14-11 | Repudiation | run started clean, finished dirty | mitigate | `p14_assert_lane_clean()` is called by every runner while the run happens, not only in review (the `run_three_way_gate.jl:270-280` idiom) |
| T-14-SC | Tampering | package installs | accept | No package is fetched in this plan; `JLD2`, `SHA`, `Dates` are stdlib or already among the frozen sixteen |
</threat_model>

<verification>
- Both automated commands exit 0.
- `git diff --exit-code HEAD -- spike/Project.toml spike/Manifest.toml` exits 0.
- `git status --porcelain -- src spike/p13` is empty.
</verification>

<success_criteria>
- No Phase-14 code path can obtain τ except by loading it with provenance asserted.
- A future divergence in any of the four τ sites, or in any pinned sha, fails a test in under 10
  seconds.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-02-SUMMARY.md` when done.
</output>

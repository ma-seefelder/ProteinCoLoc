---
phase: 14-decision-and-abstention-layer
plan: 11
type: execute
wave: 6
depends_on: ["14-09"]
files_modified:
  - spike/p14/run_p14_riskcoverage.jl
autonomous: true
requirements: []          # ROADMAP: `Requirements: TBD`. Traced to D-03 and 14-VALIDATION rows
                          # SC3-a, SC3-b, SC3-c.
decisions: [D-03, D-05]
validates: ["SC3-a", "SC3-b", "SC3-c"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "Selective skill is measured against BOTH an oracle and a random reference curve on the same data, so the base error rate cannot carry the bar (SC3-a)"
    - "Pointwise monotonicity is NOT asserted; a Spearman statistic on the pre-registered coverage range [0.20, 1.00] is (SC3-b)"
    - "The RAW curve over the FULL coverage range is reported, including the noisy low-coverage part — shown, not hidden"
    - "The 0.20 coverage floor was frozen BEFORE the run and is never chosen after seeing where the curve becomes monotone"
    - "Abstention concentrates on cases that would have been wrong, measured against the simulator's true label so it is not circular (SC3-c)"
    - "All four SC3 bars are labelled in the artifact as judgement calls with no derivation"
  artifacts:
    - path: "spike/p14/run_p14_riskcoverage.jl"
      provides: "the SC3-a/b/c runner"
      contains: "P14_SKILL_FLOOR"
      min_lines: 220
    - path: "spike/p14/p14_riskcoverage_report.jld2"
      provides: "the curve, the three reference curves, skill, spearman, AUC_hard"
    - path: "spike/figures/p14_riskcoverage.png"
      provides: "the risk-coverage figure, rendered BEFORE the gate"
  key_links:
    - from: "spike/p14/run_p14_riskcoverage.jl"
      to: "spike/validation/ood.jl"
      via: "the in-repo tie-aware roc_auc, reused never reimplemented"
      pattern: "roc_auc\\("
    - from: "spike/p14/run_p14_riskcoverage.jl"
      to: "StatsBase"
      via: "corspearman, never hand-rolled"
      pattern: "corspearman"
---

<objective>
Measure what SC3 actually asks, split into the three separable claims 14-RESEARCH §C.3 defines, and
scored against bars that were frozen before this runner existed.

- **SC3-a (GATED):** `skill = (AURC_random − AURC) / (AURC_random − AURC_oracle) ≥ P14_SKILL_FLOOR =
  0.60`. Normalized against both references on the same data, so it is immune to the base error rate.
- **SC3-b (GATED):** `corspearman(coverage, selective_risk) ≥ P14_SPEARMAN_FLOOR = 0.95` over
  coverage ∈ `[P14_COVERAGE_FLOOR = 0.20, 1.00]`. **Do not assert pointwise monotonicity** — the
  empirical curve is a step function whose denominator is tiny at low coverage, so a single hard item
  moves the selective risk by `1/k`. Report the RAW curve over the FULL range and apply the statistic
  only on the declared range.
- **SC3-c (GATED):** `AUC_hard ≥ P14_AUC_HARD_FLOOR = 0.80`, where `hard_i` is *"the argmax class
  would have been wrong"* against the SIMULATOR's true label — which exists, so the measurement is
  not circular.

**All four SC3 bars and the coverage floor are JUDGEMENT CALLS WITH NO DERIVATION.** They are frozen
in `spike/p14/consts.jl` (14-01) and must be labelled as such in the artifact and the report. They
may never be relaxed after a result. This project has recorded four cases of an underived bar
measuring something other than what it named.

Output: `spike/p14/run_p14_riskcoverage.jl`, `spike/p14/p14_riskcoverage_report.jld2`,
`spike/figures/p14_riskcoverage.png`.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**

**Wave-6 note:** wave-parallel by dependency with 14-10, 14-12 and 14-13; run sequentially if the
machine cannot carry four CPU-bound runners.
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The curve, its two reference curves, and the figure rendered BEFORE the gate</name>

  <files>spike/p14/run_p14_riskcoverage.jl</files>

  <read_first>
    - `14-RESEARCH.md` §C in full — C.1 (the construction), C.2 (**what breaks monotonicity, all four
      of which live here**), C.3 (Objects 1 and Statistics S1-S3, each with what would falsify it),
      C.4 (simulator draws only, and the circularity caveat required in any Phase-14 report).
    - `spike/validation/ood.jl:309-335` — `roc_auc`, tie-aware Mann-Whitney U, and its descending
      unique-threshold tie convention (`src/amortized/ood.jl:281`), which the sweep must match.
    - `spike/p13/run_three_way_gate.jl:215` (`p13_gate_auc`, the AUC-only wrapper one-liner),
      `:768-772` (the figure rendered BEFORE the gate, *"the evidence of an honest failure must
      survive the failure"*), and the runner skeleton sections listed in 14-09/14-10.
    - `spike/validation/run_p11_probe.jl:59` — *"corspearman (never hand-rolled, PATTERNS S6)"*.
    - `spike/figures/` — the existing `p13_*.png` conventions.
  </read_first>

  <action>
Create `spike/p14/run_p14_riskcoverage.jl` on the 14-09 runner skeleton: banner, step-0
`p14_assert_lane_clean()`, locked-threshold banner, `_p14_save_report`, smoke redirect, guarded entry
point.

**Locked-threshold banner** must print `P14_SKILL_FLOOR`, `P14_SPEARMAN_FLOOR`, `P14_COVERAGE_FLOOR`,
`P14_AUC_HARD_FLOOR` with their source file, each marked **GATED**, and each additionally marked
**JUDGEMENT CALL — NO DERIVATION**. Print `P14_JUDGEMENT_CALL_BARS` verbatim so the label is
mechanical rather than hand-written.

`main`:
1. Load the shared `spike/p14/p14_eval_pool.jld2` from 14-09; assert `counter == P14_EVAL_COUNTER`
   and `n_eval == P14_N_EVAL`; fail loudly naming 14-09 if absent. **Never redraw.**
2. **Confidence and loss.** `κ_i = p14_confidence(p_i) = max_y p̂(y|Z_i)` — the same quantity LAC
   thresholds, so the curve and the conformal layer describe ONE ordering; say so in a comment.
   `ℓ_i = 1` if the argmax (singleton) call disagrees with the true three-way class, else `0`.
3. **The curve.** Sweep θ over the DESCENDING UNIQUE values of κ — matching `roc_auc`'s existing tie
   convention, since with a sigmoid output ties are rare but the sweep must define a rule. At each θ:
   `coverage = mean(κ .>= θ)`, `selective_risk = sum(ℓ .* (κ .>= θ)) / (coverage * n)`. Record the
   full `(coverage, selective_risk)` arrays over the FULL range.
4. **The two reference curves, computed on the SAME data:**
   - **oracle** — sort by actual correctness, all correct items first; the lower envelope;
   - **random** — shuffle on `p14_rng(P14_EVAL_COUNTER)` (a deterministic, recorded shuffle, not an
     unseeded one); a straight line at the base error rate.
   Compute `AURC`, `AURC_oracle`, `AURC_random` by trapezoidal integration of selective risk against
   coverage, and `E_AURC = AURC - AURC_oracle`.
5. **Render the figure BEFORE any gate is evaluated** — `spike/figures/p14_riskcoverage.png` with the
   measured curve plus both references over the FULL coverage range, the `[P14_COVERAGE_FLOOR, 1.00]`
   statistic range shaded, and the base error rate annotated. Carry the reason in a comment: the
   evidence of an honest failure must survive the failure.
6. Persist `spike/p14/p14_riskcoverage_report.jld2` with the raw curve arrays, the two reference
   curves, `AURC`, `AURC_oracle`, `AURC_random`, `E_AURC`, the base error rate, `n_eval`, the shuffle
   seed and counter, and the full provenance block.
  </action>

  <verify>
    <automated>julia --project=spike -t auto spike/p14/run_p14_riskcoverage.jl</automated>
  </verify>

  <acceptance_criteria>
    - `spike/figures/p14_riskcoverage.png` and `spike/p14/p14_riskcoverage_report.jld2` both exist
      after the run, **including when a gate assertion fails** (mtimes newer than the run start).
    - The persisted curve covers the FULL coverage range — `minimum(coverage) < P14_COVERAGE_FLOOR` —
      so the noisy part is shown rather than hidden.
    - `grep -c 'function roc_auc\|function corspearman' spike/p14/run_p14_riskcoverage.jl` returns 0,
      and `grep -c 'roc_auc(' spike/p14/run_p14_riskcoverage.jl` ≥ 1 — reused, not reimplemented.
    - `grep -v '^\s*#' spike/p14/run_p14_riskcoverage.jl | grep -cE '\b0\.60\b|\b0\.95\b|\b0\.80\b|\b0\.20\b'`
      returns 0 — every bar is referenced by constant name.
    - The printed locked-threshold banner contains the string `JUDGEMENT CALL` next to all four bars.
  </acceptance_criteria>

  <done>
The curve and both reference curves exist over the full range, the figure is on disk before any
verdict, and no bar is inlined.
  </done>
</task>

<task type="auto">
  <name>Task 2: The three statistics, persisted then asserted (SC3-a/b/c)</name>

  <files>spike/p14/run_p14_riskcoverage.jl</files>

  <read_first>
    - `14-RESEARCH.md` §C.3 Statistics S1, S2, S3 in full — including what would falsify each and the
      `[ASSUMED]` tags on the floors, and Assumptions Log A1 and A3.
    - `14-VALIDATION.md` rows SC3-a, SC3-b, SC3-c and the Manual-Only Verifications row 1.
    - `.planning/STATE.md` — the "FOR PHASE 16, RECORDED SO IT IS NOT ASSEMBLED BY A REFEREE INSTEAD"
      paragraph: four recorded cases of a bar measuring something other than what it named, and the
      licensing standard each amendment was held to.
  </read_first>

  <action>
Append to `main`:

1. **S1 — selective skill (SC3-a).** `skill = (AURC_random - AURC) / (AURC_random - AURC_oracle)`.
   Guard the degenerate denominator (`AURC_random ≈ AURC_oracle`) BEFORE the division and record
   `skill = NaN` with a note rather than dividing by ~0. Persist `skill`, both AURCs and the floor.

2. **S2 — monotone trend, stated honestly (SC3-b).** Restrict to the pre-registered range
   `coverage ∈ [P14_COVERAGE_FLOOR, 1.00]` and compute `StatsBase.corspearman(coverage_sub,
   selective_risk_sub)`. **Never hand-roll the Spearman.** Persist the statistic, the range actually
   used, the number of curve points inside it, and the raw full-range curve alongside.
   Record explicitly in the artifact as `coverage_floor_frozen_before_run = true` together with the
   14-01 freeze-commit sha, and add a `coverage_floor_rationale` string giving §C.2 item 1 as the
   reason (below ~0.20 the denominator is too small for the ratio to mean anything). Add a
   `forbidden_action` string stating that monotonizing the curve, or choosing the floor after seeing
   where the curve becomes monotone, is the failure mode this project has paid for repeatedly and is
   not available here.

3. **S3 — abstention concentrates on hard cases (SC3-c).** `hard_i = argmax_y p̂(y|Z_i) != true_class_i`
   computed BY NAME; `abstention_score_i = 1 - p14_confidence(p_i)`;
   `AUC_hard = roc_auc(abstention_score[.!hard], abstention_score[hard])[3]` — the in-repo tie-aware
   AUC, wrapped in a one-line `p14_rc_auc` helper copied from `p13_gate_auc` rather than re-indexing
   everywhere. `hard` uses the simulator's true label, which exists, so there is no circularity;
   record that sentence as a note.

4. **The four-bar honesty block, written INTO the artifact:**
   `bars = (skill_floor = P14_SKILL_FLOOR, spearman_floor = P14_SPEARMAN_FLOOR,
   coverage_floor = P14_COVERAGE_FLOOR, auc_hard_floor = P14_AUC_HARD_FLOOR)`;
   `bars_are_judgement_calls = true`;
   `bars_note` — *these floors have no derivation. They were frozen in `spike/p14/consts.jl` in a
   commit that precedes every result-producing commit, and they may never be relaxed after a result.
   This project has recorded four separate cases of an underived bar measuring something other than
   what it named; the licensing standard for any amendment is evidence, independent of the machinery
   under audit, that the bar measures something other than what it names.*
   Also persist `assumption_a1_note` — the population risk-coverage curve is non-decreasing in
   coverage iff the confidence score is monotone in conditional risk; a one-line derivation, not a
   quoted theorem — and `circularity_note` from §C.4: the curve, the conformal quantile and the FDR
   check are all computed on draws from the same simulator the net was trained on; train-joint equals
   eval-joint by construction; **every number in SC1/SC3 is a well-specified-regime number and
   inherits the simulator's misspecification in full**, and the bounding evidence D-03 intended is
   **absent, not merely loose**.

5. **PERSIST FIRST, print, then assert** all three gates: `skill >= P14_SKILL_FLOOR`,
   `spearman >= P14_SPEARMAN_FLOOR`, `auc_hard >= P14_AUC_HARD_FLOOR`, each with a message naming its
   SC row, quoting the measured value and the bar, and stating that an honest shortfall is a Phase-14
   finding and that `P14_ITERATION_ALLOWANCE` has exactly one pre-declared trigger which is NOT any
   of these three.
  </action>

  <verify>
    <automated>julia --project=spike -e 'using JLD2; d = JLD2.load("spike/p14/p14_riskcoverage_report.jld2"); for k in ("skill","spearman","auc_hard","bars","bars_are_judgement_calls","bars_note","coverage_floor_frozen_before_run","coverage_floor_rationale","assumption_a1_note","circularity_note","curve_coverage","curve_selective_risk"); @assert haskey(d,k) k; end; @assert d["bars_are_judgement_calls"]; println("skill=", d["skill"], " spearman=", d["spearman"], " auc_hard=", d["auc_hard"])'</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints the three measured statistics.
    - `d["bars"]` equals `(skill_floor = 0.60, spearman_floor = 0.95, coverage_floor = 0.20,
      auc_hard_floor = 0.80)` — the frozen values, unchanged.
    - `d["curve_coverage"]` spans below `0.20` (the raw curve is reported in full) while the Spearman
      was computed only on the `[0.20, 1.00]` subset — assert both from the artifact.
    - `d["bars_note"]` contains the substring `judgement` and the substring `four`.
    - If ANY of the three gates fails: record the number, do **not** relax any floor, do **not**
      re-choose the coverage floor, and stop for the user. Relaxing a bar after a result is a
      pre-registration breach, and `P14_ITERATION_ALLOWANCE`'s single trigger is the conformal
      coverage band, not these.
    - `git status --porcelain -- src spike/Project.toml spike/Manifest.toml corpus artifacts` is empty.
  </acceptance_criteria>

  <done>
SC3-a, SC3-b and SC3-c each have a measured value, a frozen bar, a recorded verdict, and an artifact
that says plainly the bars are judgement calls.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pre-registered floor → a floor chosen after seeing the curve | The difference is invisible in the number and total in the claim |
| a scalar confidence sweep → the multi-trigger D-05 fusion | A curve requires a single scalar; the fusion is not one, and conflating them would produce a meaningless object |
| simulator draws → a performance claim about real microscopy | Train-joint equals eval-joint by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | a bar relaxed, or the coverage floor re-chosen, after seeing the curve | mitigate | Bars frozen in 14-01 before this runner existed; `coverage_floor_frozen_before_run` plus the freeze-commit sha persisted; `forbidden_action` string names monotonizing and post-hoc floor selection; acceptance criterion instructs stopping for the user on any failure |
| T-14-36 | Repudiation | asserting pointwise monotonicity that the empirical curve cannot satisfy | mitigate | SC3-b is a Spearman statistic on a declared range, never a pointwise claim; the RAW full-range curve is persisted and plotted, including the noisy low-coverage part (§C.2 item 1) |
| T-14-37 | Tampering | drawing a risk-coverage curve over the D-05 fusion itself | mitigate | The sweep is explicitly over the scalar `κ = max_y p̂`; a comment records that an OOD flag is binary and a set size is an integer, so **a curve cannot be drawn over the fusion** and the two objects must not be conflated (§C.2 item 4) |
| T-14-38 | Repudiation | a circular "concentrates on hard cases" measurement | mitigate | `hard` is defined against the SIMULATOR's true label, not against the abstention itself; the note is persisted |
| T-14-32 | Repudiation | a well-specified-regime number read as real-data performance | mitigate | `circularity_note` is a required artifact key stating that the bounding evidence is ABSENT, not merely loose (§C.4, D-03/D-03a) |
| T-14-17 | Tampering | re-implementing `roc_auc` or `corspearman` | mitigate | `grep` asserts `function roc_auc` / `function corspearman` absent and `roc_auc(` present; StatsBase's `corspearman` is used directly |
| T-14-31 | Repudiation | evidence of an honest failure not surviving the failure | mitigate | Figure rendered and artifact persisted BEFORE any gate is evaluated |
| T-14-11 | Repudiation | run started clean, finished dirty | mitigate | `p14_assert_lane_clean()` at step 0 plus a post-run `git status --porcelain` check |
| T-14-SC | Tampering | package installs | accept | Zero installs; `CairoMakie` and `StatsBase` are among the frozen sixteen |
</threat_model>

<verification>
- `spike/p14/p14_riskcoverage_report.jld2` and `spike/figures/p14_riskcoverage.png` exist with the
  required keys.
- Every `test_p14_*.jl` file still exits 0, run individually (never `runtests.jl`).
- `git status --porcelain -- src spike/Project.toml spike/Manifest.toml corpus artifacts` is empty.
</verification>

<success_criteria>
- Three separable statistics, three frozen bars, three recorded verdicts.
- The raw curve is on disk and in the figure over the full range, so a reader can see the part the
  statistic deliberately excludes.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-11-SUMMARY.md` when done. Record
`skill`, `spearman`, `AUC_hard`, all three bars, and state in one sentence that the bars are
judgement calls with no derivation.
</output>

---
phase: 14-decision-and-abstention-layer
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-04", "14-05"]
files_modified:
  - spike/p14/result.jl
  - spike/test/test_p14_result.jl
autonomous: true
requirements: []          # ROADMAP: `Requirements: TBD`. Traced to D-01, D-04, D-05, D-06 and the
                          # 14-VALIDATION rows SC2-a/b (reason recorded on the result) and D-01/env.
decisions: [D-01, D-04, D-05, D-06]
validates: ["SC2-a", "SC2-b", "D-01/env", "Class order"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "P14Result is a NEW AbstractColocResult subtype and bolts no field onto any shipped struct (D-01, Phase-7 D-02)"
    - "P14Result COMPOSES a ThreeHypothesisColocResult rather than restating its random === 0.0 invariant"
    - "is_ood(r) is documented as lossy: it collapses :clear and :not_checked, and the decision layer never routes through it (D-06)"
    - "delta_rho falls through to _iface_error rather than fabricating a number"
    - "The per-tile map is carried as an explicitly labelled UNCONTROLLED DISPLAY and never enters the FDR arithmetic (D-04)"
    - "Cross-method disagreement is a named field on the result, not a suppressed error (D-05)"
    - "A batch decision cannot exist without its decided fraction beside its alpha"
  artifacts:
    - path: "spike/p14/result.jl"
      provides: "P14Result <: AbstractColocResult, P14BatchDecision, the accessor surface"
      contains: "fdr_scope"
      min_lines: 200
    - path: "spike/test/test_p14_result.jl"
      provides: "subtype relations, accessor contract, lossiness, D-04 labelling"
      contains: "AbstractColocResult"
      min_lines: 110
  key_links:
    - from: "spike/p14/result.jl"
      to: "src/results.jl"
      via: "read-only include for the supertype, _iface_error, OODVerdict, CalibrationMeta"
      pattern: "\\.\\.\", \"\\.\\.\", \"src\", \"results.jl\""
    - from: "spike/p14/result.jl"
      to: "spike/p13/result.jl"
      via: "composition — a ThreeHypothesisColocResult carried as a FIELD"
      pattern: "ThreeHypothesisColocResult"
---

<objective>
Define the result types the decision layer emits: a per-pair `P14Result <: AbstractColocResult` and a
batch `P14BatchDecision`.

Purpose: this is where the phase's honesty commitments become **machine-readable fields** rather than
prose. `fdr_scope = :decided_subset_only` is not cosmetic — it is the structural form of the claim
that FDR is controlled over the decided subset only. `local_map_uncontrolled` is the structural form
of D-04. `cross_method` is the structural form of D-05's "record the disagreement as a named field".
`ood_state` is the structural form of D-06's three-valued input.

**D-01, said plainly and to be repeated in the report:** this type has a `src/`-shaped signature and
subtypes the shipped `AbstractColocResult`, but **"src-shaped" is not "in src"** — the decision layer
is NOT shipped in v2.0 and this plan makes ZERO edits to `src/`.

Output: `spike/p14/result.jl`, `spike/test/test_p14_result.jl`.

**File-list note:** `test_p14_result.jl` is an addition to `14-VALIDATION.md`'s Wave-0 test list.
That list enumerates the Wave-0 gaps, not the exhaustive Phase-14 test set; the result type needs its
own file for the same reason `test_p13_result.jl` exists.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.**
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
</context>

<interfaces>
<!-- Extracted from disk. Use directly. -->

`src/results.jl`:
- `abstract type AbstractColocResult end` (`:43`)
- `_iface_error(r, f)` (`:47-49`) — names the accessor, the type, and the four required accessors
- Required accessors, EXACTLY four: `delta_rho` (`:57`), `bayes_factor` (`:65`), `is_ood` (`:72`),
  `posterior_draws` (`:79`)
- `struct OODVerdict; score::Float64; flag::Bool; per_channel::NamedTuple; end` (`:93-97`)
- `CalibrationMeta` — eight fields `(bin_midpoints, predicted_rate, observed_rate, bin_counts, ece,
  mce, grid, gate)` (`:117-126`)
- `AmortizedColocResult` fields: `(:grid, :posterior, :delta_rho_draws, :log_bayes_factor, :ood,
  :calibration, :meta)`

`spike/p13/result.jl`:
- `struct ThreeHypothesisColocResult <: AbstractColocResult` with fields `(grid, posterior,
  log_bf_vs_random, ood, calibration, meta)`; inner constructor throws `ArgumentError` naming D-08
  unless `lbf.random === 0.0` (`:142-164`)
- `_p13_as_three_way_logbf(nt)` (`:179`), `const P13_LOG_BF_KEYS = (:coloc, :random, :exclusion)` (`:103`)
- `p13_calibration_meta(cal; grid, auc, gate = NamedTuple())` (`:337-352`) — `auc` REQUIRED, caller
  `gate` merged UNDER `required`

`src/amortized/local_map.jl:73-79`: `LocalColocMap` has fields `(grid, tiles, delta_rho, ood_flag,
meta)` — **no uncertainty field.** This is the direct evidence for D-04.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spike/p14/result.jl — P14Result and its accessor surface</name>

  <files>spike/p14/result.jl</files>

  <behavior>
    - `P14Result <: AbstractColocResult`, `isconcretetype(P14Result)`, and
      `fieldnames(AmortizedColocResult)` is unchanged (no bolted-on fields).
    - `decision(r) ∈ (:coloc, :not_coloc, :abstain)`; when `:abstain`, `abstain_reason(r) ∈
      p14_abstain_reasons()`; when `:coloc`/`:not_coloc`, `abstain_reason(r) === nothing`.
    - `is_ood(r) === (ood_state(r) === :fired)`, and `is_ood` returns `false` for BOTH `:clear` and
      `:not_checked`.
    - `delta_rho(r)` throws via `_iface_error` unless `r.meta` carries `:delta_rho_draws`.
    - `bayes_factor(r)` returns `log BF(coloc : random)` from the carried three-hypothesis result.
    - Constructing a `P14Result` whose `decision` is `:abstain` with `abstain_reason = nothing`
      throws `ArgumentError`; so does a non-abstain decision carrying a reason.
  </behavior>

  <read_first>
    - `spike/p13/result.jl` IN FULL — `:63-84` (load order and the READ-ONLY reach into `src/`, TWO
      levels up), `:86-93` (**the Julia-1.12 docstring trap: the guard block covers only the `const`s
      and the `struct`, NEVER the functions**), `:142-164` (inner-constructor validation),
      `:179-184` (the key-order normalizer), `:204-220` (the lossy-accessor documentation
      precedent), `:240-243` (the do-not-fabricate accessor), `:337-352` (`p13_calibration_meta`).
    - `src/results.jl:35-129`.
    - `src/amortized/local_map.jl:55-125` — the `LocalColocMap` field list and the vacuous-flag prose.
    - `14-RESEARCH.md` §D.2 (composition recommended over restatement), §D.6 in full.
    - `14-PATTERNS.md` §3.2 (a)-(g), §3.3, §4.2, §4.3.
  </read_first>

  <action>
Create `spike/p14/result.jl`. AGPL header. Load order matters — `using StatsBase`, `using
Statistics`, `using Images` BEFORE the transitive `src/` reach, exactly as `spike/p13/result.jl:68-70`
documents (the convenience constructor in `src/LoadImages.jl` calls `Images.otsu_threshold`).

Guarded includes, in dependency order, TWO levels up to the repo root:
`AbstractColocResult` ← `src/results.jl`; `P14_DECLARED_DEVIATIONS` ← `consts.jl`;
`ThreeHypothesisColocResult` ← `../p13/result.jl`; `p14_fuse` ← `fuse.jl`;
`p14_class_posterior` ← `posterior.jl`; `p14_conformal_set` ← `conformal.jl`.

**Guard-block split — copy it or the docstrings vanish.** Wrap ONLY the `const`s and the `struct` in
`if !isdefined(@__MODULE__, :P14Result) … end`. Every function stays OUTSIDE the guard: Julia 1.12
DROPS every docstring written inside an `if … end` block (verified on 1.12.6), while method
redefinition under a re-include is silent and harmless. Carry the explanatory comment.

**`struct P14Result <: AbstractColocResult`** with fields, in this order:
`three_way :: ThreeHypothesisColocResult` (COMPOSED, not restated — the `random === 0.0` invariant is
then enforced once, at Phase 13's constructor, and the Phase-14 type is strictly ADDITIVE over Phase
13's, so a reader can see exactly what the decision layer added);
`class_posterior :: NamedTuple` (keys `P14_CLASS_KEYS`);
`null_posterior :: Float64` (`v = 1 − P(coloc|Z)`, the composite `{random ∪ exclusion}`);
`null_split :: NamedTuple` (`p_random`, `p_exclusion`);
`decision :: Symbol`; `abstain_reason :: Union{Symbol,Nothing}`;
`ood_state :: Symbol`; `conformal :: NamedTuple` (`set`, `status`, `qhat`);
`cross_method :: NamedTuple`; `local_map_uncontrolled :: Union{Nothing,LocalColocMap}`;
`meta :: NamedTuple`.

Inner constructor validation, each throwing an `ArgumentError` that names `P14Result`, the decision
it enforces, and the observed value:
- `decision in (:coloc, :not_coloc, :abstain)`;
- `ood_state in (:fired, :clear, :not_checked)` — cite D-06;
- `decision === :abstain` ⟺ `abstain_reason isa Symbol`, and when present
  `abstain_reason in p14_abstain_reasons()` — cite D-05's *"every ABSTAIN carries which trigger
  fired; a silent abstention is nearly as bad as a wrong call"*;
- `Set(keys(class_posterior)) == Set(P14_CLASS_KEYS)` and the three sum to 1 within 1e-12;
- `null_posterior ≈ 1 - class_posterior.coloc` within 1e-12;
- `cross_method` carries the key `:basis` — cite Pitfall 5: `basis` records WHICH SCALE the classical
  comparison was made on, which is the thing that is otherwise forgotten.

**Accessors — the four required ones first, each with the documented-rather-than-silent treatment:**
- `delta_rho(r::P14Result)` — `haskey(r.meta, :delta_rho_draws) || _iface_error(r, :delta_rho)`.
  Docstring: **DO NOT FABRICATE A Δρ.** A three-way path does not measure it; letting the documented
  interface error fire is the honest answer. Carry the D-05 counter-example (ρ_s = 0.8 under
  ρ_c = 0.9 is RANDOM, never EXCLUSION) as the reason the number would mislead.
- `bayes_factor(r::P14Result)` = `bayes_factor(r.three_way)`. Docstring: this is
  `log BF(coloc : random)`, DOCUMENTED RATHER THAN SILENT because a single-number accessor on a
  three-way result is inherently lossy; a caller who wants the three-way evidence must use
  `log_bf_vs_random`.
- `is_ood(r::P14Result)` = `r.ood_state === :fired`. Docstring: **lossy on purpose.** A `false`
  return collapses `:clear` and `:not_checked`, which are different states (D-06); **the decision
  layer never routes through this accessor**; a consumer who cares must call `ood_state(r)`. Same
  treatment as `bayes_factor(::ThreeHypothesisColocResult)`.
- `posterior_draws(r::P14Result)` = `posterior_draws(r.three_way)`.

**New accessors, layered on top and never bolted-on fields:** `decision(r)`, `abstain_reason(r)`,
`ood_state(r)`, `conformal_set(r)`, `conformal_status(r)`, `null_posterior(r)`, `null_split(r)`,
`class_posterior(r)`, `cross_method(r)`, and `local_map(r)`.

`local_map(r)` docstring must say, in these words: the per-tile map is **UNCONTROLLED DISPLAY, not a
controlled call** (D-04). FDR is controlled PER IMAGE PAIR because that is the only unit where the
calibration provably holds — the posterior, the three-way BF and the SBC proof are all per-pair.
`LocalColocMap` carries `delta_rho` and `ood_flag` but **no uncertainty field**, so there is nothing
to control against; Phase 12 returned NO on calibrated per-region uncertainty, and claiming
per-region FDR after that would be exactly the "counting a closed negative as a success" failure
corrected in ROADMAP at `e29c846`. Add `p14_local_map_is_controlled(::P14Result) = false` as an
executable, greppable statement of the same thing.
  </action>

  <verify>
    <automated>julia --project=spike -e 'include("spike/p14/result.jl"); @assert P14Result <: AbstractColocResult; @assert isconcretetype(P14Result); @assert p14_local_map_is_controlled isa Function; println("result type ok")'</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0.
    - `spike/p14/result.jl` contains `local_map_uncontrolled` as a FIELD NAME (the label is in the
      type, not only in a docstring).
    - The guard block covers only `const`s and the `struct`: verify
      `julia --project=spike -e 'include("spike/p14/result.jl"); @assert @doc(is_ood) !== nothing'`
      exits 0 — docstrings survived the Julia-1.12 trap.
    - `grep -c 'ThreeHypothesisColocResult' spike/p14/result.jl` ≥ 2 and the type appears as a struct
      FIELD (composition, not re-statement of the D-08 invariant).
    - `git diff --exit-code HEAD -- src` exits 0 and `git status --porcelain -- src` is empty
      (D-01 — src-shaped is not in-src).
  </acceptance_criteria>

  <done>
`P14Result` exists as a new subtype, composes Phase 13's result, implements all four required
accessors with the lossy ones documented, and labels the per-tile map uncontrolled in the type
itself.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P14BatchDecision — the batch object that cannot omit its denominator</name>

  <files>spike/p14/result.jl</files>

  <behavior>
    - `P14BatchDecision` construction without `n_total` is a `MethodError`/`UndefKeywordError`.
    - `fdr_scope` is always `:decided_subset_only`; any other value throws.
    - `n_decided + n_abstained == n_total`, asserted at construction.
    - `abstain_reason_counts` keys are a subset of `p14_abstain_reasons()` and its values sum to
      `n_abstained`.
    - `decided_fraction == n_decided / n_total`.
    - `pi_sensitivity` is a REQUIRED field and must carry one entry per `P14_PI_COLOC_GRID` value.
  </behavior>

  <read_first>
    - `spike/p13/result.jl:313-315, 337-352` — `p13_calibration_meta`: `auc` is a REQUIRED keyword so
      a calibration verdict can never ship without its discrimination number, and the caller's `gate`
      is merged UNDER `required`. This is the structural trick to copy.
    - `14-RESEARCH.md` §D.6 "Batch object" and §E.2 in full — especially the mandatory companion
      number paragraph: *"a rule that abstains on 95 % of a batch hits any α trivially.
      `(α_FDR, n_decided/n_total)` is one quantity, not two, and quoting either alone is
      misleading."*
    - `14-RESEARCH.md` §E.1 row "Prevalence mismatch" — the π-sensitivity curve is a REQUIRED output,
      not an option (Open Question 3 recommendation).
  </read_first>

  <action>
Append `P14BatchDecision` to `spike/p14/result.jl` (inside the same guard block as the other
`const`s/`struct`s; the constructor function stays outside it).

Fields: `results :: Vector{P14Result}`; `alpha_fdr :: Float64`; `k_star :: Int`;
`threshold_t_star :: Float64`; `implied_cost_ratio :: Float64`; `posterior_expected_fdp :: Float64`;
`accepted :: Vector{Int}`; `n_total :: Int`; `n_decided :: Int`; `n_abstained :: Int`;
`decided_fraction :: Float64`; `abstain_reason_counts :: NamedTuple`;
`class_prior_used :: NamedTuple`; `class_prior_source :: Symbol`;
`pi_sensitivity :: Vector{<:NamedTuple}`; `conformal :: NamedTuple`;
`fdr_scope :: Symbol`; `meta :: NamedTuple`.

**Constructor: `p14_batch_decision(results; alpha_fdr, fdr, n_total, abstain_reason_counts,
class_prior_used, class_prior_source, pi_sensitivity, conformal, meta = NamedTuple())`** — every one
of those a REQUIRED keyword. Deriving `n_total` from `length(results)` is forbidden and the docstring
must say why: making the denominator a required input is what stops an α being quoted without the
decided fraction it is meaningless without.

Constructor assertions, each with a message naming the breach:
- `fdr_scope` is set to `:decided_subset_only` by the constructor itself and is not a caller
  parameter — there is no other legal value in this phase;
- `n_decided + n_abstained == n_total`;
- `decided_fraction == n_decided / n_total`;
- `keys(abstain_reason_counts) ⊆ p14_abstain_reasons()` and `sum(values(...)) == n_abstained`;
- `class_prior_source in (:measured_pi_class_masses, :caller_supplied)` — with a comment that the
  DEFAULT is the measured `h.meta.pi_class_masses` so the default is measured, not chosen, and that
  empirical-Bayes estimation of π from the batch is deliberately NOT offered as a default because it
  is a second estimated quantity with its own failure modes and would make the guarantee circular;
- `length(pi_sensitivity) == length(P14_PI_COLOC_GRID)` and each entry carries `(pi_coloc, k_star,
  fdp, n_accepted)` — the sensitivity curve is REQUIRED output, never optional (SC1-c: REPORTED,
  NOT GATED — no bar attached, and failing to report it IS the failure).

Add `p14_headline(b::P14BatchDecision) -> String` returning a single line that always contains BOTH
numbers, e.g. the α, the realized posterior-expected FDP, and `n_decided/n_total` as a fraction and a
percentage, plus the literal token `FDR SCOPE: DECIDED SUBSET ONLY`. The report and every runner
print this rather than assembling their own sentence, so the two numbers cannot be separated by a
copy-paste.

Add `p14_named_limits() -> NTuple{N,String}` returning the honesty items that must travel with every
Phase-14 result: (1) the decision layer is built in the spike research lane and is **NOT shipped in
v2.0** (D-01) — "src-shaped" is not "in src"; (2) the conformal guarantee is **simulator-derived**
and inherits the simulator's misspecification in full; the intended corpus bound is **absent, not
merely loose** (D-03/D-03a); (3) the real-data check is **six committed TIFFs in two conditions —
an illustration, not a coverage claim** (D-03a); (4) FDR is controlled **over the decided subset
only** and the decided fraction must be quoted beside α (§E.2); (5) per-tile output is
**uncontrolled display** (D-04); (6) SC1 is amended by D-02 and SC2 by D-05, and any report citing a
Phase-14 result must cite the original ROADMAP wording alongside it; (7) the four SC3 bars and the
0.20 coverage floor are **judgement calls with no derivation**; (8) the underlying net is an epoch-4
checkpoint of an early-overfitting run (13-REPORT limit C) and every Phase-14 number inherits that.
  </action>

  <verify>
    <automated>julia --project=spike -e 'include("spike/p14/result.jl"); @assert length(p14_named_limits()) >= 8; @assert occursin("DECIDED SUBSET ONLY", p14_headline_template_probe()); println("batch ok")'</automated>
  </verify>

  <acceptance_criteria>
    - `spike/p14/result.jl` defines `P14BatchDecision` with a `fdr_scope` field whose only assigned
      value in the file is `:decided_subset_only`
      (`grep -v '^\s*#' spike/p14/result.jl | grep 'fdr_scope' | grep -vc 'decided_subset_only'`
      returns 0).
    - Calling `p14_batch_decision(results; alpha_fdr = 0.1, …)` without `n_total` raises
      `UndefKeywordError` — assert this in Task 3's test file.
    - `p14_named_limits()` returns at least 8 strings and one of them contains the substring
      `NOT shipped in v2.0`.
    - `p14_headline` output contains both the α and the `n_decided/n_total` fraction in one string.
  </acceptance_criteria>

  <done>
The batch object carries the FDR scope, the decided fraction and the π-sensitivity curve as required
structure, and the named limits are enumerable from code rather than remembered.
  </done>
</task>

<task type="auto">
  <name>Task 3: spike/test/test_p14_result.jl</name>

  <files>spike/test/test_p14_result.jl</files>

  <read_first>
    - `spike/test/test_p13_result.jl` — `:58-66` (the `P13_REPO_ROOT` guard and the flat-namespace
      collision note), `:68-82` (fixtures built ONCE, the `_res(; kwargs...)` keyword builder),
      `:84-94` (the subtype-relation assertions including the no-bolted-on-fields check on
      `AmortizedColocResult`), `:96-99` (the `===`-not-`==` note), `:190-212` (the git testset),
      `:214-216` (CPU-only).
    - `spike/p14/result.jl` (Tasks 1-2).
  </read_first>

  <action>
Create `spike/test/test_p14_result.jl` on the `test_p13_result.jl` layout: fixtures ONCE at module
level via a `_p14res(; kwargs...)` keyword builder, one outer `@testset ... verbose = true`, no
simulation.

Testsets:
1. **"is a new AbstractColocResult subtype (Phase-7 D-02, D-01)"** — `P14Result <:
   AbstractColocResult`; `isabstracttype(AbstractColocResult)`; `isconcretetype(P14Result)`;
   `_p14res() isa AbstractColocResult`; and the other half: `fieldnames(AmortizedColocResult) ==
   (:grid, :posterior, :delta_rho_draws, :log_bayes_factor, :ood, :calibration, :meta)` — the shipped
   struct gained no field.
2. **"the four required accessors"** — `bayes_factor`, `is_ood`, `posterior_draws` all return without
   error; `delta_rho` THROWS unless `meta` carries `:delta_rho_draws`, and the error message names
   `delta_rho` and `P14Result`.
3. **"is_ood is lossy, and lossy in the documented direction (D-06)"** — `is_ood` is `false` for BOTH
   `ood_state = :clear` and `ood_state = :not_checked`, and `true` only for `:fired`; assert
   `ood_state(r)` distinguishes all three where `is_ood` cannot. Add the source-grep twin: read
   `spike/p14/decide.jl` if it exists (arms-itself-later) and assert it never calls `is_ood(`.
4. **"abstention is explainable"** — every `:abstain` result carries a reason in
   `p14_abstain_reasons()`; constructing `:abstain` with `abstain_reason = nothing` throws; and
   constructing `:coloc` WITH a reason throws.
5. **"the per-tile map is labelled uncontrolled (D-04)"** — `p14_local_map_is_controlled(r) === false`
   and the field is named `local_map_uncontrolled`; assert `fieldnames(LocalColocMap) == (:grid,
   :tiles, :delta_rho, :ood_flag, :meta)` — no uncertainty field, which is the evidence for D-04.
6. **"cross-method disagreement is recorded, not suppressed (D-05)"** — a fixture with
   `cross_method.disagree_any == true` and `decision === :coloc` constructs successfully, and
   `cross_method(r).basis` is present.
7. **"the batch object cannot omit its denominator"** — `p14_batch_decision` without `n_total`
   throws; a valid call yields `fdr_scope === :decided_subset_only`, `n_decided + n_abstained ==
   n_total`, and `length(pi_sensitivity) == length(P14_PI_COLOC_GRID)`; `p14_headline(b)` contains
   both the α and the decided fraction.
8. **"class order survives the round trip"** — build the result from a `(0.7, 0.2, 0.1)` posterior
   and assert `class_posterior(r).coloc == 0.7`, `.random == 0.2`, `.exclusion == 0.1` as three
   separate `@test`s.
9. **"src/ is byte-unchanged (D-01)"** — the git testset for `src` and `src/results.jl`.

Close with the CPU-only testset.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_result.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_result.jl` exits 0, zero failures, under 15 seconds.
    - The file asserts `fieldnames(AmortizedColocResult)` explicitly (the no-bolted-on-fields check).
    - The file asserts `is_ood` returns `false` for `:not_checked` AND that `ood_state` distinguishes
      it — both assertions present.
    - `julia --project=spike spike/test/test_p14_decoupling.jl` exits 0.
  </acceptance_criteria>

  <done>
The result contract is enforced by a running test, including the two lossy accessors, the D-04
labelling and the batch object's required denominator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| three-valued `ood_state` → the `Bool` accessor contract | `is_ood` cannot round-trip three states; a decision routed through it silently treats NOT CHECKED as in-distribution |
| per-tile map → the FDR arithmetic | Nothing calibrated underlies a per-region claim (Phase 12 returned NO) |
| batch α → the reported number | An α without its decided fraction is trivially satisfiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Spoofing | `is_ood` collapsing `:not_checked` into `:clear` | mitigate | Documented as lossy; `ood_state` is the accessor the decision layer uses; test asserts both behaviours; source-grep asserts `decide.jl` never calls `is_ood(` (D-06) |
| T-14-24 | Repudiation | per-tile output read as an FDR-controlled call | mitigate | Field NAME is `local_map_uncontrolled`; `p14_local_map_is_controlled(r) === false` is executable and greppable; docstring cites Phase 12's NO and the `e29c846` ROADMAP correction (D-04) |
| T-14-06 | Repudiation | α quoted without the decided fraction | mitigate | `n_total` is a required keyword; `decided_fraction` and `fdr_scope = :decided_subset_only` are fields; `p14_headline` emits both in one string so they cannot be separated by copy-paste |
| T-14-25 | Tampering | a fabricated Δρ | mitigate | `delta_rho` falls through to `_iface_error` unless real draws exist; the D-05 counter-example is in the docstring (`spike/p13/result.jl:240-243` precedent) |
| T-14-10 | Tampering | productionization by stealth | mitigate | ZERO edits to `src/`; git testset in this file plus `test_p14_decoupling.jl`; the "src-shaped is not in-src" statement is in the type's docstring AND in `p14_named_limits()` (D-01) |
| T-14-19 | Tampering | class prior silently switched | mitigate | `class_prior_source` is a required field restricted to `(:measured_pi_class_masses, :caller_supplied)`; π-sensitivity over the frozen grid is REQUIRED structure |
| T-14-SC | Tampering | package installs | accept | No package fetched; only `src/` and `spike/p13/` read-only reaches |
</threat_model>

<verification>
- `julia --project=spike spike/test/test_p14_result.jl` exits 0.
- All wave 0-2 test files still exit 0 (run each individually; never `runtests.jl`).
- `git diff --exit-code HEAD -- src spike/Project.toml spike/Manifest.toml` exits 0.
</verification>

<success_criteria>
- Every honesty commitment of this phase that can be a field IS a field.
- `p14_named_limits()` makes the manuscript's limit list enumerable from code, so the report cannot
  quietly drop one.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-06-SUMMARY.md` when done. List
`p14_named_limits()` verbatim in the summary — the report consumes it.
</output>

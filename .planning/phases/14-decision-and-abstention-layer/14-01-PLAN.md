---
phase: 14-decision-and-abstention-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spike/p14/consts.jl
  - spike/test/test_p14_consts.jl
autonomous: false         # opens with a blocking decision checkpoint: the five undERIVED bars can
                          # be ruled BEFORE the freeze commit, or never
requirements: []          # ROADMAP records `Requirements: TBD` for Phase 14 — no REQ-IDs exist.
                          # Traceability runs through D-NN decisions and 14-VALIDATION.md SC rows.
decisions: [D-07]
validates: ["Seeds", "SC3-a", "SC3-b", "SC3-c", "SC3-d", "SC1-c", "SC1-d"]
execution_environment: main-working-tree-no-worktrees
user_setup: []

must_haves:
  truths:
    - "Every seed, counter, α and bar Phase 14 will be scored against exists on disk in one file, in a commit that precedes every result-producing commit (D-07)"
    - "The four SC3 bars and the coverage floor are labelled in the file itself as judgement calls that may never be relaxed after a result exists"
    - "τ is NOT in this file — it is Tier-2/inherited, loaded from the Phase-13 artifact (D-07)"
    - "α_FDR and α_conformal are two separately-named bindings that no source may derive from one another (D-07)"
    - "P14 streams are provably disjoint from every P13 and forbidden stream, including the full Philox key-word cross-product"
  artifacts:
    - path: "spike/p14/consts.jl"
      provides: "Tier-1 pre-registration guard block"
      contains: "P14_DECLARED_DEVIATIONS"
      min_lines: 180
    - path: "spike/test/test_p14_consts.jl"
      provides: "literal-value + disjointness assertions"
      contains: "_p14_forbidden"
      min_lines: 90
  key_links:
    - from: "spike/p14/consts.jl"
      to: "spike/p13/consts.jl"
      via: "guarded include, forbidden set DERIVED not retyped"
      pattern: "isdefined\\(@__MODULE__, :P13_DECLARED_DEVIATIONS\\) \\|\\| include"
    - from: "spike/test/test_p14_consts.jl"
      to: "spike/p14/consts.jl"
      via: "comment-stripped source read + literal assertions"
      pattern: "P14_CONSTS_CODE"
---

<objective>
Freeze the Phase-14 pre-registration: every seed, counter, α, sample size and bar, in ONE file,
committed BEFORE any Phase-14 result exists.

Purpose: this project has recorded **four** separate cases of an underived bar measuring something
other than what it named (`.planning/STATE.md`, `12-STAGE1-VERDICT.md` §7 families). The four SC3
bars in this phase (`skill ≥ 0.60`, `spearman ≥ 0.95`, `AUC_hard ≥ 0.80`, `ood margin ≥ 0.50`) and
the `0.20` coverage floor are **judgement calls with no derivation** (14-RESEARCH Assumptions Log
A3). The only defence available is to freeze them before a result exists, put them to the user first,
and say in the file that they are judgement calls.

Output: `spike/p14/consts.jl` (Tier-1 guard block) and `spike/test/test_p14_consts.jl`.

**This is Wave 0. No plan in any later wave may run until this plan's commit exists.**
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/bm/4.5.0/templates/summary.md

**EXECUTE ON THE MAIN WORKING TREE. NO GIT WORKTREES.** The ~50 MB gitignored
`spike/data/cache/p13/7c65a1a9…` pool does not exist in a fresh worktree and was destroyed once
already this milestone (`worktree-cleanup-destroys-gitignored-bulk-data`).
</execution_context>

<context>
@.planning/phases/14-decision-and-abstention-layer/14-CONTEXT.md
@.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md
@.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Rule the five undERIVED bars — BEFORE the freeze, or never</name>

  <read_first>
    - `14-RESEARCH.md` §C.3 Statistics S1-S4 — each bar's definition, what would falsify it, and the
      `[ASSUMED]` tag on every floor; and Assumptions Log A3, which names all five as judgement calls
      with no derivation and recommends surfacing them to the user before the freeze.
    - `14-RESEARCH.md` Open Question 1 — the recommendation this checkpoint implements.
    - `.planning/STATE.md` — the paragraph "FOR PHASE 16, RECORDED SO IT IS NOT ASSEMBLED BY A
      REFEREE INSTEAD": the four recorded cases of a bar measuring something other than what it named,
      and the licensing standard each amendment was held to.
    - `14-VALIDATION.md` "Manual-Only Verifications" row 1 — the rule that a user may overrule any of
      them, but only *before* the freeze commit, never after a result.
    - `spike/p13/consts.jl:353-365` — the iteration-allowance shape, so the user can see what the one
      pre-declared remedy in this phase actually is.
  </read_first>

  <decision>
Do you accept the five pre-registered Phase-14 bars as proposed, or do you want to set any of them
yourself?

| Constant | Proposed | Scores |
|---|---|---|
| `P14_SKILL_FLOOR` | **0.60** | SC3-a — selective skill, normalized between the random and oracle risk-coverage curves |
| `P14_SPEARMAN_FLOOR` | **0.95** | SC3-b — Spearman(coverage, selective risk) |
| `P14_COVERAGE_FLOOR` | **0.20** | SC3-b — the coverage range the Spearman is computed on |
| `P14_AUC_HARD_FLOOR` | **0.80** | SC3-c — abstention concentrates on would-have-been-wrong items |
| `P14_OOD_MARGIN_FLOOR` | **0.50** | SC3-d — abstain rate under strong misspecification minus in-distribution |
  </decision>

  <context>
**These five numbers have no derivation.** 14-RESEARCH tags every one of them `[ASSUMED]`
(Assumptions Log A3), and its Open Question 1 recommends putting them to you rather than letting an
executor freeze them silently.

**Why this checkpoint is first and blocking.** This project has recorded **four** separate cases of a
bar measuring something other than what it named — SC1g's component-vs-total, the n=2-against-271
real arm, the Wald-labelled-Wilson sizing (DEF-12-04), and the Phase-12 Stage-1 control ceiling with
no derivation anywhere in its pre-registration (`.planning/STATE.md`). The discipline that catches
these is pre-registration, and pre-registration only works in one direction: **a bar may be ruled
before the freeze commit and never after a result exists.** Once Task 1 commits
`spike/p14/consts.jl`, these values are locked for the phase, and the only authorised change is the
single pre-declared `P14_ITERATION_TRIGGER` — which concerns conformal coverage and none of these
five.

Two other frozen values ship in the same commit and are less contentious, but you may rule them too:
`P14_ALPHA_CONFORMAL = 0.10` (whose SC1-d band at n = 2000 is a coverage floor of 0.880) and
`P14_N_CAL = P14_N_EVAL = 2000` (which pins realized conformal coverage to ±0.007 at 1 sd; that
arithmetic is exact, and larger is strictly better and cheap).

**What is NOT on the table here:** τ (inherited from Phase 13 by loading, D-07), α_FDR (a user
parameter supplied per call, not a frozen constant), and the SC1-b/SC1-d bands (derived from α and n
by formula, not chosen).
  </context>

  <options>
    <option id="accept-as-proposed">
      <name>Accept all five as proposed</name>
      <pros>Matches 14-RESEARCH's recommendation; the phase proceeds immediately; the values are
      conventional for selective prediction and are labelled as judgement calls in the constants
      file, in every artifact and in the report</pros>
      <cons>Five undERIVED numbers gate three success criteria; if one turns out to measure the wrong
      thing, that is a fifth entry in a list already long enough that "we pre-registered it" no
      longer settles an argument on its own</cons>
    </option>
    <option id="rule-some">
      <name>Set one or more of them yourself</name>
      <pros>The bars become yours rather than an agent's, and this is the only moment that can
      happen</pros>
      <cons>Requires a judgement now, with no Phase-14 result to inform it — which is exactly the
      point of pre-registration, but it does mean deciding blind</cons>
    </option>
    <option id="report-only">
      <name>Freeze them as REPORTED, NOT GATED — measure and report, gate nothing</name>
      <pros>Removes the risk of a wrong bar failing the phase for the wrong reason; SC1-c is already
      treated this way, so the pattern exists</pros>
      <cons>SC3 then has no falsifiable content, and 14-VALIDATION currently marks SC3-a..d as GATED.
      This would be a deliberate weakening of the phase's success criteria and must be recorded as
      such in the ROADMAP alongside SC3</cons>
    </option>
  </options>

  <acceptance_criteria>
    - The user's ruling is recorded verbatim in `14-01-SUMMARY.md` before Task 1 writes anything.
    - Any value the user changes is used in Task 1 in place of the proposed value, and the change is
      noted in `spike/p14/consts.jl` as a USER RULING with the date.
    - If `report-only` is chosen, `14-VALIDATION.md`'s SC3-a..d rows and the ROADMAP Phase-14 SC3
      entry are updated to read REPORTED NOT GATED, and the runners in 14-11 and 14-12 report the
      statistics without asserting them.
    - Task 1 does not begin until this checkpoint is resolved.
  </acceptance_criteria>

  <resume-signal>Select: accept-as-proposed, rule-some (and give the values), or report-only</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Freeze the Tier-1 pre-registration in spike/p14/consts.jl</name>

  <files>spike/p14/consts.jl</files>

  <read_first>
    - `spike/p13/consts.jl` — THE analog. Read `:21-44` (two-tier banner + append-never-overwrite),
      `:79-110` (the guard-block sentinel rule and why a seed is the worst possible sentinel),
      `:116-218` (forbidden inventory, fresh streams, counters), `:353-365` (iteration allowance +
      trigger text), `:729-745` (the executable self-checks), `:790-872` (the Tier-2 append and the
      freeze-ordering record).
    - `.planning/phases/14-decision-and-abstention-layer/14-PATTERNS.md` §3.1 (a)-(g) and §4.1-4.5.
    - `.planning/phases/14-decision-and-abstention-layer/14-VALIDATION.md` "Wave 0 Requirements"
      (the 15 constant names) and "Manual-Only Verifications" row 1.
    - `spike/p13/run_three_way_gate.jl:159-165` — the Philox key-word cross-product assertion form.
  </read_first>

  <action>
Create `spike/p14/consts.jl` as ONE Tier-1 guard block, opened with the 19-line AGPL header copied
byte-identically from `spike/test/test_p12_decoupling.jl:1-19`.

**Guard sentinel:** `P14_DECLARED_DEVIATIONS`, declared unconditionally at the FOOT of the block.
Open with `if !isdefined(@__MODULE__, :P14_DECLARED_DEVIATIONS)`. Never guard on a seed name — record
in the banner comment that guarding on a mirrored seed name made a full-suite run skip ~104 P13
constants. Every caller uses
`isdefined(@__MODULE__, :P14_DECLARED_DEVIATIONS) || include(joinpath(@__DIR__, "consts.jl"))`.

**Banner (before the guard):** state the anti-snooping contract; state the TWO-TIER structure —
Tier 1 is this block, and **τ is DELIBERATELY ABSENT because D-07 says τ is INHERITED, loaded from
the Phase-13 artifact and never re-derived or copied here**; state that no constant is ever EDITED,
only APPENDED, so this file's git history is the audit trail. Record the path-depth reminder: from
`spike/p14/` the repo root is TWO levels up.

**Forbidden-seed derivation, DERIVED never retyped.** Guarded-include `spike/p13/consts.jl` on
`:P13_DECLARED_DEVIATIONS`, then define `_p14_forbidden_list()` as `vcat(collect(UInt64,
_p13_forbidden()), UInt64[UInt64(P13_DEV_SEED), UInt64(P13_FIX_SEED)])` and
`_p14_forbidden() = Set{UInt64}(_p14_forbidden_list())`. Comment that the P13 set is RECOMPUTED via
the include rather than copied, and that P13's own two seeds are added because P13 could not forbid
itself. Mirror `P13_REPO_SALTS` into `P14_REPO_SALTS` extended with `P13_SALT`.

**Fresh streams:**
`const P14_DEV_SEED = 0x0000_0000_0B14_DE71` ("0B14" = phase 14, "DE71" = DEV-1, mirroring P11/P13),
`const P14_FIX_SEED = 0x0000_0000_0B14_F1F7`,
`const P14_SALT = 0x5851_F42D_4C95_7F2D` (the MMIX LCG multiplier; NOT `P13_SALT` and not any of the
eight entries of `P13_REPO_SALTS` — verify against that tuple, do not assume).
`p14_rng(counter) = Philox4x(UInt64, (UInt64(P14_DEV_SEED) ⊻ P14_SALT, UInt64(counter)))` and
`p14_fix_rng(counter)` the same off `P14_FIX_SEED`. `import Random123: Philox4x` inside the block.

**Counters, one per activity:** `P14_CAL_COUNTER = 1` (conformal calibration draws),
`P14_EVAL_COUNTER = 2` (the SHARED reported evaluation set — SC1-b, SC1-d and SC3-a/b/c all read it),
`P14_OOD_COUNTER = 3` (the SC3-d misspecification arm), `P14_REAL_COUNTER = 4` (the D-03a six-TIFF
illustration), `P14_FIXTURE_COUNTER = 99` (FIXTURES ONLY — never a reported counter).

**Sample sizes:** `P14_N_CAL = 2000`, `P14_N_EVAL = 2000`, `P14_N_OOD = 1000` (per arm, matched
in-distribution vs misspecified). Carry the 14-RESEARCH §B.4 arithmetic in a comment: at n = 2000 and
α = 0.10 the realized conformal coverage has sd ≈ 0.0067.

**The two α's, and they must never be conflated (D-07):**
`const P14_ALPHA_CONFORMAL = 0.10` — the conformal miscoverage level, pre-registered HERE.
`const P14_ALPHA_FDR_GRID = (0.01, 0.05, 0.10, 0.20)` — the grid SC1-b sweeps. α_FDR itself is a
**user parameter**, not a constant of this file; add a comment saying exactly that, and saying that
`P14_ALPHA_CONFORMAL` and one α_FDR grid point happen to share the value 0.10, which is precisely why
neither may ever be assigned from the other.
`const P14_PI_COLOC_GRID = (0.05, 0.10, 0.217, 0.40, 0.70)` — the SC1-c prior-sensitivity sweep,
REPORTED NOT GATED (say so in the comment; no bar attached).

**Class-order contract, frozen here so it cannot drift:**
`const P14_CLASS_KEYS = (:coloc, :random, :exclusion)`. Comment that five different class orderings
coexist in this codebase (`three_way_probs`, `three_way_log_bf`, `ThreeWayLogBF`,
`confusion_class_order`, `pi_class_masses`), that ECE/AUC/FDR are all invariant to a *consistent*
relabelling, and that every read site must therefore be BY NAME — never `[1]`/`[2]`/`[3]`.

**The four judgement-call bars + the coverage floor.** Declare
`P14_SKILL_FLOOR = 0.60`, `P14_SPEARMAN_FLOOR = 0.95`, `P14_COVERAGE_FLOOR = 0.20`,
`P14_AUC_HARD_FLOOR = 0.80`, `P14_OOD_MARGIN_FLOOR = 0.50` — **or whatever Task 0's ruling replaced
them with** — and immediately above them a comment block stating verbatim in substance: *these five
numbers are JUDGEMENT CALLS WITH NO DERIVATION; this project has recorded four cases of an underived
bar measuring something other than what it named; they are frozen here before any result exists; they
may never be relaxed after seeing a result, and this file may not be amended to relax them.* Record
Task 0's ruling and its date in the same comment block. Also declare
`const P14_JUDGEMENT_CALL_BARS = (:P14_SKILL_FLOOR, :P14_SPEARMAN_FLOOR, :P14_COVERAGE_FLOOR,
:P14_AUC_HARD_FLOOR, :P14_OOD_MARGIN_FLOOR)` so the report can enumerate them mechanically rather
than by hand.

**Vacuity label, not a verdict** (14-RESEARCH Pitfall 9): `P14_AMBIGUOUS_RATE_FLOOR = 0.01`, with a
comment that falling below it produces a LABEL (`vacuous_hedge = true`), never a failure, following
`vacuous_pass`'s design at `spike/p13/result.jl:284-298`.

**Iteration allowance** (copy the `spike/p13/consts.jl:353-365` shape exactly):
`P14_ITERATION_ALLOWANCE = 1` plus a `P14_ITERATION_TRIGGER` string naming the ONE pre-declared
condition — realized conformal coverage falls below the SC1-d band, in which case the allowance is
spent on switching the nonconformity score from LAC to APS and re-running ONCE — and stating that it
is NEVER spent on relaxing a bar after a result, and that a second iteration is not authorised by
this file and cannot be authorised by amending this file.

**Executable self-checks at the foot of the block**, each with a message naming the breach:
P14_DEV_SEED and P14_FIX_SEED each `∉ _p14_forbidden()`; the two differ; `P14_SALT ∉ P14_REPO_SALTS`;
`length(_p14_forbidden()) == length(_p14_forbidden_list())`; the P13 recompute actually ran (assert
`UInt64(P13_DEV_SEED) in _p14_forbidden()`); `P14_FIXTURE_COUNTER ∉ (P14_CAL_COUNTER,
P14_EVAL_COUNTER, P14_OOD_COUNTER, P14_REAL_COUNTER)`; and — the Phase-14-specific one — the **full
Philox key-word cross-product**: for every `c14 ∈ (P14_CAL_COUNTER, P14_EVAL_COUNTER,
P14_OOD_COUNTER, P14_REAL_COUNTER, P14_FIXTURE_COUNTER)` and every `c13 ∈ (P13_TAU_COUNTER,
P13_DATAGEN_COUNTER, P13_GATE_COUNTER, P13_ALPHA_COUNTER, P13_CONTINUITY_COUNTER,
P13_FIXTURE_COUNTER)`, assert `P14_DEV_SEED ⊻ P14_SALT ⊻ c14 != P13_DEV_SEED ⊻ P13_SALT ⊻ c13` and
the same against `P13_FIX_SEED ⊻ P13_SALT ⊻ c13`, and against the bare `P13_DEV_SEED ⊻ P13_SALT` and
`P13_FIX_SEED ⊻ P13_SALT` key words. Also assert every P14 key word pairwise distinct within P14.
Finally `const P14_DECLARED_DEVIATIONS = (...)` naming this phase's declared deviations (at minimum:
SC1 amended by D-02, SC2 amended by D-05, τ inherited not re-derived per D-07, the D-03a substrate
substitution) and `@assert length(P14_DECLARED_DEVIATIONS) == 4`.

DO NOT write τ, a Tier-2 value, or any measured number into this file in this task.
  </action>

  <verify>
    <automated>julia --project=spike -e 'include("spike/p14/consts.jl"); @assert isdefined(Main, :P14_DECLARED_DEVIATIONS); @assert !isdefined(Main, :P14_TAU); println("tier-1 ok")'</automated>
  </verify>

  <acceptance_criteria>
    - `spike/p14/consts.jl` exists and `julia --project=spike -e 'include("spike/p14/consts.jl")'`
      exits 0 (every `@assert` in the self-check block passes).
    - The file contains, as literal text, each of: `P14_DEV_SEED`, `P14_FIX_SEED`, `P14_SALT`,
      `P14_CAL_COUNTER`, `P14_EVAL_COUNTER`, `P14_OOD_COUNTER`, `P14_FIXTURE_COUNTER`,
      `P14_ALPHA_CONFORMAL`, `P14_N_CAL`, `P14_N_EVAL`, `P14_SKILL_FLOOR`, `P14_SPEARMAN_FLOOR`,
      `P14_COVERAGE_FLOOR`, `P14_AUC_HARD_FLOOR`, `P14_OOD_MARGIN_FLOOR`,
      `P14_ITERATION_ALLOWANCE`, `P14_ITERATION_TRIGGER`, `P14_CLASS_KEYS`,
      `P14_JUDGEMENT_CALL_BARS`, `P14_DECLARED_DEVIATIONS`.
    - `grep -v '^\s*#' spike/p14/consts.jl | grep -c 'P14_TAU\|const P13_TAU'` returns 0 — τ is
      NOT defined here (D-07).
    - The guard sentinel is `P14_DECLARED_DEVIATIONS`, not a seed:
      `grep -c 'isdefined(@__MODULE__, :P14_DECLARED_DEVIATIONS)' spike/p14/consts.jl` ≥ 1.
    - `P14_SALT` is not any of the eight values in `P13_REPO_SALTS` and is not `P13_SALT` — asserted
      executably inside the file, not merely by inspection.
    - The bar values match Task 0's ruling exactly, and any user-changed value carries a
      `USER RULING` comment with the date.
    - **FREEZE ORDERING (the acceptance criterion this task exists for):** this file is committed in
      its own commit whose message begins `pre-register(14):`, and that commit contains **no
      `.jld2` artifact, no `spike/p14/run_p14_*.jl`, and no measured number**. Verify with
      `git show --stat HEAD` — the changed-file list must be `spike/p14/consts.jl` (plus, at most,
      `spike/test/test_p14_consts.jl` from Task 2). Every result-producing commit in Phase 14 must
      be a descendant of it.
  </acceptance_criteria>

  <done>
`spike/p14/consts.jl` exists, includes cleanly, self-checks pass, τ is absent, the bars match the
user's ruling, and the freeze commit precedes every other Phase-14 commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assert the freeze — spike/test/test_p14_consts.jl</name>

  <files>spike/test/test_p14_consts.jl</files>

  <read_first>
    - `spike/test/test_p13_consts.jl` — THE analog. Read `:34-50` (file layout + the
      comment-stripped module-level source read), `:54-94` (seeds fresh and disjoint; streams
      counter-separated; the draw-a-number proof of stream separation), `:150-154` (iteration
      allowance assertions).
    - `spike/p14/consts.jl` (written in Task 1).
    - `14-PATTERNS.md` §3.9 and §4.7 (every test file ends with the CPU-only testset).
    - `14-VALIDATION.md` rows "Seeds" and Test Infrastructure (per-file runs only).
  </read_first>

  <action>
Create `spike/test/test_p14_consts.jl` following the `test_p13_consts.jl` layout exactly: AGPL header
→ `using Test` → guarded include of `spike/p14/consts.jl` → source read ONCE at module level → one
outer `@testset ... verbose = true`. No simulation, no figure, no training — this gate must run in
under 10 seconds.

Read the source twice at module level as `P14_CONSTS_SRC` and the comment-stripped `P14_CONSTS_CODE`
(filter lines whose `strip` starts with `#`, rejoin with `\n`), mirroring `:44-50`.

Testsets:

1. **"the locked literals"** — assert every frozen value BY VALUE, because a changed constant is a
   changed experiment: `P14_DEV_SEED == 0x0000_0000_0B14_DE71`, `P14_FIX_SEED == 0x0000_0000_0B14_F1F7`,
   `P14_SALT == 0x5851_F42D_4C95_7F2D`, `P14_ALPHA_CONFORMAL == 0.10`, `P14_N_CAL == 2000`,
   `P14_N_EVAL == 2000`, and the five bars at **whatever values Task 0's ruling froze**,
   `P14_ALPHA_FDR_GRID == (0.01, 0.05, 0.10, 0.20)`,
   `P14_PI_COLOC_GRID == (0.05, 0.10, 0.217, 0.40, 0.70)`,
   `P14_CLASS_KEYS === (:coloc, :random, :exclusion)`.

2. **"seeds are fresh and disjoint"** — `P14_DEV_SEED ∉ _p14_forbidden()`,
   `P14_FIX_SEED ∉ _p14_forbidden()`, the two differ, `P14_SALT ∉ P14_REPO_SALTS`,
   `length(_p14_forbidden()) == length(_p14_forbidden_list())`, and pairwise BY NAME against
   `VAL_MASTER_SEED`, `NPE_MASTER_SEED`, `VAL_FIX_SEED`, `RATIO_PAIR_SEED`, `CORPUS_MASTER_SEED`,
   `DEFAULT_MASTER_SEED`, `P11_DEV_SEED`, `P13_DEV_SEED`, `P13_FIX_SEED` — one `@test` per reserved
   seed so a failure says WHICH stream was collided with. Assert the recompute path actually ran:
   `any(v -> UInt64(v) in _p14_forbidden(), values(_GC.PROD_SEED_V2))`.

3. **"streams are counter-separated"** — the draw-a-number form:
   `rand(p14_rng(P14_CAL_COUNTER), UInt64) != rand(p14_rng(P14_EVAL_COUNTER), UInt64)`;
   fixture never overlaps reported even at the same counter
   (`rand(p14_fix_rng(P14_FIXTURE_COUNTER), UInt64) != rand(p14_rng(P14_FIXTURE_COUNTER), UInt64)`);
   reproducibility (`rand(p14_rng(P14_CAL_COUNTER), UInt64) == rand(p14_rng(P14_CAL_COUNTER), UInt64)`);
   `P14_FIXTURE_COUNTER ∉` the four reported counters.

4. **"the Philox key-word cross-product is pairwise distinct vs the whole P13 family"** — loop the
   full cross-product declared in Task 1 and `@test` each pair distinct, plus a draw-a-number check
   `rand(p14_rng(P14_EVAL_COUNTER), UInt64) != rand(p13_rng(P13_GATE_COUNTER), UInt64)`.

5. **"τ is NOT Tier-1 here (D-07)"** — `@test !isdefined(@__MODULE__, :P14_TAU)` and
   `@test !occursin("P14_TAU", P14_CONSTS_CODE)`, with a comment that τ is inherited by loading, and
   that a Phase-14 literal for it is the exact divergence D-07 exists to prevent.

6. **"the two alphas are distinct bindings (D-07)"** — assert `P14_ALPHA_CONFORMAL` exists as its
   own name, and grep the comment-stripped source that neither `alpha_fdr` nor `alpha_FDR` is
   assigned from `P14_ALPHA_CONFORMAL` anywhere in this file.

7. **"the judgement-call bars are declared as such"** — `P14_JUDGEMENT_CALL_BARS` has 5 entries,
   every entry `isdefined`, and the raw (un-comment-stripped) source contains the string
   `JUDGEMENT CALL` so the label travels with the file.

8. **"the iteration allowance is pre-declared and non-trivial"** —
   `P14_ITERATION_ALLOWANCE == 1`, `P14_ITERATION_TRIGGER isa AbstractString`,
   `!isempty(strip(P14_ITERATION_TRIGGER))`, and `occursin("APS", P14_ITERATION_TRIGGER)`.

Close with the CPU-only testset:
`@test !any(id -> occursin("CUDA", id.name), keys(Base.loaded_modules))`.
  </action>

  <verify>
    <automated>julia --project=spike spike/test/test_p14_consts.jl</automated>
  </verify>

  <acceptance_criteria>
    - `julia --project=spike spike/test/test_p14_consts.jl` exits 0 with zero failures and zero
      errors, in under 10 seconds.
    - The file does NOT invoke `spike/test/runtests.jl` and is independently runnable.
    - Deliberately flipping one digit of `P14_DEV_SEED` in a scratch copy makes this file exit 1
      (the literals are genuinely asserted, not merely present); revert the scratch change.
    - `grep -c 'CUDA' spike/test/test_p14_consts.jl` ≥ 1 (the CPU-only testset is present).
  </acceptance_criteria>

  <done>
The frozen pre-registration is executably asserted; a changed seed, counter, α or bar fails this
file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration → results | Every Phase-14 number crosses this boundary; a value edited after a result is unfalsifiable from the artifact alone |
| P13 reported streams → P14 DEV streams | A shared Philox key silently reuses data a reported number already rode |
| an agent's judgement → a scientific bar | Five gating numbers with no derivation are the user's to ratify, and only before the freeze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | `spike/p14/consts.jl` | mitigate | Bars ruled by the user at Task 0 and frozen before any result; `JUDGEMENT CALL` label asserted in the source by `test_p14_consts.jl`; append-never-overwrite banner; the freeze commit contains no artifact and no measured number (acceptance criterion, Task 1) |
| T-14-03 | Information disclosure | P14 RNG streams | mitigate | `_p14_forbidden()` DERIVED from `_p13_forbidden()` by include, plus P13's own two seeds; the full Philox key-word cross-product asserted, not just the seeds |
| T-14-13 | Tampering | α_FDR vs α_conformal | mitigate | Two separately-named bindings; the shared 0.10 value called out in the source; source-grep test forbids assigning one from the other |
| T-14-14 | Repudiation | τ | mitigate | τ deliberately ABSENT from Tier 1; `test_p14_consts.jl` asserts `!isdefined(:P14_TAU)` so a copied literal fails the suite (D-07) |
| T-14-52 | Repudiation | an agent silently choosing five gating numbers | mitigate | Blocking `checkpoint:decision` at Task 0, before anything is written; the ruling recorded verbatim in the summary and as a dated comment in the constants file |
| T-14-SC | Tampering | package installs | mitigate | **No package-manager install task exists in this phase (D-02).** The enforcement is the byte-frozen `spike/Project.toml`/`Manifest.toml` and the exactly-sixteen dependency assertion restated in `test_p14_decoupling.jl` (plan 14-03). No `[ASSUMED]`/`[SUS]` package exists to gate, because nothing is fetched (14-RESEARCH "Package Legitimacy Audit": not applicable, zero packages installed) |
</threat_model>

<verification>
- `julia --project=spike spike/test/test_p14_consts.jl` exits 0.
- `git diff --exit-code HEAD -- spike/Project.toml spike/Manifest.toml` exits 0.
- `git status --porcelain -- src` is empty.
- `git log --oneline -1 -- spike/p14/consts.jl` names a commit that is an ancestor of every later
  Phase-14 commit.
</verification>

<success_criteria>
- Wave 0 is complete: the pre-registration exists, was ruled by the user, is committed, and is
  asserted.
- No result-producing code exists yet, by construction.
- The five judgement-call bars are on disk, labelled as judgement calls, before any number they will
  score.
</success_criteria>

<output>
Create `.planning/phases/14-decision-and-abstention-layer/14-01-SUMMARY.md` when done. Record the
Task 0 ruling verbatim and the freeze commit sha — later plans and the report cite that sha as the
ordering evidence.
</output>

---
phase: 03-training-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/Project.toml
  - spike/Manifest.toml
  - spike/test/runtests.jl
  - spike/test/test_data_pipeline.jl
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "`using JLD2, Random123` succeeds in the spike env"
    - "NeuralEstimators stays pinned at v0.2.1 after the new deps resolve"
    - "`spike/test/test_data_pipeline.jl` runs under the harness with placeholder gates wired in"
  artifacts:
    - path: "spike/Project.toml"
      provides: "JLD2 + Random123 declared in [deps]"
      contains: "Random123"
    - path: "spike/test/test_data_pipeline.jl"
      provides: "Wave-0 test scaffold for SC-1..SC-4 + thread-repro"
      min_lines: 30
    - path: "spike/test/runtests.jl"
      provides: "wired include of test_data_pipeline.jl + extended resolve-risk gate"
      contains: "test_data_pipeline.jl"
  key_links:
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_data_pipeline.jl"
      via: "include(joinpath(@__DIR__, \"test_data_pipeline.jl\"))"
      pattern: "include.*test_data_pipeline"
    - from: "spike/test/runtests.jl"
      to: "NeuralEstimators v0.2.1"
      via: "Pkg.dependencies UUID version assert"
      pattern: "38f6df31-6b4a-4144-b2af-7ace2da57606"
---

<objective>
Wave-0 foundation for the training-data pipeline: add the two missing dependencies (`JLD2`, `Random123`) to the isolated spike environment, re-freeze the Manifest, extend the resolve-risk gate so the new deps cannot silently downgrade the pinned NeuralEstimators v0.2.1, and scaffold the `test_data_pipeline.jl` testset (with MISSING placeholders for every SC) wired into the single `runtests.jl` gate.

Purpose: Every later wave needs JLD2 (the cache deliverable) and Random123 (counter-based seeding). The resolve-risk gate is the Phase-1 GLMakie co-resolve regression guard; it must cover the new deps before any code is written against them.
Output: Updated `spike/Project.toml` + `spike/Manifest.toml`, an extended resolve-risk assertion in `runtests.jl`, and `spike/test/test_data_pipeline.jl` scaffold.

HARD CONSTRAINT (CLAUDE.md): NOTHING under `src/` or the root `Project.toml`/`Manifest.toml` may change (decoupling baseline f581d95). All edits are spike-local.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-training-data-pipeline/03-CONTEXT.md
@.planning/phases/03-training-data-pipeline/03-RESEARCH.md
@.planning/phases/03-training-data-pipeline/03-PATTERNS.md
@.planning/phases/03-training-data-pipeline/03-VALIDATION.md
@spike/Project.toml
@spike/test/runtests.jl

<interfaces>
<!-- Exact UUIDs to add (PATTERNS.md §spike/Project.toml). StatsBase + NeuralEstimators already present. -->
JLD2      = "033835bb-8acc-5ee8-8aae-3f567f8a3819"
Random123 = "74087812-796a-5b5d-8853-05524746bad3"

<!-- runtests.jl:64 — the existing resolve-risk gate keyed by NeuralEstimators UUID: -->
Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"

<!-- runtests.jl:71 — the existing one-line include wiring that test_data_pipeline.jl mirrors: -->
include(joinpath(@__DIR__, "test_simulator.jl"))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JLD2 + Random123 to the spike env and re-freeze the Manifest</name>
  <files>spike/Project.toml, spike/Manifest.toml</files>
  <read_first>
    - spike/Project.toml (the existing alphabetical [deps] block to extend)
    - .planning/phases/03-training-data-pipeline/03-PATTERNS.md §"spike/Project.toml" (exact UUID lines) and §"spike/Manifest.toml" (Phase-2 Wave-0 resolve+refreeze pattern)
    - .planning/phases/03-training-data-pipeline/03-RESEARCH.md §"Standard Stack" / "Installation" (the resolve + re-freeze procedure)
  </read_first>
  <action>
    Add exactly two UUID lines to the `[deps]` block of `spike/Project.toml`, in alphabetical position: `JLD2 = "033835bb-8acc-5ee8-8aae-3f567f8a3819"` and `Random123 = "74087812-796a-5b5d-8853-05524746bad3"`. Then run `julia --project=spike -e 'using Pkg; Pkg.resolve(); Pkg.precompile()'` to update and re-freeze `spike/Manifest.toml`. NEVER pass `--project` pointing at the repo root and NEVER edit the root `Project.toml`/`Manifest.toml` (decoupling baseline f581d95). After resolve, confirm the resolved NeuralEstimators node is still v0.2.1 (the Phase-1 GLMakie-class co-resolve regression — if JLD2/Random123 dragged it down, stop and report rather than committing a downgraded Manifest).
  </action>
  <acceptance_criteria>
    - `julia --project=spike -e 'using JLD2, Random123; println("ok")'` prints `ok` with no resolve/precompile error
    - `julia --project=spike -e 'using Pkg; @assert Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"'` exits 0 (NeuralEstimators NOT downgraded)
    - `spike/Project.toml` `[deps]` contains both `JLD2` and `Random123`; root `Project.toml`/`Manifest.toml` unchanged (`git diff --quiet -- Project.toml Manifest.toml` is clean)
  </acceptance_criteria>
  <verify>
    <automated>julia --project=spike -e 'using JLD2, Random123, Pkg; @assert Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"; println("deps ok, NE pinned")'</automated>
  </verify>
  <done>JLD2 + Random123 load in the spike env; Manifest re-frozen; NeuralEstimators stays v0.2.1; root manifests untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the resolve-risk gate and scaffold test_data_pipeline.jl wired into runtests.jl</name>
  <files>spike/test/runtests.jl, spike/test/test_data_pipeline.jl</files>
  <read_first>
    - spike/test/runtests.jl (the resolve-risk gate at L52-65 and the include wiring at L71)
    - spike/test/test_simulator.jl:37-56 (license header, `using Test`/`using Random`, the `include(joinpath(@__DIR__, "..", "contract.jl"))` boundary, nested `@testset ... verbose = true`)
    - .planning/phases/03-training-data-pipeline/03-VALIDATION.md §"Per-Task Verification Map" (the SC-1..SC-4 + thread-repro rows this scaffold will host) and §"Wave 0 Requirements"
    - .planning/phases/03-training-data-pipeline/03-PATTERNS.md §"spike/test/test_data_pipeline.jl" + §"spike/test/runtests.jl"
  </read_first>
  <action>
    Create `spike/test/test_data_pipeline.jl`: copy the verbatim 19-line AGPL `#= … =#` license block from `spike/contract.jl:1-19`, then a `# spike/test/test_data_pipeline.jl --- Phase-3 DATA-01..03 gates` role line, then `using Test`, `using Random`. Add one top-level `@testset "Phase 3 — Training-Data Pipeline" verbose = true` containing five empty child testsets named exactly `"SC-1 generator → 128-vector"`, `"SC-2 cache round-trip / resume / invalidation"`, `"SC-3 leak-free standardization"`, `"SC-4 k-fold + reserved holdout"`, `"D-11/D-12 order/thread independence"`, each holding a single `@test_skip true` placeholder with a `# Wave N` comment naming the wave that fills it (W2, W3, W3, W3, W2 respectively). This is the Wave-0 MISSING scaffold the later waves replace.
    In `spike/test/runtests.jl`: append one line `include(joinpath(@__DIR__, "test_data_pipeline.jl"))` directly after the existing `include(... "test_simulator.jl")` at L71. Extend the resolve-risk testset (the `@testset "D-04 CPU-only ..."` block) with two added asserts that JLD2 and Random123 are now present in `Pkg.dependencies()` AND that the NeuralEstimators-pinned-v0.2.1 assert still holds with the new deps resolved — making the gate explicitly cover the Wave-0 additions.
  </action>
  <acceptance_criteria>
    - `julia --project=spike spike/test/runtests.jl` exits 0; output shows the `Phase 3 — Training-Data Pipeline` testset running with skipped placeholders (no errors)
    - `grep -c 'include.*test_data_pipeline' spike/test/runtests.jl` returns 1
    - The resolve-risk testset asserts `haskey(Pkg.dependencies-by-name, "JLD2")` and `"Random123"` AND re-asserts NeuralEstimators v0.2.1 (per VALIDATION §"After every plan wave")
    - `spike/test/test_data_pipeline.jl` opens with the verbatim AGPL block and contains all five named child testsets
  </acceptance_criteria>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <done>Single `runtests.jl` gate green; resolve-risk gate covers JLD2/Random123 + NeuralEstimators pin; test_data_pipeline.jl scaffold present with five SC placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → spike env | Resolving new deps could perturb the pinned NeuralEstimators (supply-chain / co-resolve drift). No network-served *data* enters the pipeline here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Manifest co-resolve silently downgrading NeuralEstimators | mitigate | Resolve-risk gate (UUID-keyed v0.2.1 assert) extended to cover JLD2/Random123 before any code targets them |
| T-03-02 | Tampering | accidental edit to root `Project.toml`/`Manifest.toml` or `src/` | mitigate | All edits spike-local; `git diff --quiet -- Project.toml Manifest.toml src` stays clean (standing DEMO-02 gate) |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | No npm/pip/cargo packages; JLD2/Random123 are Julia General registry (curated, CLAUDE.md-mandated) — slopcheck does not apply (RESEARCH §Package Legitimacy Audit) |
| — | Spoofing/Repudiation/Info-Disclosure/DoS/EoP | — | accept | No network, auth, secrets, or user input in an offline local env scaffold — categories N/A |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` is green (smoke + simulator + new scaffold + extended resolve-risk gate).
- `julia --project=spike -e 'using JLD2, Random123'` succeeds.
- `git diff --quiet f581d95 -- src Project.toml Manifest.toml` is clean (decoupling preserved).
</verification>

<success_criteria>
JLD2 + Random123 resolvable in the spike env with NeuralEstimators still v0.2.1; resolve-risk gate extended; `test_data_pipeline.jl` scaffold wired into the single `runtests.jl` gate; root manifests + `src/` byte-identical to baseline.
</success_criteria>

<output>
Create `.planning/phases/03-training-data-pipeline/03-01-SUMMARY.md` when done.
</output>

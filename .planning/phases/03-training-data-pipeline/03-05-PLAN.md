---
phase: 03-training-data-pipeline
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - spike/NOTES.md
autonomous: false
requirements: [DATA-02]
must_haves:
  truths:
    - "A real ≥50k generation run writes shards, reloads byte-identically, and survives resume — at scale, not just on a fixture"
    - "Thread-count independence holds across separate processes (`-t1` vs `-t auto`)"
    - "The full suite (smoke + simulator + data + resolve-risk gate) is green before phase verification"
    - "Validates the 50k default budget at real scale (D-06) and thread-count-independent reproducibility across separate processes (D-12)"
  artifacts:
    - path: "spike/NOTES.md"
      provides: "recorded real-run evidence: N, wall-clock, shard count, thread-repro result"
      contains: "50k"
  key_links:
    - from: "spike/data/generate.jl generate_cache"
      to: "spike/data/cache/<hash>/ at ≥50k scale"
      via: "the at-scale real run that the fixture tests only sample"
      pattern: "generate_cache"
---

<objective>
The DATA-02 phase gate: a real, at-scale generation run that the tiny CI fixtures only sample. The automated tests prove correctness at N≈12–40; this plan proves the 50k-default budget is actually survivable on CPU — shards write, the dataset reloads byte-identically, resume works after an interruption, and the byte-identical-across-thread-counts claim (D-11/D-12) holds across SEPARATE processes (`-t1` vs `-t auto`), not just the in-process serial==parallel check. The evidence (N, wall-clock, shard count, thread-repro result) is recorded in NOTES.md.

Purpose: VALIDATION.md lists the full 50k run as a Manual-Only verification (wall-clock + disk cost too large for CI). This is the standing pre-`/gsd:verify-work` gate from the Sampling Rate contract. It is a checkpoint because the run is long and machine-dependent and the user launches it with the appropriate `-t` setting.
Output: a real cache under `spike/data/cache/<hash>/` (gitignored bulk) + recorded evidence in `spike/NOTES.md`.

HARD CONSTRAINT: no code changes here beyond the NOTES.md evidence write; `src/` + root manifests stay byte-identical to baseline f581d95.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-training-data-pipeline/03-VALIDATION.md
@.planning/phases/03-training-data-pipeline/03-RESEARCH.md
@spike/data/generate.jl
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real ≥50k generation run + cross-process thread-repro + full-suite phase gate</name>
  <files>spike/NOTES.md</files>
  <read_first>
    - .planning/phases/03-training-data-pipeline/03-VALIDATION.md §"Manual-Only Verifications" + §"Sampling Rate" (the pre-verify-work gate)
    - .planning/phases/03-training-data-pipeline/03-RESEARCH.md §"Threads.@threads Generation (D-12) — Memory & Reproducibility" (the OOM/memory governor + cross-process repro)
    - spike/data/generate.jl (`generate_cache` signature used by the run commands)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint: Claude prepares the automatable parts and the user launches the long ≥50k run and confirms. Follow the numbered steps in `<how-to-verify>`. After the run, record the evidence (N, wall-clock, thread count, shard count, holdout size, `-t1`-vs-`-t auto` byte-identity result) in a NEW `spike/NOTES.md` section. No production code changes here beyond the NOTES.md evidence write — `src/` + root manifests stay byte-identical to baseline f581d95.
  </action>
  <what-built>
    The complete DATA-01/02/03 pipeline: the in-memory generator (Plan 02), the sharded version-guarded JLD2 cache with resume + reserved holdout (Plan 03), and the leak-free k-fold loader (Plan 04). All SC-1..SC-4 + order/thread-independence gates pass on tiny fixtures. This checkpoint confirms the pipeline at the real 50k-default budget, which is too large/slow for CI.
  </what-built>
  <how-to-verify>
    1. Launch a real generation run with threads (cost-aware imsize weighting keeps this CPU-tractable):
       `julia -t auto --project=spike -e 'include("spike/data/generate.jl"); d = generate_cache("spike/data/cache"; N=50000, master_seed=20240627, n_holdout=20); println("cache dir: ", d)'`
       Confirm: shards appear under `spike/data/cache/<hash>/`, the run completes without OOM, and it prints the cache dir. Note the wall-clock.
    2. Reload + integrity: reopen every shard and assert the column counts sum to N and `summary_min` is 128×(per-shard n); confirm `holdout.jld2` has ≥20 columns. (A one-liner that `JLD2.load`s each `shard_*.jld2` and the holdout and sums columns.)
    3. Resume: delete one shard file mid-dataset, re-run the SAME `generate_cache` command, confirm ONLY the deleted shard is regenerated and the reloaded dataset is unchanged.
    4. Cross-process thread independence (D-11/D-12): generate a small dataset (e.g. N=200) once under `julia -t1` and once under `julia -t auto` into two temp roots with the same `master_seed`, then assert the `theta` + `summary_min` matrices are byte-identical across the two runs.
    5. Full suite: `julia --project=spike spike/test/runtests.jl` is green (smoke + simulator + data + resolve-risk gate; NeuralEstimators still v0.2.1).
    6. Decoupling: `git diff --quiet f581d95 -- src Project.toml Manifest.toml` is clean.
    Record in `spike/NOTES.md` a new section: N, wall-clock, thread count used, shard count, holdout size, and the `-t1`-vs-`-t auto` byte-identity result.
  </how-to-verify>
  <acceptance_criteria>
    - A ≥50k cache exists under `spike/data/cache/<hash>/` and reloads with column counts summing to N; `holdout.jld2` ≥20 columns
    - Resume regenerates only a deleted shard; final dataset unchanged
    - `-t1` vs `-t auto` runs are byte-identical in `theta` + `summary_min` (cross-process D-11/D-12)
    - `julia --project=spike spike/test/runtests.jl` exits 0 (full suite + resolve-risk gate)
    - `git diff --quiet f581d95 -- src Project.toml Manifest.toml` clean
    - `spike/NOTES.md` records the real-run evidence (N, wall-clock, shard count, thread-repro result)
  </acceptance_criteria>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <resume-signal>Type "approved" once the ≥50k run reloads cleanly, resume + cross-process thread-repro hold, the full suite is green, and the NOTES.md evidence is recorded — or describe what failed.</resume-signal>
  <done>A ≥50k cache reloads + resumes at scale, cross-process thread-repro holds, the full suite is green, decoupling intact, and NOTES.md records the evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| at-scale generation → persistent cache | Behaviors that only surface at scale (OOM under parallel large-imsize samples, partial shards on a long-run crash) are the integrity risk this gate exercises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Availability/Integrity | OOM or crash mid-50k-run leaving partial shards | mitigate | Atomic `.tmp`+`mv` + resume-by-skip (Plan 03) exercised at scale here; cost-aware imsize weighting bounds simultaneous large allocations (RESEARCH §Threads memory bound) |
| T-03-15 | Tampering | thread-count-dependent data only detectable across processes | mitigate | Cross-process `-t1` vs `-t auto` byte-identity check (the keyed-per-index guarantee, D-11/D-12) |
| T-03-16 | Tampering | accidental `src/`/manifest mutation across a long session | mitigate | `git diff --quiet f581d95 -- src Project.toml Manifest.toml` re-checked at the gate (DEMO-02) |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | No installs; offline run only |
| — | Spoofing/Repudiation/Info-Disclosure | — | accept | Offline local generation, no network/auth/secrets — N/A |
</threat_model>

<verification>
- ≥50k cache reloads byte-identically; resume + cross-process thread-repro hold.
- `julia --project=spike spike/test/runtests.jl` green.
- `git diff --quiet f581d95 -- src Project.toml Manifest.toml` clean.
</verification>

<success_criteria>
The pipeline is proven at the real 50k-default budget (DATA-02): shards write + reload + resume at scale, thread-count independence holds across processes, the full suite is green, decoupling is intact, and the evidence is recorded in NOTES.md.
</success_criteria>

<output>
Create `.planning/phases/03-training-data-pipeline/03-05-SUMMARY.md` when done.
</output>

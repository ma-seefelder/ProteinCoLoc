---
phase: 05-validation-bundle-sbc-amortized-bf-ood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/validation/consts.jl
  - spike/validation/harness.jl
  - spike/validation/sbc.jl
  - spike/validation/figures.jl
  - spike/test/test_sbc.jl
  - spike/test/test_bf.jl
  - spike/test/test_ood.jl
  - spike/test/runtests.jl
autonomous: true
requirements: [SBC-01, SBC-02, SBC-03, SBC-04]
must_haves:
  truths:
    - "One shared θ*~π→simulate→infer harness drives SBC (and, in Wave 2, BF + OOD) off the frozen Phase-4 net"
    - "Per-parameter rank histograms are produced for all 7 θ AND a dedicated paired-draw Δρ SBC (D-01)"
    - "KS and χ² uniformity p-values, a nominal-vs-empirical coverage curve, and ECE/MCE with a traffic-light verdict are computed (SBC-02)"
    - "M, L, bin count, and all pass/fail thresholds are pre-registered in a committed consts file BEFORE any reported run (SBC-03/D-02)"
    - "The reported SBC run uses a fresh disjoint Random123 stream, VAL_MASTER_SEED ≠ NPE_MASTER_SEED (D-02)"
    - "SBC output is captioned 'calibrated under the simulator' and paired with the OOD result (SBC-04)"
  artifacts:
    - path: "spike/validation/consts.jl"
      provides: "Single pre-registered constants source (SBC + BF + OOD + VAL_MASTER_SEED)"
      contains: "VAL_MASTER_SEED"
    - path: "spike/validation/harness.jl"
      provides: "Shared draw→simulate→infer chain + load_frozen_model + disjoint Random123 stream + paired Δρ path"
      contains: "function"
    - path: "spike/validation/sbc.jl"
      provides: "Rank tables (7θ + Δρ), KS/χ² uniformity, coverage curve, ECE/MCE traffic-light"
      contains: "function sbc_ranks"
    - path: "spike/validation/figures.jl"
      provides: "CairoMakie rank-histogram, coverage, reliability/ECE traffic-light figure functions"
      contains: "function"
    - path: "spike/test/test_sbc.jl"
      provides: "SBC re-runnable gate on a small fixture"
      contains: "@testset"
  key_links:
    - from: "spike/validation/harness.jl"
      to: "spike/npe/trained_npe.jld2"
      via: "load_npe (frozen estimator/zt/θzt)"
      pattern: "load_npe"
    - from: "spike/validation/sbc.jl"
      to: "spike/validation/harness.jl"
      via: "shared draw→simulate→infer chain"
      pattern: "include.*harness"
    - from: "spike/test/runtests.jl"
      to: "spike/test/test_sbc.jl"
      via: "include wired into single gate"
      pattern: "test_sbc"
---

<objective>
Build the ONE shared θ*~π→simulate→infer harness that all three Phase-5 deliverables ride, commit the pre-registered constants BEFORE any reported run (anti-snooping contract), and deliver the SBC calibration proof: per-parameter rank histograms over all 7 θ plus a dedicated paired-draw Δρ SBC, KS/χ² uniformity, a coverage curve, and ECE/MCE with a traffic-light verdict.

Purpose: SBC is the highest-scrutiny scientific-honesty deliverable. It rides the frozen Phase-4 net (no re-fitting), reports "calibrated under the simulator" paired with the OOD result, and its M/thresholds are locked in a committed file so they cannot be tuned to the outcome.

Output: `spike/validation/consts.jl` (pre-registered constants — the single source Plans 02/03 also read), `spike/validation/harness.jl` (shared harness), `spike/validation/sbc.jl` (SBC diagnostics), `spike/validation/figures.jl` (shared CairoMakie figure surface), `spike/test/test_sbc.jl` (SBC gate) + scaffolds for `test_bf.jl`/`test_ood.jl` wired into `spike/test/runtests.jl` with an extended resolve-risk gate.
</objective>

<execution_context>
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@C:/Users/Manuel/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-CONTEXT.md
@.planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-RESEARCH.md
@.planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From spike/npe/infer.jl (the amortized read surface the harness COMPOSES — do not re-derive):
```
standardize_summary(S, zt, variant::Symbol = :min)          # apply FROZEN loader zt; mask rows 65:128 pass through
posterior_for(est, Z; N::Integer = 2000, use_gpu::Bool = false)   # 7×N draws, one pass, ONE stack (vector→d_in×1)
rho_hat(est, Z, θzt; N, use_gpu=false)                       # posterior-mean ρ_true (row 1, un-standardized)
rho_draws(est, Z, θzt; N, use_gpu=false)                    # the N ρ_true draws (row 1, un-standardized)
delta_rho(est, Zsample, Zcontrol, θzt; N, use_gpu=false)     # mean(ρs .- ρc), the MC Δρ (D-03)
interval_width(draws; probs=[0.05,0.95])                     # per-param CI width via NeuralEstimators `interval`
_summary_row_partition(variant, nrows) = Loader._row_partition(variant, nrows)
```

From spike/npe/train_npe.jl:
```
load_npe(path) -> (estimator, θzt, zt, variant, d_in, meta)  # frozen model + BOTH transforms; integrity-checks "estimator"
const NPE_MASTER_SEED = 0xC0FFEE                             # VAL_MASTER_SEED MUST differ from this
```

From spike/simulator/prior.jl:
```
sample_prior(rng::AbstractRNG) -> NamedTuple                 # SOLE prior; θ has field ρ_true (+ 6 more); 7 params total
const MU_PRIOR = Truncated(Cauchy(0.0,0.3), -1.0, 1.0)       # symmetric μ-prior
```

From spike/simulator/forward.jl:
```
simulate_pair(rng::AbstractRNG, θ; imsize::Tuple{Int,Int}=(256,256)) -> Vector{Matrix{Float64}}  # [ch1,ch2]; imsize dims ≥ 64
```

From spike/contract.jl (reaches src/ correlation UNCHANGED via read-only include):
```
build_mci(data::Vector{Matrix{Float64}}; name="sim") -> MultiChannelImage
patch_summary(mci) -> Matrix{Union{Float64,Missing}}        # 8×8 pearson (UNCHANGED src)
induced_mu(mci) -> Float64
```

From spike/data/encode.jl:
```
encode_d01(M::AbstractMatrix) -> Vector{Float64}            # 128-dim :min (rows 1:64 corr, 65:128 binary mask)
```

From spike/data/loader.jl (module Loader):
```
Loader._row_partition(variant::Symbol, nrows::Int) -> (cont_rows, mask_rows)   # :min → (1:64, 65:128)
Loader.load_fold(dir, fold; K=5, master_seed, variant=:min) -> (Ztr, θtr, Zva, θva, zt)   # train-only zt fit
```

From spike/test/test_npe.jl (the pre-registration + fast-gate-loads-fixture pattern to MIRROR):
```
const NPE_MASTER_SEED = 0xC0FFEE
const NPE_REPRO_DIR = generate_cache(mktempdir(); N=..., master_seed=..., n_holdout=...)   # small fixture idiom
# pre-registered const block committed BEFORE any reported run (test_npe.jl:70-96)
```
</interfaces>

<calibration_template>
<!-- Port pattern only — DO NOT add BayesInteractomics as a dependency. -->
Read `~/Documents/GitHub/BayesInteractomics/src/diagnostics/calibration.jl` `_bin_calibration`
(≈lines 22-72) and `types.jl` `CalibrationResult` (≈lines 227-234). Copy the ~50-line
`_bin_calibration` function + a local `CalibrationResult` struct into `spike/validation/sbc.jl`.
ECE = Σ (bin_count/total)·|pred−obs|; MCE = max gap. Map SBC PIT/coverage into the
(predicted-prob, empirical-positive) binning.
</calibration_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-registration scaffold — consts.jl + test scaffolds + runtests wiring + resolve-risk gate</name>
  <files>spike/validation/consts.jl, spike/test/test_bf.jl, spike/test/test_ood.jl, spike/test/runtests.jl</files>
  <read_first>
    - spike/test/test_npe.jl (lines 1-100: license header, guarded includes, pre-registered const block, fixture idiom to mirror EXACTLY)
    - spike/test/runtests.jl (lines 40-104: resolve-risk gate structure + the per-phase `include(...)` wiring)
    - spike/npe/train_npe.jl (lines 64-69: guarded `const NPE_MASTER_SEED`/schema declaration idiom)
    - .planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-PATTERNS.md (§spike/test/test_validation.jl + §Pre-registered consts)
  </read_first>
  <action>
    Create `spike/validation/consts.jl` as the SINGLE pre-registered constants source for the whole phase (SBC-03/D-02 anti-snooping — committed BEFORE any reported run; Plans 02/03 read from here). Copy the AGPL license header block verbatim from test_npe.jl, then a `# spike/validation/consts.jl --- Phase-5 pre-registered constants (SBC/BF/OOD)` banner stating these are locked before the reported run. Declare each constant guarded (`isdefined(@__MODULE__, :SBC_M) || const SBC_M = ...`) so re-inclusion under runtests.jl is a silent no-op. Lock these exact values: SBC_M=2000, SBC_L=999, SBC_BINS=50 (L+1=1000 divisible by 50 — Pitfall 4), SBC_KS_ALPHA=0.05, SBC_CHI2_ALPHA=0.05, SBC_ECE_GREEN=0.05, SBC_ECE_YELLOW=0.10, SBC_IMSIZE=(256,256); fixture SBC_FIX_M=50, SBC_FIX_L=99, SBC_FIX_BINS=10; BF_CORR_MIN=0.95 (D-08a), BF_LOGBF_TOL=0.5 (D-08b), BF_SWEEP_LO=-0.6, BF_SWEEP_HI=0.8, BF_SWEEP_N=25 (D-08 Δρ range); OOD_ID_QUANTILE=0.95 (~5% ID FPR, D-06), OOD_KS_EPS=0.05 (D-04 negative-control KS invariance), OOD_AUC_MIN=0.80 (positive-control separability), OOD_GRID_LEVELS=4 (D-03 magnitudes/family), OOD_PP_REPS=50; VAL_MASTER_SEED=0x5BC0FFEE (the RESERVED pre-registered stream consumed ONLY by the reported Wave-3 run) with an inline comment asserting it is DISJOINT from NPE_MASTER_SEED=0xC0FFEE (D-02 fresh stream); and VAL_FIX_SEED=0xF1F7ED (a distinct fixture-only stream so the fast per-task gates never consume the reported stream, keeping the reported run never-tuned-against). Create `spike/test/test_bf.jl` and `spike/test/test_ood.jl` as MINIMAL scaffolds (license header + `using Test` + one `@testset "Phase 5 — <BF|OOD> (Wave 2)" begin @test_skip true end`) so runtests.jl can include them before Plans 02/03 fill them. Extend `spike/test/runtests.jl`: append `include(joinpath(@__DIR__, "test_sbc.jl"))`, `include(joinpath(@__DIR__, "test_bf.jl"))`, `include(joinpath(@__DIR__, "test_ood.jl"))` after the existing test_npe include, and add a Phase-5 clause to the resolve-risk `@testset` re-asserting `Pkg.dependencies()[Base.UUID("38f6df31-6b4a-4144-b2af-7ace2da57606")].version == v"0.2.1"` AND `!haskey(Pkg.project().dependencies, "Turing")` AND that no ROC/normalizing-flow package (e.g. "ROCAnalysis", "MLJ", "NormalizingFlows") entered `Pkg.project().dependencies` (Pitfall 7 co-resolve landmine).
  </action>
  <verify>
    <automated>julia --project=spike spike/test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `spike/validation/consts.jl` defines VAL_MASTER_SEED and it is not equal to 0xC0FFEE (grep: `VAL_MASTER_SEED` present with value `0x5BC0FFEE`)
    - consts.jl declares all listed SBC_*, BF_*, OOD_* constants (grep confirms SBC_M, SBC_L, SBC_BINS, BF_CORR_MIN, BF_LOGBF_TOL, OOD_ID_QUANTILE, OOD_KS_EPS present)
    - `spike/test/runtests.jl` includes test_sbc.jl, test_bf.jl, test_ood.jl (grep: three `test_sbc|test_bf|test_ood` includes)
    - runtests.jl resolve-risk gate re-asserts NeuralEstimators v0.2.1 after Phase-5 code loads and asserts no ROC/flow/Turing package present
    - `julia --project=spike spike/test/runtests.jl` exits 0 with the new (skipped) Phase-5 testsets enumerated
  </acceptance_criteria>
  <done>consts.jl committed with all locked pre-registered values; three Phase-5 test files exist and are wired into the single runtests.jl gate; resolve-risk gate extended; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Shared harness.jl — draw→simulate→infer + frozen model + disjoint Random123 stream + paired Δρ path</name>
  <files>spike/validation/harness.jl</files>
  <read_first>
    - spike/npe/infer.jl (lines 1-162: license header, guarded includes lines 55-56, standardize_summary/posterior_for/rho_draws/delta_rho — the surface to COMPOSE, not re-derive)
    - spike/npe/train_npe.jl (lines 174-186: load_npe return tuple)
    - spike/simulator/prior.jl (sample_prior + MU_PRIOR) and spike/simulator/forward.jl (simulate_pair signature + imsize≥64 guard)
    - spike/contract.jl (build_mci, patch_summary) and spike/data/encode.jl (encode_d01)
    - spike/data/seeding.jl (the Random123 counter-based seeding idiom — mirror for the disjoint VAL stream)
    - .planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-PATTERNS.md (§harness.jl: guarded order-dependent includes + Pattern 1 frozen-stats chain)
  </read_first>
  <action>
    Create `spike/validation/harness.jl` (flat top-level functions, sibling style of infer.jl — no `module` wrapper). AGPL header + `# spike/validation/harness.jl --- Phase-5 shared harness (SBC-01, BF, OOD)` banner naming the frozen-stats / CPU-only (`use_gpu=false`) / read-only-src / disjoint-seed invariants. Guarded order-dependent includes: consts.jl, then train_npe.jl (load_npe), infer.jl (posterior_for etc.), simulator prior.jl + forward.jl, contract.jl, encode.jl — each `isdefined(@__MODULE__, :sym) || include(joinpath(@__DIR__, ...))`. Define `load_frozen_model(path=joinpath(@__DIR__,"..","npe","trained_npe.jld2"))` wrapping `load_npe` (returns the frozen estimator/zt/θzt). Define `val_rng(master_seed=VAL_MASTER_SEED)` returning a Random123 counter-based RNG on a stream DISJOINT from training/holdout (mirror seeding.jl's Philox/Threefry keying; document the disjoint counter range). Define `draw_simulate_infer(m, rng; imsize=SBC_IMSIZE, N=SBC_L)` implementing the exact Pattern-1 chain: `θ★ = sample_prior(rng)` → `mci = build_mci(simulate_pair(rng, θ★; imsize))` → `Z = standardize_summary(encode_d01(patch_summary(mci)), m.zt, :min)` (FROZEN m.zt) → `draws_std = posterior_for(m.estimator, Z; N=N, use_gpu=false)` → `draws = StatsBase.reconstruct(m.θzt, draws_std)`; return `(θ★, Z, draws)` (draws = 7×N physical θ). Define `draw_simulate_infer_paired(m, rng; imsize, N)` for D-01: draw independent θ★_s, θ★_c, simulate/encode/standardize both, and return `(θ★_s, θ★_c, ρs=rho_draws(m.estimator, Z_s, m.θzt; N), ρc=rho_draws(m.estimator, Z_c, m.θzt; N))` so consumers form `Δρ_draws = ρs .- ρc` and `Δρ★ = θ★_s.ρ_true − θ★_c.ρ_true`. Reuse rho_draws/delta_rho directly — do NOT reimplement the MC difference. Every NeuralEstimators call passes `use_gpu=false` (Pitfall 1).
  </action>
  <verify>
    <automated>julia --project=spike -e 'using Pkg; Pkg.activate("spike"); include("spike/validation/harness.jl"); m=load_frozen_model(); r=val_rng(); t=draw_simulate_infer(m, r; imsize=(64,64), N=16); @assert size(t.draws,1)==7 && size(t.draws,2)==16; p=draw_simulate_infer_paired(m, r; imsize=(64,64), N=16); @assert length(p.ρs)==16 && length(p.ρc)==16; println("harness OK")'</automated>
  </verify>
  <acceptance_criteria>
    - harness.jl defines `load_frozen_model(`, `val_rng(`, `draw_simulate_infer(`, `draw_simulate_infer_paired(` (grep confirms all four)
    - `draw_simulate_infer` returns a 7×N physical-θ matrix (un-standardized via reconstruct) and never re-fits standardization (uses m.zt/m.θzt only; grep: no `fit(ZScoreTransform` in harness.jl)
    - paired path returns ρ_true draw vectors for sample and control (length N each) for the Δρ SBC
    - every NeuralEstimators call in harness.jl passes `use_gpu = false` (grep: no bare posterior_for/sampleposterior without use_gpu=false)
    - the verify snippet prints "harness OK" and exits 0
  </acceptance_criteria>
  <done>harness.jl loads the frozen net once, threads a disjoint Random123 stream, and exposes the standard and paired draw→simulate→infer chains reused by SBC/BF/OOD; smoke check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sbc.jl + figures.jl — 7θ + Δρ ranks, KS/χ² uniformity, coverage curve, ECE/MCE traffic-light, and the SBC gate</name>
  <files>spike/validation/sbc.jl, spike/validation/figures.jl, spike/test/test_sbc.jl</files>
  <read_first>
    - spike/validation/harness.jl (this plan, Task 2 — the draw chains it consumes)
    - spike/npe/ablation.jl (the batch-loop + guarded-const + pre-registered-decision-rule shape to mirror)
    - spike/02_simulator_demo.jl (Phase-2 CairoMakie headless-save figure idiom)
    - ~/Documents/GitHub/BayesInteractomics/src/diagnostics/calibration.jl (`_bin_calibration` ≈22-72) + types.jl (`CalibrationResult` ≈227-234) — copy pattern, NOT a dependency
    - ~/Documents/GitHub/BayesInteractomics/src/diagnostics/predictive_checks.jl (`_ks_test_uniform` template)
    - .planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-RESEARCH.md (§SBC Mechanics, §SBC rank uniformity code example, Pitfall 4/6)
  </read_first>
  <behavior>
    - `sbc_ranks(m; M, L, imsize, rng)` returns an M×8 integer table (7 θ columns + 1 Δρ column); every rank ∈ 0:L
    - Under a well-calibrated net + uniform ranks, `sbc_uniformity` returns KS and χ² p-values > SBC_KS_ALPHA / SBC_CHI2_ALPHA; a deliberately biased rank vector (all zeros) returns p-values ≈ 0 (test both directions)
    - `sbc_coverage` returns empirical coverage ≈ nominal α on synthetic in-interval data (monotone, near-diagonal)
    - `_bin_calibration` on perfectly-calibrated synthetic input returns ECE ≈ 0 (< SBC_ECE_GREEN → green verdict); on maximally-miscalibrated input returns ECE near its max (red verdict)
    - The Δρ rank column is computed from paired differenced draws (D-01), NOT inherited from ρ_true ranks
  </behavior>
  <action>
    Create `spike/validation/sbc.jl` (flat, guarded include of harness.jl + consts.jl + `using HypothesisTests, Distributions, Statistics, StatsBase`). AGPL header + `# spike/validation/sbc.jl --- Phase-5 SBC (SBC-01..04)` banner. Implement `sbc_ranks(m; M=SBC_M, L=SBC_L, imsize=SBC_IMSIZE, rng=val_rng())`: preallocate `rank_table = Matrix{Int}(undef, M, 8)`; loop `i in 1:M` calling `draw_simulate_infer` for the 7 marginal ranks (`rank_table[i,p] = count(<(θ★[p]), @view draws[p,:])`) and `draw_simulate_infer_paired` for the Δρ column (`Δρ_draws = p.ρs .- p.ρc`; `Δρ★ = p.θ★_s.ρ_true - p.θ★_c.ρ_true`; `rank_table[i,8] = count(<(Δρ★), Δρ_draws)`). Implement `sbc_uniformity(ranks::AbstractVector; L=SBC_L, bins=SBC_BINS)` returning `(ks_p, chi2_p)`: `u = (ranks .+ 0.5) ./ (L+1)`; `ks_p = pvalue(ExactOneSampleKSTest(u, Uniform(0,1)))`; bin into `bins` equal-width rank bins → `chi2_p = pvalue(ChisqTest(counts))` (VERIFY the ChisqTest arg form against the installed HypothesisTests signature at implementation — A3; use the counts-vs-uniform form). Implement `sbc_coverage(m; levels, ...)` producing the nominal-vs-empirical coverage curve (fraction of draws whose θ★ falls inside the α-central credible interval; may delegate to NeuralEstimators `assess`+`coverage`). Port `_bin_calibration` + a local `CalibrationResult` struct verbatim (~50 lines) into sbc.jl and add `sbc_traffic_light(ece)` returning `:green`/`:yellow`/`:red` against SBC_ECE_GREEN/SBC_ECE_YELLOW. Add a doc-string constant/function `SBC_CAPTION` returning the exact string "calibrated under the simulator; pair with the OOD result" (SBC-04). Create `spike/validation/figures.jl` (guarded, `using CairoMakie`, headless save idiom): `plot_rank_histogram(ranks; bins)`, `plot_coverage_curve(nominal, empirical)`, `plot_reliability_ece(cal::CalibrationResult)` (with traffic-light color), and stub signatures `plot_roc(fpr, tpr, auc)` + `plot_bf_agreement(logbf_amortized, logbf_kde)` for Plans 02/03 to CALL (figures.jl is owned here so Wave-2 plans do not edit it) — each saves a PNG under `spike/validation/figures/`. Keep ALL figure generation OUT of the fast test gate. Then FILL `spike/test/test_sbc.jl` (mirror test_npe.jl fast-gate-loads-fixture): SC1 (sbc_ranks on SBC_FIX_M/SBC_FIX_L → shape M×8, all ranks ∈ 0:L), SC2 (sbc_uniformity finite p-values in [0,1] on the fixture AND p≈0 on an all-zeros biased vector; _bin_calibration ECE finite and traffic-light returns a Symbol), SC3 (assert consts pre-registered + `VAL_MASTER_SEED != NPE_MASTER_SEED`), SC4-doc (assert SBC_CAPTION mentions "under the simulator" and "OOD"). The REPORTED M=2000/L=999 run is OWNED by Wave-3 plan 05-04 (run_sbc.jl), not this fast gate; fixtures here draw from val_rng(VAL_FIX_SEED) so they never consume the reported VAL_MASTER_SEED stream.
  </action>
  <verify>
    <automated>julia --project=spike spike/test/test_sbc.jl</automated>
  </verify>
  <acceptance_criteria>
    - sbc.jl defines `function sbc_ranks(`, `function sbc_uniformity(`, `function sbc_coverage(`, `_bin_calibration`, `sbc_traffic_light(` (grep confirms)
    - test_sbc.jl asserts sbc_ranks fixture output is M×8 with all entries in 0:L (SC1)
    - test_sbc.jl contains a KS/χ² p-value assertion against the pre-registered SBC_KS_ALPHA/SBC_CHI2_ALPHA AND a biased-input assertion that p≈0 (SC2)
    - test_sbc.jl asserts `VAL_MASTER_SEED != NPE_MASTER_SEED` (SC3 anti-snooping)
    - test_sbc.jl asserts SBC_CAPTION contains "under the simulator" and "OOD" (SC4)
    - figures.jl defines plot_rank_histogram/plot_coverage_curve/plot_reliability_ece and PNG-save calls; no figure call appears inside test_sbc.jl
    - `julia --project=spike spike/test/test_sbc.jl` exits 0 (fast per-task gate); full `runtests.jl` stays green at wave-merge
  </acceptance_criteria>
  <done>SBC ranks (7θ + Δρ), KS/χ² uniformity, coverage curve, and ECE/MCE traffic-light are implemented and gated on a small fixture; the shared figure surface exists; the reported-run script uses the locked M/L; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| θ/imsize → simulate_pair | Only untrusted-shaped input; already validated at simulate_pair entry (ArgumentError on out-of-range/imsize<64) |
| JLD2 artifact → load_frozen_model | Deserializing the Phase-4 trained net from local disk |

This is an offline, CPU-only, single-user scientific validation spike. NO network, NO auth, NO external/user input, NO secrets, NO persisted untrusted data. Threat surface is minimal; no high-severity threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Silent stale/wrong frozen-stats reuse | mitigate | load_npe integrity-checks the "estimator" key; harness uses ONLY m.zt/m.θzt (frozen), never re-fits (grep gate) |
| T-05-02 | (scientific integrity) | Data-snooping / tune-until-calibrated | mitigate | M/L/thresholds pre-registered in committed consts.jl BEFORE the reported run; fresh disjoint VAL_MASTER_SEED ≠ NPE_MASTER_SEED (asserted in test_sbc.jl) |
| T-05-03 | Tampering | Corrupt/half-written JLD2 (figures/result caches) | accept | Read-only load of Phase-4 artifact this plan; no new persisted model here (Plan 02 uses the atomic .tmp+mv idiom) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase; resolve-risk gate re-asserts NeuralEstimators==0.2.1 and blocks ROC/flow/Turing entering the env (Pitfall 7) |
</threat_model>

<verification>
- `julia --project=spike spike/test/runtests.jl` exits 0 with the SBC testsets green and BF/OOD scaffolds skipped.
- consts.jl is committed (pre-registration BEFORE any reported run); `VAL_MASTER_SEED != NPE_MASTER_SEED`.
- harness.jl loads the frozen net once and never re-fits standardization.
- Resolve-risk gate confirms NeuralEstimators stays v0.2.1 and no ROC/flow/Turing package entered the env.
</verification>

<success_criteria>
- SBC-01: per-parameter rank histograms for all 7 θ AND a paired-draw Δρ SBC produced (M×8 rank table, ranks ∈ 0:L).
- SBC-02: KS + χ² uniformity p-values, a coverage curve, and ECE/MCE with a traffic-light verdict computed.
- SBC-03: M/L/thresholds pre-registered in committed consts.jl; reported run uses a fresh disjoint Random123 stream.
- SBC-04: output captioned "calibrated under the simulator" and paired with the OOD result.
- The shared harness + pre-registered consts + figure surface are in place for Plans 02/03 (Wave 2).
</success_criteria>

<output>
Create `.planning/phases/05-validation-bundle-sbc-amortized-bf-ood/05-01-SUMMARY.md` when done.
</output>
